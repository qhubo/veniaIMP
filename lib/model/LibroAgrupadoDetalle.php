<?php



/**
 * Skeleton subclass for representing a row from the 'libro_agrupado_detalle' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.6.7 on:
 *
 * 03/17/23 19:31:11
 *
 * You should add additional methods to this class to meet the
 * application requirements.  This class will only be generated as
 * long as it does not already exist in the output directory.
 *
 * @package    propel.generator.lib.model
 */
class LibroAgrupadoDetalle extends BaseLibroAgrupadoDetalle
{
      public function save(PropelPDO $con = null) {
        $empresaId = UsuarioQuery::getEmpresaSeleccionada('LibroAgrupadoDetalle');

        if ($this->isNew()) {
            if ($empresaId) {
                $this->setEmpresaId($empresaId);
            }

        }
        parent::save($con);
    }
    
       public function getDescripcion() {
           $retorna = $this->getDetalle();
           $cuenta = CuentaErpContableQuery::create()->filterByCuentaContable($this->getCuentaContable())->findOne();
           if ($cuenta) {
               $retorna=$cuenta->getNombre();
           }
           return $retorna;
       }
}
