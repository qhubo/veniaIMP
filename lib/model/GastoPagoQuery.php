<?php



/**
 * Skeleton subclass for performing query and update operations on the 'gasto_pago' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.6.7 on:
 *
 * 02/25/22 05:36:58
 *
 * You should add additional methods to this class to meet the
 * application requirements.  This class will only be generated as
 * long as it does not already exist in the output directory.
 *
 * @package    propel.generator.lib.model
 */
class GastoPagoQuery extends BaseGastoPagoQuery
{
       static public function Pago($proveedorId, $bancoId) {
              $Lista = GastoPagoQuery::create()
                 ->filterByTemporal(true)
                 ->filterByTipoPago('Cheque')
                ->filterByChequeId(null)
                ->filterByProveedorId($proveedorId)
                 ->filterByBancoId($bancoId)
                ->withColumn('sum(GastoPago.ValorTotal)', 'TotalGeneral')
                ->findOne();
        $total = 0;
        if ($Lista) {
            $total = $Lista->getTotalGeneral();
        }
        return $total;
     
        }
        
        
            public function __construct($dbName = 'propel', $modelName = 'GastoPago', $modelAlias = null) {
        parent::__construct($dbName, $modelName, $modelAlias);
        $empresa_id = UsuarioQuery::getEmpresaSeleccionada('GastoPago');
        $filtra = sfContext::getInstance()->getUser()->getAttribute('usuario', null, 'filtra_empresa');
        $filtra = true;
        if ($filtra) {
            if ($empresa_id) {
                $this->filterByEmpresaId($empresa_id);
            }
        }
    }
    
}
