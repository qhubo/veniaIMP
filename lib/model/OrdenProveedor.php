<?php



/**
 * Skeleton subclass for representing a row from the 'orden_proveedor' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.6.7 on:
 *
 * 01/11/22 01:11:36
 *
 * You should add additional methods to this class to meet the
 * application requirements.  This class will only be generated as
 * long as it does not already exist in the output directory.
 *
 * @package    propel.generator.lib.model
 */
class OrdenProveedor extends BaseOrdenProveedor
{
         public function save(PropelPDO $con = null) {
        $empresaId = UsuarioQuery::getEmpresaSeleccionada('OrdenProveedor');
       
        if ($this->isNew()) {
             if ($empresaId) {
            $this->setEmpresaId($empresaId);
        }
                      if (trim($this->getCodigo()) == ""){
            $tipo = $empresaId.'ORD';
            $pre = $empresaId.'ORD';
            $numero = 1;
            $busca = CorrelativoCodigoQuery::create()
                    ->filterByTipo($tipo)
                    ->findOne();
            if (!$busca) {
                $busca = New CorrelativoCodigo();
                $busca->setTipo($tipo);
                $busca->setPrefijo($pre);
                $busca->setNumeroAsginar($numero);
                $busca->save();
            }
            $numero = $busca->getNumeroAsginar();
            $numero = $busca->getNumeroAsginar();
            $prefijo=$pre.$numero;
            if (strlen($numero)==1){
                $prefijo=$pre.'0'.$numero;
            }
            if ($numero >99){
                $prefijo=$pre.$numero;
            }   
            $this->setCodigo($prefijo);
            $busca->setNumeroAsginar($numero + 1);
            $busca->save();
                      }
        }
  
            $this->setExcento(false);
        if (!$this->getAplicaIva()) {
            $this->setExcento(true);
        }
        
        parent::save($con);
    }
    
       public function getLineas() {
         $detalles = OrdenProveedorDetalleQuery::create()
                 ->filterByOrdenProveedorId($this->getId())
                 ->find();
         $lin ='<ul>';
         foreach ($detalles as $regis) {
             $lin.="<li>".$regis->getDetalle()."</li>";             
         }
         $lin .='</ul>';
         return $lin;              
     }
    
}
