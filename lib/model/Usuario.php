<?php

/**
 * Skeleton subclass for representing a row from the 'usuario' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.6.7 on:
 *
 * 05/02/15 04:55:15
 *
 * You should add additional methods to this class to meet the
 * application requirements.  This class will only be generated as
 * long as it does not already exist in the output directory.
 *
 * @package    propel.generator.lib.model
 */
class Usuario extends BaseUsuario {

    
       static public function formato($valor, $moneda) {
        $valor = number_format((float) ($valor), 2, '.', ',');
             if ($moneda) {
      
                $valor = "<strong>".$moneda."</strong>&nbsp;" . $valor;
           
        }
        return $valor;
    }
    public function getNombreUsuario() {

        $nombre = trim(trim($this->getNombreCompleto()) . " | " . trim($this->getUsuario()));
        return $nombre;
    }

    public function getTema() {
        $tema = $this->tema;
        if (!$this->tema) {
            $tema = '/web/kunes/assets/admin/layout/css/themes/default.css';
        }
        return $tema;
    }
              public function save(PropelPDO $con = null)
 {
   if ($this->isNew()) {
                  $empresaId = UsuarioQuery::getEmpresaSeleccionada('Usuario');
        if ($empresaId) {
            $this->setEmpresaId($empresaId);
        }
        
     $clave = $this->getClave();
     $this->setClave(sha1($clave));
   } else {
//    $clave = $this->getClave();
//    $this->setClave($clave);
   }
   
   return parent::save($con);
 }
 

}
