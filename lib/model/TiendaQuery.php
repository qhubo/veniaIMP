<?php



/**
 * Skeleton subclass for performing query and update operations on the 'tienda' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.6.7 on:
 *
 * 01/11/22 01:11:38
 *
 * You should add additional methods to this class to meet the
 * application requirements.  This class will only be generated as
 * long as it does not already exist in the output directory.
 *
 * @package    propel.generator.lib.model
 */
class TiendaQuery extends BaseTiendaQuery
{
           public function __construct($dbName = 'propel', $modelName = 'Tienda', $modelAlias = null) {
        parent::__construct($dbName, $modelName, $modelAlias);
        $empresa_id = UsuarioQuery::getEmpresaSeleccionada('Tienda');
        $filtra = sfContext::getInstance()->getUser()->getAttribute('usuario', null, 'filtra_empresa');
        $filtra=true;
        if ($filtra) {
            if ($empresa_id) {
                $this->filterByEmpresaId($empresa_id);
            }
        }
    }
 static public function TiendaActivas() {
            $usuarioId = sfContext::getInstance()->getUser()->getAttribute('usuario', null, 'seguridad');
        $usuarioQue = UsuarioQuery::create()->findOneById($usuarioId);
         $listab[] = 0;
         if ($usuarioQue) {
        $listab[] = $usuarioQue->getTiendaId();
         }
        $usuasioBodega = UsuarioTiendaQuery::create()
                ->filterByUsuarioId($usuarioId)
                ->find();
        
        foreach ($usuasioBodega as $usua) {
            $listab[]=$usua->getTiendaId();
        }
        return $listab;
     }

}
