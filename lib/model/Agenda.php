<?php



/**
 * Skeleton subclass for representing a row from the 'agenda' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.6.7 on:
 *
 * Thu Oct 12 03:12:24 2023
 *
 * You should add additional methods to this class to meet the
 * application requirements.  This class will only be generated as
 * long as it does not already exist in the output directory.
 *
 * @package    propel.generator.lib.model
 */
class Agenda extends BaseAgenda
{
    
    public function getNombreDoctor() {
        $returna='';
        $usuarioQ = UsuarioQuery::create()->findOneByUsuario($this->getDoctor());
        if ($usuarioQ) {
            $returna= $usuarioQ->getNombreCompleto();
        }
        return $returna;
    }
    
    public function getEstatusSobreFecha()
    {
        $fechaActual = date("Y-m-d");
        $fechaObj = $this->getFecha("Y-m-d");
        $citaFutura = false;
        $estatus = $this->getEstatus();
        if ($fechaActual < $fechaObj) {
            $citaFutura = true;
        }
        if ($citaFutura && $estatus == "Pendiente") {
            return 'Pendiente de Confirmación';
        }
        if ($citaFutura && $estatus == "Confirmado") {
            return 'Cita Confirmada';
        }
        if (!$citaFutura && $estatus == "Pendiente") {
            return 'No asistida';
        }
        if (!$citaFutura && $estatus == "Confirmado") {
            return 'Asistida';
        }
        return '';
    }
}
