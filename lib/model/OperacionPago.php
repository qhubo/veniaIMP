<?php



/**
 * Skeleton subclass for representing a row from the 'operacion_pago' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.6.7 on:
 *
 * 01/11/22 01:11:36
 *
 * You should add additional methods to this class to meet the
 * application requirements.  This class will only be generated as
 * long as it does not already exist in the output directory.
 *
 * @package    propel.generator.lib.model
 */
class OperacionPago extends BaseOperacionPago
{
    
    static public function Codigo($id) {
       $codigo = $id;
       if (strlen($id)==1) {
         $codigo = "000".$id;    
       }
      if (strlen($id)==2) {
         $codigo = "00".$id;    
       }
        if (strlen($id)==3) {
         $codigo = "00".$id;    
       }
       return $codigo;
    }
    public function getCodigo() {
       $codigo = $this->getId();
       if (strlen($this->getId())==1) {
         $codigo = "000".$this->getId();    
       }
      if (strlen($this->getId())==2) {
         $codigo = "00".$this->getId();    
       }
        if (strlen($this->getId())==3) {
         $codigo = "00".$this->getId();    
       }
       return $codigo;
    }
         public function save(PropelPDO $con = null) {
        $empresaId = UsuarioQuery::getEmpresaSeleccionada('OperacionPago');
       
        if ($this->isNew()) {
             if ($empresaId) {
            $this->setEmpresaId($empresaId);
        }
        }
        parent::save($con);
    }
    
    
    
}
