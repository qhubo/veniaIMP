<?php



/**
 * Skeleton subclass for representing a row from the 'libro_agrupado' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.6.7 on:
 *
 * 03/17/23 19:31:11
 *
 * You should add additional methods to this class to meet the
 * application requirements.  This class will only be generated as
 * long as it does not already exist in the output directory.
 *
 * @package    propel.generator.lib.model
 */
class LibroAgrupado extends BaseLibroAgrupado
{
          public function save(PropelPDO $con = null) {
        $empresaId = UsuarioQuery::getEmpresaSeleccionada('LibroAgrupado');

        if ($this->isNew()) {
            if ($empresaId) {
                $this->setEmpresaId($empresaId);
            }

        }
        parent::save($con);
    }
    
           public function getSaldo($fechafin) {
               $listaDetalle = LibroAgrupadoDetalleQuery::create()
                       ->filterByLibroAgrupadoId($this->getId())
                       ->find();
               $total=0;
               foreach($listaDetalle as $reg) {
                 //     $fechafin= date("Y-m-d",strtotime($fechafin."+ 1 days")); 
               $saldo = CuentaErpContablePeer::getSaldoFecha($fechafin, $reg->getCuentaContable(),false);
               $total= $total+$saldo;
               
               }
               return $total;
           }
}
