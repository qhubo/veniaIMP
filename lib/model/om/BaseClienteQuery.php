<?php


/**
 * Base class that represents a query for the 'cliente' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.6.7 on:
 *
 * 12/30/25 22:58:28
 *
 * @method ClienteQuery orderById($order = Criteria::ASC) Order by the id column
 * @method ClienteQuery orderByEmpresaId($order = Criteria::ASC) Order by the empresa_id column
 * @method ClienteQuery orderByCodigo($order = Criteria::ASC) Order by the codigo column
 * @method ClienteQuery orderByNombre($order = Criteria::ASC) Order by the nombre column
 * @method ClienteQuery orderByPequenoContribuye($order = Criteria::ASC) Order by the pequeno_contribuye column
 * @method ClienteQuery orderByRegimenIsr($order = Criteria::ASC) Order by the regimen_isr column
 * @method ClienteQuery orderByDireccion($order = Criteria::ASC) Order by the direccion column
 * @method ClienteQuery orderByTelefono($order = Criteria::ASC) Order by the telefono column
 * @method ClienteQuery orderByCorreoElectronico($order = Criteria::ASC) Order by the correo_electronico column
 * @method ClienteQuery orderByObservaciones($order = Criteria::ASC) Order by the observaciones column
 * @method ClienteQuery orderByNit($order = Criteria::ASC) Order by the nit column
 * @method ClienteQuery orderByDiasCredito($order = Criteria::ASC) Order by the dias_credito column
 * @method ClienteQuery orderByTipoReferencia($order = Criteria::ASC) Order by the tipo_referencia column
 * @method ClienteQuery orderByActivo($order = Criteria::ASC) Order by the activo column
 * @method ClienteQuery orderByTieneCredito($order = Criteria::ASC) Order by the tiene_credito column
 * @method ClienteQuery orderByAvenidaCalle($order = Criteria::ASC) Order by the avenida_calle column
 * @method ClienteQuery orderByZona($order = Criteria::ASC) Order by the zona column
 * @method ClienteQuery orderByDepartamentoId($order = Criteria::ASC) Order by the departamento_id column
 * @method ClienteQuery orderByMunicipioId($order = Criteria::ASC) Order by the municipio_id column
 * @method ClienteQuery orderByContacto($order = Criteria::ASC) Order by the contacto column
 * @method ClienteQuery orderByCorreoContacto($order = Criteria::ASC) Order by the correo_contacto column
 * @method ClienteQuery orderByImagen($order = Criteria::ASC) Order by the imagen column
 * @method ClienteQuery orderByTelefonoContacto($order = Criteria::ASC) Order by the telefono_contacto column
 * @method ClienteQuery orderByTipificacion($order = Criteria::ASC) Order by the tipificacion column
 * @method ClienteQuery orderByFecha($order = Criteria::ASC) Order by the fecha column
 * @method ClienteQuery orderByTipoProducto($order = Criteria::ASC) Order by the tipo_producto column
 * @method ClienteQuery orderByPorcentajeNegociado($order = Criteria::ASC) Order by the porcentaje_negociado column
 * @method ClienteQuery orderByLimiteCredito($order = Criteria::ASC) Order by the limite_credito column
 * @method ClienteQuery orderByPaisId($order = Criteria::ASC) Order by the pais_id column
 * @method ClienteQuery orderByNombreFacturar($order = Criteria::ASC) Order by the nombre_facturar column
 *
 * @method ClienteQuery groupById() Group by the id column
 * @method ClienteQuery groupByEmpresaId() Group by the empresa_id column
 * @method ClienteQuery groupByCodigo() Group by the codigo column
 * @method ClienteQuery groupByNombre() Group by the nombre column
 * @method ClienteQuery groupByPequenoContribuye() Group by the pequeno_contribuye column
 * @method ClienteQuery groupByRegimenIsr() Group by the regimen_isr column
 * @method ClienteQuery groupByDireccion() Group by the direccion column
 * @method ClienteQuery groupByTelefono() Group by the telefono column
 * @method ClienteQuery groupByCorreoElectronico() Group by the correo_electronico column
 * @method ClienteQuery groupByObservaciones() Group by the observaciones column
 * @method ClienteQuery groupByNit() Group by the nit column
 * @method ClienteQuery groupByDiasCredito() Group by the dias_credito column
 * @method ClienteQuery groupByTipoReferencia() Group by the tipo_referencia column
 * @method ClienteQuery groupByActivo() Group by the activo column
 * @method ClienteQuery groupByTieneCredito() Group by the tiene_credito column
 * @method ClienteQuery groupByAvenidaCalle() Group by the avenida_calle column
 * @method ClienteQuery groupByZona() Group by the zona column
 * @method ClienteQuery groupByDepartamentoId() Group by the departamento_id column
 * @method ClienteQuery groupByMunicipioId() Group by the municipio_id column
 * @method ClienteQuery groupByContacto() Group by the contacto column
 * @method ClienteQuery groupByCorreoContacto() Group by the correo_contacto column
 * @method ClienteQuery groupByImagen() Group by the imagen column
 * @method ClienteQuery groupByTelefonoContacto() Group by the telefono_contacto column
 * @method ClienteQuery groupByTipificacion() Group by the tipificacion column
 * @method ClienteQuery groupByFecha() Group by the fecha column
 * @method ClienteQuery groupByTipoProducto() Group by the tipo_producto column
 * @method ClienteQuery groupByPorcentajeNegociado() Group by the porcentaje_negociado column
 * @method ClienteQuery groupByLimiteCredito() Group by the limite_credito column
 * @method ClienteQuery groupByPaisId() Group by the pais_id column
 * @method ClienteQuery groupByNombreFacturar() Group by the nombre_facturar column
 *
 * @method ClienteQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method ClienteQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method ClienteQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method ClienteQuery leftJoinEmpresa($relationAlias = null) Adds a LEFT JOIN clause to the query using the Empresa relation
 * @method ClienteQuery rightJoinEmpresa($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Empresa relation
 * @method ClienteQuery innerJoinEmpresa($relationAlias = null) Adds a INNER JOIN clause to the query using the Empresa relation
 *
 * @method ClienteQuery leftJoinDepartamento($relationAlias = null) Adds a LEFT JOIN clause to the query using the Departamento relation
 * @method ClienteQuery rightJoinDepartamento($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Departamento relation
 * @method ClienteQuery innerJoinDepartamento($relationAlias = null) Adds a INNER JOIN clause to the query using the Departamento relation
 *
 * @method ClienteQuery leftJoinMunicipio($relationAlias = null) Adds a LEFT JOIN clause to the query using the Municipio relation
 * @method ClienteQuery rightJoinMunicipio($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Municipio relation
 * @method ClienteQuery innerJoinMunicipio($relationAlias = null) Adds a INNER JOIN clause to the query using the Municipio relation
 *
 * @method ClienteQuery leftJoinPais($relationAlias = null) Adds a LEFT JOIN clause to the query using the Pais relation
 * @method ClienteQuery rightJoinPais($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Pais relation
 * @method ClienteQuery innerJoinPais($relationAlias = null) Adds a INNER JOIN clause to the query using the Pais relation
 *
 * @method ClienteQuery leftJoinOperacion($relationAlias = null) Adds a LEFT JOIN clause to the query using the Operacion relation
 * @method ClienteQuery rightJoinOperacion($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Operacion relation
 * @method ClienteQuery innerJoinOperacion($relationAlias = null) Adds a INNER JOIN clause to the query using the Operacion relation
 *
 * @method ClienteQuery leftJoinOrdenCotizacion($relationAlias = null) Adds a LEFT JOIN clause to the query using the OrdenCotizacion relation
 * @method ClienteQuery rightJoinOrdenCotizacion($relationAlias = null) Adds a RIGHT JOIN clause to the query using the OrdenCotizacion relation
 * @method ClienteQuery innerJoinOrdenCotizacion($relationAlias = null) Adds a INNER JOIN clause to the query using the OrdenCotizacion relation
 *
 * @method ClienteQuery leftJoinNotaCredito($relationAlias = null) Adds a LEFT JOIN clause to the query using the NotaCredito relation
 * @method ClienteQuery rightJoinNotaCredito($relationAlias = null) Adds a RIGHT JOIN clause to the query using the NotaCredito relation
 * @method ClienteQuery innerJoinNotaCredito($relationAlias = null) Adds a INNER JOIN clause to the query using the NotaCredito relation
 *
 * @method ClienteQuery leftJoinAgenda($relationAlias = null) Adds a LEFT JOIN clause to the query using the Agenda relation
 * @method ClienteQuery rightJoinAgenda($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Agenda relation
 * @method ClienteQuery innerJoinAgenda($relationAlias = null) Adds a INNER JOIN clause to the query using the Agenda relation
 *
 * @method ClienteQuery leftJoinRecetario($relationAlias = null) Adds a LEFT JOIN clause to the query using the Recetario relation
 * @method ClienteQuery rightJoinRecetario($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Recetario relation
 * @method ClienteQuery innerJoinRecetario($relationAlias = null) Adds a INNER JOIN clause to the query using the Recetario relation
 *
 * @method Cliente findOne(PropelPDO $con = null) Return the first Cliente matching the query
 * @method Cliente findOneOrCreate(PropelPDO $con = null) Return the first Cliente matching the query, or a new Cliente object populated from the query conditions when no match is found
 *
 * @method Cliente findOneById(int $id) Return the first Cliente filtered by the id column
 * @method Cliente findOneByEmpresaId(int $empresa_id) Return the first Cliente filtered by the empresa_id column
 * @method Cliente findOneByCodigo(string $codigo) Return the first Cliente filtered by the codigo column
 * @method Cliente findOneByNombre(string $nombre) Return the first Cliente filtered by the nombre column
 * @method Cliente findOneByPequenoContribuye(boolean $pequeno_contribuye) Return the first Cliente filtered by the pequeno_contribuye column
 * @method Cliente findOneByRegimenIsr(boolean $regimen_isr) Return the first Cliente filtered by the regimen_isr column
 * @method Cliente findOneByDireccion(string $direccion) Return the first Cliente filtered by the direccion column
 * @method Cliente findOneByTelefono(string $telefono) Return the first Cliente filtered by the telefono column
 * @method Cliente findOneByCorreoElectronico(string $correo_electronico) Return the first Cliente filtered by the correo_electronico column
 * @method Cliente findOneByObservaciones(string $observaciones) Return the first Cliente filtered by the observaciones column
 * @method Cliente findOneByNit(string $nit) Return the first Cliente filtered by the nit column
 * @method Cliente findOneByDiasCredito(int $dias_credito) Return the first Cliente filtered by the dias_credito column
 * @method Cliente findOneByTipoReferencia(string $tipo_referencia) Return the first Cliente filtered by the tipo_referencia column
 * @method Cliente findOneByActivo(boolean $activo) Return the first Cliente filtered by the activo column
 * @method Cliente findOneByTieneCredito(boolean $tiene_credito) Return the first Cliente filtered by the tiene_credito column
 * @method Cliente findOneByAvenidaCalle(string $avenida_calle) Return the first Cliente filtered by the avenida_calle column
 * @method Cliente findOneByZona(string $zona) Return the first Cliente filtered by the zona column
 * @method Cliente findOneByDepartamentoId(int $departamento_id) Return the first Cliente filtered by the departamento_id column
 * @method Cliente findOneByMunicipioId(int $municipio_id) Return the first Cliente filtered by the municipio_id column
 * @method Cliente findOneByContacto(string $contacto) Return the first Cliente filtered by the contacto column
 * @method Cliente findOneByCorreoContacto(string $correo_contacto) Return the first Cliente filtered by the correo_contacto column
 * @method Cliente findOneByImagen(string $imagen) Return the first Cliente filtered by the imagen column
 * @method Cliente findOneByTelefonoContacto(string $telefono_contacto) Return the first Cliente filtered by the telefono_contacto column
 * @method Cliente findOneByTipificacion(int $tipificacion) Return the first Cliente filtered by the tipificacion column
 * @method Cliente findOneByFecha(string $fecha) Return the first Cliente filtered by the fecha column
 * @method Cliente findOneByTipoProducto(string $tipo_producto) Return the first Cliente filtered by the tipo_producto column
 * @method Cliente findOneByPorcentajeNegociado(double $porcentaje_negociado) Return the first Cliente filtered by the porcentaje_negociado column
 * @method Cliente findOneByLimiteCredito(double $limite_credito) Return the first Cliente filtered by the limite_credito column
 * @method Cliente findOneByPaisId(int $pais_id) Return the first Cliente filtered by the pais_id column
 * @method Cliente findOneByNombreFacturar(string $nombre_facturar) Return the first Cliente filtered by the nombre_facturar column
 *
 * @method array findById(int $id) Return Cliente objects filtered by the id column
 * @method array findByEmpresaId(int $empresa_id) Return Cliente objects filtered by the empresa_id column
 * @method array findByCodigo(string $codigo) Return Cliente objects filtered by the codigo column
 * @method array findByNombre(string $nombre) Return Cliente objects filtered by the nombre column
 * @method array findByPequenoContribuye(boolean $pequeno_contribuye) Return Cliente objects filtered by the pequeno_contribuye column
 * @method array findByRegimenIsr(boolean $regimen_isr) Return Cliente objects filtered by the regimen_isr column
 * @method array findByDireccion(string $direccion) Return Cliente objects filtered by the direccion column
 * @method array findByTelefono(string $telefono) Return Cliente objects filtered by the telefono column
 * @method array findByCorreoElectronico(string $correo_electronico) Return Cliente objects filtered by the correo_electronico column
 * @method array findByObservaciones(string $observaciones) Return Cliente objects filtered by the observaciones column
 * @method array findByNit(string $nit) Return Cliente objects filtered by the nit column
 * @method array findByDiasCredito(int $dias_credito) Return Cliente objects filtered by the dias_credito column
 * @method array findByTipoReferencia(string $tipo_referencia) Return Cliente objects filtered by the tipo_referencia column
 * @method array findByActivo(boolean $activo) Return Cliente objects filtered by the activo column
 * @method array findByTieneCredito(boolean $tiene_credito) Return Cliente objects filtered by the tiene_credito column
 * @method array findByAvenidaCalle(string $avenida_calle) Return Cliente objects filtered by the avenida_calle column
 * @method array findByZona(string $zona) Return Cliente objects filtered by the zona column
 * @method array findByDepartamentoId(int $departamento_id) Return Cliente objects filtered by the departamento_id column
 * @method array findByMunicipioId(int $municipio_id) Return Cliente objects filtered by the municipio_id column
 * @method array findByContacto(string $contacto) Return Cliente objects filtered by the contacto column
 * @method array findByCorreoContacto(string $correo_contacto) Return Cliente objects filtered by the correo_contacto column
 * @method array findByImagen(string $imagen) Return Cliente objects filtered by the imagen column
 * @method array findByTelefonoContacto(string $telefono_contacto) Return Cliente objects filtered by the telefono_contacto column
 * @method array findByTipificacion(int $tipificacion) Return Cliente objects filtered by the tipificacion column
 * @method array findByFecha(string $fecha) Return Cliente objects filtered by the fecha column
 * @method array findByTipoProducto(string $tipo_producto) Return Cliente objects filtered by the tipo_producto column
 * @method array findByPorcentajeNegociado(double $porcentaje_negociado) Return Cliente objects filtered by the porcentaje_negociado column
 * @method array findByLimiteCredito(double $limite_credito) Return Cliente objects filtered by the limite_credito column
 * @method array findByPaisId(int $pais_id) Return Cliente objects filtered by the pais_id column
 * @method array findByNombreFacturar(string $nombre_facturar) Return Cliente objects filtered by the nombre_facturar column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseClienteQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseClienteQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = 'propel', $modelName = 'Cliente', $modelAlias = null)
    {
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new ClienteQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param     ClienteQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return ClienteQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof ClienteQuery) {
            return $criteria;
        }
        $query = new ClienteQuery();
        if (null !== $modelAlias) {
            $query->setModelAlias($modelAlias);
        }
        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return   Cliente|Cliente[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = ClientePeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is alredy in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(ClientePeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return   Cliente A model object, or null if the key is not found
     * @throws   PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT `ID`, `EMPRESA_ID`, `CODIGO`, `NOMBRE`, `PEQUENO_CONTRIBUYE`, `REGIMEN_ISR`, `DIRECCION`, `TELEFONO`, `CORREO_ELECTRONICO`, `OBSERVACIONES`, `NIT`, `DIAS_CREDITO`, `TIPO_REFERENCIA`, `ACTIVO`, `TIENE_CREDITO`, `AVENIDA_CALLE`, `ZONA`, `DEPARTAMENTO_ID`, `MUNICIPIO_ID`, `CONTACTO`, `CORREO_CONTACTO`, `IMAGEN`, `TELEFONO_CONTACTO`, `TIPIFICACION`, `FECHA`, `TIPO_PRODUCTO`, `PORCENTAJE_NEGOCIADO`, `LIMITE_CREDITO`, `PAIS_ID`, `NOMBRE_FACTURAR` FROM `cliente` WHERE `ID` = :p0';
        try {
            $stmt = $con->prepare($sql);
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new Cliente();
            $obj->hydrate($row);
            ClientePeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return Cliente|Cliente[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|Cliente[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return ClienteQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(ClientePeer::ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return ClienteQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(ClientePeer::ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the id column
     *
     * Example usage:
     * <code>
     * $query->filterById(1234); // WHERE id = 1234
     * $query->filterById(array(12, 34)); // WHERE id IN (12, 34)
     * $query->filterById(array('min' => 12)); // WHERE id > 12
     * </code>
     *
     * @param     mixed $id The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ClienteQuery The current query, for fluid interface
     */
    public function filterById($id = null, $comparison = null)
    {
        if (is_array($id) && null === $comparison) {
            $comparison = Criteria::IN;
        }

        return $this->addUsingAlias(ClientePeer::ID, $id, $comparison);
    }

    /**
     * Filter the query on the empresa_id column
     *
     * Example usage:
     * <code>
     * $query->filterByEmpresaId(1234); // WHERE empresa_id = 1234
     * $query->filterByEmpresaId(array(12, 34)); // WHERE empresa_id IN (12, 34)
     * $query->filterByEmpresaId(array('min' => 12)); // WHERE empresa_id > 12
     * </code>
     *
     * @see       filterByEmpresa()
     *
     * @param     mixed $empresaId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ClienteQuery The current query, for fluid interface
     */
    public function filterByEmpresaId($empresaId = null, $comparison = null)
    {
        if (is_array($empresaId)) {
            $useMinMax = false;
            if (isset($empresaId['min'])) {
                $this->addUsingAlias(ClientePeer::EMPRESA_ID, $empresaId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($empresaId['max'])) {
                $this->addUsingAlias(ClientePeer::EMPRESA_ID, $empresaId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ClientePeer::EMPRESA_ID, $empresaId, $comparison);
    }

    /**
     * Filter the query on the codigo column
     *
     * Example usage:
     * <code>
     * $query->filterByCodigo('fooValue');   // WHERE codigo = 'fooValue'
     * $query->filterByCodigo('%fooValue%'); // WHERE codigo LIKE '%fooValue%'
     * </code>
     *
     * @param     string $codigo The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ClienteQuery The current query, for fluid interface
     */
    public function filterByCodigo($codigo = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($codigo)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $codigo)) {
                $codigo = str_replace('*', '%', $codigo);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ClientePeer::CODIGO, $codigo, $comparison);
    }

    /**
     * Filter the query on the nombre column
     *
     * Example usage:
     * <code>
     * $query->filterByNombre('fooValue');   // WHERE nombre = 'fooValue'
     * $query->filterByNombre('%fooValue%'); // WHERE nombre LIKE '%fooValue%'
     * </code>
     *
     * @param     string $nombre The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ClienteQuery The current query, for fluid interface
     */
    public function filterByNombre($nombre = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($nombre)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $nombre)) {
                $nombre = str_replace('*', '%', $nombre);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ClientePeer::NOMBRE, $nombre, $comparison);
    }

    /**
     * Filter the query on the pequeno_contribuye column
     *
     * Example usage:
     * <code>
     * $query->filterByPequenoContribuye(true); // WHERE pequeno_contribuye = true
     * $query->filterByPequenoContribuye('yes'); // WHERE pequeno_contribuye = true
     * </code>
     *
     * @param     boolean|string $pequenoContribuye The value to use as filter.
     *              Non-boolean arguments are converted using the following rules:
     *                * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *                * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     *              Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ClienteQuery The current query, for fluid interface
     */
    public function filterByPequenoContribuye($pequenoContribuye = null, $comparison = null)
    {
        if (is_string($pequenoContribuye)) {
            $pequeno_contribuye = in_array(strtolower($pequenoContribuye), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
        }

        return $this->addUsingAlias(ClientePeer::PEQUENO_CONTRIBUYE, $pequenoContribuye, $comparison);
    }

    /**
     * Filter the query on the regimen_isr column
     *
     * Example usage:
     * <code>
     * $query->filterByRegimenIsr(true); // WHERE regimen_isr = true
     * $query->filterByRegimenIsr('yes'); // WHERE regimen_isr = true
     * </code>
     *
     * @param     boolean|string $regimenIsr The value to use as filter.
     *              Non-boolean arguments are converted using the following rules:
     *                * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *                * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     *              Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ClienteQuery The current query, for fluid interface
     */
    public function filterByRegimenIsr($regimenIsr = null, $comparison = null)
    {
        if (is_string($regimenIsr)) {
            $regimen_isr = in_array(strtolower($regimenIsr), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
        }

        return $this->addUsingAlias(ClientePeer::REGIMEN_ISR, $regimenIsr, $comparison);
    }

    /**
     * Filter the query on the direccion column
     *
     * Example usage:
     * <code>
     * $query->filterByDireccion('fooValue');   // WHERE direccion = 'fooValue'
     * $query->filterByDireccion('%fooValue%'); // WHERE direccion LIKE '%fooValue%'
     * </code>
     *
     * @param     string $direccion The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ClienteQuery The current query, for fluid interface
     */
    public function filterByDireccion($direccion = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($direccion)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $direccion)) {
                $direccion = str_replace('*', '%', $direccion);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ClientePeer::DIRECCION, $direccion, $comparison);
    }

    /**
     * Filter the query on the telefono column
     *
     * Example usage:
     * <code>
     * $query->filterByTelefono('fooValue');   // WHERE telefono = 'fooValue'
     * $query->filterByTelefono('%fooValue%'); // WHERE telefono LIKE '%fooValue%'
     * </code>
     *
     * @param     string $telefono The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ClienteQuery The current query, for fluid interface
     */
    public function filterByTelefono($telefono = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($telefono)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $telefono)) {
                $telefono = str_replace('*', '%', $telefono);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ClientePeer::TELEFONO, $telefono, $comparison);
    }

    /**
     * Filter the query on the correo_electronico column
     *
     * Example usage:
     * <code>
     * $query->filterByCorreoElectronico('fooValue');   // WHERE correo_electronico = 'fooValue'
     * $query->filterByCorreoElectronico('%fooValue%'); // WHERE correo_electronico LIKE '%fooValue%'
     * </code>
     *
     * @param     string $correoElectronico The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ClienteQuery The current query, for fluid interface
     */
    public function filterByCorreoElectronico($correoElectronico = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($correoElectronico)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $correoElectronico)) {
                $correoElectronico = str_replace('*', '%', $correoElectronico);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ClientePeer::CORREO_ELECTRONICO, $correoElectronico, $comparison);
    }

    /**
     * Filter the query on the observaciones column
     *
     * Example usage:
     * <code>
     * $query->filterByObservaciones('fooValue');   // WHERE observaciones = 'fooValue'
     * $query->filterByObservaciones('%fooValue%'); // WHERE observaciones LIKE '%fooValue%'
     * </code>
     *
     * @param     string $observaciones The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ClienteQuery The current query, for fluid interface
     */
    public function filterByObservaciones($observaciones = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($observaciones)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $observaciones)) {
                $observaciones = str_replace('*', '%', $observaciones);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ClientePeer::OBSERVACIONES, $observaciones, $comparison);
    }

    /**
     * Filter the query on the nit column
     *
     * Example usage:
     * <code>
     * $query->filterByNit('fooValue');   // WHERE nit = 'fooValue'
     * $query->filterByNit('%fooValue%'); // WHERE nit LIKE '%fooValue%'
     * </code>
     *
     * @param     string $nit The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ClienteQuery The current query, for fluid interface
     */
    public function filterByNit($nit = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($nit)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $nit)) {
                $nit = str_replace('*', '%', $nit);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ClientePeer::NIT, $nit, $comparison);
    }

    /**
     * Filter the query on the dias_credito column
     *
     * Example usage:
     * <code>
     * $query->filterByDiasCredito(1234); // WHERE dias_credito = 1234
     * $query->filterByDiasCredito(array(12, 34)); // WHERE dias_credito IN (12, 34)
     * $query->filterByDiasCredito(array('min' => 12)); // WHERE dias_credito > 12
     * </code>
     *
     * @param     mixed $diasCredito The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ClienteQuery The current query, for fluid interface
     */
    public function filterByDiasCredito($diasCredito = null, $comparison = null)
    {
        if (is_array($diasCredito)) {
            $useMinMax = false;
            if (isset($diasCredito['min'])) {
                $this->addUsingAlias(ClientePeer::DIAS_CREDITO, $diasCredito['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($diasCredito['max'])) {
                $this->addUsingAlias(ClientePeer::DIAS_CREDITO, $diasCredito['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ClientePeer::DIAS_CREDITO, $diasCredito, $comparison);
    }

    /**
     * Filter the query on the tipo_referencia column
     *
     * Example usage:
     * <code>
     * $query->filterByTipoReferencia('fooValue');   // WHERE tipo_referencia = 'fooValue'
     * $query->filterByTipoReferencia('%fooValue%'); // WHERE tipo_referencia LIKE '%fooValue%'
     * </code>
     *
     * @param     string $tipoReferencia The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ClienteQuery The current query, for fluid interface
     */
    public function filterByTipoReferencia($tipoReferencia = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($tipoReferencia)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $tipoReferencia)) {
                $tipoReferencia = str_replace('*', '%', $tipoReferencia);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ClientePeer::TIPO_REFERENCIA, $tipoReferencia, $comparison);
    }

    /**
     * Filter the query on the activo column
     *
     * Example usage:
     * <code>
     * $query->filterByActivo(true); // WHERE activo = true
     * $query->filterByActivo('yes'); // WHERE activo = true
     * </code>
     *
     * @param     boolean|string $activo The value to use as filter.
     *              Non-boolean arguments are converted using the following rules:
     *                * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *                * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     *              Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ClienteQuery The current query, for fluid interface
     */
    public function filterByActivo($activo = null, $comparison = null)
    {
        if (is_string($activo)) {
            $activo = in_array(strtolower($activo), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
        }

        return $this->addUsingAlias(ClientePeer::ACTIVO, $activo, $comparison);
    }

    /**
     * Filter the query on the tiene_credito column
     *
     * Example usage:
     * <code>
     * $query->filterByTieneCredito(true); // WHERE tiene_credito = true
     * $query->filterByTieneCredito('yes'); // WHERE tiene_credito = true
     * </code>
     *
     * @param     boolean|string $tieneCredito The value to use as filter.
     *              Non-boolean arguments are converted using the following rules:
     *                * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *                * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     *              Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ClienteQuery The current query, for fluid interface
     */
    public function filterByTieneCredito($tieneCredito = null, $comparison = null)
    {
        if (is_string($tieneCredito)) {
            $tiene_credito = in_array(strtolower($tieneCredito), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
        }

        return $this->addUsingAlias(ClientePeer::TIENE_CREDITO, $tieneCredito, $comparison);
    }

    /**
     * Filter the query on the avenida_calle column
     *
     * Example usage:
     * <code>
     * $query->filterByAvenidaCalle('fooValue');   // WHERE avenida_calle = 'fooValue'
     * $query->filterByAvenidaCalle('%fooValue%'); // WHERE avenida_calle LIKE '%fooValue%'
     * </code>
     *
     * @param     string $avenidaCalle The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ClienteQuery The current query, for fluid interface
     */
    public function filterByAvenidaCalle($avenidaCalle = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($avenidaCalle)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $avenidaCalle)) {
                $avenidaCalle = str_replace('*', '%', $avenidaCalle);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ClientePeer::AVENIDA_CALLE, $avenidaCalle, $comparison);
    }

    /**
     * Filter the query on the zona column
     *
     * Example usage:
     * <code>
     * $query->filterByZona('fooValue');   // WHERE zona = 'fooValue'
     * $query->filterByZona('%fooValue%'); // WHERE zona LIKE '%fooValue%'
     * </code>
     *
     * @param     string $zona The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ClienteQuery The current query, for fluid interface
     */
    public function filterByZona($zona = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($zona)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $zona)) {
                $zona = str_replace('*', '%', $zona);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ClientePeer::ZONA, $zona, $comparison);
    }

    /**
     * Filter the query on the departamento_id column
     *
     * Example usage:
     * <code>
     * $query->filterByDepartamentoId(1234); // WHERE departamento_id = 1234
     * $query->filterByDepartamentoId(array(12, 34)); // WHERE departamento_id IN (12, 34)
     * $query->filterByDepartamentoId(array('min' => 12)); // WHERE departamento_id > 12
     * </code>
     *
     * @see       filterByDepartamento()
     *
     * @param     mixed $departamentoId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ClienteQuery The current query, for fluid interface
     */
    public function filterByDepartamentoId($departamentoId = null, $comparison = null)
    {
        if (is_array($departamentoId)) {
            $useMinMax = false;
            if (isset($departamentoId['min'])) {
                $this->addUsingAlias(ClientePeer::DEPARTAMENTO_ID, $departamentoId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($departamentoId['max'])) {
                $this->addUsingAlias(ClientePeer::DEPARTAMENTO_ID, $departamentoId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ClientePeer::DEPARTAMENTO_ID, $departamentoId, $comparison);
    }

    /**
     * Filter the query on the municipio_id column
     *
     * Example usage:
     * <code>
     * $query->filterByMunicipioId(1234); // WHERE municipio_id = 1234
     * $query->filterByMunicipioId(array(12, 34)); // WHERE municipio_id IN (12, 34)
     * $query->filterByMunicipioId(array('min' => 12)); // WHERE municipio_id > 12
     * </code>
     *
     * @see       filterByMunicipio()
     *
     * @param     mixed $municipioId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ClienteQuery The current query, for fluid interface
     */
    public function filterByMunicipioId($municipioId = null, $comparison = null)
    {
        if (is_array($municipioId)) {
            $useMinMax = false;
            if (isset($municipioId['min'])) {
                $this->addUsingAlias(ClientePeer::MUNICIPIO_ID, $municipioId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($municipioId['max'])) {
                $this->addUsingAlias(ClientePeer::MUNICIPIO_ID, $municipioId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ClientePeer::MUNICIPIO_ID, $municipioId, $comparison);
    }

    /**
     * Filter the query on the contacto column
     *
     * Example usage:
     * <code>
     * $query->filterByContacto('fooValue');   // WHERE contacto = 'fooValue'
     * $query->filterByContacto('%fooValue%'); // WHERE contacto LIKE '%fooValue%'
     * </code>
     *
     * @param     string $contacto The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ClienteQuery The current query, for fluid interface
     */
    public function filterByContacto($contacto = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($contacto)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $contacto)) {
                $contacto = str_replace('*', '%', $contacto);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ClientePeer::CONTACTO, $contacto, $comparison);
    }

    /**
     * Filter the query on the correo_contacto column
     *
     * Example usage:
     * <code>
     * $query->filterByCorreoContacto('fooValue');   // WHERE correo_contacto = 'fooValue'
     * $query->filterByCorreoContacto('%fooValue%'); // WHERE correo_contacto LIKE '%fooValue%'
     * </code>
     *
     * @param     string $correoContacto The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ClienteQuery The current query, for fluid interface
     */
    public function filterByCorreoContacto($correoContacto = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($correoContacto)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $correoContacto)) {
                $correoContacto = str_replace('*', '%', $correoContacto);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ClientePeer::CORREO_CONTACTO, $correoContacto, $comparison);
    }

    /**
     * Filter the query on the imagen column
     *
     * Example usage:
     * <code>
     * $query->filterByImagen('fooValue');   // WHERE imagen = 'fooValue'
     * $query->filterByImagen('%fooValue%'); // WHERE imagen LIKE '%fooValue%'
     * </code>
     *
     * @param     string $imagen The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ClienteQuery The current query, for fluid interface
     */
    public function filterByImagen($imagen = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($imagen)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $imagen)) {
                $imagen = str_replace('*', '%', $imagen);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ClientePeer::IMAGEN, $imagen, $comparison);
    }

    /**
     * Filter the query on the telefono_contacto column
     *
     * Example usage:
     * <code>
     * $query->filterByTelefonoContacto('fooValue');   // WHERE telefono_contacto = 'fooValue'
     * $query->filterByTelefonoContacto('%fooValue%'); // WHERE telefono_contacto LIKE '%fooValue%'
     * </code>
     *
     * @param     string $telefonoContacto The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ClienteQuery The current query, for fluid interface
     */
    public function filterByTelefonoContacto($telefonoContacto = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($telefonoContacto)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $telefonoContacto)) {
                $telefonoContacto = str_replace('*', '%', $telefonoContacto);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ClientePeer::TELEFONO_CONTACTO, $telefonoContacto, $comparison);
    }

    /**
     * Filter the query on the tipificacion column
     *
     * Example usage:
     * <code>
     * $query->filterByTipificacion(1234); // WHERE tipificacion = 1234
     * $query->filterByTipificacion(array(12, 34)); // WHERE tipificacion IN (12, 34)
     * $query->filterByTipificacion(array('min' => 12)); // WHERE tipificacion > 12
     * </code>
     *
     * @param     mixed $tipificacion The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ClienteQuery The current query, for fluid interface
     */
    public function filterByTipificacion($tipificacion = null, $comparison = null)
    {
        if (is_array($tipificacion)) {
            $useMinMax = false;
            if (isset($tipificacion['min'])) {
                $this->addUsingAlias(ClientePeer::TIPIFICACION, $tipificacion['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($tipificacion['max'])) {
                $this->addUsingAlias(ClientePeer::TIPIFICACION, $tipificacion['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ClientePeer::TIPIFICACION, $tipificacion, $comparison);
    }

    /**
     * Filter the query on the fecha column
     *
     * Example usage:
     * <code>
     * $query->filterByFecha('2011-03-14'); // WHERE fecha = '2011-03-14'
     * $query->filterByFecha('now'); // WHERE fecha = '2011-03-14'
     * $query->filterByFecha(array('max' => 'yesterday')); // WHERE fecha > '2011-03-13'
     * </code>
     *
     * @param     mixed $fecha The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ClienteQuery The current query, for fluid interface
     */
    public function filterByFecha($fecha = null, $comparison = null)
    {
        if (is_array($fecha)) {
            $useMinMax = false;
            if (isset($fecha['min'])) {
                $this->addUsingAlias(ClientePeer::FECHA, $fecha['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($fecha['max'])) {
                $this->addUsingAlias(ClientePeer::FECHA, $fecha['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ClientePeer::FECHA, $fecha, $comparison);
    }

    /**
     * Filter the query on the tipo_producto column
     *
     * Example usage:
     * <code>
     * $query->filterByTipoProducto('fooValue');   // WHERE tipo_producto = 'fooValue'
     * $query->filterByTipoProducto('%fooValue%'); // WHERE tipo_producto LIKE '%fooValue%'
     * </code>
     *
     * @param     string $tipoProducto The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ClienteQuery The current query, for fluid interface
     */
    public function filterByTipoProducto($tipoProducto = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($tipoProducto)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $tipoProducto)) {
                $tipoProducto = str_replace('*', '%', $tipoProducto);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ClientePeer::TIPO_PRODUCTO, $tipoProducto, $comparison);
    }

    /**
     * Filter the query on the porcentaje_negociado column
     *
     * Example usage:
     * <code>
     * $query->filterByPorcentajeNegociado(1234); // WHERE porcentaje_negociado = 1234
     * $query->filterByPorcentajeNegociado(array(12, 34)); // WHERE porcentaje_negociado IN (12, 34)
     * $query->filterByPorcentajeNegociado(array('min' => 12)); // WHERE porcentaje_negociado > 12
     * </code>
     *
     * @param     mixed $porcentajeNegociado The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ClienteQuery The current query, for fluid interface
     */
    public function filterByPorcentajeNegociado($porcentajeNegociado = null, $comparison = null)
    {
        if (is_array($porcentajeNegociado)) {
            $useMinMax = false;
            if (isset($porcentajeNegociado['min'])) {
                $this->addUsingAlias(ClientePeer::PORCENTAJE_NEGOCIADO, $porcentajeNegociado['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($porcentajeNegociado['max'])) {
                $this->addUsingAlias(ClientePeer::PORCENTAJE_NEGOCIADO, $porcentajeNegociado['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ClientePeer::PORCENTAJE_NEGOCIADO, $porcentajeNegociado, $comparison);
    }

    /**
     * Filter the query on the limite_credito column
     *
     * Example usage:
     * <code>
     * $query->filterByLimiteCredito(1234); // WHERE limite_credito = 1234
     * $query->filterByLimiteCredito(array(12, 34)); // WHERE limite_credito IN (12, 34)
     * $query->filterByLimiteCredito(array('min' => 12)); // WHERE limite_credito > 12
     * </code>
     *
     * @param     mixed $limiteCredito The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ClienteQuery The current query, for fluid interface
     */
    public function filterByLimiteCredito($limiteCredito = null, $comparison = null)
    {
        if (is_array($limiteCredito)) {
            $useMinMax = false;
            if (isset($limiteCredito['min'])) {
                $this->addUsingAlias(ClientePeer::LIMITE_CREDITO, $limiteCredito['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($limiteCredito['max'])) {
                $this->addUsingAlias(ClientePeer::LIMITE_CREDITO, $limiteCredito['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ClientePeer::LIMITE_CREDITO, $limiteCredito, $comparison);
    }

    /**
     * Filter the query on the pais_id column
     *
     * Example usage:
     * <code>
     * $query->filterByPaisId(1234); // WHERE pais_id = 1234
     * $query->filterByPaisId(array(12, 34)); // WHERE pais_id IN (12, 34)
     * $query->filterByPaisId(array('min' => 12)); // WHERE pais_id > 12
     * </code>
     *
     * @see       filterByPais()
     *
     * @param     mixed $paisId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ClienteQuery The current query, for fluid interface
     */
    public function filterByPaisId($paisId = null, $comparison = null)
    {
        if (is_array($paisId)) {
            $useMinMax = false;
            if (isset($paisId['min'])) {
                $this->addUsingAlias(ClientePeer::PAIS_ID, $paisId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($paisId['max'])) {
                $this->addUsingAlias(ClientePeer::PAIS_ID, $paisId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ClientePeer::PAIS_ID, $paisId, $comparison);
    }

    /**
     * Filter the query on the nombre_facturar column
     *
     * Example usage:
     * <code>
     * $query->filterByNombreFacturar('fooValue');   // WHERE nombre_facturar = 'fooValue'
     * $query->filterByNombreFacturar('%fooValue%'); // WHERE nombre_facturar LIKE '%fooValue%'
     * </code>
     *
     * @param     string $nombreFacturar The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ClienteQuery The current query, for fluid interface
     */
    public function filterByNombreFacturar($nombreFacturar = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($nombreFacturar)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $nombreFacturar)) {
                $nombreFacturar = str_replace('*', '%', $nombreFacturar);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ClientePeer::NOMBRE_FACTURAR, $nombreFacturar, $comparison);
    }

    /**
     * Filter the query by a related Empresa object
     *
     * @param   Empresa|PropelObjectCollection $empresa The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   ClienteQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByEmpresa($empresa, $comparison = null)
    {
        if ($empresa instanceof Empresa) {
            return $this
                ->addUsingAlias(ClientePeer::EMPRESA_ID, $empresa->getId(), $comparison);
        } elseif ($empresa instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(ClientePeer::EMPRESA_ID, $empresa->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByEmpresa() only accepts arguments of type Empresa or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Empresa relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ClienteQuery The current query, for fluid interface
     */
    public function joinEmpresa($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Empresa');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Empresa');
        }

        return $this;
    }

    /**
     * Use the Empresa relation Empresa object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   EmpresaQuery A secondary query class using the current class as primary query
     */
    public function useEmpresaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinEmpresa($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Empresa', 'EmpresaQuery');
    }

    /**
     * Filter the query by a related Departamento object
     *
     * @param   Departamento|PropelObjectCollection $departamento The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   ClienteQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByDepartamento($departamento, $comparison = null)
    {
        if ($departamento instanceof Departamento) {
            return $this
                ->addUsingAlias(ClientePeer::DEPARTAMENTO_ID, $departamento->getId(), $comparison);
        } elseif ($departamento instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(ClientePeer::DEPARTAMENTO_ID, $departamento->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByDepartamento() only accepts arguments of type Departamento or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Departamento relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ClienteQuery The current query, for fluid interface
     */
    public function joinDepartamento($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Departamento');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Departamento');
        }

        return $this;
    }

    /**
     * Use the Departamento relation Departamento object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   DepartamentoQuery A secondary query class using the current class as primary query
     */
    public function useDepartamentoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinDepartamento($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Departamento', 'DepartamentoQuery');
    }

    /**
     * Filter the query by a related Municipio object
     *
     * @param   Municipio|PropelObjectCollection $municipio The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   ClienteQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByMunicipio($municipio, $comparison = null)
    {
        if ($municipio instanceof Municipio) {
            return $this
                ->addUsingAlias(ClientePeer::MUNICIPIO_ID, $municipio->getId(), $comparison);
        } elseif ($municipio instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(ClientePeer::MUNICIPIO_ID, $municipio->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByMunicipio() only accepts arguments of type Municipio or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Municipio relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ClienteQuery The current query, for fluid interface
     */
    public function joinMunicipio($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Municipio');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Municipio');
        }

        return $this;
    }

    /**
     * Use the Municipio relation Municipio object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   MunicipioQuery A secondary query class using the current class as primary query
     */
    public function useMunicipioQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinMunicipio($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Municipio', 'MunicipioQuery');
    }

    /**
     * Filter the query by a related Pais object
     *
     * @param   Pais|PropelObjectCollection $pais The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   ClienteQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByPais($pais, $comparison = null)
    {
        if ($pais instanceof Pais) {
            return $this
                ->addUsingAlias(ClientePeer::PAIS_ID, $pais->getId(), $comparison);
        } elseif ($pais instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(ClientePeer::PAIS_ID, $pais->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByPais() only accepts arguments of type Pais or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Pais relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ClienteQuery The current query, for fluid interface
     */
    public function joinPais($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Pais');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Pais');
        }

        return $this;
    }

    /**
     * Use the Pais relation Pais object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   PaisQuery A secondary query class using the current class as primary query
     */
    public function usePaisQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinPais($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Pais', 'PaisQuery');
    }

    /**
     * Filter the query by a related Operacion object
     *
     * @param   Operacion|PropelObjectCollection $operacion  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   ClienteQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByOperacion($operacion, $comparison = null)
    {
        if ($operacion instanceof Operacion) {
            return $this
                ->addUsingAlias(ClientePeer::ID, $operacion->getClienteId(), $comparison);
        } elseif ($operacion instanceof PropelObjectCollection) {
            return $this
                ->useOperacionQuery()
                ->filterByPrimaryKeys($operacion->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByOperacion() only accepts arguments of type Operacion or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Operacion relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ClienteQuery The current query, for fluid interface
     */
    public function joinOperacion($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Operacion');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Operacion');
        }

        return $this;
    }

    /**
     * Use the Operacion relation Operacion object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   OperacionQuery A secondary query class using the current class as primary query
     */
    public function useOperacionQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinOperacion($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Operacion', 'OperacionQuery');
    }

    /**
     * Filter the query by a related OrdenCotizacion object
     *
     * @param   OrdenCotizacion|PropelObjectCollection $ordenCotizacion  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   ClienteQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByOrdenCotizacion($ordenCotizacion, $comparison = null)
    {
        if ($ordenCotizacion instanceof OrdenCotizacion) {
            return $this
                ->addUsingAlias(ClientePeer::ID, $ordenCotizacion->getClienteId(), $comparison);
        } elseif ($ordenCotizacion instanceof PropelObjectCollection) {
            return $this
                ->useOrdenCotizacionQuery()
                ->filterByPrimaryKeys($ordenCotizacion->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByOrdenCotizacion() only accepts arguments of type OrdenCotizacion or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the OrdenCotizacion relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ClienteQuery The current query, for fluid interface
     */
    public function joinOrdenCotizacion($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('OrdenCotizacion');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'OrdenCotizacion');
        }

        return $this;
    }

    /**
     * Use the OrdenCotizacion relation OrdenCotizacion object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   OrdenCotizacionQuery A secondary query class using the current class as primary query
     */
    public function useOrdenCotizacionQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinOrdenCotizacion($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'OrdenCotizacion', 'OrdenCotizacionQuery');
    }

    /**
     * Filter the query by a related NotaCredito object
     *
     * @param   NotaCredito|PropelObjectCollection $notaCredito  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   ClienteQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByNotaCredito($notaCredito, $comparison = null)
    {
        if ($notaCredito instanceof NotaCredito) {
            return $this
                ->addUsingAlias(ClientePeer::ID, $notaCredito->getClienteId(), $comparison);
        } elseif ($notaCredito instanceof PropelObjectCollection) {
            return $this
                ->useNotaCreditoQuery()
                ->filterByPrimaryKeys($notaCredito->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByNotaCredito() only accepts arguments of type NotaCredito or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the NotaCredito relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ClienteQuery The current query, for fluid interface
     */
    public function joinNotaCredito($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('NotaCredito');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'NotaCredito');
        }

        return $this;
    }

    /**
     * Use the NotaCredito relation NotaCredito object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   NotaCreditoQuery A secondary query class using the current class as primary query
     */
    public function useNotaCreditoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinNotaCredito($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'NotaCredito', 'NotaCreditoQuery');
    }

    /**
     * Filter the query by a related Agenda object
     *
     * @param   Agenda|PropelObjectCollection $agenda  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   ClienteQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByAgenda($agenda, $comparison = null)
    {
        if ($agenda instanceof Agenda) {
            return $this
                ->addUsingAlias(ClientePeer::ID, $agenda->getClienteId(), $comparison);
        } elseif ($agenda instanceof PropelObjectCollection) {
            return $this
                ->useAgendaQuery()
                ->filterByPrimaryKeys($agenda->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByAgenda() only accepts arguments of type Agenda or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Agenda relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ClienteQuery The current query, for fluid interface
     */
    public function joinAgenda($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Agenda');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Agenda');
        }

        return $this;
    }

    /**
     * Use the Agenda relation Agenda object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   AgendaQuery A secondary query class using the current class as primary query
     */
    public function useAgendaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinAgenda($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Agenda', 'AgendaQuery');
    }

    /**
     * Filter the query by a related Recetario object
     *
     * @param   Recetario|PropelObjectCollection $recetario  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   ClienteQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByRecetario($recetario, $comparison = null)
    {
        if ($recetario instanceof Recetario) {
            return $this
                ->addUsingAlias(ClientePeer::ID, $recetario->getClienteId(), $comparison);
        } elseif ($recetario instanceof PropelObjectCollection) {
            return $this
                ->useRecetarioQuery()
                ->filterByPrimaryKeys($recetario->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByRecetario() only accepts arguments of type Recetario or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Recetario relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ClienteQuery The current query, for fluid interface
     */
    public function joinRecetario($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Recetario');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Recetario');
        }

        return $this;
    }

    /**
     * Use the Recetario relation Recetario object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   RecetarioQuery A secondary query class using the current class as primary query
     */
    public function useRecetarioQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinRecetario($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Recetario', 'RecetarioQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   Cliente $cliente Object to remove from the list of results
     *
     * @return ClienteQuery The current query, for fluid interface
     */
    public function prune($cliente = null)
    {
        if ($cliente) {
            $this->addUsingAlias(ClientePeer::ID, $cliente->getId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}
