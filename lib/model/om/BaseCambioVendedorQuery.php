<?php


/**
 * Base class that represents a query for the 'cambio_vendedor' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.6.7 on:
 *
 * 12/30/25 15:40:28
 *
 * @method CambioVendedorQuery orderById($order = Criteria::ASC) Order by the id column
 * @method CambioVendedorQuery orderByCodigoOpera($order = Criteria::ASC) Order by the codigo_opera column
 * @method CambioVendedorQuery orderByVendedorAnterior($order = Criteria::ASC) Order by the vendedor_anterior column
 * @method CambioVendedorQuery orderByVendedorActualizado($order = Criteria::ASC) Order by the vendedor_actualizado column
 * @method CambioVendedorQuery orderByUsuario($order = Criteria::ASC) Order by the usuario column
 * @method CambioVendedorQuery orderByFecha($order = Criteria::ASC) Order by the fecha column
 *
 * @method CambioVendedorQuery groupById() Group by the id column
 * @method CambioVendedorQuery groupByCodigoOpera() Group by the codigo_opera column
 * @method CambioVendedorQuery groupByVendedorAnterior() Group by the vendedor_anterior column
 * @method CambioVendedorQuery groupByVendedorActualizado() Group by the vendedor_actualizado column
 * @method CambioVendedorQuery groupByUsuario() Group by the usuario column
 * @method CambioVendedorQuery groupByFecha() Group by the fecha column
 *
 * @method CambioVendedorQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method CambioVendedorQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method CambioVendedorQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method CambioVendedor findOne(PropelPDO $con = null) Return the first CambioVendedor matching the query
 * @method CambioVendedor findOneOrCreate(PropelPDO $con = null) Return the first CambioVendedor matching the query, or a new CambioVendedor object populated from the query conditions when no match is found
 *
 * @method CambioVendedor findOneById(int $id) Return the first CambioVendedor filtered by the id column
 * @method CambioVendedor findOneByCodigoOpera(string $codigo_opera) Return the first CambioVendedor filtered by the codigo_opera column
 * @method CambioVendedor findOneByVendedorAnterior(string $vendedor_anterior) Return the first CambioVendedor filtered by the vendedor_anterior column
 * @method CambioVendedor findOneByVendedorActualizado(string $vendedor_actualizado) Return the first CambioVendedor filtered by the vendedor_actualizado column
 * @method CambioVendedor findOneByUsuario(string $usuario) Return the first CambioVendedor filtered by the usuario column
 * @method CambioVendedor findOneByFecha(string $fecha) Return the first CambioVendedor filtered by the fecha column
 *
 * @method array findById(int $id) Return CambioVendedor objects filtered by the id column
 * @method array findByCodigoOpera(string $codigo_opera) Return CambioVendedor objects filtered by the codigo_opera column
 * @method array findByVendedorAnterior(string $vendedor_anterior) Return CambioVendedor objects filtered by the vendedor_anterior column
 * @method array findByVendedorActualizado(string $vendedor_actualizado) Return CambioVendedor objects filtered by the vendedor_actualizado column
 * @method array findByUsuario(string $usuario) Return CambioVendedor objects filtered by the usuario column
 * @method array findByFecha(string $fecha) Return CambioVendedor objects filtered by the fecha column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseCambioVendedorQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseCambioVendedorQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = 'propel', $modelName = 'CambioVendedor', $modelAlias = null)
    {
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new CambioVendedorQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param     CambioVendedorQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return CambioVendedorQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof CambioVendedorQuery) {
            return $criteria;
        }
        $query = new CambioVendedorQuery();
        if (null !== $modelAlias) {
            $query->setModelAlias($modelAlias);
        }
        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return   CambioVendedor|CambioVendedor[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = CambioVendedorPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is alredy in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(CambioVendedorPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return   CambioVendedor A model object, or null if the key is not found
     * @throws   PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT `ID`, `CODIGO_OPERA`, `VENDEDOR_ANTERIOR`, `VENDEDOR_ACTUALIZADO`, `USUARIO`, `FECHA` FROM `cambio_vendedor` WHERE `ID` = :p0';
        try {
            $stmt = $con->prepare($sql);
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new CambioVendedor();
            $obj->hydrate($row);
            CambioVendedorPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return CambioVendedor|CambioVendedor[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|CambioVendedor[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return CambioVendedorQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(CambioVendedorPeer::ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return CambioVendedorQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(CambioVendedorPeer::ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the id column
     *
     * Example usage:
     * <code>
     * $query->filterById(1234); // WHERE id = 1234
     * $query->filterById(array(12, 34)); // WHERE id IN (12, 34)
     * $query->filterById(array('min' => 12)); // WHERE id > 12
     * </code>
     *
     * @param     mixed $id The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return CambioVendedorQuery The current query, for fluid interface
     */
    public function filterById($id = null, $comparison = null)
    {
        if (is_array($id) && null === $comparison) {
            $comparison = Criteria::IN;
        }

        return $this->addUsingAlias(CambioVendedorPeer::ID, $id, $comparison);
    }

    /**
     * Filter the query on the codigo_opera column
     *
     * Example usage:
     * <code>
     * $query->filterByCodigoOpera('fooValue');   // WHERE codigo_opera = 'fooValue'
     * $query->filterByCodigoOpera('%fooValue%'); // WHERE codigo_opera LIKE '%fooValue%'
     * </code>
     *
     * @param     string $codigoOpera The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return CambioVendedorQuery The current query, for fluid interface
     */
    public function filterByCodigoOpera($codigoOpera = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($codigoOpera)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $codigoOpera)) {
                $codigoOpera = str_replace('*', '%', $codigoOpera);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(CambioVendedorPeer::CODIGO_OPERA, $codigoOpera, $comparison);
    }

    /**
     * Filter the query on the vendedor_anterior column
     *
     * Example usage:
     * <code>
     * $query->filterByVendedorAnterior('fooValue');   // WHERE vendedor_anterior = 'fooValue'
     * $query->filterByVendedorAnterior('%fooValue%'); // WHERE vendedor_anterior LIKE '%fooValue%'
     * </code>
     *
     * @param     string $vendedorAnterior The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return CambioVendedorQuery The current query, for fluid interface
     */
    public function filterByVendedorAnterior($vendedorAnterior = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($vendedorAnterior)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $vendedorAnterior)) {
                $vendedorAnterior = str_replace('*', '%', $vendedorAnterior);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(CambioVendedorPeer::VENDEDOR_ANTERIOR, $vendedorAnterior, $comparison);
    }

    /**
     * Filter the query on the vendedor_actualizado column
     *
     * Example usage:
     * <code>
     * $query->filterByVendedorActualizado('fooValue');   // WHERE vendedor_actualizado = 'fooValue'
     * $query->filterByVendedorActualizado('%fooValue%'); // WHERE vendedor_actualizado LIKE '%fooValue%'
     * </code>
     *
     * @param     string $vendedorActualizado The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return CambioVendedorQuery The current query, for fluid interface
     */
    public function filterByVendedorActualizado($vendedorActualizado = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($vendedorActualizado)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $vendedorActualizado)) {
                $vendedorActualizado = str_replace('*', '%', $vendedorActualizado);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(CambioVendedorPeer::VENDEDOR_ACTUALIZADO, $vendedorActualizado, $comparison);
    }

    /**
     * Filter the query on the usuario column
     *
     * Example usage:
     * <code>
     * $query->filterByUsuario('fooValue');   // WHERE usuario = 'fooValue'
     * $query->filterByUsuario('%fooValue%'); // WHERE usuario LIKE '%fooValue%'
     * </code>
     *
     * @param     string $usuario The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return CambioVendedorQuery The current query, for fluid interface
     */
    public function filterByUsuario($usuario = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($usuario)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $usuario)) {
                $usuario = str_replace('*', '%', $usuario);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(CambioVendedorPeer::USUARIO, $usuario, $comparison);
    }

    /**
     * Filter the query on the fecha column
     *
     * Example usage:
     * <code>
     * $query->filterByFecha('2011-03-14'); // WHERE fecha = '2011-03-14'
     * $query->filterByFecha('now'); // WHERE fecha = '2011-03-14'
     * $query->filterByFecha(array('max' => 'yesterday')); // WHERE fecha > '2011-03-13'
     * </code>
     *
     * @param     mixed $fecha The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return CambioVendedorQuery The current query, for fluid interface
     */
    public function filterByFecha($fecha = null, $comparison = null)
    {
        if (is_array($fecha)) {
            $useMinMax = false;
            if (isset($fecha['min'])) {
                $this->addUsingAlias(CambioVendedorPeer::FECHA, $fecha['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($fecha['max'])) {
                $this->addUsingAlias(CambioVendedorPeer::FECHA, $fecha['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(CambioVendedorPeer::FECHA, $fecha, $comparison);
    }

    /**
     * Exclude object from result
     *
     * @param   CambioVendedor $cambioVendedor Object to remove from the list of results
     *
     * @return CambioVendedorQuery The current query, for fluid interface
     */
    public function prune($cambioVendedor = null)
    {
        if ($cambioVendedor) {
            $this->addUsingAlias(CambioVendedorPeer::ID, $cambioVendedor->getId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}
