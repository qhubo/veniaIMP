<?php


/**
 * Base class that represents a query for the 'lista_empaque_unida' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.6.7 on:
 *
 * 06/04/26 03:19:12
 *
 * @method ListaEmpaqueUnidaQuery orderById($order = Criteria::ASC) Order by the id column
 * @method ListaEmpaqueUnidaQuery orderByEmpresaId($order = Criteria::ASC) Order by the empresa_id column
 * @method ListaEmpaqueUnidaQuery orderByTitulo($order = Criteria::ASC) Order by the titulo column
 * @method ListaEmpaqueUnidaQuery orderByUsuario($order = Criteria::ASC) Order by the usuario column
 *
 * @method ListaEmpaqueUnidaQuery groupById() Group by the id column
 * @method ListaEmpaqueUnidaQuery groupByEmpresaId() Group by the empresa_id column
 * @method ListaEmpaqueUnidaQuery groupByTitulo() Group by the titulo column
 * @method ListaEmpaqueUnidaQuery groupByUsuario() Group by the usuario column
 *
 * @method ListaEmpaqueUnidaQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method ListaEmpaqueUnidaQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method ListaEmpaqueUnidaQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method ListaEmpaqueUnidaQuery leftJoinEmpresa($relationAlias = null) Adds a LEFT JOIN clause to the query using the Empresa relation
 * @method ListaEmpaqueUnidaQuery rightJoinEmpresa($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Empresa relation
 * @method ListaEmpaqueUnidaQuery innerJoinEmpresa($relationAlias = null) Adds a INNER JOIN clause to the query using the Empresa relation
 *
 * @method ListaEmpaqueUnidaQuery leftJoinListaEmpaqueUnidaDetalle($relationAlias = null) Adds a LEFT JOIN clause to the query using the ListaEmpaqueUnidaDetalle relation
 * @method ListaEmpaqueUnidaQuery rightJoinListaEmpaqueUnidaDetalle($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ListaEmpaqueUnidaDetalle relation
 * @method ListaEmpaqueUnidaQuery innerJoinListaEmpaqueUnidaDetalle($relationAlias = null) Adds a INNER JOIN clause to the query using the ListaEmpaqueUnidaDetalle relation
 *
 * @method ListaEmpaqueUnida findOne(PropelPDO $con = null) Return the first ListaEmpaqueUnida matching the query
 * @method ListaEmpaqueUnida findOneOrCreate(PropelPDO $con = null) Return the first ListaEmpaqueUnida matching the query, or a new ListaEmpaqueUnida object populated from the query conditions when no match is found
 *
 * @method ListaEmpaqueUnida findOneById(int $id) Return the first ListaEmpaqueUnida filtered by the id column
 * @method ListaEmpaqueUnida findOneByEmpresaId(int $empresa_id) Return the first ListaEmpaqueUnida filtered by the empresa_id column
 * @method ListaEmpaqueUnida findOneByTitulo(string $titulo) Return the first ListaEmpaqueUnida filtered by the titulo column
 * @method ListaEmpaqueUnida findOneByUsuario(string $usuario) Return the first ListaEmpaqueUnida filtered by the usuario column
 *
 * @method array findById(int $id) Return ListaEmpaqueUnida objects filtered by the id column
 * @method array findByEmpresaId(int $empresa_id) Return ListaEmpaqueUnida objects filtered by the empresa_id column
 * @method array findByTitulo(string $titulo) Return ListaEmpaqueUnida objects filtered by the titulo column
 * @method array findByUsuario(string $usuario) Return ListaEmpaqueUnida objects filtered by the usuario column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseListaEmpaqueUnidaQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseListaEmpaqueUnidaQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = 'propel', $modelName = 'ListaEmpaqueUnida', $modelAlias = null)
    {
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new ListaEmpaqueUnidaQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param     ListaEmpaqueUnidaQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return ListaEmpaqueUnidaQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof ListaEmpaqueUnidaQuery) {
            return $criteria;
        }
        $query = new ListaEmpaqueUnidaQuery();
        if (null !== $modelAlias) {
            $query->setModelAlias($modelAlias);
        }
        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return   ListaEmpaqueUnida|ListaEmpaqueUnida[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = ListaEmpaqueUnidaPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is alredy in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(ListaEmpaqueUnidaPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return   ListaEmpaqueUnida A model object, or null if the key is not found
     * @throws   PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT `ID`, `EMPRESA_ID`, `TITULO`, `USUARIO` FROM `lista_empaque_unida` WHERE `ID` = :p0';
        try {
            $stmt = $con->prepare($sql);
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new ListaEmpaqueUnida();
            $obj->hydrate($row);
            ListaEmpaqueUnidaPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return ListaEmpaqueUnida|ListaEmpaqueUnida[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|ListaEmpaqueUnida[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return ListaEmpaqueUnidaQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(ListaEmpaqueUnidaPeer::ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return ListaEmpaqueUnidaQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(ListaEmpaqueUnidaPeer::ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the id column
     *
     * Example usage:
     * <code>
     * $query->filterById(1234); // WHERE id = 1234
     * $query->filterById(array(12, 34)); // WHERE id IN (12, 34)
     * $query->filterById(array('min' => 12)); // WHERE id > 12
     * </code>
     *
     * @param     mixed $id The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ListaEmpaqueUnidaQuery The current query, for fluid interface
     */
    public function filterById($id = null, $comparison = null)
    {
        if (is_array($id) && null === $comparison) {
            $comparison = Criteria::IN;
        }

        return $this->addUsingAlias(ListaEmpaqueUnidaPeer::ID, $id, $comparison);
    }

    /**
     * Filter the query on the empresa_id column
     *
     * Example usage:
     * <code>
     * $query->filterByEmpresaId(1234); // WHERE empresa_id = 1234
     * $query->filterByEmpresaId(array(12, 34)); // WHERE empresa_id IN (12, 34)
     * $query->filterByEmpresaId(array('min' => 12)); // WHERE empresa_id > 12
     * </code>
     *
     * @see       filterByEmpresa()
     *
     * @param     mixed $empresaId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ListaEmpaqueUnidaQuery The current query, for fluid interface
     */
    public function filterByEmpresaId($empresaId = null, $comparison = null)
    {
        if (is_array($empresaId)) {
            $useMinMax = false;
            if (isset($empresaId['min'])) {
                $this->addUsingAlias(ListaEmpaqueUnidaPeer::EMPRESA_ID, $empresaId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($empresaId['max'])) {
                $this->addUsingAlias(ListaEmpaqueUnidaPeer::EMPRESA_ID, $empresaId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ListaEmpaqueUnidaPeer::EMPRESA_ID, $empresaId, $comparison);
    }

    /**
     * Filter the query on the titulo column
     *
     * Example usage:
     * <code>
     * $query->filterByTitulo('fooValue');   // WHERE titulo = 'fooValue'
     * $query->filterByTitulo('%fooValue%'); // WHERE titulo LIKE '%fooValue%'
     * </code>
     *
     * @param     string $titulo The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ListaEmpaqueUnidaQuery The current query, for fluid interface
     */
    public function filterByTitulo($titulo = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($titulo)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $titulo)) {
                $titulo = str_replace('*', '%', $titulo);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ListaEmpaqueUnidaPeer::TITULO, $titulo, $comparison);
    }

    /**
     * Filter the query on the usuario column
     *
     * Example usage:
     * <code>
     * $query->filterByUsuario('fooValue');   // WHERE usuario = 'fooValue'
     * $query->filterByUsuario('%fooValue%'); // WHERE usuario LIKE '%fooValue%'
     * </code>
     *
     * @param     string $usuario The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ListaEmpaqueUnidaQuery The current query, for fluid interface
     */
    public function filterByUsuario($usuario = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($usuario)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $usuario)) {
                $usuario = str_replace('*', '%', $usuario);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ListaEmpaqueUnidaPeer::USUARIO, $usuario, $comparison);
    }

    /**
     * Filter the query by a related Empresa object
     *
     * @param   Empresa|PropelObjectCollection $empresa The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   ListaEmpaqueUnidaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByEmpresa($empresa, $comparison = null)
    {
        if ($empresa instanceof Empresa) {
            return $this
                ->addUsingAlias(ListaEmpaqueUnidaPeer::EMPRESA_ID, $empresa->getId(), $comparison);
        } elseif ($empresa instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(ListaEmpaqueUnidaPeer::EMPRESA_ID, $empresa->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByEmpresa() only accepts arguments of type Empresa or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Empresa relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ListaEmpaqueUnidaQuery The current query, for fluid interface
     */
    public function joinEmpresa($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Empresa');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Empresa');
        }

        return $this;
    }

    /**
     * Use the Empresa relation Empresa object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   EmpresaQuery A secondary query class using the current class as primary query
     */
    public function useEmpresaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinEmpresa($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Empresa', 'EmpresaQuery');
    }

    /**
     * Filter the query by a related ListaEmpaqueUnidaDetalle object
     *
     * @param   ListaEmpaqueUnidaDetalle|PropelObjectCollection $listaEmpaqueUnidaDetalle  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   ListaEmpaqueUnidaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByListaEmpaqueUnidaDetalle($listaEmpaqueUnidaDetalle, $comparison = null)
    {
        if ($listaEmpaqueUnidaDetalle instanceof ListaEmpaqueUnidaDetalle) {
            return $this
                ->addUsingAlias(ListaEmpaqueUnidaPeer::ID, $listaEmpaqueUnidaDetalle->getListaEmpaqueUnidaId(), $comparison);
        } elseif ($listaEmpaqueUnidaDetalle instanceof PropelObjectCollection) {
            return $this
                ->useListaEmpaqueUnidaDetalleQuery()
                ->filterByPrimaryKeys($listaEmpaqueUnidaDetalle->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByListaEmpaqueUnidaDetalle() only accepts arguments of type ListaEmpaqueUnidaDetalle or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ListaEmpaqueUnidaDetalle relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ListaEmpaqueUnidaQuery The current query, for fluid interface
     */
    public function joinListaEmpaqueUnidaDetalle($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ListaEmpaqueUnidaDetalle');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ListaEmpaqueUnidaDetalle');
        }

        return $this;
    }

    /**
     * Use the ListaEmpaqueUnidaDetalle relation ListaEmpaqueUnidaDetalle object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ListaEmpaqueUnidaDetalleQuery A secondary query class using the current class as primary query
     */
    public function useListaEmpaqueUnidaDetalleQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinListaEmpaqueUnidaDetalle($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ListaEmpaqueUnidaDetalle', 'ListaEmpaqueUnidaDetalleQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   ListaEmpaqueUnida $listaEmpaqueUnida Object to remove from the list of results
     *
     * @return ListaEmpaqueUnidaQuery The current query, for fluid interface
     */
    public function prune($listaEmpaqueUnida = null)
    {
        if ($listaEmpaqueUnida) {
            $this->addUsingAlias(ListaEmpaqueUnidaPeer::ID, $listaEmpaqueUnida->getId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}
