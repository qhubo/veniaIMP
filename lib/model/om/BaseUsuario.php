<?php


/**
 * Base class that represents a row from the 'usuario' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.6.7 on:
 *
 * 12/28/25 02:48:01
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseUsuario extends BaseObject implements Persistent
{
    /**
     * Peer class name
     */
    const PEER = 'UsuarioPeer';

    /**
     * The Peer class.
     * Instance provides a convenient way of calling static methods on a class
     * that calling code may not be able to identify.
     * @var        UsuarioPeer
     */
    protected static $peer;

    /**
     * The flag var to prevent infinit loop in deep copy
     * @var       boolean
     */
    protected $startCopy = false;

    /**
     * The value for the id field.
     * @var        int
     */
    protected $id;

    /**
     * The value for the usuario field.
     * @var        string
     */
    protected $usuario;

    /**
     * The value for the nombre_completo field.
     * @var        string
     */
    protected $nombre_completo;

    /**
     * The value for the tipo_usuario field.
     * @var        string
     */
    protected $tipo_usuario;

    /**
     * The value for the clave field.
     * @var        string
     */
    protected $clave;

    /**
     * The value for the correo field.
     * @var        string
     */
    protected $correo;

    /**
     * The value for the estado field.
     * @var        string
     */
    protected $estado;

    /**
     * The value for the imagen field.
     * @var        string
     */
    protected $imagen;

    /**
     * The value for the administrador field.
     * Note: this column has a database default value of: false
     * @var        boolean
     */
    protected $administrador;

    /**
     * The value for the ultimo_ingreso field.
     * @var        string
     */
    protected $ultimo_ingreso;

    /**
     * The value for the token field.
     * @var        string
     */
    protected $token;

    /**
     * The value for the activo field.
     * Note: this column has a database default value of: true
     * @var        boolean
     */
    protected $activo;

    /**
     * The value for the validado field.
     * Note: this column has a database default value of: false
     * @var        boolean
     */
    protected $validado;

    /**
     * The value for the nivel_usuario field.
     * Note: this column has a database default value of: ''
     * @var        string
     */
    protected $nivel_usuario;

    /**
     * The value for the ip field.
     * @var        string
     */
    protected $ip;

    /**
     * The value for the tienda_id field.
     * @var        int
     */
    protected $tienda_id;

    /**
     * The value for the empresa_id field.
     * @var        int
     */
    protected $empresa_id;

    /**
     * The value for the vendedor_id field.
     * @var        int
     */
    protected $vendedor_id;

    /**
     * @var        Tienda
     */
    protected $aTienda;

    /**
     * @var        Empresa
     */
    protected $aEmpresa;

    /**
     * @var        Vendedor
     */
    protected $aVendedor;

    /**
     * @var        PropelObjectCollection|UsuarioTienda[] Collection to store aggregation of UsuarioTienda objects.
     */
    protected $collUsuarioTiendas;
    protected $collUsuarioTiendasPartial;

    /**
     * @var        PropelObjectCollection|UsuarioEmpresa[] Collection to store aggregation of UsuarioEmpresa objects.
     */
    protected $collUsuarioEmpresas;
    protected $collUsuarioEmpresasPartial;

    /**
     * @var        PropelObjectCollection|UsuarioPerfil[] Collection to store aggregation of UsuarioPerfil objects.
     */
    protected $collUsuarioPerfils;
    protected $collUsuarioPerfilsPartial;

    /**
     * @var        PropelObjectCollection|IngresoProductoPro[] Collection to store aggregation of IngresoProductoPro objects.
     */
    protected $collIngresoProductoPros;
    protected $collIngresoProductoProsPartial;

    /**
     * @var        PropelObjectCollection|SalidaProducto[] Collection to store aggregation of SalidaProducto objects.
     */
    protected $collSalidaProductos;
    protected $collSalidaProductosPartial;

    /**
     * @var        PropelObjectCollection|SalidaProductoDetalle[] Collection to store aggregation of SalidaProductoDetalle objects.
     */
    protected $collSalidaProductoDetalles;
    protected $collSalidaProductoDetallesPartial;

    /**
     * @var        PropelObjectCollection|UsuarioCorteValor[] Collection to store aggregation of UsuarioCorteValor objects.
     */
    protected $collUsuarioCorteValors;
    protected $collUsuarioCorteValorsPartial;

    /**
     * @var        PropelObjectCollection|IngresoProducto[] Collection to store aggregation of IngresoProducto objects.
     */
    protected $collIngresoProductos;
    protected $collIngresoProductosPartial;

    /**
     * @var        PropelObjectCollection|PrecioProducto[] Collection to store aggregation of PrecioProducto objects.
     */
    protected $collPrecioProductos;
    protected $collPrecioProductosPartial;

    /**
     * Flag to prevent endless save loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInSave = false;

    /**
     * Flag to prevent endless validation loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInValidation = false;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $usuarioTiendasScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $usuarioEmpresasScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $usuarioPerfilsScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $ingresoProductoProsScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $salidaProductosScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $salidaProductoDetallesScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $usuarioCorteValorsScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $ingresoProductosScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $precioProductosScheduledForDeletion = null;

    /**
     * Applies default values to this object.
     * This method should be called from the object's constructor (or
     * equivalent initialization method).
     * @see        __construct()
     */
    public function applyDefaultValues()
    {
        $this->administrador = false;
        $this->activo = true;
        $this->validado = false;
        $this->nivel_usuario = '';
    }

    /**
     * Initializes internal state of BaseUsuario object.
     * @see        applyDefaults()
     */
    public function __construct()
    {
        parent::__construct();
        $this->applyDefaultValues();
    }

    /**
     * Get the [id] column value.
     *
     * @return int
     */
    public function getId()
    {
        return $this->id;
    }

    /**
     * Get the [usuario] column value.
     *
     * @return string
     */
    public function getUsuario()
    {
        return $this->usuario;
    }

    /**
     * Get the [nombre_completo] column value.
     *
     * @return string
     */
    public function getNombreCompleto()
    {
        return $this->nombre_completo;
    }

    /**
     * Get the [tipo_usuario] column value.
     *
     * @return string
     */
    public function getTipoUsuario()
    {
        return $this->tipo_usuario;
    }

    /**
     * Get the [clave] column value.
     *
     * @return string
     */
    public function getClave()
    {
        return $this->clave;
    }

    /**
     * Get the [correo] column value.
     *
     * @return string
     */
    public function getCorreo()
    {
        return $this->correo;
    }

    /**
     * Get the [estado] column value.
     *
     * @return string
     */
    public function getEstado()
    {
        return $this->estado;
    }

    /**
     * Get the [imagen] column value.
     *
     * @return string
     */
    public function getImagen()
    {
        return $this->imagen;
    }

    /**
     * Get the [administrador] column value.
     *
     * @return boolean
     */
    public function getAdministrador()
    {
        return $this->administrador;
    }

    /**
     * Get the [optionally formatted] temporal [ultimo_ingreso] column value.
     *
     *
     * @param string $format The date/time format string (either date()-style or strftime()-style).
     *				 If format is null, then the raw DateTime object will be returned.
     * @return mixed Formatted date/time value as string or DateTime object (if format is null), null if column is null, and 0 if column value is 0000-00-00 00:00:00
     * @throws PropelException - if unable to parse/validate the date/time value.
     */
    public function getUltimoIngreso($format = 'Y-m-d H:i:s')
    {
        if ($this->ultimo_ingreso === null) {
            return null;
        }

        if ($this->ultimo_ingreso === '0000-00-00 00:00:00') {
            // while technically this is not a default value of null,
            // this seems to be closest in meaning.
            return null;
        } else {
            try {
                $dt = new DateTime($this->ultimo_ingreso);
            } catch (Exception $x) {
                throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->ultimo_ingreso, true), $x);
            }
        }

        if ($format === null) {
            // Because propel.useDateTimeClass is true, we return a DateTime object.
            return $dt;
        } elseif (strpos($format, '%') !== false) {
            return strftime($format, $dt->format('U'));
        } else {
            return $dt->format($format);
        }
    }

    /**
     * Get the [token] column value.
     *
     * @return string
     */
    public function getToken()
    {
        return $this->token;
    }

    /**
     * Get the [activo] column value.
     *
     * @return boolean
     */
    public function getActivo()
    {
        return $this->activo;
    }

    /**
     * Get the [validado] column value.
     *
     * @return boolean
     */
    public function getValidado()
    {
        return $this->validado;
    }

    /**
     * Get the [nivel_usuario] column value.
     *
     * @return string
     */
    public function getNivelUsuario()
    {
        return $this->nivel_usuario;
    }

    /**
     * Get the [ip] column value.
     *
     * @return string
     */
    public function getIp()
    {
        return $this->ip;
    }

    /**
     * Get the [tienda_id] column value.
     *
     * @return int
     */
    public function getTiendaId()
    {
        return $this->tienda_id;
    }

    /**
     * Get the [empresa_id] column value.
     *
     * @return int
     */
    public function getEmpresaId()
    {
        return $this->empresa_id;
    }

    /**
     * Get the [vendedor_id] column value.
     *
     * @return int
     */
    public function getVendedorId()
    {
        return $this->vendedor_id;
    }

    /**
     * Set the value of [id] column.
     *
     * @param int $v new value
     * @return Usuario The current object (for fluent API support)
     */
    public function setId($v)
    {
        if ($v !== null) {
            $v = (int) $v;
        }

        if ($this->id !== $v) {
            $this->id = $v;
            $this->modifiedColumns[] = UsuarioPeer::ID;
        }


        return $this;
    } // setId()

    /**
     * Set the value of [usuario] column.
     *
     * @param string $v new value
     * @return Usuario The current object (for fluent API support)
     */
    public function setUsuario($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->usuario !== $v) {
            $this->usuario = $v;
            $this->modifiedColumns[] = UsuarioPeer::USUARIO;
        }


        return $this;
    } // setUsuario()

    /**
     * Set the value of [nombre_completo] column.
     *
     * @param string $v new value
     * @return Usuario The current object (for fluent API support)
     */
    public function setNombreCompleto($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->nombre_completo !== $v) {
            $this->nombre_completo = $v;
            $this->modifiedColumns[] = UsuarioPeer::NOMBRE_COMPLETO;
        }


        return $this;
    } // setNombreCompleto()

    /**
     * Set the value of [tipo_usuario] column.
     *
     * @param string $v new value
     * @return Usuario The current object (for fluent API support)
     */
    public function setTipoUsuario($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->tipo_usuario !== $v) {
            $this->tipo_usuario = $v;
            $this->modifiedColumns[] = UsuarioPeer::TIPO_USUARIO;
        }


        return $this;
    } // setTipoUsuario()

    /**
     * Set the value of [clave] column.
     *
     * @param string $v new value
     * @return Usuario The current object (for fluent API support)
     */
    public function setClave($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->clave !== $v) {
            $this->clave = $v;
            $this->modifiedColumns[] = UsuarioPeer::CLAVE;
        }


        return $this;
    } // setClave()

    /**
     * Set the value of [correo] column.
     *
     * @param string $v new value
     * @return Usuario The current object (for fluent API support)
     */
    public function setCorreo($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->correo !== $v) {
            $this->correo = $v;
            $this->modifiedColumns[] = UsuarioPeer::CORREO;
        }


        return $this;
    } // setCorreo()

    /**
     * Set the value of [estado] column.
     *
     * @param string $v new value
     * @return Usuario The current object (for fluent API support)
     */
    public function setEstado($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->estado !== $v) {
            $this->estado = $v;
            $this->modifiedColumns[] = UsuarioPeer::ESTADO;
        }


        return $this;
    } // setEstado()

    /**
     * Set the value of [imagen] column.
     *
     * @param string $v new value
     * @return Usuario The current object (for fluent API support)
     */
    public function setImagen($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->imagen !== $v) {
            $this->imagen = $v;
            $this->modifiedColumns[] = UsuarioPeer::IMAGEN;
        }


        return $this;
    } // setImagen()

    /**
     * Sets the value of the [administrador] column.
     * Non-boolean arguments are converted using the following rules:
     *   * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *   * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     * Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     *
     * @param boolean|integer|string $v The new value
     * @return Usuario The current object (for fluent API support)
     */
    public function setAdministrador($v)
    {
        if ($v !== null) {
            if (is_string($v)) {
                $v = in_array(strtolower($v), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
            } else {
                $v = (boolean) $v;
            }
        }

        if ($this->administrador !== $v) {
            $this->administrador = $v;
            $this->modifiedColumns[] = UsuarioPeer::ADMINISTRADOR;
        }


        return $this;
    } // setAdministrador()

    /**
     * Sets the value of [ultimo_ingreso] column to a normalized version of the date/time value specified.
     *
     * @param mixed $v string, integer (timestamp), or DateTime value.
     *               Empty strings are treated as null.
     * @return Usuario The current object (for fluent API support)
     */
    public function setUltimoIngreso($v)
    {
        $dt = PropelDateTime::newInstance($v, null, 'DateTime');
        if ($this->ultimo_ingreso !== null || $dt !== null) {
            $currentDateAsString = ($this->ultimo_ingreso !== null && $tmpDt = new DateTime($this->ultimo_ingreso)) ? $tmpDt->format('Y-m-d H:i:s') : null;
            $newDateAsString = $dt ? $dt->format('Y-m-d H:i:s') : null;
            if ($currentDateAsString !== $newDateAsString) {
                $this->ultimo_ingreso = $newDateAsString;
                $this->modifiedColumns[] = UsuarioPeer::ULTIMO_INGRESO;
            }
        } // if either are not null


        return $this;
    } // setUltimoIngreso()

    /**
     * Set the value of [token] column.
     *
     * @param string $v new value
     * @return Usuario The current object (for fluent API support)
     */
    public function setToken($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->token !== $v) {
            $this->token = $v;
            $this->modifiedColumns[] = UsuarioPeer::TOKEN;
        }


        return $this;
    } // setToken()

    /**
     * Sets the value of the [activo] column.
     * Non-boolean arguments are converted using the following rules:
     *   * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *   * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     * Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     *
     * @param boolean|integer|string $v The new value
     * @return Usuario The current object (for fluent API support)
     */
    public function setActivo($v)
    {
        if ($v !== null) {
            if (is_string($v)) {
                $v = in_array(strtolower($v), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
            } else {
                $v = (boolean) $v;
            }
        }

        if ($this->activo !== $v) {
            $this->activo = $v;
            $this->modifiedColumns[] = UsuarioPeer::ACTIVO;
        }


        return $this;
    } // setActivo()

    /**
     * Sets the value of the [validado] column.
     * Non-boolean arguments are converted using the following rules:
     *   * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *   * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     * Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     *
     * @param boolean|integer|string $v The new value
     * @return Usuario The current object (for fluent API support)
     */
    public function setValidado($v)
    {
        if ($v !== null) {
            if (is_string($v)) {
                $v = in_array(strtolower($v), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
            } else {
                $v = (boolean) $v;
            }
        }

        if ($this->validado !== $v) {
            $this->validado = $v;
            $this->modifiedColumns[] = UsuarioPeer::VALIDADO;
        }


        return $this;
    } // setValidado()

    /**
     * Set the value of [nivel_usuario] column.
     *
     * @param string $v new value
     * @return Usuario The current object (for fluent API support)
     */
    public function setNivelUsuario($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->nivel_usuario !== $v) {
            $this->nivel_usuario = $v;
            $this->modifiedColumns[] = UsuarioPeer::NIVEL_USUARIO;
        }


        return $this;
    } // setNivelUsuario()

    /**
     * Set the value of [ip] column.
     *
     * @param string $v new value
     * @return Usuario The current object (for fluent API support)
     */
    public function setIp($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->ip !== $v) {
            $this->ip = $v;
            $this->modifiedColumns[] = UsuarioPeer::IP;
        }


        return $this;
    } // setIp()

    /**
     * Set the value of [tienda_id] column.
     *
     * @param int $v new value
     * @return Usuario The current object (for fluent API support)
     */
    public function setTiendaId($v)
    {
        if ($v !== null) {
            $v = (int) $v;
        }

        if ($this->tienda_id !== $v) {
            $this->tienda_id = $v;
            $this->modifiedColumns[] = UsuarioPeer::TIENDA_ID;
        }

        if ($this->aTienda !== null && $this->aTienda->getId() !== $v) {
            $this->aTienda = null;
        }


        return $this;
    } // setTiendaId()

    /**
     * Set the value of [empresa_id] column.
     *
     * @param int $v new value
     * @return Usuario The current object (for fluent API support)
     */
    public function setEmpresaId($v)
    {
        if ($v !== null) {
            $v = (int) $v;
        }

        if ($this->empresa_id !== $v) {
            $this->empresa_id = $v;
            $this->modifiedColumns[] = UsuarioPeer::EMPRESA_ID;
        }

        if ($this->aEmpresa !== null && $this->aEmpresa->getId() !== $v) {
            $this->aEmpresa = null;
        }


        return $this;
    } // setEmpresaId()

    /**
     * Set the value of [vendedor_id] column.
     *
     * @param int $v new value
     * @return Usuario The current object (for fluent API support)
     */
    public function setVendedorId($v)
    {
        if ($v !== null) {
            $v = (int) $v;
        }

        if ($this->vendedor_id !== $v) {
            $this->vendedor_id = $v;
            $this->modifiedColumns[] = UsuarioPeer::VENDEDOR_ID;
        }

        if ($this->aVendedor !== null && $this->aVendedor->getId() !== $v) {
            $this->aVendedor = null;
        }


        return $this;
    } // setVendedorId()

    /**
     * Indicates whether the columns in this object are only set to default values.
     *
     * This method can be used in conjunction with isModified() to indicate whether an object is both
     * modified _and_ has some values set which are non-default.
     *
     * @return boolean Whether the columns in this object are only been set with default values.
     */
    public function hasOnlyDefaultValues()
    {
            if ($this->administrador !== false) {
                return false;
            }

            if ($this->activo !== true) {
                return false;
            }

            if ($this->validado !== false) {
                return false;
            }

            if ($this->nivel_usuario !== '') {
                return false;
            }

        // otherwise, everything was equal, so return true
        return true;
    } // hasOnlyDefaultValues()

    /**
     * Hydrates (populates) the object variables with values from the database resultset.
     *
     * An offset (0-based "start column") is specified so that objects can be hydrated
     * with a subset of the columns in the resultset rows.  This is needed, for example,
     * for results of JOIN queries where the resultset row includes columns from two or
     * more tables.
     *
     * @param array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
     * @param int $startcol 0-based offset column which indicates which restultset column to start with.
     * @param boolean $rehydrate Whether this object is being re-hydrated from the database.
     * @return int             next starting column
     * @throws PropelException - Any caught Exception will be rewrapped as a PropelException.
     */
    public function hydrate($row, $startcol = 0, $rehydrate = false)
    {
        try {

            $this->id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
            $this->usuario = ($row[$startcol + 1] !== null) ? (string) $row[$startcol + 1] : null;
            $this->nombre_completo = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
            $this->tipo_usuario = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
            $this->clave = ($row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
            $this->correo = ($row[$startcol + 5] !== null) ? (string) $row[$startcol + 5] : null;
            $this->estado = ($row[$startcol + 6] !== null) ? (string) $row[$startcol + 6] : null;
            $this->imagen = ($row[$startcol + 7] !== null) ? (string) $row[$startcol + 7] : null;
            $this->administrador = ($row[$startcol + 8] !== null) ? (boolean) $row[$startcol + 8] : null;
            $this->ultimo_ingreso = ($row[$startcol + 9] !== null) ? (string) $row[$startcol + 9] : null;
            $this->token = ($row[$startcol + 10] !== null) ? (string) $row[$startcol + 10] : null;
            $this->activo = ($row[$startcol + 11] !== null) ? (boolean) $row[$startcol + 11] : null;
            $this->validado = ($row[$startcol + 12] !== null) ? (boolean) $row[$startcol + 12] : null;
            $this->nivel_usuario = ($row[$startcol + 13] !== null) ? (string) $row[$startcol + 13] : null;
            $this->ip = ($row[$startcol + 14] !== null) ? (string) $row[$startcol + 14] : null;
            $this->tienda_id = ($row[$startcol + 15] !== null) ? (int) $row[$startcol + 15] : null;
            $this->empresa_id = ($row[$startcol + 16] !== null) ? (int) $row[$startcol + 16] : null;
            $this->vendedor_id = ($row[$startcol + 17] !== null) ? (int) $row[$startcol + 17] : null;
            $this->resetModified();

            $this->setNew(false);

            if ($rehydrate) {
                $this->ensureConsistency();
            }

            return $startcol + 18; // 18 = UsuarioPeer::NUM_HYDRATE_COLUMNS.

        } catch (Exception $e) {
            throw new PropelException("Error populating Usuario object", $e);
        }
    }

    /**
     * Checks and repairs the internal consistency of the object.
     *
     * This method is executed after an already-instantiated object is re-hydrated
     * from the database.  It exists to check any foreign keys to make sure that
     * the objects related to the current object are correct based on foreign key.
     *
     * You can override this method in the stub class, but you should always invoke
     * the base method from the overridden method (i.e. parent::ensureConsistency()),
     * in case your model changes.
     *
     * @throws PropelException
     */
    public function ensureConsistency()
    {

        if ($this->aTienda !== null && $this->tienda_id !== $this->aTienda->getId()) {
            $this->aTienda = null;
        }
        if ($this->aEmpresa !== null && $this->empresa_id !== $this->aEmpresa->getId()) {
            $this->aEmpresa = null;
        }
        if ($this->aVendedor !== null && $this->vendedor_id !== $this->aVendedor->getId()) {
            $this->aVendedor = null;
        }
    } // ensureConsistency

    /**
     * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
     *
     * This will only work if the object has been saved and has a valid primary key set.
     *
     * @param boolean $deep (optional) Whether to also de-associated any related objects.
     * @param PropelPDO $con (optional) The PropelPDO connection to use.
     * @return void
     * @throws PropelException - if this object is deleted, unsaved or doesn't have pk match in db
     */
    public function reload($deep = false, PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("Cannot reload a deleted object.");
        }

        if ($this->isNew()) {
            throw new PropelException("Cannot reload an unsaved object.");
        }

        if ($con === null) {
            $con = Propel::getConnection(UsuarioPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }

        // We don't need to alter the object instance pool; we're just modifying this instance
        // already in the pool.

        $stmt = UsuarioPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
        $row = $stmt->fetch(PDO::FETCH_NUM);
        $stmt->closeCursor();
        if (!$row) {
            throw new PropelException('Cannot find matching row in the database to reload object values.');
        }
        $this->hydrate($row, 0, true); // rehydrate

        if ($deep) {  // also de-associate any related objects?

            $this->aTienda = null;
            $this->aEmpresa = null;
            $this->aVendedor = null;
            $this->collUsuarioTiendas = null;

            $this->collUsuarioEmpresas = null;

            $this->collUsuarioPerfils = null;

            $this->collIngresoProductoPros = null;

            $this->collSalidaProductos = null;

            $this->collSalidaProductoDetalles = null;

            $this->collUsuarioCorteValors = null;

            $this->collIngresoProductos = null;

            $this->collPrecioProductos = null;

        } // if (deep)
    }

    /**
     * Removes this object from datastore and sets delete attribute.
     *
     * @param PropelPDO $con
     * @return void
     * @throws PropelException
     * @throws Exception
     * @see        BaseObject::setDeleted()
     * @see        BaseObject::isDeleted()
     */
    public function delete(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("This object has already been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(UsuarioPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        try {
            $deleteQuery = UsuarioQuery::create()
                ->filterByPrimaryKey($this->getPrimaryKey());
            $ret = $this->preDelete($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BaseUsuario:delete:pre') as $callable)
            {
              if (call_user_func($callable, $this, $con))
              {
                $con->commit();
                return;
              }
            }

            if ($ret) {
                $deleteQuery->delete($con);
                $this->postDelete($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BaseUsuario:delete:post') as $callable)
                {
                  call_user_func($callable, $this, $con);
                }

                $con->commit();
                $this->setDeleted(true);
            } else {
                $con->commit();
            }
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Persists this object to the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All modified related objects will also be persisted in the doSave()
     * method.  This method wraps all precipitate database operations in a
     * single transaction.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @throws Exception
     * @see        doSave()
     */
    public function save(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("You cannot save an object that has been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(UsuarioPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        $isInsert = $this->isNew();
        try {
            $ret = $this->preSave($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BaseUsuario:save:pre') as $callable)
            {
              if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
              {
                  $con->commit();
                return $affectedRows;
              }
            }

            if ($isInsert) {
                $ret = $ret && $this->preInsert($con);
            } else {
                $ret = $ret && $this->preUpdate($con);
            }
            if ($ret) {
                $affectedRows = $this->doSave($con);
                if ($isInsert) {
                    $this->postInsert($con);
                } else {
                    $this->postUpdate($con);
                }
                $this->postSave($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BaseUsuario:save:post') as $callable)
                {
                  call_user_func($callable, $this, $con, $affectedRows);
                }

                UsuarioPeer::addInstanceToPool($this);
            } else {
                $affectedRows = 0;
            }
            $con->commit();

            return $affectedRows;
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Performs the work of inserting or updating the row in the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All related objects are also updated in this method.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @see        save()
     */
    protected function doSave(PropelPDO $con)
    {
        $affectedRows = 0; // initialize var to track total num of affected rows
        if (!$this->alreadyInSave) {
            $this->alreadyInSave = true;

            // We call the save method on the following object(s) if they
            // were passed to this object by their coresponding set
            // method.  This object relates to these object(s) by a
            // foreign key reference.

            if ($this->aTienda !== null) {
                if ($this->aTienda->isModified() || $this->aTienda->isNew()) {
                    $affectedRows += $this->aTienda->save($con);
                }
                $this->setTienda($this->aTienda);
            }

            if ($this->aEmpresa !== null) {
                if ($this->aEmpresa->isModified() || $this->aEmpresa->isNew()) {
                    $affectedRows += $this->aEmpresa->save($con);
                }
                $this->setEmpresa($this->aEmpresa);
            }

            if ($this->aVendedor !== null) {
                if ($this->aVendedor->isModified() || $this->aVendedor->isNew()) {
                    $affectedRows += $this->aVendedor->save($con);
                }
                $this->setVendedor($this->aVendedor);
            }

            if ($this->isNew() || $this->isModified()) {
                // persist changes
                if ($this->isNew()) {
                    $this->doInsert($con);
                } else {
                    $this->doUpdate($con);
                }
                $affectedRows += 1;
                $this->resetModified();
            }

            if ($this->usuarioTiendasScheduledForDeletion !== null) {
                if (!$this->usuarioTiendasScheduledForDeletion->isEmpty()) {
                    foreach ($this->usuarioTiendasScheduledForDeletion as $usuarioTienda) {
                        // need to save related object because we set the relation to null
                        $usuarioTienda->save($con);
                    }
                    $this->usuarioTiendasScheduledForDeletion = null;
                }
            }

            if ($this->collUsuarioTiendas !== null) {
                foreach ($this->collUsuarioTiendas as $referrerFK) {
                    if (!$referrerFK->isDeleted()) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->usuarioEmpresasScheduledForDeletion !== null) {
                if (!$this->usuarioEmpresasScheduledForDeletion->isEmpty()) {
                    foreach ($this->usuarioEmpresasScheduledForDeletion as $usuarioEmpresa) {
                        // need to save related object because we set the relation to null
                        $usuarioEmpresa->save($con);
                    }
                    $this->usuarioEmpresasScheduledForDeletion = null;
                }
            }

            if ($this->collUsuarioEmpresas !== null) {
                foreach ($this->collUsuarioEmpresas as $referrerFK) {
                    if (!$referrerFK->isDeleted()) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->usuarioPerfilsScheduledForDeletion !== null) {
                if (!$this->usuarioPerfilsScheduledForDeletion->isEmpty()) {
                    foreach ($this->usuarioPerfilsScheduledForDeletion as $usuarioPerfil) {
                        // need to save related object because we set the relation to null
                        $usuarioPerfil->save($con);
                    }
                    $this->usuarioPerfilsScheduledForDeletion = null;
                }
            }

            if ($this->collUsuarioPerfils !== null) {
                foreach ($this->collUsuarioPerfils as $referrerFK) {
                    if (!$referrerFK->isDeleted()) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->ingresoProductoProsScheduledForDeletion !== null) {
                if (!$this->ingresoProductoProsScheduledForDeletion->isEmpty()) {
                    foreach ($this->ingresoProductoProsScheduledForDeletion as $ingresoProductoPro) {
                        // need to save related object because we set the relation to null
                        $ingresoProductoPro->save($con);
                    }
                    $this->ingresoProductoProsScheduledForDeletion = null;
                }
            }

            if ($this->collIngresoProductoPros !== null) {
                foreach ($this->collIngresoProductoPros as $referrerFK) {
                    if (!$referrerFK->isDeleted()) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->salidaProductosScheduledForDeletion !== null) {
                if (!$this->salidaProductosScheduledForDeletion->isEmpty()) {
                    foreach ($this->salidaProductosScheduledForDeletion as $salidaProducto) {
                        // need to save related object because we set the relation to null
                        $salidaProducto->save($con);
                    }
                    $this->salidaProductosScheduledForDeletion = null;
                }
            }

            if ($this->collSalidaProductos !== null) {
                foreach ($this->collSalidaProductos as $referrerFK) {
                    if (!$referrerFK->isDeleted()) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->salidaProductoDetallesScheduledForDeletion !== null) {
                if (!$this->salidaProductoDetallesScheduledForDeletion->isEmpty()) {
                    foreach ($this->salidaProductoDetallesScheduledForDeletion as $salidaProductoDetalle) {
                        // need to save related object because we set the relation to null
                        $salidaProductoDetalle->save($con);
                    }
                    $this->salidaProductoDetallesScheduledForDeletion = null;
                }
            }

            if ($this->collSalidaProductoDetalles !== null) {
                foreach ($this->collSalidaProductoDetalles as $referrerFK) {
                    if (!$referrerFK->isDeleted()) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->usuarioCorteValorsScheduledForDeletion !== null) {
                if (!$this->usuarioCorteValorsScheduledForDeletion->isEmpty()) {
                    foreach ($this->usuarioCorteValorsScheduledForDeletion as $usuarioCorteValor) {
                        // need to save related object because we set the relation to null
                        $usuarioCorteValor->save($con);
                    }
                    $this->usuarioCorteValorsScheduledForDeletion = null;
                }
            }

            if ($this->collUsuarioCorteValors !== null) {
                foreach ($this->collUsuarioCorteValors as $referrerFK) {
                    if (!$referrerFK->isDeleted()) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->ingresoProductosScheduledForDeletion !== null) {
                if (!$this->ingresoProductosScheduledForDeletion->isEmpty()) {
                    foreach ($this->ingresoProductosScheduledForDeletion as $ingresoProducto) {
                        // need to save related object because we set the relation to null
                        $ingresoProducto->save($con);
                    }
                    $this->ingresoProductosScheduledForDeletion = null;
                }
            }

            if ($this->collIngresoProductos !== null) {
                foreach ($this->collIngresoProductos as $referrerFK) {
                    if (!$referrerFK->isDeleted()) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->precioProductosScheduledForDeletion !== null) {
                if (!$this->precioProductosScheduledForDeletion->isEmpty()) {
                    foreach ($this->precioProductosScheduledForDeletion as $precioProducto) {
                        // need to save related object because we set the relation to null
                        $precioProducto->save($con);
                    }
                    $this->precioProductosScheduledForDeletion = null;
                }
            }

            if ($this->collPrecioProductos !== null) {
                foreach ($this->collPrecioProductos as $referrerFK) {
                    if (!$referrerFK->isDeleted()) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            $this->alreadyInSave = false;

        }

        return $affectedRows;
    } // doSave()

    /**
     * Insert the row in the database.
     *
     * @param PropelPDO $con
     *
     * @throws PropelException
     * @see        doSave()
     */
    protected function doInsert(PropelPDO $con)
    {
        $modifiedColumns = array();
        $index = 0;

        $this->modifiedColumns[] = UsuarioPeer::ID;
        if (null !== $this->id) {
            throw new PropelException('Cannot insert a value for auto-increment primary key (' . UsuarioPeer::ID . ')');
        }

         // check the columns in natural order for more readable SQL queries
        if ($this->isColumnModified(UsuarioPeer::ID)) {
            $modifiedColumns[':p' . $index++]  = '`ID`';
        }
        if ($this->isColumnModified(UsuarioPeer::USUARIO)) {
            $modifiedColumns[':p' . $index++]  = '`USUARIO`';
        }
        if ($this->isColumnModified(UsuarioPeer::NOMBRE_COMPLETO)) {
            $modifiedColumns[':p' . $index++]  = '`NOMBRE_COMPLETO`';
        }
        if ($this->isColumnModified(UsuarioPeer::TIPO_USUARIO)) {
            $modifiedColumns[':p' . $index++]  = '`TIPO_USUARIO`';
        }
        if ($this->isColumnModified(UsuarioPeer::CLAVE)) {
            $modifiedColumns[':p' . $index++]  = '`CLAVE`';
        }
        if ($this->isColumnModified(UsuarioPeer::CORREO)) {
            $modifiedColumns[':p' . $index++]  = '`CORREO`';
        }
        if ($this->isColumnModified(UsuarioPeer::ESTADO)) {
            $modifiedColumns[':p' . $index++]  = '`ESTADO`';
        }
        if ($this->isColumnModified(UsuarioPeer::IMAGEN)) {
            $modifiedColumns[':p' . $index++]  = '`IMAGEN`';
        }
        if ($this->isColumnModified(UsuarioPeer::ADMINISTRADOR)) {
            $modifiedColumns[':p' . $index++]  = '`ADMINISTRADOR`';
        }
        if ($this->isColumnModified(UsuarioPeer::ULTIMO_INGRESO)) {
            $modifiedColumns[':p' . $index++]  = '`ULTIMO_INGRESO`';
        }
        if ($this->isColumnModified(UsuarioPeer::TOKEN)) {
            $modifiedColumns[':p' . $index++]  = '`TOKEN`';
        }
        if ($this->isColumnModified(UsuarioPeer::ACTIVO)) {
            $modifiedColumns[':p' . $index++]  = '`ACTIVO`';
        }
        if ($this->isColumnModified(UsuarioPeer::VALIDADO)) {
            $modifiedColumns[':p' . $index++]  = '`VALIDADO`';
        }
        if ($this->isColumnModified(UsuarioPeer::NIVEL_USUARIO)) {
            $modifiedColumns[':p' . $index++]  = '`NIVEL_USUARIO`';
        }
        if ($this->isColumnModified(UsuarioPeer::IP)) {
            $modifiedColumns[':p' . $index++]  = '`IP`';
        }
        if ($this->isColumnModified(UsuarioPeer::TIENDA_ID)) {
            $modifiedColumns[':p' . $index++]  = '`TIENDA_ID`';
        }
        if ($this->isColumnModified(UsuarioPeer::EMPRESA_ID)) {
            $modifiedColumns[':p' . $index++]  = '`EMPRESA_ID`';
        }
        if ($this->isColumnModified(UsuarioPeer::VENDEDOR_ID)) {
            $modifiedColumns[':p' . $index++]  = '`VENDEDOR_ID`';
        }

        $sql = sprintf(
            'INSERT INTO `usuario` (%s) VALUES (%s)',
            implode(', ', $modifiedColumns),
            implode(', ', array_keys($modifiedColumns))
        );

        try {
            $stmt = $con->prepare($sql);
            foreach ($modifiedColumns as $identifier => $columnName) {
                switch ($columnName) {
                    case '`ID`':
                        $stmt->bindValue($identifier, $this->id, PDO::PARAM_INT);
                        break;
                    case '`USUARIO`':
                        $stmt->bindValue($identifier, $this->usuario, PDO::PARAM_STR);
                        break;
                    case '`NOMBRE_COMPLETO`':
                        $stmt->bindValue($identifier, $this->nombre_completo, PDO::PARAM_STR);
                        break;
                    case '`TIPO_USUARIO`':
                        $stmt->bindValue($identifier, $this->tipo_usuario, PDO::PARAM_STR);
                        break;
                    case '`CLAVE`':
                        $stmt->bindValue($identifier, $this->clave, PDO::PARAM_STR);
                        break;
                    case '`CORREO`':
                        $stmt->bindValue($identifier, $this->correo, PDO::PARAM_STR);
                        break;
                    case '`ESTADO`':
                        $stmt->bindValue($identifier, $this->estado, PDO::PARAM_STR);
                        break;
                    case '`IMAGEN`':
                        $stmt->bindValue($identifier, $this->imagen, PDO::PARAM_STR);
                        break;
                    case '`ADMINISTRADOR`':
                        $stmt->bindValue($identifier, (int) $this->administrador, PDO::PARAM_INT);
                        break;
                    case '`ULTIMO_INGRESO`':
                        $stmt->bindValue($identifier, $this->ultimo_ingreso, PDO::PARAM_STR);
                        break;
                    case '`TOKEN`':
                        $stmt->bindValue($identifier, $this->token, PDO::PARAM_STR);
                        break;
                    case '`ACTIVO`':
                        $stmt->bindValue($identifier, (int) $this->activo, PDO::PARAM_INT);
                        break;
                    case '`VALIDADO`':
                        $stmt->bindValue($identifier, (int) $this->validado, PDO::PARAM_INT);
                        break;
                    case '`NIVEL_USUARIO`':
                        $stmt->bindValue($identifier, $this->nivel_usuario, PDO::PARAM_STR);
                        break;
                    case '`IP`':
                        $stmt->bindValue($identifier, $this->ip, PDO::PARAM_STR);
                        break;
                    case '`TIENDA_ID`':
                        $stmt->bindValue($identifier, $this->tienda_id, PDO::PARAM_INT);
                        break;
                    case '`EMPRESA_ID`':
                        $stmt->bindValue($identifier, $this->empresa_id, PDO::PARAM_INT);
                        break;
                    case '`VENDEDOR_ID`':
                        $stmt->bindValue($identifier, $this->vendedor_id, PDO::PARAM_INT);
                        break;
                }
            }
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute INSERT statement [%s]', $sql), $e);
        }

        try {
            $pk = $con->lastInsertId();
        } catch (Exception $e) {
            throw new PropelException('Unable to get autoincrement id.', $e);
        }
        $this->setId($pk);

        $this->setNew(false);
    }

    /**
     * Update the row in the database.
     *
     * @param PropelPDO $con
     *
     * @see        doSave()
     */
    protected function doUpdate(PropelPDO $con)
    {
        $selectCriteria = $this->buildPkeyCriteria();
        $valuesCriteria = $this->buildCriteria();
        BasePeer::doUpdate($selectCriteria, $valuesCriteria, $con);
    }

    /**
     * Array of ValidationFailed objects.
     * @var        array ValidationFailed[]
     */
    protected $validationFailures = array();

    /**
     * Gets any ValidationFailed objects that resulted from last call to validate().
     *
     *
     * @return array ValidationFailed[]
     * @see        validate()
     */
    public function getValidationFailures()
    {
        return $this->validationFailures;
    }

    /**
     * Validates the objects modified field values and all objects related to this table.
     *
     * If $columns is either a column name or an array of column names
     * only those columns are validated.
     *
     * @param mixed $columns Column name or an array of column names.
     * @return boolean Whether all columns pass validation.
     * @see        doValidate()
     * @see        getValidationFailures()
     */
    public function validate($columns = null)
    {
        $res = $this->doValidate($columns);
        if ($res === true) {
            $this->validationFailures = array();

            return true;
        } else {
            $this->validationFailures = $res;

            return false;
        }
    }

    /**
     * This function performs the validation work for complex object models.
     *
     * In addition to checking the current object, all related objects will
     * also be validated.  If all pass then <code>true</code> is returned; otherwise
     * an aggreagated array of ValidationFailed objects will be returned.
     *
     * @param array $columns Array of column names to validate.
     * @return mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
     */
    protected function doValidate($columns = null)
    {
        if (!$this->alreadyInValidation) {
            $this->alreadyInValidation = true;
            $retval = null;

            $failureMap = array();


            // We call the validate method on the following object(s) if they
            // were passed to this object by their coresponding set
            // method.  This object relates to these object(s) by a
            // foreign key reference.

            if ($this->aTienda !== null) {
                if (!$this->aTienda->validate($columns)) {
                    $failureMap = array_merge($failureMap, $this->aTienda->getValidationFailures());
                }
            }

            if ($this->aEmpresa !== null) {
                if (!$this->aEmpresa->validate($columns)) {
                    $failureMap = array_merge($failureMap, $this->aEmpresa->getValidationFailures());
                }
            }

            if ($this->aVendedor !== null) {
                if (!$this->aVendedor->validate($columns)) {
                    $failureMap = array_merge($failureMap, $this->aVendedor->getValidationFailures());
                }
            }


            if (($retval = UsuarioPeer::doValidate($this, $columns)) !== true) {
                $failureMap = array_merge($failureMap, $retval);
            }


                if ($this->collUsuarioTiendas !== null) {
                    foreach ($this->collUsuarioTiendas as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collUsuarioEmpresas !== null) {
                    foreach ($this->collUsuarioEmpresas as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collUsuarioPerfils !== null) {
                    foreach ($this->collUsuarioPerfils as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collIngresoProductoPros !== null) {
                    foreach ($this->collIngresoProductoPros as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collSalidaProductos !== null) {
                    foreach ($this->collSalidaProductos as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collSalidaProductoDetalles !== null) {
                    foreach ($this->collSalidaProductoDetalles as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collUsuarioCorteValors !== null) {
                    foreach ($this->collUsuarioCorteValors as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collIngresoProductos !== null) {
                    foreach ($this->collIngresoProductos as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collPrecioProductos !== null) {
                    foreach ($this->collPrecioProductos as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }


            $this->alreadyInValidation = false;
        }

        return (!empty($failureMap) ? $failureMap : true);
    }

    /**
     * Retrieves a field from the object by name passed in as a string.
     *
     * @param string $name name
     * @param string $type The type of fieldname the $name is of:
     *               one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *               BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *               Defaults to BasePeer::TYPE_PHPNAME
     * @return mixed Value of field.
     */
    public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = UsuarioPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
        $field = $this->getByPosition($pos);

        return $field;
    }

    /**
     * Retrieves a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @return mixed Value of field at $pos
     */
    public function getByPosition($pos)
    {
        switch ($pos) {
            case 0:
                return $this->getId();
                break;
            case 1:
                return $this->getUsuario();
                break;
            case 2:
                return $this->getNombreCompleto();
                break;
            case 3:
                return $this->getTipoUsuario();
                break;
            case 4:
                return $this->getClave();
                break;
            case 5:
                return $this->getCorreo();
                break;
            case 6:
                return $this->getEstado();
                break;
            case 7:
                return $this->getImagen();
                break;
            case 8:
                return $this->getAdministrador();
                break;
            case 9:
                return $this->getUltimoIngreso();
                break;
            case 10:
                return $this->getToken();
                break;
            case 11:
                return $this->getActivo();
                break;
            case 12:
                return $this->getValidado();
                break;
            case 13:
                return $this->getNivelUsuario();
                break;
            case 14:
                return $this->getIp();
                break;
            case 15:
                return $this->getTiendaId();
                break;
            case 16:
                return $this->getEmpresaId();
                break;
            case 17:
                return $this->getVendedorId();
                break;
            default:
                return null;
                break;
        } // switch()
    }

    /**
     * Exports the object as an array.
     *
     * You can specify the key type of the array by passing one of the class
     * type constants.
     *
     * @param     string  $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     *                    BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                    Defaults to BasePeer::TYPE_PHPNAME.
     * @param     boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns. Defaults to true.
     * @param     array $alreadyDumpedObjects List of objects to skip to avoid recursion
     * @param     boolean $includeForeignObjects (optional) Whether to include hydrated related objects. Default to FALSE.
     *
     * @return array an associative array containing the field names (as keys) and field values
     */
    public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true, $alreadyDumpedObjects = array(), $includeForeignObjects = false)
    {
        if (isset($alreadyDumpedObjects['Usuario'][$this->getPrimaryKey()])) {
            return '*RECURSION*';
        }
        $alreadyDumpedObjects['Usuario'][$this->getPrimaryKey()] = true;
        $keys = UsuarioPeer::getFieldNames($keyType);
        $result = array(
            $keys[0] => $this->getId(),
            $keys[1] => $this->getUsuario(),
            $keys[2] => $this->getNombreCompleto(),
            $keys[3] => $this->getTipoUsuario(),
            $keys[4] => $this->getClave(),
            $keys[5] => $this->getCorreo(),
            $keys[6] => $this->getEstado(),
            $keys[7] => $this->getImagen(),
            $keys[8] => $this->getAdministrador(),
            $keys[9] => $this->getUltimoIngreso(),
            $keys[10] => $this->getToken(),
            $keys[11] => $this->getActivo(),
            $keys[12] => $this->getValidado(),
            $keys[13] => $this->getNivelUsuario(),
            $keys[14] => $this->getIp(),
            $keys[15] => $this->getTiendaId(),
            $keys[16] => $this->getEmpresaId(),
            $keys[17] => $this->getVendedorId(),
        );
        if ($includeForeignObjects) {
            if (null !== $this->aTienda) {
                $result['Tienda'] = $this->aTienda->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
            }
            if (null !== $this->aEmpresa) {
                $result['Empresa'] = $this->aEmpresa->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
            }
            if (null !== $this->aVendedor) {
                $result['Vendedor'] = $this->aVendedor->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
            }
            if (null !== $this->collUsuarioTiendas) {
                $result['UsuarioTiendas'] = $this->collUsuarioTiendas->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collUsuarioEmpresas) {
                $result['UsuarioEmpresas'] = $this->collUsuarioEmpresas->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collUsuarioPerfils) {
                $result['UsuarioPerfils'] = $this->collUsuarioPerfils->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collIngresoProductoPros) {
                $result['IngresoProductoPros'] = $this->collIngresoProductoPros->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collSalidaProductos) {
                $result['SalidaProductos'] = $this->collSalidaProductos->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collSalidaProductoDetalles) {
                $result['SalidaProductoDetalles'] = $this->collSalidaProductoDetalles->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collUsuarioCorteValors) {
                $result['UsuarioCorteValors'] = $this->collUsuarioCorteValors->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collIngresoProductos) {
                $result['IngresoProductos'] = $this->collIngresoProductos->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collPrecioProductos) {
                $result['PrecioProductos'] = $this->collPrecioProductos->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
        }

        return $result;
    }

    /**
     * Sets a field from the object by name passed in as a string.
     *
     * @param string $name peer name
     * @param mixed $value field value
     * @param string $type The type of fieldname the $name is of:
     *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                     Defaults to BasePeer::TYPE_PHPNAME
     * @return void
     */
    public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = UsuarioPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);

        $this->setByPosition($pos, $value);
    }

    /**
     * Sets a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @param mixed $value field value
     * @return void
     */
    public function setByPosition($pos, $value)
    {
        switch ($pos) {
            case 0:
                $this->setId($value);
                break;
            case 1:
                $this->setUsuario($value);
                break;
            case 2:
                $this->setNombreCompleto($value);
                break;
            case 3:
                $this->setTipoUsuario($value);
                break;
            case 4:
                $this->setClave($value);
                break;
            case 5:
                $this->setCorreo($value);
                break;
            case 6:
                $this->setEstado($value);
                break;
            case 7:
                $this->setImagen($value);
                break;
            case 8:
                $this->setAdministrador($value);
                break;
            case 9:
                $this->setUltimoIngreso($value);
                break;
            case 10:
                $this->setToken($value);
                break;
            case 11:
                $this->setActivo($value);
                break;
            case 12:
                $this->setValidado($value);
                break;
            case 13:
                $this->setNivelUsuario($value);
                break;
            case 14:
                $this->setIp($value);
                break;
            case 15:
                $this->setTiendaId($value);
                break;
            case 16:
                $this->setEmpresaId($value);
                break;
            case 17:
                $this->setVendedorId($value);
                break;
        } // switch()
    }

    /**
     * Populates the object using an array.
     *
     * This is particularly useful when populating an object from one of the
     * request arrays (e.g. $_POST).  This method goes through the column
     * names, checking to see whether a matching key exists in populated
     * array. If so the setByName() method is called for that column.
     *
     * You can specify the key type of the array by additionally passing one
     * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     * The default key type is the column's BasePeer::TYPE_PHPNAME
     *
     * @param array  $arr     An array to populate the object from.
     * @param string $keyType The type of keys the array uses.
     * @return void
     */
    public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
    {
        $keys = UsuarioPeer::getFieldNames($keyType);

        if (array_key_exists($keys[0], $arr)) $this->setId($arr[$keys[0]]);
        if (array_key_exists($keys[1], $arr)) $this->setUsuario($arr[$keys[1]]);
        if (array_key_exists($keys[2], $arr)) $this->setNombreCompleto($arr[$keys[2]]);
        if (array_key_exists($keys[3], $arr)) $this->setTipoUsuario($arr[$keys[3]]);
        if (array_key_exists($keys[4], $arr)) $this->setClave($arr[$keys[4]]);
        if (array_key_exists($keys[5], $arr)) $this->setCorreo($arr[$keys[5]]);
        if (array_key_exists($keys[6], $arr)) $this->setEstado($arr[$keys[6]]);
        if (array_key_exists($keys[7], $arr)) $this->setImagen($arr[$keys[7]]);
        if (array_key_exists($keys[8], $arr)) $this->setAdministrador($arr[$keys[8]]);
        if (array_key_exists($keys[9], $arr)) $this->setUltimoIngreso($arr[$keys[9]]);
        if (array_key_exists($keys[10], $arr)) $this->setToken($arr[$keys[10]]);
        if (array_key_exists($keys[11], $arr)) $this->setActivo($arr[$keys[11]]);
        if (array_key_exists($keys[12], $arr)) $this->setValidado($arr[$keys[12]]);
        if (array_key_exists($keys[13], $arr)) $this->setNivelUsuario($arr[$keys[13]]);
        if (array_key_exists($keys[14], $arr)) $this->setIp($arr[$keys[14]]);
        if (array_key_exists($keys[15], $arr)) $this->setTiendaId($arr[$keys[15]]);
        if (array_key_exists($keys[16], $arr)) $this->setEmpresaId($arr[$keys[16]]);
        if (array_key_exists($keys[17], $arr)) $this->setVendedorId($arr[$keys[17]]);
    }

    /**
     * Build a Criteria object containing the values of all modified columns in this object.
     *
     * @return Criteria The Criteria object containing all modified values.
     */
    public function buildCriteria()
    {
        $criteria = new Criteria(UsuarioPeer::DATABASE_NAME);

        if ($this->isColumnModified(UsuarioPeer::ID)) $criteria->add(UsuarioPeer::ID, $this->id);
        if ($this->isColumnModified(UsuarioPeer::USUARIO)) $criteria->add(UsuarioPeer::USUARIO, $this->usuario);
        if ($this->isColumnModified(UsuarioPeer::NOMBRE_COMPLETO)) $criteria->add(UsuarioPeer::NOMBRE_COMPLETO, $this->nombre_completo);
        if ($this->isColumnModified(UsuarioPeer::TIPO_USUARIO)) $criteria->add(UsuarioPeer::TIPO_USUARIO, $this->tipo_usuario);
        if ($this->isColumnModified(UsuarioPeer::CLAVE)) $criteria->add(UsuarioPeer::CLAVE, $this->clave);
        if ($this->isColumnModified(UsuarioPeer::CORREO)) $criteria->add(UsuarioPeer::CORREO, $this->correo);
        if ($this->isColumnModified(UsuarioPeer::ESTADO)) $criteria->add(UsuarioPeer::ESTADO, $this->estado);
        if ($this->isColumnModified(UsuarioPeer::IMAGEN)) $criteria->add(UsuarioPeer::IMAGEN, $this->imagen);
        if ($this->isColumnModified(UsuarioPeer::ADMINISTRADOR)) $criteria->add(UsuarioPeer::ADMINISTRADOR, $this->administrador);
        if ($this->isColumnModified(UsuarioPeer::ULTIMO_INGRESO)) $criteria->add(UsuarioPeer::ULTIMO_INGRESO, $this->ultimo_ingreso);
        if ($this->isColumnModified(UsuarioPeer::TOKEN)) $criteria->add(UsuarioPeer::TOKEN, $this->token);
        if ($this->isColumnModified(UsuarioPeer::ACTIVO)) $criteria->add(UsuarioPeer::ACTIVO, $this->activo);
        if ($this->isColumnModified(UsuarioPeer::VALIDADO)) $criteria->add(UsuarioPeer::VALIDADO, $this->validado);
        if ($this->isColumnModified(UsuarioPeer::NIVEL_USUARIO)) $criteria->add(UsuarioPeer::NIVEL_USUARIO, $this->nivel_usuario);
        if ($this->isColumnModified(UsuarioPeer::IP)) $criteria->add(UsuarioPeer::IP, $this->ip);
        if ($this->isColumnModified(UsuarioPeer::TIENDA_ID)) $criteria->add(UsuarioPeer::TIENDA_ID, $this->tienda_id);
        if ($this->isColumnModified(UsuarioPeer::EMPRESA_ID)) $criteria->add(UsuarioPeer::EMPRESA_ID, $this->empresa_id);
        if ($this->isColumnModified(UsuarioPeer::VENDEDOR_ID)) $criteria->add(UsuarioPeer::VENDEDOR_ID, $this->vendedor_id);

        return $criteria;
    }

    /**
     * Builds a Criteria object containing the primary key for this object.
     *
     * Unlike buildCriteria() this method includes the primary key values regardless
     * of whether or not they have been modified.
     *
     * @return Criteria The Criteria object containing value(s) for primary key(s).
     */
    public function buildPkeyCriteria()
    {
        $criteria = new Criteria(UsuarioPeer::DATABASE_NAME);
        $criteria->add(UsuarioPeer::ID, $this->id);

        return $criteria;
    }

    /**
     * Returns the primary key for this object (row).
     * @return int
     */
    public function getPrimaryKey()
    {
        return $this->getId();
    }

    /**
     * Generic method to set the primary key (id column).
     *
     * @param  int $key Primary key.
     * @return void
     */
    public function setPrimaryKey($key)
    {
        $this->setId($key);
    }

    /**
     * Returns true if the primary key for this object is null.
     * @return boolean
     */
    public function isPrimaryKeyNull()
    {

        return null === $this->getId();
    }

    /**
     * Sets contents of passed object to values from current object.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param object $copyObj An object of Usuario (or compatible) type.
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @param boolean $makeNew Whether to reset autoincrement PKs and make the object new.
     * @throws PropelException
     */
    public function copyInto($copyObj, $deepCopy = false, $makeNew = true)
    {
        $copyObj->setUsuario($this->getUsuario());
        $copyObj->setNombreCompleto($this->getNombreCompleto());
        $copyObj->setTipoUsuario($this->getTipoUsuario());
        $copyObj->setClave($this->getClave());
        $copyObj->setCorreo($this->getCorreo());
        $copyObj->setEstado($this->getEstado());
        $copyObj->setImagen($this->getImagen());
        $copyObj->setAdministrador($this->getAdministrador());
        $copyObj->setUltimoIngreso($this->getUltimoIngreso());
        $copyObj->setToken($this->getToken());
        $copyObj->setActivo($this->getActivo());
        $copyObj->setValidado($this->getValidado());
        $copyObj->setNivelUsuario($this->getNivelUsuario());
        $copyObj->setIp($this->getIp());
        $copyObj->setTiendaId($this->getTiendaId());
        $copyObj->setEmpresaId($this->getEmpresaId());
        $copyObj->setVendedorId($this->getVendedorId());

        if ($deepCopy && !$this->startCopy) {
            // important: temporarily setNew(false) because this affects the behavior of
            // the getter/setter methods for fkey referrer objects.
            $copyObj->setNew(false);
            // store object hash to prevent cycle
            $this->startCopy = true;

            foreach ($this->getUsuarioTiendas() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addUsuarioTienda($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getUsuarioEmpresas() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addUsuarioEmpresa($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getUsuarioPerfils() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addUsuarioPerfil($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getIngresoProductoPros() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addIngresoProductoPro($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getSalidaProductos() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addSalidaProducto($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getSalidaProductoDetalles() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addSalidaProductoDetalle($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getUsuarioCorteValors() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addUsuarioCorteValor($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getIngresoProductos() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addIngresoProducto($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getPrecioProductos() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addPrecioProducto($relObj->copy($deepCopy));
                }
            }

            //unflag object copy
            $this->startCopy = false;
        } // if ($deepCopy)

        if ($makeNew) {
            $copyObj->setNew(true);
            $copyObj->setId(NULL); // this is a auto-increment column, so set to default value
        }
    }

    /**
     * Makes a copy of this object that will be inserted as a new row in table when saved.
     * It creates a new object filling in the simple attributes, but skipping any primary
     * keys that are defined for the table.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @return Usuario Clone of current object.
     * @throws PropelException
     */
    public function copy($deepCopy = false)
    {
        // we use get_class(), because this might be a subclass
        $clazz = get_class($this);
        $copyObj = new $clazz();
        $this->copyInto($copyObj, $deepCopy);

        return $copyObj;
    }

    /**
     * Returns a peer instance associated with this om.
     *
     * Since Peer classes are not to have any instance attributes, this method returns the
     * same instance for all member of this class. The method could therefore
     * be static, but this would prevent one from overriding the behavior.
     *
     * @return UsuarioPeer
     */
    public function getPeer()
    {
        if (self::$peer === null) {
            self::$peer = new UsuarioPeer();
        }

        return self::$peer;
    }

    /**
     * Declares an association between this object and a Tienda object.
     *
     * @param             Tienda $v
     * @return Usuario The current object (for fluent API support)
     * @throws PropelException
     */
    public function setTienda(Tienda $v = null)
    {
        if ($v === null) {
            $this->setTiendaId(NULL);
        } else {
            $this->setTiendaId($v->getId());
        }

        $this->aTienda = $v;

        // Add binding for other direction of this n:n relationship.
        // If this object has already been added to the Tienda object, it will not be re-added.
        if ($v !== null) {
            $v->addUsuario($this);
        }


        return $this;
    }


    /**
     * Get the associated Tienda object
     *
     * @param PropelPDO $con Optional Connection object.
     * @return Tienda The associated Tienda object.
     * @throws PropelException
     */
    public function getTienda(PropelPDO $con = null)
    {
        if ($this->aTienda === null && ($this->tienda_id !== null)) {
            $this->aTienda = TiendaQuery::create()->findPk($this->tienda_id, $con);
            /* The following can be used additionally to
                guarantee the related object contains a reference
                to this object.  This level of coupling may, however, be
                undesirable since it could result in an only partially populated collection
                in the referenced object.
                $this->aTienda->addUsuarios($this);
             */
        }

        return $this->aTienda;
    }

    /**
     * Declares an association between this object and a Empresa object.
     *
     * @param             Empresa $v
     * @return Usuario The current object (for fluent API support)
     * @throws PropelException
     */
    public function setEmpresa(Empresa $v = null)
    {
        if ($v === null) {
            $this->setEmpresaId(NULL);
        } else {
            $this->setEmpresaId($v->getId());
        }

        $this->aEmpresa = $v;

        // Add binding for other direction of this n:n relationship.
        // If this object has already been added to the Empresa object, it will not be re-added.
        if ($v !== null) {
            $v->addUsuario($this);
        }


        return $this;
    }


    /**
     * Get the associated Empresa object
     *
     * @param PropelPDO $con Optional Connection object.
     * @return Empresa The associated Empresa object.
     * @throws PropelException
     */
    public function getEmpresa(PropelPDO $con = null)
    {
        if ($this->aEmpresa === null && ($this->empresa_id !== null)) {
            $this->aEmpresa = EmpresaQuery::create()->findPk($this->empresa_id, $con);
            /* The following can be used additionally to
                guarantee the related object contains a reference
                to this object.  This level of coupling may, however, be
                undesirable since it could result in an only partially populated collection
                in the referenced object.
                $this->aEmpresa->addUsuarios($this);
             */
        }

        return $this->aEmpresa;
    }

    /**
     * Declares an association between this object and a Vendedor object.
     *
     * @param             Vendedor $v
     * @return Usuario The current object (for fluent API support)
     * @throws PropelException
     */
    public function setVendedor(Vendedor $v = null)
    {
        if ($v === null) {
            $this->setVendedorId(NULL);
        } else {
            $this->setVendedorId($v->getId());
        }

        $this->aVendedor = $v;

        // Add binding for other direction of this n:n relationship.
        // If this object has already been added to the Vendedor object, it will not be re-added.
        if ($v !== null) {
            $v->addUsuario($this);
        }


        return $this;
    }


    /**
     * Get the associated Vendedor object
     *
     * @param PropelPDO $con Optional Connection object.
     * @return Vendedor The associated Vendedor object.
     * @throws PropelException
     */
    public function getVendedor(PropelPDO $con = null)
    {
        if ($this->aVendedor === null && ($this->vendedor_id !== null)) {
            $this->aVendedor = VendedorQuery::create()->findPk($this->vendedor_id, $con);
            /* The following can be used additionally to
                guarantee the related object contains a reference
                to this object.  This level of coupling may, however, be
                undesirable since it could result in an only partially populated collection
                in the referenced object.
                $this->aVendedor->addUsuarios($this);
             */
        }

        return $this->aVendedor;
    }


    /**
     * Initializes a collection based on the name of a relation.
     * Avoids crafting an 'init[$relationName]s' method name
     * that wouldn't work when StandardEnglishPluralizer is used.
     *
     * @param string $relationName The name of the relation to initialize
     * @return void
     */
    public function initRelation($relationName)
    {
        if ('UsuarioTienda' == $relationName) {
            $this->initUsuarioTiendas();
        }
        if ('UsuarioEmpresa' == $relationName) {
            $this->initUsuarioEmpresas();
        }
        if ('UsuarioPerfil' == $relationName) {
            $this->initUsuarioPerfils();
        }
        if ('IngresoProductoPro' == $relationName) {
            $this->initIngresoProductoPros();
        }
        if ('SalidaProducto' == $relationName) {
            $this->initSalidaProductos();
        }
        if ('SalidaProductoDetalle' == $relationName) {
            $this->initSalidaProductoDetalles();
        }
        if ('UsuarioCorteValor' == $relationName) {
            $this->initUsuarioCorteValors();
        }
        if ('IngresoProducto' == $relationName) {
            $this->initIngresoProductos();
        }
        if ('PrecioProducto' == $relationName) {
            $this->initPrecioProductos();
        }
    }

    /**
     * Clears out the collUsuarioTiendas collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return void
     * @see        addUsuarioTiendas()
     */
    public function clearUsuarioTiendas()
    {
        $this->collUsuarioTiendas = null; // important to set this to null since that means it is uninitialized
        $this->collUsuarioTiendasPartial = null;
    }

    /**
     * reset is the collUsuarioTiendas collection loaded partially
     *
     * @return void
     */
    public function resetPartialUsuarioTiendas($v = true)
    {
        $this->collUsuarioTiendasPartial = $v;
    }

    /**
     * Initializes the collUsuarioTiendas collection.
     *
     * By default this just sets the collUsuarioTiendas collection to an empty array (like clearcollUsuarioTiendas());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initUsuarioTiendas($overrideExisting = true)
    {
        if (null !== $this->collUsuarioTiendas && !$overrideExisting) {
            return;
        }
        $this->collUsuarioTiendas = new PropelObjectCollection();
        $this->collUsuarioTiendas->setModel('UsuarioTienda');
    }

    /**
     * Gets an array of UsuarioTienda objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this Usuario is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|UsuarioTienda[] List of UsuarioTienda objects
     * @throws PropelException
     */
    public function getUsuarioTiendas($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collUsuarioTiendasPartial && !$this->isNew();
        if (null === $this->collUsuarioTiendas || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collUsuarioTiendas) {
                // return empty collection
                $this->initUsuarioTiendas();
            } else {
                $collUsuarioTiendas = UsuarioTiendaQuery::create(null, $criteria)
                    ->filterByUsuario($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collUsuarioTiendasPartial && count($collUsuarioTiendas)) {
                      $this->initUsuarioTiendas(false);

                      foreach($collUsuarioTiendas as $obj) {
                        if (false == $this->collUsuarioTiendas->contains($obj)) {
                          $this->collUsuarioTiendas->append($obj);
                        }
                      }

                      $this->collUsuarioTiendasPartial = true;
                    }

                    return $collUsuarioTiendas;
                }

                if($partial && $this->collUsuarioTiendas) {
                    foreach($this->collUsuarioTiendas as $obj) {
                        if($obj->isNew()) {
                            $collUsuarioTiendas[] = $obj;
                        }
                    }
                }

                $this->collUsuarioTiendas = $collUsuarioTiendas;
                $this->collUsuarioTiendasPartial = false;
            }
        }

        return $this->collUsuarioTiendas;
    }

    /**
     * Sets a collection of UsuarioTienda objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $usuarioTiendas A Propel collection.
     * @param PropelPDO $con Optional connection object
     */
    public function setUsuarioTiendas(PropelCollection $usuarioTiendas, PropelPDO $con = null)
    {
        $this->usuarioTiendasScheduledForDeletion = $this->getUsuarioTiendas(new Criteria(), $con)->diff($usuarioTiendas);

        foreach ($this->usuarioTiendasScheduledForDeletion as $usuarioTiendaRemoved) {
            $usuarioTiendaRemoved->setUsuario(null);
        }

        $this->collUsuarioTiendas = null;
        foreach ($usuarioTiendas as $usuarioTienda) {
            $this->addUsuarioTienda($usuarioTienda);
        }

        $this->collUsuarioTiendas = $usuarioTiendas;
        $this->collUsuarioTiendasPartial = false;
    }

    /**
     * Returns the number of related UsuarioTienda objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related UsuarioTienda objects.
     * @throws PropelException
     */
    public function countUsuarioTiendas(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collUsuarioTiendasPartial && !$this->isNew();
        if (null === $this->collUsuarioTiendas || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collUsuarioTiendas) {
                return 0;
            } else {
                if($partial && !$criteria) {
                    return count($this->getUsuarioTiendas());
                }
                $query = UsuarioTiendaQuery::create(null, $criteria);
                if ($distinct) {
                    $query->distinct();
                }

                return $query
                    ->filterByUsuario($this)
                    ->count($con);
            }
        } else {
            return count($this->collUsuarioTiendas);
        }
    }

    /**
     * Method called to associate a UsuarioTienda object to this object
     * through the UsuarioTienda foreign key attribute.
     *
     * @param    UsuarioTienda $l UsuarioTienda
     * @return Usuario The current object (for fluent API support)
     */
    public function addUsuarioTienda(UsuarioTienda $l)
    {
        if ($this->collUsuarioTiendas === null) {
            $this->initUsuarioTiendas();
            $this->collUsuarioTiendasPartial = true;
        }
        if (!$this->collUsuarioTiendas->contains($l)) { // only add it if the **same** object is not already associated
            $this->doAddUsuarioTienda($l);
        }

        return $this;
    }

    /**
     * @param	UsuarioTienda $usuarioTienda The usuarioTienda object to add.
     */
    protected function doAddUsuarioTienda($usuarioTienda)
    {
        $this->collUsuarioTiendas[]= $usuarioTienda;
        $usuarioTienda->setUsuario($this);
    }

    /**
     * @param	UsuarioTienda $usuarioTienda The usuarioTienda object to remove.
     */
    public function removeUsuarioTienda($usuarioTienda)
    {
        if ($this->getUsuarioTiendas()->contains($usuarioTienda)) {
            $this->collUsuarioTiendas->remove($this->collUsuarioTiendas->search($usuarioTienda));
            if (null === $this->usuarioTiendasScheduledForDeletion) {
                $this->usuarioTiendasScheduledForDeletion = clone $this->collUsuarioTiendas;
                $this->usuarioTiendasScheduledForDeletion->clear();
            }
            $this->usuarioTiendasScheduledForDeletion[]= $usuarioTienda;
            $usuarioTienda->setUsuario(null);
        }
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Usuario is new, it will return
     * an empty collection; or if this Usuario has previously
     * been saved, it will retrieve related UsuarioTiendas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Usuario.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|UsuarioTienda[] List of UsuarioTienda objects
     */
    public function getUsuarioTiendasJoinTienda($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = UsuarioTiendaQuery::create(null, $criteria);
        $query->joinWith('Tienda', $join_behavior);

        return $this->getUsuarioTiendas($query, $con);
    }

    /**
     * Clears out the collUsuarioEmpresas collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return void
     * @see        addUsuarioEmpresas()
     */
    public function clearUsuarioEmpresas()
    {
        $this->collUsuarioEmpresas = null; // important to set this to null since that means it is uninitialized
        $this->collUsuarioEmpresasPartial = null;
    }

    /**
     * reset is the collUsuarioEmpresas collection loaded partially
     *
     * @return void
     */
    public function resetPartialUsuarioEmpresas($v = true)
    {
        $this->collUsuarioEmpresasPartial = $v;
    }

    /**
     * Initializes the collUsuarioEmpresas collection.
     *
     * By default this just sets the collUsuarioEmpresas collection to an empty array (like clearcollUsuarioEmpresas());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initUsuarioEmpresas($overrideExisting = true)
    {
        if (null !== $this->collUsuarioEmpresas && !$overrideExisting) {
            return;
        }
        $this->collUsuarioEmpresas = new PropelObjectCollection();
        $this->collUsuarioEmpresas->setModel('UsuarioEmpresa');
    }

    /**
     * Gets an array of UsuarioEmpresa objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this Usuario is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|UsuarioEmpresa[] List of UsuarioEmpresa objects
     * @throws PropelException
     */
    public function getUsuarioEmpresas($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collUsuarioEmpresasPartial && !$this->isNew();
        if (null === $this->collUsuarioEmpresas || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collUsuarioEmpresas) {
                // return empty collection
                $this->initUsuarioEmpresas();
            } else {
                $collUsuarioEmpresas = UsuarioEmpresaQuery::create(null, $criteria)
                    ->filterByUsuario($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collUsuarioEmpresasPartial && count($collUsuarioEmpresas)) {
                      $this->initUsuarioEmpresas(false);

                      foreach($collUsuarioEmpresas as $obj) {
                        if (false == $this->collUsuarioEmpresas->contains($obj)) {
                          $this->collUsuarioEmpresas->append($obj);
                        }
                      }

                      $this->collUsuarioEmpresasPartial = true;
                    }

                    return $collUsuarioEmpresas;
                }

                if($partial && $this->collUsuarioEmpresas) {
                    foreach($this->collUsuarioEmpresas as $obj) {
                        if($obj->isNew()) {
                            $collUsuarioEmpresas[] = $obj;
                        }
                    }
                }

                $this->collUsuarioEmpresas = $collUsuarioEmpresas;
                $this->collUsuarioEmpresasPartial = false;
            }
        }

        return $this->collUsuarioEmpresas;
    }

    /**
     * Sets a collection of UsuarioEmpresa objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $usuarioEmpresas A Propel collection.
     * @param PropelPDO $con Optional connection object
     */
    public function setUsuarioEmpresas(PropelCollection $usuarioEmpresas, PropelPDO $con = null)
    {
        $this->usuarioEmpresasScheduledForDeletion = $this->getUsuarioEmpresas(new Criteria(), $con)->diff($usuarioEmpresas);

        foreach ($this->usuarioEmpresasScheduledForDeletion as $usuarioEmpresaRemoved) {
            $usuarioEmpresaRemoved->setUsuario(null);
        }

        $this->collUsuarioEmpresas = null;
        foreach ($usuarioEmpresas as $usuarioEmpresa) {
            $this->addUsuarioEmpresa($usuarioEmpresa);
        }

        $this->collUsuarioEmpresas = $usuarioEmpresas;
        $this->collUsuarioEmpresasPartial = false;
    }

    /**
     * Returns the number of related UsuarioEmpresa objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related UsuarioEmpresa objects.
     * @throws PropelException
     */
    public function countUsuarioEmpresas(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collUsuarioEmpresasPartial && !$this->isNew();
        if (null === $this->collUsuarioEmpresas || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collUsuarioEmpresas) {
                return 0;
            } else {
                if($partial && !$criteria) {
                    return count($this->getUsuarioEmpresas());
                }
                $query = UsuarioEmpresaQuery::create(null, $criteria);
                if ($distinct) {
                    $query->distinct();
                }

                return $query
                    ->filterByUsuario($this)
                    ->count($con);
            }
        } else {
            return count($this->collUsuarioEmpresas);
        }
    }

    /**
     * Method called to associate a UsuarioEmpresa object to this object
     * through the UsuarioEmpresa foreign key attribute.
     *
     * @param    UsuarioEmpresa $l UsuarioEmpresa
     * @return Usuario The current object (for fluent API support)
     */
    public function addUsuarioEmpresa(UsuarioEmpresa $l)
    {
        if ($this->collUsuarioEmpresas === null) {
            $this->initUsuarioEmpresas();
            $this->collUsuarioEmpresasPartial = true;
        }
        if (!$this->collUsuarioEmpresas->contains($l)) { // only add it if the **same** object is not already associated
            $this->doAddUsuarioEmpresa($l);
        }

        return $this;
    }

    /**
     * @param	UsuarioEmpresa $usuarioEmpresa The usuarioEmpresa object to add.
     */
    protected function doAddUsuarioEmpresa($usuarioEmpresa)
    {
        $this->collUsuarioEmpresas[]= $usuarioEmpresa;
        $usuarioEmpresa->setUsuario($this);
    }

    /**
     * @param	UsuarioEmpresa $usuarioEmpresa The usuarioEmpresa object to remove.
     */
    public function removeUsuarioEmpresa($usuarioEmpresa)
    {
        if ($this->getUsuarioEmpresas()->contains($usuarioEmpresa)) {
            $this->collUsuarioEmpresas->remove($this->collUsuarioEmpresas->search($usuarioEmpresa));
            if (null === $this->usuarioEmpresasScheduledForDeletion) {
                $this->usuarioEmpresasScheduledForDeletion = clone $this->collUsuarioEmpresas;
                $this->usuarioEmpresasScheduledForDeletion->clear();
            }
            $this->usuarioEmpresasScheduledForDeletion[]= $usuarioEmpresa;
            $usuarioEmpresa->setUsuario(null);
        }
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Usuario is new, it will return
     * an empty collection; or if this Usuario has previously
     * been saved, it will retrieve related UsuarioEmpresas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Usuario.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|UsuarioEmpresa[] List of UsuarioEmpresa objects
     */
    public function getUsuarioEmpresasJoinEmpresa($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = UsuarioEmpresaQuery::create(null, $criteria);
        $query->joinWith('Empresa', $join_behavior);

        return $this->getUsuarioEmpresas($query, $con);
    }

    /**
     * Clears out the collUsuarioPerfils collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return void
     * @see        addUsuarioPerfils()
     */
    public function clearUsuarioPerfils()
    {
        $this->collUsuarioPerfils = null; // important to set this to null since that means it is uninitialized
        $this->collUsuarioPerfilsPartial = null;
    }

    /**
     * reset is the collUsuarioPerfils collection loaded partially
     *
     * @return void
     */
    public function resetPartialUsuarioPerfils($v = true)
    {
        $this->collUsuarioPerfilsPartial = $v;
    }

    /**
     * Initializes the collUsuarioPerfils collection.
     *
     * By default this just sets the collUsuarioPerfils collection to an empty array (like clearcollUsuarioPerfils());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initUsuarioPerfils($overrideExisting = true)
    {
        if (null !== $this->collUsuarioPerfils && !$overrideExisting) {
            return;
        }
        $this->collUsuarioPerfils = new PropelObjectCollection();
        $this->collUsuarioPerfils->setModel('UsuarioPerfil');
    }

    /**
     * Gets an array of UsuarioPerfil objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this Usuario is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|UsuarioPerfil[] List of UsuarioPerfil objects
     * @throws PropelException
     */
    public function getUsuarioPerfils($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collUsuarioPerfilsPartial && !$this->isNew();
        if (null === $this->collUsuarioPerfils || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collUsuarioPerfils) {
                // return empty collection
                $this->initUsuarioPerfils();
            } else {
                $collUsuarioPerfils = UsuarioPerfilQuery::create(null, $criteria)
                    ->filterByUsuario($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collUsuarioPerfilsPartial && count($collUsuarioPerfils)) {
                      $this->initUsuarioPerfils(false);

                      foreach($collUsuarioPerfils as $obj) {
                        if (false == $this->collUsuarioPerfils->contains($obj)) {
                          $this->collUsuarioPerfils->append($obj);
                        }
                      }

                      $this->collUsuarioPerfilsPartial = true;
                    }

                    return $collUsuarioPerfils;
                }

                if($partial && $this->collUsuarioPerfils) {
                    foreach($this->collUsuarioPerfils as $obj) {
                        if($obj->isNew()) {
                            $collUsuarioPerfils[] = $obj;
                        }
                    }
                }

                $this->collUsuarioPerfils = $collUsuarioPerfils;
                $this->collUsuarioPerfilsPartial = false;
            }
        }

        return $this->collUsuarioPerfils;
    }

    /**
     * Sets a collection of UsuarioPerfil objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $usuarioPerfils A Propel collection.
     * @param PropelPDO $con Optional connection object
     */
    public function setUsuarioPerfils(PropelCollection $usuarioPerfils, PropelPDO $con = null)
    {
        $this->usuarioPerfilsScheduledForDeletion = $this->getUsuarioPerfils(new Criteria(), $con)->diff($usuarioPerfils);

        foreach ($this->usuarioPerfilsScheduledForDeletion as $usuarioPerfilRemoved) {
            $usuarioPerfilRemoved->setUsuario(null);
        }

        $this->collUsuarioPerfils = null;
        foreach ($usuarioPerfils as $usuarioPerfil) {
            $this->addUsuarioPerfil($usuarioPerfil);
        }

        $this->collUsuarioPerfils = $usuarioPerfils;
        $this->collUsuarioPerfilsPartial = false;
    }

    /**
     * Returns the number of related UsuarioPerfil objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related UsuarioPerfil objects.
     * @throws PropelException
     */
    public function countUsuarioPerfils(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collUsuarioPerfilsPartial && !$this->isNew();
        if (null === $this->collUsuarioPerfils || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collUsuarioPerfils) {
                return 0;
            } else {
                if($partial && !$criteria) {
                    return count($this->getUsuarioPerfils());
                }
                $query = UsuarioPerfilQuery::create(null, $criteria);
                if ($distinct) {
                    $query->distinct();
                }

                return $query
                    ->filterByUsuario($this)
                    ->count($con);
            }
        } else {
            return count($this->collUsuarioPerfils);
        }
    }

    /**
     * Method called to associate a UsuarioPerfil object to this object
     * through the UsuarioPerfil foreign key attribute.
     *
     * @param    UsuarioPerfil $l UsuarioPerfil
     * @return Usuario The current object (for fluent API support)
     */
    public function addUsuarioPerfil(UsuarioPerfil $l)
    {
        if ($this->collUsuarioPerfils === null) {
            $this->initUsuarioPerfils();
            $this->collUsuarioPerfilsPartial = true;
        }
        if (!$this->collUsuarioPerfils->contains($l)) { // only add it if the **same** object is not already associated
            $this->doAddUsuarioPerfil($l);
        }

        return $this;
    }

    /**
     * @param	UsuarioPerfil $usuarioPerfil The usuarioPerfil object to add.
     */
    protected function doAddUsuarioPerfil($usuarioPerfil)
    {
        $this->collUsuarioPerfils[]= $usuarioPerfil;
        $usuarioPerfil->setUsuario($this);
    }

    /**
     * @param	UsuarioPerfil $usuarioPerfil The usuarioPerfil object to remove.
     */
    public function removeUsuarioPerfil($usuarioPerfil)
    {
        if ($this->getUsuarioPerfils()->contains($usuarioPerfil)) {
            $this->collUsuarioPerfils->remove($this->collUsuarioPerfils->search($usuarioPerfil));
            if (null === $this->usuarioPerfilsScheduledForDeletion) {
                $this->usuarioPerfilsScheduledForDeletion = clone $this->collUsuarioPerfils;
                $this->usuarioPerfilsScheduledForDeletion->clear();
            }
            $this->usuarioPerfilsScheduledForDeletion[]= $usuarioPerfil;
            $usuarioPerfil->setUsuario(null);
        }
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Usuario is new, it will return
     * an empty collection; or if this Usuario has previously
     * been saved, it will retrieve related UsuarioPerfils from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Usuario.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|UsuarioPerfil[] List of UsuarioPerfil objects
     */
    public function getUsuarioPerfilsJoinPerfil($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = UsuarioPerfilQuery::create(null, $criteria);
        $query->joinWith('Perfil', $join_behavior);

        return $this->getUsuarioPerfils($query, $con);
    }

    /**
     * Clears out the collIngresoProductoPros collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return void
     * @see        addIngresoProductoPros()
     */
    public function clearIngresoProductoPros()
    {
        $this->collIngresoProductoPros = null; // important to set this to null since that means it is uninitialized
        $this->collIngresoProductoProsPartial = null;
    }

    /**
     * reset is the collIngresoProductoPros collection loaded partially
     *
     * @return void
     */
    public function resetPartialIngresoProductoPros($v = true)
    {
        $this->collIngresoProductoProsPartial = $v;
    }

    /**
     * Initializes the collIngresoProductoPros collection.
     *
     * By default this just sets the collIngresoProductoPros collection to an empty array (like clearcollIngresoProductoPros());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initIngresoProductoPros($overrideExisting = true)
    {
        if (null !== $this->collIngresoProductoPros && !$overrideExisting) {
            return;
        }
        $this->collIngresoProductoPros = new PropelObjectCollection();
        $this->collIngresoProductoPros->setModel('IngresoProductoPro');
    }

    /**
     * Gets an array of IngresoProductoPro objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this Usuario is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|IngresoProductoPro[] List of IngresoProductoPro objects
     * @throws PropelException
     */
    public function getIngresoProductoPros($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collIngresoProductoProsPartial && !$this->isNew();
        if (null === $this->collIngresoProductoPros || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collIngresoProductoPros) {
                // return empty collection
                $this->initIngresoProductoPros();
            } else {
                $collIngresoProductoPros = IngresoProductoProQuery::create(null, $criteria)
                    ->filterByUsuario($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collIngresoProductoProsPartial && count($collIngresoProductoPros)) {
                      $this->initIngresoProductoPros(false);

                      foreach($collIngresoProductoPros as $obj) {
                        if (false == $this->collIngresoProductoPros->contains($obj)) {
                          $this->collIngresoProductoPros->append($obj);
                        }
                      }

                      $this->collIngresoProductoProsPartial = true;
                    }

                    return $collIngresoProductoPros;
                }

                if($partial && $this->collIngresoProductoPros) {
                    foreach($this->collIngresoProductoPros as $obj) {
                        if($obj->isNew()) {
                            $collIngresoProductoPros[] = $obj;
                        }
                    }
                }

                $this->collIngresoProductoPros = $collIngresoProductoPros;
                $this->collIngresoProductoProsPartial = false;
            }
        }

        return $this->collIngresoProductoPros;
    }

    /**
     * Sets a collection of IngresoProductoPro objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $ingresoProductoPros A Propel collection.
     * @param PropelPDO $con Optional connection object
     */
    public function setIngresoProductoPros(PropelCollection $ingresoProductoPros, PropelPDO $con = null)
    {
        $this->ingresoProductoProsScheduledForDeletion = $this->getIngresoProductoPros(new Criteria(), $con)->diff($ingresoProductoPros);

        foreach ($this->ingresoProductoProsScheduledForDeletion as $ingresoProductoProRemoved) {
            $ingresoProductoProRemoved->setUsuario(null);
        }

        $this->collIngresoProductoPros = null;
        foreach ($ingresoProductoPros as $ingresoProductoPro) {
            $this->addIngresoProductoPro($ingresoProductoPro);
        }

        $this->collIngresoProductoPros = $ingresoProductoPros;
        $this->collIngresoProductoProsPartial = false;
    }

    /**
     * Returns the number of related IngresoProductoPro objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related IngresoProductoPro objects.
     * @throws PropelException
     */
    public function countIngresoProductoPros(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collIngresoProductoProsPartial && !$this->isNew();
        if (null === $this->collIngresoProductoPros || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collIngresoProductoPros) {
                return 0;
            } else {
                if($partial && !$criteria) {
                    return count($this->getIngresoProductoPros());
                }
                $query = IngresoProductoProQuery::create(null, $criteria);
                if ($distinct) {
                    $query->distinct();
                }

                return $query
                    ->filterByUsuario($this)
                    ->count($con);
            }
        } else {
            return count($this->collIngresoProductoPros);
        }
    }

    /**
     * Method called to associate a IngresoProductoPro object to this object
     * through the IngresoProductoPro foreign key attribute.
     *
     * @param    IngresoProductoPro $l IngresoProductoPro
     * @return Usuario The current object (for fluent API support)
     */
    public function addIngresoProductoPro(IngresoProductoPro $l)
    {
        if ($this->collIngresoProductoPros === null) {
            $this->initIngresoProductoPros();
            $this->collIngresoProductoProsPartial = true;
        }
        if (!$this->collIngresoProductoPros->contains($l)) { // only add it if the **same** object is not already associated
            $this->doAddIngresoProductoPro($l);
        }

        return $this;
    }

    /**
     * @param	IngresoProductoPro $ingresoProductoPro The ingresoProductoPro object to add.
     */
    protected function doAddIngresoProductoPro($ingresoProductoPro)
    {
        $this->collIngresoProductoPros[]= $ingresoProductoPro;
        $ingresoProductoPro->setUsuario($this);
    }

    /**
     * @param	IngresoProductoPro $ingresoProductoPro The ingresoProductoPro object to remove.
     */
    public function removeIngresoProductoPro($ingresoProductoPro)
    {
        if ($this->getIngresoProductoPros()->contains($ingresoProductoPro)) {
            $this->collIngresoProductoPros->remove($this->collIngresoProductoPros->search($ingresoProductoPro));
            if (null === $this->ingresoProductoProsScheduledForDeletion) {
                $this->ingresoProductoProsScheduledForDeletion = clone $this->collIngresoProductoPros;
                $this->ingresoProductoProsScheduledForDeletion->clear();
            }
            $this->ingresoProductoProsScheduledForDeletion[]= $ingresoProductoPro;
            $ingresoProductoPro->setUsuario(null);
        }
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Usuario is new, it will return
     * an empty collection; or if this Usuario has previously
     * been saved, it will retrieve related IngresoProductoPros from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Usuario.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|IngresoProductoPro[] List of IngresoProductoPro objects
     */
    public function getIngresoProductoProsJoinProveedor($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = IngresoProductoProQuery::create(null, $criteria);
        $query->joinWith('Proveedor', $join_behavior);

        return $this->getIngresoProductoPros($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Usuario is new, it will return
     * an empty collection; or if this Usuario has previously
     * been saved, it will retrieve related IngresoProductoPros from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Usuario.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|IngresoProductoPro[] List of IngresoProductoPro objects
     */
    public function getIngresoProductoProsJoinTienda($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = IngresoProductoProQuery::create(null, $criteria);
        $query->joinWith('Tienda', $join_behavior);

        return $this->getIngresoProductoPros($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Usuario is new, it will return
     * an empty collection; or if this Usuario has previously
     * been saved, it will retrieve related IngresoProductoPros from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Usuario.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|IngresoProductoPro[] List of IngresoProductoPro objects
     */
    public function getIngresoProductoProsJoinEmpresa($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = IngresoProductoProQuery::create(null, $criteria);
        $query->joinWith('Empresa', $join_behavior);

        return $this->getIngresoProductoPros($query, $con);
    }

    /**
     * Clears out the collSalidaProductos collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return void
     * @see        addSalidaProductos()
     */
    public function clearSalidaProductos()
    {
        $this->collSalidaProductos = null; // important to set this to null since that means it is uninitialized
        $this->collSalidaProductosPartial = null;
    }

    /**
     * reset is the collSalidaProductos collection loaded partially
     *
     * @return void
     */
    public function resetPartialSalidaProductos($v = true)
    {
        $this->collSalidaProductosPartial = $v;
    }

    /**
     * Initializes the collSalidaProductos collection.
     *
     * By default this just sets the collSalidaProductos collection to an empty array (like clearcollSalidaProductos());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initSalidaProductos($overrideExisting = true)
    {
        if (null !== $this->collSalidaProductos && !$overrideExisting) {
            return;
        }
        $this->collSalidaProductos = new PropelObjectCollection();
        $this->collSalidaProductos->setModel('SalidaProducto');
    }

    /**
     * Gets an array of SalidaProducto objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this Usuario is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|SalidaProducto[] List of SalidaProducto objects
     * @throws PropelException
     */
    public function getSalidaProductos($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collSalidaProductosPartial && !$this->isNew();
        if (null === $this->collSalidaProductos || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collSalidaProductos) {
                // return empty collection
                $this->initSalidaProductos();
            } else {
                $collSalidaProductos = SalidaProductoQuery::create(null, $criteria)
                    ->filterByUsuario($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collSalidaProductosPartial && count($collSalidaProductos)) {
                      $this->initSalidaProductos(false);

                      foreach($collSalidaProductos as $obj) {
                        if (false == $this->collSalidaProductos->contains($obj)) {
                          $this->collSalidaProductos->append($obj);
                        }
                      }

                      $this->collSalidaProductosPartial = true;
                    }

                    return $collSalidaProductos;
                }

                if($partial && $this->collSalidaProductos) {
                    foreach($this->collSalidaProductos as $obj) {
                        if($obj->isNew()) {
                            $collSalidaProductos[] = $obj;
                        }
                    }
                }

                $this->collSalidaProductos = $collSalidaProductos;
                $this->collSalidaProductosPartial = false;
            }
        }

        return $this->collSalidaProductos;
    }

    /**
     * Sets a collection of SalidaProducto objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $salidaProductos A Propel collection.
     * @param PropelPDO $con Optional connection object
     */
    public function setSalidaProductos(PropelCollection $salidaProductos, PropelPDO $con = null)
    {
        $this->salidaProductosScheduledForDeletion = $this->getSalidaProductos(new Criteria(), $con)->diff($salidaProductos);

        foreach ($this->salidaProductosScheduledForDeletion as $salidaProductoRemoved) {
            $salidaProductoRemoved->setUsuario(null);
        }

        $this->collSalidaProductos = null;
        foreach ($salidaProductos as $salidaProducto) {
            $this->addSalidaProducto($salidaProducto);
        }

        $this->collSalidaProductos = $salidaProductos;
        $this->collSalidaProductosPartial = false;
    }

    /**
     * Returns the number of related SalidaProducto objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related SalidaProducto objects.
     * @throws PropelException
     */
    public function countSalidaProductos(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collSalidaProductosPartial && !$this->isNew();
        if (null === $this->collSalidaProductos || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collSalidaProductos) {
                return 0;
            } else {
                if($partial && !$criteria) {
                    return count($this->getSalidaProductos());
                }
                $query = SalidaProductoQuery::create(null, $criteria);
                if ($distinct) {
                    $query->distinct();
                }

                return $query
                    ->filterByUsuario($this)
                    ->count($con);
            }
        } else {
            return count($this->collSalidaProductos);
        }
    }

    /**
     * Method called to associate a SalidaProducto object to this object
     * through the SalidaProducto foreign key attribute.
     *
     * @param    SalidaProducto $l SalidaProducto
     * @return Usuario The current object (for fluent API support)
     */
    public function addSalidaProducto(SalidaProducto $l)
    {
        if ($this->collSalidaProductos === null) {
            $this->initSalidaProductos();
            $this->collSalidaProductosPartial = true;
        }
        if (!$this->collSalidaProductos->contains($l)) { // only add it if the **same** object is not already associated
            $this->doAddSalidaProducto($l);
        }

        return $this;
    }

    /**
     * @param	SalidaProducto $salidaProducto The salidaProducto object to add.
     */
    protected function doAddSalidaProducto($salidaProducto)
    {
        $this->collSalidaProductos[]= $salidaProducto;
        $salidaProducto->setUsuario($this);
    }

    /**
     * @param	SalidaProducto $salidaProducto The salidaProducto object to remove.
     */
    public function removeSalidaProducto($salidaProducto)
    {
        if ($this->getSalidaProductos()->contains($salidaProducto)) {
            $this->collSalidaProductos->remove($this->collSalidaProductos->search($salidaProducto));
            if (null === $this->salidaProductosScheduledForDeletion) {
                $this->salidaProductosScheduledForDeletion = clone $this->collSalidaProductos;
                $this->salidaProductosScheduledForDeletion->clear();
            }
            $this->salidaProductosScheduledForDeletion[]= $salidaProducto;
            $salidaProducto->setUsuario(null);
        }
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Usuario is new, it will return
     * an empty collection; or if this Usuario has previously
     * been saved, it will retrieve related SalidaProductos from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Usuario.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|SalidaProducto[] List of SalidaProducto objects
     */
    public function getSalidaProductosJoinTienda($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = SalidaProductoQuery::create(null, $criteria);
        $query->joinWith('Tienda', $join_behavior);

        return $this->getSalidaProductos($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Usuario is new, it will return
     * an empty collection; or if this Usuario has previously
     * been saved, it will retrieve related SalidaProductos from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Usuario.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|SalidaProducto[] List of SalidaProducto objects
     */
    public function getSalidaProductosJoinEmpresa($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = SalidaProductoQuery::create(null, $criteria);
        $query->joinWith('Empresa', $join_behavior);

        return $this->getSalidaProductos($query, $con);
    }

    /**
     * Clears out the collSalidaProductoDetalles collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return void
     * @see        addSalidaProductoDetalles()
     */
    public function clearSalidaProductoDetalles()
    {
        $this->collSalidaProductoDetalles = null; // important to set this to null since that means it is uninitialized
        $this->collSalidaProductoDetallesPartial = null;
    }

    /**
     * reset is the collSalidaProductoDetalles collection loaded partially
     *
     * @return void
     */
    public function resetPartialSalidaProductoDetalles($v = true)
    {
        $this->collSalidaProductoDetallesPartial = $v;
    }

    /**
     * Initializes the collSalidaProductoDetalles collection.
     *
     * By default this just sets the collSalidaProductoDetalles collection to an empty array (like clearcollSalidaProductoDetalles());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initSalidaProductoDetalles($overrideExisting = true)
    {
        if (null !== $this->collSalidaProductoDetalles && !$overrideExisting) {
            return;
        }
        $this->collSalidaProductoDetalles = new PropelObjectCollection();
        $this->collSalidaProductoDetalles->setModel('SalidaProductoDetalle');
    }

    /**
     * Gets an array of SalidaProductoDetalle objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this Usuario is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|SalidaProductoDetalle[] List of SalidaProductoDetalle objects
     * @throws PropelException
     */
    public function getSalidaProductoDetalles($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collSalidaProductoDetallesPartial && !$this->isNew();
        if (null === $this->collSalidaProductoDetalles || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collSalidaProductoDetalles) {
                // return empty collection
                $this->initSalidaProductoDetalles();
            } else {
                $collSalidaProductoDetalles = SalidaProductoDetalleQuery::create(null, $criteria)
                    ->filterByUsuario($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collSalidaProductoDetallesPartial && count($collSalidaProductoDetalles)) {
                      $this->initSalidaProductoDetalles(false);

                      foreach($collSalidaProductoDetalles as $obj) {
                        if (false == $this->collSalidaProductoDetalles->contains($obj)) {
                          $this->collSalidaProductoDetalles->append($obj);
                        }
                      }

                      $this->collSalidaProductoDetallesPartial = true;
                    }

                    return $collSalidaProductoDetalles;
                }

                if($partial && $this->collSalidaProductoDetalles) {
                    foreach($this->collSalidaProductoDetalles as $obj) {
                        if($obj->isNew()) {
                            $collSalidaProductoDetalles[] = $obj;
                        }
                    }
                }

                $this->collSalidaProductoDetalles = $collSalidaProductoDetalles;
                $this->collSalidaProductoDetallesPartial = false;
            }
        }

        return $this->collSalidaProductoDetalles;
    }

    /**
     * Sets a collection of SalidaProductoDetalle objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $salidaProductoDetalles A Propel collection.
     * @param PropelPDO $con Optional connection object
     */
    public function setSalidaProductoDetalles(PropelCollection $salidaProductoDetalles, PropelPDO $con = null)
    {
        $this->salidaProductoDetallesScheduledForDeletion = $this->getSalidaProductoDetalles(new Criteria(), $con)->diff($salidaProductoDetalles);

        foreach ($this->salidaProductoDetallesScheduledForDeletion as $salidaProductoDetalleRemoved) {
            $salidaProductoDetalleRemoved->setUsuario(null);
        }

        $this->collSalidaProductoDetalles = null;
        foreach ($salidaProductoDetalles as $salidaProductoDetalle) {
            $this->addSalidaProductoDetalle($salidaProductoDetalle);
        }

        $this->collSalidaProductoDetalles = $salidaProductoDetalles;
        $this->collSalidaProductoDetallesPartial = false;
    }

    /**
     * Returns the number of related SalidaProductoDetalle objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related SalidaProductoDetalle objects.
     * @throws PropelException
     */
    public function countSalidaProductoDetalles(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collSalidaProductoDetallesPartial && !$this->isNew();
        if (null === $this->collSalidaProductoDetalles || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collSalidaProductoDetalles) {
                return 0;
            } else {
                if($partial && !$criteria) {
                    return count($this->getSalidaProductoDetalles());
                }
                $query = SalidaProductoDetalleQuery::create(null, $criteria);
                if ($distinct) {
                    $query->distinct();
                }

                return $query
                    ->filterByUsuario($this)
                    ->count($con);
            }
        } else {
            return count($this->collSalidaProductoDetalles);
        }
    }

    /**
     * Method called to associate a SalidaProductoDetalle object to this object
     * through the SalidaProductoDetalle foreign key attribute.
     *
     * @param    SalidaProductoDetalle $l SalidaProductoDetalle
     * @return Usuario The current object (for fluent API support)
     */
    public function addSalidaProductoDetalle(SalidaProductoDetalle $l)
    {
        if ($this->collSalidaProductoDetalles === null) {
            $this->initSalidaProductoDetalles();
            $this->collSalidaProductoDetallesPartial = true;
        }
        if (!$this->collSalidaProductoDetalles->contains($l)) { // only add it if the **same** object is not already associated
            $this->doAddSalidaProductoDetalle($l);
        }

        return $this;
    }

    /**
     * @param	SalidaProductoDetalle $salidaProductoDetalle The salidaProductoDetalle object to add.
     */
    protected function doAddSalidaProductoDetalle($salidaProductoDetalle)
    {
        $this->collSalidaProductoDetalles[]= $salidaProductoDetalle;
        $salidaProductoDetalle->setUsuario($this);
    }

    /**
     * @param	SalidaProductoDetalle $salidaProductoDetalle The salidaProductoDetalle object to remove.
     */
    public function removeSalidaProductoDetalle($salidaProductoDetalle)
    {
        if ($this->getSalidaProductoDetalles()->contains($salidaProductoDetalle)) {
            $this->collSalidaProductoDetalles->remove($this->collSalidaProductoDetalles->search($salidaProductoDetalle));
            if (null === $this->salidaProductoDetallesScheduledForDeletion) {
                $this->salidaProductoDetallesScheduledForDeletion = clone $this->collSalidaProductoDetalles;
                $this->salidaProductoDetallesScheduledForDeletion->clear();
            }
            $this->salidaProductoDetallesScheduledForDeletion[]= $salidaProductoDetalle;
            $salidaProductoDetalle->setUsuario(null);
        }
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Usuario is new, it will return
     * an empty collection; or if this Usuario has previously
     * been saved, it will retrieve related SalidaProductoDetalles from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Usuario.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|SalidaProductoDetalle[] List of SalidaProductoDetalle objects
     */
    public function getSalidaProductoDetallesJoinSalidaProducto($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = SalidaProductoDetalleQuery::create(null, $criteria);
        $query->joinWith('SalidaProducto', $join_behavior);

        return $this->getSalidaProductoDetalles($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Usuario is new, it will return
     * an empty collection; or if this Usuario has previously
     * been saved, it will retrieve related SalidaProductoDetalles from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Usuario.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|SalidaProductoDetalle[] List of SalidaProductoDetalle objects
     */
    public function getSalidaProductoDetallesJoinProducto($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = SalidaProductoDetalleQuery::create(null, $criteria);
        $query->joinWith('Producto', $join_behavior);

        return $this->getSalidaProductoDetalles($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Usuario is new, it will return
     * an empty collection; or if this Usuario has previously
     * been saved, it will retrieve related SalidaProductoDetalles from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Usuario.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|SalidaProductoDetalle[] List of SalidaProductoDetalle objects
     */
    public function getSalidaProductoDetallesJoinEmpresa($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = SalidaProductoDetalleQuery::create(null, $criteria);
        $query->joinWith('Empresa', $join_behavior);

        return $this->getSalidaProductoDetalles($query, $con);
    }

    /**
     * Clears out the collUsuarioCorteValors collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return void
     * @see        addUsuarioCorteValors()
     */
    public function clearUsuarioCorteValors()
    {
        $this->collUsuarioCorteValors = null; // important to set this to null since that means it is uninitialized
        $this->collUsuarioCorteValorsPartial = null;
    }

    /**
     * reset is the collUsuarioCorteValors collection loaded partially
     *
     * @return void
     */
    public function resetPartialUsuarioCorteValors($v = true)
    {
        $this->collUsuarioCorteValorsPartial = $v;
    }

    /**
     * Initializes the collUsuarioCorteValors collection.
     *
     * By default this just sets the collUsuarioCorteValors collection to an empty array (like clearcollUsuarioCorteValors());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initUsuarioCorteValors($overrideExisting = true)
    {
        if (null !== $this->collUsuarioCorteValors && !$overrideExisting) {
            return;
        }
        $this->collUsuarioCorteValors = new PropelObjectCollection();
        $this->collUsuarioCorteValors->setModel('UsuarioCorteValor');
    }

    /**
     * Gets an array of UsuarioCorteValor objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this Usuario is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|UsuarioCorteValor[] List of UsuarioCorteValor objects
     * @throws PropelException
     */
    public function getUsuarioCorteValors($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collUsuarioCorteValorsPartial && !$this->isNew();
        if (null === $this->collUsuarioCorteValors || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collUsuarioCorteValors) {
                // return empty collection
                $this->initUsuarioCorteValors();
            } else {
                $collUsuarioCorteValors = UsuarioCorteValorQuery::create(null, $criteria)
                    ->filterByUsuario($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collUsuarioCorteValorsPartial && count($collUsuarioCorteValors)) {
                      $this->initUsuarioCorteValors(false);

                      foreach($collUsuarioCorteValors as $obj) {
                        if (false == $this->collUsuarioCorteValors->contains($obj)) {
                          $this->collUsuarioCorteValors->append($obj);
                        }
                      }

                      $this->collUsuarioCorteValorsPartial = true;
                    }

                    return $collUsuarioCorteValors;
                }

                if($partial && $this->collUsuarioCorteValors) {
                    foreach($this->collUsuarioCorteValors as $obj) {
                        if($obj->isNew()) {
                            $collUsuarioCorteValors[] = $obj;
                        }
                    }
                }

                $this->collUsuarioCorteValors = $collUsuarioCorteValors;
                $this->collUsuarioCorteValorsPartial = false;
            }
        }

        return $this->collUsuarioCorteValors;
    }

    /**
     * Sets a collection of UsuarioCorteValor objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $usuarioCorteValors A Propel collection.
     * @param PropelPDO $con Optional connection object
     */
    public function setUsuarioCorteValors(PropelCollection $usuarioCorteValors, PropelPDO $con = null)
    {
        $this->usuarioCorteValorsScheduledForDeletion = $this->getUsuarioCorteValors(new Criteria(), $con)->diff($usuarioCorteValors);

        foreach ($this->usuarioCorteValorsScheduledForDeletion as $usuarioCorteValorRemoved) {
            $usuarioCorteValorRemoved->setUsuario(null);
        }

        $this->collUsuarioCorteValors = null;
        foreach ($usuarioCorteValors as $usuarioCorteValor) {
            $this->addUsuarioCorteValor($usuarioCorteValor);
        }

        $this->collUsuarioCorteValors = $usuarioCorteValors;
        $this->collUsuarioCorteValorsPartial = false;
    }

    /**
     * Returns the number of related UsuarioCorteValor objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related UsuarioCorteValor objects.
     * @throws PropelException
     */
    public function countUsuarioCorteValors(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collUsuarioCorteValorsPartial && !$this->isNew();
        if (null === $this->collUsuarioCorteValors || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collUsuarioCorteValors) {
                return 0;
            } else {
                if($partial && !$criteria) {
                    return count($this->getUsuarioCorteValors());
                }
                $query = UsuarioCorteValorQuery::create(null, $criteria);
                if ($distinct) {
                    $query->distinct();
                }

                return $query
                    ->filterByUsuario($this)
                    ->count($con);
            }
        } else {
            return count($this->collUsuarioCorteValors);
        }
    }

    /**
     * Method called to associate a UsuarioCorteValor object to this object
     * through the UsuarioCorteValor foreign key attribute.
     *
     * @param    UsuarioCorteValor $l UsuarioCorteValor
     * @return Usuario The current object (for fluent API support)
     */
    public function addUsuarioCorteValor(UsuarioCorteValor $l)
    {
        if ($this->collUsuarioCorteValors === null) {
            $this->initUsuarioCorteValors();
            $this->collUsuarioCorteValorsPartial = true;
        }
        if (!$this->collUsuarioCorteValors->contains($l)) { // only add it if the **same** object is not already associated
            $this->doAddUsuarioCorteValor($l);
        }

        return $this;
    }

    /**
     * @param	UsuarioCorteValor $usuarioCorteValor The usuarioCorteValor object to add.
     */
    protected function doAddUsuarioCorteValor($usuarioCorteValor)
    {
        $this->collUsuarioCorteValors[]= $usuarioCorteValor;
        $usuarioCorteValor->setUsuario($this);
    }

    /**
     * @param	UsuarioCorteValor $usuarioCorteValor The usuarioCorteValor object to remove.
     */
    public function removeUsuarioCorteValor($usuarioCorteValor)
    {
        if ($this->getUsuarioCorteValors()->contains($usuarioCorteValor)) {
            $this->collUsuarioCorteValors->remove($this->collUsuarioCorteValors->search($usuarioCorteValor));
            if (null === $this->usuarioCorteValorsScheduledForDeletion) {
                $this->usuarioCorteValorsScheduledForDeletion = clone $this->collUsuarioCorteValors;
                $this->usuarioCorteValorsScheduledForDeletion->clear();
            }
            $this->usuarioCorteValorsScheduledForDeletion[]= $usuarioCorteValor;
            $usuarioCorteValor->setUsuario(null);
        }
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Usuario is new, it will return
     * an empty collection; or if this Usuario has previously
     * been saved, it will retrieve related UsuarioCorteValors from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Usuario.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|UsuarioCorteValor[] List of UsuarioCorteValor objects
     */
    public function getUsuarioCorteValorsJoinBanco($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = UsuarioCorteValorQuery::create(null, $criteria);
        $query->joinWith('Banco', $join_behavior);

        return $this->getUsuarioCorteValors($query, $con);
    }

    /**
     * Clears out the collIngresoProductos collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return void
     * @see        addIngresoProductos()
     */
    public function clearIngresoProductos()
    {
        $this->collIngresoProductos = null; // important to set this to null since that means it is uninitialized
        $this->collIngresoProductosPartial = null;
    }

    /**
     * reset is the collIngresoProductos collection loaded partially
     *
     * @return void
     */
    public function resetPartialIngresoProductos($v = true)
    {
        $this->collIngresoProductosPartial = $v;
    }

    /**
     * Initializes the collIngresoProductos collection.
     *
     * By default this just sets the collIngresoProductos collection to an empty array (like clearcollIngresoProductos());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initIngresoProductos($overrideExisting = true)
    {
        if (null !== $this->collIngresoProductos && !$overrideExisting) {
            return;
        }
        $this->collIngresoProductos = new PropelObjectCollection();
        $this->collIngresoProductos->setModel('IngresoProducto');
    }

    /**
     * Gets an array of IngresoProducto objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this Usuario is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|IngresoProducto[] List of IngresoProducto objects
     * @throws PropelException
     */
    public function getIngresoProductos($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collIngresoProductosPartial && !$this->isNew();
        if (null === $this->collIngresoProductos || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collIngresoProductos) {
                // return empty collection
                $this->initIngresoProductos();
            } else {
                $collIngresoProductos = IngresoProductoQuery::create(null, $criteria)
                    ->filterByUsuario($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collIngresoProductosPartial && count($collIngresoProductos)) {
                      $this->initIngresoProductos(false);

                      foreach($collIngresoProductos as $obj) {
                        if (false == $this->collIngresoProductos->contains($obj)) {
                          $this->collIngresoProductos->append($obj);
                        }
                      }

                      $this->collIngresoProductosPartial = true;
                    }

                    return $collIngresoProductos;
                }

                if($partial && $this->collIngresoProductos) {
                    foreach($this->collIngresoProductos as $obj) {
                        if($obj->isNew()) {
                            $collIngresoProductos[] = $obj;
                        }
                    }
                }

                $this->collIngresoProductos = $collIngresoProductos;
                $this->collIngresoProductosPartial = false;
            }
        }

        return $this->collIngresoProductos;
    }

    /**
     * Sets a collection of IngresoProducto objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $ingresoProductos A Propel collection.
     * @param PropelPDO $con Optional connection object
     */
    public function setIngresoProductos(PropelCollection $ingresoProductos, PropelPDO $con = null)
    {
        $this->ingresoProductosScheduledForDeletion = $this->getIngresoProductos(new Criteria(), $con)->diff($ingresoProductos);

        foreach ($this->ingresoProductosScheduledForDeletion as $ingresoProductoRemoved) {
            $ingresoProductoRemoved->setUsuario(null);
        }

        $this->collIngresoProductos = null;
        foreach ($ingresoProductos as $ingresoProducto) {
            $this->addIngresoProducto($ingresoProducto);
        }

        $this->collIngresoProductos = $ingresoProductos;
        $this->collIngresoProductosPartial = false;
    }

    /**
     * Returns the number of related IngresoProducto objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related IngresoProducto objects.
     * @throws PropelException
     */
    public function countIngresoProductos(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collIngresoProductosPartial && !$this->isNew();
        if (null === $this->collIngresoProductos || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collIngresoProductos) {
                return 0;
            } else {
                if($partial && !$criteria) {
                    return count($this->getIngresoProductos());
                }
                $query = IngresoProductoQuery::create(null, $criteria);
                if ($distinct) {
                    $query->distinct();
                }

                return $query
                    ->filterByUsuario($this)
                    ->count($con);
            }
        } else {
            return count($this->collIngresoProductos);
        }
    }

    /**
     * Method called to associate a IngresoProducto object to this object
     * through the IngresoProducto foreign key attribute.
     *
     * @param    IngresoProducto $l IngresoProducto
     * @return Usuario The current object (for fluent API support)
     */
    public function addIngresoProducto(IngresoProducto $l)
    {
        if ($this->collIngresoProductos === null) {
            $this->initIngresoProductos();
            $this->collIngresoProductosPartial = true;
        }
        if (!$this->collIngresoProductos->contains($l)) { // only add it if the **same** object is not already associated
            $this->doAddIngresoProducto($l);
        }

        return $this;
    }

    /**
     * @param	IngresoProducto $ingresoProducto The ingresoProducto object to add.
     */
    protected function doAddIngresoProducto($ingresoProducto)
    {
        $this->collIngresoProductos[]= $ingresoProducto;
        $ingresoProducto->setUsuario($this);
    }

    /**
     * @param	IngresoProducto $ingresoProducto The ingresoProducto object to remove.
     */
    public function removeIngresoProducto($ingresoProducto)
    {
        if ($this->getIngresoProductos()->contains($ingresoProducto)) {
            $this->collIngresoProductos->remove($this->collIngresoProductos->search($ingresoProducto));
            if (null === $this->ingresoProductosScheduledForDeletion) {
                $this->ingresoProductosScheduledForDeletion = clone $this->collIngresoProductos;
                $this->ingresoProductosScheduledForDeletion->clear();
            }
            $this->ingresoProductosScheduledForDeletion[]= $ingresoProducto;
            $ingresoProducto->setUsuario(null);
        }
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Usuario is new, it will return
     * an empty collection; or if this Usuario has previously
     * been saved, it will retrieve related IngresoProductos from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Usuario.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|IngresoProducto[] List of IngresoProducto objects
     */
    public function getIngresoProductosJoinTienda($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = IngresoProductoQuery::create(null, $criteria);
        $query->joinWith('Tienda', $join_behavior);

        return $this->getIngresoProductos($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Usuario is new, it will return
     * an empty collection; or if this Usuario has previously
     * been saved, it will retrieve related IngresoProductos from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Usuario.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|IngresoProducto[] List of IngresoProducto objects
     */
    public function getIngresoProductosJoinEmpresa($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = IngresoProductoQuery::create(null, $criteria);
        $query->joinWith('Empresa', $join_behavior);

        return $this->getIngresoProductos($query, $con);
    }

    /**
     * Clears out the collPrecioProductos collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return void
     * @see        addPrecioProductos()
     */
    public function clearPrecioProductos()
    {
        $this->collPrecioProductos = null; // important to set this to null since that means it is uninitialized
        $this->collPrecioProductosPartial = null;
    }

    /**
     * reset is the collPrecioProductos collection loaded partially
     *
     * @return void
     */
    public function resetPartialPrecioProductos($v = true)
    {
        $this->collPrecioProductosPartial = $v;
    }

    /**
     * Initializes the collPrecioProductos collection.
     *
     * By default this just sets the collPrecioProductos collection to an empty array (like clearcollPrecioProductos());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initPrecioProductos($overrideExisting = true)
    {
        if (null !== $this->collPrecioProductos && !$overrideExisting) {
            return;
        }
        $this->collPrecioProductos = new PropelObjectCollection();
        $this->collPrecioProductos->setModel('PrecioProducto');
    }

    /**
     * Gets an array of PrecioProducto objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this Usuario is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|PrecioProducto[] List of PrecioProducto objects
     * @throws PropelException
     */
    public function getPrecioProductos($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collPrecioProductosPartial && !$this->isNew();
        if (null === $this->collPrecioProductos || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collPrecioProductos) {
                // return empty collection
                $this->initPrecioProductos();
            } else {
                $collPrecioProductos = PrecioProductoQuery::create(null, $criteria)
                    ->filterByUsuario($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collPrecioProductosPartial && count($collPrecioProductos)) {
                      $this->initPrecioProductos(false);

                      foreach($collPrecioProductos as $obj) {
                        if (false == $this->collPrecioProductos->contains($obj)) {
                          $this->collPrecioProductos->append($obj);
                        }
                      }

                      $this->collPrecioProductosPartial = true;
                    }

                    return $collPrecioProductos;
                }

                if($partial && $this->collPrecioProductos) {
                    foreach($this->collPrecioProductos as $obj) {
                        if($obj->isNew()) {
                            $collPrecioProductos[] = $obj;
                        }
                    }
                }

                $this->collPrecioProductos = $collPrecioProductos;
                $this->collPrecioProductosPartial = false;
            }
        }

        return $this->collPrecioProductos;
    }

    /**
     * Sets a collection of PrecioProducto objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $precioProductos A Propel collection.
     * @param PropelPDO $con Optional connection object
     */
    public function setPrecioProductos(PropelCollection $precioProductos, PropelPDO $con = null)
    {
        $this->precioProductosScheduledForDeletion = $this->getPrecioProductos(new Criteria(), $con)->diff($precioProductos);

        foreach ($this->precioProductosScheduledForDeletion as $precioProductoRemoved) {
            $precioProductoRemoved->setUsuario(null);
        }

        $this->collPrecioProductos = null;
        foreach ($precioProductos as $precioProducto) {
            $this->addPrecioProducto($precioProducto);
        }

        $this->collPrecioProductos = $precioProductos;
        $this->collPrecioProductosPartial = false;
    }

    /**
     * Returns the number of related PrecioProducto objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related PrecioProducto objects.
     * @throws PropelException
     */
    public function countPrecioProductos(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collPrecioProductosPartial && !$this->isNew();
        if (null === $this->collPrecioProductos || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collPrecioProductos) {
                return 0;
            } else {
                if($partial && !$criteria) {
                    return count($this->getPrecioProductos());
                }
                $query = PrecioProductoQuery::create(null, $criteria);
                if ($distinct) {
                    $query->distinct();
                }

                return $query
                    ->filterByUsuario($this)
                    ->count($con);
            }
        } else {
            return count($this->collPrecioProductos);
        }
    }

    /**
     * Method called to associate a PrecioProducto object to this object
     * through the PrecioProducto foreign key attribute.
     *
     * @param    PrecioProducto $l PrecioProducto
     * @return Usuario The current object (for fluent API support)
     */
    public function addPrecioProducto(PrecioProducto $l)
    {
        if ($this->collPrecioProductos === null) {
            $this->initPrecioProductos();
            $this->collPrecioProductosPartial = true;
        }
        if (!$this->collPrecioProductos->contains($l)) { // only add it if the **same** object is not already associated
            $this->doAddPrecioProducto($l);
        }

        return $this;
    }

    /**
     * @param	PrecioProducto $precioProducto The precioProducto object to add.
     */
    protected function doAddPrecioProducto($precioProducto)
    {
        $this->collPrecioProductos[]= $precioProducto;
        $precioProducto->setUsuario($this);
    }

    /**
     * @param	PrecioProducto $precioProducto The precioProducto object to remove.
     */
    public function removePrecioProducto($precioProducto)
    {
        if ($this->getPrecioProductos()->contains($precioProducto)) {
            $this->collPrecioProductos->remove($this->collPrecioProductos->search($precioProducto));
            if (null === $this->precioProductosScheduledForDeletion) {
                $this->precioProductosScheduledForDeletion = clone $this->collPrecioProductos;
                $this->precioProductosScheduledForDeletion->clear();
            }
            $this->precioProductosScheduledForDeletion[]= $precioProducto;
            $precioProducto->setUsuario(null);
        }
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Usuario is new, it will return
     * an empty collection; or if this Usuario has previously
     * been saved, it will retrieve related PrecioProductos from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Usuario.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|PrecioProducto[] List of PrecioProducto objects
     */
    public function getPrecioProductosJoinTienda($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = PrecioProductoQuery::create(null, $criteria);
        $query->joinWith('Tienda', $join_behavior);

        return $this->getPrecioProductos($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this Usuario is new, it will return
     * an empty collection; or if this Usuario has previously
     * been saved, it will retrieve related PrecioProductos from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in Usuario.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|PrecioProducto[] List of PrecioProducto objects
     */
    public function getPrecioProductosJoinEmpresa($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = PrecioProductoQuery::create(null, $criteria);
        $query->joinWith('Empresa', $join_behavior);

        return $this->getPrecioProductos($query, $con);
    }

    /**
     * Clears the current object and sets all attributes to their default values
     */
    public function clear()
    {
        $this->id = null;
        $this->usuario = null;
        $this->nombre_completo = null;
        $this->tipo_usuario = null;
        $this->clave = null;
        $this->correo = null;
        $this->estado = null;
        $this->imagen = null;
        $this->administrador = null;
        $this->ultimo_ingreso = null;
        $this->token = null;
        $this->activo = null;
        $this->validado = null;
        $this->nivel_usuario = null;
        $this->ip = null;
        $this->tienda_id = null;
        $this->empresa_id = null;
        $this->vendedor_id = null;
        $this->alreadyInSave = false;
        $this->alreadyInValidation = false;
        $this->clearAllReferences();
        $this->applyDefaultValues();
        $this->resetModified();
        $this->setNew(true);
        $this->setDeleted(false);
    }

    /**
     * Resets all references to other model objects or collections of model objects.
     *
     * This method is a user-space workaround for PHP's inability to garbage collect
     * objects with circular references (even in PHP 5.3). This is currently necessary
     * when using Propel in certain daemon or large-volumne/high-memory operations.
     *
     * @param boolean $deep Whether to also clear the references on all referrer objects.
     */
    public function clearAllReferences($deep = false)
    {
        if ($deep) {
            if ($this->collUsuarioTiendas) {
                foreach ($this->collUsuarioTiendas as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collUsuarioEmpresas) {
                foreach ($this->collUsuarioEmpresas as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collUsuarioPerfils) {
                foreach ($this->collUsuarioPerfils as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collIngresoProductoPros) {
                foreach ($this->collIngresoProductoPros as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collSalidaProductos) {
                foreach ($this->collSalidaProductos as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collSalidaProductoDetalles) {
                foreach ($this->collSalidaProductoDetalles as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collUsuarioCorteValors) {
                foreach ($this->collUsuarioCorteValors as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collIngresoProductos) {
                foreach ($this->collIngresoProductos as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collPrecioProductos) {
                foreach ($this->collPrecioProductos as $o) {
                    $o->clearAllReferences($deep);
                }
            }
        } // if ($deep)

        if ($this->collUsuarioTiendas instanceof PropelCollection) {
            $this->collUsuarioTiendas->clearIterator();
        }
        $this->collUsuarioTiendas = null;
        if ($this->collUsuarioEmpresas instanceof PropelCollection) {
            $this->collUsuarioEmpresas->clearIterator();
        }
        $this->collUsuarioEmpresas = null;
        if ($this->collUsuarioPerfils instanceof PropelCollection) {
            $this->collUsuarioPerfils->clearIterator();
        }
        $this->collUsuarioPerfils = null;
        if ($this->collIngresoProductoPros instanceof PropelCollection) {
            $this->collIngresoProductoPros->clearIterator();
        }
        $this->collIngresoProductoPros = null;
        if ($this->collSalidaProductos instanceof PropelCollection) {
            $this->collSalidaProductos->clearIterator();
        }
        $this->collSalidaProductos = null;
        if ($this->collSalidaProductoDetalles instanceof PropelCollection) {
            $this->collSalidaProductoDetalles->clearIterator();
        }
        $this->collSalidaProductoDetalles = null;
        if ($this->collUsuarioCorteValors instanceof PropelCollection) {
            $this->collUsuarioCorteValors->clearIterator();
        }
        $this->collUsuarioCorteValors = null;
        if ($this->collIngresoProductos instanceof PropelCollection) {
            $this->collIngresoProductos->clearIterator();
        }
        $this->collIngresoProductos = null;
        if ($this->collPrecioProductos instanceof PropelCollection) {
            $this->collPrecioProductos->clearIterator();
        }
        $this->collPrecioProductos = null;
        $this->aTienda = null;
        $this->aEmpresa = null;
        $this->aVendedor = null;
    }

    /**
     * return the string representation of this object
     *
     * @return string The value of the 'usuario' column
     */
    public function __toString()
    {
        return (string) $this->getUsuario();
    }

    /**
     * return true is the object is in saving state
     *
     * @return boolean
     */
    public function isAlreadyInSave()
    {
        return $this->alreadyInSave;
    }

    /**
     * Catches calls to virtual methods
     */
    public function __call($name, $params)
    {

        // symfony_behaviors behavior
        if ($callable = sfMixer::getCallable('BaseUsuario:' . $name))
        {
          array_unshift($params, $this);
          return call_user_func_array($callable, $params);
        }


        return parent::__call($name, $params);
    }

}
