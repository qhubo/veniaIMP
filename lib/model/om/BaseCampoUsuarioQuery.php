<?php


/**
 * Base class that represents a query for the 'campo_usuario' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.6.7 on:
 *
 * 12/30/25 15:40:24
 *
 * @method CampoUsuarioQuery orderById($order = Criteria::ASC) Order by the id column
 * @method CampoUsuarioQuery orderByEmpresaId($order = Criteria::ASC) Order by the empresa_id column
 * @method CampoUsuarioQuery orderByTipoDocumento($order = Criteria::ASC) Order by the tipo_documento column
 * @method CampoUsuarioQuery orderByCodigo($order = Criteria::ASC) Order by the codigo column
 * @method CampoUsuarioQuery orderByNombre($order = Criteria::ASC) Order by the nombre column
 * @method CampoUsuarioQuery orderByTipoCampo($order = Criteria::ASC) Order by the tipo_campo column
 * @method CampoUsuarioQuery orderByValores($order = Criteria::ASC) Order by the valores column
 * @method CampoUsuarioQuery orderByRequerido($order = Criteria::ASC) Order by the requerido column
 * @method CampoUsuarioQuery orderByActivo($order = Criteria::ASC) Order by the activo column
 * @method CampoUsuarioQuery orderByOrden($order = Criteria::ASC) Order by the orden column
 * @method CampoUsuarioQuery orderByTiendaId($order = Criteria::ASC) Order by the tienda_id column
 *
 * @method CampoUsuarioQuery groupById() Group by the id column
 * @method CampoUsuarioQuery groupByEmpresaId() Group by the empresa_id column
 * @method CampoUsuarioQuery groupByTipoDocumento() Group by the tipo_documento column
 * @method CampoUsuarioQuery groupByCodigo() Group by the codigo column
 * @method CampoUsuarioQuery groupByNombre() Group by the nombre column
 * @method CampoUsuarioQuery groupByTipoCampo() Group by the tipo_campo column
 * @method CampoUsuarioQuery groupByValores() Group by the valores column
 * @method CampoUsuarioQuery groupByRequerido() Group by the requerido column
 * @method CampoUsuarioQuery groupByActivo() Group by the activo column
 * @method CampoUsuarioQuery groupByOrden() Group by the orden column
 * @method CampoUsuarioQuery groupByTiendaId() Group by the tienda_id column
 *
 * @method CampoUsuarioQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method CampoUsuarioQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method CampoUsuarioQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method CampoUsuarioQuery leftJoinEmpresa($relationAlias = null) Adds a LEFT JOIN clause to the query using the Empresa relation
 * @method CampoUsuarioQuery rightJoinEmpresa($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Empresa relation
 * @method CampoUsuarioQuery innerJoinEmpresa($relationAlias = null) Adds a INNER JOIN clause to the query using the Empresa relation
 *
 * @method CampoUsuarioQuery leftJoinTienda($relationAlias = null) Adds a LEFT JOIN clause to the query using the Tienda relation
 * @method CampoUsuarioQuery rightJoinTienda($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Tienda relation
 * @method CampoUsuarioQuery innerJoinTienda($relationAlias = null) Adds a INNER JOIN clause to the query using the Tienda relation
 *
 * @method CampoUsuarioQuery leftJoinValorUsuario($relationAlias = null) Adds a LEFT JOIN clause to the query using the ValorUsuario relation
 * @method CampoUsuarioQuery rightJoinValorUsuario($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ValorUsuario relation
 * @method CampoUsuarioQuery innerJoinValorUsuario($relationAlias = null) Adds a INNER JOIN clause to the query using the ValorUsuario relation
 *
 * @method CampoUsuario findOne(PropelPDO $con = null) Return the first CampoUsuario matching the query
 * @method CampoUsuario findOneOrCreate(PropelPDO $con = null) Return the first CampoUsuario matching the query, or a new CampoUsuario object populated from the query conditions when no match is found
 *
 * @method CampoUsuario findOneById(int $id) Return the first CampoUsuario filtered by the id column
 * @method CampoUsuario findOneByEmpresaId(int $empresa_id) Return the first CampoUsuario filtered by the empresa_id column
 * @method CampoUsuario findOneByTipoDocumento(string $tipo_documento) Return the first CampoUsuario filtered by the tipo_documento column
 * @method CampoUsuario findOneByCodigo(string $codigo) Return the first CampoUsuario filtered by the codigo column
 * @method CampoUsuario findOneByNombre(string $nombre) Return the first CampoUsuario filtered by the nombre column
 * @method CampoUsuario findOneByTipoCampo(string $tipo_campo) Return the first CampoUsuario filtered by the tipo_campo column
 * @method CampoUsuario findOneByValores(string $valores) Return the first CampoUsuario filtered by the valores column
 * @method CampoUsuario findOneByRequerido(boolean $requerido) Return the first CampoUsuario filtered by the requerido column
 * @method CampoUsuario findOneByActivo(boolean $activo) Return the first CampoUsuario filtered by the activo column
 * @method CampoUsuario findOneByOrden(int $orden) Return the first CampoUsuario filtered by the orden column
 * @method CampoUsuario findOneByTiendaId(int $tienda_id) Return the first CampoUsuario filtered by the tienda_id column
 *
 * @method array findById(int $id) Return CampoUsuario objects filtered by the id column
 * @method array findByEmpresaId(int $empresa_id) Return CampoUsuario objects filtered by the empresa_id column
 * @method array findByTipoDocumento(string $tipo_documento) Return CampoUsuario objects filtered by the tipo_documento column
 * @method array findByCodigo(string $codigo) Return CampoUsuario objects filtered by the codigo column
 * @method array findByNombre(string $nombre) Return CampoUsuario objects filtered by the nombre column
 * @method array findByTipoCampo(string $tipo_campo) Return CampoUsuario objects filtered by the tipo_campo column
 * @method array findByValores(string $valores) Return CampoUsuario objects filtered by the valores column
 * @method array findByRequerido(boolean $requerido) Return CampoUsuario objects filtered by the requerido column
 * @method array findByActivo(boolean $activo) Return CampoUsuario objects filtered by the activo column
 * @method array findByOrden(int $orden) Return CampoUsuario objects filtered by the orden column
 * @method array findByTiendaId(int $tienda_id) Return CampoUsuario objects filtered by the tienda_id column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseCampoUsuarioQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseCampoUsuarioQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = 'propel', $modelName = 'CampoUsuario', $modelAlias = null)
    {
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new CampoUsuarioQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param     CampoUsuarioQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return CampoUsuarioQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof CampoUsuarioQuery) {
            return $criteria;
        }
        $query = new CampoUsuarioQuery();
        if (null !== $modelAlias) {
            $query->setModelAlias($modelAlias);
        }
        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return   CampoUsuario|CampoUsuario[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = CampoUsuarioPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is alredy in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(CampoUsuarioPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return   CampoUsuario A model object, or null if the key is not found
     * @throws   PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT `ID`, `EMPRESA_ID`, `TIPO_DOCUMENTO`, `CODIGO`, `NOMBRE`, `TIPO_CAMPO`, `VALORES`, `REQUERIDO`, `ACTIVO`, `ORDEN`, `TIENDA_ID` FROM `campo_usuario` WHERE `ID` = :p0';
        try {
            $stmt = $con->prepare($sql);
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new CampoUsuario();
            $obj->hydrate($row);
            CampoUsuarioPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return CampoUsuario|CampoUsuario[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|CampoUsuario[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return CampoUsuarioQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(CampoUsuarioPeer::ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return CampoUsuarioQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(CampoUsuarioPeer::ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the id column
     *
     * Example usage:
     * <code>
     * $query->filterById(1234); // WHERE id = 1234
     * $query->filterById(array(12, 34)); // WHERE id IN (12, 34)
     * $query->filterById(array('min' => 12)); // WHERE id > 12
     * </code>
     *
     * @param     mixed $id The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return CampoUsuarioQuery The current query, for fluid interface
     */
    public function filterById($id = null, $comparison = null)
    {
        if (is_array($id) && null === $comparison) {
            $comparison = Criteria::IN;
        }

        return $this->addUsingAlias(CampoUsuarioPeer::ID, $id, $comparison);
    }

    /**
     * Filter the query on the empresa_id column
     *
     * Example usage:
     * <code>
     * $query->filterByEmpresaId(1234); // WHERE empresa_id = 1234
     * $query->filterByEmpresaId(array(12, 34)); // WHERE empresa_id IN (12, 34)
     * $query->filterByEmpresaId(array('min' => 12)); // WHERE empresa_id > 12
     * </code>
     *
     * @see       filterByEmpresa()
     *
     * @param     mixed $empresaId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return CampoUsuarioQuery The current query, for fluid interface
     */
    public function filterByEmpresaId($empresaId = null, $comparison = null)
    {
        if (is_array($empresaId)) {
            $useMinMax = false;
            if (isset($empresaId['min'])) {
                $this->addUsingAlias(CampoUsuarioPeer::EMPRESA_ID, $empresaId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($empresaId['max'])) {
                $this->addUsingAlias(CampoUsuarioPeer::EMPRESA_ID, $empresaId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(CampoUsuarioPeer::EMPRESA_ID, $empresaId, $comparison);
    }

    /**
     * Filter the query on the tipo_documento column
     *
     * Example usage:
     * <code>
     * $query->filterByTipoDocumento('fooValue');   // WHERE tipo_documento = 'fooValue'
     * $query->filterByTipoDocumento('%fooValue%'); // WHERE tipo_documento LIKE '%fooValue%'
     * </code>
     *
     * @param     string $tipoDocumento The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return CampoUsuarioQuery The current query, for fluid interface
     */
    public function filterByTipoDocumento($tipoDocumento = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($tipoDocumento)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $tipoDocumento)) {
                $tipoDocumento = str_replace('*', '%', $tipoDocumento);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(CampoUsuarioPeer::TIPO_DOCUMENTO, $tipoDocumento, $comparison);
    }

    /**
     * Filter the query on the codigo column
     *
     * Example usage:
     * <code>
     * $query->filterByCodigo('fooValue');   // WHERE codigo = 'fooValue'
     * $query->filterByCodigo('%fooValue%'); // WHERE codigo LIKE '%fooValue%'
     * </code>
     *
     * @param     string $codigo The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return CampoUsuarioQuery The current query, for fluid interface
     */
    public function filterByCodigo($codigo = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($codigo)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $codigo)) {
                $codigo = str_replace('*', '%', $codigo);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(CampoUsuarioPeer::CODIGO, $codigo, $comparison);
    }

    /**
     * Filter the query on the nombre column
     *
     * Example usage:
     * <code>
     * $query->filterByNombre('fooValue');   // WHERE nombre = 'fooValue'
     * $query->filterByNombre('%fooValue%'); // WHERE nombre LIKE '%fooValue%'
     * </code>
     *
     * @param     string $nombre The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return CampoUsuarioQuery The current query, for fluid interface
     */
    public function filterByNombre($nombre = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($nombre)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $nombre)) {
                $nombre = str_replace('*', '%', $nombre);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(CampoUsuarioPeer::NOMBRE, $nombre, $comparison);
    }

    /**
     * Filter the query on the tipo_campo column
     *
     * Example usage:
     * <code>
     * $query->filterByTipoCampo('fooValue');   // WHERE tipo_campo = 'fooValue'
     * $query->filterByTipoCampo('%fooValue%'); // WHERE tipo_campo LIKE '%fooValue%'
     * </code>
     *
     * @param     string $tipoCampo The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return CampoUsuarioQuery The current query, for fluid interface
     */
    public function filterByTipoCampo($tipoCampo = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($tipoCampo)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $tipoCampo)) {
                $tipoCampo = str_replace('*', '%', $tipoCampo);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(CampoUsuarioPeer::TIPO_CAMPO, $tipoCampo, $comparison);
    }

    /**
     * Filter the query on the valores column
     *
     * Example usage:
     * <code>
     * $query->filterByValores('fooValue');   // WHERE valores = 'fooValue'
     * $query->filterByValores('%fooValue%'); // WHERE valores LIKE '%fooValue%'
     * </code>
     *
     * @param     string $valores The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return CampoUsuarioQuery The current query, for fluid interface
     */
    public function filterByValores($valores = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($valores)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $valores)) {
                $valores = str_replace('*', '%', $valores);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(CampoUsuarioPeer::VALORES, $valores, $comparison);
    }

    /**
     * Filter the query on the requerido column
     *
     * Example usage:
     * <code>
     * $query->filterByRequerido(true); // WHERE requerido = true
     * $query->filterByRequerido('yes'); // WHERE requerido = true
     * </code>
     *
     * @param     boolean|string $requerido The value to use as filter.
     *              Non-boolean arguments are converted using the following rules:
     *                * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *                * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     *              Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return CampoUsuarioQuery The current query, for fluid interface
     */
    public function filterByRequerido($requerido = null, $comparison = null)
    {
        if (is_string($requerido)) {
            $requerido = in_array(strtolower($requerido), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
        }

        return $this->addUsingAlias(CampoUsuarioPeer::REQUERIDO, $requerido, $comparison);
    }

    /**
     * Filter the query on the activo column
     *
     * Example usage:
     * <code>
     * $query->filterByActivo(true); // WHERE activo = true
     * $query->filterByActivo('yes'); // WHERE activo = true
     * </code>
     *
     * @param     boolean|string $activo The value to use as filter.
     *              Non-boolean arguments are converted using the following rules:
     *                * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *                * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     *              Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return CampoUsuarioQuery The current query, for fluid interface
     */
    public function filterByActivo($activo = null, $comparison = null)
    {
        if (is_string($activo)) {
            $activo = in_array(strtolower($activo), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
        }

        return $this->addUsingAlias(CampoUsuarioPeer::ACTIVO, $activo, $comparison);
    }

    /**
     * Filter the query on the orden column
     *
     * Example usage:
     * <code>
     * $query->filterByOrden(1234); // WHERE orden = 1234
     * $query->filterByOrden(array(12, 34)); // WHERE orden IN (12, 34)
     * $query->filterByOrden(array('min' => 12)); // WHERE orden > 12
     * </code>
     *
     * @param     mixed $orden The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return CampoUsuarioQuery The current query, for fluid interface
     */
    public function filterByOrden($orden = null, $comparison = null)
    {
        if (is_array($orden)) {
            $useMinMax = false;
            if (isset($orden['min'])) {
                $this->addUsingAlias(CampoUsuarioPeer::ORDEN, $orden['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($orden['max'])) {
                $this->addUsingAlias(CampoUsuarioPeer::ORDEN, $orden['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(CampoUsuarioPeer::ORDEN, $orden, $comparison);
    }

    /**
     * Filter the query on the tienda_id column
     *
     * Example usage:
     * <code>
     * $query->filterByTiendaId(1234); // WHERE tienda_id = 1234
     * $query->filterByTiendaId(array(12, 34)); // WHERE tienda_id IN (12, 34)
     * $query->filterByTiendaId(array('min' => 12)); // WHERE tienda_id > 12
     * </code>
     *
     * @see       filterByTienda()
     *
     * @param     mixed $tiendaId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return CampoUsuarioQuery The current query, for fluid interface
     */
    public function filterByTiendaId($tiendaId = null, $comparison = null)
    {
        if (is_array($tiendaId)) {
            $useMinMax = false;
            if (isset($tiendaId['min'])) {
                $this->addUsingAlias(CampoUsuarioPeer::TIENDA_ID, $tiendaId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($tiendaId['max'])) {
                $this->addUsingAlias(CampoUsuarioPeer::TIENDA_ID, $tiendaId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(CampoUsuarioPeer::TIENDA_ID, $tiendaId, $comparison);
    }

    /**
     * Filter the query by a related Empresa object
     *
     * @param   Empresa|PropelObjectCollection $empresa The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   CampoUsuarioQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByEmpresa($empresa, $comparison = null)
    {
        if ($empresa instanceof Empresa) {
            return $this
                ->addUsingAlias(CampoUsuarioPeer::EMPRESA_ID, $empresa->getId(), $comparison);
        } elseif ($empresa instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(CampoUsuarioPeer::EMPRESA_ID, $empresa->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByEmpresa() only accepts arguments of type Empresa or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Empresa relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return CampoUsuarioQuery The current query, for fluid interface
     */
    public function joinEmpresa($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Empresa');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Empresa');
        }

        return $this;
    }

    /**
     * Use the Empresa relation Empresa object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   EmpresaQuery A secondary query class using the current class as primary query
     */
    public function useEmpresaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinEmpresa($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Empresa', 'EmpresaQuery');
    }

    /**
     * Filter the query by a related Tienda object
     *
     * @param   Tienda|PropelObjectCollection $tienda The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   CampoUsuarioQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByTienda($tienda, $comparison = null)
    {
        if ($tienda instanceof Tienda) {
            return $this
                ->addUsingAlias(CampoUsuarioPeer::TIENDA_ID, $tienda->getId(), $comparison);
        } elseif ($tienda instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(CampoUsuarioPeer::TIENDA_ID, $tienda->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByTienda() only accepts arguments of type Tienda or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Tienda relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return CampoUsuarioQuery The current query, for fluid interface
     */
    public function joinTienda($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Tienda');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Tienda');
        }

        return $this;
    }

    /**
     * Use the Tienda relation Tienda object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   TiendaQuery A secondary query class using the current class as primary query
     */
    public function useTiendaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinTienda($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Tienda', 'TiendaQuery');
    }

    /**
     * Filter the query by a related ValorUsuario object
     *
     * @param   ValorUsuario|PropelObjectCollection $valorUsuario  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   CampoUsuarioQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByValorUsuario($valorUsuario, $comparison = null)
    {
        if ($valorUsuario instanceof ValorUsuario) {
            return $this
                ->addUsingAlias(CampoUsuarioPeer::ID, $valorUsuario->getCampoUsuarioId(), $comparison);
        } elseif ($valorUsuario instanceof PropelObjectCollection) {
            return $this
                ->useValorUsuarioQuery()
                ->filterByPrimaryKeys($valorUsuario->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByValorUsuario() only accepts arguments of type ValorUsuario or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ValorUsuario relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return CampoUsuarioQuery The current query, for fluid interface
     */
    public function joinValorUsuario($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ValorUsuario');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ValorUsuario');
        }

        return $this;
    }

    /**
     * Use the ValorUsuario relation ValorUsuario object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ValorUsuarioQuery A secondary query class using the current class as primary query
     */
    public function useValorUsuarioQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinValorUsuario($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ValorUsuario', 'ValorUsuarioQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   CampoUsuario $campoUsuario Object to remove from the list of results
     *
     * @return CampoUsuarioQuery The current query, for fluid interface
     */
    public function prune($campoUsuario = null)
    {
        if ($campoUsuario) {
            $this->addUsingAlias(CampoUsuarioPeer::ID, $campoUsuario->getId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}
