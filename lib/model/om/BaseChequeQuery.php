<?php


/**
 * Base class that represents a query for the 'cheque' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.6.7 on:
 *
 * 12/28/25 02:48:08
 *
 * @method ChequeQuery orderById($order = Criteria::ASC) Order by the id column
 * @method ChequeQuery orderByBancoId($order = Criteria::ASC) Order by the banco_id column
 * @method ChequeQuery orderByDocumentoChequeId($order = Criteria::ASC) Order by the documento_cheque_id column
 * @method ChequeQuery orderByProveedorId($order = Criteria::ASC) Order by the proveedor_id column
 * @method ChequeQuery orderByNumero($order = Criteria::ASC) Order by the numero column
 * @method ChequeQuery orderByBeneficiario($order = Criteria::ASC) Order by the beneficiario column
 * @method ChequeQuery orderByFechaCheque($order = Criteria::ASC) Order by the fecha_cheque column
 * @method ChequeQuery orderByValor($order = Criteria::ASC) Order by the valor column
 * @method ChequeQuery orderByMotivo($order = Criteria::ASC) Order by the motivo column
 * @method ChequeQuery orderByEstatus($order = Criteria::ASC) Order by the estatus column
 * @method ChequeQuery orderByNegociable($order = Criteria::ASC) Order by the negociable column
 * @method ChequeQuery orderByUsuario($order = Criteria::ASC) Order by the usuario column
 * @method ChequeQuery orderByFechaCreo($order = Criteria::ASC) Order by the fecha_creo column
 * @method ChequeQuery orderByOrdenDevolucionId($order = Criteria::ASC) Order by the orden_devolucion_id column
 * @method ChequeQuery orderBySolicitudChequeId($order = Criteria::ASC) Order by the solicitud_cheque_id column
 *
 * @method ChequeQuery groupById() Group by the id column
 * @method ChequeQuery groupByBancoId() Group by the banco_id column
 * @method ChequeQuery groupByDocumentoChequeId() Group by the documento_cheque_id column
 * @method ChequeQuery groupByProveedorId() Group by the proveedor_id column
 * @method ChequeQuery groupByNumero() Group by the numero column
 * @method ChequeQuery groupByBeneficiario() Group by the beneficiario column
 * @method ChequeQuery groupByFechaCheque() Group by the fecha_cheque column
 * @method ChequeQuery groupByValor() Group by the valor column
 * @method ChequeQuery groupByMotivo() Group by the motivo column
 * @method ChequeQuery groupByEstatus() Group by the estatus column
 * @method ChequeQuery groupByNegociable() Group by the negociable column
 * @method ChequeQuery groupByUsuario() Group by the usuario column
 * @method ChequeQuery groupByFechaCreo() Group by the fecha_creo column
 * @method ChequeQuery groupByOrdenDevolucionId() Group by the orden_devolucion_id column
 * @method ChequeQuery groupBySolicitudChequeId() Group by the solicitud_cheque_id column
 *
 * @method ChequeQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method ChequeQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method ChequeQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method ChequeQuery leftJoinBanco($relationAlias = null) Adds a LEFT JOIN clause to the query using the Banco relation
 * @method ChequeQuery rightJoinBanco($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Banco relation
 * @method ChequeQuery innerJoinBanco($relationAlias = null) Adds a INNER JOIN clause to the query using the Banco relation
 *
 * @method ChequeQuery leftJoinDocumentoCheque($relationAlias = null) Adds a LEFT JOIN clause to the query using the DocumentoCheque relation
 * @method ChequeQuery rightJoinDocumentoCheque($relationAlias = null) Adds a RIGHT JOIN clause to the query using the DocumentoCheque relation
 * @method ChequeQuery innerJoinDocumentoCheque($relationAlias = null) Adds a INNER JOIN clause to the query using the DocumentoCheque relation
 *
 * @method ChequeQuery leftJoinProveedor($relationAlias = null) Adds a LEFT JOIN clause to the query using the Proveedor relation
 * @method ChequeQuery rightJoinProveedor($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Proveedor relation
 * @method ChequeQuery innerJoinProveedor($relationAlias = null) Adds a INNER JOIN clause to the query using the Proveedor relation
 *
 * @method ChequeQuery leftJoinOrdenDevolucion($relationAlias = null) Adds a LEFT JOIN clause to the query using the OrdenDevolucion relation
 * @method ChequeQuery rightJoinOrdenDevolucion($relationAlias = null) Adds a RIGHT JOIN clause to the query using the OrdenDevolucion relation
 * @method ChequeQuery innerJoinOrdenDevolucion($relationAlias = null) Adds a INNER JOIN clause to the query using the OrdenDevolucion relation
 *
 * @method ChequeQuery leftJoinSolicitudCheque($relationAlias = null) Adds a LEFT JOIN clause to the query using the SolicitudCheque relation
 * @method ChequeQuery rightJoinSolicitudCheque($relationAlias = null) Adds a RIGHT JOIN clause to the query using the SolicitudCheque relation
 * @method ChequeQuery innerJoinSolicitudCheque($relationAlias = null) Adds a INNER JOIN clause to the query using the SolicitudCheque relation
 *
 * @method ChequeQuery leftJoinOrdenProveedorPago($relationAlias = null) Adds a LEFT JOIN clause to the query using the OrdenProveedorPago relation
 * @method ChequeQuery rightJoinOrdenProveedorPago($relationAlias = null) Adds a RIGHT JOIN clause to the query using the OrdenProveedorPago relation
 * @method ChequeQuery innerJoinOrdenProveedorPago($relationAlias = null) Adds a INNER JOIN clause to the query using the OrdenProveedorPago relation
 *
 * @method ChequeQuery leftJoinGastoPago($relationAlias = null) Adds a LEFT JOIN clause to the query using the GastoPago relation
 * @method ChequeQuery rightJoinGastoPago($relationAlias = null) Adds a RIGHT JOIN clause to the query using the GastoPago relation
 * @method ChequeQuery innerJoinGastoPago($relationAlias = null) Adds a INNER JOIN clause to the query using the GastoPago relation
 *
 * @method Cheque findOne(PropelPDO $con = null) Return the first Cheque matching the query
 * @method Cheque findOneOrCreate(PropelPDO $con = null) Return the first Cheque matching the query, or a new Cheque object populated from the query conditions when no match is found
 *
 * @method Cheque findOneById(int $id) Return the first Cheque filtered by the id column
 * @method Cheque findOneByBancoId(int $banco_id) Return the first Cheque filtered by the banco_id column
 * @method Cheque findOneByDocumentoChequeId(int $documento_cheque_id) Return the first Cheque filtered by the documento_cheque_id column
 * @method Cheque findOneByProveedorId(int $proveedor_id) Return the first Cheque filtered by the proveedor_id column
 * @method Cheque findOneByNumero(string $numero) Return the first Cheque filtered by the numero column
 * @method Cheque findOneByBeneficiario(string $beneficiario) Return the first Cheque filtered by the beneficiario column
 * @method Cheque findOneByFechaCheque(string $fecha_cheque) Return the first Cheque filtered by the fecha_cheque column
 * @method Cheque findOneByValor(double $valor) Return the first Cheque filtered by the valor column
 * @method Cheque findOneByMotivo(string $motivo) Return the first Cheque filtered by the motivo column
 * @method Cheque findOneByEstatus(string $estatus) Return the first Cheque filtered by the estatus column
 * @method Cheque findOneByNegociable(boolean $negociable) Return the first Cheque filtered by the negociable column
 * @method Cheque findOneByUsuario(string $usuario) Return the first Cheque filtered by the usuario column
 * @method Cheque findOneByFechaCreo(string $fecha_creo) Return the first Cheque filtered by the fecha_creo column
 * @method Cheque findOneByOrdenDevolucionId(int $orden_devolucion_id) Return the first Cheque filtered by the orden_devolucion_id column
 * @method Cheque findOneBySolicitudChequeId(int $solicitud_cheque_id) Return the first Cheque filtered by the solicitud_cheque_id column
 *
 * @method array findById(int $id) Return Cheque objects filtered by the id column
 * @method array findByBancoId(int $banco_id) Return Cheque objects filtered by the banco_id column
 * @method array findByDocumentoChequeId(int $documento_cheque_id) Return Cheque objects filtered by the documento_cheque_id column
 * @method array findByProveedorId(int $proveedor_id) Return Cheque objects filtered by the proveedor_id column
 * @method array findByNumero(string $numero) Return Cheque objects filtered by the numero column
 * @method array findByBeneficiario(string $beneficiario) Return Cheque objects filtered by the beneficiario column
 * @method array findByFechaCheque(string $fecha_cheque) Return Cheque objects filtered by the fecha_cheque column
 * @method array findByValor(double $valor) Return Cheque objects filtered by the valor column
 * @method array findByMotivo(string $motivo) Return Cheque objects filtered by the motivo column
 * @method array findByEstatus(string $estatus) Return Cheque objects filtered by the estatus column
 * @method array findByNegociable(boolean $negociable) Return Cheque objects filtered by the negociable column
 * @method array findByUsuario(string $usuario) Return Cheque objects filtered by the usuario column
 * @method array findByFechaCreo(string $fecha_creo) Return Cheque objects filtered by the fecha_creo column
 * @method array findByOrdenDevolucionId(int $orden_devolucion_id) Return Cheque objects filtered by the orden_devolucion_id column
 * @method array findBySolicitudChequeId(int $solicitud_cheque_id) Return Cheque objects filtered by the solicitud_cheque_id column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseChequeQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseChequeQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = 'propel', $modelName = 'Cheque', $modelAlias = null)
    {
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new ChequeQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param     ChequeQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return ChequeQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof ChequeQuery) {
            return $criteria;
        }
        $query = new ChequeQuery();
        if (null !== $modelAlias) {
            $query->setModelAlias($modelAlias);
        }
        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return   Cheque|Cheque[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = ChequePeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is alredy in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(ChequePeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return   Cheque A model object, or null if the key is not found
     * @throws   PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT `ID`, `BANCO_ID`, `DOCUMENTO_CHEQUE_ID`, `PROVEEDOR_ID`, `NUMERO`, `BENEFICIARIO`, `FECHA_CHEQUE`, `VALOR`, `MOTIVO`, `ESTATUS`, `NEGOCIABLE`, `USUARIO`, `FECHA_CREO`, `ORDEN_DEVOLUCION_ID`, `SOLICITUD_CHEQUE_ID` FROM `cheque` WHERE `ID` = :p0';
        try {
            $stmt = $con->prepare($sql);
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new Cheque();
            $obj->hydrate($row);
            ChequePeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return Cheque|Cheque[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|Cheque[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return ChequeQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(ChequePeer::ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return ChequeQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(ChequePeer::ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the id column
     *
     * Example usage:
     * <code>
     * $query->filterById(1234); // WHERE id = 1234
     * $query->filterById(array(12, 34)); // WHERE id IN (12, 34)
     * $query->filterById(array('min' => 12)); // WHERE id > 12
     * </code>
     *
     * @param     mixed $id The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ChequeQuery The current query, for fluid interface
     */
    public function filterById($id = null, $comparison = null)
    {
        if (is_array($id) && null === $comparison) {
            $comparison = Criteria::IN;
        }

        return $this->addUsingAlias(ChequePeer::ID, $id, $comparison);
    }

    /**
     * Filter the query on the banco_id column
     *
     * Example usage:
     * <code>
     * $query->filterByBancoId(1234); // WHERE banco_id = 1234
     * $query->filterByBancoId(array(12, 34)); // WHERE banco_id IN (12, 34)
     * $query->filterByBancoId(array('min' => 12)); // WHERE banco_id > 12
     * </code>
     *
     * @see       filterByBanco()
     *
     * @param     mixed $bancoId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ChequeQuery The current query, for fluid interface
     */
    public function filterByBancoId($bancoId = null, $comparison = null)
    {
        if (is_array($bancoId)) {
            $useMinMax = false;
            if (isset($bancoId['min'])) {
                $this->addUsingAlias(ChequePeer::BANCO_ID, $bancoId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($bancoId['max'])) {
                $this->addUsingAlias(ChequePeer::BANCO_ID, $bancoId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ChequePeer::BANCO_ID, $bancoId, $comparison);
    }

    /**
     * Filter the query on the documento_cheque_id column
     *
     * Example usage:
     * <code>
     * $query->filterByDocumentoChequeId(1234); // WHERE documento_cheque_id = 1234
     * $query->filterByDocumentoChequeId(array(12, 34)); // WHERE documento_cheque_id IN (12, 34)
     * $query->filterByDocumentoChequeId(array('min' => 12)); // WHERE documento_cheque_id > 12
     * </code>
     *
     * @see       filterByDocumentoCheque()
     *
     * @param     mixed $documentoChequeId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ChequeQuery The current query, for fluid interface
     */
    public function filterByDocumentoChequeId($documentoChequeId = null, $comparison = null)
    {
        if (is_array($documentoChequeId)) {
            $useMinMax = false;
            if (isset($documentoChequeId['min'])) {
                $this->addUsingAlias(ChequePeer::DOCUMENTO_CHEQUE_ID, $documentoChequeId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($documentoChequeId['max'])) {
                $this->addUsingAlias(ChequePeer::DOCUMENTO_CHEQUE_ID, $documentoChequeId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ChequePeer::DOCUMENTO_CHEQUE_ID, $documentoChequeId, $comparison);
    }

    /**
     * Filter the query on the proveedor_id column
     *
     * Example usage:
     * <code>
     * $query->filterByProveedorId(1234); // WHERE proveedor_id = 1234
     * $query->filterByProveedorId(array(12, 34)); // WHERE proveedor_id IN (12, 34)
     * $query->filterByProveedorId(array('min' => 12)); // WHERE proveedor_id > 12
     * </code>
     *
     * @see       filterByProveedor()
     *
     * @param     mixed $proveedorId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ChequeQuery The current query, for fluid interface
     */
    public function filterByProveedorId($proveedorId = null, $comparison = null)
    {
        if (is_array($proveedorId)) {
            $useMinMax = false;
            if (isset($proveedorId['min'])) {
                $this->addUsingAlias(ChequePeer::PROVEEDOR_ID, $proveedorId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($proveedorId['max'])) {
                $this->addUsingAlias(ChequePeer::PROVEEDOR_ID, $proveedorId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ChequePeer::PROVEEDOR_ID, $proveedorId, $comparison);
    }

    /**
     * Filter the query on the numero column
     *
     * Example usage:
     * <code>
     * $query->filterByNumero('fooValue');   // WHERE numero = 'fooValue'
     * $query->filterByNumero('%fooValue%'); // WHERE numero LIKE '%fooValue%'
     * </code>
     *
     * @param     string $numero The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ChequeQuery The current query, for fluid interface
     */
    public function filterByNumero($numero = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($numero)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $numero)) {
                $numero = str_replace('*', '%', $numero);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ChequePeer::NUMERO, $numero, $comparison);
    }

    /**
     * Filter the query on the beneficiario column
     *
     * Example usage:
     * <code>
     * $query->filterByBeneficiario('fooValue');   // WHERE beneficiario = 'fooValue'
     * $query->filterByBeneficiario('%fooValue%'); // WHERE beneficiario LIKE '%fooValue%'
     * </code>
     *
     * @param     string $beneficiario The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ChequeQuery The current query, for fluid interface
     */
    public function filterByBeneficiario($beneficiario = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($beneficiario)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $beneficiario)) {
                $beneficiario = str_replace('*', '%', $beneficiario);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ChequePeer::BENEFICIARIO, $beneficiario, $comparison);
    }

    /**
     * Filter the query on the fecha_cheque column
     *
     * Example usage:
     * <code>
     * $query->filterByFechaCheque('2011-03-14'); // WHERE fecha_cheque = '2011-03-14'
     * $query->filterByFechaCheque('now'); // WHERE fecha_cheque = '2011-03-14'
     * $query->filterByFechaCheque(array('max' => 'yesterday')); // WHERE fecha_cheque > '2011-03-13'
     * </code>
     *
     * @param     mixed $fechaCheque The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ChequeQuery The current query, for fluid interface
     */
    public function filterByFechaCheque($fechaCheque = null, $comparison = null)
    {
        if (is_array($fechaCheque)) {
            $useMinMax = false;
            if (isset($fechaCheque['min'])) {
                $this->addUsingAlias(ChequePeer::FECHA_CHEQUE, $fechaCheque['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($fechaCheque['max'])) {
                $this->addUsingAlias(ChequePeer::FECHA_CHEQUE, $fechaCheque['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ChequePeer::FECHA_CHEQUE, $fechaCheque, $comparison);
    }

    /**
     * Filter the query on the valor column
     *
     * Example usage:
     * <code>
     * $query->filterByValor(1234); // WHERE valor = 1234
     * $query->filterByValor(array(12, 34)); // WHERE valor IN (12, 34)
     * $query->filterByValor(array('min' => 12)); // WHERE valor > 12
     * </code>
     *
     * @param     mixed $valor The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ChequeQuery The current query, for fluid interface
     */
    public function filterByValor($valor = null, $comparison = null)
    {
        if (is_array($valor)) {
            $useMinMax = false;
            if (isset($valor['min'])) {
                $this->addUsingAlias(ChequePeer::VALOR, $valor['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($valor['max'])) {
                $this->addUsingAlias(ChequePeer::VALOR, $valor['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ChequePeer::VALOR, $valor, $comparison);
    }

    /**
     * Filter the query on the motivo column
     *
     * Example usage:
     * <code>
     * $query->filterByMotivo('fooValue');   // WHERE motivo = 'fooValue'
     * $query->filterByMotivo('%fooValue%'); // WHERE motivo LIKE '%fooValue%'
     * </code>
     *
     * @param     string $motivo The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ChequeQuery The current query, for fluid interface
     */
    public function filterByMotivo($motivo = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($motivo)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $motivo)) {
                $motivo = str_replace('*', '%', $motivo);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ChequePeer::MOTIVO, $motivo, $comparison);
    }

    /**
     * Filter the query on the estatus column
     *
     * Example usage:
     * <code>
     * $query->filterByEstatus('fooValue');   // WHERE estatus = 'fooValue'
     * $query->filterByEstatus('%fooValue%'); // WHERE estatus LIKE '%fooValue%'
     * </code>
     *
     * @param     string $estatus The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ChequeQuery The current query, for fluid interface
     */
    public function filterByEstatus($estatus = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($estatus)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $estatus)) {
                $estatus = str_replace('*', '%', $estatus);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ChequePeer::ESTATUS, $estatus, $comparison);
    }

    /**
     * Filter the query on the negociable column
     *
     * Example usage:
     * <code>
     * $query->filterByNegociable(true); // WHERE negociable = true
     * $query->filterByNegociable('yes'); // WHERE negociable = true
     * </code>
     *
     * @param     boolean|string $negociable The value to use as filter.
     *              Non-boolean arguments are converted using the following rules:
     *                * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *                * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     *              Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ChequeQuery The current query, for fluid interface
     */
    public function filterByNegociable($negociable = null, $comparison = null)
    {
        if (is_string($negociable)) {
            $negociable = in_array(strtolower($negociable), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
        }

        return $this->addUsingAlias(ChequePeer::NEGOCIABLE, $negociable, $comparison);
    }

    /**
     * Filter the query on the usuario column
     *
     * Example usage:
     * <code>
     * $query->filterByUsuario('fooValue');   // WHERE usuario = 'fooValue'
     * $query->filterByUsuario('%fooValue%'); // WHERE usuario LIKE '%fooValue%'
     * </code>
     *
     * @param     string $usuario The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ChequeQuery The current query, for fluid interface
     */
    public function filterByUsuario($usuario = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($usuario)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $usuario)) {
                $usuario = str_replace('*', '%', $usuario);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(ChequePeer::USUARIO, $usuario, $comparison);
    }

    /**
     * Filter the query on the fecha_creo column
     *
     * Example usage:
     * <code>
     * $query->filterByFechaCreo('2011-03-14'); // WHERE fecha_creo = '2011-03-14'
     * $query->filterByFechaCreo('now'); // WHERE fecha_creo = '2011-03-14'
     * $query->filterByFechaCreo(array('max' => 'yesterday')); // WHERE fecha_creo > '2011-03-13'
     * </code>
     *
     * @param     mixed $fechaCreo The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ChequeQuery The current query, for fluid interface
     */
    public function filterByFechaCreo($fechaCreo = null, $comparison = null)
    {
        if (is_array($fechaCreo)) {
            $useMinMax = false;
            if (isset($fechaCreo['min'])) {
                $this->addUsingAlias(ChequePeer::FECHA_CREO, $fechaCreo['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($fechaCreo['max'])) {
                $this->addUsingAlias(ChequePeer::FECHA_CREO, $fechaCreo['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ChequePeer::FECHA_CREO, $fechaCreo, $comparison);
    }

    /**
     * Filter the query on the orden_devolucion_id column
     *
     * Example usage:
     * <code>
     * $query->filterByOrdenDevolucionId(1234); // WHERE orden_devolucion_id = 1234
     * $query->filterByOrdenDevolucionId(array(12, 34)); // WHERE orden_devolucion_id IN (12, 34)
     * $query->filterByOrdenDevolucionId(array('min' => 12)); // WHERE orden_devolucion_id > 12
     * </code>
     *
     * @see       filterByOrdenDevolucion()
     *
     * @param     mixed $ordenDevolucionId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ChequeQuery The current query, for fluid interface
     */
    public function filterByOrdenDevolucionId($ordenDevolucionId = null, $comparison = null)
    {
        if (is_array($ordenDevolucionId)) {
            $useMinMax = false;
            if (isset($ordenDevolucionId['min'])) {
                $this->addUsingAlias(ChequePeer::ORDEN_DEVOLUCION_ID, $ordenDevolucionId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($ordenDevolucionId['max'])) {
                $this->addUsingAlias(ChequePeer::ORDEN_DEVOLUCION_ID, $ordenDevolucionId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ChequePeer::ORDEN_DEVOLUCION_ID, $ordenDevolucionId, $comparison);
    }

    /**
     * Filter the query on the solicitud_cheque_id column
     *
     * Example usage:
     * <code>
     * $query->filterBySolicitudChequeId(1234); // WHERE solicitud_cheque_id = 1234
     * $query->filterBySolicitudChequeId(array(12, 34)); // WHERE solicitud_cheque_id IN (12, 34)
     * $query->filterBySolicitudChequeId(array('min' => 12)); // WHERE solicitud_cheque_id > 12
     * </code>
     *
     * @see       filterBySolicitudCheque()
     *
     * @param     mixed $solicitudChequeId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return ChequeQuery The current query, for fluid interface
     */
    public function filterBySolicitudChequeId($solicitudChequeId = null, $comparison = null)
    {
        if (is_array($solicitudChequeId)) {
            $useMinMax = false;
            if (isset($solicitudChequeId['min'])) {
                $this->addUsingAlias(ChequePeer::SOLICITUD_CHEQUE_ID, $solicitudChequeId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($solicitudChequeId['max'])) {
                $this->addUsingAlias(ChequePeer::SOLICITUD_CHEQUE_ID, $solicitudChequeId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(ChequePeer::SOLICITUD_CHEQUE_ID, $solicitudChequeId, $comparison);
    }

    /**
     * Filter the query by a related Banco object
     *
     * @param   Banco|PropelObjectCollection $banco The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   ChequeQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByBanco($banco, $comparison = null)
    {
        if ($banco instanceof Banco) {
            return $this
                ->addUsingAlias(ChequePeer::BANCO_ID, $banco->getId(), $comparison);
        } elseif ($banco instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(ChequePeer::BANCO_ID, $banco->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByBanco() only accepts arguments of type Banco or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Banco relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ChequeQuery The current query, for fluid interface
     */
    public function joinBanco($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Banco');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Banco');
        }

        return $this;
    }

    /**
     * Use the Banco relation Banco object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   BancoQuery A secondary query class using the current class as primary query
     */
    public function useBancoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinBanco($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Banco', 'BancoQuery');
    }

    /**
     * Filter the query by a related DocumentoCheque object
     *
     * @param   DocumentoCheque|PropelObjectCollection $documentoCheque The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   ChequeQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByDocumentoCheque($documentoCheque, $comparison = null)
    {
        if ($documentoCheque instanceof DocumentoCheque) {
            return $this
                ->addUsingAlias(ChequePeer::DOCUMENTO_CHEQUE_ID, $documentoCheque->getId(), $comparison);
        } elseif ($documentoCheque instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(ChequePeer::DOCUMENTO_CHEQUE_ID, $documentoCheque->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByDocumentoCheque() only accepts arguments of type DocumentoCheque or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the DocumentoCheque relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ChequeQuery The current query, for fluid interface
     */
    public function joinDocumentoCheque($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('DocumentoCheque');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'DocumentoCheque');
        }

        return $this;
    }

    /**
     * Use the DocumentoCheque relation DocumentoCheque object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   DocumentoChequeQuery A secondary query class using the current class as primary query
     */
    public function useDocumentoChequeQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinDocumentoCheque($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'DocumentoCheque', 'DocumentoChequeQuery');
    }

    /**
     * Filter the query by a related Proveedor object
     *
     * @param   Proveedor|PropelObjectCollection $proveedor The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   ChequeQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByProveedor($proveedor, $comparison = null)
    {
        if ($proveedor instanceof Proveedor) {
            return $this
                ->addUsingAlias(ChequePeer::PROVEEDOR_ID, $proveedor->getId(), $comparison);
        } elseif ($proveedor instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(ChequePeer::PROVEEDOR_ID, $proveedor->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByProveedor() only accepts arguments of type Proveedor or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Proveedor relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ChequeQuery The current query, for fluid interface
     */
    public function joinProveedor($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Proveedor');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Proveedor');
        }

        return $this;
    }

    /**
     * Use the Proveedor relation Proveedor object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ProveedorQuery A secondary query class using the current class as primary query
     */
    public function useProveedorQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinProveedor($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Proveedor', 'ProveedorQuery');
    }

    /**
     * Filter the query by a related OrdenDevolucion object
     *
     * @param   OrdenDevolucion|PropelObjectCollection $ordenDevolucion The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   ChequeQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByOrdenDevolucion($ordenDevolucion, $comparison = null)
    {
        if ($ordenDevolucion instanceof OrdenDevolucion) {
            return $this
                ->addUsingAlias(ChequePeer::ORDEN_DEVOLUCION_ID, $ordenDevolucion->getId(), $comparison);
        } elseif ($ordenDevolucion instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(ChequePeer::ORDEN_DEVOLUCION_ID, $ordenDevolucion->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByOrdenDevolucion() only accepts arguments of type OrdenDevolucion or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the OrdenDevolucion relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ChequeQuery The current query, for fluid interface
     */
    public function joinOrdenDevolucion($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('OrdenDevolucion');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'OrdenDevolucion');
        }

        return $this;
    }

    /**
     * Use the OrdenDevolucion relation OrdenDevolucion object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   OrdenDevolucionQuery A secondary query class using the current class as primary query
     */
    public function useOrdenDevolucionQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinOrdenDevolucion($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'OrdenDevolucion', 'OrdenDevolucionQuery');
    }

    /**
     * Filter the query by a related SolicitudCheque object
     *
     * @param   SolicitudCheque|PropelObjectCollection $solicitudCheque The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   ChequeQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterBySolicitudCheque($solicitudCheque, $comparison = null)
    {
        if ($solicitudCheque instanceof SolicitudCheque) {
            return $this
                ->addUsingAlias(ChequePeer::SOLICITUD_CHEQUE_ID, $solicitudCheque->getId(), $comparison);
        } elseif ($solicitudCheque instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(ChequePeer::SOLICITUD_CHEQUE_ID, $solicitudCheque->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterBySolicitudCheque() only accepts arguments of type SolicitudCheque or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the SolicitudCheque relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ChequeQuery The current query, for fluid interface
     */
    public function joinSolicitudCheque($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('SolicitudCheque');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'SolicitudCheque');
        }

        return $this;
    }

    /**
     * Use the SolicitudCheque relation SolicitudCheque object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   SolicitudChequeQuery A secondary query class using the current class as primary query
     */
    public function useSolicitudChequeQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinSolicitudCheque($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'SolicitudCheque', 'SolicitudChequeQuery');
    }

    /**
     * Filter the query by a related OrdenProveedorPago object
     *
     * @param   OrdenProveedorPago|PropelObjectCollection $ordenProveedorPago  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   ChequeQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByOrdenProveedorPago($ordenProveedorPago, $comparison = null)
    {
        if ($ordenProveedorPago instanceof OrdenProveedorPago) {
            return $this
                ->addUsingAlias(ChequePeer::ID, $ordenProveedorPago->getChequeId(), $comparison);
        } elseif ($ordenProveedorPago instanceof PropelObjectCollection) {
            return $this
                ->useOrdenProveedorPagoQuery()
                ->filterByPrimaryKeys($ordenProveedorPago->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByOrdenProveedorPago() only accepts arguments of type OrdenProveedorPago or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the OrdenProveedorPago relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ChequeQuery The current query, for fluid interface
     */
    public function joinOrdenProveedorPago($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('OrdenProveedorPago');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'OrdenProveedorPago');
        }

        return $this;
    }

    /**
     * Use the OrdenProveedorPago relation OrdenProveedorPago object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   OrdenProveedorPagoQuery A secondary query class using the current class as primary query
     */
    public function useOrdenProveedorPagoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinOrdenProveedorPago($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'OrdenProveedorPago', 'OrdenProveedorPagoQuery');
    }

    /**
     * Filter the query by a related GastoPago object
     *
     * @param   GastoPago|PropelObjectCollection $gastoPago  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   ChequeQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByGastoPago($gastoPago, $comparison = null)
    {
        if ($gastoPago instanceof GastoPago) {
            return $this
                ->addUsingAlias(ChequePeer::ID, $gastoPago->getChequeId(), $comparison);
        } elseif ($gastoPago instanceof PropelObjectCollection) {
            return $this
                ->useGastoPagoQuery()
                ->filterByPrimaryKeys($gastoPago->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByGastoPago() only accepts arguments of type GastoPago or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the GastoPago relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return ChequeQuery The current query, for fluid interface
     */
    public function joinGastoPago($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('GastoPago');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'GastoPago');
        }

        return $this;
    }

    /**
     * Use the GastoPago relation GastoPago object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   GastoPagoQuery A secondary query class using the current class as primary query
     */
    public function useGastoPagoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinGastoPago($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'GastoPago', 'GastoPagoQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   Cheque $cheque Object to remove from the list of results
     *
     * @return ChequeQuery The current query, for fluid interface
     */
    public function prune($cheque = null)
    {
        if ($cheque) {
            $this->addUsingAlias(ChequePeer::ID, $cheque->getId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}
