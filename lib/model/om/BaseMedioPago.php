<?php


/**
 * Base class that represents a row from the 'medio_pago' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.6.7 on:
 *
 * 12/30/25 15:40:22
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseMedioPago extends BaseObject implements Persistent
{
    /**
     * Peer class name
     */
    const PEER = 'MedioPagoPeer';

    /**
     * The Peer class.
     * Instance provides a convenient way of calling static methods on a class
     * that calling code may not be able to identify.
     * @var        MedioPagoPeer
     */
    protected static $peer;

    /**
     * The flag var to prevent infinit loop in deep copy
     * @var       boolean
     */
    protected $startCopy = false;

    /**
     * The value for the id field.
     * @var        int
     */
    protected $id;

    /**
     * The value for the empresa_id field.
     * @var        int
     */
    protected $empresa_id;

    /**
     * The value for the codigo field.
     * @var        string
     */
    protected $codigo;

    /**
     * The value for the nombre field.
     * @var        string
     */
    protected $nombre;

    /**
     * The value for the activo field.
     * Note: this column has a database default value of: true
     * @var        boolean
     */
    protected $activo;

    /**
     * The value for the cuenta_contable field.
     * @var        string
     */
    protected $cuenta_contable;

    /**
     * The value for the orden field.
     * @var        int
     */
    protected $orden;

    /**
     * The value for the pos field.
     * Note: this column has a database default value of: true
     * @var        boolean
     */
    protected $pos;

    /**
     * The value for the aplica_mov_banco field.
     * Note: this column has a database default value of: false
     * @var        boolean
     */
    protected $aplica_mov_banco;

    /**
     * The value for the created_by field.
     * @var        string
     */
    protected $created_by;

    /**
     * The value for the updated_by field.
     * @var        string
     */
    protected $updated_by;

    /**
     * The value for the created_at field.
     * @var        string
     */
    protected $created_at;

    /**
     * The value for the updated_at field.
     * @var        string
     */
    protected $updated_at;

    /**
     * The value for the aplica_cuotas field.
     * Note: this column has a database default value of: false
     * @var        boolean
     */
    protected $aplica_cuotas;

    /**
     * The value for the numero_cuotas field.
     * @var        int
     */
    protected $numero_cuotas;

    /**
     * The value for the comision field.
     * @var        double
     */
    protected $comision;

    /**
     * The value for the retiene_isr field.
     * Note: this column has a database default value of: false
     * @var        boolean
     */
    protected $retiene_isr;

    /**
     * The value for the pide_banco field.
     * Note: this column has a database default value of: false
     * @var        boolean
     */
    protected $pide_banco;

    /**
     * The value for the banco_id field.
     * @var        int
     */
    protected $banco_id;

    /**
     * The value for the pago_proveedor field.
     * Note: this column has a database default value of: false
     * @var        boolean
     */
    protected $pago_proveedor;

    /**
     * @var        Empresa
     */
    protected $aEmpresa;

    /**
     * @var        Banco
     */
    protected $aBanco;

    /**
     * @var        PropelObjectCollection|MovimientoBanco[] Collection to store aggregation of MovimientoBanco objects.
     */
    protected $collMovimientoBancos;
    protected $collMovimientoBancosPartial;

    /**
     * @var        PropelObjectCollection|Partida[] Collection to store aggregation of Partida objects.
     */
    protected $collPartidas;
    protected $collPartidasPartial;

    /**
     * @var        PropelObjectCollection|NotaCredito[] Collection to store aggregation of NotaCredito objects.
     */
    protected $collNotaCreditos;
    protected $collNotaCreditosPartial;

    /**
     * @var        PropelObjectCollection|VentaResumida[] Collection to store aggregation of VentaResumida objects.
     */
    protected $collVentaResumidas;
    protected $collVentaResumidasPartial;

    /**
     * @var        PropelObjectCollection|VentaResumidaLinea[] Collection to store aggregation of VentaResumidaLinea objects.
     */
    protected $collVentaResumidaLineas;
    protected $collVentaResumidaLineasPartial;

    /**
     * @var        PropelObjectCollection|EgresosRecibido[] Collection to store aggregation of EgresosRecibido objects.
     */
    protected $collEgresosRecibidos;
    protected $collEgresosRecibidosPartial;

    /**
     * Flag to prevent endless save loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInSave = false;

    /**
     * Flag to prevent endless validation loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInValidation = false;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $movimientoBancosScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $partidasScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $notaCreditosScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $ventaResumidasScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $ventaResumidaLineasScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $egresosRecibidosScheduledForDeletion = null;

    /**
     * Applies default values to this object.
     * This method should be called from the object's constructor (or
     * equivalent initialization method).
     * @see        __construct()
     */
    public function applyDefaultValues()
    {
        $this->activo = true;
        $this->pos = true;
        $this->aplica_mov_banco = false;
        $this->aplica_cuotas = false;
        $this->retiene_isr = false;
        $this->pide_banco = false;
        $this->pago_proveedor = false;
    }

    /**
     * Initializes internal state of BaseMedioPago object.
     * @see        applyDefaults()
     */
    public function __construct()
    {
        parent::__construct();
        $this->applyDefaultValues();
    }

    /**
     * Get the [id] column value.
     *
     * @return int
     */
    public function getId()
    {
        return $this->id;
    }

    /**
     * Get the [empresa_id] column value.
     *
     * @return int
     */
    public function getEmpresaId()
    {
        return $this->empresa_id;
    }

    /**
     * Get the [codigo] column value.
     *
     * @return string
     */
    public function getCodigo()
    {
        return $this->codigo;
    }

    /**
     * Get the [nombre] column value.
     *
     * @return string
     */
    public function getNombre()
    {
        return $this->nombre;
    }

    /**
     * Get the [activo] column value.
     *
     * @return boolean
     */
    public function getActivo()
    {
        return $this->activo;
    }

    /**
     * Get the [cuenta_contable] column value.
     *
     * @return string
     */
    public function getCuentaContable()
    {
        return $this->cuenta_contable;
    }

    /**
     * Get the [orden] column value.
     *
     * @return int
     */
    public function getOrden()
    {
        return $this->orden;
    }

    /**
     * Get the [pos] column value.
     *
     * @return boolean
     */
    public function getPos()
    {
        return $this->pos;
    }

    /**
     * Get the [aplica_mov_banco] column value.
     *
     * @return boolean
     */
    public function getAplicaMovBanco()
    {
        return $this->aplica_mov_banco;
    }

    /**
     * Get the [created_by] column value.
     *
     * @return string
     */
    public function getCreatedBy()
    {
        return $this->created_by;
    }

    /**
     * Get the [updated_by] column value.
     *
     * @return string
     */
    public function getUpdatedBy()
    {
        return $this->updated_by;
    }

    /**
     * Get the [optionally formatted] temporal [created_at] column value.
     *
     *
     * @param string $format The date/time format string (either date()-style or strftime()-style).
     *				 If format is null, then the raw DateTime object will be returned.
     * @return mixed Formatted date/time value as string or DateTime object (if format is null), null if column is null, and 0 if column value is 0000-00-00 00:00:00
     * @throws PropelException - if unable to parse/validate the date/time value.
     */
    public function getCreatedAt($format = 'Y-m-d H:i:s')
    {
        if ($this->created_at === null) {
            return null;
        }

        if ($this->created_at === '0000-00-00 00:00:00') {
            // while technically this is not a default value of null,
            // this seems to be closest in meaning.
            return null;
        } else {
            try {
                $dt = new DateTime($this->created_at);
            } catch (Exception $x) {
                throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->created_at, true), $x);
            }
        }

        if ($format === null) {
            // Because propel.useDateTimeClass is true, we return a DateTime object.
            return $dt;
        } elseif (strpos($format, '%') !== false) {
            return strftime($format, $dt->format('U'));
        } else {
            return $dt->format($format);
        }
    }

    /**
     * Get the [optionally formatted] temporal [updated_at] column value.
     *
     *
     * @param string $format The date/time format string (either date()-style or strftime()-style).
     *				 If format is null, then the raw DateTime object will be returned.
     * @return mixed Formatted date/time value as string or DateTime object (if format is null), null if column is null, and 0 if column value is 0000-00-00 00:00:00
     * @throws PropelException - if unable to parse/validate the date/time value.
     */
    public function getUpdatedAt($format = 'Y-m-d H:i:s')
    {
        if ($this->updated_at === null) {
            return null;
        }

        if ($this->updated_at === '0000-00-00 00:00:00') {
            // while technically this is not a default value of null,
            // this seems to be closest in meaning.
            return null;
        } else {
            try {
                $dt = new DateTime($this->updated_at);
            } catch (Exception $x) {
                throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->updated_at, true), $x);
            }
        }

        if ($format === null) {
            // Because propel.useDateTimeClass is true, we return a DateTime object.
            return $dt;
        } elseif (strpos($format, '%') !== false) {
            return strftime($format, $dt->format('U'));
        } else {
            return $dt->format($format);
        }
    }

    /**
     * Get the [aplica_cuotas] column value.
     *
     * @return boolean
     */
    public function getAplicaCuotas()
    {
        return $this->aplica_cuotas;
    }

    /**
     * Get the [numero_cuotas] column value.
     *
     * @return int
     */
    public function getNumeroCuotas()
    {
        return $this->numero_cuotas;
    }

    /**
     * Get the [comision] column value.
     *
     * @return double
     */
    public function getComision()
    {
        return $this->comision;
    }

    /**
     * Get the [retiene_isr] column value.
     *
     * @return boolean
     */
    public function getRetieneIsr()
    {
        return $this->retiene_isr;
    }

    /**
     * Get the [pide_banco] column value.
     *
     * @return boolean
     */
    public function getPideBanco()
    {
        return $this->pide_banco;
    }

    /**
     * Get the [banco_id] column value.
     *
     * @return int
     */
    public function getBancoId()
    {
        return $this->banco_id;
    }

    /**
     * Get the [pago_proveedor] column value.
     *
     * @return boolean
     */
    public function getPagoProveedor()
    {
        return $this->pago_proveedor;
    }

    /**
     * Set the value of [id] column.
     *
     * @param int $v new value
     * @return MedioPago The current object (for fluent API support)
     */
    public function setId($v)
    {
        if ($v !== null) {
            $v = (int) $v;
        }

        if ($this->id !== $v) {
            $this->id = $v;
            $this->modifiedColumns[] = MedioPagoPeer::ID;
        }


        return $this;
    } // setId()

    /**
     * Set the value of [empresa_id] column.
     *
     * @param int $v new value
     * @return MedioPago The current object (for fluent API support)
     */
    public function setEmpresaId($v)
    {
        if ($v !== null) {
            $v = (int) $v;
        }

        if ($this->empresa_id !== $v) {
            $this->empresa_id = $v;
            $this->modifiedColumns[] = MedioPagoPeer::EMPRESA_ID;
        }

        if ($this->aEmpresa !== null && $this->aEmpresa->getId() !== $v) {
            $this->aEmpresa = null;
        }


        return $this;
    } // setEmpresaId()

    /**
     * Set the value of [codigo] column.
     *
     * @param string $v new value
     * @return MedioPago The current object (for fluent API support)
     */
    public function setCodigo($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->codigo !== $v) {
            $this->codigo = $v;
            $this->modifiedColumns[] = MedioPagoPeer::CODIGO;
        }


        return $this;
    } // setCodigo()

    /**
     * Set the value of [nombre] column.
     *
     * @param string $v new value
     * @return MedioPago The current object (for fluent API support)
     */
    public function setNombre($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->nombre !== $v) {
            $this->nombre = $v;
            $this->modifiedColumns[] = MedioPagoPeer::NOMBRE;
        }


        return $this;
    } // setNombre()

    /**
     * Sets the value of the [activo] column.
     * Non-boolean arguments are converted using the following rules:
     *   * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *   * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     * Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     *
     * @param boolean|integer|string $v The new value
     * @return MedioPago The current object (for fluent API support)
     */
    public function setActivo($v)
    {
        if ($v !== null) {
            if (is_string($v)) {
                $v = in_array(strtolower($v), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
            } else {
                $v = (boolean) $v;
            }
        }

        if ($this->activo !== $v) {
            $this->activo = $v;
            $this->modifiedColumns[] = MedioPagoPeer::ACTIVO;
        }


        return $this;
    } // setActivo()

    /**
     * Set the value of [cuenta_contable] column.
     *
     * @param string $v new value
     * @return MedioPago The current object (for fluent API support)
     */
    public function setCuentaContable($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->cuenta_contable !== $v) {
            $this->cuenta_contable = $v;
            $this->modifiedColumns[] = MedioPagoPeer::CUENTA_CONTABLE;
        }


        return $this;
    } // setCuentaContable()

    /**
     * Set the value of [orden] column.
     *
     * @param int $v new value
     * @return MedioPago The current object (for fluent API support)
     */
    public function setOrden($v)
    {
        if ($v !== null) {
            $v = (int) $v;
        }

        if ($this->orden !== $v) {
            $this->orden = $v;
            $this->modifiedColumns[] = MedioPagoPeer::ORDEN;
        }


        return $this;
    } // setOrden()

    /**
     * Sets the value of the [pos] column.
     * Non-boolean arguments are converted using the following rules:
     *   * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *   * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     * Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     *
     * @param boolean|integer|string $v The new value
     * @return MedioPago The current object (for fluent API support)
     */
    public function setPos($v)
    {
        if ($v !== null) {
            if (is_string($v)) {
                $v = in_array(strtolower($v), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
            } else {
                $v = (boolean) $v;
            }
        }

        if ($this->pos !== $v) {
            $this->pos = $v;
            $this->modifiedColumns[] = MedioPagoPeer::POS;
        }


        return $this;
    } // setPos()

    /**
     * Sets the value of the [aplica_mov_banco] column.
     * Non-boolean arguments are converted using the following rules:
     *   * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *   * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     * Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     *
     * @param boolean|integer|string $v The new value
     * @return MedioPago The current object (for fluent API support)
     */
    public function setAplicaMovBanco($v)
    {
        if ($v !== null) {
            if (is_string($v)) {
                $v = in_array(strtolower($v), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
            } else {
                $v = (boolean) $v;
            }
        }

        if ($this->aplica_mov_banco !== $v) {
            $this->aplica_mov_banco = $v;
            $this->modifiedColumns[] = MedioPagoPeer::APLICA_MOV_BANCO;
        }


        return $this;
    } // setAplicaMovBanco()

    /**
     * Set the value of [created_by] column.
     *
     * @param string $v new value
     * @return MedioPago The current object (for fluent API support)
     */
    public function setCreatedBy($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->created_by !== $v) {
            $this->created_by = $v;
            $this->modifiedColumns[] = MedioPagoPeer::CREATED_BY;
        }


        return $this;
    } // setCreatedBy()

    /**
     * Set the value of [updated_by] column.
     *
     * @param string $v new value
     * @return MedioPago The current object (for fluent API support)
     */
    public function setUpdatedBy($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->updated_by !== $v) {
            $this->updated_by = $v;
            $this->modifiedColumns[] = MedioPagoPeer::UPDATED_BY;
        }


        return $this;
    } // setUpdatedBy()

    /**
     * Sets the value of [created_at] column to a normalized version of the date/time value specified.
     *
     * @param mixed $v string, integer (timestamp), or DateTime value.
     *               Empty strings are treated as null.
     * @return MedioPago The current object (for fluent API support)
     */
    public function setCreatedAt($v)
    {
        $dt = PropelDateTime::newInstance($v, null, 'DateTime');
        if ($this->created_at !== null || $dt !== null) {
            $currentDateAsString = ($this->created_at !== null && $tmpDt = new DateTime($this->created_at)) ? $tmpDt->format('Y-m-d H:i:s') : null;
            $newDateAsString = $dt ? $dt->format('Y-m-d H:i:s') : null;
            if ($currentDateAsString !== $newDateAsString) {
                $this->created_at = $newDateAsString;
                $this->modifiedColumns[] = MedioPagoPeer::CREATED_AT;
            }
        } // if either are not null


        return $this;
    } // setCreatedAt()

    /**
     * Sets the value of [updated_at] column to a normalized version of the date/time value specified.
     *
     * @param mixed $v string, integer (timestamp), or DateTime value.
     *               Empty strings are treated as null.
     * @return MedioPago The current object (for fluent API support)
     */
    public function setUpdatedAt($v)
    {
        $dt = PropelDateTime::newInstance($v, null, 'DateTime');
        if ($this->updated_at !== null || $dt !== null) {
            $currentDateAsString = ($this->updated_at !== null && $tmpDt = new DateTime($this->updated_at)) ? $tmpDt->format('Y-m-d H:i:s') : null;
            $newDateAsString = $dt ? $dt->format('Y-m-d H:i:s') : null;
            if ($currentDateAsString !== $newDateAsString) {
                $this->updated_at = $newDateAsString;
                $this->modifiedColumns[] = MedioPagoPeer::UPDATED_AT;
            }
        } // if either are not null


        return $this;
    } // setUpdatedAt()

    /**
     * Sets the value of the [aplica_cuotas] column.
     * Non-boolean arguments are converted using the following rules:
     *   * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *   * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     * Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     *
     * @param boolean|integer|string $v The new value
     * @return MedioPago The current object (for fluent API support)
     */
    public function setAplicaCuotas($v)
    {
        if ($v !== null) {
            if (is_string($v)) {
                $v = in_array(strtolower($v), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
            } else {
                $v = (boolean) $v;
            }
        }

        if ($this->aplica_cuotas !== $v) {
            $this->aplica_cuotas = $v;
            $this->modifiedColumns[] = MedioPagoPeer::APLICA_CUOTAS;
        }


        return $this;
    } // setAplicaCuotas()

    /**
     * Set the value of [numero_cuotas] column.
     *
     * @param int $v new value
     * @return MedioPago The current object (for fluent API support)
     */
    public function setNumeroCuotas($v)
    {
        if ($v !== null) {
            $v = (int) $v;
        }

        if ($this->numero_cuotas !== $v) {
            $this->numero_cuotas = $v;
            $this->modifiedColumns[] = MedioPagoPeer::NUMERO_CUOTAS;
        }


        return $this;
    } // setNumeroCuotas()

    /**
     * Set the value of [comision] column.
     *
     * @param double $v new value
     * @return MedioPago The current object (for fluent API support)
     */
    public function setComision($v)
    {
        if ($v !== null) {
            $v = (double) $v;
        }

        if ($this->comision !== $v) {
            $this->comision = $v;
            $this->modifiedColumns[] = MedioPagoPeer::COMISION;
        }


        return $this;
    } // setComision()

    /**
     * Sets the value of the [retiene_isr] column.
     * Non-boolean arguments are converted using the following rules:
     *   * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *   * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     * Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     *
     * @param boolean|integer|string $v The new value
     * @return MedioPago The current object (for fluent API support)
     */
    public function setRetieneIsr($v)
    {
        if ($v !== null) {
            if (is_string($v)) {
                $v = in_array(strtolower($v), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
            } else {
                $v = (boolean) $v;
            }
        }

        if ($this->retiene_isr !== $v) {
            $this->retiene_isr = $v;
            $this->modifiedColumns[] = MedioPagoPeer::RETIENE_ISR;
        }


        return $this;
    } // setRetieneIsr()

    /**
     * Sets the value of the [pide_banco] column.
     * Non-boolean arguments are converted using the following rules:
     *   * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *   * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     * Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     *
     * @param boolean|integer|string $v The new value
     * @return MedioPago The current object (for fluent API support)
     */
    public function setPideBanco($v)
    {
        if ($v !== null) {
            if (is_string($v)) {
                $v = in_array(strtolower($v), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
            } else {
                $v = (boolean) $v;
            }
        }

        if ($this->pide_banco !== $v) {
            $this->pide_banco = $v;
            $this->modifiedColumns[] = MedioPagoPeer::PIDE_BANCO;
        }


        return $this;
    } // setPideBanco()

    /**
     * Set the value of [banco_id] column.
     *
     * @param int $v new value
     * @return MedioPago The current object (for fluent API support)
     */
    public function setBancoId($v)
    {
        if ($v !== null) {
            $v = (int) $v;
        }

        if ($this->banco_id !== $v) {
            $this->banco_id = $v;
            $this->modifiedColumns[] = MedioPagoPeer::BANCO_ID;
        }

        if ($this->aBanco !== null && $this->aBanco->getId() !== $v) {
            $this->aBanco = null;
        }


        return $this;
    } // setBancoId()

    /**
     * Sets the value of the [pago_proveedor] column.
     * Non-boolean arguments are converted using the following rules:
     *   * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *   * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     * Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     *
     * @param boolean|integer|string $v The new value
     * @return MedioPago The current object (for fluent API support)
     */
    public function setPagoProveedor($v)
    {
        if ($v !== null) {
            if (is_string($v)) {
                $v = in_array(strtolower($v), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
            } else {
                $v = (boolean) $v;
            }
        }

        if ($this->pago_proveedor !== $v) {
            $this->pago_proveedor = $v;
            $this->modifiedColumns[] = MedioPagoPeer::PAGO_PROVEEDOR;
        }


        return $this;
    } // setPagoProveedor()

    /**
     * Indicates whether the columns in this object are only set to default values.
     *
     * This method can be used in conjunction with isModified() to indicate whether an object is both
     * modified _and_ has some values set which are non-default.
     *
     * @return boolean Whether the columns in this object are only been set with default values.
     */
    public function hasOnlyDefaultValues()
    {
            if ($this->activo !== true) {
                return false;
            }

            if ($this->pos !== true) {
                return false;
            }

            if ($this->aplica_mov_banco !== false) {
                return false;
            }

            if ($this->aplica_cuotas !== false) {
                return false;
            }

            if ($this->retiene_isr !== false) {
                return false;
            }

            if ($this->pide_banco !== false) {
                return false;
            }

            if ($this->pago_proveedor !== false) {
                return false;
            }

        // otherwise, everything was equal, so return true
        return true;
    } // hasOnlyDefaultValues()

    /**
     * Hydrates (populates) the object variables with values from the database resultset.
     *
     * An offset (0-based "start column") is specified so that objects can be hydrated
     * with a subset of the columns in the resultset rows.  This is needed, for example,
     * for results of JOIN queries where the resultset row includes columns from two or
     * more tables.
     *
     * @param array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
     * @param int $startcol 0-based offset column which indicates which restultset column to start with.
     * @param boolean $rehydrate Whether this object is being re-hydrated from the database.
     * @return int             next starting column
     * @throws PropelException - Any caught Exception will be rewrapped as a PropelException.
     */
    public function hydrate($row, $startcol = 0, $rehydrate = false)
    {
        try {

            $this->id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
            $this->empresa_id = ($row[$startcol + 1] !== null) ? (int) $row[$startcol + 1] : null;
            $this->codigo = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
            $this->nombre = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
            $this->activo = ($row[$startcol + 4] !== null) ? (boolean) $row[$startcol + 4] : null;
            $this->cuenta_contable = ($row[$startcol + 5] !== null) ? (string) $row[$startcol + 5] : null;
            $this->orden = ($row[$startcol + 6] !== null) ? (int) $row[$startcol + 6] : null;
            $this->pos = ($row[$startcol + 7] !== null) ? (boolean) $row[$startcol + 7] : null;
            $this->aplica_mov_banco = ($row[$startcol + 8] !== null) ? (boolean) $row[$startcol + 8] : null;
            $this->created_by = ($row[$startcol + 9] !== null) ? (string) $row[$startcol + 9] : null;
            $this->updated_by = ($row[$startcol + 10] !== null) ? (string) $row[$startcol + 10] : null;
            $this->created_at = ($row[$startcol + 11] !== null) ? (string) $row[$startcol + 11] : null;
            $this->updated_at = ($row[$startcol + 12] !== null) ? (string) $row[$startcol + 12] : null;
            $this->aplica_cuotas = ($row[$startcol + 13] !== null) ? (boolean) $row[$startcol + 13] : null;
            $this->numero_cuotas = ($row[$startcol + 14] !== null) ? (int) $row[$startcol + 14] : null;
            $this->comision = ($row[$startcol + 15] !== null) ? (double) $row[$startcol + 15] : null;
            $this->retiene_isr = ($row[$startcol + 16] !== null) ? (boolean) $row[$startcol + 16] : null;
            $this->pide_banco = ($row[$startcol + 17] !== null) ? (boolean) $row[$startcol + 17] : null;
            $this->banco_id = ($row[$startcol + 18] !== null) ? (int) $row[$startcol + 18] : null;
            $this->pago_proveedor = ($row[$startcol + 19] !== null) ? (boolean) $row[$startcol + 19] : null;
            $this->resetModified();

            $this->setNew(false);

            if ($rehydrate) {
                $this->ensureConsistency();
            }

            return $startcol + 20; // 20 = MedioPagoPeer::NUM_HYDRATE_COLUMNS.

        } catch (Exception $e) {
            throw new PropelException("Error populating MedioPago object", $e);
        }
    }

    /**
     * Checks and repairs the internal consistency of the object.
     *
     * This method is executed after an already-instantiated object is re-hydrated
     * from the database.  It exists to check any foreign keys to make sure that
     * the objects related to the current object are correct based on foreign key.
     *
     * You can override this method in the stub class, but you should always invoke
     * the base method from the overridden method (i.e. parent::ensureConsistency()),
     * in case your model changes.
     *
     * @throws PropelException
     */
    public function ensureConsistency()
    {

        if ($this->aEmpresa !== null && $this->empresa_id !== $this->aEmpresa->getId()) {
            $this->aEmpresa = null;
        }
        if ($this->aBanco !== null && $this->banco_id !== $this->aBanco->getId()) {
            $this->aBanco = null;
        }
    } // ensureConsistency

    /**
     * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
     *
     * This will only work if the object has been saved and has a valid primary key set.
     *
     * @param boolean $deep (optional) Whether to also de-associated any related objects.
     * @param PropelPDO $con (optional) The PropelPDO connection to use.
     * @return void
     * @throws PropelException - if this object is deleted, unsaved or doesn't have pk match in db
     */
    public function reload($deep = false, PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("Cannot reload a deleted object.");
        }

        if ($this->isNew()) {
            throw new PropelException("Cannot reload an unsaved object.");
        }

        if ($con === null) {
            $con = Propel::getConnection(MedioPagoPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }

        // We don't need to alter the object instance pool; we're just modifying this instance
        // already in the pool.

        $stmt = MedioPagoPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
        $row = $stmt->fetch(PDO::FETCH_NUM);
        $stmt->closeCursor();
        if (!$row) {
            throw new PropelException('Cannot find matching row in the database to reload object values.');
        }
        $this->hydrate($row, 0, true); // rehydrate

        if ($deep) {  // also de-associate any related objects?

            $this->aEmpresa = null;
            $this->aBanco = null;
            $this->collMovimientoBancos = null;

            $this->collPartidas = null;

            $this->collNotaCreditos = null;

            $this->collVentaResumidas = null;

            $this->collVentaResumidaLineas = null;

            $this->collEgresosRecibidos = null;

        } // if (deep)
    }

    /**
     * Removes this object from datastore and sets delete attribute.
     *
     * @param PropelPDO $con
     * @return void
     * @throws PropelException
     * @throws Exception
     * @see        BaseObject::setDeleted()
     * @see        BaseObject::isDeleted()
     */
    public function delete(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("This object has already been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(MedioPagoPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        try {
            $deleteQuery = MedioPagoQuery::create()
                ->filterByPrimaryKey($this->getPrimaryKey());
            $ret = $this->preDelete($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BaseMedioPago:delete:pre') as $callable)
            {
              if (call_user_func($callable, $this, $con))
              {
                $con->commit();
                return;
              }
            }

            if ($ret) {
                $deleteQuery->delete($con);
                $this->postDelete($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BaseMedioPago:delete:post') as $callable)
                {
                  call_user_func($callable, $this, $con);
                }

                $con->commit();
                $this->setDeleted(true);
            } else {
                $con->commit();
            }
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Persists this object to the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All modified related objects will also be persisted in the doSave()
     * method.  This method wraps all precipitate database operations in a
     * single transaction.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @throws Exception
     * @see        doSave()
     */
    public function save(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("You cannot save an object that has been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(MedioPagoPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        $isInsert = $this->isNew();
        try {
            $ret = $this->preSave($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BaseMedioPago:save:pre') as $callable)
            {
              if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
              {
                  $con->commit();
                return $affectedRows;
              }
            }

            // symfony_timestampable behavior
            if ($this->isModified() && !$this->isColumnModified(MedioPagoPeer::UPDATED_AT))
            {
                $this->setUpdatedAt(time());
            }
            if ($isInsert) {
                $ret = $ret && $this->preInsert($con);
                // symfony_timestampable behavior
                if (!$this->isColumnModified(MedioPagoPeer::CREATED_AT))
                {
                  $this->setCreatedAt(time());
                }

            } else {
                $ret = $ret && $this->preUpdate($con);
            }
            if ($ret) {
                $affectedRows = $this->doSave($con);
                if ($isInsert) {
                    $this->postInsert($con);
                } else {
                    $this->postUpdate($con);
                }
                $this->postSave($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BaseMedioPago:save:post') as $callable)
                {
                  call_user_func($callable, $this, $con, $affectedRows);
                }

                MedioPagoPeer::addInstanceToPool($this);
            } else {
                $affectedRows = 0;
            }
            $con->commit();

            return $affectedRows;
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Performs the work of inserting or updating the row in the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All related objects are also updated in this method.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @see        save()
     */
    protected function doSave(PropelPDO $con)
    {
        $affectedRows = 0; // initialize var to track total num of affected rows
        if (!$this->alreadyInSave) {
            $this->alreadyInSave = true;

            // We call the save method on the following object(s) if they
            // were passed to this object by their coresponding set
            // method.  This object relates to these object(s) by a
            // foreign key reference.

            if ($this->aEmpresa !== null) {
                if ($this->aEmpresa->isModified() || $this->aEmpresa->isNew()) {
                    $affectedRows += $this->aEmpresa->save($con);
                }
                $this->setEmpresa($this->aEmpresa);
            }

            if ($this->aBanco !== null) {
                if ($this->aBanco->isModified() || $this->aBanco->isNew()) {
                    $affectedRows += $this->aBanco->save($con);
                }
                $this->setBanco($this->aBanco);
            }

            if ($this->isNew() || $this->isModified()) {
                // persist changes
                if ($this->isNew()) {
                    $this->doInsert($con);
                } else {
                    $this->doUpdate($con);
                }
                $affectedRows += 1;
                $this->resetModified();
            }

            if ($this->movimientoBancosScheduledForDeletion !== null) {
                if (!$this->movimientoBancosScheduledForDeletion->isEmpty()) {
                    foreach ($this->movimientoBancosScheduledForDeletion as $movimientoBanco) {
                        // need to save related object because we set the relation to null
                        $movimientoBanco->save($con);
                    }
                    $this->movimientoBancosScheduledForDeletion = null;
                }
            }

            if ($this->collMovimientoBancos !== null) {
                foreach ($this->collMovimientoBancos as $referrerFK) {
                    if (!$referrerFK->isDeleted()) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->partidasScheduledForDeletion !== null) {
                if (!$this->partidasScheduledForDeletion->isEmpty()) {
                    foreach ($this->partidasScheduledForDeletion as $partida) {
                        // need to save related object because we set the relation to null
                        $partida->save($con);
                    }
                    $this->partidasScheduledForDeletion = null;
                }
            }

            if ($this->collPartidas !== null) {
                foreach ($this->collPartidas as $referrerFK) {
                    if (!$referrerFK->isDeleted()) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->notaCreditosScheduledForDeletion !== null) {
                if (!$this->notaCreditosScheduledForDeletion->isEmpty()) {
                    foreach ($this->notaCreditosScheduledForDeletion as $notaCredito) {
                        // need to save related object because we set the relation to null
                        $notaCredito->save($con);
                    }
                    $this->notaCreditosScheduledForDeletion = null;
                }
            }

            if ($this->collNotaCreditos !== null) {
                foreach ($this->collNotaCreditos as $referrerFK) {
                    if (!$referrerFK->isDeleted()) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->ventaResumidasScheduledForDeletion !== null) {
                if (!$this->ventaResumidasScheduledForDeletion->isEmpty()) {
                    foreach ($this->ventaResumidasScheduledForDeletion as $ventaResumida) {
                        // need to save related object because we set the relation to null
                        $ventaResumida->save($con);
                    }
                    $this->ventaResumidasScheduledForDeletion = null;
                }
            }

            if ($this->collVentaResumidas !== null) {
                foreach ($this->collVentaResumidas as $referrerFK) {
                    if (!$referrerFK->isDeleted()) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->ventaResumidaLineasScheduledForDeletion !== null) {
                if (!$this->ventaResumidaLineasScheduledForDeletion->isEmpty()) {
                    foreach ($this->ventaResumidaLineasScheduledForDeletion as $ventaResumidaLinea) {
                        // need to save related object because we set the relation to null
                        $ventaResumidaLinea->save($con);
                    }
                    $this->ventaResumidaLineasScheduledForDeletion = null;
                }
            }

            if ($this->collVentaResumidaLineas !== null) {
                foreach ($this->collVentaResumidaLineas as $referrerFK) {
                    if (!$referrerFK->isDeleted()) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->egresosRecibidosScheduledForDeletion !== null) {
                if (!$this->egresosRecibidosScheduledForDeletion->isEmpty()) {
                    foreach ($this->egresosRecibidosScheduledForDeletion as $egresosRecibido) {
                        // need to save related object because we set the relation to null
                        $egresosRecibido->save($con);
                    }
                    $this->egresosRecibidosScheduledForDeletion = null;
                }
            }

            if ($this->collEgresosRecibidos !== null) {
                foreach ($this->collEgresosRecibidos as $referrerFK) {
                    if (!$referrerFK->isDeleted()) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            $this->alreadyInSave = false;

        }

        return $affectedRows;
    } // doSave()

    /**
     * Insert the row in the database.
     *
     * @param PropelPDO $con
     *
     * @throws PropelException
     * @see        doSave()
     */
    protected function doInsert(PropelPDO $con)
    {
        $modifiedColumns = array();
        $index = 0;

        $this->modifiedColumns[] = MedioPagoPeer::ID;
        if (null !== $this->id) {
            throw new PropelException('Cannot insert a value for auto-increment primary key (' . MedioPagoPeer::ID . ')');
        }

         // check the columns in natural order for more readable SQL queries
        if ($this->isColumnModified(MedioPagoPeer::ID)) {
            $modifiedColumns[':p' . $index++]  = '`ID`';
        }
        if ($this->isColumnModified(MedioPagoPeer::EMPRESA_ID)) {
            $modifiedColumns[':p' . $index++]  = '`EMPRESA_ID`';
        }
        if ($this->isColumnModified(MedioPagoPeer::CODIGO)) {
            $modifiedColumns[':p' . $index++]  = '`CODIGO`';
        }
        if ($this->isColumnModified(MedioPagoPeer::NOMBRE)) {
            $modifiedColumns[':p' . $index++]  = '`NOMBRE`';
        }
        if ($this->isColumnModified(MedioPagoPeer::ACTIVO)) {
            $modifiedColumns[':p' . $index++]  = '`ACTIVO`';
        }
        if ($this->isColumnModified(MedioPagoPeer::CUENTA_CONTABLE)) {
            $modifiedColumns[':p' . $index++]  = '`CUENTA_CONTABLE`';
        }
        if ($this->isColumnModified(MedioPagoPeer::ORDEN)) {
            $modifiedColumns[':p' . $index++]  = '`ORDEN`';
        }
        if ($this->isColumnModified(MedioPagoPeer::POS)) {
            $modifiedColumns[':p' . $index++]  = '`POS`';
        }
        if ($this->isColumnModified(MedioPagoPeer::APLICA_MOV_BANCO)) {
            $modifiedColumns[':p' . $index++]  = '`APLICA_MOV_BANCO`';
        }
        if ($this->isColumnModified(MedioPagoPeer::CREATED_BY)) {
            $modifiedColumns[':p' . $index++]  = '`CREATED_BY`';
        }
        if ($this->isColumnModified(MedioPagoPeer::UPDATED_BY)) {
            $modifiedColumns[':p' . $index++]  = '`UPDATED_BY`';
        }
        if ($this->isColumnModified(MedioPagoPeer::CREATED_AT)) {
            $modifiedColumns[':p' . $index++]  = '`CREATED_AT`';
        }
        if ($this->isColumnModified(MedioPagoPeer::UPDATED_AT)) {
            $modifiedColumns[':p' . $index++]  = '`UPDATED_AT`';
        }
        if ($this->isColumnModified(MedioPagoPeer::APLICA_CUOTAS)) {
            $modifiedColumns[':p' . $index++]  = '`APLICA_CUOTAS`';
        }
        if ($this->isColumnModified(MedioPagoPeer::NUMERO_CUOTAS)) {
            $modifiedColumns[':p' . $index++]  = '`NUMERO_CUOTAS`';
        }
        if ($this->isColumnModified(MedioPagoPeer::COMISION)) {
            $modifiedColumns[':p' . $index++]  = '`COMISION`';
        }
        if ($this->isColumnModified(MedioPagoPeer::RETIENE_ISR)) {
            $modifiedColumns[':p' . $index++]  = '`RETIENE_ISR`';
        }
        if ($this->isColumnModified(MedioPagoPeer::PIDE_BANCO)) {
            $modifiedColumns[':p' . $index++]  = '`PIDE_BANCO`';
        }
        if ($this->isColumnModified(MedioPagoPeer::BANCO_ID)) {
            $modifiedColumns[':p' . $index++]  = '`BANCO_ID`';
        }
        if ($this->isColumnModified(MedioPagoPeer::PAGO_PROVEEDOR)) {
            $modifiedColumns[':p' . $index++]  = '`PAGO_PROVEEDOR`';
        }

        $sql = sprintf(
            'INSERT INTO `medio_pago` (%s) VALUES (%s)',
            implode(', ', $modifiedColumns),
            implode(', ', array_keys($modifiedColumns))
        );

        try {
            $stmt = $con->prepare($sql);
            foreach ($modifiedColumns as $identifier => $columnName) {
                switch ($columnName) {
                    case '`ID`':
                        $stmt->bindValue($identifier, $this->id, PDO::PARAM_INT);
                        break;
                    case '`EMPRESA_ID`':
                        $stmt->bindValue($identifier, $this->empresa_id, PDO::PARAM_INT);
                        break;
                    case '`CODIGO`':
                        $stmt->bindValue($identifier, $this->codigo, PDO::PARAM_STR);
                        break;
                    case '`NOMBRE`':
                        $stmt->bindValue($identifier, $this->nombre, PDO::PARAM_STR);
                        break;
                    case '`ACTIVO`':
                        $stmt->bindValue($identifier, (int) $this->activo, PDO::PARAM_INT);
                        break;
                    case '`CUENTA_CONTABLE`':
                        $stmt->bindValue($identifier, $this->cuenta_contable, PDO::PARAM_STR);
                        break;
                    case '`ORDEN`':
                        $stmt->bindValue($identifier, $this->orden, PDO::PARAM_INT);
                        break;
                    case '`POS`':
                        $stmt->bindValue($identifier, (int) $this->pos, PDO::PARAM_INT);
                        break;
                    case '`APLICA_MOV_BANCO`':
                        $stmt->bindValue($identifier, (int) $this->aplica_mov_banco, PDO::PARAM_INT);
                        break;
                    case '`CREATED_BY`':
                        $stmt->bindValue($identifier, $this->created_by, PDO::PARAM_STR);
                        break;
                    case '`UPDATED_BY`':
                        $stmt->bindValue($identifier, $this->updated_by, PDO::PARAM_STR);
                        break;
                    case '`CREATED_AT`':
                        $stmt->bindValue($identifier, $this->created_at, PDO::PARAM_STR);
                        break;
                    case '`UPDATED_AT`':
                        $stmt->bindValue($identifier, $this->updated_at, PDO::PARAM_STR);
                        break;
                    case '`APLICA_CUOTAS`':
                        $stmt->bindValue($identifier, (int) $this->aplica_cuotas, PDO::PARAM_INT);
                        break;
                    case '`NUMERO_CUOTAS`':
                        $stmt->bindValue($identifier, $this->numero_cuotas, PDO::PARAM_INT);
                        break;
                    case '`COMISION`':
                        $stmt->bindValue($identifier, $this->comision, PDO::PARAM_STR);
                        break;
                    case '`RETIENE_ISR`':
                        $stmt->bindValue($identifier, (int) $this->retiene_isr, PDO::PARAM_INT);
                        break;
                    case '`PIDE_BANCO`':
                        $stmt->bindValue($identifier, (int) $this->pide_banco, PDO::PARAM_INT);
                        break;
                    case '`BANCO_ID`':
                        $stmt->bindValue($identifier, $this->banco_id, PDO::PARAM_INT);
                        break;
                    case '`PAGO_PROVEEDOR`':
                        $stmt->bindValue($identifier, (int) $this->pago_proveedor, PDO::PARAM_INT);
                        break;
                }
            }
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute INSERT statement [%s]', $sql), $e);
        }

        try {
            $pk = $con->lastInsertId();
        } catch (Exception $e) {
            throw new PropelException('Unable to get autoincrement id.', $e);
        }
        $this->setId($pk);

        $this->setNew(false);
    }

    /**
     * Update the row in the database.
     *
     * @param PropelPDO $con
     *
     * @see        doSave()
     */
    protected function doUpdate(PropelPDO $con)
    {
        $selectCriteria = $this->buildPkeyCriteria();
        $valuesCriteria = $this->buildCriteria();
        BasePeer::doUpdate($selectCriteria, $valuesCriteria, $con);
    }

    /**
     * Array of ValidationFailed objects.
     * @var        array ValidationFailed[]
     */
    protected $validationFailures = array();

    /**
     * Gets any ValidationFailed objects that resulted from last call to validate().
     *
     *
     * @return array ValidationFailed[]
     * @see        validate()
     */
    public function getValidationFailures()
    {
        return $this->validationFailures;
    }

    /**
     * Validates the objects modified field values and all objects related to this table.
     *
     * If $columns is either a column name or an array of column names
     * only those columns are validated.
     *
     * @param mixed $columns Column name or an array of column names.
     * @return boolean Whether all columns pass validation.
     * @see        doValidate()
     * @see        getValidationFailures()
     */
    public function validate($columns = null)
    {
        $res = $this->doValidate($columns);
        if ($res === true) {
            $this->validationFailures = array();

            return true;
        } else {
            $this->validationFailures = $res;

            return false;
        }
    }

    /**
     * This function performs the validation work for complex object models.
     *
     * In addition to checking the current object, all related objects will
     * also be validated.  If all pass then <code>true</code> is returned; otherwise
     * an aggreagated array of ValidationFailed objects will be returned.
     *
     * @param array $columns Array of column names to validate.
     * @return mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
     */
    protected function doValidate($columns = null)
    {
        if (!$this->alreadyInValidation) {
            $this->alreadyInValidation = true;
            $retval = null;

            $failureMap = array();


            // We call the validate method on the following object(s) if they
            // were passed to this object by their coresponding set
            // method.  This object relates to these object(s) by a
            // foreign key reference.

            if ($this->aEmpresa !== null) {
                if (!$this->aEmpresa->validate($columns)) {
                    $failureMap = array_merge($failureMap, $this->aEmpresa->getValidationFailures());
                }
            }

            if ($this->aBanco !== null) {
                if (!$this->aBanco->validate($columns)) {
                    $failureMap = array_merge($failureMap, $this->aBanco->getValidationFailures());
                }
            }


            if (($retval = MedioPagoPeer::doValidate($this, $columns)) !== true) {
                $failureMap = array_merge($failureMap, $retval);
            }


                if ($this->collMovimientoBancos !== null) {
                    foreach ($this->collMovimientoBancos as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collPartidas !== null) {
                    foreach ($this->collPartidas as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collNotaCreditos !== null) {
                    foreach ($this->collNotaCreditos as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collVentaResumidas !== null) {
                    foreach ($this->collVentaResumidas as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collVentaResumidaLineas !== null) {
                    foreach ($this->collVentaResumidaLineas as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collEgresosRecibidos !== null) {
                    foreach ($this->collEgresosRecibidos as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }


            $this->alreadyInValidation = false;
        }

        return (!empty($failureMap) ? $failureMap : true);
    }

    /**
     * Retrieves a field from the object by name passed in as a string.
     *
     * @param string $name name
     * @param string $type The type of fieldname the $name is of:
     *               one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *               BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *               Defaults to BasePeer::TYPE_PHPNAME
     * @return mixed Value of field.
     */
    public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = MedioPagoPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
        $field = $this->getByPosition($pos);

        return $field;
    }

    /**
     * Retrieves a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @return mixed Value of field at $pos
     */
    public function getByPosition($pos)
    {
        switch ($pos) {
            case 0:
                return $this->getId();
                break;
            case 1:
                return $this->getEmpresaId();
                break;
            case 2:
                return $this->getCodigo();
                break;
            case 3:
                return $this->getNombre();
                break;
            case 4:
                return $this->getActivo();
                break;
            case 5:
                return $this->getCuentaContable();
                break;
            case 6:
                return $this->getOrden();
                break;
            case 7:
                return $this->getPos();
                break;
            case 8:
                return $this->getAplicaMovBanco();
                break;
            case 9:
                return $this->getCreatedBy();
                break;
            case 10:
                return $this->getUpdatedBy();
                break;
            case 11:
                return $this->getCreatedAt();
                break;
            case 12:
                return $this->getUpdatedAt();
                break;
            case 13:
                return $this->getAplicaCuotas();
                break;
            case 14:
                return $this->getNumeroCuotas();
                break;
            case 15:
                return $this->getComision();
                break;
            case 16:
                return $this->getRetieneIsr();
                break;
            case 17:
                return $this->getPideBanco();
                break;
            case 18:
                return $this->getBancoId();
                break;
            case 19:
                return $this->getPagoProveedor();
                break;
            default:
                return null;
                break;
        } // switch()
    }

    /**
     * Exports the object as an array.
     *
     * You can specify the key type of the array by passing one of the class
     * type constants.
     *
     * @param     string  $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     *                    BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                    Defaults to BasePeer::TYPE_PHPNAME.
     * @param     boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns. Defaults to true.
     * @param     array $alreadyDumpedObjects List of objects to skip to avoid recursion
     * @param     boolean $includeForeignObjects (optional) Whether to include hydrated related objects. Default to FALSE.
     *
     * @return array an associative array containing the field names (as keys) and field values
     */
    public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true, $alreadyDumpedObjects = array(), $includeForeignObjects = false)
    {
        if (isset($alreadyDumpedObjects['MedioPago'][$this->getPrimaryKey()])) {
            return '*RECURSION*';
        }
        $alreadyDumpedObjects['MedioPago'][$this->getPrimaryKey()] = true;
        $keys = MedioPagoPeer::getFieldNames($keyType);
        $result = array(
            $keys[0] => $this->getId(),
            $keys[1] => $this->getEmpresaId(),
            $keys[2] => $this->getCodigo(),
            $keys[3] => $this->getNombre(),
            $keys[4] => $this->getActivo(),
            $keys[5] => $this->getCuentaContable(),
            $keys[6] => $this->getOrden(),
            $keys[7] => $this->getPos(),
            $keys[8] => $this->getAplicaMovBanco(),
            $keys[9] => $this->getCreatedBy(),
            $keys[10] => $this->getUpdatedBy(),
            $keys[11] => $this->getCreatedAt(),
            $keys[12] => $this->getUpdatedAt(),
            $keys[13] => $this->getAplicaCuotas(),
            $keys[14] => $this->getNumeroCuotas(),
            $keys[15] => $this->getComision(),
            $keys[16] => $this->getRetieneIsr(),
            $keys[17] => $this->getPideBanco(),
            $keys[18] => $this->getBancoId(),
            $keys[19] => $this->getPagoProveedor(),
        );
        if ($includeForeignObjects) {
            if (null !== $this->aEmpresa) {
                $result['Empresa'] = $this->aEmpresa->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
            }
            if (null !== $this->aBanco) {
                $result['Banco'] = $this->aBanco->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
            }
            if (null !== $this->collMovimientoBancos) {
                $result['MovimientoBancos'] = $this->collMovimientoBancos->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collPartidas) {
                $result['Partidas'] = $this->collPartidas->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collNotaCreditos) {
                $result['NotaCreditos'] = $this->collNotaCreditos->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collVentaResumidas) {
                $result['VentaResumidas'] = $this->collVentaResumidas->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collVentaResumidaLineas) {
                $result['VentaResumidaLineas'] = $this->collVentaResumidaLineas->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collEgresosRecibidos) {
                $result['EgresosRecibidos'] = $this->collEgresosRecibidos->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
        }

        return $result;
    }

    /**
     * Sets a field from the object by name passed in as a string.
     *
     * @param string $name peer name
     * @param mixed $value field value
     * @param string $type The type of fieldname the $name is of:
     *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                     Defaults to BasePeer::TYPE_PHPNAME
     * @return void
     */
    public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = MedioPagoPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);

        $this->setByPosition($pos, $value);
    }

    /**
     * Sets a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @param mixed $value field value
     * @return void
     */
    public function setByPosition($pos, $value)
    {
        switch ($pos) {
            case 0:
                $this->setId($value);
                break;
            case 1:
                $this->setEmpresaId($value);
                break;
            case 2:
                $this->setCodigo($value);
                break;
            case 3:
                $this->setNombre($value);
                break;
            case 4:
                $this->setActivo($value);
                break;
            case 5:
                $this->setCuentaContable($value);
                break;
            case 6:
                $this->setOrden($value);
                break;
            case 7:
                $this->setPos($value);
                break;
            case 8:
                $this->setAplicaMovBanco($value);
                break;
            case 9:
                $this->setCreatedBy($value);
                break;
            case 10:
                $this->setUpdatedBy($value);
                break;
            case 11:
                $this->setCreatedAt($value);
                break;
            case 12:
                $this->setUpdatedAt($value);
                break;
            case 13:
                $this->setAplicaCuotas($value);
                break;
            case 14:
                $this->setNumeroCuotas($value);
                break;
            case 15:
                $this->setComision($value);
                break;
            case 16:
                $this->setRetieneIsr($value);
                break;
            case 17:
                $this->setPideBanco($value);
                break;
            case 18:
                $this->setBancoId($value);
                break;
            case 19:
                $this->setPagoProveedor($value);
                break;
        } // switch()
    }

    /**
     * Populates the object using an array.
     *
     * This is particularly useful when populating an object from one of the
     * request arrays (e.g. $_POST).  This method goes through the column
     * names, checking to see whether a matching key exists in populated
     * array. If so the setByName() method is called for that column.
     *
     * You can specify the key type of the array by additionally passing one
     * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     * The default key type is the column's BasePeer::TYPE_PHPNAME
     *
     * @param array  $arr     An array to populate the object from.
     * @param string $keyType The type of keys the array uses.
     * @return void
     */
    public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
    {
        $keys = MedioPagoPeer::getFieldNames($keyType);

        if (array_key_exists($keys[0], $arr)) $this->setId($arr[$keys[0]]);
        if (array_key_exists($keys[1], $arr)) $this->setEmpresaId($arr[$keys[1]]);
        if (array_key_exists($keys[2], $arr)) $this->setCodigo($arr[$keys[2]]);
        if (array_key_exists($keys[3], $arr)) $this->setNombre($arr[$keys[3]]);
        if (array_key_exists($keys[4], $arr)) $this->setActivo($arr[$keys[4]]);
        if (array_key_exists($keys[5], $arr)) $this->setCuentaContable($arr[$keys[5]]);
        if (array_key_exists($keys[6], $arr)) $this->setOrden($arr[$keys[6]]);
        if (array_key_exists($keys[7], $arr)) $this->setPos($arr[$keys[7]]);
        if (array_key_exists($keys[8], $arr)) $this->setAplicaMovBanco($arr[$keys[8]]);
        if (array_key_exists($keys[9], $arr)) $this->setCreatedBy($arr[$keys[9]]);
        if (array_key_exists($keys[10], $arr)) $this->setUpdatedBy($arr[$keys[10]]);
        if (array_key_exists($keys[11], $arr)) $this->setCreatedAt($arr[$keys[11]]);
        if (array_key_exists($keys[12], $arr)) $this->setUpdatedAt($arr[$keys[12]]);
        if (array_key_exists($keys[13], $arr)) $this->setAplicaCuotas($arr[$keys[13]]);
        if (array_key_exists($keys[14], $arr)) $this->setNumeroCuotas($arr[$keys[14]]);
        if (array_key_exists($keys[15], $arr)) $this->setComision($arr[$keys[15]]);
        if (array_key_exists($keys[16], $arr)) $this->setRetieneIsr($arr[$keys[16]]);
        if (array_key_exists($keys[17], $arr)) $this->setPideBanco($arr[$keys[17]]);
        if (array_key_exists($keys[18], $arr)) $this->setBancoId($arr[$keys[18]]);
        if (array_key_exists($keys[19], $arr)) $this->setPagoProveedor($arr[$keys[19]]);
    }

    /**
     * Build a Criteria object containing the values of all modified columns in this object.
     *
     * @return Criteria The Criteria object containing all modified values.
     */
    public function buildCriteria()
    {
        $criteria = new Criteria(MedioPagoPeer::DATABASE_NAME);

        if ($this->isColumnModified(MedioPagoPeer::ID)) $criteria->add(MedioPagoPeer::ID, $this->id);
        if ($this->isColumnModified(MedioPagoPeer::EMPRESA_ID)) $criteria->add(MedioPagoPeer::EMPRESA_ID, $this->empresa_id);
        if ($this->isColumnModified(MedioPagoPeer::CODIGO)) $criteria->add(MedioPagoPeer::CODIGO, $this->codigo);
        if ($this->isColumnModified(MedioPagoPeer::NOMBRE)) $criteria->add(MedioPagoPeer::NOMBRE, $this->nombre);
        if ($this->isColumnModified(MedioPagoPeer::ACTIVO)) $criteria->add(MedioPagoPeer::ACTIVO, $this->activo);
        if ($this->isColumnModified(MedioPagoPeer::CUENTA_CONTABLE)) $criteria->add(MedioPagoPeer::CUENTA_CONTABLE, $this->cuenta_contable);
        if ($this->isColumnModified(MedioPagoPeer::ORDEN)) $criteria->add(MedioPagoPeer::ORDEN, $this->orden);
        if ($this->isColumnModified(MedioPagoPeer::POS)) $criteria->add(MedioPagoPeer::POS, $this->pos);
        if ($this->isColumnModified(MedioPagoPeer::APLICA_MOV_BANCO)) $criteria->add(MedioPagoPeer::APLICA_MOV_BANCO, $this->aplica_mov_banco);
        if ($this->isColumnModified(MedioPagoPeer::CREATED_BY)) $criteria->add(MedioPagoPeer::CREATED_BY, $this->created_by);
        if ($this->isColumnModified(MedioPagoPeer::UPDATED_BY)) $criteria->add(MedioPagoPeer::UPDATED_BY, $this->updated_by);
        if ($this->isColumnModified(MedioPagoPeer::CREATED_AT)) $criteria->add(MedioPagoPeer::CREATED_AT, $this->created_at);
        if ($this->isColumnModified(MedioPagoPeer::UPDATED_AT)) $criteria->add(MedioPagoPeer::UPDATED_AT, $this->updated_at);
        if ($this->isColumnModified(MedioPagoPeer::APLICA_CUOTAS)) $criteria->add(MedioPagoPeer::APLICA_CUOTAS, $this->aplica_cuotas);
        if ($this->isColumnModified(MedioPagoPeer::NUMERO_CUOTAS)) $criteria->add(MedioPagoPeer::NUMERO_CUOTAS, $this->numero_cuotas);
        if ($this->isColumnModified(MedioPagoPeer::COMISION)) $criteria->add(MedioPagoPeer::COMISION, $this->comision);
        if ($this->isColumnModified(MedioPagoPeer::RETIENE_ISR)) $criteria->add(MedioPagoPeer::RETIENE_ISR, $this->retiene_isr);
        if ($this->isColumnModified(MedioPagoPeer::PIDE_BANCO)) $criteria->add(MedioPagoPeer::PIDE_BANCO, $this->pide_banco);
        if ($this->isColumnModified(MedioPagoPeer::BANCO_ID)) $criteria->add(MedioPagoPeer::BANCO_ID, $this->banco_id);
        if ($this->isColumnModified(MedioPagoPeer::PAGO_PROVEEDOR)) $criteria->add(MedioPagoPeer::PAGO_PROVEEDOR, $this->pago_proveedor);

        return $criteria;
    }

    /**
     * Builds a Criteria object containing the primary key for this object.
     *
     * Unlike buildCriteria() this method includes the primary key values regardless
     * of whether or not they have been modified.
     *
     * @return Criteria The Criteria object containing value(s) for primary key(s).
     */
    public function buildPkeyCriteria()
    {
        $criteria = new Criteria(MedioPagoPeer::DATABASE_NAME);
        $criteria->add(MedioPagoPeer::ID, $this->id);

        return $criteria;
    }

    /**
     * Returns the primary key for this object (row).
     * @return int
     */
    public function getPrimaryKey()
    {
        return $this->getId();
    }

    /**
     * Generic method to set the primary key (id column).
     *
     * @param  int $key Primary key.
     * @return void
     */
    public function setPrimaryKey($key)
    {
        $this->setId($key);
    }

    /**
     * Returns true if the primary key for this object is null.
     * @return boolean
     */
    public function isPrimaryKeyNull()
    {

        return null === $this->getId();
    }

    /**
     * Sets contents of passed object to values from current object.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param object $copyObj An object of MedioPago (or compatible) type.
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @param boolean $makeNew Whether to reset autoincrement PKs and make the object new.
     * @throws PropelException
     */
    public function copyInto($copyObj, $deepCopy = false, $makeNew = true)
    {
        $copyObj->setEmpresaId($this->getEmpresaId());
        $copyObj->setCodigo($this->getCodigo());
        $copyObj->setNombre($this->getNombre());
        $copyObj->setActivo($this->getActivo());
        $copyObj->setCuentaContable($this->getCuentaContable());
        $copyObj->setOrden($this->getOrden());
        $copyObj->setPos($this->getPos());
        $copyObj->setAplicaMovBanco($this->getAplicaMovBanco());
        $copyObj->setCreatedBy($this->getCreatedBy());
        $copyObj->setUpdatedBy($this->getUpdatedBy());
        $copyObj->setCreatedAt($this->getCreatedAt());
        $copyObj->setUpdatedAt($this->getUpdatedAt());
        $copyObj->setAplicaCuotas($this->getAplicaCuotas());
        $copyObj->setNumeroCuotas($this->getNumeroCuotas());
        $copyObj->setComision($this->getComision());
        $copyObj->setRetieneIsr($this->getRetieneIsr());
        $copyObj->setPideBanco($this->getPideBanco());
        $copyObj->setBancoId($this->getBancoId());
        $copyObj->setPagoProveedor($this->getPagoProveedor());

        if ($deepCopy && !$this->startCopy) {
            // important: temporarily setNew(false) because this affects the behavior of
            // the getter/setter methods for fkey referrer objects.
            $copyObj->setNew(false);
            // store object hash to prevent cycle
            $this->startCopy = true;

            foreach ($this->getMovimientoBancos() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addMovimientoBanco($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getPartidas() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addPartida($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getNotaCreditos() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addNotaCredito($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getVentaResumidas() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addVentaResumida($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getVentaResumidaLineas() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addVentaResumidaLinea($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getEgresosRecibidos() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addEgresosRecibido($relObj->copy($deepCopy));
                }
            }

            //unflag object copy
            $this->startCopy = false;
        } // if ($deepCopy)

        if ($makeNew) {
            $copyObj->setNew(true);
            $copyObj->setId(NULL); // this is a auto-increment column, so set to default value
        }
    }

    /**
     * Makes a copy of this object that will be inserted as a new row in table when saved.
     * It creates a new object filling in the simple attributes, but skipping any primary
     * keys that are defined for the table.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @return MedioPago Clone of current object.
     * @throws PropelException
     */
    public function copy($deepCopy = false)
    {
        // we use get_class(), because this might be a subclass
        $clazz = get_class($this);
        $copyObj = new $clazz();
        $this->copyInto($copyObj, $deepCopy);

        return $copyObj;
    }

    /**
     * Returns a peer instance associated with this om.
     *
     * Since Peer classes are not to have any instance attributes, this method returns the
     * same instance for all member of this class. The method could therefore
     * be static, but this would prevent one from overriding the behavior.
     *
     * @return MedioPagoPeer
     */
    public function getPeer()
    {
        if (self::$peer === null) {
            self::$peer = new MedioPagoPeer();
        }

        return self::$peer;
    }

    /**
     * Declares an association between this object and a Empresa object.
     *
     * @param             Empresa $v
     * @return MedioPago The current object (for fluent API support)
     * @throws PropelException
     */
    public function setEmpresa(Empresa $v = null)
    {
        if ($v === null) {
            $this->setEmpresaId(NULL);
        } else {
            $this->setEmpresaId($v->getId());
        }

        $this->aEmpresa = $v;

        // Add binding for other direction of this n:n relationship.
        // If this object has already been added to the Empresa object, it will not be re-added.
        if ($v !== null) {
            $v->addMedioPago($this);
        }


        return $this;
    }


    /**
     * Get the associated Empresa object
     *
     * @param PropelPDO $con Optional Connection object.
     * @return Empresa The associated Empresa object.
     * @throws PropelException
     */
    public function getEmpresa(PropelPDO $con = null)
    {
        if ($this->aEmpresa === null && ($this->empresa_id !== null)) {
            $this->aEmpresa = EmpresaQuery::create()->findPk($this->empresa_id, $con);
            /* The following can be used additionally to
                guarantee the related object contains a reference
                to this object.  This level of coupling may, however, be
                undesirable since it could result in an only partially populated collection
                in the referenced object.
                $this->aEmpresa->addMedioPagos($this);
             */
        }

        return $this->aEmpresa;
    }

    /**
     * Declares an association between this object and a Banco object.
     *
     * @param             Banco $v
     * @return MedioPago The current object (for fluent API support)
     * @throws PropelException
     */
    public function setBanco(Banco $v = null)
    {
        if ($v === null) {
            $this->setBancoId(NULL);
        } else {
            $this->setBancoId($v->getId());
        }

        $this->aBanco = $v;

        // Add binding for other direction of this n:n relationship.
        // If this object has already been added to the Banco object, it will not be re-added.
        if ($v !== null) {
            $v->addMedioPago($this);
        }


        return $this;
    }


    /**
     * Get the associated Banco object
     *
     * @param PropelPDO $con Optional Connection object.
     * @return Banco The associated Banco object.
     * @throws PropelException
     */
    public function getBanco(PropelPDO $con = null)
    {
        if ($this->aBanco === null && ($this->banco_id !== null)) {
            $this->aBanco = BancoQuery::create()->findPk($this->banco_id, $con);
            /* The following can be used additionally to
                guarantee the related object contains a reference
                to this object.  This level of coupling may, however, be
                undesirable since it could result in an only partially populated collection
                in the referenced object.
                $this->aBanco->addMedioPagos($this);
             */
        }

        return $this->aBanco;
    }


    /**
     * Initializes a collection based on the name of a relation.
     * Avoids crafting an 'init[$relationName]s' method name
     * that wouldn't work when StandardEnglishPluralizer is used.
     *
     * @param string $relationName The name of the relation to initialize
     * @return void
     */
    public function initRelation($relationName)
    {
        if ('MovimientoBanco' == $relationName) {
            $this->initMovimientoBancos();
        }
        if ('Partida' == $relationName) {
            $this->initPartidas();
        }
        if ('NotaCredito' == $relationName) {
            $this->initNotaCreditos();
        }
        if ('VentaResumida' == $relationName) {
            $this->initVentaResumidas();
        }
        if ('VentaResumidaLinea' == $relationName) {
            $this->initVentaResumidaLineas();
        }
        if ('EgresosRecibido' == $relationName) {
            $this->initEgresosRecibidos();
        }
    }

    /**
     * Clears out the collMovimientoBancos collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return void
     * @see        addMovimientoBancos()
     */
    public function clearMovimientoBancos()
    {
        $this->collMovimientoBancos = null; // important to set this to null since that means it is uninitialized
        $this->collMovimientoBancosPartial = null;
    }

    /**
     * reset is the collMovimientoBancos collection loaded partially
     *
     * @return void
     */
    public function resetPartialMovimientoBancos($v = true)
    {
        $this->collMovimientoBancosPartial = $v;
    }

    /**
     * Initializes the collMovimientoBancos collection.
     *
     * By default this just sets the collMovimientoBancos collection to an empty array (like clearcollMovimientoBancos());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initMovimientoBancos($overrideExisting = true)
    {
        if (null !== $this->collMovimientoBancos && !$overrideExisting) {
            return;
        }
        $this->collMovimientoBancos = new PropelObjectCollection();
        $this->collMovimientoBancos->setModel('MovimientoBanco');
    }

    /**
     * Gets an array of MovimientoBanco objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this MedioPago is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|MovimientoBanco[] List of MovimientoBanco objects
     * @throws PropelException
     */
    public function getMovimientoBancos($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collMovimientoBancosPartial && !$this->isNew();
        if (null === $this->collMovimientoBancos || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collMovimientoBancos) {
                // return empty collection
                $this->initMovimientoBancos();
            } else {
                $collMovimientoBancos = MovimientoBancoQuery::create(null, $criteria)
                    ->filterByMedioPago($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collMovimientoBancosPartial && count($collMovimientoBancos)) {
                      $this->initMovimientoBancos(false);

                      foreach($collMovimientoBancos as $obj) {
                        if (false == $this->collMovimientoBancos->contains($obj)) {
                          $this->collMovimientoBancos->append($obj);
                        }
                      }

                      $this->collMovimientoBancosPartial = true;
                    }

                    return $collMovimientoBancos;
                }

                if($partial && $this->collMovimientoBancos) {
                    foreach($this->collMovimientoBancos as $obj) {
                        if($obj->isNew()) {
                            $collMovimientoBancos[] = $obj;
                        }
                    }
                }

                $this->collMovimientoBancos = $collMovimientoBancos;
                $this->collMovimientoBancosPartial = false;
            }
        }

        return $this->collMovimientoBancos;
    }

    /**
     * Sets a collection of MovimientoBanco objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $movimientoBancos A Propel collection.
     * @param PropelPDO $con Optional connection object
     */
    public function setMovimientoBancos(PropelCollection $movimientoBancos, PropelPDO $con = null)
    {
        $this->movimientoBancosScheduledForDeletion = $this->getMovimientoBancos(new Criteria(), $con)->diff($movimientoBancos);

        foreach ($this->movimientoBancosScheduledForDeletion as $movimientoBancoRemoved) {
            $movimientoBancoRemoved->setMedioPago(null);
        }

        $this->collMovimientoBancos = null;
        foreach ($movimientoBancos as $movimientoBanco) {
            $this->addMovimientoBanco($movimientoBanco);
        }

        $this->collMovimientoBancos = $movimientoBancos;
        $this->collMovimientoBancosPartial = false;
    }

    /**
     * Returns the number of related MovimientoBanco objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related MovimientoBanco objects.
     * @throws PropelException
     */
    public function countMovimientoBancos(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collMovimientoBancosPartial && !$this->isNew();
        if (null === $this->collMovimientoBancos || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collMovimientoBancos) {
                return 0;
            } else {
                if($partial && !$criteria) {
                    return count($this->getMovimientoBancos());
                }
                $query = MovimientoBancoQuery::create(null, $criteria);
                if ($distinct) {
                    $query->distinct();
                }

                return $query
                    ->filterByMedioPago($this)
                    ->count($con);
            }
        } else {
            return count($this->collMovimientoBancos);
        }
    }

    /**
     * Method called to associate a MovimientoBanco object to this object
     * through the MovimientoBanco foreign key attribute.
     *
     * @param    MovimientoBanco $l MovimientoBanco
     * @return MedioPago The current object (for fluent API support)
     */
    public function addMovimientoBanco(MovimientoBanco $l)
    {
        if ($this->collMovimientoBancos === null) {
            $this->initMovimientoBancos();
            $this->collMovimientoBancosPartial = true;
        }
        if (!$this->collMovimientoBancos->contains($l)) { // only add it if the **same** object is not already associated
            $this->doAddMovimientoBanco($l);
        }

        return $this;
    }

    /**
     * @param	MovimientoBanco $movimientoBanco The movimientoBanco object to add.
     */
    protected function doAddMovimientoBanco($movimientoBanco)
    {
        $this->collMovimientoBancos[]= $movimientoBanco;
        $movimientoBanco->setMedioPago($this);
    }

    /**
     * @param	MovimientoBanco $movimientoBanco The movimientoBanco object to remove.
     */
    public function removeMovimientoBanco($movimientoBanco)
    {
        if ($this->getMovimientoBancos()->contains($movimientoBanco)) {
            $this->collMovimientoBancos->remove($this->collMovimientoBancos->search($movimientoBanco));
            if (null === $this->movimientoBancosScheduledForDeletion) {
                $this->movimientoBancosScheduledForDeletion = clone $this->collMovimientoBancos;
                $this->movimientoBancosScheduledForDeletion->clear();
            }
            $this->movimientoBancosScheduledForDeletion[]= $movimientoBanco;
            $movimientoBanco->setMedioPago(null);
        }
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this MedioPago is new, it will return
     * an empty collection; or if this MedioPago has previously
     * been saved, it will retrieve related MovimientoBancos from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in MedioPago.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|MovimientoBanco[] List of MovimientoBanco objects
     */
    public function getMovimientoBancosJoinEmpresa($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = MovimientoBancoQuery::create(null, $criteria);
        $query->joinWith('Empresa', $join_behavior);

        return $this->getMovimientoBancos($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this MedioPago is new, it will return
     * an empty collection; or if this MedioPago has previously
     * been saved, it will retrieve related MovimientoBancos from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in MedioPago.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|MovimientoBanco[] List of MovimientoBanco objects
     */
    public function getMovimientoBancosJoinBancoRelatedByBancoId($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = MovimientoBancoQuery::create(null, $criteria);
        $query->joinWith('BancoRelatedByBancoId', $join_behavior);

        return $this->getMovimientoBancos($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this MedioPago is new, it will return
     * an empty collection; or if this MedioPago has previously
     * been saved, it will retrieve related MovimientoBancos from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in MedioPago.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|MovimientoBanco[] List of MovimientoBanco objects
     */
    public function getMovimientoBancosJoinBancoRelatedByBancoOrigen($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = MovimientoBancoQuery::create(null, $criteria);
        $query->joinWith('BancoRelatedByBancoOrigen', $join_behavior);

        return $this->getMovimientoBancos($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this MedioPago is new, it will return
     * an empty collection; or if this MedioPago has previously
     * been saved, it will retrieve related MovimientoBancos from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in MedioPago.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|MovimientoBanco[] List of MovimientoBanco objects
     */
    public function getMovimientoBancosJoinTienda($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = MovimientoBancoQuery::create(null, $criteria);
        $query->joinWith('Tienda', $join_behavior);

        return $this->getMovimientoBancos($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this MedioPago is new, it will return
     * an empty collection; or if this MedioPago has previously
     * been saved, it will retrieve related MovimientoBancos from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in MedioPago.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|MovimientoBanco[] List of MovimientoBanco objects
     */
    public function getMovimientoBancosJoinBitacoraArchivo($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = MovimientoBancoQuery::create(null, $criteria);
        $query->joinWith('BitacoraArchivo', $join_behavior);

        return $this->getMovimientoBancos($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this MedioPago is new, it will return
     * an empty collection; or if this MedioPago has previously
     * been saved, it will retrieve related MovimientoBancos from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in MedioPago.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|MovimientoBanco[] List of MovimientoBanco objects
     */
    public function getMovimientoBancosJoinVentaResumidaLinea($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = MovimientoBancoQuery::create(null, $criteria);
        $query->joinWith('VentaResumidaLinea', $join_behavior);

        return $this->getMovimientoBancos($query, $con);
    }

    /**
     * Clears out the collPartidas collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return void
     * @see        addPartidas()
     */
    public function clearPartidas()
    {
        $this->collPartidas = null; // important to set this to null since that means it is uninitialized
        $this->collPartidasPartial = null;
    }

    /**
     * reset is the collPartidas collection loaded partially
     *
     * @return void
     */
    public function resetPartialPartidas($v = true)
    {
        $this->collPartidasPartial = $v;
    }

    /**
     * Initializes the collPartidas collection.
     *
     * By default this just sets the collPartidas collection to an empty array (like clearcollPartidas());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initPartidas($overrideExisting = true)
    {
        if (null !== $this->collPartidas && !$overrideExisting) {
            return;
        }
        $this->collPartidas = new PropelObjectCollection();
        $this->collPartidas->setModel('Partida');
    }

    /**
     * Gets an array of Partida objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this MedioPago is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|Partida[] List of Partida objects
     * @throws PropelException
     */
    public function getPartidas($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collPartidasPartial && !$this->isNew();
        if (null === $this->collPartidas || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collPartidas) {
                // return empty collection
                $this->initPartidas();
            } else {
                $collPartidas = PartidaQuery::create(null, $criteria)
                    ->filterByMedioPago($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collPartidasPartial && count($collPartidas)) {
                      $this->initPartidas(false);

                      foreach($collPartidas as $obj) {
                        if (false == $this->collPartidas->contains($obj)) {
                          $this->collPartidas->append($obj);
                        }
                      }

                      $this->collPartidasPartial = true;
                    }

                    return $collPartidas;
                }

                if($partial && $this->collPartidas) {
                    foreach($this->collPartidas as $obj) {
                        if($obj->isNew()) {
                            $collPartidas[] = $obj;
                        }
                    }
                }

                $this->collPartidas = $collPartidas;
                $this->collPartidasPartial = false;
            }
        }

        return $this->collPartidas;
    }

    /**
     * Sets a collection of Partida objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $partidas A Propel collection.
     * @param PropelPDO $con Optional connection object
     */
    public function setPartidas(PropelCollection $partidas, PropelPDO $con = null)
    {
        $this->partidasScheduledForDeletion = $this->getPartidas(new Criteria(), $con)->diff($partidas);

        foreach ($this->partidasScheduledForDeletion as $partidaRemoved) {
            $partidaRemoved->setMedioPago(null);
        }

        $this->collPartidas = null;
        foreach ($partidas as $partida) {
            $this->addPartida($partida);
        }

        $this->collPartidas = $partidas;
        $this->collPartidasPartial = false;
    }

    /**
     * Returns the number of related Partida objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related Partida objects.
     * @throws PropelException
     */
    public function countPartidas(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collPartidasPartial && !$this->isNew();
        if (null === $this->collPartidas || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collPartidas) {
                return 0;
            } else {
                if($partial && !$criteria) {
                    return count($this->getPartidas());
                }
                $query = PartidaQuery::create(null, $criteria);
                if ($distinct) {
                    $query->distinct();
                }

                return $query
                    ->filterByMedioPago($this)
                    ->count($con);
            }
        } else {
            return count($this->collPartidas);
        }
    }

    /**
     * Method called to associate a Partida object to this object
     * through the Partida foreign key attribute.
     *
     * @param    Partida $l Partida
     * @return MedioPago The current object (for fluent API support)
     */
    public function addPartida(Partida $l)
    {
        if ($this->collPartidas === null) {
            $this->initPartidas();
            $this->collPartidasPartial = true;
        }
        if (!$this->collPartidas->contains($l)) { // only add it if the **same** object is not already associated
            $this->doAddPartida($l);
        }

        return $this;
    }

    /**
     * @param	Partida $partida The partida object to add.
     */
    protected function doAddPartida($partida)
    {
        $this->collPartidas[]= $partida;
        $partida->setMedioPago($this);
    }

    /**
     * @param	Partida $partida The partida object to remove.
     */
    public function removePartida($partida)
    {
        if ($this->getPartidas()->contains($partida)) {
            $this->collPartidas->remove($this->collPartidas->search($partida));
            if (null === $this->partidasScheduledForDeletion) {
                $this->partidasScheduledForDeletion = clone $this->collPartidas;
                $this->partidasScheduledForDeletion->clear();
            }
            $this->partidasScheduledForDeletion[]= $partida;
            $partida->setMedioPago(null);
        }
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this MedioPago is new, it will return
     * an empty collection; or if this MedioPago has previously
     * been saved, it will retrieve related Partidas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in MedioPago.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Partida[] List of Partida objects
     */
    public function getPartidasJoinEmpresa($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = PartidaQuery::create(null, $criteria);
        $query->joinWith('Empresa', $join_behavior);

        return $this->getPartidas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this MedioPago is new, it will return
     * an empty collection; or if this MedioPago has previously
     * been saved, it will retrieve related Partidas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in MedioPago.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Partida[] List of Partida objects
     */
    public function getPartidasJoinTienda($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = PartidaQuery::create(null, $criteria);
        $query->joinWith('Tienda', $join_behavior);

        return $this->getPartidas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this MedioPago is new, it will return
     * an empty collection; or if this MedioPago has previously
     * been saved, it will retrieve related Partidas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in MedioPago.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Partida[] List of Partida objects
     */
    public function getPartidasJoinPartidaAgrupa($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = PartidaQuery::create(null, $criteria);
        $query->joinWith('PartidaAgrupa', $join_behavior);

        return $this->getPartidas($query, $con);
    }

    /**
     * Clears out the collNotaCreditos collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return void
     * @see        addNotaCreditos()
     */
    public function clearNotaCreditos()
    {
        $this->collNotaCreditos = null; // important to set this to null since that means it is uninitialized
        $this->collNotaCreditosPartial = null;
    }

    /**
     * reset is the collNotaCreditos collection loaded partially
     *
     * @return void
     */
    public function resetPartialNotaCreditos($v = true)
    {
        $this->collNotaCreditosPartial = $v;
    }

    /**
     * Initializes the collNotaCreditos collection.
     *
     * By default this just sets the collNotaCreditos collection to an empty array (like clearcollNotaCreditos());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initNotaCreditos($overrideExisting = true)
    {
        if (null !== $this->collNotaCreditos && !$overrideExisting) {
            return;
        }
        $this->collNotaCreditos = new PropelObjectCollection();
        $this->collNotaCreditos->setModel('NotaCredito');
    }

    /**
     * Gets an array of NotaCredito objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this MedioPago is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|NotaCredito[] List of NotaCredito objects
     * @throws PropelException
     */
    public function getNotaCreditos($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collNotaCreditosPartial && !$this->isNew();
        if (null === $this->collNotaCreditos || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collNotaCreditos) {
                // return empty collection
                $this->initNotaCreditos();
            } else {
                $collNotaCreditos = NotaCreditoQuery::create(null, $criteria)
                    ->filterByMedioPago($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collNotaCreditosPartial && count($collNotaCreditos)) {
                      $this->initNotaCreditos(false);

                      foreach($collNotaCreditos as $obj) {
                        if (false == $this->collNotaCreditos->contains($obj)) {
                          $this->collNotaCreditos->append($obj);
                        }
                      }

                      $this->collNotaCreditosPartial = true;
                    }

                    return $collNotaCreditos;
                }

                if($partial && $this->collNotaCreditos) {
                    foreach($this->collNotaCreditos as $obj) {
                        if($obj->isNew()) {
                            $collNotaCreditos[] = $obj;
                        }
                    }
                }

                $this->collNotaCreditos = $collNotaCreditos;
                $this->collNotaCreditosPartial = false;
            }
        }

        return $this->collNotaCreditos;
    }

    /**
     * Sets a collection of NotaCredito objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $notaCreditos A Propel collection.
     * @param PropelPDO $con Optional connection object
     */
    public function setNotaCreditos(PropelCollection $notaCreditos, PropelPDO $con = null)
    {
        $this->notaCreditosScheduledForDeletion = $this->getNotaCreditos(new Criteria(), $con)->diff($notaCreditos);

        foreach ($this->notaCreditosScheduledForDeletion as $notaCreditoRemoved) {
            $notaCreditoRemoved->setMedioPago(null);
        }

        $this->collNotaCreditos = null;
        foreach ($notaCreditos as $notaCredito) {
            $this->addNotaCredito($notaCredito);
        }

        $this->collNotaCreditos = $notaCreditos;
        $this->collNotaCreditosPartial = false;
    }

    /**
     * Returns the number of related NotaCredito objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related NotaCredito objects.
     * @throws PropelException
     */
    public function countNotaCreditos(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collNotaCreditosPartial && !$this->isNew();
        if (null === $this->collNotaCreditos || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collNotaCreditos) {
                return 0;
            } else {
                if($partial && !$criteria) {
                    return count($this->getNotaCreditos());
                }
                $query = NotaCreditoQuery::create(null, $criteria);
                if ($distinct) {
                    $query->distinct();
                }

                return $query
                    ->filterByMedioPago($this)
                    ->count($con);
            }
        } else {
            return count($this->collNotaCreditos);
        }
    }

    /**
     * Method called to associate a NotaCredito object to this object
     * through the NotaCredito foreign key attribute.
     *
     * @param    NotaCredito $l NotaCredito
     * @return MedioPago The current object (for fluent API support)
     */
    public function addNotaCredito(NotaCredito $l)
    {
        if ($this->collNotaCreditos === null) {
            $this->initNotaCreditos();
            $this->collNotaCreditosPartial = true;
        }
        if (!$this->collNotaCreditos->contains($l)) { // only add it if the **same** object is not already associated
            $this->doAddNotaCredito($l);
        }

        return $this;
    }

    /**
     * @param	NotaCredito $notaCredito The notaCredito object to add.
     */
    protected function doAddNotaCredito($notaCredito)
    {
        $this->collNotaCreditos[]= $notaCredito;
        $notaCredito->setMedioPago($this);
    }

    /**
     * @param	NotaCredito $notaCredito The notaCredito object to remove.
     */
    public function removeNotaCredito($notaCredito)
    {
        if ($this->getNotaCreditos()->contains($notaCredito)) {
            $this->collNotaCreditos->remove($this->collNotaCreditos->search($notaCredito));
            if (null === $this->notaCreditosScheduledForDeletion) {
                $this->notaCreditosScheduledForDeletion = clone $this->collNotaCreditos;
                $this->notaCreditosScheduledForDeletion->clear();
            }
            $this->notaCreditosScheduledForDeletion[]= $notaCredito;
            $notaCredito->setMedioPago(null);
        }
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this MedioPago is new, it will return
     * an empty collection; or if this MedioPago has previously
     * been saved, it will retrieve related NotaCreditos from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in MedioPago.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|NotaCredito[] List of NotaCredito objects
     */
    public function getNotaCreditosJoinCliente($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = NotaCreditoQuery::create(null, $criteria);
        $query->joinWith('Cliente', $join_behavior);

        return $this->getNotaCreditos($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this MedioPago is new, it will return
     * an empty collection; or if this MedioPago has previously
     * been saved, it will retrieve related NotaCreditos from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in MedioPago.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|NotaCredito[] List of NotaCredito objects
     */
    public function getNotaCreditosJoinProveedor($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = NotaCreditoQuery::create(null, $criteria);
        $query->joinWith('Proveedor', $join_behavior);

        return $this->getNotaCreditos($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this MedioPago is new, it will return
     * an empty collection; or if this MedioPago has previously
     * been saved, it will retrieve related NotaCreditos from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in MedioPago.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|NotaCredito[] List of NotaCredito objects
     */
    public function getNotaCreditosJoinEmpresa($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = NotaCreditoQuery::create(null, $criteria);
        $query->joinWith('Empresa', $join_behavior);

        return $this->getNotaCreditos($query, $con);
    }

    /**
     * Clears out the collVentaResumidas collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return void
     * @see        addVentaResumidas()
     */
    public function clearVentaResumidas()
    {
        $this->collVentaResumidas = null; // important to set this to null since that means it is uninitialized
        $this->collVentaResumidasPartial = null;
    }

    /**
     * reset is the collVentaResumidas collection loaded partially
     *
     * @return void
     */
    public function resetPartialVentaResumidas($v = true)
    {
        $this->collVentaResumidasPartial = $v;
    }

    /**
     * Initializes the collVentaResumidas collection.
     *
     * By default this just sets the collVentaResumidas collection to an empty array (like clearcollVentaResumidas());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initVentaResumidas($overrideExisting = true)
    {
        if (null !== $this->collVentaResumidas && !$overrideExisting) {
            return;
        }
        $this->collVentaResumidas = new PropelObjectCollection();
        $this->collVentaResumidas->setModel('VentaResumida');
    }

    /**
     * Gets an array of VentaResumida objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this MedioPago is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|VentaResumida[] List of VentaResumida objects
     * @throws PropelException
     */
    public function getVentaResumidas($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collVentaResumidasPartial && !$this->isNew();
        if (null === $this->collVentaResumidas || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collVentaResumidas) {
                // return empty collection
                $this->initVentaResumidas();
            } else {
                $collVentaResumidas = VentaResumidaQuery::create(null, $criteria)
                    ->filterByMedioPago($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collVentaResumidasPartial && count($collVentaResumidas)) {
                      $this->initVentaResumidas(false);

                      foreach($collVentaResumidas as $obj) {
                        if (false == $this->collVentaResumidas->contains($obj)) {
                          $this->collVentaResumidas->append($obj);
                        }
                      }

                      $this->collVentaResumidasPartial = true;
                    }

                    return $collVentaResumidas;
                }

                if($partial && $this->collVentaResumidas) {
                    foreach($this->collVentaResumidas as $obj) {
                        if($obj->isNew()) {
                            $collVentaResumidas[] = $obj;
                        }
                    }
                }

                $this->collVentaResumidas = $collVentaResumidas;
                $this->collVentaResumidasPartial = false;
            }
        }

        return $this->collVentaResumidas;
    }

    /**
     * Sets a collection of VentaResumida objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $ventaResumidas A Propel collection.
     * @param PropelPDO $con Optional connection object
     */
    public function setVentaResumidas(PropelCollection $ventaResumidas, PropelPDO $con = null)
    {
        $this->ventaResumidasScheduledForDeletion = $this->getVentaResumidas(new Criteria(), $con)->diff($ventaResumidas);

        foreach ($this->ventaResumidasScheduledForDeletion as $ventaResumidaRemoved) {
            $ventaResumidaRemoved->setMedioPago(null);
        }

        $this->collVentaResumidas = null;
        foreach ($ventaResumidas as $ventaResumida) {
            $this->addVentaResumida($ventaResumida);
        }

        $this->collVentaResumidas = $ventaResumidas;
        $this->collVentaResumidasPartial = false;
    }

    /**
     * Returns the number of related VentaResumida objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related VentaResumida objects.
     * @throws PropelException
     */
    public function countVentaResumidas(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collVentaResumidasPartial && !$this->isNew();
        if (null === $this->collVentaResumidas || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collVentaResumidas) {
                return 0;
            } else {
                if($partial && !$criteria) {
                    return count($this->getVentaResumidas());
                }
                $query = VentaResumidaQuery::create(null, $criteria);
                if ($distinct) {
                    $query->distinct();
                }

                return $query
                    ->filterByMedioPago($this)
                    ->count($con);
            }
        } else {
            return count($this->collVentaResumidas);
        }
    }

    /**
     * Method called to associate a VentaResumida object to this object
     * through the VentaResumida foreign key attribute.
     *
     * @param    VentaResumida $l VentaResumida
     * @return MedioPago The current object (for fluent API support)
     */
    public function addVentaResumida(VentaResumida $l)
    {
        if ($this->collVentaResumidas === null) {
            $this->initVentaResumidas();
            $this->collVentaResumidasPartial = true;
        }
        if (!$this->collVentaResumidas->contains($l)) { // only add it if the **same** object is not already associated
            $this->doAddVentaResumida($l);
        }

        return $this;
    }

    /**
     * @param	VentaResumida $ventaResumida The ventaResumida object to add.
     */
    protected function doAddVentaResumida($ventaResumida)
    {
        $this->collVentaResumidas[]= $ventaResumida;
        $ventaResumida->setMedioPago($this);
    }

    /**
     * @param	VentaResumida $ventaResumida The ventaResumida object to remove.
     */
    public function removeVentaResumida($ventaResumida)
    {
        if ($this->getVentaResumidas()->contains($ventaResumida)) {
            $this->collVentaResumidas->remove($this->collVentaResumidas->search($ventaResumida));
            if (null === $this->ventaResumidasScheduledForDeletion) {
                $this->ventaResumidasScheduledForDeletion = clone $this->collVentaResumidas;
                $this->ventaResumidasScheduledForDeletion->clear();
            }
            $this->ventaResumidasScheduledForDeletion[]= $ventaResumida;
            $ventaResumida->setMedioPago(null);
        }
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this MedioPago is new, it will return
     * an empty collection; or if this MedioPago has previously
     * been saved, it will retrieve related VentaResumidas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in MedioPago.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|VentaResumida[] List of VentaResumida objects
     */
    public function getVentaResumidasJoinTienda($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = VentaResumidaQuery::create(null, $criteria);
        $query->joinWith('Tienda', $join_behavior);

        return $this->getVentaResumidas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this MedioPago is new, it will return
     * an empty collection; or if this MedioPago has previously
     * been saved, it will retrieve related VentaResumidas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in MedioPago.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|VentaResumida[] List of VentaResumida objects
     */
    public function getVentaResumidasJoinEmpresa($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = VentaResumidaQuery::create(null, $criteria);
        $query->joinWith('Empresa', $join_behavior);

        return $this->getVentaResumidas($query, $con);
    }

    /**
     * Clears out the collVentaResumidaLineas collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return void
     * @see        addVentaResumidaLineas()
     */
    public function clearVentaResumidaLineas()
    {
        $this->collVentaResumidaLineas = null; // important to set this to null since that means it is uninitialized
        $this->collVentaResumidaLineasPartial = null;
    }

    /**
     * reset is the collVentaResumidaLineas collection loaded partially
     *
     * @return void
     */
    public function resetPartialVentaResumidaLineas($v = true)
    {
        $this->collVentaResumidaLineasPartial = $v;
    }

    /**
     * Initializes the collVentaResumidaLineas collection.
     *
     * By default this just sets the collVentaResumidaLineas collection to an empty array (like clearcollVentaResumidaLineas());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initVentaResumidaLineas($overrideExisting = true)
    {
        if (null !== $this->collVentaResumidaLineas && !$overrideExisting) {
            return;
        }
        $this->collVentaResumidaLineas = new PropelObjectCollection();
        $this->collVentaResumidaLineas->setModel('VentaResumidaLinea');
    }

    /**
     * Gets an array of VentaResumidaLinea objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this MedioPago is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|VentaResumidaLinea[] List of VentaResumidaLinea objects
     * @throws PropelException
     */
    public function getVentaResumidaLineas($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collVentaResumidaLineasPartial && !$this->isNew();
        if (null === $this->collVentaResumidaLineas || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collVentaResumidaLineas) {
                // return empty collection
                $this->initVentaResumidaLineas();
            } else {
                $collVentaResumidaLineas = VentaResumidaLineaQuery::create(null, $criteria)
                    ->filterByMedioPago($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collVentaResumidaLineasPartial && count($collVentaResumidaLineas)) {
                      $this->initVentaResumidaLineas(false);

                      foreach($collVentaResumidaLineas as $obj) {
                        if (false == $this->collVentaResumidaLineas->contains($obj)) {
                          $this->collVentaResumidaLineas->append($obj);
                        }
                      }

                      $this->collVentaResumidaLineasPartial = true;
                    }

                    return $collVentaResumidaLineas;
                }

                if($partial && $this->collVentaResumidaLineas) {
                    foreach($this->collVentaResumidaLineas as $obj) {
                        if($obj->isNew()) {
                            $collVentaResumidaLineas[] = $obj;
                        }
                    }
                }

                $this->collVentaResumidaLineas = $collVentaResumidaLineas;
                $this->collVentaResumidaLineasPartial = false;
            }
        }

        return $this->collVentaResumidaLineas;
    }

    /**
     * Sets a collection of VentaResumidaLinea objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $ventaResumidaLineas A Propel collection.
     * @param PropelPDO $con Optional connection object
     */
    public function setVentaResumidaLineas(PropelCollection $ventaResumidaLineas, PropelPDO $con = null)
    {
        $this->ventaResumidaLineasScheduledForDeletion = $this->getVentaResumidaLineas(new Criteria(), $con)->diff($ventaResumidaLineas);

        foreach ($this->ventaResumidaLineasScheduledForDeletion as $ventaResumidaLineaRemoved) {
            $ventaResumidaLineaRemoved->setMedioPago(null);
        }

        $this->collVentaResumidaLineas = null;
        foreach ($ventaResumidaLineas as $ventaResumidaLinea) {
            $this->addVentaResumidaLinea($ventaResumidaLinea);
        }

        $this->collVentaResumidaLineas = $ventaResumidaLineas;
        $this->collVentaResumidaLineasPartial = false;
    }

    /**
     * Returns the number of related VentaResumidaLinea objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related VentaResumidaLinea objects.
     * @throws PropelException
     */
    public function countVentaResumidaLineas(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collVentaResumidaLineasPartial && !$this->isNew();
        if (null === $this->collVentaResumidaLineas || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collVentaResumidaLineas) {
                return 0;
            } else {
                if($partial && !$criteria) {
                    return count($this->getVentaResumidaLineas());
                }
                $query = VentaResumidaLineaQuery::create(null, $criteria);
                if ($distinct) {
                    $query->distinct();
                }

                return $query
                    ->filterByMedioPago($this)
                    ->count($con);
            }
        } else {
            return count($this->collVentaResumidaLineas);
        }
    }

    /**
     * Method called to associate a VentaResumidaLinea object to this object
     * through the VentaResumidaLinea foreign key attribute.
     *
     * @param    VentaResumidaLinea $l VentaResumidaLinea
     * @return MedioPago The current object (for fluent API support)
     */
    public function addVentaResumidaLinea(VentaResumidaLinea $l)
    {
        if ($this->collVentaResumidaLineas === null) {
            $this->initVentaResumidaLineas();
            $this->collVentaResumidaLineasPartial = true;
        }
        if (!$this->collVentaResumidaLineas->contains($l)) { // only add it if the **same** object is not already associated
            $this->doAddVentaResumidaLinea($l);
        }

        return $this;
    }

    /**
     * @param	VentaResumidaLinea $ventaResumidaLinea The ventaResumidaLinea object to add.
     */
    protected function doAddVentaResumidaLinea($ventaResumidaLinea)
    {
        $this->collVentaResumidaLineas[]= $ventaResumidaLinea;
        $ventaResumidaLinea->setMedioPago($this);
    }

    /**
     * @param	VentaResumidaLinea $ventaResumidaLinea The ventaResumidaLinea object to remove.
     */
    public function removeVentaResumidaLinea($ventaResumidaLinea)
    {
        if ($this->getVentaResumidaLineas()->contains($ventaResumidaLinea)) {
            $this->collVentaResumidaLineas->remove($this->collVentaResumidaLineas->search($ventaResumidaLinea));
            if (null === $this->ventaResumidaLineasScheduledForDeletion) {
                $this->ventaResumidaLineasScheduledForDeletion = clone $this->collVentaResumidaLineas;
                $this->ventaResumidaLineasScheduledForDeletion->clear();
            }
            $this->ventaResumidaLineasScheduledForDeletion[]= $ventaResumidaLinea;
            $ventaResumidaLinea->setMedioPago(null);
        }
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this MedioPago is new, it will return
     * an empty collection; or if this MedioPago has previously
     * been saved, it will retrieve related VentaResumidaLineas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in MedioPago.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|VentaResumidaLinea[] List of VentaResumidaLinea objects
     */
    public function getVentaResumidaLineasJoinVentaResumida($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = VentaResumidaLineaQuery::create(null, $criteria);
        $query->joinWith('VentaResumida', $join_behavior);

        return $this->getVentaResumidaLineas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this MedioPago is new, it will return
     * an empty collection; or if this MedioPago has previously
     * been saved, it will retrieve related VentaResumidaLineas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in MedioPago.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|VentaResumidaLinea[] List of VentaResumidaLinea objects
     */
    public function getVentaResumidaLineasJoinEmpresa($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = VentaResumidaLineaQuery::create(null, $criteria);
        $query->joinWith('Empresa', $join_behavior);

        return $this->getVentaResumidaLineas($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this MedioPago is new, it will return
     * an empty collection; or if this MedioPago has previously
     * been saved, it will retrieve related VentaResumidaLineas from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in MedioPago.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|VentaResumidaLinea[] List of VentaResumidaLinea objects
     */
    public function getVentaResumidaLineasJoinBanco($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = VentaResumidaLineaQuery::create(null, $criteria);
        $query->joinWith('Banco', $join_behavior);

        return $this->getVentaResumidaLineas($query, $con);
    }

    /**
     * Clears out the collEgresosRecibidos collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return void
     * @see        addEgresosRecibidos()
     */
    public function clearEgresosRecibidos()
    {
        $this->collEgresosRecibidos = null; // important to set this to null since that means it is uninitialized
        $this->collEgresosRecibidosPartial = null;
    }

    /**
     * reset is the collEgresosRecibidos collection loaded partially
     *
     * @return void
     */
    public function resetPartialEgresosRecibidos($v = true)
    {
        $this->collEgresosRecibidosPartial = $v;
    }

    /**
     * Initializes the collEgresosRecibidos collection.
     *
     * By default this just sets the collEgresosRecibidos collection to an empty array (like clearcollEgresosRecibidos());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initEgresosRecibidos($overrideExisting = true)
    {
        if (null !== $this->collEgresosRecibidos && !$overrideExisting) {
            return;
        }
        $this->collEgresosRecibidos = new PropelObjectCollection();
        $this->collEgresosRecibidos->setModel('EgresosRecibido');
    }

    /**
     * Gets an array of EgresosRecibido objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this MedioPago is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|EgresosRecibido[] List of EgresosRecibido objects
     * @throws PropelException
     */
    public function getEgresosRecibidos($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collEgresosRecibidosPartial && !$this->isNew();
        if (null === $this->collEgresosRecibidos || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collEgresosRecibidos) {
                // return empty collection
                $this->initEgresosRecibidos();
            } else {
                $collEgresosRecibidos = EgresosRecibidoQuery::create(null, $criteria)
                    ->filterByMedioPago($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collEgresosRecibidosPartial && count($collEgresosRecibidos)) {
                      $this->initEgresosRecibidos(false);

                      foreach($collEgresosRecibidos as $obj) {
                        if (false == $this->collEgresosRecibidos->contains($obj)) {
                          $this->collEgresosRecibidos->append($obj);
                        }
                      }

                      $this->collEgresosRecibidosPartial = true;
                    }

                    return $collEgresosRecibidos;
                }

                if($partial && $this->collEgresosRecibidos) {
                    foreach($this->collEgresosRecibidos as $obj) {
                        if($obj->isNew()) {
                            $collEgresosRecibidos[] = $obj;
                        }
                    }
                }

                $this->collEgresosRecibidos = $collEgresosRecibidos;
                $this->collEgresosRecibidosPartial = false;
            }
        }

        return $this->collEgresosRecibidos;
    }

    /**
     * Sets a collection of EgresosRecibido objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $egresosRecibidos A Propel collection.
     * @param PropelPDO $con Optional connection object
     */
    public function setEgresosRecibidos(PropelCollection $egresosRecibidos, PropelPDO $con = null)
    {
        $this->egresosRecibidosScheduledForDeletion = $this->getEgresosRecibidos(new Criteria(), $con)->diff($egresosRecibidos);

        foreach ($this->egresosRecibidosScheduledForDeletion as $egresosRecibidoRemoved) {
            $egresosRecibidoRemoved->setMedioPago(null);
        }

        $this->collEgresosRecibidos = null;
        foreach ($egresosRecibidos as $egresosRecibido) {
            $this->addEgresosRecibido($egresosRecibido);
        }

        $this->collEgresosRecibidos = $egresosRecibidos;
        $this->collEgresosRecibidosPartial = false;
    }

    /**
     * Returns the number of related EgresosRecibido objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related EgresosRecibido objects.
     * @throws PropelException
     */
    public function countEgresosRecibidos(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collEgresosRecibidosPartial && !$this->isNew();
        if (null === $this->collEgresosRecibidos || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collEgresosRecibidos) {
                return 0;
            } else {
                if($partial && !$criteria) {
                    return count($this->getEgresosRecibidos());
                }
                $query = EgresosRecibidoQuery::create(null, $criteria);
                if ($distinct) {
                    $query->distinct();
                }

                return $query
                    ->filterByMedioPago($this)
                    ->count($con);
            }
        } else {
            return count($this->collEgresosRecibidos);
        }
    }

    /**
     * Method called to associate a EgresosRecibido object to this object
     * through the EgresosRecibido foreign key attribute.
     *
     * @param    EgresosRecibido $l EgresosRecibido
     * @return MedioPago The current object (for fluent API support)
     */
    public function addEgresosRecibido(EgresosRecibido $l)
    {
        if ($this->collEgresosRecibidos === null) {
            $this->initEgresosRecibidos();
            $this->collEgresosRecibidosPartial = true;
        }
        if (!$this->collEgresosRecibidos->contains($l)) { // only add it if the **same** object is not already associated
            $this->doAddEgresosRecibido($l);
        }

        return $this;
    }

    /**
     * @param	EgresosRecibido $egresosRecibido The egresosRecibido object to add.
     */
    protected function doAddEgresosRecibido($egresosRecibido)
    {
        $this->collEgresosRecibidos[]= $egresosRecibido;
        $egresosRecibido->setMedioPago($this);
    }

    /**
     * @param	EgresosRecibido $egresosRecibido The egresosRecibido object to remove.
     */
    public function removeEgresosRecibido($egresosRecibido)
    {
        if ($this->getEgresosRecibidos()->contains($egresosRecibido)) {
            $this->collEgresosRecibidos->remove($this->collEgresosRecibidos->search($egresosRecibido));
            if (null === $this->egresosRecibidosScheduledForDeletion) {
                $this->egresosRecibidosScheduledForDeletion = clone $this->collEgresosRecibidos;
                $this->egresosRecibidosScheduledForDeletion->clear();
            }
            $this->egresosRecibidosScheduledForDeletion[]= $egresosRecibido;
            $egresosRecibido->setMedioPago(null);
        }
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this MedioPago is new, it will return
     * an empty collection; or if this MedioPago has previously
     * been saved, it will retrieve related EgresosRecibidos from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in MedioPago.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|EgresosRecibido[] List of EgresosRecibido objects
     */
    public function getEgresosRecibidosJoinTienda($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = EgresosRecibidoQuery::create(null, $criteria);
        $query->joinWith('Tienda', $join_behavior);

        return $this->getEgresosRecibidos($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this MedioPago is new, it will return
     * an empty collection; or if this MedioPago has previously
     * been saved, it will retrieve related EgresosRecibidos from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in MedioPago.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|EgresosRecibido[] List of EgresosRecibido objects
     */
    public function getEgresosRecibidosJoinEmpresa($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = EgresosRecibidoQuery::create(null, $criteria);
        $query->joinWith('Empresa', $join_behavior);

        return $this->getEgresosRecibidos($query, $con);
    }

    /**
     * Clears the current object and sets all attributes to their default values
     */
    public function clear()
    {
        $this->id = null;
        $this->empresa_id = null;
        $this->codigo = null;
        $this->nombre = null;
        $this->activo = null;
        $this->cuenta_contable = null;
        $this->orden = null;
        $this->pos = null;
        $this->aplica_mov_banco = null;
        $this->created_by = null;
        $this->updated_by = null;
        $this->created_at = null;
        $this->updated_at = null;
        $this->aplica_cuotas = null;
        $this->numero_cuotas = null;
        $this->comision = null;
        $this->retiene_isr = null;
        $this->pide_banco = null;
        $this->banco_id = null;
        $this->pago_proveedor = null;
        $this->alreadyInSave = false;
        $this->alreadyInValidation = false;
        $this->clearAllReferences();
        $this->applyDefaultValues();
        $this->resetModified();
        $this->setNew(true);
        $this->setDeleted(false);
    }

    /**
     * Resets all references to other model objects or collections of model objects.
     *
     * This method is a user-space workaround for PHP's inability to garbage collect
     * objects with circular references (even in PHP 5.3). This is currently necessary
     * when using Propel in certain daemon or large-volumne/high-memory operations.
     *
     * @param boolean $deep Whether to also clear the references on all referrer objects.
     */
    public function clearAllReferences($deep = false)
    {
        if ($deep) {
            if ($this->collMovimientoBancos) {
                foreach ($this->collMovimientoBancos as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collPartidas) {
                foreach ($this->collPartidas as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collNotaCreditos) {
                foreach ($this->collNotaCreditos as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collVentaResumidas) {
                foreach ($this->collVentaResumidas as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collVentaResumidaLineas) {
                foreach ($this->collVentaResumidaLineas as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collEgresosRecibidos) {
                foreach ($this->collEgresosRecibidos as $o) {
                    $o->clearAllReferences($deep);
                }
            }
        } // if ($deep)

        if ($this->collMovimientoBancos instanceof PropelCollection) {
            $this->collMovimientoBancos->clearIterator();
        }
        $this->collMovimientoBancos = null;
        if ($this->collPartidas instanceof PropelCollection) {
            $this->collPartidas->clearIterator();
        }
        $this->collPartidas = null;
        if ($this->collNotaCreditos instanceof PropelCollection) {
            $this->collNotaCreditos->clearIterator();
        }
        $this->collNotaCreditos = null;
        if ($this->collVentaResumidas instanceof PropelCollection) {
            $this->collVentaResumidas->clearIterator();
        }
        $this->collVentaResumidas = null;
        if ($this->collVentaResumidaLineas instanceof PropelCollection) {
            $this->collVentaResumidaLineas->clearIterator();
        }
        $this->collVentaResumidaLineas = null;
        if ($this->collEgresosRecibidos instanceof PropelCollection) {
            $this->collEgresosRecibidos->clearIterator();
        }
        $this->collEgresosRecibidos = null;
        $this->aEmpresa = null;
        $this->aBanco = null;
    }

    /**
     * return the string representation of this object
     *
     * @return string The value of the 'nombre' column
     */
    public function __toString()
    {
        return (string) $this->getNombre();
    }

    /**
     * return true is the object is in saving state
     *
     * @return boolean
     */
    public function isAlreadyInSave()
    {
        return $this->alreadyInSave;
    }

    /**
     * Catches calls to virtual methods
     */
    public function __call($name, $params)
    {

        // symfony_behaviors behavior
        if ($callable = sfMixer::getCallable('BaseMedioPago:' . $name))
        {
          array_unshift($params, $this);
          return call_user_func_array($callable, $params);
        }


        return parent::__call($name, $params);
    }

}
