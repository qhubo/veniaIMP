<?php


/**
 * Base class that represents a row from the 'solicitud_cheque' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.6.7 on:
 *
 * 12/26/25 01:36:34
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseSolicitudCheque extends BaseObject implements Persistent
{
    /**
     * Peer class name
     */
    const PEER = 'SolicitudChequePeer';

    /**
     * The Peer class.
     * Instance provides a convenient way of calling static methods on a class
     * that calling code may not be able to identify.
     * @var        SolicitudChequePeer
     */
    protected static $peer;

    /**
     * The flag var to prevent infinit loop in deep copy
     * @var       boolean
     */
    protected $startCopy = false;

    /**
     * The value for the id field.
     * @var        int
     */
    protected $id;

    /**
     * The value for the codigo field.
     * @var        string
     */
    protected $codigo;

    /**
     * The value for the empresa_id field.
     * @var        int
     */
    protected $empresa_id;

    /**
     * The value for the tipo field.
     * @var        string
     */
    protected $tipo;

    /**
     * The value for the nombre field.
     * @var        string
     */
    protected $nombre;

    /**
     * The value for the referencia field.
     * @var        string
     */
    protected $referencia;

    /**
     * The value for the motivo field.
     * @var        string
     */
    protected $motivo;

    /**
     * The value for the valor field.
     * @var        double
     */
    protected $valor;

    /**
     * The value for the partida_no field.
     * @var        int
     */
    protected $partida_no;

    /**
     * The value for the estatus field.
     * @var        string
     */
    protected $estatus;

    /**
     * The value for the fecha field.
     * @var        string
     */
    protected $fecha;

    /**
     * The value for the usuario field.
     * @var        string
     */
    protected $usuario;

    /**
     * The value for the usuario_confirmo field.
     * @var        string
     */
    protected $usuario_confirmo;

    /**
     * The value for the fecha_confirmo field.
     * @var        string
     */
    protected $fecha_confirmo;

    /**
     * The value for the cheque_no field.
     * Note: this column has a database default value of: 0
     * @var        int
     */
    protected $cheque_no;

    /**
     * @var        Empresa
     */
    protected $aEmpresa;

    /**
     * @var        PropelObjectCollection|Cheque[] Collection to store aggregation of Cheque objects.
     */
    protected $collCheques;
    protected $collChequesPartial;

    /**
     * Flag to prevent endless save loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInSave = false;

    /**
     * Flag to prevent endless validation loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInValidation = false;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $chequesScheduledForDeletion = null;

    /**
     * Applies default values to this object.
     * This method should be called from the object's constructor (or
     * equivalent initialization method).
     * @see        __construct()
     */
    public function applyDefaultValues()
    {
        $this->cheque_no = 0;
    }

    /**
     * Initializes internal state of BaseSolicitudCheque object.
     * @see        applyDefaults()
     */
    public function __construct()
    {
        parent::__construct();
        $this->applyDefaultValues();
    }

    /**
     * Get the [id] column value.
     *
     * @return int
     */
    public function getId()
    {
        return $this->id;
    }

    /**
     * Get the [codigo] column value.
     *
     * @return string
     */
    public function getCodigo()
    {
        return $this->codigo;
    }

    /**
     * Get the [empresa_id] column value.
     *
     * @return int
     */
    public function getEmpresaId()
    {
        return $this->empresa_id;
    }

    /**
     * Get the [tipo] column value.
     *
     * @return string
     */
    public function getTipo()
    {
        return $this->tipo;
    }

    /**
     * Get the [nombre] column value.
     *
     * @return string
     */
    public function getNombre()
    {
        return $this->nombre;
    }

    /**
     * Get the [referencia] column value.
     *
     * @return string
     */
    public function getReferencia()
    {
        return $this->referencia;
    }

    /**
     * Get the [motivo] column value.
     *
     * @return string
     */
    public function getMotivo()
    {
        return $this->motivo;
    }

    /**
     * Get the [valor] column value.
     *
     * @return double
     */
    public function getValor()
    {
        return $this->valor;
    }

    /**
     * Get the [partida_no] column value.
     *
     * @return int
     */
    public function getPartidaNo()
    {
        return $this->partida_no;
    }

    /**
     * Get the [estatus] column value.
     *
     * @return string
     */
    public function getEstatus()
    {
        return $this->estatus;
    }

    /**
     * Get the [optionally formatted] temporal [fecha] column value.
     *
     *
     * @param string $format The date/time format string (either date()-style or strftime()-style).
     *				 If format is null, then the raw DateTime object will be returned.
     * @return mixed Formatted date/time value as string or DateTime object (if format is null), null if column is null, and 0 if column value is 0000-00-00
     * @throws PropelException - if unable to parse/validate the date/time value.
     */
    public function getFecha($format = 'Y-m-d')
    {
        if ($this->fecha === null) {
            return null;
        }

        if ($this->fecha === '0000-00-00') {
            // while technically this is not a default value of null,
            // this seems to be closest in meaning.
            return null;
        } else {
            try {
                $dt = new DateTime($this->fecha);
            } catch (Exception $x) {
                throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->fecha, true), $x);
            }
        }

        if ($format === null) {
            // Because propel.useDateTimeClass is true, we return a DateTime object.
            return $dt;
        } elseif (strpos($format, '%') !== false) {
            return strftime($format, $dt->format('U'));
        } else {
            return $dt->format($format);
        }
    }

    /**
     * Get the [usuario] column value.
     *
     * @return string
     */
    public function getUsuario()
    {
        return $this->usuario;
    }

    /**
     * Get the [usuario_confirmo] column value.
     *
     * @return string
     */
    public function getUsuarioConfirmo()
    {
        return $this->usuario_confirmo;
    }

    /**
     * Get the [optionally formatted] temporal [fecha_confirmo] column value.
     *
     *
     * @param string $format The date/time format string (either date()-style or strftime()-style).
     *				 If format is null, then the raw DateTime object will be returned.
     * @return mixed Formatted date/time value as string or DateTime object (if format is null), null if column is null, and 0 if column value is 0000-00-00 00:00:00
     * @throws PropelException - if unable to parse/validate the date/time value.
     */
    public function getFechaConfirmo($format = 'Y-m-d H:i:s')
    {
        if ($this->fecha_confirmo === null) {
            return null;
        }

        if ($this->fecha_confirmo === '0000-00-00 00:00:00') {
            // while technically this is not a default value of null,
            // this seems to be closest in meaning.
            return null;
        } else {
            try {
                $dt = new DateTime($this->fecha_confirmo);
            } catch (Exception $x) {
                throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->fecha_confirmo, true), $x);
            }
        }

        if ($format === null) {
            // Because propel.useDateTimeClass is true, we return a DateTime object.
            return $dt;
        } elseif (strpos($format, '%') !== false) {
            return strftime($format, $dt->format('U'));
        } else {
            return $dt->format($format);
        }
    }

    /**
     * Get the [cheque_no] column value.
     *
     * @return int
     */
    public function getChequeNo()
    {
        return $this->cheque_no;
    }

    /**
     * Set the value of [id] column.
     *
     * @param int $v new value
     * @return SolicitudCheque The current object (for fluent API support)
     */
    public function setId($v)
    {
        if ($v !== null) {
            $v = (int) $v;
        }

        if ($this->id !== $v) {
            $this->id = $v;
            $this->modifiedColumns[] = SolicitudChequePeer::ID;
        }


        return $this;
    } // setId()

    /**
     * Set the value of [codigo] column.
     *
     * @param string $v new value
     * @return SolicitudCheque The current object (for fluent API support)
     */
    public function setCodigo($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->codigo !== $v) {
            $this->codigo = $v;
            $this->modifiedColumns[] = SolicitudChequePeer::CODIGO;
        }


        return $this;
    } // setCodigo()

    /**
     * Set the value of [empresa_id] column.
     *
     * @param int $v new value
     * @return SolicitudCheque The current object (for fluent API support)
     */
    public function setEmpresaId($v)
    {
        if ($v !== null) {
            $v = (int) $v;
        }

        if ($this->empresa_id !== $v) {
            $this->empresa_id = $v;
            $this->modifiedColumns[] = SolicitudChequePeer::EMPRESA_ID;
        }

        if ($this->aEmpresa !== null && $this->aEmpresa->getId() !== $v) {
            $this->aEmpresa = null;
        }


        return $this;
    } // setEmpresaId()

    /**
     * Set the value of [tipo] column.
     *
     * @param string $v new value
     * @return SolicitudCheque The current object (for fluent API support)
     */
    public function setTipo($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->tipo !== $v) {
            $this->tipo = $v;
            $this->modifiedColumns[] = SolicitudChequePeer::TIPO;
        }


        return $this;
    } // setTipo()

    /**
     * Set the value of [nombre] column.
     *
     * @param string $v new value
     * @return SolicitudCheque The current object (for fluent API support)
     */
    public function setNombre($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->nombre !== $v) {
            $this->nombre = $v;
            $this->modifiedColumns[] = SolicitudChequePeer::NOMBRE;
        }


        return $this;
    } // setNombre()

    /**
     * Set the value of [referencia] column.
     *
     * @param string $v new value
     * @return SolicitudCheque The current object (for fluent API support)
     */
    public function setReferencia($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->referencia !== $v) {
            $this->referencia = $v;
            $this->modifiedColumns[] = SolicitudChequePeer::REFERENCIA;
        }


        return $this;
    } // setReferencia()

    /**
     * Set the value of [motivo] column.
     *
     * @param string $v new value
     * @return SolicitudCheque The current object (for fluent API support)
     */
    public function setMotivo($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->motivo !== $v) {
            $this->motivo = $v;
            $this->modifiedColumns[] = SolicitudChequePeer::MOTIVO;
        }


        return $this;
    } // setMotivo()

    /**
     * Set the value of [valor] column.
     *
     * @param double $v new value
     * @return SolicitudCheque The current object (for fluent API support)
     */
    public function setValor($v)
    {
        if ($v !== null) {
            $v = (double) $v;
        }

        if ($this->valor !== $v) {
            $this->valor = $v;
            $this->modifiedColumns[] = SolicitudChequePeer::VALOR;
        }


        return $this;
    } // setValor()

    /**
     * Set the value of [partida_no] column.
     *
     * @param int $v new value
     * @return SolicitudCheque The current object (for fluent API support)
     */
    public function setPartidaNo($v)
    {
        if ($v !== null) {
            $v = (int) $v;
        }

        if ($this->partida_no !== $v) {
            $this->partida_no = $v;
            $this->modifiedColumns[] = SolicitudChequePeer::PARTIDA_NO;
        }


        return $this;
    } // setPartidaNo()

    /**
     * Set the value of [estatus] column.
     *
     * @param string $v new value
     * @return SolicitudCheque The current object (for fluent API support)
     */
    public function setEstatus($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->estatus !== $v) {
            $this->estatus = $v;
            $this->modifiedColumns[] = SolicitudChequePeer::ESTATUS;
        }


        return $this;
    } // setEstatus()

    /**
     * Sets the value of [fecha] column to a normalized version of the date/time value specified.
     *
     * @param mixed $v string, integer (timestamp), or DateTime value.
     *               Empty strings are treated as null.
     * @return SolicitudCheque The current object (for fluent API support)
     */
    public function setFecha($v)
    {
        $dt = PropelDateTime::newInstance($v, null, 'DateTime');
        if ($this->fecha !== null || $dt !== null) {
            $currentDateAsString = ($this->fecha !== null && $tmpDt = new DateTime($this->fecha)) ? $tmpDt->format('Y-m-d') : null;
            $newDateAsString = $dt ? $dt->format('Y-m-d') : null;
            if ($currentDateAsString !== $newDateAsString) {
                $this->fecha = $newDateAsString;
                $this->modifiedColumns[] = SolicitudChequePeer::FECHA;
            }
        } // if either are not null


        return $this;
    } // setFecha()

    /**
     * Set the value of [usuario] column.
     *
     * @param string $v new value
     * @return SolicitudCheque The current object (for fluent API support)
     */
    public function setUsuario($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->usuario !== $v) {
            $this->usuario = $v;
            $this->modifiedColumns[] = SolicitudChequePeer::USUARIO;
        }


        return $this;
    } // setUsuario()

    /**
     * Set the value of [usuario_confirmo] column.
     *
     * @param string $v new value
     * @return SolicitudCheque The current object (for fluent API support)
     */
    public function setUsuarioConfirmo($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->usuario_confirmo !== $v) {
            $this->usuario_confirmo = $v;
            $this->modifiedColumns[] = SolicitudChequePeer::USUARIO_CONFIRMO;
        }


        return $this;
    } // setUsuarioConfirmo()

    /**
     * Sets the value of [fecha_confirmo] column to a normalized version of the date/time value specified.
     *
     * @param mixed $v string, integer (timestamp), or DateTime value.
     *               Empty strings are treated as null.
     * @return SolicitudCheque The current object (for fluent API support)
     */
    public function setFechaConfirmo($v)
    {
        $dt = PropelDateTime::newInstance($v, null, 'DateTime');
        if ($this->fecha_confirmo !== null || $dt !== null) {
            $currentDateAsString = ($this->fecha_confirmo !== null && $tmpDt = new DateTime($this->fecha_confirmo)) ? $tmpDt->format('Y-m-d H:i:s') : null;
            $newDateAsString = $dt ? $dt->format('Y-m-d H:i:s') : null;
            if ($currentDateAsString !== $newDateAsString) {
                $this->fecha_confirmo = $newDateAsString;
                $this->modifiedColumns[] = SolicitudChequePeer::FECHA_CONFIRMO;
            }
        } // if either are not null


        return $this;
    } // setFechaConfirmo()

    /**
     * Set the value of [cheque_no] column.
     *
     * @param int $v new value
     * @return SolicitudCheque The current object (for fluent API support)
     */
    public function setChequeNo($v)
    {
        if ($v !== null) {
            $v = (int) $v;
        }

        if ($this->cheque_no !== $v) {
            $this->cheque_no = $v;
            $this->modifiedColumns[] = SolicitudChequePeer::CHEQUE_NO;
        }


        return $this;
    } // setChequeNo()

    /**
     * Indicates whether the columns in this object are only set to default values.
     *
     * This method can be used in conjunction with isModified() to indicate whether an object is both
     * modified _and_ has some values set which are non-default.
     *
     * @return boolean Whether the columns in this object are only been set with default values.
     */
    public function hasOnlyDefaultValues()
    {
            if ($this->cheque_no !== 0) {
                return false;
            }

        // otherwise, everything was equal, so return true
        return true;
    } // hasOnlyDefaultValues()

    /**
     * Hydrates (populates) the object variables with values from the database resultset.
     *
     * An offset (0-based "start column") is specified so that objects can be hydrated
     * with a subset of the columns in the resultset rows.  This is needed, for example,
     * for results of JOIN queries where the resultset row includes columns from two or
     * more tables.
     *
     * @param array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
     * @param int $startcol 0-based offset column which indicates which restultset column to start with.
     * @param boolean $rehydrate Whether this object is being re-hydrated from the database.
     * @return int             next starting column
     * @throws PropelException - Any caught Exception will be rewrapped as a PropelException.
     */
    public function hydrate($row, $startcol = 0, $rehydrate = false)
    {
        try {

            $this->id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
            $this->codigo = ($row[$startcol + 1] !== null) ? (string) $row[$startcol + 1] : null;
            $this->empresa_id = ($row[$startcol + 2] !== null) ? (int) $row[$startcol + 2] : null;
            $this->tipo = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
            $this->nombre = ($row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
            $this->referencia = ($row[$startcol + 5] !== null) ? (string) $row[$startcol + 5] : null;
            $this->motivo = ($row[$startcol + 6] !== null) ? (string) $row[$startcol + 6] : null;
            $this->valor = ($row[$startcol + 7] !== null) ? (double) $row[$startcol + 7] : null;
            $this->partida_no = ($row[$startcol + 8] !== null) ? (int) $row[$startcol + 8] : null;
            $this->estatus = ($row[$startcol + 9] !== null) ? (string) $row[$startcol + 9] : null;
            $this->fecha = ($row[$startcol + 10] !== null) ? (string) $row[$startcol + 10] : null;
            $this->usuario = ($row[$startcol + 11] !== null) ? (string) $row[$startcol + 11] : null;
            $this->usuario_confirmo = ($row[$startcol + 12] !== null) ? (string) $row[$startcol + 12] : null;
            $this->fecha_confirmo = ($row[$startcol + 13] !== null) ? (string) $row[$startcol + 13] : null;
            $this->cheque_no = ($row[$startcol + 14] !== null) ? (int) $row[$startcol + 14] : null;
            $this->resetModified();

            $this->setNew(false);

            if ($rehydrate) {
                $this->ensureConsistency();
            }

            return $startcol + 15; // 15 = SolicitudChequePeer::NUM_HYDRATE_COLUMNS.

        } catch (Exception $e) {
            throw new PropelException("Error populating SolicitudCheque object", $e);
        }
    }

    /**
     * Checks and repairs the internal consistency of the object.
     *
     * This method is executed after an already-instantiated object is re-hydrated
     * from the database.  It exists to check any foreign keys to make sure that
     * the objects related to the current object are correct based on foreign key.
     *
     * You can override this method in the stub class, but you should always invoke
     * the base method from the overridden method (i.e. parent::ensureConsistency()),
     * in case your model changes.
     *
     * @throws PropelException
     */
    public function ensureConsistency()
    {

        if ($this->aEmpresa !== null && $this->empresa_id !== $this->aEmpresa->getId()) {
            $this->aEmpresa = null;
        }
    } // ensureConsistency

    /**
     * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
     *
     * This will only work if the object has been saved and has a valid primary key set.
     *
     * @param boolean $deep (optional) Whether to also de-associated any related objects.
     * @param PropelPDO $con (optional) The PropelPDO connection to use.
     * @return void
     * @throws PropelException - if this object is deleted, unsaved or doesn't have pk match in db
     */
    public function reload($deep = false, PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("Cannot reload a deleted object.");
        }

        if ($this->isNew()) {
            throw new PropelException("Cannot reload an unsaved object.");
        }

        if ($con === null) {
            $con = Propel::getConnection(SolicitudChequePeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }

        // We don't need to alter the object instance pool; we're just modifying this instance
        // already in the pool.

        $stmt = SolicitudChequePeer::doSelectStmt($this->buildPkeyCriteria(), $con);
        $row = $stmt->fetch(PDO::FETCH_NUM);
        $stmt->closeCursor();
        if (!$row) {
            throw new PropelException('Cannot find matching row in the database to reload object values.');
        }
        $this->hydrate($row, 0, true); // rehydrate

        if ($deep) {  // also de-associate any related objects?

            $this->aEmpresa = null;
            $this->collCheques = null;

        } // if (deep)
    }

    /**
     * Removes this object from datastore and sets delete attribute.
     *
     * @param PropelPDO $con
     * @return void
     * @throws PropelException
     * @throws Exception
     * @see        BaseObject::setDeleted()
     * @see        BaseObject::isDeleted()
     */
    public function delete(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("This object has already been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(SolicitudChequePeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        try {
            $deleteQuery = SolicitudChequeQuery::create()
                ->filterByPrimaryKey($this->getPrimaryKey());
            $ret = $this->preDelete($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BaseSolicitudCheque:delete:pre') as $callable)
            {
              if (call_user_func($callable, $this, $con))
              {
                $con->commit();
                return;
              }
            }

            if ($ret) {
                $deleteQuery->delete($con);
                $this->postDelete($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BaseSolicitudCheque:delete:post') as $callable)
                {
                  call_user_func($callable, $this, $con);
                }

                $con->commit();
                $this->setDeleted(true);
            } else {
                $con->commit();
            }
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Persists this object to the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All modified related objects will also be persisted in the doSave()
     * method.  This method wraps all precipitate database operations in a
     * single transaction.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @throws Exception
     * @see        doSave()
     */
    public function save(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("You cannot save an object that has been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(SolicitudChequePeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        $isInsert = $this->isNew();
        try {
            $ret = $this->preSave($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BaseSolicitudCheque:save:pre') as $callable)
            {
              if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
              {
                  $con->commit();
                return $affectedRows;
              }
            }

            if ($isInsert) {
                $ret = $ret && $this->preInsert($con);
            } else {
                $ret = $ret && $this->preUpdate($con);
            }
            if ($ret) {
                $affectedRows = $this->doSave($con);
                if ($isInsert) {
                    $this->postInsert($con);
                } else {
                    $this->postUpdate($con);
                }
                $this->postSave($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BaseSolicitudCheque:save:post') as $callable)
                {
                  call_user_func($callable, $this, $con, $affectedRows);
                }

                SolicitudChequePeer::addInstanceToPool($this);
            } else {
                $affectedRows = 0;
            }
            $con->commit();

            return $affectedRows;
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Performs the work of inserting or updating the row in the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All related objects are also updated in this method.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @see        save()
     */
    protected function doSave(PropelPDO $con)
    {
        $affectedRows = 0; // initialize var to track total num of affected rows
        if (!$this->alreadyInSave) {
            $this->alreadyInSave = true;

            // We call the save method on the following object(s) if they
            // were passed to this object by their coresponding set
            // method.  This object relates to these object(s) by a
            // foreign key reference.

            if ($this->aEmpresa !== null) {
                if ($this->aEmpresa->isModified() || $this->aEmpresa->isNew()) {
                    $affectedRows += $this->aEmpresa->save($con);
                }
                $this->setEmpresa($this->aEmpresa);
            }

            if ($this->isNew() || $this->isModified()) {
                // persist changes
                if ($this->isNew()) {
                    $this->doInsert($con);
                } else {
                    $this->doUpdate($con);
                }
                $affectedRows += 1;
                $this->resetModified();
            }

            if ($this->chequesScheduledForDeletion !== null) {
                if (!$this->chequesScheduledForDeletion->isEmpty()) {
                    foreach ($this->chequesScheduledForDeletion as $cheque) {
                        // need to save related object because we set the relation to null
                        $cheque->save($con);
                    }
                    $this->chequesScheduledForDeletion = null;
                }
            }

            if ($this->collCheques !== null) {
                foreach ($this->collCheques as $referrerFK) {
                    if (!$referrerFK->isDeleted()) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            $this->alreadyInSave = false;

        }

        return $affectedRows;
    } // doSave()

    /**
     * Insert the row in the database.
     *
     * @param PropelPDO $con
     *
     * @throws PropelException
     * @see        doSave()
     */
    protected function doInsert(PropelPDO $con)
    {
        $modifiedColumns = array();
        $index = 0;

        $this->modifiedColumns[] = SolicitudChequePeer::ID;
        if (null !== $this->id) {
            throw new PropelException('Cannot insert a value for auto-increment primary key (' . SolicitudChequePeer::ID . ')');
        }

         // check the columns in natural order for more readable SQL queries
        if ($this->isColumnModified(SolicitudChequePeer::ID)) {
            $modifiedColumns[':p' . $index++]  = '`ID`';
        }
        if ($this->isColumnModified(SolicitudChequePeer::CODIGO)) {
            $modifiedColumns[':p' . $index++]  = '`CODIGO`';
        }
        if ($this->isColumnModified(SolicitudChequePeer::EMPRESA_ID)) {
            $modifiedColumns[':p' . $index++]  = '`EMPRESA_ID`';
        }
        if ($this->isColumnModified(SolicitudChequePeer::TIPO)) {
            $modifiedColumns[':p' . $index++]  = '`TIPO`';
        }
        if ($this->isColumnModified(SolicitudChequePeer::NOMBRE)) {
            $modifiedColumns[':p' . $index++]  = '`NOMBRE`';
        }
        if ($this->isColumnModified(SolicitudChequePeer::REFERENCIA)) {
            $modifiedColumns[':p' . $index++]  = '`REFERENCIA`';
        }
        if ($this->isColumnModified(SolicitudChequePeer::MOTIVO)) {
            $modifiedColumns[':p' . $index++]  = '`MOTIVO`';
        }
        if ($this->isColumnModified(SolicitudChequePeer::VALOR)) {
            $modifiedColumns[':p' . $index++]  = '`VALOR`';
        }
        if ($this->isColumnModified(SolicitudChequePeer::PARTIDA_NO)) {
            $modifiedColumns[':p' . $index++]  = '`PARTIDA_NO`';
        }
        if ($this->isColumnModified(SolicitudChequePeer::ESTATUS)) {
            $modifiedColumns[':p' . $index++]  = '`ESTATUS`';
        }
        if ($this->isColumnModified(SolicitudChequePeer::FECHA)) {
            $modifiedColumns[':p' . $index++]  = '`FECHA`';
        }
        if ($this->isColumnModified(SolicitudChequePeer::USUARIO)) {
            $modifiedColumns[':p' . $index++]  = '`USUARIO`';
        }
        if ($this->isColumnModified(SolicitudChequePeer::USUARIO_CONFIRMO)) {
            $modifiedColumns[':p' . $index++]  = '`USUARIO_CONFIRMO`';
        }
        if ($this->isColumnModified(SolicitudChequePeer::FECHA_CONFIRMO)) {
            $modifiedColumns[':p' . $index++]  = '`FECHA_CONFIRMO`';
        }
        if ($this->isColumnModified(SolicitudChequePeer::CHEQUE_NO)) {
            $modifiedColumns[':p' . $index++]  = '`CHEQUE_NO`';
        }

        $sql = sprintf(
            'INSERT INTO `solicitud_cheque` (%s) VALUES (%s)',
            implode(', ', $modifiedColumns),
            implode(', ', array_keys($modifiedColumns))
        );

        try {
            $stmt = $con->prepare($sql);
            foreach ($modifiedColumns as $identifier => $columnName) {
                switch ($columnName) {
                    case '`ID`':
                        $stmt->bindValue($identifier, $this->id, PDO::PARAM_INT);
                        break;
                    case '`CODIGO`':
                        $stmt->bindValue($identifier, $this->codigo, PDO::PARAM_STR);
                        break;
                    case '`EMPRESA_ID`':
                        $stmt->bindValue($identifier, $this->empresa_id, PDO::PARAM_INT);
                        break;
                    case '`TIPO`':
                        $stmt->bindValue($identifier, $this->tipo, PDO::PARAM_STR);
                        break;
                    case '`NOMBRE`':
                        $stmt->bindValue($identifier, $this->nombre, PDO::PARAM_STR);
                        break;
                    case '`REFERENCIA`':
                        $stmt->bindValue($identifier, $this->referencia, PDO::PARAM_STR);
                        break;
                    case '`MOTIVO`':
                        $stmt->bindValue($identifier, $this->motivo, PDO::PARAM_STR);
                        break;
                    case '`VALOR`':
                        $stmt->bindValue($identifier, $this->valor, PDO::PARAM_STR);
                        break;
                    case '`PARTIDA_NO`':
                        $stmt->bindValue($identifier, $this->partida_no, PDO::PARAM_INT);
                        break;
                    case '`ESTATUS`':
                        $stmt->bindValue($identifier, $this->estatus, PDO::PARAM_STR);
                        break;
                    case '`FECHA`':
                        $stmt->bindValue($identifier, $this->fecha, PDO::PARAM_STR);
                        break;
                    case '`USUARIO`':
                        $stmt->bindValue($identifier, $this->usuario, PDO::PARAM_STR);
                        break;
                    case '`USUARIO_CONFIRMO`':
                        $stmt->bindValue($identifier, $this->usuario_confirmo, PDO::PARAM_STR);
                        break;
                    case '`FECHA_CONFIRMO`':
                        $stmt->bindValue($identifier, $this->fecha_confirmo, PDO::PARAM_STR);
                        break;
                    case '`CHEQUE_NO`':
                        $stmt->bindValue($identifier, $this->cheque_no, PDO::PARAM_INT);
                        break;
                }
            }
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute INSERT statement [%s]', $sql), $e);
        }

        try {
            $pk = $con->lastInsertId();
        } catch (Exception $e) {
            throw new PropelException('Unable to get autoincrement id.', $e);
        }
        $this->setId($pk);

        $this->setNew(false);
    }

    /**
     * Update the row in the database.
     *
     * @param PropelPDO $con
     *
     * @see        doSave()
     */
    protected function doUpdate(PropelPDO $con)
    {
        $selectCriteria = $this->buildPkeyCriteria();
        $valuesCriteria = $this->buildCriteria();
        BasePeer::doUpdate($selectCriteria, $valuesCriteria, $con);
    }

    /**
     * Array of ValidationFailed objects.
     * @var        array ValidationFailed[]
     */
    protected $validationFailures = array();

    /**
     * Gets any ValidationFailed objects that resulted from last call to validate().
     *
     *
     * @return array ValidationFailed[]
     * @see        validate()
     */
    public function getValidationFailures()
    {
        return $this->validationFailures;
    }

    /**
     * Validates the objects modified field values and all objects related to this table.
     *
     * If $columns is either a column name or an array of column names
     * only those columns are validated.
     *
     * @param mixed $columns Column name or an array of column names.
     * @return boolean Whether all columns pass validation.
     * @see        doValidate()
     * @see        getValidationFailures()
     */
    public function validate($columns = null)
    {
        $res = $this->doValidate($columns);
        if ($res === true) {
            $this->validationFailures = array();

            return true;
        } else {
            $this->validationFailures = $res;

            return false;
        }
    }

    /**
     * This function performs the validation work for complex object models.
     *
     * In addition to checking the current object, all related objects will
     * also be validated.  If all pass then <code>true</code> is returned; otherwise
     * an aggreagated array of ValidationFailed objects will be returned.
     *
     * @param array $columns Array of column names to validate.
     * @return mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
     */
    protected function doValidate($columns = null)
    {
        if (!$this->alreadyInValidation) {
            $this->alreadyInValidation = true;
            $retval = null;

            $failureMap = array();


            // We call the validate method on the following object(s) if they
            // were passed to this object by their coresponding set
            // method.  This object relates to these object(s) by a
            // foreign key reference.

            if ($this->aEmpresa !== null) {
                if (!$this->aEmpresa->validate($columns)) {
                    $failureMap = array_merge($failureMap, $this->aEmpresa->getValidationFailures());
                }
            }


            if (($retval = SolicitudChequePeer::doValidate($this, $columns)) !== true) {
                $failureMap = array_merge($failureMap, $retval);
            }


                if ($this->collCheques !== null) {
                    foreach ($this->collCheques as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }


            $this->alreadyInValidation = false;
        }

        return (!empty($failureMap) ? $failureMap : true);
    }

    /**
     * Retrieves a field from the object by name passed in as a string.
     *
     * @param string $name name
     * @param string $type The type of fieldname the $name is of:
     *               one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *               BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *               Defaults to BasePeer::TYPE_PHPNAME
     * @return mixed Value of field.
     */
    public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = SolicitudChequePeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
        $field = $this->getByPosition($pos);

        return $field;
    }

    /**
     * Retrieves a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @return mixed Value of field at $pos
     */
    public function getByPosition($pos)
    {
        switch ($pos) {
            case 0:
                return $this->getId();
                break;
            case 1:
                return $this->getCodigo();
                break;
            case 2:
                return $this->getEmpresaId();
                break;
            case 3:
                return $this->getTipo();
                break;
            case 4:
                return $this->getNombre();
                break;
            case 5:
                return $this->getReferencia();
                break;
            case 6:
                return $this->getMotivo();
                break;
            case 7:
                return $this->getValor();
                break;
            case 8:
                return $this->getPartidaNo();
                break;
            case 9:
                return $this->getEstatus();
                break;
            case 10:
                return $this->getFecha();
                break;
            case 11:
                return $this->getUsuario();
                break;
            case 12:
                return $this->getUsuarioConfirmo();
                break;
            case 13:
                return $this->getFechaConfirmo();
                break;
            case 14:
                return $this->getChequeNo();
                break;
            default:
                return null;
                break;
        } // switch()
    }

    /**
     * Exports the object as an array.
     *
     * You can specify the key type of the array by passing one of the class
     * type constants.
     *
     * @param     string  $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     *                    BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                    Defaults to BasePeer::TYPE_PHPNAME.
     * @param     boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns. Defaults to true.
     * @param     array $alreadyDumpedObjects List of objects to skip to avoid recursion
     * @param     boolean $includeForeignObjects (optional) Whether to include hydrated related objects. Default to FALSE.
     *
     * @return array an associative array containing the field names (as keys) and field values
     */
    public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true, $alreadyDumpedObjects = array(), $includeForeignObjects = false)
    {
        if (isset($alreadyDumpedObjects['SolicitudCheque'][$this->getPrimaryKey()])) {
            return '*RECURSION*';
        }
        $alreadyDumpedObjects['SolicitudCheque'][$this->getPrimaryKey()] = true;
        $keys = SolicitudChequePeer::getFieldNames($keyType);
        $result = array(
            $keys[0] => $this->getId(),
            $keys[1] => $this->getCodigo(),
            $keys[2] => $this->getEmpresaId(),
            $keys[3] => $this->getTipo(),
            $keys[4] => $this->getNombre(),
            $keys[5] => $this->getReferencia(),
            $keys[6] => $this->getMotivo(),
            $keys[7] => $this->getValor(),
            $keys[8] => $this->getPartidaNo(),
            $keys[9] => $this->getEstatus(),
            $keys[10] => $this->getFecha(),
            $keys[11] => $this->getUsuario(),
            $keys[12] => $this->getUsuarioConfirmo(),
            $keys[13] => $this->getFechaConfirmo(),
            $keys[14] => $this->getChequeNo(),
        );
        if ($includeForeignObjects) {
            if (null !== $this->aEmpresa) {
                $result['Empresa'] = $this->aEmpresa->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
            }
            if (null !== $this->collCheques) {
                $result['Cheques'] = $this->collCheques->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
        }

        return $result;
    }

    /**
     * Sets a field from the object by name passed in as a string.
     *
     * @param string $name peer name
     * @param mixed $value field value
     * @param string $type The type of fieldname the $name is of:
     *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                     Defaults to BasePeer::TYPE_PHPNAME
     * @return void
     */
    public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = SolicitudChequePeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);

        $this->setByPosition($pos, $value);
    }

    /**
     * Sets a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @param mixed $value field value
     * @return void
     */
    public function setByPosition($pos, $value)
    {
        switch ($pos) {
            case 0:
                $this->setId($value);
                break;
            case 1:
                $this->setCodigo($value);
                break;
            case 2:
                $this->setEmpresaId($value);
                break;
            case 3:
                $this->setTipo($value);
                break;
            case 4:
                $this->setNombre($value);
                break;
            case 5:
                $this->setReferencia($value);
                break;
            case 6:
                $this->setMotivo($value);
                break;
            case 7:
                $this->setValor($value);
                break;
            case 8:
                $this->setPartidaNo($value);
                break;
            case 9:
                $this->setEstatus($value);
                break;
            case 10:
                $this->setFecha($value);
                break;
            case 11:
                $this->setUsuario($value);
                break;
            case 12:
                $this->setUsuarioConfirmo($value);
                break;
            case 13:
                $this->setFechaConfirmo($value);
                break;
            case 14:
                $this->setChequeNo($value);
                break;
        } // switch()
    }

    /**
     * Populates the object using an array.
     *
     * This is particularly useful when populating an object from one of the
     * request arrays (e.g. $_POST).  This method goes through the column
     * names, checking to see whether a matching key exists in populated
     * array. If so the setByName() method is called for that column.
     *
     * You can specify the key type of the array by additionally passing one
     * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     * The default key type is the column's BasePeer::TYPE_PHPNAME
     *
     * @param array  $arr     An array to populate the object from.
     * @param string $keyType The type of keys the array uses.
     * @return void
     */
    public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
    {
        $keys = SolicitudChequePeer::getFieldNames($keyType);

        if (array_key_exists($keys[0], $arr)) $this->setId($arr[$keys[0]]);
        if (array_key_exists($keys[1], $arr)) $this->setCodigo($arr[$keys[1]]);
        if (array_key_exists($keys[2], $arr)) $this->setEmpresaId($arr[$keys[2]]);
        if (array_key_exists($keys[3], $arr)) $this->setTipo($arr[$keys[3]]);
        if (array_key_exists($keys[4], $arr)) $this->setNombre($arr[$keys[4]]);
        if (array_key_exists($keys[5], $arr)) $this->setReferencia($arr[$keys[5]]);
        if (array_key_exists($keys[6], $arr)) $this->setMotivo($arr[$keys[6]]);
        if (array_key_exists($keys[7], $arr)) $this->setValor($arr[$keys[7]]);
        if (array_key_exists($keys[8], $arr)) $this->setPartidaNo($arr[$keys[8]]);
        if (array_key_exists($keys[9], $arr)) $this->setEstatus($arr[$keys[9]]);
        if (array_key_exists($keys[10], $arr)) $this->setFecha($arr[$keys[10]]);
        if (array_key_exists($keys[11], $arr)) $this->setUsuario($arr[$keys[11]]);
        if (array_key_exists($keys[12], $arr)) $this->setUsuarioConfirmo($arr[$keys[12]]);
        if (array_key_exists($keys[13], $arr)) $this->setFechaConfirmo($arr[$keys[13]]);
        if (array_key_exists($keys[14], $arr)) $this->setChequeNo($arr[$keys[14]]);
    }

    /**
     * Build a Criteria object containing the values of all modified columns in this object.
     *
     * @return Criteria The Criteria object containing all modified values.
     */
    public function buildCriteria()
    {
        $criteria = new Criteria(SolicitudChequePeer::DATABASE_NAME);

        if ($this->isColumnModified(SolicitudChequePeer::ID)) $criteria->add(SolicitudChequePeer::ID, $this->id);
        if ($this->isColumnModified(SolicitudChequePeer::CODIGO)) $criteria->add(SolicitudChequePeer::CODIGO, $this->codigo);
        if ($this->isColumnModified(SolicitudChequePeer::EMPRESA_ID)) $criteria->add(SolicitudChequePeer::EMPRESA_ID, $this->empresa_id);
        if ($this->isColumnModified(SolicitudChequePeer::TIPO)) $criteria->add(SolicitudChequePeer::TIPO, $this->tipo);
        if ($this->isColumnModified(SolicitudChequePeer::NOMBRE)) $criteria->add(SolicitudChequePeer::NOMBRE, $this->nombre);
        if ($this->isColumnModified(SolicitudChequePeer::REFERENCIA)) $criteria->add(SolicitudChequePeer::REFERENCIA, $this->referencia);
        if ($this->isColumnModified(SolicitudChequePeer::MOTIVO)) $criteria->add(SolicitudChequePeer::MOTIVO, $this->motivo);
        if ($this->isColumnModified(SolicitudChequePeer::VALOR)) $criteria->add(SolicitudChequePeer::VALOR, $this->valor);
        if ($this->isColumnModified(SolicitudChequePeer::PARTIDA_NO)) $criteria->add(SolicitudChequePeer::PARTIDA_NO, $this->partida_no);
        if ($this->isColumnModified(SolicitudChequePeer::ESTATUS)) $criteria->add(SolicitudChequePeer::ESTATUS, $this->estatus);
        if ($this->isColumnModified(SolicitudChequePeer::FECHA)) $criteria->add(SolicitudChequePeer::FECHA, $this->fecha);
        if ($this->isColumnModified(SolicitudChequePeer::USUARIO)) $criteria->add(SolicitudChequePeer::USUARIO, $this->usuario);
        if ($this->isColumnModified(SolicitudChequePeer::USUARIO_CONFIRMO)) $criteria->add(SolicitudChequePeer::USUARIO_CONFIRMO, $this->usuario_confirmo);
        if ($this->isColumnModified(SolicitudChequePeer::FECHA_CONFIRMO)) $criteria->add(SolicitudChequePeer::FECHA_CONFIRMO, $this->fecha_confirmo);
        if ($this->isColumnModified(SolicitudChequePeer::CHEQUE_NO)) $criteria->add(SolicitudChequePeer::CHEQUE_NO, $this->cheque_no);

        return $criteria;
    }

    /**
     * Builds a Criteria object containing the primary key for this object.
     *
     * Unlike buildCriteria() this method includes the primary key values regardless
     * of whether or not they have been modified.
     *
     * @return Criteria The Criteria object containing value(s) for primary key(s).
     */
    public function buildPkeyCriteria()
    {
        $criteria = new Criteria(SolicitudChequePeer::DATABASE_NAME);
        $criteria->add(SolicitudChequePeer::ID, $this->id);

        return $criteria;
    }

    /**
     * Returns the primary key for this object (row).
     * @return int
     */
    public function getPrimaryKey()
    {
        return $this->getId();
    }

    /**
     * Generic method to set the primary key (id column).
     *
     * @param  int $key Primary key.
     * @return void
     */
    public function setPrimaryKey($key)
    {
        $this->setId($key);
    }

    /**
     * Returns true if the primary key for this object is null.
     * @return boolean
     */
    public function isPrimaryKeyNull()
    {

        return null === $this->getId();
    }

    /**
     * Sets contents of passed object to values from current object.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param object $copyObj An object of SolicitudCheque (or compatible) type.
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @param boolean $makeNew Whether to reset autoincrement PKs and make the object new.
     * @throws PropelException
     */
    public function copyInto($copyObj, $deepCopy = false, $makeNew = true)
    {
        $copyObj->setCodigo($this->getCodigo());
        $copyObj->setEmpresaId($this->getEmpresaId());
        $copyObj->setTipo($this->getTipo());
        $copyObj->setNombre($this->getNombre());
        $copyObj->setReferencia($this->getReferencia());
        $copyObj->setMotivo($this->getMotivo());
        $copyObj->setValor($this->getValor());
        $copyObj->setPartidaNo($this->getPartidaNo());
        $copyObj->setEstatus($this->getEstatus());
        $copyObj->setFecha($this->getFecha());
        $copyObj->setUsuario($this->getUsuario());
        $copyObj->setUsuarioConfirmo($this->getUsuarioConfirmo());
        $copyObj->setFechaConfirmo($this->getFechaConfirmo());
        $copyObj->setChequeNo($this->getChequeNo());

        if ($deepCopy && !$this->startCopy) {
            // important: temporarily setNew(false) because this affects the behavior of
            // the getter/setter methods for fkey referrer objects.
            $copyObj->setNew(false);
            // store object hash to prevent cycle
            $this->startCopy = true;

            foreach ($this->getCheques() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addCheque($relObj->copy($deepCopy));
                }
            }

            //unflag object copy
            $this->startCopy = false;
        } // if ($deepCopy)

        if ($makeNew) {
            $copyObj->setNew(true);
            $copyObj->setId(NULL); // this is a auto-increment column, so set to default value
        }
    }

    /**
     * Makes a copy of this object that will be inserted as a new row in table when saved.
     * It creates a new object filling in the simple attributes, but skipping any primary
     * keys that are defined for the table.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @return SolicitudCheque Clone of current object.
     * @throws PropelException
     */
    public function copy($deepCopy = false)
    {
        // we use get_class(), because this might be a subclass
        $clazz = get_class($this);
        $copyObj = new $clazz();
        $this->copyInto($copyObj, $deepCopy);

        return $copyObj;
    }

    /**
     * Returns a peer instance associated with this om.
     *
     * Since Peer classes are not to have any instance attributes, this method returns the
     * same instance for all member of this class. The method could therefore
     * be static, but this would prevent one from overriding the behavior.
     *
     * @return SolicitudChequePeer
     */
    public function getPeer()
    {
        if (self::$peer === null) {
            self::$peer = new SolicitudChequePeer();
        }

        return self::$peer;
    }

    /**
     * Declares an association between this object and a Empresa object.
     *
     * @param             Empresa $v
     * @return SolicitudCheque The current object (for fluent API support)
     * @throws PropelException
     */
    public function setEmpresa(Empresa $v = null)
    {
        if ($v === null) {
            $this->setEmpresaId(NULL);
        } else {
            $this->setEmpresaId($v->getId());
        }

        $this->aEmpresa = $v;

        // Add binding for other direction of this n:n relationship.
        // If this object has already been added to the Empresa object, it will not be re-added.
        if ($v !== null) {
            $v->addSolicitudCheque($this);
        }


        return $this;
    }


    /**
     * Get the associated Empresa object
     *
     * @param PropelPDO $con Optional Connection object.
     * @return Empresa The associated Empresa object.
     * @throws PropelException
     */
    public function getEmpresa(PropelPDO $con = null)
    {
        if ($this->aEmpresa === null && ($this->empresa_id !== null)) {
            $this->aEmpresa = EmpresaQuery::create()->findPk($this->empresa_id, $con);
            /* The following can be used additionally to
                guarantee the related object contains a reference
                to this object.  This level of coupling may, however, be
                undesirable since it could result in an only partially populated collection
                in the referenced object.
                $this->aEmpresa->addSolicitudCheques($this);
             */
        }

        return $this->aEmpresa;
    }


    /**
     * Initializes a collection based on the name of a relation.
     * Avoids crafting an 'init[$relationName]s' method name
     * that wouldn't work when StandardEnglishPluralizer is used.
     *
     * @param string $relationName The name of the relation to initialize
     * @return void
     */
    public function initRelation($relationName)
    {
        if ('Cheque' == $relationName) {
            $this->initCheques();
        }
    }

    /**
     * Clears out the collCheques collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return void
     * @see        addCheques()
     */
    public function clearCheques()
    {
        $this->collCheques = null; // important to set this to null since that means it is uninitialized
        $this->collChequesPartial = null;
    }

    /**
     * reset is the collCheques collection loaded partially
     *
     * @return void
     */
    public function resetPartialCheques($v = true)
    {
        $this->collChequesPartial = $v;
    }

    /**
     * Initializes the collCheques collection.
     *
     * By default this just sets the collCheques collection to an empty array (like clearcollCheques());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initCheques($overrideExisting = true)
    {
        if (null !== $this->collCheques && !$overrideExisting) {
            return;
        }
        $this->collCheques = new PropelObjectCollection();
        $this->collCheques->setModel('Cheque');
    }

    /**
     * Gets an array of Cheque objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this SolicitudCheque is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|Cheque[] List of Cheque objects
     * @throws PropelException
     */
    public function getCheques($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collChequesPartial && !$this->isNew();
        if (null === $this->collCheques || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collCheques) {
                // return empty collection
                $this->initCheques();
            } else {
                $collCheques = ChequeQuery::create(null, $criteria)
                    ->filterBySolicitudCheque($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collChequesPartial && count($collCheques)) {
                      $this->initCheques(false);

                      foreach($collCheques as $obj) {
                        if (false == $this->collCheques->contains($obj)) {
                          $this->collCheques->append($obj);
                        }
                      }

                      $this->collChequesPartial = true;
                    }

                    return $collCheques;
                }

                if($partial && $this->collCheques) {
                    foreach($this->collCheques as $obj) {
                        if($obj->isNew()) {
                            $collCheques[] = $obj;
                        }
                    }
                }

                $this->collCheques = $collCheques;
                $this->collChequesPartial = false;
            }
        }

        return $this->collCheques;
    }

    /**
     * Sets a collection of Cheque objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $cheques A Propel collection.
     * @param PropelPDO $con Optional connection object
     */
    public function setCheques(PropelCollection $cheques, PropelPDO $con = null)
    {
        $this->chequesScheduledForDeletion = $this->getCheques(new Criteria(), $con)->diff($cheques);

        foreach ($this->chequesScheduledForDeletion as $chequeRemoved) {
            $chequeRemoved->setSolicitudCheque(null);
        }

        $this->collCheques = null;
        foreach ($cheques as $cheque) {
            $this->addCheque($cheque);
        }

        $this->collCheques = $cheques;
        $this->collChequesPartial = false;
    }

    /**
     * Returns the number of related Cheque objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related Cheque objects.
     * @throws PropelException
     */
    public function countCheques(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collChequesPartial && !$this->isNew();
        if (null === $this->collCheques || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collCheques) {
                return 0;
            } else {
                if($partial && !$criteria) {
                    return count($this->getCheques());
                }
                $query = ChequeQuery::create(null, $criteria);
                if ($distinct) {
                    $query->distinct();
                }

                return $query
                    ->filterBySolicitudCheque($this)
                    ->count($con);
            }
        } else {
            return count($this->collCheques);
        }
    }

    /**
     * Method called to associate a Cheque object to this object
     * through the Cheque foreign key attribute.
     *
     * @param    Cheque $l Cheque
     * @return SolicitudCheque The current object (for fluent API support)
     */
    public function addCheque(Cheque $l)
    {
        if ($this->collCheques === null) {
            $this->initCheques();
            $this->collChequesPartial = true;
        }
        if (!$this->collCheques->contains($l)) { // only add it if the **same** object is not already associated
            $this->doAddCheque($l);
        }

        return $this;
    }

    /**
     * @param	Cheque $cheque The cheque object to add.
     */
    protected function doAddCheque($cheque)
    {
        $this->collCheques[]= $cheque;
        $cheque->setSolicitudCheque($this);
    }

    /**
     * @param	Cheque $cheque The cheque object to remove.
     */
    public function removeCheque($cheque)
    {
        if ($this->getCheques()->contains($cheque)) {
            $this->collCheques->remove($this->collCheques->search($cheque));
            if (null === $this->chequesScheduledForDeletion) {
                $this->chequesScheduledForDeletion = clone $this->collCheques;
                $this->chequesScheduledForDeletion->clear();
            }
            $this->chequesScheduledForDeletion[]= $cheque;
            $cheque->setSolicitudCheque(null);
        }
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this SolicitudCheque is new, it will return
     * an empty collection; or if this SolicitudCheque has previously
     * been saved, it will retrieve related Cheques from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in SolicitudCheque.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Cheque[] List of Cheque objects
     */
    public function getChequesJoinBanco($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ChequeQuery::create(null, $criteria);
        $query->joinWith('Banco', $join_behavior);

        return $this->getCheques($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this SolicitudCheque is new, it will return
     * an empty collection; or if this SolicitudCheque has previously
     * been saved, it will retrieve related Cheques from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in SolicitudCheque.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Cheque[] List of Cheque objects
     */
    public function getChequesJoinDocumentoCheque($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ChequeQuery::create(null, $criteria);
        $query->joinWith('DocumentoCheque', $join_behavior);

        return $this->getCheques($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this SolicitudCheque is new, it will return
     * an empty collection; or if this SolicitudCheque has previously
     * been saved, it will retrieve related Cheques from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in SolicitudCheque.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Cheque[] List of Cheque objects
     */
    public function getChequesJoinProveedor($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ChequeQuery::create(null, $criteria);
        $query->joinWith('Proveedor', $join_behavior);

        return $this->getCheques($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this SolicitudCheque is new, it will return
     * an empty collection; or if this SolicitudCheque has previously
     * been saved, it will retrieve related Cheques from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in SolicitudCheque.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|Cheque[] List of Cheque objects
     */
    public function getChequesJoinOrdenDevolucion($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = ChequeQuery::create(null, $criteria);
        $query->joinWith('OrdenDevolucion', $join_behavior);

        return $this->getCheques($query, $con);
    }

    /**
     * Clears the current object and sets all attributes to their default values
     */
    public function clear()
    {
        $this->id = null;
        $this->codigo = null;
        $this->empresa_id = null;
        $this->tipo = null;
        $this->nombre = null;
        $this->referencia = null;
        $this->motivo = null;
        $this->valor = null;
        $this->partida_no = null;
        $this->estatus = null;
        $this->fecha = null;
        $this->usuario = null;
        $this->usuario_confirmo = null;
        $this->fecha_confirmo = null;
        $this->cheque_no = null;
        $this->alreadyInSave = false;
        $this->alreadyInValidation = false;
        $this->clearAllReferences();
        $this->applyDefaultValues();
        $this->resetModified();
        $this->setNew(true);
        $this->setDeleted(false);
    }

    /**
     * Resets all references to other model objects or collections of model objects.
     *
     * This method is a user-space workaround for PHP's inability to garbage collect
     * objects with circular references (even in PHP 5.3). This is currently necessary
     * when using Propel in certain daemon or large-volumne/high-memory operations.
     *
     * @param boolean $deep Whether to also clear the references on all referrer objects.
     */
    public function clearAllReferences($deep = false)
    {
        if ($deep) {
            if ($this->collCheques) {
                foreach ($this->collCheques as $o) {
                    $o->clearAllReferences($deep);
                }
            }
        } // if ($deep)

        if ($this->collCheques instanceof PropelCollection) {
            $this->collCheques->clearIterator();
        }
        $this->collCheques = null;
        $this->aEmpresa = null;
    }

    /**
     * return the string representation of this object
     *
     * @return string
     */
    public function __toString()
    {
        return (string) $this->exportTo(SolicitudChequePeer::DEFAULT_STRING_FORMAT);
    }

    /**
     * return true is the object is in saving state
     *
     * @return boolean
     */
    public function isAlreadyInSave()
    {
        return $this->alreadyInSave;
    }

    /**
     * Catches calls to virtual methods
     */
    public function __call($name, $params)
    {

        // symfony_behaviors behavior
        if ($callable = sfMixer::getCallable('BaseSolicitudCheque:' . $name))
        {
          array_unshift($params, $this);
          return call_user_func_array($callable, $params);
        }


        return parent::__call($name, $params);
    }

}
