<?php


/**
 * Base class that represents a row from the 'orden_vendedor' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.6.7 on:
 *
 * 12/30/25 22:58:32
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseOrdenVendedor extends BaseObject implements Persistent
{
    /**
     * Peer class name
     */
    const PEER = 'OrdenVendedorPeer';

    /**
     * The Peer class.
     * Instance provides a convenient way of calling static methods on a class
     * that calling code may not be able to identify.
     * @var        OrdenVendedorPeer
     */
    protected static $peer;

    /**
     * The flag var to prevent infinit loop in deep copy
     * @var       boolean
     */
    protected $startCopy = false;

    /**
     * The value for the id field.
     * @var        int
     */
    protected $id;

    /**
     * The value for the observaciones field.
     * @var        string
     */
    protected $observaciones;

    /**
     * The value for the estado field.
     * @var        string
     */
    protected $estado;

    /**
     * The value for the fecha field.
     * @var        string
     */
    protected $fecha;

    /**
     * The value for the usuario field.
     * @var        string
     */
    protected $usuario;

    /**
     * The value for the empresa_id field.
     * @var        int
     */
    protected $empresa_id;

    /**
     * The value for the vendedor_id field.
     * @var        int
     */
    protected $vendedor_id;

    /**
     * @var        Empresa
     */
    protected $aEmpresa;

    /**
     * @var        Vendedor
     */
    protected $aVendedor;

    /**
     * @var        PropelObjectCollection|VendedorProducto[] Collection to store aggregation of VendedorProducto objects.
     */
    protected $collVendedorProductos;
    protected $collVendedorProductosPartial;

    /**
     * @var        PropelObjectCollection|OrdenUbicacion[] Collection to store aggregation of OrdenUbicacion objects.
     */
    protected $collOrdenUbicacions;
    protected $collOrdenUbicacionsPartial;

    /**
     * Flag to prevent endless save loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInSave = false;

    /**
     * Flag to prevent endless validation loop, if this object is referenced
     * by another object which falls in this transaction.
     * @var        boolean
     */
    protected $alreadyInValidation = false;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $vendedorProductosScheduledForDeletion = null;

    /**
     * An array of objects scheduled for deletion.
     * @var		PropelObjectCollection
     */
    protected $ordenUbicacionsScheduledForDeletion = null;

    /**
     * Get the [id] column value.
     *
     * @return int
     */
    public function getId()
    {
        return $this->id;
    }

    /**
     * Get the [observaciones] column value.
     *
     * @return string
     */
    public function getObservaciones()
    {
        return $this->observaciones;
    }

    /**
     * Get the [estado] column value.
     *
     * @return string
     */
    public function getEstado()
    {
        return $this->estado;
    }

    /**
     * Get the [optionally formatted] temporal [fecha] column value.
     *
     *
     * @param string $format The date/time format string (either date()-style or strftime()-style).
     *				 If format is null, then the raw DateTime object will be returned.
     * @return mixed Formatted date/time value as string or DateTime object (if format is null), null if column is null, and 0 if column value is 0000-00-00 00:00:00
     * @throws PropelException - if unable to parse/validate the date/time value.
     */
    public function getFecha($format = 'Y-m-d H:i:s')
    {
        if ($this->fecha === null) {
            return null;
        }

        if ($this->fecha === '0000-00-00 00:00:00') {
            // while technically this is not a default value of null,
            // this seems to be closest in meaning.
            return null;
        } else {
            try {
                $dt = new DateTime($this->fecha);
            } catch (Exception $x) {
                throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->fecha, true), $x);
            }
        }

        if ($format === null) {
            // Because propel.useDateTimeClass is true, we return a DateTime object.
            return $dt;
        } elseif (strpos($format, '%') !== false) {
            return strftime($format, $dt->format('U'));
        } else {
            return $dt->format($format);
        }
    }

    /**
     * Get the [usuario] column value.
     *
     * @return string
     */
    public function getUsuario()
    {
        return $this->usuario;
    }

    /**
     * Get the [empresa_id] column value.
     *
     * @return int
     */
    public function getEmpresaId()
    {
        return $this->empresa_id;
    }

    /**
     * Get the [vendedor_id] column value.
     *
     * @return int
     */
    public function getVendedorId()
    {
        return $this->vendedor_id;
    }

    /**
     * Set the value of [id] column.
     *
     * @param int $v new value
     * @return OrdenVendedor The current object (for fluent API support)
     */
    public function setId($v)
    {
        if ($v !== null) {
            $v = (int) $v;
        }

        if ($this->id !== $v) {
            $this->id = $v;
            $this->modifiedColumns[] = OrdenVendedorPeer::ID;
        }


        return $this;
    } // setId()

    /**
     * Set the value of [observaciones] column.
     *
     * @param string $v new value
     * @return OrdenVendedor The current object (for fluent API support)
     */
    public function setObservaciones($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->observaciones !== $v) {
            $this->observaciones = $v;
            $this->modifiedColumns[] = OrdenVendedorPeer::OBSERVACIONES;
        }


        return $this;
    } // setObservaciones()

    /**
     * Set the value of [estado] column.
     *
     * @param string $v new value
     * @return OrdenVendedor The current object (for fluent API support)
     */
    public function setEstado($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->estado !== $v) {
            $this->estado = $v;
            $this->modifiedColumns[] = OrdenVendedorPeer::ESTADO;
        }


        return $this;
    } // setEstado()

    /**
     * Sets the value of [fecha] column to a normalized version of the date/time value specified.
     *
     * @param mixed $v string, integer (timestamp), or DateTime value.
     *               Empty strings are treated as null.
     * @return OrdenVendedor The current object (for fluent API support)
     */
    public function setFecha($v)
    {
        $dt = PropelDateTime::newInstance($v, null, 'DateTime');
        if ($this->fecha !== null || $dt !== null) {
            $currentDateAsString = ($this->fecha !== null && $tmpDt = new DateTime($this->fecha)) ? $tmpDt->format('Y-m-d H:i:s') : null;
            $newDateAsString = $dt ? $dt->format('Y-m-d H:i:s') : null;
            if ($currentDateAsString !== $newDateAsString) {
                $this->fecha = $newDateAsString;
                $this->modifiedColumns[] = OrdenVendedorPeer::FECHA;
            }
        } // if either are not null


        return $this;
    } // setFecha()

    /**
     * Set the value of [usuario] column.
     *
     * @param string $v new value
     * @return OrdenVendedor The current object (for fluent API support)
     */
    public function setUsuario($v)
    {
        if ($v !== null) {
            $v = (string) $v;
        }

        if ($this->usuario !== $v) {
            $this->usuario = $v;
            $this->modifiedColumns[] = OrdenVendedorPeer::USUARIO;
        }


        return $this;
    } // setUsuario()

    /**
     * Set the value of [empresa_id] column.
     *
     * @param int $v new value
     * @return OrdenVendedor The current object (for fluent API support)
     */
    public function setEmpresaId($v)
    {
        if ($v !== null) {
            $v = (int) $v;
        }

        if ($this->empresa_id !== $v) {
            $this->empresa_id = $v;
            $this->modifiedColumns[] = OrdenVendedorPeer::EMPRESA_ID;
        }

        if ($this->aEmpresa !== null && $this->aEmpresa->getId() !== $v) {
            $this->aEmpresa = null;
        }


        return $this;
    } // setEmpresaId()

    /**
     * Set the value of [vendedor_id] column.
     *
     * @param int $v new value
     * @return OrdenVendedor The current object (for fluent API support)
     */
    public function setVendedorId($v)
    {
        if ($v !== null) {
            $v = (int) $v;
        }

        if ($this->vendedor_id !== $v) {
            $this->vendedor_id = $v;
            $this->modifiedColumns[] = OrdenVendedorPeer::VENDEDOR_ID;
        }

        if ($this->aVendedor !== null && $this->aVendedor->getId() !== $v) {
            $this->aVendedor = null;
        }


        return $this;
    } // setVendedorId()

    /**
     * Indicates whether the columns in this object are only set to default values.
     *
     * This method can be used in conjunction with isModified() to indicate whether an object is both
     * modified _and_ has some values set which are non-default.
     *
     * @return boolean Whether the columns in this object are only been set with default values.
     */
    public function hasOnlyDefaultValues()
    {
        // otherwise, everything was equal, so return true
        return true;
    } // hasOnlyDefaultValues()

    /**
     * Hydrates (populates) the object variables with values from the database resultset.
     *
     * An offset (0-based "start column") is specified so that objects can be hydrated
     * with a subset of the columns in the resultset rows.  This is needed, for example,
     * for results of JOIN queries where the resultset row includes columns from two or
     * more tables.
     *
     * @param array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
     * @param int $startcol 0-based offset column which indicates which restultset column to start with.
     * @param boolean $rehydrate Whether this object is being re-hydrated from the database.
     * @return int             next starting column
     * @throws PropelException - Any caught Exception will be rewrapped as a PropelException.
     */
    public function hydrate($row, $startcol = 0, $rehydrate = false)
    {
        try {

            $this->id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
            $this->observaciones = ($row[$startcol + 1] !== null) ? (string) $row[$startcol + 1] : null;
            $this->estado = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
            $this->fecha = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
            $this->usuario = ($row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
            $this->empresa_id = ($row[$startcol + 5] !== null) ? (int) $row[$startcol + 5] : null;
            $this->vendedor_id = ($row[$startcol + 6] !== null) ? (int) $row[$startcol + 6] : null;
            $this->resetModified();

            $this->setNew(false);

            if ($rehydrate) {
                $this->ensureConsistency();
            }

            return $startcol + 7; // 7 = OrdenVendedorPeer::NUM_HYDRATE_COLUMNS.

        } catch (Exception $e) {
            throw new PropelException("Error populating OrdenVendedor object", $e);
        }
    }

    /**
     * Checks and repairs the internal consistency of the object.
     *
     * This method is executed after an already-instantiated object is re-hydrated
     * from the database.  It exists to check any foreign keys to make sure that
     * the objects related to the current object are correct based on foreign key.
     *
     * You can override this method in the stub class, but you should always invoke
     * the base method from the overridden method (i.e. parent::ensureConsistency()),
     * in case your model changes.
     *
     * @throws PropelException
     */
    public function ensureConsistency()
    {

        if ($this->aEmpresa !== null && $this->empresa_id !== $this->aEmpresa->getId()) {
            $this->aEmpresa = null;
        }
        if ($this->aVendedor !== null && $this->vendedor_id !== $this->aVendedor->getId()) {
            $this->aVendedor = null;
        }
    } // ensureConsistency

    /**
     * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
     *
     * This will only work if the object has been saved and has a valid primary key set.
     *
     * @param boolean $deep (optional) Whether to also de-associated any related objects.
     * @param PropelPDO $con (optional) The PropelPDO connection to use.
     * @return void
     * @throws PropelException - if this object is deleted, unsaved or doesn't have pk match in db
     */
    public function reload($deep = false, PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("Cannot reload a deleted object.");
        }

        if ($this->isNew()) {
            throw new PropelException("Cannot reload an unsaved object.");
        }

        if ($con === null) {
            $con = Propel::getConnection(OrdenVendedorPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }

        // We don't need to alter the object instance pool; we're just modifying this instance
        // already in the pool.

        $stmt = OrdenVendedorPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
        $row = $stmt->fetch(PDO::FETCH_NUM);
        $stmt->closeCursor();
        if (!$row) {
            throw new PropelException('Cannot find matching row in the database to reload object values.');
        }
        $this->hydrate($row, 0, true); // rehydrate

        if ($deep) {  // also de-associate any related objects?

            $this->aEmpresa = null;
            $this->aVendedor = null;
            $this->collVendedorProductos = null;

            $this->collOrdenUbicacions = null;

        } // if (deep)
    }

    /**
     * Removes this object from datastore and sets delete attribute.
     *
     * @param PropelPDO $con
     * @return void
     * @throws PropelException
     * @throws Exception
     * @see        BaseObject::setDeleted()
     * @see        BaseObject::isDeleted()
     */
    public function delete(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("This object has already been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(OrdenVendedorPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        try {
            $deleteQuery = OrdenVendedorQuery::create()
                ->filterByPrimaryKey($this->getPrimaryKey());
            $ret = $this->preDelete($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BaseOrdenVendedor:delete:pre') as $callable)
            {
              if (call_user_func($callable, $this, $con))
              {
                $con->commit();
                return;
              }
            }

            if ($ret) {
                $deleteQuery->delete($con);
                $this->postDelete($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BaseOrdenVendedor:delete:post') as $callable)
                {
                  call_user_func($callable, $this, $con);
                }

                $con->commit();
                $this->setDeleted(true);
            } else {
                $con->commit();
            }
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Persists this object to the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All modified related objects will also be persisted in the doSave()
     * method.  This method wraps all precipitate database operations in a
     * single transaction.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @throws Exception
     * @see        doSave()
     */
    public function save(PropelPDO $con = null)
    {
        if ($this->isDeleted()) {
            throw new PropelException("You cannot save an object that has been deleted.");
        }

        if ($con === null) {
            $con = Propel::getConnection(OrdenVendedorPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
        }

        $con->beginTransaction();
        $isInsert = $this->isNew();
        try {
            $ret = $this->preSave($con);
            // symfony_behaviors behavior
            foreach (sfMixer::getCallables('BaseOrdenVendedor:save:pre') as $callable)
            {
              if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
              {
                  $con->commit();
                return $affectedRows;
              }
            }

            if ($isInsert) {
                $ret = $ret && $this->preInsert($con);
            } else {
                $ret = $ret && $this->preUpdate($con);
            }
            if ($ret) {
                $affectedRows = $this->doSave($con);
                if ($isInsert) {
                    $this->postInsert($con);
                } else {
                    $this->postUpdate($con);
                }
                $this->postSave($con);
                // symfony_behaviors behavior
                foreach (sfMixer::getCallables('BaseOrdenVendedor:save:post') as $callable)
                {
                  call_user_func($callable, $this, $con, $affectedRows);
                }

                OrdenVendedorPeer::addInstanceToPool($this);
            } else {
                $affectedRows = 0;
            }
            $con->commit();

            return $affectedRows;
        } catch (Exception $e) {
            $con->rollBack();
            throw $e;
        }
    }

    /**
     * Performs the work of inserting or updating the row in the database.
     *
     * If the object is new, it inserts it; otherwise an update is performed.
     * All related objects are also updated in this method.
     *
     * @param PropelPDO $con
     * @return int             The number of rows affected by this insert/update and any referring fk objects' save() operations.
     * @throws PropelException
     * @see        save()
     */
    protected function doSave(PropelPDO $con)
    {
        $affectedRows = 0; // initialize var to track total num of affected rows
        if (!$this->alreadyInSave) {
            $this->alreadyInSave = true;

            // We call the save method on the following object(s) if they
            // were passed to this object by their coresponding set
            // method.  This object relates to these object(s) by a
            // foreign key reference.

            if ($this->aEmpresa !== null) {
                if ($this->aEmpresa->isModified() || $this->aEmpresa->isNew()) {
                    $affectedRows += $this->aEmpresa->save($con);
                }
                $this->setEmpresa($this->aEmpresa);
            }

            if ($this->aVendedor !== null) {
                if ($this->aVendedor->isModified() || $this->aVendedor->isNew()) {
                    $affectedRows += $this->aVendedor->save($con);
                }
                $this->setVendedor($this->aVendedor);
            }

            if ($this->isNew() || $this->isModified()) {
                // persist changes
                if ($this->isNew()) {
                    $this->doInsert($con);
                } else {
                    $this->doUpdate($con);
                }
                $affectedRows += 1;
                $this->resetModified();
            }

            if ($this->vendedorProductosScheduledForDeletion !== null) {
                if (!$this->vendedorProductosScheduledForDeletion->isEmpty()) {
                    foreach ($this->vendedorProductosScheduledForDeletion as $vendedorProducto) {
                        // need to save related object because we set the relation to null
                        $vendedorProducto->save($con);
                    }
                    $this->vendedorProductosScheduledForDeletion = null;
                }
            }

            if ($this->collVendedorProductos !== null) {
                foreach ($this->collVendedorProductos as $referrerFK) {
                    if (!$referrerFK->isDeleted()) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            if ($this->ordenUbicacionsScheduledForDeletion !== null) {
                if (!$this->ordenUbicacionsScheduledForDeletion->isEmpty()) {
                    foreach ($this->ordenUbicacionsScheduledForDeletion as $ordenUbicacion) {
                        // need to save related object because we set the relation to null
                        $ordenUbicacion->save($con);
                    }
                    $this->ordenUbicacionsScheduledForDeletion = null;
                }
            }

            if ($this->collOrdenUbicacions !== null) {
                foreach ($this->collOrdenUbicacions as $referrerFK) {
                    if (!$referrerFK->isDeleted()) {
                        $affectedRows += $referrerFK->save($con);
                    }
                }
            }

            $this->alreadyInSave = false;

        }

        return $affectedRows;
    } // doSave()

    /**
     * Insert the row in the database.
     *
     * @param PropelPDO $con
     *
     * @throws PropelException
     * @see        doSave()
     */
    protected function doInsert(PropelPDO $con)
    {
        $modifiedColumns = array();
        $index = 0;

        $this->modifiedColumns[] = OrdenVendedorPeer::ID;
        if (null !== $this->id) {
            throw new PropelException('Cannot insert a value for auto-increment primary key (' . OrdenVendedorPeer::ID . ')');
        }

         // check the columns in natural order for more readable SQL queries
        if ($this->isColumnModified(OrdenVendedorPeer::ID)) {
            $modifiedColumns[':p' . $index++]  = '`ID`';
        }
        if ($this->isColumnModified(OrdenVendedorPeer::OBSERVACIONES)) {
            $modifiedColumns[':p' . $index++]  = '`OBSERVACIONES`';
        }
        if ($this->isColumnModified(OrdenVendedorPeer::ESTADO)) {
            $modifiedColumns[':p' . $index++]  = '`ESTADO`';
        }
        if ($this->isColumnModified(OrdenVendedorPeer::FECHA)) {
            $modifiedColumns[':p' . $index++]  = '`FECHA`';
        }
        if ($this->isColumnModified(OrdenVendedorPeer::USUARIO)) {
            $modifiedColumns[':p' . $index++]  = '`USUARIO`';
        }
        if ($this->isColumnModified(OrdenVendedorPeer::EMPRESA_ID)) {
            $modifiedColumns[':p' . $index++]  = '`EMPRESA_ID`';
        }
        if ($this->isColumnModified(OrdenVendedorPeer::VENDEDOR_ID)) {
            $modifiedColumns[':p' . $index++]  = '`VENDEDOR_ID`';
        }

        $sql = sprintf(
            'INSERT INTO `orden_vendedor` (%s) VALUES (%s)',
            implode(', ', $modifiedColumns),
            implode(', ', array_keys($modifiedColumns))
        );

        try {
            $stmt = $con->prepare($sql);
            foreach ($modifiedColumns as $identifier => $columnName) {
                switch ($columnName) {
                    case '`ID`':
                        $stmt->bindValue($identifier, $this->id, PDO::PARAM_INT);
                        break;
                    case '`OBSERVACIONES`':
                        $stmt->bindValue($identifier, $this->observaciones, PDO::PARAM_STR);
                        break;
                    case '`ESTADO`':
                        $stmt->bindValue($identifier, $this->estado, PDO::PARAM_STR);
                        break;
                    case '`FECHA`':
                        $stmt->bindValue($identifier, $this->fecha, PDO::PARAM_STR);
                        break;
                    case '`USUARIO`':
                        $stmt->bindValue($identifier, $this->usuario, PDO::PARAM_STR);
                        break;
                    case '`EMPRESA_ID`':
                        $stmt->bindValue($identifier, $this->empresa_id, PDO::PARAM_INT);
                        break;
                    case '`VENDEDOR_ID`':
                        $stmt->bindValue($identifier, $this->vendedor_id, PDO::PARAM_INT);
                        break;
                }
            }
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute INSERT statement [%s]', $sql), $e);
        }

        try {
            $pk = $con->lastInsertId();
        } catch (Exception $e) {
            throw new PropelException('Unable to get autoincrement id.', $e);
        }
        $this->setId($pk);

        $this->setNew(false);
    }

    /**
     * Update the row in the database.
     *
     * @param PropelPDO $con
     *
     * @see        doSave()
     */
    protected function doUpdate(PropelPDO $con)
    {
        $selectCriteria = $this->buildPkeyCriteria();
        $valuesCriteria = $this->buildCriteria();
        BasePeer::doUpdate($selectCriteria, $valuesCriteria, $con);
    }

    /**
     * Array of ValidationFailed objects.
     * @var        array ValidationFailed[]
     */
    protected $validationFailures = array();

    /**
     * Gets any ValidationFailed objects that resulted from last call to validate().
     *
     *
     * @return array ValidationFailed[]
     * @see        validate()
     */
    public function getValidationFailures()
    {
        return $this->validationFailures;
    }

    /**
     * Validates the objects modified field values and all objects related to this table.
     *
     * If $columns is either a column name or an array of column names
     * only those columns are validated.
     *
     * @param mixed $columns Column name or an array of column names.
     * @return boolean Whether all columns pass validation.
     * @see        doValidate()
     * @see        getValidationFailures()
     */
    public function validate($columns = null)
    {
        $res = $this->doValidate($columns);
        if ($res === true) {
            $this->validationFailures = array();

            return true;
        } else {
            $this->validationFailures = $res;

            return false;
        }
    }

    /**
     * This function performs the validation work for complex object models.
     *
     * In addition to checking the current object, all related objects will
     * also be validated.  If all pass then <code>true</code> is returned; otherwise
     * an aggreagated array of ValidationFailed objects will be returned.
     *
     * @param array $columns Array of column names to validate.
     * @return mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
     */
    protected function doValidate($columns = null)
    {
        if (!$this->alreadyInValidation) {
            $this->alreadyInValidation = true;
            $retval = null;

            $failureMap = array();


            // We call the validate method on the following object(s) if they
            // were passed to this object by their coresponding set
            // method.  This object relates to these object(s) by a
            // foreign key reference.

            if ($this->aEmpresa !== null) {
                if (!$this->aEmpresa->validate($columns)) {
                    $failureMap = array_merge($failureMap, $this->aEmpresa->getValidationFailures());
                }
            }

            if ($this->aVendedor !== null) {
                if (!$this->aVendedor->validate($columns)) {
                    $failureMap = array_merge($failureMap, $this->aVendedor->getValidationFailures());
                }
            }


            if (($retval = OrdenVendedorPeer::doValidate($this, $columns)) !== true) {
                $failureMap = array_merge($failureMap, $retval);
            }


                if ($this->collVendedorProductos !== null) {
                    foreach ($this->collVendedorProductos as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }

                if ($this->collOrdenUbicacions !== null) {
                    foreach ($this->collOrdenUbicacions as $referrerFK) {
                        if (!$referrerFK->validate($columns)) {
                            $failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
                        }
                    }
                }


            $this->alreadyInValidation = false;
        }

        return (!empty($failureMap) ? $failureMap : true);
    }

    /**
     * Retrieves a field from the object by name passed in as a string.
     *
     * @param string $name name
     * @param string $type The type of fieldname the $name is of:
     *               one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *               BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *               Defaults to BasePeer::TYPE_PHPNAME
     * @return mixed Value of field.
     */
    public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = OrdenVendedorPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
        $field = $this->getByPosition($pos);

        return $field;
    }

    /**
     * Retrieves a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @return mixed Value of field at $pos
     */
    public function getByPosition($pos)
    {
        switch ($pos) {
            case 0:
                return $this->getId();
                break;
            case 1:
                return $this->getObservaciones();
                break;
            case 2:
                return $this->getEstado();
                break;
            case 3:
                return $this->getFecha();
                break;
            case 4:
                return $this->getUsuario();
                break;
            case 5:
                return $this->getEmpresaId();
                break;
            case 6:
                return $this->getVendedorId();
                break;
            default:
                return null;
                break;
        } // switch()
    }

    /**
     * Exports the object as an array.
     *
     * You can specify the key type of the array by passing one of the class
     * type constants.
     *
     * @param     string  $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     *                    BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                    Defaults to BasePeer::TYPE_PHPNAME.
     * @param     boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns. Defaults to true.
     * @param     array $alreadyDumpedObjects List of objects to skip to avoid recursion
     * @param     boolean $includeForeignObjects (optional) Whether to include hydrated related objects. Default to FALSE.
     *
     * @return array an associative array containing the field names (as keys) and field values
     */
    public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true, $alreadyDumpedObjects = array(), $includeForeignObjects = false)
    {
        if (isset($alreadyDumpedObjects['OrdenVendedor'][$this->getPrimaryKey()])) {
            return '*RECURSION*';
        }
        $alreadyDumpedObjects['OrdenVendedor'][$this->getPrimaryKey()] = true;
        $keys = OrdenVendedorPeer::getFieldNames($keyType);
        $result = array(
            $keys[0] => $this->getId(),
            $keys[1] => $this->getObservaciones(),
            $keys[2] => $this->getEstado(),
            $keys[3] => $this->getFecha(),
            $keys[4] => $this->getUsuario(),
            $keys[5] => $this->getEmpresaId(),
            $keys[6] => $this->getVendedorId(),
        );
        if ($includeForeignObjects) {
            if (null !== $this->aEmpresa) {
                $result['Empresa'] = $this->aEmpresa->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
            }
            if (null !== $this->aVendedor) {
                $result['Vendedor'] = $this->aVendedor->toArray($keyType, $includeLazyLoadColumns,  $alreadyDumpedObjects, true);
            }
            if (null !== $this->collVendedorProductos) {
                $result['VendedorProductos'] = $this->collVendedorProductos->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
            if (null !== $this->collOrdenUbicacions) {
                $result['OrdenUbicacions'] = $this->collOrdenUbicacions->toArray(null, true, $keyType, $includeLazyLoadColumns, $alreadyDumpedObjects);
            }
        }

        return $result;
    }

    /**
     * Sets a field from the object by name passed in as a string.
     *
     * @param string $name peer name
     * @param mixed $value field value
     * @param string $type The type of fieldname the $name is of:
     *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
     *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     *                     Defaults to BasePeer::TYPE_PHPNAME
     * @return void
     */
    public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
    {
        $pos = OrdenVendedorPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);

        $this->setByPosition($pos, $value);
    }

    /**
     * Sets a field from the object by Position as specified in the xml schema.
     * Zero-based.
     *
     * @param int $pos position in xml schema
     * @param mixed $value field value
     * @return void
     */
    public function setByPosition($pos, $value)
    {
        switch ($pos) {
            case 0:
                $this->setId($value);
                break;
            case 1:
                $this->setObservaciones($value);
                break;
            case 2:
                $this->setEstado($value);
                break;
            case 3:
                $this->setFecha($value);
                break;
            case 4:
                $this->setUsuario($value);
                break;
            case 5:
                $this->setEmpresaId($value);
                break;
            case 6:
                $this->setVendedorId($value);
                break;
        } // switch()
    }

    /**
     * Populates the object using an array.
     *
     * This is particularly useful when populating an object from one of the
     * request arrays (e.g. $_POST).  This method goes through the column
     * names, checking to see whether a matching key exists in populated
     * array. If so the setByName() method is called for that column.
     *
     * You can specify the key type of the array by additionally passing one
     * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
     * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
     * The default key type is the column's BasePeer::TYPE_PHPNAME
     *
     * @param array  $arr     An array to populate the object from.
     * @param string $keyType The type of keys the array uses.
     * @return void
     */
    public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
    {
        $keys = OrdenVendedorPeer::getFieldNames($keyType);

        if (array_key_exists($keys[0], $arr)) $this->setId($arr[$keys[0]]);
        if (array_key_exists($keys[1], $arr)) $this->setObservaciones($arr[$keys[1]]);
        if (array_key_exists($keys[2], $arr)) $this->setEstado($arr[$keys[2]]);
        if (array_key_exists($keys[3], $arr)) $this->setFecha($arr[$keys[3]]);
        if (array_key_exists($keys[4], $arr)) $this->setUsuario($arr[$keys[4]]);
        if (array_key_exists($keys[5], $arr)) $this->setEmpresaId($arr[$keys[5]]);
        if (array_key_exists($keys[6], $arr)) $this->setVendedorId($arr[$keys[6]]);
    }

    /**
     * Build a Criteria object containing the values of all modified columns in this object.
     *
     * @return Criteria The Criteria object containing all modified values.
     */
    public function buildCriteria()
    {
        $criteria = new Criteria(OrdenVendedorPeer::DATABASE_NAME);

        if ($this->isColumnModified(OrdenVendedorPeer::ID)) $criteria->add(OrdenVendedorPeer::ID, $this->id);
        if ($this->isColumnModified(OrdenVendedorPeer::OBSERVACIONES)) $criteria->add(OrdenVendedorPeer::OBSERVACIONES, $this->observaciones);
        if ($this->isColumnModified(OrdenVendedorPeer::ESTADO)) $criteria->add(OrdenVendedorPeer::ESTADO, $this->estado);
        if ($this->isColumnModified(OrdenVendedorPeer::FECHA)) $criteria->add(OrdenVendedorPeer::FECHA, $this->fecha);
        if ($this->isColumnModified(OrdenVendedorPeer::USUARIO)) $criteria->add(OrdenVendedorPeer::USUARIO, $this->usuario);
        if ($this->isColumnModified(OrdenVendedorPeer::EMPRESA_ID)) $criteria->add(OrdenVendedorPeer::EMPRESA_ID, $this->empresa_id);
        if ($this->isColumnModified(OrdenVendedorPeer::VENDEDOR_ID)) $criteria->add(OrdenVendedorPeer::VENDEDOR_ID, $this->vendedor_id);

        return $criteria;
    }

    /**
     * Builds a Criteria object containing the primary key for this object.
     *
     * Unlike buildCriteria() this method includes the primary key values regardless
     * of whether or not they have been modified.
     *
     * @return Criteria The Criteria object containing value(s) for primary key(s).
     */
    public function buildPkeyCriteria()
    {
        $criteria = new Criteria(OrdenVendedorPeer::DATABASE_NAME);
        $criteria->add(OrdenVendedorPeer::ID, $this->id);

        return $criteria;
    }

    /**
     * Returns the primary key for this object (row).
     * @return int
     */
    public function getPrimaryKey()
    {
        return $this->getId();
    }

    /**
     * Generic method to set the primary key (id column).
     *
     * @param  int $key Primary key.
     * @return void
     */
    public function setPrimaryKey($key)
    {
        $this->setId($key);
    }

    /**
     * Returns true if the primary key for this object is null.
     * @return boolean
     */
    public function isPrimaryKeyNull()
    {

        return null === $this->getId();
    }

    /**
     * Sets contents of passed object to values from current object.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param object $copyObj An object of OrdenVendedor (or compatible) type.
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @param boolean $makeNew Whether to reset autoincrement PKs and make the object new.
     * @throws PropelException
     */
    public function copyInto($copyObj, $deepCopy = false, $makeNew = true)
    {
        $copyObj->setObservaciones($this->getObservaciones());
        $copyObj->setEstado($this->getEstado());
        $copyObj->setFecha($this->getFecha());
        $copyObj->setUsuario($this->getUsuario());
        $copyObj->setEmpresaId($this->getEmpresaId());
        $copyObj->setVendedorId($this->getVendedorId());

        if ($deepCopy && !$this->startCopy) {
            // important: temporarily setNew(false) because this affects the behavior of
            // the getter/setter methods for fkey referrer objects.
            $copyObj->setNew(false);
            // store object hash to prevent cycle
            $this->startCopy = true;

            foreach ($this->getVendedorProductos() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addVendedorProducto($relObj->copy($deepCopy));
                }
            }

            foreach ($this->getOrdenUbicacions() as $relObj) {
                if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
                    $copyObj->addOrdenUbicacion($relObj->copy($deepCopy));
                }
            }

            //unflag object copy
            $this->startCopy = false;
        } // if ($deepCopy)

        if ($makeNew) {
            $copyObj->setNew(true);
            $copyObj->setId(NULL); // this is a auto-increment column, so set to default value
        }
    }

    /**
     * Makes a copy of this object that will be inserted as a new row in table when saved.
     * It creates a new object filling in the simple attributes, but skipping any primary
     * keys that are defined for the table.
     *
     * If desired, this method can also make copies of all associated (fkey referrers)
     * objects.
     *
     * @param boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
     * @return OrdenVendedor Clone of current object.
     * @throws PropelException
     */
    public function copy($deepCopy = false)
    {
        // we use get_class(), because this might be a subclass
        $clazz = get_class($this);
        $copyObj = new $clazz();
        $this->copyInto($copyObj, $deepCopy);

        return $copyObj;
    }

    /**
     * Returns a peer instance associated with this om.
     *
     * Since Peer classes are not to have any instance attributes, this method returns the
     * same instance for all member of this class. The method could therefore
     * be static, but this would prevent one from overriding the behavior.
     *
     * @return OrdenVendedorPeer
     */
    public function getPeer()
    {
        if (self::$peer === null) {
            self::$peer = new OrdenVendedorPeer();
        }

        return self::$peer;
    }

    /**
     * Declares an association between this object and a Empresa object.
     *
     * @param             Empresa $v
     * @return OrdenVendedor The current object (for fluent API support)
     * @throws PropelException
     */
    public function setEmpresa(Empresa $v = null)
    {
        if ($v === null) {
            $this->setEmpresaId(NULL);
        } else {
            $this->setEmpresaId($v->getId());
        }

        $this->aEmpresa = $v;

        // Add binding for other direction of this n:n relationship.
        // If this object has already been added to the Empresa object, it will not be re-added.
        if ($v !== null) {
            $v->addOrdenVendedor($this);
        }


        return $this;
    }


    /**
     * Get the associated Empresa object
     *
     * @param PropelPDO $con Optional Connection object.
     * @return Empresa The associated Empresa object.
     * @throws PropelException
     */
    public function getEmpresa(PropelPDO $con = null)
    {
        if ($this->aEmpresa === null && ($this->empresa_id !== null)) {
            $this->aEmpresa = EmpresaQuery::create()->findPk($this->empresa_id, $con);
            /* The following can be used additionally to
                guarantee the related object contains a reference
                to this object.  This level of coupling may, however, be
                undesirable since it could result in an only partially populated collection
                in the referenced object.
                $this->aEmpresa->addOrdenVendedors($this);
             */
        }

        return $this->aEmpresa;
    }

    /**
     * Declares an association between this object and a Vendedor object.
     *
     * @param             Vendedor $v
     * @return OrdenVendedor The current object (for fluent API support)
     * @throws PropelException
     */
    public function setVendedor(Vendedor $v = null)
    {
        if ($v === null) {
            $this->setVendedorId(NULL);
        } else {
            $this->setVendedorId($v->getId());
        }

        $this->aVendedor = $v;

        // Add binding for other direction of this n:n relationship.
        // If this object has already been added to the Vendedor object, it will not be re-added.
        if ($v !== null) {
            $v->addOrdenVendedor($this);
        }


        return $this;
    }


    /**
     * Get the associated Vendedor object
     *
     * @param PropelPDO $con Optional Connection object.
     * @return Vendedor The associated Vendedor object.
     * @throws PropelException
     */
    public function getVendedor(PropelPDO $con = null)
    {
        if ($this->aVendedor === null && ($this->vendedor_id !== null)) {
            $this->aVendedor = VendedorQuery::create()->findPk($this->vendedor_id, $con);
            /* The following can be used additionally to
                guarantee the related object contains a reference
                to this object.  This level of coupling may, however, be
                undesirable since it could result in an only partially populated collection
                in the referenced object.
                $this->aVendedor->addOrdenVendedors($this);
             */
        }

        return $this->aVendedor;
    }


    /**
     * Initializes a collection based on the name of a relation.
     * Avoids crafting an 'init[$relationName]s' method name
     * that wouldn't work when StandardEnglishPluralizer is used.
     *
     * @param string $relationName The name of the relation to initialize
     * @return void
     */
    public function initRelation($relationName)
    {
        if ('VendedorProducto' == $relationName) {
            $this->initVendedorProductos();
        }
        if ('OrdenUbicacion' == $relationName) {
            $this->initOrdenUbicacions();
        }
    }

    /**
     * Clears out the collVendedorProductos collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return void
     * @see        addVendedorProductos()
     */
    public function clearVendedorProductos()
    {
        $this->collVendedorProductos = null; // important to set this to null since that means it is uninitialized
        $this->collVendedorProductosPartial = null;
    }

    /**
     * reset is the collVendedorProductos collection loaded partially
     *
     * @return void
     */
    public function resetPartialVendedorProductos($v = true)
    {
        $this->collVendedorProductosPartial = $v;
    }

    /**
     * Initializes the collVendedorProductos collection.
     *
     * By default this just sets the collVendedorProductos collection to an empty array (like clearcollVendedorProductos());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initVendedorProductos($overrideExisting = true)
    {
        if (null !== $this->collVendedorProductos && !$overrideExisting) {
            return;
        }
        $this->collVendedorProductos = new PropelObjectCollection();
        $this->collVendedorProductos->setModel('VendedorProducto');
    }

    /**
     * Gets an array of VendedorProducto objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this OrdenVendedor is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|VendedorProducto[] List of VendedorProducto objects
     * @throws PropelException
     */
    public function getVendedorProductos($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collVendedorProductosPartial && !$this->isNew();
        if (null === $this->collVendedorProductos || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collVendedorProductos) {
                // return empty collection
                $this->initVendedorProductos();
            } else {
                $collVendedorProductos = VendedorProductoQuery::create(null, $criteria)
                    ->filterByOrdenVendedor($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collVendedorProductosPartial && count($collVendedorProductos)) {
                      $this->initVendedorProductos(false);

                      foreach($collVendedorProductos as $obj) {
                        if (false == $this->collVendedorProductos->contains($obj)) {
                          $this->collVendedorProductos->append($obj);
                        }
                      }

                      $this->collVendedorProductosPartial = true;
                    }

                    return $collVendedorProductos;
                }

                if($partial && $this->collVendedorProductos) {
                    foreach($this->collVendedorProductos as $obj) {
                        if($obj->isNew()) {
                            $collVendedorProductos[] = $obj;
                        }
                    }
                }

                $this->collVendedorProductos = $collVendedorProductos;
                $this->collVendedorProductosPartial = false;
            }
        }

        return $this->collVendedorProductos;
    }

    /**
     * Sets a collection of VendedorProducto objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $vendedorProductos A Propel collection.
     * @param PropelPDO $con Optional connection object
     */
    public function setVendedorProductos(PropelCollection $vendedorProductos, PropelPDO $con = null)
    {
        $this->vendedorProductosScheduledForDeletion = $this->getVendedorProductos(new Criteria(), $con)->diff($vendedorProductos);

        foreach ($this->vendedorProductosScheduledForDeletion as $vendedorProductoRemoved) {
            $vendedorProductoRemoved->setOrdenVendedor(null);
        }

        $this->collVendedorProductos = null;
        foreach ($vendedorProductos as $vendedorProducto) {
            $this->addVendedorProducto($vendedorProducto);
        }

        $this->collVendedorProductos = $vendedorProductos;
        $this->collVendedorProductosPartial = false;
    }

    /**
     * Returns the number of related VendedorProducto objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related VendedorProducto objects.
     * @throws PropelException
     */
    public function countVendedorProductos(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collVendedorProductosPartial && !$this->isNew();
        if (null === $this->collVendedorProductos || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collVendedorProductos) {
                return 0;
            } else {
                if($partial && !$criteria) {
                    return count($this->getVendedorProductos());
                }
                $query = VendedorProductoQuery::create(null, $criteria);
                if ($distinct) {
                    $query->distinct();
                }

                return $query
                    ->filterByOrdenVendedor($this)
                    ->count($con);
            }
        } else {
            return count($this->collVendedorProductos);
        }
    }

    /**
     * Method called to associate a VendedorProducto object to this object
     * through the VendedorProducto foreign key attribute.
     *
     * @param    VendedorProducto $l VendedorProducto
     * @return OrdenVendedor The current object (for fluent API support)
     */
    public function addVendedorProducto(VendedorProducto $l)
    {
        if ($this->collVendedorProductos === null) {
            $this->initVendedorProductos();
            $this->collVendedorProductosPartial = true;
        }
        if (!$this->collVendedorProductos->contains($l)) { // only add it if the **same** object is not already associated
            $this->doAddVendedorProducto($l);
        }

        return $this;
    }

    /**
     * @param	VendedorProducto $vendedorProducto The vendedorProducto object to add.
     */
    protected function doAddVendedorProducto($vendedorProducto)
    {
        $this->collVendedorProductos[]= $vendedorProducto;
        $vendedorProducto->setOrdenVendedor($this);
    }

    /**
     * @param	VendedorProducto $vendedorProducto The vendedorProducto object to remove.
     */
    public function removeVendedorProducto($vendedorProducto)
    {
        if ($this->getVendedorProductos()->contains($vendedorProducto)) {
            $this->collVendedorProductos->remove($this->collVendedorProductos->search($vendedorProducto));
            if (null === $this->vendedorProductosScheduledForDeletion) {
                $this->vendedorProductosScheduledForDeletion = clone $this->collVendedorProductos;
                $this->vendedorProductosScheduledForDeletion->clear();
            }
            $this->vendedorProductosScheduledForDeletion[]= $vendedorProducto;
            $vendedorProducto->setOrdenVendedor(null);
        }
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this OrdenVendedor is new, it will return
     * an empty collection; or if this OrdenVendedor has previously
     * been saved, it will retrieve related VendedorProductos from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in OrdenVendedor.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|VendedorProducto[] List of VendedorProducto objects
     */
    public function getVendedorProductosJoinVendedor($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = VendedorProductoQuery::create(null, $criteria);
        $query->joinWith('Vendedor', $join_behavior);

        return $this->getVendedorProductos($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this OrdenVendedor is new, it will return
     * an empty collection; or if this OrdenVendedor has previously
     * been saved, it will retrieve related VendedorProductos from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in OrdenVendedor.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|VendedorProducto[] List of VendedorProducto objects
     */
    public function getVendedorProductosJoinProducto($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = VendedorProductoQuery::create(null, $criteria);
        $query->joinWith('Producto', $join_behavior);

        return $this->getVendedorProductos($query, $con);
    }

    /**
     * Clears out the collOrdenUbicacions collection
     *
     * This does not modify the database; however, it will remove any associated objects, causing
     * them to be refetched by subsequent calls to accessor method.
     *
     * @return void
     * @see        addOrdenUbicacions()
     */
    public function clearOrdenUbicacions()
    {
        $this->collOrdenUbicacions = null; // important to set this to null since that means it is uninitialized
        $this->collOrdenUbicacionsPartial = null;
    }

    /**
     * reset is the collOrdenUbicacions collection loaded partially
     *
     * @return void
     */
    public function resetPartialOrdenUbicacions($v = true)
    {
        $this->collOrdenUbicacionsPartial = $v;
    }

    /**
     * Initializes the collOrdenUbicacions collection.
     *
     * By default this just sets the collOrdenUbicacions collection to an empty array (like clearcollOrdenUbicacions());
     * however, you may wish to override this method in your stub class to provide setting appropriate
     * to your application -- for example, setting the initial array to the values stored in database.
     *
     * @param boolean $overrideExisting If set to true, the method call initializes
     *                                        the collection even if it is not empty
     *
     * @return void
     */
    public function initOrdenUbicacions($overrideExisting = true)
    {
        if (null !== $this->collOrdenUbicacions && !$overrideExisting) {
            return;
        }
        $this->collOrdenUbicacions = new PropelObjectCollection();
        $this->collOrdenUbicacions->setModel('OrdenUbicacion');
    }

    /**
     * Gets an array of OrdenUbicacion objects which contain a foreign key that references this object.
     *
     * If the $criteria is not null, it is used to always fetch the results from the database.
     * Otherwise the results are fetched from the database the first time, then cached.
     * Next time the same method is called without $criteria, the cached collection is returned.
     * If this OrdenVendedor is new, it will return
     * an empty collection or the current collection; the criteria is ignored on a new object.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @return PropelObjectCollection|OrdenUbicacion[] List of OrdenUbicacion objects
     * @throws PropelException
     */
    public function getOrdenUbicacions($criteria = null, PropelPDO $con = null)
    {
        $partial = $this->collOrdenUbicacionsPartial && !$this->isNew();
        if (null === $this->collOrdenUbicacions || null !== $criteria  || $partial) {
            if ($this->isNew() && null === $this->collOrdenUbicacions) {
                // return empty collection
                $this->initOrdenUbicacions();
            } else {
                $collOrdenUbicacions = OrdenUbicacionQuery::create(null, $criteria)
                    ->filterByOrdenVendedor($this)
                    ->find($con);
                if (null !== $criteria) {
                    if (false !== $this->collOrdenUbicacionsPartial && count($collOrdenUbicacions)) {
                      $this->initOrdenUbicacions(false);

                      foreach($collOrdenUbicacions as $obj) {
                        if (false == $this->collOrdenUbicacions->contains($obj)) {
                          $this->collOrdenUbicacions->append($obj);
                        }
                      }

                      $this->collOrdenUbicacionsPartial = true;
                    }

                    return $collOrdenUbicacions;
                }

                if($partial && $this->collOrdenUbicacions) {
                    foreach($this->collOrdenUbicacions as $obj) {
                        if($obj->isNew()) {
                            $collOrdenUbicacions[] = $obj;
                        }
                    }
                }

                $this->collOrdenUbicacions = $collOrdenUbicacions;
                $this->collOrdenUbicacionsPartial = false;
            }
        }

        return $this->collOrdenUbicacions;
    }

    /**
     * Sets a collection of OrdenUbicacion objects related by a one-to-many relationship
     * to the current object.
     * It will also schedule objects for deletion based on a diff between old objects (aka persisted)
     * and new objects from the given Propel collection.
     *
     * @param PropelCollection $ordenUbicacions A Propel collection.
     * @param PropelPDO $con Optional connection object
     */
    public function setOrdenUbicacions(PropelCollection $ordenUbicacions, PropelPDO $con = null)
    {
        $this->ordenUbicacionsScheduledForDeletion = $this->getOrdenUbicacions(new Criteria(), $con)->diff($ordenUbicacions);

        foreach ($this->ordenUbicacionsScheduledForDeletion as $ordenUbicacionRemoved) {
            $ordenUbicacionRemoved->setOrdenVendedor(null);
        }

        $this->collOrdenUbicacions = null;
        foreach ($ordenUbicacions as $ordenUbicacion) {
            $this->addOrdenUbicacion($ordenUbicacion);
        }

        $this->collOrdenUbicacions = $ordenUbicacions;
        $this->collOrdenUbicacionsPartial = false;
    }

    /**
     * Returns the number of related OrdenUbicacion objects.
     *
     * @param Criteria $criteria
     * @param boolean $distinct
     * @param PropelPDO $con
     * @return int             Count of related OrdenUbicacion objects.
     * @throws PropelException
     */
    public function countOrdenUbicacions(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
    {
        $partial = $this->collOrdenUbicacionsPartial && !$this->isNew();
        if (null === $this->collOrdenUbicacions || null !== $criteria || $partial) {
            if ($this->isNew() && null === $this->collOrdenUbicacions) {
                return 0;
            } else {
                if($partial && !$criteria) {
                    return count($this->getOrdenUbicacions());
                }
                $query = OrdenUbicacionQuery::create(null, $criteria);
                if ($distinct) {
                    $query->distinct();
                }

                return $query
                    ->filterByOrdenVendedor($this)
                    ->count($con);
            }
        } else {
            return count($this->collOrdenUbicacions);
        }
    }

    /**
     * Method called to associate a OrdenUbicacion object to this object
     * through the OrdenUbicacion foreign key attribute.
     *
     * @param    OrdenUbicacion $l OrdenUbicacion
     * @return OrdenVendedor The current object (for fluent API support)
     */
    public function addOrdenUbicacion(OrdenUbicacion $l)
    {
        if ($this->collOrdenUbicacions === null) {
            $this->initOrdenUbicacions();
            $this->collOrdenUbicacionsPartial = true;
        }
        if (!$this->collOrdenUbicacions->contains($l)) { // only add it if the **same** object is not already associated
            $this->doAddOrdenUbicacion($l);
        }

        return $this;
    }

    /**
     * @param	OrdenUbicacion $ordenUbicacion The ordenUbicacion object to add.
     */
    protected function doAddOrdenUbicacion($ordenUbicacion)
    {
        $this->collOrdenUbicacions[]= $ordenUbicacion;
        $ordenUbicacion->setOrdenVendedor($this);
    }

    /**
     * @param	OrdenUbicacion $ordenUbicacion The ordenUbicacion object to remove.
     */
    public function removeOrdenUbicacion($ordenUbicacion)
    {
        if ($this->getOrdenUbicacions()->contains($ordenUbicacion)) {
            $this->collOrdenUbicacions->remove($this->collOrdenUbicacions->search($ordenUbicacion));
            if (null === $this->ordenUbicacionsScheduledForDeletion) {
                $this->ordenUbicacionsScheduledForDeletion = clone $this->collOrdenUbicacions;
                $this->ordenUbicacionsScheduledForDeletion->clear();
            }
            $this->ordenUbicacionsScheduledForDeletion[]= $ordenUbicacion;
            $ordenUbicacion->setOrdenVendedor(null);
        }
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this OrdenVendedor is new, it will return
     * an empty collection; or if this OrdenVendedor has previously
     * been saved, it will retrieve related OrdenUbicacions from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in OrdenVendedor.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|OrdenUbicacion[] List of OrdenUbicacion objects
     */
    public function getOrdenUbicacionsJoinTienda($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = OrdenUbicacionQuery::create(null, $criteria);
        $query->joinWith('Tienda', $join_behavior);

        return $this->getOrdenUbicacions($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this OrdenVendedor is new, it will return
     * an empty collection; or if this OrdenVendedor has previously
     * been saved, it will retrieve related OrdenUbicacions from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in OrdenVendedor.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|OrdenUbicacion[] List of OrdenUbicacion objects
     */
    public function getOrdenUbicacionsJoinProducto($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = OrdenUbicacionQuery::create(null, $criteria);
        $query->joinWith('Producto', $join_behavior);

        return $this->getOrdenUbicacions($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this OrdenVendedor is new, it will return
     * an empty collection; or if this OrdenVendedor has previously
     * been saved, it will retrieve related OrdenUbicacions from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in OrdenVendedor.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|OrdenUbicacion[] List of OrdenUbicacion objects
     */
    public function getOrdenUbicacionsJoinOrdenCotizacion($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = OrdenUbicacionQuery::create(null, $criteria);
        $query->joinWith('OrdenCotizacion', $join_behavior);

        return $this->getOrdenUbicacions($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this OrdenVendedor is new, it will return
     * an empty collection; or if this OrdenVendedor has previously
     * been saved, it will retrieve related OrdenUbicacions from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in OrdenVendedor.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|OrdenUbicacion[] List of OrdenUbicacion objects
     */
    public function getOrdenUbicacionsJoinVendedorProducto($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = OrdenUbicacionQuery::create(null, $criteria);
        $query->joinWith('VendedorProducto', $join_behavior);

        return $this->getOrdenUbicacions($query, $con);
    }


    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this OrdenVendedor is new, it will return
     * an empty collection; or if this OrdenVendedor has previously
     * been saved, it will retrieve related OrdenUbicacions from storage.
     *
     * This method is protected by default in order to keep the public
     * api reasonable.  You can provide public methods for those you
     * actually need in OrdenVendedor.
     *
     * @param Criteria $criteria optional Criteria object to narrow the query
     * @param PropelPDO $con optional connection object
     * @param string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
     * @return PropelObjectCollection|OrdenUbicacion[] List of OrdenUbicacion objects
     */
    public function getOrdenUbicacionsJoinSalidaProducto($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
    {
        $query = OrdenUbicacionQuery::create(null, $criteria);
        $query->joinWith('SalidaProducto', $join_behavior);

        return $this->getOrdenUbicacions($query, $con);
    }

    /**
     * Clears the current object and sets all attributes to their default values
     */
    public function clear()
    {
        $this->id = null;
        $this->observaciones = null;
        $this->estado = null;
        $this->fecha = null;
        $this->usuario = null;
        $this->empresa_id = null;
        $this->vendedor_id = null;
        $this->alreadyInSave = false;
        $this->alreadyInValidation = false;
        $this->clearAllReferences();
        $this->resetModified();
        $this->setNew(true);
        $this->setDeleted(false);
    }

    /**
     * Resets all references to other model objects or collections of model objects.
     *
     * This method is a user-space workaround for PHP's inability to garbage collect
     * objects with circular references (even in PHP 5.3). This is currently necessary
     * when using Propel in certain daemon or large-volumne/high-memory operations.
     *
     * @param boolean $deep Whether to also clear the references on all referrer objects.
     */
    public function clearAllReferences($deep = false)
    {
        if ($deep) {
            if ($this->collVendedorProductos) {
                foreach ($this->collVendedorProductos as $o) {
                    $o->clearAllReferences($deep);
                }
            }
            if ($this->collOrdenUbicacions) {
                foreach ($this->collOrdenUbicacions as $o) {
                    $o->clearAllReferences($deep);
                }
            }
        } // if ($deep)

        if ($this->collVendedorProductos instanceof PropelCollection) {
            $this->collVendedorProductos->clearIterator();
        }
        $this->collVendedorProductos = null;
        if ($this->collOrdenUbicacions instanceof PropelCollection) {
            $this->collOrdenUbicacions->clearIterator();
        }
        $this->collOrdenUbicacions = null;
        $this->aEmpresa = null;
        $this->aVendedor = null;
    }

    /**
     * return the string representation of this object
     *
     * @return string
     */
    public function __toString()
    {
        return (string) $this->exportTo(OrdenVendedorPeer::DEFAULT_STRING_FORMAT);
    }

    /**
     * return true is the object is in saving state
     *
     * @return boolean
     */
    public function isAlreadyInSave()
    {
        return $this->alreadyInSave;
    }

    /**
     * Catches calls to virtual methods
     */
    public function __call($name, $params)
    {

        // symfony_behaviors behavior
        if ($callable = sfMixer::getCallable('BaseOrdenVendedor:' . $name))
        {
          array_unshift($params, $this);
          return call_user_func_array($callable, $params);
        }


        return parent::__call($name, $params);
    }

}
