<?php


/**
 * Base class that represents a query for the 'perfil_menu' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.6.7 on:
 *
 * 12/30/25 22:58:26
 *
 * @method PerfilMenuQuery orderById($order = Criteria::ASC) Order by the id column
 * @method PerfilMenuQuery orderByPerfilId($order = Criteria::ASC) Order by the perfil_id column
 * @method PerfilMenuQuery orderByMenuSeguridadId($order = Criteria::ASC) Order by the menu_seguridad_id column
 *
 * @method PerfilMenuQuery groupById() Group by the id column
 * @method PerfilMenuQuery groupByPerfilId() Group by the perfil_id column
 * @method PerfilMenuQuery groupByMenuSeguridadId() Group by the menu_seguridad_id column
 *
 * @method PerfilMenuQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method PerfilMenuQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method PerfilMenuQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method PerfilMenuQuery leftJoinPerfil($relationAlias = null) Adds a LEFT JOIN clause to the query using the Perfil relation
 * @method PerfilMenuQuery rightJoinPerfil($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Perfil relation
 * @method PerfilMenuQuery innerJoinPerfil($relationAlias = null) Adds a INNER JOIN clause to the query using the Perfil relation
 *
 * @method PerfilMenuQuery leftJoinMenuSeguridad($relationAlias = null) Adds a LEFT JOIN clause to the query using the MenuSeguridad relation
 * @method PerfilMenuQuery rightJoinMenuSeguridad($relationAlias = null) Adds a RIGHT JOIN clause to the query using the MenuSeguridad relation
 * @method PerfilMenuQuery innerJoinMenuSeguridad($relationAlias = null) Adds a INNER JOIN clause to the query using the MenuSeguridad relation
 *
 * @method PerfilMenu findOne(PropelPDO $con = null) Return the first PerfilMenu matching the query
 * @method PerfilMenu findOneOrCreate(PropelPDO $con = null) Return the first PerfilMenu matching the query, or a new PerfilMenu object populated from the query conditions when no match is found
 *
 * @method PerfilMenu findOneById(int $id) Return the first PerfilMenu filtered by the id column
 * @method PerfilMenu findOneByPerfilId(int $perfil_id) Return the first PerfilMenu filtered by the perfil_id column
 * @method PerfilMenu findOneByMenuSeguridadId(int $menu_seguridad_id) Return the first PerfilMenu filtered by the menu_seguridad_id column
 *
 * @method array findById(int $id) Return PerfilMenu objects filtered by the id column
 * @method array findByPerfilId(int $perfil_id) Return PerfilMenu objects filtered by the perfil_id column
 * @method array findByMenuSeguridadId(int $menu_seguridad_id) Return PerfilMenu objects filtered by the menu_seguridad_id column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BasePerfilMenuQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BasePerfilMenuQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = 'propel', $modelName = 'PerfilMenu', $modelAlias = null)
    {
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new PerfilMenuQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param     PerfilMenuQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return PerfilMenuQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof PerfilMenuQuery) {
            return $criteria;
        }
        $query = new PerfilMenuQuery();
        if (null !== $modelAlias) {
            $query->setModelAlias($modelAlias);
        }
        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return   PerfilMenu|PerfilMenu[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = PerfilMenuPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is alredy in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(PerfilMenuPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return   PerfilMenu A model object, or null if the key is not found
     * @throws   PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT `ID`, `PERFIL_ID`, `MENU_SEGURIDAD_ID` FROM `perfil_menu` WHERE `ID` = :p0';
        try {
            $stmt = $con->prepare($sql);
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new PerfilMenu();
            $obj->hydrate($row);
            PerfilMenuPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return PerfilMenu|PerfilMenu[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|PerfilMenu[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return PerfilMenuQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(PerfilMenuPeer::ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return PerfilMenuQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(PerfilMenuPeer::ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the id column
     *
     * Example usage:
     * <code>
     * $query->filterById(1234); // WHERE id = 1234
     * $query->filterById(array(12, 34)); // WHERE id IN (12, 34)
     * $query->filterById(array('min' => 12)); // WHERE id > 12
     * </code>
     *
     * @param     mixed $id The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return PerfilMenuQuery The current query, for fluid interface
     */
    public function filterById($id = null, $comparison = null)
    {
        if (is_array($id) && null === $comparison) {
            $comparison = Criteria::IN;
        }

        return $this->addUsingAlias(PerfilMenuPeer::ID, $id, $comparison);
    }

    /**
     * Filter the query on the perfil_id column
     *
     * Example usage:
     * <code>
     * $query->filterByPerfilId(1234); // WHERE perfil_id = 1234
     * $query->filterByPerfilId(array(12, 34)); // WHERE perfil_id IN (12, 34)
     * $query->filterByPerfilId(array('min' => 12)); // WHERE perfil_id > 12
     * </code>
     *
     * @see       filterByPerfil()
     *
     * @param     mixed $perfilId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return PerfilMenuQuery The current query, for fluid interface
     */
    public function filterByPerfilId($perfilId = null, $comparison = null)
    {
        if (is_array($perfilId)) {
            $useMinMax = false;
            if (isset($perfilId['min'])) {
                $this->addUsingAlias(PerfilMenuPeer::PERFIL_ID, $perfilId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($perfilId['max'])) {
                $this->addUsingAlias(PerfilMenuPeer::PERFIL_ID, $perfilId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(PerfilMenuPeer::PERFIL_ID, $perfilId, $comparison);
    }

    /**
     * Filter the query on the menu_seguridad_id column
     *
     * Example usage:
     * <code>
     * $query->filterByMenuSeguridadId(1234); // WHERE menu_seguridad_id = 1234
     * $query->filterByMenuSeguridadId(array(12, 34)); // WHERE menu_seguridad_id IN (12, 34)
     * $query->filterByMenuSeguridadId(array('min' => 12)); // WHERE menu_seguridad_id > 12
     * </code>
     *
     * @see       filterByMenuSeguridad()
     *
     * @param     mixed $menuSeguridadId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return PerfilMenuQuery The current query, for fluid interface
     */
    public function filterByMenuSeguridadId($menuSeguridadId = null, $comparison = null)
    {
        if (is_array($menuSeguridadId)) {
            $useMinMax = false;
            if (isset($menuSeguridadId['min'])) {
                $this->addUsingAlias(PerfilMenuPeer::MENU_SEGURIDAD_ID, $menuSeguridadId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($menuSeguridadId['max'])) {
                $this->addUsingAlias(PerfilMenuPeer::MENU_SEGURIDAD_ID, $menuSeguridadId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(PerfilMenuPeer::MENU_SEGURIDAD_ID, $menuSeguridadId, $comparison);
    }

    /**
     * Filter the query by a related Perfil object
     *
     * @param   Perfil|PropelObjectCollection $perfil The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   PerfilMenuQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByPerfil($perfil, $comparison = null)
    {
        if ($perfil instanceof Perfil) {
            return $this
                ->addUsingAlias(PerfilMenuPeer::PERFIL_ID, $perfil->getId(), $comparison);
        } elseif ($perfil instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(PerfilMenuPeer::PERFIL_ID, $perfil->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByPerfil() only accepts arguments of type Perfil or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Perfil relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return PerfilMenuQuery The current query, for fluid interface
     */
    public function joinPerfil($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Perfil');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Perfil');
        }

        return $this;
    }

    /**
     * Use the Perfil relation Perfil object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   PerfilQuery A secondary query class using the current class as primary query
     */
    public function usePerfilQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinPerfil($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Perfil', 'PerfilQuery');
    }

    /**
     * Filter the query by a related MenuSeguridad object
     *
     * @param   MenuSeguridad|PropelObjectCollection $menuSeguridad The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   PerfilMenuQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByMenuSeguridad($menuSeguridad, $comparison = null)
    {
        if ($menuSeguridad instanceof MenuSeguridad) {
            return $this
                ->addUsingAlias(PerfilMenuPeer::MENU_SEGURIDAD_ID, $menuSeguridad->getId(), $comparison);
        } elseif ($menuSeguridad instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(PerfilMenuPeer::MENU_SEGURIDAD_ID, $menuSeguridad->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByMenuSeguridad() only accepts arguments of type MenuSeguridad or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the MenuSeguridad relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return PerfilMenuQuery The current query, for fluid interface
     */
    public function joinMenuSeguridad($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('MenuSeguridad');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'MenuSeguridad');
        }

        return $this;
    }

    /**
     * Use the MenuSeguridad relation MenuSeguridad object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   MenuSeguridadQuery A secondary query class using the current class as primary query
     */
    public function useMenuSeguridadQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinMenuSeguridad($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'MenuSeguridad', 'MenuSeguridadQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   PerfilMenu $perfilMenu Object to remove from the list of results
     *
     * @return PerfilMenuQuery The current query, for fluid interface
     */
    public function prune($perfilMenu = null)
    {
        if ($perfilMenu) {
            $this->addUsingAlias(PerfilMenuPeer::ID, $perfilMenu->getId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}
