<?php


/**
 * Base class that represents a query for the 'tienda' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.6.7 on:
 *
 * 12/30/25 22:58:27
 *
 * @method TiendaQuery orderById($order = Criteria::ASC) Order by the id column
 * @method TiendaQuery orderByEmpresaId($order = Criteria::ASC) Order by the empresa_id column
 * @method TiendaQuery orderByCodigo($order = Criteria::ASC) Order by the codigo column
 * @method TiendaQuery orderByNombre($order = Criteria::ASC) Order by the nombre column
 * @method TiendaQuery orderByCodigoEstablecimiento($order = Criteria::ASC) Order by the codigo_establecimiento column
 * @method TiendaQuery orderByDireccion($order = Criteria::ASC) Order by the direccion column
 * @method TiendaQuery orderByDepartamentoId($order = Criteria::ASC) Order by the departamento_id column
 * @method TiendaQuery orderByMunicipioId($order = Criteria::ASC) Order by the municipio_id column
 * @method TiendaQuery orderByObservaciones($order = Criteria::ASC) Order by the observaciones column
 * @method TiendaQuery orderByCorreo($order = Criteria::ASC) Order by the correo column
 * @method TiendaQuery orderByTelefono($order = Criteria::ASC) Order by the telefono column
 * @method TiendaQuery orderByNombreComercial($order = Criteria::ASC) Order by the nombre_comercial column
 * @method TiendaQuery orderByActivo($order = Criteria::ASC) Order by the activo column
 * @method TiendaQuery orderByCuentaDebe($order = Criteria::ASC) Order by the cuenta_debe column
 * @method TiendaQuery orderByCuentaHaber($order = Criteria::ASC) Order by the cuenta_haber column
 * @method TiendaQuery orderByCreatedBy($order = Criteria::ASC) Order by the created_by column
 * @method TiendaQuery orderByUpdatedBy($order = Criteria::ASC) Order by the updated_by column
 * @method TiendaQuery orderByCreatedAt($order = Criteria::ASC) Order by the created_at column
 * @method TiendaQuery orderByUpdatedAt($order = Criteria::ASC) Order by the updated_at column
 * @method TiendaQuery orderByTipo($order = Criteria::ASC) Order by the tipo column
 * @method TiendaQuery orderByNit($order = Criteria::ASC) Order by the nit column
 * @method TiendaQuery orderByFeelUsuario($order = Criteria::ASC) Order by the feel_usuario column
 * @method TiendaQuery orderByFeelToken($order = Criteria::ASC) Order by the feel_token column
 * @method TiendaQuery orderByFeelLlave($order = Criteria::ASC) Order by the feel_llave column
 * @method TiendaQuery orderByActivaBuscador($order = Criteria::ASC) Order by the activa_buscador column
 *
 * @method TiendaQuery groupById() Group by the id column
 * @method TiendaQuery groupByEmpresaId() Group by the empresa_id column
 * @method TiendaQuery groupByCodigo() Group by the codigo column
 * @method TiendaQuery groupByNombre() Group by the nombre column
 * @method TiendaQuery groupByCodigoEstablecimiento() Group by the codigo_establecimiento column
 * @method TiendaQuery groupByDireccion() Group by the direccion column
 * @method TiendaQuery groupByDepartamentoId() Group by the departamento_id column
 * @method TiendaQuery groupByMunicipioId() Group by the municipio_id column
 * @method TiendaQuery groupByObservaciones() Group by the observaciones column
 * @method TiendaQuery groupByCorreo() Group by the correo column
 * @method TiendaQuery groupByTelefono() Group by the telefono column
 * @method TiendaQuery groupByNombreComercial() Group by the nombre_comercial column
 * @method TiendaQuery groupByActivo() Group by the activo column
 * @method TiendaQuery groupByCuentaDebe() Group by the cuenta_debe column
 * @method TiendaQuery groupByCuentaHaber() Group by the cuenta_haber column
 * @method TiendaQuery groupByCreatedBy() Group by the created_by column
 * @method TiendaQuery groupByUpdatedBy() Group by the updated_by column
 * @method TiendaQuery groupByCreatedAt() Group by the created_at column
 * @method TiendaQuery groupByUpdatedAt() Group by the updated_at column
 * @method TiendaQuery groupByTipo() Group by the tipo column
 * @method TiendaQuery groupByNit() Group by the nit column
 * @method TiendaQuery groupByFeelUsuario() Group by the feel_usuario column
 * @method TiendaQuery groupByFeelToken() Group by the feel_token column
 * @method TiendaQuery groupByFeelLlave() Group by the feel_llave column
 * @method TiendaQuery groupByActivaBuscador() Group by the activa_buscador column
 *
 * @method TiendaQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method TiendaQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method TiendaQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method TiendaQuery leftJoinEmpresa($relationAlias = null) Adds a LEFT JOIN clause to the query using the Empresa relation
 * @method TiendaQuery rightJoinEmpresa($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Empresa relation
 * @method TiendaQuery innerJoinEmpresa($relationAlias = null) Adds a INNER JOIN clause to the query using the Empresa relation
 *
 * @method TiendaQuery leftJoinDepartamento($relationAlias = null) Adds a LEFT JOIN clause to the query using the Departamento relation
 * @method TiendaQuery rightJoinDepartamento($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Departamento relation
 * @method TiendaQuery innerJoinDepartamento($relationAlias = null) Adds a INNER JOIN clause to the query using the Departamento relation
 *
 * @method TiendaQuery leftJoinMunicipio($relationAlias = null) Adds a LEFT JOIN clause to the query using the Municipio relation
 * @method TiendaQuery rightJoinMunicipio($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Municipio relation
 * @method TiendaQuery innerJoinMunicipio($relationAlias = null) Adds a INNER JOIN clause to the query using the Municipio relation
 *
 * @method TiendaQuery leftJoinUsuario($relationAlias = null) Adds a LEFT JOIN clause to the query using the Usuario relation
 * @method TiendaQuery rightJoinUsuario($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Usuario relation
 * @method TiendaQuery innerJoinUsuario($relationAlias = null) Adds a INNER JOIN clause to the query using the Usuario relation
 *
 * @method TiendaQuery leftJoinUsuarioTienda($relationAlias = null) Adds a LEFT JOIN clause to the query using the UsuarioTienda relation
 * @method TiendaQuery rightJoinUsuarioTienda($relationAlias = null) Adds a RIGHT JOIN clause to the query using the UsuarioTienda relation
 * @method TiendaQuery innerJoinUsuarioTienda($relationAlias = null) Adds a INNER JOIN clause to the query using the UsuarioTienda relation
 *
 * @method TiendaQuery leftJoinOperacion($relationAlias = null) Adds a LEFT JOIN clause to the query using the Operacion relation
 * @method TiendaQuery rightJoinOperacion($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Operacion relation
 * @method TiendaQuery innerJoinOperacion($relationAlias = null) Adds a INNER JOIN clause to the query using the Operacion relation
 *
 * @method TiendaQuery leftJoinOrdenProveedor($relationAlias = null) Adds a LEFT JOIN clause to the query using the OrdenProveedor relation
 * @method TiendaQuery rightJoinOrdenProveedor($relationAlias = null) Adds a RIGHT JOIN clause to the query using the OrdenProveedor relation
 * @method TiendaQuery innerJoinOrdenProveedor($relationAlias = null) Adds a INNER JOIN clause to the query using the OrdenProveedor relation
 *
 * @method TiendaQuery leftJoinMovimientoBanco($relationAlias = null) Adds a LEFT JOIN clause to the query using the MovimientoBanco relation
 * @method TiendaQuery rightJoinMovimientoBanco($relationAlias = null) Adds a RIGHT JOIN clause to the query using the MovimientoBanco relation
 * @method TiendaQuery innerJoinMovimientoBanco($relationAlias = null) Adds a INNER JOIN clause to the query using the MovimientoBanco relation
 *
 * @method TiendaQuery leftJoinPartida($relationAlias = null) Adds a LEFT JOIN clause to the query using the Partida relation
 * @method TiendaQuery rightJoinPartida($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Partida relation
 * @method TiendaQuery innerJoinPartida($relationAlias = null) Adds a INNER JOIN clause to the query using the Partida relation
 *
 * @method TiendaQuery leftJoinIngresoProductoPro($relationAlias = null) Adds a LEFT JOIN clause to the query using the IngresoProductoPro relation
 * @method TiendaQuery rightJoinIngresoProductoPro($relationAlias = null) Adds a RIGHT JOIN clause to the query using the IngresoProductoPro relation
 * @method TiendaQuery innerJoinIngresoProductoPro($relationAlias = null) Adds a INNER JOIN clause to the query using the IngresoProductoPro relation
 *
 * @method TiendaQuery leftJoinSalidaProducto($relationAlias = null) Adds a LEFT JOIN clause to the query using the SalidaProducto relation
 * @method TiendaQuery rightJoinSalidaProducto($relationAlias = null) Adds a RIGHT JOIN clause to the query using the SalidaProducto relation
 * @method TiendaQuery innerJoinSalidaProducto($relationAlias = null) Adds a INNER JOIN clause to the query using the SalidaProducto relation
 *
 * @method TiendaQuery leftJoinCierreCaja($relationAlias = null) Adds a LEFT JOIN clause to the query using the CierreCaja relation
 * @method TiendaQuery rightJoinCierreCaja($relationAlias = null) Adds a RIGHT JOIN clause to the query using the CierreCaja relation
 * @method TiendaQuery innerJoinCierreCaja($relationAlias = null) Adds a INNER JOIN clause to the query using the CierreCaja relation
 *
 * @method TiendaQuery leftJoinCierre($relationAlias = null) Adds a LEFT JOIN clause to the query using the Cierre relation
 * @method TiendaQuery rightJoinCierre($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Cierre relation
 * @method TiendaQuery innerJoinCierre($relationAlias = null) Adds a INNER JOIN clause to the query using the Cierre relation
 *
 * @method TiendaQuery leftJoinCampoUsuario($relationAlias = null) Adds a LEFT JOIN clause to the query using the CampoUsuario relation
 * @method TiendaQuery rightJoinCampoUsuario($relationAlias = null) Adds a RIGHT JOIN clause to the query using the CampoUsuario relation
 * @method TiendaQuery innerJoinCampoUsuario($relationAlias = null) Adds a INNER JOIN clause to the query using the CampoUsuario relation
 *
 * @method TiendaQuery leftJoinGasto($relationAlias = null) Adds a LEFT JOIN clause to the query using the Gasto relation
 * @method TiendaQuery rightJoinGasto($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Gasto relation
 * @method TiendaQuery innerJoinGasto($relationAlias = null) Adds a INNER JOIN clause to the query using the Gasto relation
 *
 * @method TiendaQuery leftJoinProductoExistencia($relationAlias = null) Adds a LEFT JOIN clause to the query using the ProductoExistencia relation
 * @method TiendaQuery rightJoinProductoExistencia($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ProductoExistencia relation
 * @method TiendaQuery innerJoinProductoExistencia($relationAlias = null) Adds a INNER JOIN clause to the query using the ProductoExistencia relation
 *
 * @method TiendaQuery leftJoinProductoUbicacion($relationAlias = null) Adds a LEFT JOIN clause to the query using the ProductoUbicacion relation
 * @method TiendaQuery rightJoinProductoUbicacion($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ProductoUbicacion relation
 * @method TiendaQuery innerJoinProductoUbicacion($relationAlias = null) Adds a INNER JOIN clause to the query using the ProductoUbicacion relation
 *
 * @method TiendaQuery leftJoinIngresoProducto($relationAlias = null) Adds a LEFT JOIN clause to the query using the IngresoProducto relation
 * @method TiendaQuery rightJoinIngresoProducto($relationAlias = null) Adds a RIGHT JOIN clause to the query using the IngresoProducto relation
 * @method TiendaQuery innerJoinIngresoProducto($relationAlias = null) Adds a INNER JOIN clause to the query using the IngresoProducto relation
 *
 * @method TiendaQuery leftJoinProductoMovimiento($relationAlias = null) Adds a LEFT JOIN clause to the query using the ProductoMovimiento relation
 * @method TiendaQuery rightJoinProductoMovimiento($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ProductoMovimiento relation
 * @method TiendaQuery innerJoinProductoMovimiento($relationAlias = null) Adds a INNER JOIN clause to the query using the ProductoMovimiento relation
 *
 * @method TiendaQuery leftJoinPrecioProducto($relationAlias = null) Adds a LEFT JOIN clause to the query using the PrecioProducto relation
 * @method TiendaQuery rightJoinPrecioProducto($relationAlias = null) Adds a RIGHT JOIN clause to the query using the PrecioProducto relation
 * @method TiendaQuery innerJoinPrecioProducto($relationAlias = null) Adds a INNER JOIN clause to the query using the PrecioProducto relation
 *
 * @method TiendaQuery leftJoinOrdenCotizacion($relationAlias = null) Adds a LEFT JOIN clause to the query using the OrdenCotizacion relation
 * @method TiendaQuery rightJoinOrdenCotizacion($relationAlias = null) Adds a RIGHT JOIN clause to the query using the OrdenCotizacion relation
 * @method TiendaQuery innerJoinOrdenCotizacion($relationAlias = null) Adds a INNER JOIN clause to the query using the OrdenCotizacion relation
 *
 * @method TiendaQuery leftJoinVentaResumida($relationAlias = null) Adds a LEFT JOIN clause to the query using the VentaResumida relation
 * @method TiendaQuery rightJoinVentaResumida($relationAlias = null) Adds a RIGHT JOIN clause to the query using the VentaResumida relation
 * @method TiendaQuery innerJoinVentaResumida($relationAlias = null) Adds a INNER JOIN clause to the query using the VentaResumida relation
 *
 * @method TiendaQuery leftJoinOrdenDevolucion($relationAlias = null) Adds a LEFT JOIN clause to the query using the OrdenDevolucion relation
 * @method TiendaQuery rightJoinOrdenDevolucion($relationAlias = null) Adds a RIGHT JOIN clause to the query using the OrdenDevolucion relation
 * @method TiendaQuery innerJoinOrdenDevolucion($relationAlias = null) Adds a INNER JOIN clause to the query using the OrdenDevolucion relation
 *
 * @method TiendaQuery leftJoinEgresosRecibido($relationAlias = null) Adds a LEFT JOIN clause to the query using the EgresosRecibido relation
 * @method TiendaQuery rightJoinEgresosRecibido($relationAlias = null) Adds a RIGHT JOIN clause to the query using the EgresosRecibido relation
 * @method TiendaQuery innerJoinEgresosRecibido($relationAlias = null) Adds a INNER JOIN clause to the query using the EgresosRecibido relation
 *
 * @method TiendaQuery leftJoinGastoCaja($relationAlias = null) Adds a LEFT JOIN clause to the query using the GastoCaja relation
 * @method TiendaQuery rightJoinGastoCaja($relationAlias = null) Adds a RIGHT JOIN clause to the query using the GastoCaja relation
 * @method TiendaQuery innerJoinGastoCaja($relationAlias = null) Adds a INNER JOIN clause to the query using the GastoCaja relation
 *
 * @method TiendaQuery leftJoinSolicitudDevolucion($relationAlias = null) Adds a LEFT JOIN clause to the query using the SolicitudDevolucion relation
 * @method TiendaQuery rightJoinSolicitudDevolucion($relationAlias = null) Adds a RIGHT JOIN clause to the query using the SolicitudDevolucion relation
 * @method TiendaQuery innerJoinSolicitudDevolucion($relationAlias = null) Adds a INNER JOIN clause to the query using the SolicitudDevolucion relation
 *
 * @method TiendaQuery leftJoinSolicitudDeposito($relationAlias = null) Adds a LEFT JOIN clause to the query using the SolicitudDeposito relation
 * @method TiendaQuery rightJoinSolicitudDeposito($relationAlias = null) Adds a RIGHT JOIN clause to the query using the SolicitudDeposito relation
 * @method TiendaQuery innerJoinSolicitudDeposito($relationAlias = null) Adds a INNER JOIN clause to the query using the SolicitudDeposito relation
 *
 * @method TiendaQuery leftJoinAgenda($relationAlias = null) Adds a LEFT JOIN clause to the query using the Agenda relation
 * @method TiendaQuery rightJoinAgenda($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Agenda relation
 * @method TiendaQuery innerJoinAgenda($relationAlias = null) Adds a INNER JOIN clause to the query using the Agenda relation
 *
 * @method TiendaQuery leftJoinInventarioVence($relationAlias = null) Adds a LEFT JOIN clause to the query using the InventarioVence relation
 * @method TiendaQuery rightJoinInventarioVence($relationAlias = null) Adds a RIGHT JOIN clause to the query using the InventarioVence relation
 * @method TiendaQuery innerJoinInventarioVence($relationAlias = null) Adds a INNER JOIN clause to the query using the InventarioVence relation
 *
 * @method TiendaQuery leftJoinOrdenUbicacion($relationAlias = null) Adds a LEFT JOIN clause to the query using the OrdenUbicacion relation
 * @method TiendaQuery rightJoinOrdenUbicacion($relationAlias = null) Adds a RIGHT JOIN clause to the query using the OrdenUbicacion relation
 * @method TiendaQuery innerJoinOrdenUbicacion($relationAlias = null) Adds a INNER JOIN clause to the query using the OrdenUbicacion relation
 *
 * @method TiendaQuery leftJoinTrasladoUbicacion($relationAlias = null) Adds a LEFT JOIN clause to the query using the TrasladoUbicacion relation
 * @method TiendaQuery rightJoinTrasladoUbicacion($relationAlias = null) Adds a RIGHT JOIN clause to the query using the TrasladoUbicacion relation
 * @method TiendaQuery innerJoinTrasladoUbicacion($relationAlias = null) Adds a INNER JOIN clause to the query using the TrasladoUbicacion relation
 *
 * @method TiendaQuery leftJoinTrasladoUbicacionDetalle($relationAlias = null) Adds a LEFT JOIN clause to the query using the TrasladoUbicacionDetalle relation
 * @method TiendaQuery rightJoinTrasladoUbicacionDetalle($relationAlias = null) Adds a RIGHT JOIN clause to the query using the TrasladoUbicacionDetalle relation
 * @method TiendaQuery innerJoinTrasladoUbicacionDetalle($relationAlias = null) Adds a INNER JOIN clause to the query using the TrasladoUbicacionDetalle relation
 *
 * @method Tienda findOne(PropelPDO $con = null) Return the first Tienda matching the query
 * @method Tienda findOneOrCreate(PropelPDO $con = null) Return the first Tienda matching the query, or a new Tienda object populated from the query conditions when no match is found
 *
 * @method Tienda findOneById(int $id) Return the first Tienda filtered by the id column
 * @method Tienda findOneByEmpresaId(int $empresa_id) Return the first Tienda filtered by the empresa_id column
 * @method Tienda findOneByCodigo(string $codigo) Return the first Tienda filtered by the codigo column
 * @method Tienda findOneByNombre(string $nombre) Return the first Tienda filtered by the nombre column
 * @method Tienda findOneByCodigoEstablecimiento(string $codigo_establecimiento) Return the first Tienda filtered by the codigo_establecimiento column
 * @method Tienda findOneByDireccion(string $direccion) Return the first Tienda filtered by the direccion column
 * @method Tienda findOneByDepartamentoId(int $departamento_id) Return the first Tienda filtered by the departamento_id column
 * @method Tienda findOneByMunicipioId(int $municipio_id) Return the first Tienda filtered by the municipio_id column
 * @method Tienda findOneByObservaciones(string $observaciones) Return the first Tienda filtered by the observaciones column
 * @method Tienda findOneByCorreo(string $correo) Return the first Tienda filtered by the correo column
 * @method Tienda findOneByTelefono(string $telefono) Return the first Tienda filtered by the telefono column
 * @method Tienda findOneByNombreComercial(string $nombre_comercial) Return the first Tienda filtered by the nombre_comercial column
 * @method Tienda findOneByActivo(boolean $activo) Return the first Tienda filtered by the activo column
 * @method Tienda findOneByCuentaDebe(string $cuenta_debe) Return the first Tienda filtered by the cuenta_debe column
 * @method Tienda findOneByCuentaHaber(string $cuenta_haber) Return the first Tienda filtered by the cuenta_haber column
 * @method Tienda findOneByCreatedBy(string $created_by) Return the first Tienda filtered by the created_by column
 * @method Tienda findOneByUpdatedBy(string $updated_by) Return the first Tienda filtered by the updated_by column
 * @method Tienda findOneByCreatedAt(string $created_at) Return the first Tienda filtered by the created_at column
 * @method Tienda findOneByUpdatedAt(string $updated_at) Return the first Tienda filtered by the updated_at column
 * @method Tienda findOneByTipo(string $tipo) Return the first Tienda filtered by the tipo column
 * @method Tienda findOneByNit(string $nit) Return the first Tienda filtered by the nit column
 * @method Tienda findOneByFeelUsuario(string $feel_usuario) Return the first Tienda filtered by the feel_usuario column
 * @method Tienda findOneByFeelToken(string $feel_token) Return the first Tienda filtered by the feel_token column
 * @method Tienda findOneByFeelLlave(string $feel_llave) Return the first Tienda filtered by the feel_llave column
 * @method Tienda findOneByActivaBuscador(boolean $activa_buscador) Return the first Tienda filtered by the activa_buscador column
 *
 * @method array findById(int $id) Return Tienda objects filtered by the id column
 * @method array findByEmpresaId(int $empresa_id) Return Tienda objects filtered by the empresa_id column
 * @method array findByCodigo(string $codigo) Return Tienda objects filtered by the codigo column
 * @method array findByNombre(string $nombre) Return Tienda objects filtered by the nombre column
 * @method array findByCodigoEstablecimiento(string $codigo_establecimiento) Return Tienda objects filtered by the codigo_establecimiento column
 * @method array findByDireccion(string $direccion) Return Tienda objects filtered by the direccion column
 * @method array findByDepartamentoId(int $departamento_id) Return Tienda objects filtered by the departamento_id column
 * @method array findByMunicipioId(int $municipio_id) Return Tienda objects filtered by the municipio_id column
 * @method array findByObservaciones(string $observaciones) Return Tienda objects filtered by the observaciones column
 * @method array findByCorreo(string $correo) Return Tienda objects filtered by the correo column
 * @method array findByTelefono(string $telefono) Return Tienda objects filtered by the telefono column
 * @method array findByNombreComercial(string $nombre_comercial) Return Tienda objects filtered by the nombre_comercial column
 * @method array findByActivo(boolean $activo) Return Tienda objects filtered by the activo column
 * @method array findByCuentaDebe(string $cuenta_debe) Return Tienda objects filtered by the cuenta_debe column
 * @method array findByCuentaHaber(string $cuenta_haber) Return Tienda objects filtered by the cuenta_haber column
 * @method array findByCreatedBy(string $created_by) Return Tienda objects filtered by the created_by column
 * @method array findByUpdatedBy(string $updated_by) Return Tienda objects filtered by the updated_by column
 * @method array findByCreatedAt(string $created_at) Return Tienda objects filtered by the created_at column
 * @method array findByUpdatedAt(string $updated_at) Return Tienda objects filtered by the updated_at column
 * @method array findByTipo(string $tipo) Return Tienda objects filtered by the tipo column
 * @method array findByNit(string $nit) Return Tienda objects filtered by the nit column
 * @method array findByFeelUsuario(string $feel_usuario) Return Tienda objects filtered by the feel_usuario column
 * @method array findByFeelToken(string $feel_token) Return Tienda objects filtered by the feel_token column
 * @method array findByFeelLlave(string $feel_llave) Return Tienda objects filtered by the feel_llave column
 * @method array findByActivaBuscador(boolean $activa_buscador) Return Tienda objects filtered by the activa_buscador column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseTiendaQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseTiendaQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = 'propel', $modelName = 'Tienda', $modelAlias = null)
    {
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new TiendaQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param     TiendaQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return TiendaQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof TiendaQuery) {
            return $criteria;
        }
        $query = new TiendaQuery();
        if (null !== $modelAlias) {
            $query->setModelAlias($modelAlias);
        }
        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return   Tienda|Tienda[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = TiendaPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is alredy in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(TiendaPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return   Tienda A model object, or null if the key is not found
     * @throws   PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT `ID`, `EMPRESA_ID`, `CODIGO`, `NOMBRE`, `CODIGO_ESTABLECIMIENTO`, `DIRECCION`, `DEPARTAMENTO_ID`, `MUNICIPIO_ID`, `OBSERVACIONES`, `CORREO`, `TELEFONO`, `NOMBRE_COMERCIAL`, `ACTIVO`, `CUENTA_DEBE`, `CUENTA_HABER`, `CREATED_BY`, `UPDATED_BY`, `CREATED_AT`, `UPDATED_AT`, `TIPO`, `NIT`, `FEEL_USUARIO`, `FEEL_TOKEN`, `FEEL_LLAVE`, `ACTIVA_BUSCADOR` FROM `tienda` WHERE `ID` = :p0';
        try {
            $stmt = $con->prepare($sql);
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new Tienda();
            $obj->hydrate($row);
            TiendaPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return Tienda|Tienda[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|Tienda[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(TiendaPeer::ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(TiendaPeer::ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the id column
     *
     * Example usage:
     * <code>
     * $query->filterById(1234); // WHERE id = 1234
     * $query->filterById(array(12, 34)); // WHERE id IN (12, 34)
     * $query->filterById(array('min' => 12)); // WHERE id > 12
     * </code>
     *
     * @param     mixed $id The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function filterById($id = null, $comparison = null)
    {
        if (is_array($id) && null === $comparison) {
            $comparison = Criteria::IN;
        }

        return $this->addUsingAlias(TiendaPeer::ID, $id, $comparison);
    }

    /**
     * Filter the query on the empresa_id column
     *
     * Example usage:
     * <code>
     * $query->filterByEmpresaId(1234); // WHERE empresa_id = 1234
     * $query->filterByEmpresaId(array(12, 34)); // WHERE empresa_id IN (12, 34)
     * $query->filterByEmpresaId(array('min' => 12)); // WHERE empresa_id > 12
     * </code>
     *
     * @see       filterByEmpresa()
     *
     * @param     mixed $empresaId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function filterByEmpresaId($empresaId = null, $comparison = null)
    {
        if (is_array($empresaId)) {
            $useMinMax = false;
            if (isset($empresaId['min'])) {
                $this->addUsingAlias(TiendaPeer::EMPRESA_ID, $empresaId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($empresaId['max'])) {
                $this->addUsingAlias(TiendaPeer::EMPRESA_ID, $empresaId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(TiendaPeer::EMPRESA_ID, $empresaId, $comparison);
    }

    /**
     * Filter the query on the codigo column
     *
     * Example usage:
     * <code>
     * $query->filterByCodigo('fooValue');   // WHERE codigo = 'fooValue'
     * $query->filterByCodigo('%fooValue%'); // WHERE codigo LIKE '%fooValue%'
     * </code>
     *
     * @param     string $codigo The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function filterByCodigo($codigo = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($codigo)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $codigo)) {
                $codigo = str_replace('*', '%', $codigo);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(TiendaPeer::CODIGO, $codigo, $comparison);
    }

    /**
     * Filter the query on the nombre column
     *
     * Example usage:
     * <code>
     * $query->filterByNombre('fooValue');   // WHERE nombre = 'fooValue'
     * $query->filterByNombre('%fooValue%'); // WHERE nombre LIKE '%fooValue%'
     * </code>
     *
     * @param     string $nombre The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function filterByNombre($nombre = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($nombre)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $nombre)) {
                $nombre = str_replace('*', '%', $nombre);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(TiendaPeer::NOMBRE, $nombre, $comparison);
    }

    /**
     * Filter the query on the codigo_establecimiento column
     *
     * Example usage:
     * <code>
     * $query->filterByCodigoEstablecimiento('fooValue');   // WHERE codigo_establecimiento = 'fooValue'
     * $query->filterByCodigoEstablecimiento('%fooValue%'); // WHERE codigo_establecimiento LIKE '%fooValue%'
     * </code>
     *
     * @param     string $codigoEstablecimiento The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function filterByCodigoEstablecimiento($codigoEstablecimiento = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($codigoEstablecimiento)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $codigoEstablecimiento)) {
                $codigoEstablecimiento = str_replace('*', '%', $codigoEstablecimiento);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(TiendaPeer::CODIGO_ESTABLECIMIENTO, $codigoEstablecimiento, $comparison);
    }

    /**
     * Filter the query on the direccion column
     *
     * Example usage:
     * <code>
     * $query->filterByDireccion('fooValue');   // WHERE direccion = 'fooValue'
     * $query->filterByDireccion('%fooValue%'); // WHERE direccion LIKE '%fooValue%'
     * </code>
     *
     * @param     string $direccion The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function filterByDireccion($direccion = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($direccion)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $direccion)) {
                $direccion = str_replace('*', '%', $direccion);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(TiendaPeer::DIRECCION, $direccion, $comparison);
    }

    /**
     * Filter the query on the departamento_id column
     *
     * Example usage:
     * <code>
     * $query->filterByDepartamentoId(1234); // WHERE departamento_id = 1234
     * $query->filterByDepartamentoId(array(12, 34)); // WHERE departamento_id IN (12, 34)
     * $query->filterByDepartamentoId(array('min' => 12)); // WHERE departamento_id > 12
     * </code>
     *
     * @see       filterByDepartamento()
     *
     * @param     mixed $departamentoId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function filterByDepartamentoId($departamentoId = null, $comparison = null)
    {
        if (is_array($departamentoId)) {
            $useMinMax = false;
            if (isset($departamentoId['min'])) {
                $this->addUsingAlias(TiendaPeer::DEPARTAMENTO_ID, $departamentoId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($departamentoId['max'])) {
                $this->addUsingAlias(TiendaPeer::DEPARTAMENTO_ID, $departamentoId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(TiendaPeer::DEPARTAMENTO_ID, $departamentoId, $comparison);
    }

    /**
     * Filter the query on the municipio_id column
     *
     * Example usage:
     * <code>
     * $query->filterByMunicipioId(1234); // WHERE municipio_id = 1234
     * $query->filterByMunicipioId(array(12, 34)); // WHERE municipio_id IN (12, 34)
     * $query->filterByMunicipioId(array('min' => 12)); // WHERE municipio_id > 12
     * </code>
     *
     * @see       filterByMunicipio()
     *
     * @param     mixed $municipioId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function filterByMunicipioId($municipioId = null, $comparison = null)
    {
        if (is_array($municipioId)) {
            $useMinMax = false;
            if (isset($municipioId['min'])) {
                $this->addUsingAlias(TiendaPeer::MUNICIPIO_ID, $municipioId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($municipioId['max'])) {
                $this->addUsingAlias(TiendaPeer::MUNICIPIO_ID, $municipioId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(TiendaPeer::MUNICIPIO_ID, $municipioId, $comparison);
    }

    /**
     * Filter the query on the observaciones column
     *
     * Example usage:
     * <code>
     * $query->filterByObservaciones('fooValue');   // WHERE observaciones = 'fooValue'
     * $query->filterByObservaciones('%fooValue%'); // WHERE observaciones LIKE '%fooValue%'
     * </code>
     *
     * @param     string $observaciones The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function filterByObservaciones($observaciones = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($observaciones)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $observaciones)) {
                $observaciones = str_replace('*', '%', $observaciones);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(TiendaPeer::OBSERVACIONES, $observaciones, $comparison);
    }

    /**
     * Filter the query on the correo column
     *
     * Example usage:
     * <code>
     * $query->filterByCorreo('fooValue');   // WHERE correo = 'fooValue'
     * $query->filterByCorreo('%fooValue%'); // WHERE correo LIKE '%fooValue%'
     * </code>
     *
     * @param     string $correo The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function filterByCorreo($correo = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($correo)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $correo)) {
                $correo = str_replace('*', '%', $correo);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(TiendaPeer::CORREO, $correo, $comparison);
    }

    /**
     * Filter the query on the telefono column
     *
     * Example usage:
     * <code>
     * $query->filterByTelefono('fooValue');   // WHERE telefono = 'fooValue'
     * $query->filterByTelefono('%fooValue%'); // WHERE telefono LIKE '%fooValue%'
     * </code>
     *
     * @param     string $telefono The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function filterByTelefono($telefono = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($telefono)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $telefono)) {
                $telefono = str_replace('*', '%', $telefono);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(TiendaPeer::TELEFONO, $telefono, $comparison);
    }

    /**
     * Filter the query on the nombre_comercial column
     *
     * Example usage:
     * <code>
     * $query->filterByNombreComercial('fooValue');   // WHERE nombre_comercial = 'fooValue'
     * $query->filterByNombreComercial('%fooValue%'); // WHERE nombre_comercial LIKE '%fooValue%'
     * </code>
     *
     * @param     string $nombreComercial The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function filterByNombreComercial($nombreComercial = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($nombreComercial)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $nombreComercial)) {
                $nombreComercial = str_replace('*', '%', $nombreComercial);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(TiendaPeer::NOMBRE_COMERCIAL, $nombreComercial, $comparison);
    }

    /**
     * Filter the query on the activo column
     *
     * Example usage:
     * <code>
     * $query->filterByActivo(true); // WHERE activo = true
     * $query->filterByActivo('yes'); // WHERE activo = true
     * </code>
     *
     * @param     boolean|string $activo The value to use as filter.
     *              Non-boolean arguments are converted using the following rules:
     *                * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *                * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     *              Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function filterByActivo($activo = null, $comparison = null)
    {
        if (is_string($activo)) {
            $activo = in_array(strtolower($activo), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
        }

        return $this->addUsingAlias(TiendaPeer::ACTIVO, $activo, $comparison);
    }

    /**
     * Filter the query on the cuenta_debe column
     *
     * Example usage:
     * <code>
     * $query->filterByCuentaDebe('fooValue');   // WHERE cuenta_debe = 'fooValue'
     * $query->filterByCuentaDebe('%fooValue%'); // WHERE cuenta_debe LIKE '%fooValue%'
     * </code>
     *
     * @param     string $cuentaDebe The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function filterByCuentaDebe($cuentaDebe = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($cuentaDebe)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $cuentaDebe)) {
                $cuentaDebe = str_replace('*', '%', $cuentaDebe);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(TiendaPeer::CUENTA_DEBE, $cuentaDebe, $comparison);
    }

    /**
     * Filter the query on the cuenta_haber column
     *
     * Example usage:
     * <code>
     * $query->filterByCuentaHaber('fooValue');   // WHERE cuenta_haber = 'fooValue'
     * $query->filterByCuentaHaber('%fooValue%'); // WHERE cuenta_haber LIKE '%fooValue%'
     * </code>
     *
     * @param     string $cuentaHaber The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function filterByCuentaHaber($cuentaHaber = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($cuentaHaber)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $cuentaHaber)) {
                $cuentaHaber = str_replace('*', '%', $cuentaHaber);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(TiendaPeer::CUENTA_HABER, $cuentaHaber, $comparison);
    }

    /**
     * Filter the query on the created_by column
     *
     * Example usage:
     * <code>
     * $query->filterByCreatedBy('fooValue');   // WHERE created_by = 'fooValue'
     * $query->filterByCreatedBy('%fooValue%'); // WHERE created_by LIKE '%fooValue%'
     * </code>
     *
     * @param     string $createdBy The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function filterByCreatedBy($createdBy = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($createdBy)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $createdBy)) {
                $createdBy = str_replace('*', '%', $createdBy);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(TiendaPeer::CREATED_BY, $createdBy, $comparison);
    }

    /**
     * Filter the query on the updated_by column
     *
     * Example usage:
     * <code>
     * $query->filterByUpdatedBy('fooValue');   // WHERE updated_by = 'fooValue'
     * $query->filterByUpdatedBy('%fooValue%'); // WHERE updated_by LIKE '%fooValue%'
     * </code>
     *
     * @param     string $updatedBy The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function filterByUpdatedBy($updatedBy = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($updatedBy)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $updatedBy)) {
                $updatedBy = str_replace('*', '%', $updatedBy);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(TiendaPeer::UPDATED_BY, $updatedBy, $comparison);
    }

    /**
     * Filter the query on the created_at column
     *
     * Example usage:
     * <code>
     * $query->filterByCreatedAt('2011-03-14'); // WHERE created_at = '2011-03-14'
     * $query->filterByCreatedAt('now'); // WHERE created_at = '2011-03-14'
     * $query->filterByCreatedAt(array('max' => 'yesterday')); // WHERE created_at > '2011-03-13'
     * </code>
     *
     * @param     mixed $createdAt The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function filterByCreatedAt($createdAt = null, $comparison = null)
    {
        if (is_array($createdAt)) {
            $useMinMax = false;
            if (isset($createdAt['min'])) {
                $this->addUsingAlias(TiendaPeer::CREATED_AT, $createdAt['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($createdAt['max'])) {
                $this->addUsingAlias(TiendaPeer::CREATED_AT, $createdAt['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(TiendaPeer::CREATED_AT, $createdAt, $comparison);
    }

    /**
     * Filter the query on the updated_at column
     *
     * Example usage:
     * <code>
     * $query->filterByUpdatedAt('2011-03-14'); // WHERE updated_at = '2011-03-14'
     * $query->filterByUpdatedAt('now'); // WHERE updated_at = '2011-03-14'
     * $query->filterByUpdatedAt(array('max' => 'yesterday')); // WHERE updated_at > '2011-03-13'
     * </code>
     *
     * @param     mixed $updatedAt The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function filterByUpdatedAt($updatedAt = null, $comparison = null)
    {
        if (is_array($updatedAt)) {
            $useMinMax = false;
            if (isset($updatedAt['min'])) {
                $this->addUsingAlias(TiendaPeer::UPDATED_AT, $updatedAt['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($updatedAt['max'])) {
                $this->addUsingAlias(TiendaPeer::UPDATED_AT, $updatedAt['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(TiendaPeer::UPDATED_AT, $updatedAt, $comparison);
    }

    /**
     * Filter the query on the tipo column
     *
     * Example usage:
     * <code>
     * $query->filterByTipo('fooValue');   // WHERE tipo = 'fooValue'
     * $query->filterByTipo('%fooValue%'); // WHERE tipo LIKE '%fooValue%'
     * </code>
     *
     * @param     string $tipo The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function filterByTipo($tipo = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($tipo)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $tipo)) {
                $tipo = str_replace('*', '%', $tipo);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(TiendaPeer::TIPO, $tipo, $comparison);
    }

    /**
     * Filter the query on the nit column
     *
     * Example usage:
     * <code>
     * $query->filterByNit('fooValue');   // WHERE nit = 'fooValue'
     * $query->filterByNit('%fooValue%'); // WHERE nit LIKE '%fooValue%'
     * </code>
     *
     * @param     string $nit The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function filterByNit($nit = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($nit)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $nit)) {
                $nit = str_replace('*', '%', $nit);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(TiendaPeer::NIT, $nit, $comparison);
    }

    /**
     * Filter the query on the feel_usuario column
     *
     * Example usage:
     * <code>
     * $query->filterByFeelUsuario('fooValue');   // WHERE feel_usuario = 'fooValue'
     * $query->filterByFeelUsuario('%fooValue%'); // WHERE feel_usuario LIKE '%fooValue%'
     * </code>
     *
     * @param     string $feelUsuario The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function filterByFeelUsuario($feelUsuario = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($feelUsuario)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $feelUsuario)) {
                $feelUsuario = str_replace('*', '%', $feelUsuario);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(TiendaPeer::FEEL_USUARIO, $feelUsuario, $comparison);
    }

    /**
     * Filter the query on the feel_token column
     *
     * Example usage:
     * <code>
     * $query->filterByFeelToken('fooValue');   // WHERE feel_token = 'fooValue'
     * $query->filterByFeelToken('%fooValue%'); // WHERE feel_token LIKE '%fooValue%'
     * </code>
     *
     * @param     string $feelToken The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function filterByFeelToken($feelToken = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($feelToken)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $feelToken)) {
                $feelToken = str_replace('*', '%', $feelToken);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(TiendaPeer::FEEL_TOKEN, $feelToken, $comparison);
    }

    /**
     * Filter the query on the feel_llave column
     *
     * Example usage:
     * <code>
     * $query->filterByFeelLlave('fooValue');   // WHERE feel_llave = 'fooValue'
     * $query->filterByFeelLlave('%fooValue%'); // WHERE feel_llave LIKE '%fooValue%'
     * </code>
     *
     * @param     string $feelLlave The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function filterByFeelLlave($feelLlave = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($feelLlave)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $feelLlave)) {
                $feelLlave = str_replace('*', '%', $feelLlave);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(TiendaPeer::FEEL_LLAVE, $feelLlave, $comparison);
    }

    /**
     * Filter the query on the activa_buscador column
     *
     * Example usage:
     * <code>
     * $query->filterByActivaBuscador(true); // WHERE activa_buscador = true
     * $query->filterByActivaBuscador('yes'); // WHERE activa_buscador = true
     * </code>
     *
     * @param     boolean|string $activaBuscador The value to use as filter.
     *              Non-boolean arguments are converted using the following rules:
     *                * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *                * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     *              Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function filterByActivaBuscador($activaBuscador = null, $comparison = null)
    {
        if (is_string($activaBuscador)) {
            $activa_buscador = in_array(strtolower($activaBuscador), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
        }

        return $this->addUsingAlias(TiendaPeer::ACTIVA_BUSCADOR, $activaBuscador, $comparison);
    }

    /**
     * Filter the query by a related Empresa object
     *
     * @param   Empresa|PropelObjectCollection $empresa The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   TiendaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByEmpresa($empresa, $comparison = null)
    {
        if ($empresa instanceof Empresa) {
            return $this
                ->addUsingAlias(TiendaPeer::EMPRESA_ID, $empresa->getId(), $comparison);
        } elseif ($empresa instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(TiendaPeer::EMPRESA_ID, $empresa->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByEmpresa() only accepts arguments of type Empresa or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Empresa relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function joinEmpresa($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Empresa');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Empresa');
        }

        return $this;
    }

    /**
     * Use the Empresa relation Empresa object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   EmpresaQuery A secondary query class using the current class as primary query
     */
    public function useEmpresaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinEmpresa($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Empresa', 'EmpresaQuery');
    }

    /**
     * Filter the query by a related Departamento object
     *
     * @param   Departamento|PropelObjectCollection $departamento The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   TiendaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByDepartamento($departamento, $comparison = null)
    {
        if ($departamento instanceof Departamento) {
            return $this
                ->addUsingAlias(TiendaPeer::DEPARTAMENTO_ID, $departamento->getId(), $comparison);
        } elseif ($departamento instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(TiendaPeer::DEPARTAMENTO_ID, $departamento->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByDepartamento() only accepts arguments of type Departamento or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Departamento relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function joinDepartamento($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Departamento');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Departamento');
        }

        return $this;
    }

    /**
     * Use the Departamento relation Departamento object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   DepartamentoQuery A secondary query class using the current class as primary query
     */
    public function useDepartamentoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinDepartamento($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Departamento', 'DepartamentoQuery');
    }

    /**
     * Filter the query by a related Municipio object
     *
     * @param   Municipio|PropelObjectCollection $municipio The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   TiendaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByMunicipio($municipio, $comparison = null)
    {
        if ($municipio instanceof Municipio) {
            return $this
                ->addUsingAlias(TiendaPeer::MUNICIPIO_ID, $municipio->getId(), $comparison);
        } elseif ($municipio instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(TiendaPeer::MUNICIPIO_ID, $municipio->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByMunicipio() only accepts arguments of type Municipio or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Municipio relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function joinMunicipio($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Municipio');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Municipio');
        }

        return $this;
    }

    /**
     * Use the Municipio relation Municipio object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   MunicipioQuery A secondary query class using the current class as primary query
     */
    public function useMunicipioQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinMunicipio($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Municipio', 'MunicipioQuery');
    }

    /**
     * Filter the query by a related Usuario object
     *
     * @param   Usuario|PropelObjectCollection $usuario  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   TiendaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByUsuario($usuario, $comparison = null)
    {
        if ($usuario instanceof Usuario) {
            return $this
                ->addUsingAlias(TiendaPeer::ID, $usuario->getTiendaId(), $comparison);
        } elseif ($usuario instanceof PropelObjectCollection) {
            return $this
                ->useUsuarioQuery()
                ->filterByPrimaryKeys($usuario->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByUsuario() only accepts arguments of type Usuario or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Usuario relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function joinUsuario($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Usuario');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Usuario');
        }

        return $this;
    }

    /**
     * Use the Usuario relation Usuario object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   UsuarioQuery A secondary query class using the current class as primary query
     */
    public function useUsuarioQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinUsuario($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Usuario', 'UsuarioQuery');
    }

    /**
     * Filter the query by a related UsuarioTienda object
     *
     * @param   UsuarioTienda|PropelObjectCollection $usuarioTienda  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   TiendaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByUsuarioTienda($usuarioTienda, $comparison = null)
    {
        if ($usuarioTienda instanceof UsuarioTienda) {
            return $this
                ->addUsingAlias(TiendaPeer::ID, $usuarioTienda->getTiendaId(), $comparison);
        } elseif ($usuarioTienda instanceof PropelObjectCollection) {
            return $this
                ->useUsuarioTiendaQuery()
                ->filterByPrimaryKeys($usuarioTienda->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByUsuarioTienda() only accepts arguments of type UsuarioTienda or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the UsuarioTienda relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function joinUsuarioTienda($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('UsuarioTienda');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'UsuarioTienda');
        }

        return $this;
    }

    /**
     * Use the UsuarioTienda relation UsuarioTienda object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   UsuarioTiendaQuery A secondary query class using the current class as primary query
     */
    public function useUsuarioTiendaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinUsuarioTienda($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'UsuarioTienda', 'UsuarioTiendaQuery');
    }

    /**
     * Filter the query by a related Operacion object
     *
     * @param   Operacion|PropelObjectCollection $operacion  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   TiendaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByOperacion($operacion, $comparison = null)
    {
        if ($operacion instanceof Operacion) {
            return $this
                ->addUsingAlias(TiendaPeer::ID, $operacion->getTiendaId(), $comparison);
        } elseif ($operacion instanceof PropelObjectCollection) {
            return $this
                ->useOperacionQuery()
                ->filterByPrimaryKeys($operacion->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByOperacion() only accepts arguments of type Operacion or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Operacion relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function joinOperacion($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Operacion');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Operacion');
        }

        return $this;
    }

    /**
     * Use the Operacion relation Operacion object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   OperacionQuery A secondary query class using the current class as primary query
     */
    public function useOperacionQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinOperacion($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Operacion', 'OperacionQuery');
    }

    /**
     * Filter the query by a related OrdenProveedor object
     *
     * @param   OrdenProveedor|PropelObjectCollection $ordenProveedor  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   TiendaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByOrdenProveedor($ordenProveedor, $comparison = null)
    {
        if ($ordenProveedor instanceof OrdenProveedor) {
            return $this
                ->addUsingAlias(TiendaPeer::ID, $ordenProveedor->getTiendaId(), $comparison);
        } elseif ($ordenProveedor instanceof PropelObjectCollection) {
            return $this
                ->useOrdenProveedorQuery()
                ->filterByPrimaryKeys($ordenProveedor->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByOrdenProveedor() only accepts arguments of type OrdenProveedor or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the OrdenProveedor relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function joinOrdenProveedor($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('OrdenProveedor');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'OrdenProveedor');
        }

        return $this;
    }

    /**
     * Use the OrdenProveedor relation OrdenProveedor object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   OrdenProveedorQuery A secondary query class using the current class as primary query
     */
    public function useOrdenProveedorQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinOrdenProveedor($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'OrdenProveedor', 'OrdenProveedorQuery');
    }

    /**
     * Filter the query by a related MovimientoBanco object
     *
     * @param   MovimientoBanco|PropelObjectCollection $movimientoBanco  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   TiendaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByMovimientoBanco($movimientoBanco, $comparison = null)
    {
        if ($movimientoBanco instanceof MovimientoBanco) {
            return $this
                ->addUsingAlias(TiendaPeer::ID, $movimientoBanco->getTiendaId(), $comparison);
        } elseif ($movimientoBanco instanceof PropelObjectCollection) {
            return $this
                ->useMovimientoBancoQuery()
                ->filterByPrimaryKeys($movimientoBanco->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByMovimientoBanco() only accepts arguments of type MovimientoBanco or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the MovimientoBanco relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function joinMovimientoBanco($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('MovimientoBanco');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'MovimientoBanco');
        }

        return $this;
    }

    /**
     * Use the MovimientoBanco relation MovimientoBanco object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   MovimientoBancoQuery A secondary query class using the current class as primary query
     */
    public function useMovimientoBancoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinMovimientoBanco($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'MovimientoBanco', 'MovimientoBancoQuery');
    }

    /**
     * Filter the query by a related Partida object
     *
     * @param   Partida|PropelObjectCollection $partida  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   TiendaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByPartida($partida, $comparison = null)
    {
        if ($partida instanceof Partida) {
            return $this
                ->addUsingAlias(TiendaPeer::ID, $partida->getTiendaId(), $comparison);
        } elseif ($partida instanceof PropelObjectCollection) {
            return $this
                ->usePartidaQuery()
                ->filterByPrimaryKeys($partida->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByPartida() only accepts arguments of type Partida or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Partida relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function joinPartida($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Partida');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Partida');
        }

        return $this;
    }

    /**
     * Use the Partida relation Partida object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   PartidaQuery A secondary query class using the current class as primary query
     */
    public function usePartidaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinPartida($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Partida', 'PartidaQuery');
    }

    /**
     * Filter the query by a related IngresoProductoPro object
     *
     * @param   IngresoProductoPro|PropelObjectCollection $ingresoProductoPro  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   TiendaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByIngresoProductoPro($ingresoProductoPro, $comparison = null)
    {
        if ($ingresoProductoPro instanceof IngresoProductoPro) {
            return $this
                ->addUsingAlias(TiendaPeer::ID, $ingresoProductoPro->getTiendaId(), $comparison);
        } elseif ($ingresoProductoPro instanceof PropelObjectCollection) {
            return $this
                ->useIngresoProductoProQuery()
                ->filterByPrimaryKeys($ingresoProductoPro->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByIngresoProductoPro() only accepts arguments of type IngresoProductoPro or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the IngresoProductoPro relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function joinIngresoProductoPro($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('IngresoProductoPro');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'IngresoProductoPro');
        }

        return $this;
    }

    /**
     * Use the IngresoProductoPro relation IngresoProductoPro object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   IngresoProductoProQuery A secondary query class using the current class as primary query
     */
    public function useIngresoProductoProQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinIngresoProductoPro($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'IngresoProductoPro', 'IngresoProductoProQuery');
    }

    /**
     * Filter the query by a related SalidaProducto object
     *
     * @param   SalidaProducto|PropelObjectCollection $salidaProducto  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   TiendaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterBySalidaProducto($salidaProducto, $comparison = null)
    {
        if ($salidaProducto instanceof SalidaProducto) {
            return $this
                ->addUsingAlias(TiendaPeer::ID, $salidaProducto->getTiendaId(), $comparison);
        } elseif ($salidaProducto instanceof PropelObjectCollection) {
            return $this
                ->useSalidaProductoQuery()
                ->filterByPrimaryKeys($salidaProducto->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterBySalidaProducto() only accepts arguments of type SalidaProducto or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the SalidaProducto relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function joinSalidaProducto($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('SalidaProducto');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'SalidaProducto');
        }

        return $this;
    }

    /**
     * Use the SalidaProducto relation SalidaProducto object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   SalidaProductoQuery A secondary query class using the current class as primary query
     */
    public function useSalidaProductoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinSalidaProducto($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'SalidaProducto', 'SalidaProductoQuery');
    }

    /**
     * Filter the query by a related CierreCaja object
     *
     * @param   CierreCaja|PropelObjectCollection $cierreCaja  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   TiendaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByCierreCaja($cierreCaja, $comparison = null)
    {
        if ($cierreCaja instanceof CierreCaja) {
            return $this
                ->addUsingAlias(TiendaPeer::ID, $cierreCaja->getTiendaId(), $comparison);
        } elseif ($cierreCaja instanceof PropelObjectCollection) {
            return $this
                ->useCierreCajaQuery()
                ->filterByPrimaryKeys($cierreCaja->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByCierreCaja() only accepts arguments of type CierreCaja or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the CierreCaja relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function joinCierreCaja($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('CierreCaja');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'CierreCaja');
        }

        return $this;
    }

    /**
     * Use the CierreCaja relation CierreCaja object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   CierreCajaQuery A secondary query class using the current class as primary query
     */
    public function useCierreCajaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinCierreCaja($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'CierreCaja', 'CierreCajaQuery');
    }

    /**
     * Filter the query by a related Cierre object
     *
     * @param   Cierre|PropelObjectCollection $cierre  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   TiendaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByCierre($cierre, $comparison = null)
    {
        if ($cierre instanceof Cierre) {
            return $this
                ->addUsingAlias(TiendaPeer::ID, $cierre->getTiendaId(), $comparison);
        } elseif ($cierre instanceof PropelObjectCollection) {
            return $this
                ->useCierreQuery()
                ->filterByPrimaryKeys($cierre->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByCierre() only accepts arguments of type Cierre or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Cierre relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function joinCierre($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Cierre');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Cierre');
        }

        return $this;
    }

    /**
     * Use the Cierre relation Cierre object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   CierreQuery A secondary query class using the current class as primary query
     */
    public function useCierreQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinCierre($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Cierre', 'CierreQuery');
    }

    /**
     * Filter the query by a related CampoUsuario object
     *
     * @param   CampoUsuario|PropelObjectCollection $campoUsuario  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   TiendaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByCampoUsuario($campoUsuario, $comparison = null)
    {
        if ($campoUsuario instanceof CampoUsuario) {
            return $this
                ->addUsingAlias(TiendaPeer::ID, $campoUsuario->getTiendaId(), $comparison);
        } elseif ($campoUsuario instanceof PropelObjectCollection) {
            return $this
                ->useCampoUsuarioQuery()
                ->filterByPrimaryKeys($campoUsuario->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByCampoUsuario() only accepts arguments of type CampoUsuario or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the CampoUsuario relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function joinCampoUsuario($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('CampoUsuario');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'CampoUsuario');
        }

        return $this;
    }

    /**
     * Use the CampoUsuario relation CampoUsuario object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   CampoUsuarioQuery A secondary query class using the current class as primary query
     */
    public function useCampoUsuarioQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinCampoUsuario($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'CampoUsuario', 'CampoUsuarioQuery');
    }

    /**
     * Filter the query by a related Gasto object
     *
     * @param   Gasto|PropelObjectCollection $gasto  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   TiendaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByGasto($gasto, $comparison = null)
    {
        if ($gasto instanceof Gasto) {
            return $this
                ->addUsingAlias(TiendaPeer::ID, $gasto->getTiendaId(), $comparison);
        } elseif ($gasto instanceof PropelObjectCollection) {
            return $this
                ->useGastoQuery()
                ->filterByPrimaryKeys($gasto->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByGasto() only accepts arguments of type Gasto or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Gasto relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function joinGasto($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Gasto');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Gasto');
        }

        return $this;
    }

    /**
     * Use the Gasto relation Gasto object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   GastoQuery A secondary query class using the current class as primary query
     */
    public function useGastoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinGasto($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Gasto', 'GastoQuery');
    }

    /**
     * Filter the query by a related ProductoExistencia object
     *
     * @param   ProductoExistencia|PropelObjectCollection $productoExistencia  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   TiendaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByProductoExistencia($productoExistencia, $comparison = null)
    {
        if ($productoExistencia instanceof ProductoExistencia) {
            return $this
                ->addUsingAlias(TiendaPeer::ID, $productoExistencia->getTiendaId(), $comparison);
        } elseif ($productoExistencia instanceof PropelObjectCollection) {
            return $this
                ->useProductoExistenciaQuery()
                ->filterByPrimaryKeys($productoExistencia->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByProductoExistencia() only accepts arguments of type ProductoExistencia or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ProductoExistencia relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function joinProductoExistencia($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ProductoExistencia');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ProductoExistencia');
        }

        return $this;
    }

    /**
     * Use the ProductoExistencia relation ProductoExistencia object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ProductoExistenciaQuery A secondary query class using the current class as primary query
     */
    public function useProductoExistenciaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinProductoExistencia($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ProductoExistencia', 'ProductoExistenciaQuery');
    }

    /**
     * Filter the query by a related ProductoUbicacion object
     *
     * @param   ProductoUbicacion|PropelObjectCollection $productoUbicacion  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   TiendaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByProductoUbicacion($productoUbicacion, $comparison = null)
    {
        if ($productoUbicacion instanceof ProductoUbicacion) {
            return $this
                ->addUsingAlias(TiendaPeer::ID, $productoUbicacion->getTiendaId(), $comparison);
        } elseif ($productoUbicacion instanceof PropelObjectCollection) {
            return $this
                ->useProductoUbicacionQuery()
                ->filterByPrimaryKeys($productoUbicacion->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByProductoUbicacion() only accepts arguments of type ProductoUbicacion or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ProductoUbicacion relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function joinProductoUbicacion($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ProductoUbicacion');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ProductoUbicacion');
        }

        return $this;
    }

    /**
     * Use the ProductoUbicacion relation ProductoUbicacion object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ProductoUbicacionQuery A secondary query class using the current class as primary query
     */
    public function useProductoUbicacionQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinProductoUbicacion($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ProductoUbicacion', 'ProductoUbicacionQuery');
    }

    /**
     * Filter the query by a related IngresoProducto object
     *
     * @param   IngresoProducto|PropelObjectCollection $ingresoProducto  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   TiendaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByIngresoProducto($ingresoProducto, $comparison = null)
    {
        if ($ingresoProducto instanceof IngresoProducto) {
            return $this
                ->addUsingAlias(TiendaPeer::ID, $ingresoProducto->getTiendaId(), $comparison);
        } elseif ($ingresoProducto instanceof PropelObjectCollection) {
            return $this
                ->useIngresoProductoQuery()
                ->filterByPrimaryKeys($ingresoProducto->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByIngresoProducto() only accepts arguments of type IngresoProducto or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the IngresoProducto relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function joinIngresoProducto($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('IngresoProducto');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'IngresoProducto');
        }

        return $this;
    }

    /**
     * Use the IngresoProducto relation IngresoProducto object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   IngresoProductoQuery A secondary query class using the current class as primary query
     */
    public function useIngresoProductoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinIngresoProducto($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'IngresoProducto', 'IngresoProductoQuery');
    }

    /**
     * Filter the query by a related ProductoMovimiento object
     *
     * @param   ProductoMovimiento|PropelObjectCollection $productoMovimiento  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   TiendaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByProductoMovimiento($productoMovimiento, $comparison = null)
    {
        if ($productoMovimiento instanceof ProductoMovimiento) {
            return $this
                ->addUsingAlias(TiendaPeer::ID, $productoMovimiento->getTiendaId(), $comparison);
        } elseif ($productoMovimiento instanceof PropelObjectCollection) {
            return $this
                ->useProductoMovimientoQuery()
                ->filterByPrimaryKeys($productoMovimiento->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByProductoMovimiento() only accepts arguments of type ProductoMovimiento or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ProductoMovimiento relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function joinProductoMovimiento($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ProductoMovimiento');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ProductoMovimiento');
        }

        return $this;
    }

    /**
     * Use the ProductoMovimiento relation ProductoMovimiento object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ProductoMovimientoQuery A secondary query class using the current class as primary query
     */
    public function useProductoMovimientoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinProductoMovimiento($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ProductoMovimiento', 'ProductoMovimientoQuery');
    }

    /**
     * Filter the query by a related PrecioProducto object
     *
     * @param   PrecioProducto|PropelObjectCollection $precioProducto  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   TiendaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByPrecioProducto($precioProducto, $comparison = null)
    {
        if ($precioProducto instanceof PrecioProducto) {
            return $this
                ->addUsingAlias(TiendaPeer::ID, $precioProducto->getTiendaId(), $comparison);
        } elseif ($precioProducto instanceof PropelObjectCollection) {
            return $this
                ->usePrecioProductoQuery()
                ->filterByPrimaryKeys($precioProducto->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByPrecioProducto() only accepts arguments of type PrecioProducto or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the PrecioProducto relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function joinPrecioProducto($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('PrecioProducto');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'PrecioProducto');
        }

        return $this;
    }

    /**
     * Use the PrecioProducto relation PrecioProducto object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   PrecioProductoQuery A secondary query class using the current class as primary query
     */
    public function usePrecioProductoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinPrecioProducto($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'PrecioProducto', 'PrecioProductoQuery');
    }

    /**
     * Filter the query by a related OrdenCotizacion object
     *
     * @param   OrdenCotizacion|PropelObjectCollection $ordenCotizacion  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   TiendaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByOrdenCotizacion($ordenCotizacion, $comparison = null)
    {
        if ($ordenCotizacion instanceof OrdenCotizacion) {
            return $this
                ->addUsingAlias(TiendaPeer::ID, $ordenCotizacion->getTiendaId(), $comparison);
        } elseif ($ordenCotizacion instanceof PropelObjectCollection) {
            return $this
                ->useOrdenCotizacionQuery()
                ->filterByPrimaryKeys($ordenCotizacion->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByOrdenCotizacion() only accepts arguments of type OrdenCotizacion or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the OrdenCotizacion relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function joinOrdenCotizacion($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('OrdenCotizacion');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'OrdenCotizacion');
        }

        return $this;
    }

    /**
     * Use the OrdenCotizacion relation OrdenCotizacion object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   OrdenCotizacionQuery A secondary query class using the current class as primary query
     */
    public function useOrdenCotizacionQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinOrdenCotizacion($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'OrdenCotizacion', 'OrdenCotizacionQuery');
    }

    /**
     * Filter the query by a related VentaResumida object
     *
     * @param   VentaResumida|PropelObjectCollection $ventaResumida  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   TiendaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByVentaResumida($ventaResumida, $comparison = null)
    {
        if ($ventaResumida instanceof VentaResumida) {
            return $this
                ->addUsingAlias(TiendaPeer::ID, $ventaResumida->getTiendaId(), $comparison);
        } elseif ($ventaResumida instanceof PropelObjectCollection) {
            return $this
                ->useVentaResumidaQuery()
                ->filterByPrimaryKeys($ventaResumida->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByVentaResumida() only accepts arguments of type VentaResumida or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the VentaResumida relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function joinVentaResumida($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('VentaResumida');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'VentaResumida');
        }

        return $this;
    }

    /**
     * Use the VentaResumida relation VentaResumida object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   VentaResumidaQuery A secondary query class using the current class as primary query
     */
    public function useVentaResumidaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinVentaResumida($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'VentaResumida', 'VentaResumidaQuery');
    }

    /**
     * Filter the query by a related OrdenDevolucion object
     *
     * @param   OrdenDevolucion|PropelObjectCollection $ordenDevolucion  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   TiendaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByOrdenDevolucion($ordenDevolucion, $comparison = null)
    {
        if ($ordenDevolucion instanceof OrdenDevolucion) {
            return $this
                ->addUsingAlias(TiendaPeer::ID, $ordenDevolucion->getTiendaId(), $comparison);
        } elseif ($ordenDevolucion instanceof PropelObjectCollection) {
            return $this
                ->useOrdenDevolucionQuery()
                ->filterByPrimaryKeys($ordenDevolucion->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByOrdenDevolucion() only accepts arguments of type OrdenDevolucion or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the OrdenDevolucion relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function joinOrdenDevolucion($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('OrdenDevolucion');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'OrdenDevolucion');
        }

        return $this;
    }

    /**
     * Use the OrdenDevolucion relation OrdenDevolucion object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   OrdenDevolucionQuery A secondary query class using the current class as primary query
     */
    public function useOrdenDevolucionQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinOrdenDevolucion($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'OrdenDevolucion', 'OrdenDevolucionQuery');
    }

    /**
     * Filter the query by a related EgresosRecibido object
     *
     * @param   EgresosRecibido|PropelObjectCollection $egresosRecibido  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   TiendaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByEgresosRecibido($egresosRecibido, $comparison = null)
    {
        if ($egresosRecibido instanceof EgresosRecibido) {
            return $this
                ->addUsingAlias(TiendaPeer::ID, $egresosRecibido->getTiendaId(), $comparison);
        } elseif ($egresosRecibido instanceof PropelObjectCollection) {
            return $this
                ->useEgresosRecibidoQuery()
                ->filterByPrimaryKeys($egresosRecibido->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByEgresosRecibido() only accepts arguments of type EgresosRecibido or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the EgresosRecibido relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function joinEgresosRecibido($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('EgresosRecibido');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'EgresosRecibido');
        }

        return $this;
    }

    /**
     * Use the EgresosRecibido relation EgresosRecibido object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   EgresosRecibidoQuery A secondary query class using the current class as primary query
     */
    public function useEgresosRecibidoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinEgresosRecibido($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'EgresosRecibido', 'EgresosRecibidoQuery');
    }

    /**
     * Filter the query by a related GastoCaja object
     *
     * @param   GastoCaja|PropelObjectCollection $gastoCaja  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   TiendaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByGastoCaja($gastoCaja, $comparison = null)
    {
        if ($gastoCaja instanceof GastoCaja) {
            return $this
                ->addUsingAlias(TiendaPeer::ID, $gastoCaja->getTiendaId(), $comparison);
        } elseif ($gastoCaja instanceof PropelObjectCollection) {
            return $this
                ->useGastoCajaQuery()
                ->filterByPrimaryKeys($gastoCaja->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByGastoCaja() only accepts arguments of type GastoCaja or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the GastoCaja relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function joinGastoCaja($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('GastoCaja');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'GastoCaja');
        }

        return $this;
    }

    /**
     * Use the GastoCaja relation GastoCaja object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   GastoCajaQuery A secondary query class using the current class as primary query
     */
    public function useGastoCajaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinGastoCaja($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'GastoCaja', 'GastoCajaQuery');
    }

    /**
     * Filter the query by a related SolicitudDevolucion object
     *
     * @param   SolicitudDevolucion|PropelObjectCollection $solicitudDevolucion  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   TiendaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterBySolicitudDevolucion($solicitudDevolucion, $comparison = null)
    {
        if ($solicitudDevolucion instanceof SolicitudDevolucion) {
            return $this
                ->addUsingAlias(TiendaPeer::ID, $solicitudDevolucion->getTiendaId(), $comparison);
        } elseif ($solicitudDevolucion instanceof PropelObjectCollection) {
            return $this
                ->useSolicitudDevolucionQuery()
                ->filterByPrimaryKeys($solicitudDevolucion->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterBySolicitudDevolucion() only accepts arguments of type SolicitudDevolucion or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the SolicitudDevolucion relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function joinSolicitudDevolucion($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('SolicitudDevolucion');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'SolicitudDevolucion');
        }

        return $this;
    }

    /**
     * Use the SolicitudDevolucion relation SolicitudDevolucion object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   SolicitudDevolucionQuery A secondary query class using the current class as primary query
     */
    public function useSolicitudDevolucionQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinSolicitudDevolucion($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'SolicitudDevolucion', 'SolicitudDevolucionQuery');
    }

    /**
     * Filter the query by a related SolicitudDeposito object
     *
     * @param   SolicitudDeposito|PropelObjectCollection $solicitudDeposito  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   TiendaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterBySolicitudDeposito($solicitudDeposito, $comparison = null)
    {
        if ($solicitudDeposito instanceof SolicitudDeposito) {
            return $this
                ->addUsingAlias(TiendaPeer::ID, $solicitudDeposito->getTiendaId(), $comparison);
        } elseif ($solicitudDeposito instanceof PropelObjectCollection) {
            return $this
                ->useSolicitudDepositoQuery()
                ->filterByPrimaryKeys($solicitudDeposito->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterBySolicitudDeposito() only accepts arguments of type SolicitudDeposito or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the SolicitudDeposito relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function joinSolicitudDeposito($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('SolicitudDeposito');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'SolicitudDeposito');
        }

        return $this;
    }

    /**
     * Use the SolicitudDeposito relation SolicitudDeposito object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   SolicitudDepositoQuery A secondary query class using the current class as primary query
     */
    public function useSolicitudDepositoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinSolicitudDeposito($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'SolicitudDeposito', 'SolicitudDepositoQuery');
    }

    /**
     * Filter the query by a related Agenda object
     *
     * @param   Agenda|PropelObjectCollection $agenda  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   TiendaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByAgenda($agenda, $comparison = null)
    {
        if ($agenda instanceof Agenda) {
            return $this
                ->addUsingAlias(TiendaPeer::ID, $agenda->getTiendaId(), $comparison);
        } elseif ($agenda instanceof PropelObjectCollection) {
            return $this
                ->useAgendaQuery()
                ->filterByPrimaryKeys($agenda->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByAgenda() only accepts arguments of type Agenda or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Agenda relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function joinAgenda($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Agenda');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Agenda');
        }

        return $this;
    }

    /**
     * Use the Agenda relation Agenda object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   AgendaQuery A secondary query class using the current class as primary query
     */
    public function useAgendaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinAgenda($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Agenda', 'AgendaQuery');
    }

    /**
     * Filter the query by a related InventarioVence object
     *
     * @param   InventarioVence|PropelObjectCollection $inventarioVence  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   TiendaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByInventarioVence($inventarioVence, $comparison = null)
    {
        if ($inventarioVence instanceof InventarioVence) {
            return $this
                ->addUsingAlias(TiendaPeer::ID, $inventarioVence->getTiendaId(), $comparison);
        } elseif ($inventarioVence instanceof PropelObjectCollection) {
            return $this
                ->useInventarioVenceQuery()
                ->filterByPrimaryKeys($inventarioVence->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByInventarioVence() only accepts arguments of type InventarioVence or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the InventarioVence relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function joinInventarioVence($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('InventarioVence');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'InventarioVence');
        }

        return $this;
    }

    /**
     * Use the InventarioVence relation InventarioVence object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   InventarioVenceQuery A secondary query class using the current class as primary query
     */
    public function useInventarioVenceQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinInventarioVence($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'InventarioVence', 'InventarioVenceQuery');
    }

    /**
     * Filter the query by a related OrdenUbicacion object
     *
     * @param   OrdenUbicacion|PropelObjectCollection $ordenUbicacion  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   TiendaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByOrdenUbicacion($ordenUbicacion, $comparison = null)
    {
        if ($ordenUbicacion instanceof OrdenUbicacion) {
            return $this
                ->addUsingAlias(TiendaPeer::ID, $ordenUbicacion->getTiendaId(), $comparison);
        } elseif ($ordenUbicacion instanceof PropelObjectCollection) {
            return $this
                ->useOrdenUbicacionQuery()
                ->filterByPrimaryKeys($ordenUbicacion->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByOrdenUbicacion() only accepts arguments of type OrdenUbicacion or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the OrdenUbicacion relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function joinOrdenUbicacion($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('OrdenUbicacion');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'OrdenUbicacion');
        }

        return $this;
    }

    /**
     * Use the OrdenUbicacion relation OrdenUbicacion object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   OrdenUbicacionQuery A secondary query class using the current class as primary query
     */
    public function useOrdenUbicacionQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinOrdenUbicacion($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'OrdenUbicacion', 'OrdenUbicacionQuery');
    }

    /**
     * Filter the query by a related TrasladoUbicacion object
     *
     * @param   TrasladoUbicacion|PropelObjectCollection $trasladoUbicacion  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   TiendaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByTrasladoUbicacion($trasladoUbicacion, $comparison = null)
    {
        if ($trasladoUbicacion instanceof TrasladoUbicacion) {
            return $this
                ->addUsingAlias(TiendaPeer::ID, $trasladoUbicacion->getTiendaId(), $comparison);
        } elseif ($trasladoUbicacion instanceof PropelObjectCollection) {
            return $this
                ->useTrasladoUbicacionQuery()
                ->filterByPrimaryKeys($trasladoUbicacion->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByTrasladoUbicacion() only accepts arguments of type TrasladoUbicacion or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the TrasladoUbicacion relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function joinTrasladoUbicacion($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('TrasladoUbicacion');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'TrasladoUbicacion');
        }

        return $this;
    }

    /**
     * Use the TrasladoUbicacion relation TrasladoUbicacion object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   TrasladoUbicacionQuery A secondary query class using the current class as primary query
     */
    public function useTrasladoUbicacionQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinTrasladoUbicacion($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'TrasladoUbicacion', 'TrasladoUbicacionQuery');
    }

    /**
     * Filter the query by a related TrasladoUbicacionDetalle object
     *
     * @param   TrasladoUbicacionDetalle|PropelObjectCollection $trasladoUbicacionDetalle  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   TiendaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByTrasladoUbicacionDetalle($trasladoUbicacionDetalle, $comparison = null)
    {
        if ($trasladoUbicacionDetalle instanceof TrasladoUbicacionDetalle) {
            return $this
                ->addUsingAlias(TiendaPeer::ID, $trasladoUbicacionDetalle->getTiendaId(), $comparison);
        } elseif ($trasladoUbicacionDetalle instanceof PropelObjectCollection) {
            return $this
                ->useTrasladoUbicacionDetalleQuery()
                ->filterByPrimaryKeys($trasladoUbicacionDetalle->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByTrasladoUbicacionDetalle() only accepts arguments of type TrasladoUbicacionDetalle or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the TrasladoUbicacionDetalle relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function joinTrasladoUbicacionDetalle($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('TrasladoUbicacionDetalle');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'TrasladoUbicacionDetalle');
        }

        return $this;
    }

    /**
     * Use the TrasladoUbicacionDetalle relation TrasladoUbicacionDetalle object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   TrasladoUbicacionDetalleQuery A secondary query class using the current class as primary query
     */
    public function useTrasladoUbicacionDetalleQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinTrasladoUbicacionDetalle($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'TrasladoUbicacionDetalle', 'TrasladoUbicacionDetalleQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   Tienda $tienda Object to remove from the list of results
     *
     * @return TiendaQuery The current query, for fluid interface
     */
    public function prune($tienda = null)
    {
        if ($tienda) {
            $this->addUsingAlias(TiendaPeer::ID, $tienda->getId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}
