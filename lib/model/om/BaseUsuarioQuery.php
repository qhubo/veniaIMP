<?php


/**
 * Base class that represents a query for the 'usuario' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.6.7 on:
 *
 * 01/03/26 04:09:36
 *
 * @method UsuarioQuery orderById($order = Criteria::ASC) Order by the id column
 * @method UsuarioQuery orderByUsuario($order = Criteria::ASC) Order by the usuario column
 * @method UsuarioQuery orderByNombreCompleto($order = Criteria::ASC) Order by the nombre_completo column
 * @method UsuarioQuery orderByTipoUsuario($order = Criteria::ASC) Order by the tipo_usuario column
 * @method UsuarioQuery orderByClave($order = Criteria::ASC) Order by the clave column
 * @method UsuarioQuery orderByCorreo($order = Criteria::ASC) Order by the correo column
 * @method UsuarioQuery orderByEstado($order = Criteria::ASC) Order by the estado column
 * @method UsuarioQuery orderByImagen($order = Criteria::ASC) Order by the imagen column
 * @method UsuarioQuery orderByAdministrador($order = Criteria::ASC) Order by the administrador column
 * @method UsuarioQuery orderByUltimoIngreso($order = Criteria::ASC) Order by the ultimo_ingreso column
 * @method UsuarioQuery orderByToken($order = Criteria::ASC) Order by the token column
 * @method UsuarioQuery orderByActivo($order = Criteria::ASC) Order by the activo column
 * @method UsuarioQuery orderByValidado($order = Criteria::ASC) Order by the validado column
 * @method UsuarioQuery orderByNivelUsuario($order = Criteria::ASC) Order by the nivel_usuario column
 * @method UsuarioQuery orderByIp($order = Criteria::ASC) Order by the ip column
 * @method UsuarioQuery orderByTiendaId($order = Criteria::ASC) Order by the tienda_id column
 * @method UsuarioQuery orderByEmpresaId($order = Criteria::ASC) Order by the empresa_id column
 * @method UsuarioQuery orderByVendedorId($order = Criteria::ASC) Order by the vendedor_id column
 *
 * @method UsuarioQuery groupById() Group by the id column
 * @method UsuarioQuery groupByUsuario() Group by the usuario column
 * @method UsuarioQuery groupByNombreCompleto() Group by the nombre_completo column
 * @method UsuarioQuery groupByTipoUsuario() Group by the tipo_usuario column
 * @method UsuarioQuery groupByClave() Group by the clave column
 * @method UsuarioQuery groupByCorreo() Group by the correo column
 * @method UsuarioQuery groupByEstado() Group by the estado column
 * @method UsuarioQuery groupByImagen() Group by the imagen column
 * @method UsuarioQuery groupByAdministrador() Group by the administrador column
 * @method UsuarioQuery groupByUltimoIngreso() Group by the ultimo_ingreso column
 * @method UsuarioQuery groupByToken() Group by the token column
 * @method UsuarioQuery groupByActivo() Group by the activo column
 * @method UsuarioQuery groupByValidado() Group by the validado column
 * @method UsuarioQuery groupByNivelUsuario() Group by the nivel_usuario column
 * @method UsuarioQuery groupByIp() Group by the ip column
 * @method UsuarioQuery groupByTiendaId() Group by the tienda_id column
 * @method UsuarioQuery groupByEmpresaId() Group by the empresa_id column
 * @method UsuarioQuery groupByVendedorId() Group by the vendedor_id column
 *
 * @method UsuarioQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method UsuarioQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method UsuarioQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method UsuarioQuery leftJoinTienda($relationAlias = null) Adds a LEFT JOIN clause to the query using the Tienda relation
 * @method UsuarioQuery rightJoinTienda($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Tienda relation
 * @method UsuarioQuery innerJoinTienda($relationAlias = null) Adds a INNER JOIN clause to the query using the Tienda relation
 *
 * @method UsuarioQuery leftJoinEmpresa($relationAlias = null) Adds a LEFT JOIN clause to the query using the Empresa relation
 * @method UsuarioQuery rightJoinEmpresa($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Empresa relation
 * @method UsuarioQuery innerJoinEmpresa($relationAlias = null) Adds a INNER JOIN clause to the query using the Empresa relation
 *
 * @method UsuarioQuery leftJoinVendedor($relationAlias = null) Adds a LEFT JOIN clause to the query using the Vendedor relation
 * @method UsuarioQuery rightJoinVendedor($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Vendedor relation
 * @method UsuarioQuery innerJoinVendedor($relationAlias = null) Adds a INNER JOIN clause to the query using the Vendedor relation
 *
 * @method UsuarioQuery leftJoinUsuarioTienda($relationAlias = null) Adds a LEFT JOIN clause to the query using the UsuarioTienda relation
 * @method UsuarioQuery rightJoinUsuarioTienda($relationAlias = null) Adds a RIGHT JOIN clause to the query using the UsuarioTienda relation
 * @method UsuarioQuery innerJoinUsuarioTienda($relationAlias = null) Adds a INNER JOIN clause to the query using the UsuarioTienda relation
 *
 * @method UsuarioQuery leftJoinUsuarioEmpresa($relationAlias = null) Adds a LEFT JOIN clause to the query using the UsuarioEmpresa relation
 * @method UsuarioQuery rightJoinUsuarioEmpresa($relationAlias = null) Adds a RIGHT JOIN clause to the query using the UsuarioEmpresa relation
 * @method UsuarioQuery innerJoinUsuarioEmpresa($relationAlias = null) Adds a INNER JOIN clause to the query using the UsuarioEmpresa relation
 *
 * @method UsuarioQuery leftJoinUsuarioPerfil($relationAlias = null) Adds a LEFT JOIN clause to the query using the UsuarioPerfil relation
 * @method UsuarioQuery rightJoinUsuarioPerfil($relationAlias = null) Adds a RIGHT JOIN clause to the query using the UsuarioPerfil relation
 * @method UsuarioQuery innerJoinUsuarioPerfil($relationAlias = null) Adds a INNER JOIN clause to the query using the UsuarioPerfil relation
 *
 * @method UsuarioQuery leftJoinIngresoProductoPro($relationAlias = null) Adds a LEFT JOIN clause to the query using the IngresoProductoPro relation
 * @method UsuarioQuery rightJoinIngresoProductoPro($relationAlias = null) Adds a RIGHT JOIN clause to the query using the IngresoProductoPro relation
 * @method UsuarioQuery innerJoinIngresoProductoPro($relationAlias = null) Adds a INNER JOIN clause to the query using the IngresoProductoPro relation
 *
 * @method UsuarioQuery leftJoinSalidaProducto($relationAlias = null) Adds a LEFT JOIN clause to the query using the SalidaProducto relation
 * @method UsuarioQuery rightJoinSalidaProducto($relationAlias = null) Adds a RIGHT JOIN clause to the query using the SalidaProducto relation
 * @method UsuarioQuery innerJoinSalidaProducto($relationAlias = null) Adds a INNER JOIN clause to the query using the SalidaProducto relation
 *
 * @method UsuarioQuery leftJoinSalidaProductoDetalle($relationAlias = null) Adds a LEFT JOIN clause to the query using the SalidaProductoDetalle relation
 * @method UsuarioQuery rightJoinSalidaProductoDetalle($relationAlias = null) Adds a RIGHT JOIN clause to the query using the SalidaProductoDetalle relation
 * @method UsuarioQuery innerJoinSalidaProductoDetalle($relationAlias = null) Adds a INNER JOIN clause to the query using the SalidaProductoDetalle relation
 *
 * @method UsuarioQuery leftJoinUsuarioCorteValor($relationAlias = null) Adds a LEFT JOIN clause to the query using the UsuarioCorteValor relation
 * @method UsuarioQuery rightJoinUsuarioCorteValor($relationAlias = null) Adds a RIGHT JOIN clause to the query using the UsuarioCorteValor relation
 * @method UsuarioQuery innerJoinUsuarioCorteValor($relationAlias = null) Adds a INNER JOIN clause to the query using the UsuarioCorteValor relation
 *
 * @method UsuarioQuery leftJoinIngresoProducto($relationAlias = null) Adds a LEFT JOIN clause to the query using the IngresoProducto relation
 * @method UsuarioQuery rightJoinIngresoProducto($relationAlias = null) Adds a RIGHT JOIN clause to the query using the IngresoProducto relation
 * @method UsuarioQuery innerJoinIngresoProducto($relationAlias = null) Adds a INNER JOIN clause to the query using the IngresoProducto relation
 *
 * @method UsuarioQuery leftJoinPrecioProducto($relationAlias = null) Adds a LEFT JOIN clause to the query using the PrecioProducto relation
 * @method UsuarioQuery rightJoinPrecioProducto($relationAlias = null) Adds a RIGHT JOIN clause to the query using the PrecioProducto relation
 * @method UsuarioQuery innerJoinPrecioProducto($relationAlias = null) Adds a INNER JOIN clause to the query using the PrecioProducto relation
 *
 * @method Usuario findOne(PropelPDO $con = null) Return the first Usuario matching the query
 * @method Usuario findOneOrCreate(PropelPDO $con = null) Return the first Usuario matching the query, or a new Usuario object populated from the query conditions when no match is found
 *
 * @method Usuario findOneById(int $id) Return the first Usuario filtered by the id column
 * @method Usuario findOneByUsuario(string $usuario) Return the first Usuario filtered by the usuario column
 * @method Usuario findOneByNombreCompleto(string $nombre_completo) Return the first Usuario filtered by the nombre_completo column
 * @method Usuario findOneByTipoUsuario(string $tipo_usuario) Return the first Usuario filtered by the tipo_usuario column
 * @method Usuario findOneByClave(string $clave) Return the first Usuario filtered by the clave column
 * @method Usuario findOneByCorreo(string $correo) Return the first Usuario filtered by the correo column
 * @method Usuario findOneByEstado(string $estado) Return the first Usuario filtered by the estado column
 * @method Usuario findOneByImagen(string $imagen) Return the first Usuario filtered by the imagen column
 * @method Usuario findOneByAdministrador(boolean $administrador) Return the first Usuario filtered by the administrador column
 * @method Usuario findOneByUltimoIngreso(string $ultimo_ingreso) Return the first Usuario filtered by the ultimo_ingreso column
 * @method Usuario findOneByToken(string $token) Return the first Usuario filtered by the token column
 * @method Usuario findOneByActivo(boolean $activo) Return the first Usuario filtered by the activo column
 * @method Usuario findOneByValidado(boolean $validado) Return the first Usuario filtered by the validado column
 * @method Usuario findOneByNivelUsuario(string $nivel_usuario) Return the first Usuario filtered by the nivel_usuario column
 * @method Usuario findOneByIp(string $ip) Return the first Usuario filtered by the ip column
 * @method Usuario findOneByTiendaId(int $tienda_id) Return the first Usuario filtered by the tienda_id column
 * @method Usuario findOneByEmpresaId(int $empresa_id) Return the first Usuario filtered by the empresa_id column
 * @method Usuario findOneByVendedorId(int $vendedor_id) Return the first Usuario filtered by the vendedor_id column
 *
 * @method array findById(int $id) Return Usuario objects filtered by the id column
 * @method array findByUsuario(string $usuario) Return Usuario objects filtered by the usuario column
 * @method array findByNombreCompleto(string $nombre_completo) Return Usuario objects filtered by the nombre_completo column
 * @method array findByTipoUsuario(string $tipo_usuario) Return Usuario objects filtered by the tipo_usuario column
 * @method array findByClave(string $clave) Return Usuario objects filtered by the clave column
 * @method array findByCorreo(string $correo) Return Usuario objects filtered by the correo column
 * @method array findByEstado(string $estado) Return Usuario objects filtered by the estado column
 * @method array findByImagen(string $imagen) Return Usuario objects filtered by the imagen column
 * @method array findByAdministrador(boolean $administrador) Return Usuario objects filtered by the administrador column
 * @method array findByUltimoIngreso(string $ultimo_ingreso) Return Usuario objects filtered by the ultimo_ingreso column
 * @method array findByToken(string $token) Return Usuario objects filtered by the token column
 * @method array findByActivo(boolean $activo) Return Usuario objects filtered by the activo column
 * @method array findByValidado(boolean $validado) Return Usuario objects filtered by the validado column
 * @method array findByNivelUsuario(string $nivel_usuario) Return Usuario objects filtered by the nivel_usuario column
 * @method array findByIp(string $ip) Return Usuario objects filtered by the ip column
 * @method array findByTiendaId(int $tienda_id) Return Usuario objects filtered by the tienda_id column
 * @method array findByEmpresaId(int $empresa_id) Return Usuario objects filtered by the empresa_id column
 * @method array findByVendedorId(int $vendedor_id) Return Usuario objects filtered by the vendedor_id column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseUsuarioQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseUsuarioQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = 'propel', $modelName = 'Usuario', $modelAlias = null)
    {
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new UsuarioQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param     UsuarioQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return UsuarioQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof UsuarioQuery) {
            return $criteria;
        }
        $query = new UsuarioQuery();
        if (null !== $modelAlias) {
            $query->setModelAlias($modelAlias);
        }
        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return   Usuario|Usuario[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = UsuarioPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is alredy in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(UsuarioPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return   Usuario A model object, or null if the key is not found
     * @throws   PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT `ID`, `USUARIO`, `NOMBRE_COMPLETO`, `TIPO_USUARIO`, `CLAVE`, `CORREO`, `ESTADO`, `IMAGEN`, `ADMINISTRADOR`, `ULTIMO_INGRESO`, `TOKEN`, `ACTIVO`, `VALIDADO`, `NIVEL_USUARIO`, `IP`, `TIENDA_ID`, `EMPRESA_ID`, `VENDEDOR_ID` FROM `usuario` WHERE `ID` = :p0';
        try {
            $stmt = $con->prepare($sql);
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new Usuario();
            $obj->hydrate($row);
            UsuarioPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return Usuario|Usuario[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|Usuario[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return UsuarioQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(UsuarioPeer::ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return UsuarioQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(UsuarioPeer::ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the id column
     *
     * Example usage:
     * <code>
     * $query->filterById(1234); // WHERE id = 1234
     * $query->filterById(array(12, 34)); // WHERE id IN (12, 34)
     * $query->filterById(array('min' => 12)); // WHERE id > 12
     * </code>
     *
     * @param     mixed $id The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return UsuarioQuery The current query, for fluid interface
     */
    public function filterById($id = null, $comparison = null)
    {
        if (is_array($id) && null === $comparison) {
            $comparison = Criteria::IN;
        }

        return $this->addUsingAlias(UsuarioPeer::ID, $id, $comparison);
    }

    /**
     * Filter the query on the usuario column
     *
     * Example usage:
     * <code>
     * $query->filterByUsuario('fooValue');   // WHERE usuario = 'fooValue'
     * $query->filterByUsuario('%fooValue%'); // WHERE usuario LIKE '%fooValue%'
     * </code>
     *
     * @param     string $usuario The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return UsuarioQuery The current query, for fluid interface
     */
    public function filterByUsuario($usuario = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($usuario)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $usuario)) {
                $usuario = str_replace('*', '%', $usuario);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(UsuarioPeer::USUARIO, $usuario, $comparison);
    }

    /**
     * Filter the query on the nombre_completo column
     *
     * Example usage:
     * <code>
     * $query->filterByNombreCompleto('fooValue');   // WHERE nombre_completo = 'fooValue'
     * $query->filterByNombreCompleto('%fooValue%'); // WHERE nombre_completo LIKE '%fooValue%'
     * </code>
     *
     * @param     string $nombreCompleto The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return UsuarioQuery The current query, for fluid interface
     */
    public function filterByNombreCompleto($nombreCompleto = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($nombreCompleto)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $nombreCompleto)) {
                $nombreCompleto = str_replace('*', '%', $nombreCompleto);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(UsuarioPeer::NOMBRE_COMPLETO, $nombreCompleto, $comparison);
    }

    /**
     * Filter the query on the tipo_usuario column
     *
     * Example usage:
     * <code>
     * $query->filterByTipoUsuario('fooValue');   // WHERE tipo_usuario = 'fooValue'
     * $query->filterByTipoUsuario('%fooValue%'); // WHERE tipo_usuario LIKE '%fooValue%'
     * </code>
     *
     * @param     string $tipoUsuario The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return UsuarioQuery The current query, for fluid interface
     */
    public function filterByTipoUsuario($tipoUsuario = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($tipoUsuario)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $tipoUsuario)) {
                $tipoUsuario = str_replace('*', '%', $tipoUsuario);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(UsuarioPeer::TIPO_USUARIO, $tipoUsuario, $comparison);
    }

    /**
     * Filter the query on the clave column
     *
     * Example usage:
     * <code>
     * $query->filterByClave('fooValue');   // WHERE clave = 'fooValue'
     * $query->filterByClave('%fooValue%'); // WHERE clave LIKE '%fooValue%'
     * </code>
     *
     * @param     string $clave The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return UsuarioQuery The current query, for fluid interface
     */
    public function filterByClave($clave = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($clave)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $clave)) {
                $clave = str_replace('*', '%', $clave);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(UsuarioPeer::CLAVE, $clave, $comparison);
    }

    /**
     * Filter the query on the correo column
     *
     * Example usage:
     * <code>
     * $query->filterByCorreo('fooValue');   // WHERE correo = 'fooValue'
     * $query->filterByCorreo('%fooValue%'); // WHERE correo LIKE '%fooValue%'
     * </code>
     *
     * @param     string $correo The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return UsuarioQuery The current query, for fluid interface
     */
    public function filterByCorreo($correo = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($correo)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $correo)) {
                $correo = str_replace('*', '%', $correo);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(UsuarioPeer::CORREO, $correo, $comparison);
    }

    /**
     * Filter the query on the estado column
     *
     * Example usage:
     * <code>
     * $query->filterByEstado('fooValue');   // WHERE estado = 'fooValue'
     * $query->filterByEstado('%fooValue%'); // WHERE estado LIKE '%fooValue%'
     * </code>
     *
     * @param     string $estado The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return UsuarioQuery The current query, for fluid interface
     */
    public function filterByEstado($estado = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($estado)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $estado)) {
                $estado = str_replace('*', '%', $estado);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(UsuarioPeer::ESTADO, $estado, $comparison);
    }

    /**
     * Filter the query on the imagen column
     *
     * Example usage:
     * <code>
     * $query->filterByImagen('fooValue');   // WHERE imagen = 'fooValue'
     * $query->filterByImagen('%fooValue%'); // WHERE imagen LIKE '%fooValue%'
     * </code>
     *
     * @param     string $imagen The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return UsuarioQuery The current query, for fluid interface
     */
    public function filterByImagen($imagen = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($imagen)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $imagen)) {
                $imagen = str_replace('*', '%', $imagen);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(UsuarioPeer::IMAGEN, $imagen, $comparison);
    }

    /**
     * Filter the query on the administrador column
     *
     * Example usage:
     * <code>
     * $query->filterByAdministrador(true); // WHERE administrador = true
     * $query->filterByAdministrador('yes'); // WHERE administrador = true
     * </code>
     *
     * @param     boolean|string $administrador The value to use as filter.
     *              Non-boolean arguments are converted using the following rules:
     *                * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *                * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     *              Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return UsuarioQuery The current query, for fluid interface
     */
    public function filterByAdministrador($administrador = null, $comparison = null)
    {
        if (is_string($administrador)) {
            $administrador = in_array(strtolower($administrador), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
        }

        return $this->addUsingAlias(UsuarioPeer::ADMINISTRADOR, $administrador, $comparison);
    }

    /**
     * Filter the query on the ultimo_ingreso column
     *
     * Example usage:
     * <code>
     * $query->filterByUltimoIngreso('2011-03-14'); // WHERE ultimo_ingreso = '2011-03-14'
     * $query->filterByUltimoIngreso('now'); // WHERE ultimo_ingreso = '2011-03-14'
     * $query->filterByUltimoIngreso(array('max' => 'yesterday')); // WHERE ultimo_ingreso > '2011-03-13'
     * </code>
     *
     * @param     mixed $ultimoIngreso The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return UsuarioQuery The current query, for fluid interface
     */
    public function filterByUltimoIngreso($ultimoIngreso = null, $comparison = null)
    {
        if (is_array($ultimoIngreso)) {
            $useMinMax = false;
            if (isset($ultimoIngreso['min'])) {
                $this->addUsingAlias(UsuarioPeer::ULTIMO_INGRESO, $ultimoIngreso['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($ultimoIngreso['max'])) {
                $this->addUsingAlias(UsuarioPeer::ULTIMO_INGRESO, $ultimoIngreso['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(UsuarioPeer::ULTIMO_INGRESO, $ultimoIngreso, $comparison);
    }

    /**
     * Filter the query on the token column
     *
     * Example usage:
     * <code>
     * $query->filterByToken('fooValue');   // WHERE token = 'fooValue'
     * $query->filterByToken('%fooValue%'); // WHERE token LIKE '%fooValue%'
     * </code>
     *
     * @param     string $token The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return UsuarioQuery The current query, for fluid interface
     */
    public function filterByToken($token = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($token)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $token)) {
                $token = str_replace('*', '%', $token);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(UsuarioPeer::TOKEN, $token, $comparison);
    }

    /**
     * Filter the query on the activo column
     *
     * Example usage:
     * <code>
     * $query->filterByActivo(true); // WHERE activo = true
     * $query->filterByActivo('yes'); // WHERE activo = true
     * </code>
     *
     * @param     boolean|string $activo The value to use as filter.
     *              Non-boolean arguments are converted using the following rules:
     *                * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *                * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     *              Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return UsuarioQuery The current query, for fluid interface
     */
    public function filterByActivo($activo = null, $comparison = null)
    {
        if (is_string($activo)) {
            $activo = in_array(strtolower($activo), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
        }

        return $this->addUsingAlias(UsuarioPeer::ACTIVO, $activo, $comparison);
    }

    /**
     * Filter the query on the validado column
     *
     * Example usage:
     * <code>
     * $query->filterByValidado(true); // WHERE validado = true
     * $query->filterByValidado('yes'); // WHERE validado = true
     * </code>
     *
     * @param     boolean|string $validado The value to use as filter.
     *              Non-boolean arguments are converted using the following rules:
     *                * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *                * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     *              Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return UsuarioQuery The current query, for fluid interface
     */
    public function filterByValidado($validado = null, $comparison = null)
    {
        if (is_string($validado)) {
            $validado = in_array(strtolower($validado), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
        }

        return $this->addUsingAlias(UsuarioPeer::VALIDADO, $validado, $comparison);
    }

    /**
     * Filter the query on the nivel_usuario column
     *
     * Example usage:
     * <code>
     * $query->filterByNivelUsuario('fooValue');   // WHERE nivel_usuario = 'fooValue'
     * $query->filterByNivelUsuario('%fooValue%'); // WHERE nivel_usuario LIKE '%fooValue%'
     * </code>
     *
     * @param     string $nivelUsuario The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return UsuarioQuery The current query, for fluid interface
     */
    public function filterByNivelUsuario($nivelUsuario = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($nivelUsuario)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $nivelUsuario)) {
                $nivelUsuario = str_replace('*', '%', $nivelUsuario);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(UsuarioPeer::NIVEL_USUARIO, $nivelUsuario, $comparison);
    }

    /**
     * Filter the query on the ip column
     *
     * Example usage:
     * <code>
     * $query->filterByIp('fooValue');   // WHERE ip = 'fooValue'
     * $query->filterByIp('%fooValue%'); // WHERE ip LIKE '%fooValue%'
     * </code>
     *
     * @param     string $ip The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return UsuarioQuery The current query, for fluid interface
     */
    public function filterByIp($ip = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($ip)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $ip)) {
                $ip = str_replace('*', '%', $ip);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(UsuarioPeer::IP, $ip, $comparison);
    }

    /**
     * Filter the query on the tienda_id column
     *
     * Example usage:
     * <code>
     * $query->filterByTiendaId(1234); // WHERE tienda_id = 1234
     * $query->filterByTiendaId(array(12, 34)); // WHERE tienda_id IN (12, 34)
     * $query->filterByTiendaId(array('min' => 12)); // WHERE tienda_id > 12
     * </code>
     *
     * @see       filterByTienda()
     *
     * @param     mixed $tiendaId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return UsuarioQuery The current query, for fluid interface
     */
    public function filterByTiendaId($tiendaId = null, $comparison = null)
    {
        if (is_array($tiendaId)) {
            $useMinMax = false;
            if (isset($tiendaId['min'])) {
                $this->addUsingAlias(UsuarioPeer::TIENDA_ID, $tiendaId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($tiendaId['max'])) {
                $this->addUsingAlias(UsuarioPeer::TIENDA_ID, $tiendaId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(UsuarioPeer::TIENDA_ID, $tiendaId, $comparison);
    }

    /**
     * Filter the query on the empresa_id column
     *
     * Example usage:
     * <code>
     * $query->filterByEmpresaId(1234); // WHERE empresa_id = 1234
     * $query->filterByEmpresaId(array(12, 34)); // WHERE empresa_id IN (12, 34)
     * $query->filterByEmpresaId(array('min' => 12)); // WHERE empresa_id > 12
     * </code>
     *
     * @see       filterByEmpresa()
     *
     * @param     mixed $empresaId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return UsuarioQuery The current query, for fluid interface
     */
    public function filterByEmpresaId($empresaId = null, $comparison = null)
    {
        if (is_array($empresaId)) {
            $useMinMax = false;
            if (isset($empresaId['min'])) {
                $this->addUsingAlias(UsuarioPeer::EMPRESA_ID, $empresaId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($empresaId['max'])) {
                $this->addUsingAlias(UsuarioPeer::EMPRESA_ID, $empresaId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(UsuarioPeer::EMPRESA_ID, $empresaId, $comparison);
    }

    /**
     * Filter the query on the vendedor_id column
     *
     * Example usage:
     * <code>
     * $query->filterByVendedorId(1234); // WHERE vendedor_id = 1234
     * $query->filterByVendedorId(array(12, 34)); // WHERE vendedor_id IN (12, 34)
     * $query->filterByVendedorId(array('min' => 12)); // WHERE vendedor_id > 12
     * </code>
     *
     * @see       filterByVendedor()
     *
     * @param     mixed $vendedorId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return UsuarioQuery The current query, for fluid interface
     */
    public function filterByVendedorId($vendedorId = null, $comparison = null)
    {
        if (is_array($vendedorId)) {
            $useMinMax = false;
            if (isset($vendedorId['min'])) {
                $this->addUsingAlias(UsuarioPeer::VENDEDOR_ID, $vendedorId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($vendedorId['max'])) {
                $this->addUsingAlias(UsuarioPeer::VENDEDOR_ID, $vendedorId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(UsuarioPeer::VENDEDOR_ID, $vendedorId, $comparison);
    }

    /**
     * Filter the query by a related Tienda object
     *
     * @param   Tienda|PropelObjectCollection $tienda The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   UsuarioQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByTienda($tienda, $comparison = null)
    {
        if ($tienda instanceof Tienda) {
            return $this
                ->addUsingAlias(UsuarioPeer::TIENDA_ID, $tienda->getId(), $comparison);
        } elseif ($tienda instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(UsuarioPeer::TIENDA_ID, $tienda->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByTienda() only accepts arguments of type Tienda or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Tienda relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return UsuarioQuery The current query, for fluid interface
     */
    public function joinTienda($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Tienda');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Tienda');
        }

        return $this;
    }

    /**
     * Use the Tienda relation Tienda object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   TiendaQuery A secondary query class using the current class as primary query
     */
    public function useTiendaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinTienda($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Tienda', 'TiendaQuery');
    }

    /**
     * Filter the query by a related Empresa object
     *
     * @param   Empresa|PropelObjectCollection $empresa The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   UsuarioQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByEmpresa($empresa, $comparison = null)
    {
        if ($empresa instanceof Empresa) {
            return $this
                ->addUsingAlias(UsuarioPeer::EMPRESA_ID, $empresa->getId(), $comparison);
        } elseif ($empresa instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(UsuarioPeer::EMPRESA_ID, $empresa->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByEmpresa() only accepts arguments of type Empresa or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Empresa relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return UsuarioQuery The current query, for fluid interface
     */
    public function joinEmpresa($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Empresa');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Empresa');
        }

        return $this;
    }

    /**
     * Use the Empresa relation Empresa object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   EmpresaQuery A secondary query class using the current class as primary query
     */
    public function useEmpresaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinEmpresa($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Empresa', 'EmpresaQuery');
    }

    /**
     * Filter the query by a related Vendedor object
     *
     * @param   Vendedor|PropelObjectCollection $vendedor The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   UsuarioQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByVendedor($vendedor, $comparison = null)
    {
        if ($vendedor instanceof Vendedor) {
            return $this
                ->addUsingAlias(UsuarioPeer::VENDEDOR_ID, $vendedor->getId(), $comparison);
        } elseif ($vendedor instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(UsuarioPeer::VENDEDOR_ID, $vendedor->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByVendedor() only accepts arguments of type Vendedor or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Vendedor relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return UsuarioQuery The current query, for fluid interface
     */
    public function joinVendedor($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Vendedor');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Vendedor');
        }

        return $this;
    }

    /**
     * Use the Vendedor relation Vendedor object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   VendedorQuery A secondary query class using the current class as primary query
     */
    public function useVendedorQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinVendedor($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Vendedor', 'VendedorQuery');
    }

    /**
     * Filter the query by a related UsuarioTienda object
     *
     * @param   UsuarioTienda|PropelObjectCollection $usuarioTienda  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   UsuarioQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByUsuarioTienda($usuarioTienda, $comparison = null)
    {
        if ($usuarioTienda instanceof UsuarioTienda) {
            return $this
                ->addUsingAlias(UsuarioPeer::ID, $usuarioTienda->getUsuarioId(), $comparison);
        } elseif ($usuarioTienda instanceof PropelObjectCollection) {
            return $this
                ->useUsuarioTiendaQuery()
                ->filterByPrimaryKeys($usuarioTienda->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByUsuarioTienda() only accepts arguments of type UsuarioTienda or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the UsuarioTienda relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return UsuarioQuery The current query, for fluid interface
     */
    public function joinUsuarioTienda($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('UsuarioTienda');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'UsuarioTienda');
        }

        return $this;
    }

    /**
     * Use the UsuarioTienda relation UsuarioTienda object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   UsuarioTiendaQuery A secondary query class using the current class as primary query
     */
    public function useUsuarioTiendaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinUsuarioTienda($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'UsuarioTienda', 'UsuarioTiendaQuery');
    }

    /**
     * Filter the query by a related UsuarioEmpresa object
     *
     * @param   UsuarioEmpresa|PropelObjectCollection $usuarioEmpresa  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   UsuarioQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByUsuarioEmpresa($usuarioEmpresa, $comparison = null)
    {
        if ($usuarioEmpresa instanceof UsuarioEmpresa) {
            return $this
                ->addUsingAlias(UsuarioPeer::ID, $usuarioEmpresa->getUsuarioId(), $comparison);
        } elseif ($usuarioEmpresa instanceof PropelObjectCollection) {
            return $this
                ->useUsuarioEmpresaQuery()
                ->filterByPrimaryKeys($usuarioEmpresa->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByUsuarioEmpresa() only accepts arguments of type UsuarioEmpresa or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the UsuarioEmpresa relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return UsuarioQuery The current query, for fluid interface
     */
    public function joinUsuarioEmpresa($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('UsuarioEmpresa');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'UsuarioEmpresa');
        }

        return $this;
    }

    /**
     * Use the UsuarioEmpresa relation UsuarioEmpresa object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   UsuarioEmpresaQuery A secondary query class using the current class as primary query
     */
    public function useUsuarioEmpresaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinUsuarioEmpresa($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'UsuarioEmpresa', 'UsuarioEmpresaQuery');
    }

    /**
     * Filter the query by a related UsuarioPerfil object
     *
     * @param   UsuarioPerfil|PropelObjectCollection $usuarioPerfil  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   UsuarioQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByUsuarioPerfil($usuarioPerfil, $comparison = null)
    {
        if ($usuarioPerfil instanceof UsuarioPerfil) {
            return $this
                ->addUsingAlias(UsuarioPeer::ID, $usuarioPerfil->getUsuarioId(), $comparison);
        } elseif ($usuarioPerfil instanceof PropelObjectCollection) {
            return $this
                ->useUsuarioPerfilQuery()
                ->filterByPrimaryKeys($usuarioPerfil->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByUsuarioPerfil() only accepts arguments of type UsuarioPerfil or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the UsuarioPerfil relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return UsuarioQuery The current query, for fluid interface
     */
    public function joinUsuarioPerfil($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('UsuarioPerfil');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'UsuarioPerfil');
        }

        return $this;
    }

    /**
     * Use the UsuarioPerfil relation UsuarioPerfil object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   UsuarioPerfilQuery A secondary query class using the current class as primary query
     */
    public function useUsuarioPerfilQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinUsuarioPerfil($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'UsuarioPerfil', 'UsuarioPerfilQuery');
    }

    /**
     * Filter the query by a related IngresoProductoPro object
     *
     * @param   IngresoProductoPro|PropelObjectCollection $ingresoProductoPro  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   UsuarioQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByIngresoProductoPro($ingresoProductoPro, $comparison = null)
    {
        if ($ingresoProductoPro instanceof IngresoProductoPro) {
            return $this
                ->addUsingAlias(UsuarioPeer::ID, $ingresoProductoPro->getUsuarioId(), $comparison);
        } elseif ($ingresoProductoPro instanceof PropelObjectCollection) {
            return $this
                ->useIngresoProductoProQuery()
                ->filterByPrimaryKeys($ingresoProductoPro->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByIngresoProductoPro() only accepts arguments of type IngresoProductoPro or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the IngresoProductoPro relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return UsuarioQuery The current query, for fluid interface
     */
    public function joinIngresoProductoPro($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('IngresoProductoPro');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'IngresoProductoPro');
        }

        return $this;
    }

    /**
     * Use the IngresoProductoPro relation IngresoProductoPro object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   IngresoProductoProQuery A secondary query class using the current class as primary query
     */
    public function useIngresoProductoProQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinIngresoProductoPro($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'IngresoProductoPro', 'IngresoProductoProQuery');
    }

    /**
     * Filter the query by a related SalidaProducto object
     *
     * @param   SalidaProducto|PropelObjectCollection $salidaProducto  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   UsuarioQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterBySalidaProducto($salidaProducto, $comparison = null)
    {
        if ($salidaProducto instanceof SalidaProducto) {
            return $this
                ->addUsingAlias(UsuarioPeer::ID, $salidaProducto->getUsuarioId(), $comparison);
        } elseif ($salidaProducto instanceof PropelObjectCollection) {
            return $this
                ->useSalidaProductoQuery()
                ->filterByPrimaryKeys($salidaProducto->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterBySalidaProducto() only accepts arguments of type SalidaProducto or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the SalidaProducto relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return UsuarioQuery The current query, for fluid interface
     */
    public function joinSalidaProducto($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('SalidaProducto');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'SalidaProducto');
        }

        return $this;
    }

    /**
     * Use the SalidaProducto relation SalidaProducto object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   SalidaProductoQuery A secondary query class using the current class as primary query
     */
    public function useSalidaProductoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinSalidaProducto($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'SalidaProducto', 'SalidaProductoQuery');
    }

    /**
     * Filter the query by a related SalidaProductoDetalle object
     *
     * @param   SalidaProductoDetalle|PropelObjectCollection $salidaProductoDetalle  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   UsuarioQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterBySalidaProductoDetalle($salidaProductoDetalle, $comparison = null)
    {
        if ($salidaProductoDetalle instanceof SalidaProductoDetalle) {
            return $this
                ->addUsingAlias(UsuarioPeer::ID, $salidaProductoDetalle->getUsuarioId(), $comparison);
        } elseif ($salidaProductoDetalle instanceof PropelObjectCollection) {
            return $this
                ->useSalidaProductoDetalleQuery()
                ->filterByPrimaryKeys($salidaProductoDetalle->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterBySalidaProductoDetalle() only accepts arguments of type SalidaProductoDetalle or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the SalidaProductoDetalle relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return UsuarioQuery The current query, for fluid interface
     */
    public function joinSalidaProductoDetalle($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('SalidaProductoDetalle');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'SalidaProductoDetalle');
        }

        return $this;
    }

    /**
     * Use the SalidaProductoDetalle relation SalidaProductoDetalle object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   SalidaProductoDetalleQuery A secondary query class using the current class as primary query
     */
    public function useSalidaProductoDetalleQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinSalidaProductoDetalle($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'SalidaProductoDetalle', 'SalidaProductoDetalleQuery');
    }

    /**
     * Filter the query by a related UsuarioCorteValor object
     *
     * @param   UsuarioCorteValor|PropelObjectCollection $usuarioCorteValor  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   UsuarioQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByUsuarioCorteValor($usuarioCorteValor, $comparison = null)
    {
        if ($usuarioCorteValor instanceof UsuarioCorteValor) {
            return $this
                ->addUsingAlias(UsuarioPeer::ID, $usuarioCorteValor->getUsuarioId(), $comparison);
        } elseif ($usuarioCorteValor instanceof PropelObjectCollection) {
            return $this
                ->useUsuarioCorteValorQuery()
                ->filterByPrimaryKeys($usuarioCorteValor->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByUsuarioCorteValor() only accepts arguments of type UsuarioCorteValor or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the UsuarioCorteValor relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return UsuarioQuery The current query, for fluid interface
     */
    public function joinUsuarioCorteValor($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('UsuarioCorteValor');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'UsuarioCorteValor');
        }

        return $this;
    }

    /**
     * Use the UsuarioCorteValor relation UsuarioCorteValor object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   UsuarioCorteValorQuery A secondary query class using the current class as primary query
     */
    public function useUsuarioCorteValorQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinUsuarioCorteValor($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'UsuarioCorteValor', 'UsuarioCorteValorQuery');
    }

    /**
     * Filter the query by a related IngresoProducto object
     *
     * @param   IngresoProducto|PropelObjectCollection $ingresoProducto  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   UsuarioQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByIngresoProducto($ingresoProducto, $comparison = null)
    {
        if ($ingresoProducto instanceof IngresoProducto) {
            return $this
                ->addUsingAlias(UsuarioPeer::ID, $ingresoProducto->getUsuarioId(), $comparison);
        } elseif ($ingresoProducto instanceof PropelObjectCollection) {
            return $this
                ->useIngresoProductoQuery()
                ->filterByPrimaryKeys($ingresoProducto->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByIngresoProducto() only accepts arguments of type IngresoProducto or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the IngresoProducto relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return UsuarioQuery The current query, for fluid interface
     */
    public function joinIngresoProducto($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('IngresoProducto');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'IngresoProducto');
        }

        return $this;
    }

    /**
     * Use the IngresoProducto relation IngresoProducto object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   IngresoProductoQuery A secondary query class using the current class as primary query
     */
    public function useIngresoProductoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinIngresoProducto($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'IngresoProducto', 'IngresoProductoQuery');
    }

    /**
     * Filter the query by a related PrecioProducto object
     *
     * @param   PrecioProducto|PropelObjectCollection $precioProducto  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   UsuarioQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByPrecioProducto($precioProducto, $comparison = null)
    {
        if ($precioProducto instanceof PrecioProducto) {
            return $this
                ->addUsingAlias(UsuarioPeer::ID, $precioProducto->getUsuarioId(), $comparison);
        } elseif ($precioProducto instanceof PropelObjectCollection) {
            return $this
                ->usePrecioProductoQuery()
                ->filterByPrimaryKeys($precioProducto->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByPrecioProducto() only accepts arguments of type PrecioProducto or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the PrecioProducto relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return UsuarioQuery The current query, for fluid interface
     */
    public function joinPrecioProducto($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('PrecioProducto');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'PrecioProducto');
        }

        return $this;
    }

    /**
     * Use the PrecioProducto relation PrecioProducto object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   PrecioProductoQuery A secondary query class using the current class as primary query
     */
    public function usePrecioProductoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinPrecioProducto($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'PrecioProducto', 'PrecioProductoQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   Usuario $usuario Object to remove from the list of results
     *
     * @return UsuarioQuery The current query, for fluid interface
     */
    public function prune($usuario = null)
    {
        if ($usuario) {
            $this->addUsingAlias(UsuarioPeer::ID, $usuario->getId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}
