<?php


/**
 * Base class that represents a query for the 'cierre_caja' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.6.7 on:
 *
 * 01/03/26 04:09:41
 *
 * @method CierreCajaQuery orderById($order = Criteria::ASC) Order by the id column
 * @method CierreCajaQuery orderByFechaCreo($order = Criteria::ASC) Order by the fecha_creo column
 * @method CierreCajaQuery orderByUsuario($order = Criteria::ASC) Order by the usuario column
 * @method CierreCajaQuery orderByFechaCalendario($order = Criteria::ASC) Order by the fecha_calendario column
 * @method CierreCajaQuery orderByValorTotal($order = Criteria::ASC) Order by the valor_total column
 * @method CierreCajaQuery orderByNoDocumentos($order = Criteria::ASC) Order by the no_documentos column
 * @method CierreCajaQuery orderByValorCaja($order = Criteria::ASC) Order by the valor_caja column
 * @method CierreCajaQuery orderByEstatus($order = Criteria::ASC) Order by the estatus column
 * @method CierreCajaQuery orderByInicio($order = Criteria::ASC) Order by the inicio column
 * @method CierreCajaQuery orderByFin($order = Criteria::ASC) Order by the fin column
 * @method CierreCajaQuery orderByEmpresaId($order = Criteria::ASC) Order by the empresa_id column
 * @method CierreCajaQuery orderByTiendaId($order = Criteria::ASC) Order by the tienda_id column
 * @method CierreCajaQuery orderByCierreDiaId($order = Criteria::ASC) Order by the cierre_dia_id column
 *
 * @method CierreCajaQuery groupById() Group by the id column
 * @method CierreCajaQuery groupByFechaCreo() Group by the fecha_creo column
 * @method CierreCajaQuery groupByUsuario() Group by the usuario column
 * @method CierreCajaQuery groupByFechaCalendario() Group by the fecha_calendario column
 * @method CierreCajaQuery groupByValorTotal() Group by the valor_total column
 * @method CierreCajaQuery groupByNoDocumentos() Group by the no_documentos column
 * @method CierreCajaQuery groupByValorCaja() Group by the valor_caja column
 * @method CierreCajaQuery groupByEstatus() Group by the estatus column
 * @method CierreCajaQuery groupByInicio() Group by the inicio column
 * @method CierreCajaQuery groupByFin() Group by the fin column
 * @method CierreCajaQuery groupByEmpresaId() Group by the empresa_id column
 * @method CierreCajaQuery groupByTiendaId() Group by the tienda_id column
 * @method CierreCajaQuery groupByCierreDiaId() Group by the cierre_dia_id column
 *
 * @method CierreCajaQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method CierreCajaQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method CierreCajaQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method CierreCajaQuery leftJoinEmpresa($relationAlias = null) Adds a LEFT JOIN clause to the query using the Empresa relation
 * @method CierreCajaQuery rightJoinEmpresa($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Empresa relation
 * @method CierreCajaQuery innerJoinEmpresa($relationAlias = null) Adds a INNER JOIN clause to the query using the Empresa relation
 *
 * @method CierreCajaQuery leftJoinTienda($relationAlias = null) Adds a LEFT JOIN clause to the query using the Tienda relation
 * @method CierreCajaQuery rightJoinTienda($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Tienda relation
 * @method CierreCajaQuery innerJoinTienda($relationAlias = null) Adds a INNER JOIN clause to the query using the Tienda relation
 *
 * @method CierreCajaQuery leftJoinCierre($relationAlias = null) Adds a LEFT JOIN clause to the query using the Cierre relation
 * @method CierreCajaQuery rightJoinCierre($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Cierre relation
 * @method CierreCajaQuery innerJoinCierre($relationAlias = null) Adds a INNER JOIN clause to the query using the Cierre relation
 *
 * @method CierreCajaQuery leftJoinCierreCajaValor($relationAlias = null) Adds a LEFT JOIN clause to the query using the CierreCajaValor relation
 * @method CierreCajaQuery rightJoinCierreCajaValor($relationAlias = null) Adds a RIGHT JOIN clause to the query using the CierreCajaValor relation
 * @method CierreCajaQuery innerJoinCierreCajaValor($relationAlias = null) Adds a INNER JOIN clause to the query using the CierreCajaValor relation
 *
 * @method CierreCajaQuery leftJoinCierreCheck($relationAlias = null) Adds a LEFT JOIN clause to the query using the CierreCheck relation
 * @method CierreCajaQuery rightJoinCierreCheck($relationAlias = null) Adds a RIGHT JOIN clause to the query using the CierreCheck relation
 * @method CierreCajaQuery innerJoinCierreCheck($relationAlias = null) Adds a INNER JOIN clause to the query using the CierreCheck relation
 *
 * @method CierreCaja findOne(PropelPDO $con = null) Return the first CierreCaja matching the query
 * @method CierreCaja findOneOrCreate(PropelPDO $con = null) Return the first CierreCaja matching the query, or a new CierreCaja object populated from the query conditions when no match is found
 *
 * @method CierreCaja findOneById(int $id) Return the first CierreCaja filtered by the id column
 * @method CierreCaja findOneByFechaCreo(string $fecha_creo) Return the first CierreCaja filtered by the fecha_creo column
 * @method CierreCaja findOneByUsuario(string $usuario) Return the first CierreCaja filtered by the usuario column
 * @method CierreCaja findOneByFechaCalendario(string $fecha_calendario) Return the first CierreCaja filtered by the fecha_calendario column
 * @method CierreCaja findOneByValorTotal(double $valor_total) Return the first CierreCaja filtered by the valor_total column
 * @method CierreCaja findOneByNoDocumentos(int $no_documentos) Return the first CierreCaja filtered by the no_documentos column
 * @method CierreCaja findOneByValorCaja(double $valor_caja) Return the first CierreCaja filtered by the valor_caja column
 * @method CierreCaja findOneByEstatus(string $estatus) Return the first CierreCaja filtered by the estatus column
 * @method CierreCaja findOneByInicio(string $inicio) Return the first CierreCaja filtered by the inicio column
 * @method CierreCaja findOneByFin(string $fin) Return the first CierreCaja filtered by the fin column
 * @method CierreCaja findOneByEmpresaId(int $empresa_id) Return the first CierreCaja filtered by the empresa_id column
 * @method CierreCaja findOneByTiendaId(int $tienda_id) Return the first CierreCaja filtered by the tienda_id column
 * @method CierreCaja findOneByCierreDiaId(int $cierre_dia_id) Return the first CierreCaja filtered by the cierre_dia_id column
 *
 * @method array findById(int $id) Return CierreCaja objects filtered by the id column
 * @method array findByFechaCreo(string $fecha_creo) Return CierreCaja objects filtered by the fecha_creo column
 * @method array findByUsuario(string $usuario) Return CierreCaja objects filtered by the usuario column
 * @method array findByFechaCalendario(string $fecha_calendario) Return CierreCaja objects filtered by the fecha_calendario column
 * @method array findByValorTotal(double $valor_total) Return CierreCaja objects filtered by the valor_total column
 * @method array findByNoDocumentos(int $no_documentos) Return CierreCaja objects filtered by the no_documentos column
 * @method array findByValorCaja(double $valor_caja) Return CierreCaja objects filtered by the valor_caja column
 * @method array findByEstatus(string $estatus) Return CierreCaja objects filtered by the estatus column
 * @method array findByInicio(string $inicio) Return CierreCaja objects filtered by the inicio column
 * @method array findByFin(string $fin) Return CierreCaja objects filtered by the fin column
 * @method array findByEmpresaId(int $empresa_id) Return CierreCaja objects filtered by the empresa_id column
 * @method array findByTiendaId(int $tienda_id) Return CierreCaja objects filtered by the tienda_id column
 * @method array findByCierreDiaId(int $cierre_dia_id) Return CierreCaja objects filtered by the cierre_dia_id column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseCierreCajaQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseCierreCajaQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = 'propel', $modelName = 'CierreCaja', $modelAlias = null)
    {
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new CierreCajaQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param     CierreCajaQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return CierreCajaQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof CierreCajaQuery) {
            return $criteria;
        }
        $query = new CierreCajaQuery();
        if (null !== $modelAlias) {
            $query->setModelAlias($modelAlias);
        }
        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return   CierreCaja|CierreCaja[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = CierreCajaPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is alredy in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(CierreCajaPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return   CierreCaja A model object, or null if the key is not found
     * @throws   PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT `ID`, `FECHA_CREO`, `USUARIO`, `FECHA_CALENDARIO`, `VALOR_TOTAL`, `NO_DOCUMENTOS`, `VALOR_CAJA`, `ESTATUS`, `INICIO`, `FIN`, `EMPRESA_ID`, `TIENDA_ID`, `CIERRE_DIA_ID` FROM `cierre_caja` WHERE `ID` = :p0';
        try {
            $stmt = $con->prepare($sql);
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new CierreCaja();
            $obj->hydrate($row);
            CierreCajaPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return CierreCaja|CierreCaja[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|CierreCaja[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return CierreCajaQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(CierreCajaPeer::ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return CierreCajaQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(CierreCajaPeer::ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the id column
     *
     * Example usage:
     * <code>
     * $query->filterById(1234); // WHERE id = 1234
     * $query->filterById(array(12, 34)); // WHERE id IN (12, 34)
     * $query->filterById(array('min' => 12)); // WHERE id > 12
     * </code>
     *
     * @param     mixed $id The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return CierreCajaQuery The current query, for fluid interface
     */
    public function filterById($id = null, $comparison = null)
    {
        if (is_array($id) && null === $comparison) {
            $comparison = Criteria::IN;
        }

        return $this->addUsingAlias(CierreCajaPeer::ID, $id, $comparison);
    }

    /**
     * Filter the query on the fecha_creo column
     *
     * Example usage:
     * <code>
     * $query->filterByFechaCreo('2011-03-14'); // WHERE fecha_creo = '2011-03-14'
     * $query->filterByFechaCreo('now'); // WHERE fecha_creo = '2011-03-14'
     * $query->filterByFechaCreo(array('max' => 'yesterday')); // WHERE fecha_creo > '2011-03-13'
     * </code>
     *
     * @param     mixed $fechaCreo The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return CierreCajaQuery The current query, for fluid interface
     */
    public function filterByFechaCreo($fechaCreo = null, $comparison = null)
    {
        if (is_array($fechaCreo)) {
            $useMinMax = false;
            if (isset($fechaCreo['min'])) {
                $this->addUsingAlias(CierreCajaPeer::FECHA_CREO, $fechaCreo['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($fechaCreo['max'])) {
                $this->addUsingAlias(CierreCajaPeer::FECHA_CREO, $fechaCreo['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(CierreCajaPeer::FECHA_CREO, $fechaCreo, $comparison);
    }

    /**
     * Filter the query on the usuario column
     *
     * Example usage:
     * <code>
     * $query->filterByUsuario('fooValue');   // WHERE usuario = 'fooValue'
     * $query->filterByUsuario('%fooValue%'); // WHERE usuario LIKE '%fooValue%'
     * </code>
     *
     * @param     string $usuario The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return CierreCajaQuery The current query, for fluid interface
     */
    public function filterByUsuario($usuario = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($usuario)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $usuario)) {
                $usuario = str_replace('*', '%', $usuario);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(CierreCajaPeer::USUARIO, $usuario, $comparison);
    }

    /**
     * Filter the query on the fecha_calendario column
     *
     * Example usage:
     * <code>
     * $query->filterByFechaCalendario('2011-03-14'); // WHERE fecha_calendario = '2011-03-14'
     * $query->filterByFechaCalendario('now'); // WHERE fecha_calendario = '2011-03-14'
     * $query->filterByFechaCalendario(array('max' => 'yesterday')); // WHERE fecha_calendario > '2011-03-13'
     * </code>
     *
     * @param     mixed $fechaCalendario The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return CierreCajaQuery The current query, for fluid interface
     */
    public function filterByFechaCalendario($fechaCalendario = null, $comparison = null)
    {
        if (is_array($fechaCalendario)) {
            $useMinMax = false;
            if (isset($fechaCalendario['min'])) {
                $this->addUsingAlias(CierreCajaPeer::FECHA_CALENDARIO, $fechaCalendario['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($fechaCalendario['max'])) {
                $this->addUsingAlias(CierreCajaPeer::FECHA_CALENDARIO, $fechaCalendario['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(CierreCajaPeer::FECHA_CALENDARIO, $fechaCalendario, $comparison);
    }

    /**
     * Filter the query on the valor_total column
     *
     * Example usage:
     * <code>
     * $query->filterByValorTotal(1234); // WHERE valor_total = 1234
     * $query->filterByValorTotal(array(12, 34)); // WHERE valor_total IN (12, 34)
     * $query->filterByValorTotal(array('min' => 12)); // WHERE valor_total > 12
     * </code>
     *
     * @param     mixed $valorTotal The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return CierreCajaQuery The current query, for fluid interface
     */
    public function filterByValorTotal($valorTotal = null, $comparison = null)
    {
        if (is_array($valorTotal)) {
            $useMinMax = false;
            if (isset($valorTotal['min'])) {
                $this->addUsingAlias(CierreCajaPeer::VALOR_TOTAL, $valorTotal['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($valorTotal['max'])) {
                $this->addUsingAlias(CierreCajaPeer::VALOR_TOTAL, $valorTotal['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(CierreCajaPeer::VALOR_TOTAL, $valorTotal, $comparison);
    }

    /**
     * Filter the query on the no_documentos column
     *
     * Example usage:
     * <code>
     * $query->filterByNoDocumentos(1234); // WHERE no_documentos = 1234
     * $query->filterByNoDocumentos(array(12, 34)); // WHERE no_documentos IN (12, 34)
     * $query->filterByNoDocumentos(array('min' => 12)); // WHERE no_documentos > 12
     * </code>
     *
     * @param     mixed $noDocumentos The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return CierreCajaQuery The current query, for fluid interface
     */
    public function filterByNoDocumentos($noDocumentos = null, $comparison = null)
    {
        if (is_array($noDocumentos)) {
            $useMinMax = false;
            if (isset($noDocumentos['min'])) {
                $this->addUsingAlias(CierreCajaPeer::NO_DOCUMENTOS, $noDocumentos['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($noDocumentos['max'])) {
                $this->addUsingAlias(CierreCajaPeer::NO_DOCUMENTOS, $noDocumentos['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(CierreCajaPeer::NO_DOCUMENTOS, $noDocumentos, $comparison);
    }

    /**
     * Filter the query on the valor_caja column
     *
     * Example usage:
     * <code>
     * $query->filterByValorCaja(1234); // WHERE valor_caja = 1234
     * $query->filterByValorCaja(array(12, 34)); // WHERE valor_caja IN (12, 34)
     * $query->filterByValorCaja(array('min' => 12)); // WHERE valor_caja > 12
     * </code>
     *
     * @param     mixed $valorCaja The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return CierreCajaQuery The current query, for fluid interface
     */
    public function filterByValorCaja($valorCaja = null, $comparison = null)
    {
        if (is_array($valorCaja)) {
            $useMinMax = false;
            if (isset($valorCaja['min'])) {
                $this->addUsingAlias(CierreCajaPeer::VALOR_CAJA, $valorCaja['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($valorCaja['max'])) {
                $this->addUsingAlias(CierreCajaPeer::VALOR_CAJA, $valorCaja['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(CierreCajaPeer::VALOR_CAJA, $valorCaja, $comparison);
    }

    /**
     * Filter the query on the estatus column
     *
     * Example usage:
     * <code>
     * $query->filterByEstatus('fooValue');   // WHERE estatus = 'fooValue'
     * $query->filterByEstatus('%fooValue%'); // WHERE estatus LIKE '%fooValue%'
     * </code>
     *
     * @param     string $estatus The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return CierreCajaQuery The current query, for fluid interface
     */
    public function filterByEstatus($estatus = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($estatus)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $estatus)) {
                $estatus = str_replace('*', '%', $estatus);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(CierreCajaPeer::ESTATUS, $estatus, $comparison);
    }

    /**
     * Filter the query on the inicio column
     *
     * Example usage:
     * <code>
     * $query->filterByInicio('2011-03-14'); // WHERE inicio = '2011-03-14'
     * $query->filterByInicio('now'); // WHERE inicio = '2011-03-14'
     * $query->filterByInicio(array('max' => 'yesterday')); // WHERE inicio > '2011-03-13'
     * </code>
     *
     * @param     mixed $inicio The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return CierreCajaQuery The current query, for fluid interface
     */
    public function filterByInicio($inicio = null, $comparison = null)
    {
        if (is_array($inicio)) {
            $useMinMax = false;
            if (isset($inicio['min'])) {
                $this->addUsingAlias(CierreCajaPeer::INICIO, $inicio['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($inicio['max'])) {
                $this->addUsingAlias(CierreCajaPeer::INICIO, $inicio['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(CierreCajaPeer::INICIO, $inicio, $comparison);
    }

    /**
     * Filter the query on the fin column
     *
     * Example usage:
     * <code>
     * $query->filterByFin('2011-03-14'); // WHERE fin = '2011-03-14'
     * $query->filterByFin('now'); // WHERE fin = '2011-03-14'
     * $query->filterByFin(array('max' => 'yesterday')); // WHERE fin > '2011-03-13'
     * </code>
     *
     * @param     mixed $fin The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return CierreCajaQuery The current query, for fluid interface
     */
    public function filterByFin($fin = null, $comparison = null)
    {
        if (is_array($fin)) {
            $useMinMax = false;
            if (isset($fin['min'])) {
                $this->addUsingAlias(CierreCajaPeer::FIN, $fin['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($fin['max'])) {
                $this->addUsingAlias(CierreCajaPeer::FIN, $fin['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(CierreCajaPeer::FIN, $fin, $comparison);
    }

    /**
     * Filter the query on the empresa_id column
     *
     * Example usage:
     * <code>
     * $query->filterByEmpresaId(1234); // WHERE empresa_id = 1234
     * $query->filterByEmpresaId(array(12, 34)); // WHERE empresa_id IN (12, 34)
     * $query->filterByEmpresaId(array('min' => 12)); // WHERE empresa_id > 12
     * </code>
     *
     * @see       filterByEmpresa()
     *
     * @param     mixed $empresaId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return CierreCajaQuery The current query, for fluid interface
     */
    public function filterByEmpresaId($empresaId = null, $comparison = null)
    {
        if (is_array($empresaId)) {
            $useMinMax = false;
            if (isset($empresaId['min'])) {
                $this->addUsingAlias(CierreCajaPeer::EMPRESA_ID, $empresaId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($empresaId['max'])) {
                $this->addUsingAlias(CierreCajaPeer::EMPRESA_ID, $empresaId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(CierreCajaPeer::EMPRESA_ID, $empresaId, $comparison);
    }

    /**
     * Filter the query on the tienda_id column
     *
     * Example usage:
     * <code>
     * $query->filterByTiendaId(1234); // WHERE tienda_id = 1234
     * $query->filterByTiendaId(array(12, 34)); // WHERE tienda_id IN (12, 34)
     * $query->filterByTiendaId(array('min' => 12)); // WHERE tienda_id > 12
     * </code>
     *
     * @see       filterByTienda()
     *
     * @param     mixed $tiendaId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return CierreCajaQuery The current query, for fluid interface
     */
    public function filterByTiendaId($tiendaId = null, $comparison = null)
    {
        if (is_array($tiendaId)) {
            $useMinMax = false;
            if (isset($tiendaId['min'])) {
                $this->addUsingAlias(CierreCajaPeer::TIENDA_ID, $tiendaId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($tiendaId['max'])) {
                $this->addUsingAlias(CierreCajaPeer::TIENDA_ID, $tiendaId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(CierreCajaPeer::TIENDA_ID, $tiendaId, $comparison);
    }

    /**
     * Filter the query on the cierre_dia_id column
     *
     * Example usage:
     * <code>
     * $query->filterByCierreDiaId(1234); // WHERE cierre_dia_id = 1234
     * $query->filterByCierreDiaId(array(12, 34)); // WHERE cierre_dia_id IN (12, 34)
     * $query->filterByCierreDiaId(array('min' => 12)); // WHERE cierre_dia_id > 12
     * </code>
     *
     * @see       filterByCierre()
     *
     * @param     mixed $cierreDiaId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return CierreCajaQuery The current query, for fluid interface
     */
    public function filterByCierreDiaId($cierreDiaId = null, $comparison = null)
    {
        if (is_array($cierreDiaId)) {
            $useMinMax = false;
            if (isset($cierreDiaId['min'])) {
                $this->addUsingAlias(CierreCajaPeer::CIERRE_DIA_ID, $cierreDiaId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($cierreDiaId['max'])) {
                $this->addUsingAlias(CierreCajaPeer::CIERRE_DIA_ID, $cierreDiaId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(CierreCajaPeer::CIERRE_DIA_ID, $cierreDiaId, $comparison);
    }

    /**
     * Filter the query by a related Empresa object
     *
     * @param   Empresa|PropelObjectCollection $empresa The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   CierreCajaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByEmpresa($empresa, $comparison = null)
    {
        if ($empresa instanceof Empresa) {
            return $this
                ->addUsingAlias(CierreCajaPeer::EMPRESA_ID, $empresa->getId(), $comparison);
        } elseif ($empresa instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(CierreCajaPeer::EMPRESA_ID, $empresa->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByEmpresa() only accepts arguments of type Empresa or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Empresa relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return CierreCajaQuery The current query, for fluid interface
     */
    public function joinEmpresa($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Empresa');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Empresa');
        }

        return $this;
    }

    /**
     * Use the Empresa relation Empresa object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   EmpresaQuery A secondary query class using the current class as primary query
     */
    public function useEmpresaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinEmpresa($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Empresa', 'EmpresaQuery');
    }

    /**
     * Filter the query by a related Tienda object
     *
     * @param   Tienda|PropelObjectCollection $tienda The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   CierreCajaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByTienda($tienda, $comparison = null)
    {
        if ($tienda instanceof Tienda) {
            return $this
                ->addUsingAlias(CierreCajaPeer::TIENDA_ID, $tienda->getId(), $comparison);
        } elseif ($tienda instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(CierreCajaPeer::TIENDA_ID, $tienda->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByTienda() only accepts arguments of type Tienda or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Tienda relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return CierreCajaQuery The current query, for fluid interface
     */
    public function joinTienda($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Tienda');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Tienda');
        }

        return $this;
    }

    /**
     * Use the Tienda relation Tienda object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   TiendaQuery A secondary query class using the current class as primary query
     */
    public function useTiendaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinTienda($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Tienda', 'TiendaQuery');
    }

    /**
     * Filter the query by a related Cierre object
     *
     * @param   Cierre|PropelObjectCollection $cierre The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   CierreCajaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByCierre($cierre, $comparison = null)
    {
        if ($cierre instanceof Cierre) {
            return $this
                ->addUsingAlias(CierreCajaPeer::CIERRE_DIA_ID, $cierre->getId(), $comparison);
        } elseif ($cierre instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(CierreCajaPeer::CIERRE_DIA_ID, $cierre->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByCierre() only accepts arguments of type Cierre or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Cierre relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return CierreCajaQuery The current query, for fluid interface
     */
    public function joinCierre($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Cierre');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Cierre');
        }

        return $this;
    }

    /**
     * Use the Cierre relation Cierre object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   CierreQuery A secondary query class using the current class as primary query
     */
    public function useCierreQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinCierre($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Cierre', 'CierreQuery');
    }

    /**
     * Filter the query by a related CierreCajaValor object
     *
     * @param   CierreCajaValor|PropelObjectCollection $cierreCajaValor  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   CierreCajaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByCierreCajaValor($cierreCajaValor, $comparison = null)
    {
        if ($cierreCajaValor instanceof CierreCajaValor) {
            return $this
                ->addUsingAlias(CierreCajaPeer::ID, $cierreCajaValor->getCierreCajaId(), $comparison);
        } elseif ($cierreCajaValor instanceof PropelObjectCollection) {
            return $this
                ->useCierreCajaValorQuery()
                ->filterByPrimaryKeys($cierreCajaValor->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByCierreCajaValor() only accepts arguments of type CierreCajaValor or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the CierreCajaValor relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return CierreCajaQuery The current query, for fluid interface
     */
    public function joinCierreCajaValor($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('CierreCajaValor');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'CierreCajaValor');
        }

        return $this;
    }

    /**
     * Use the CierreCajaValor relation CierreCajaValor object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   CierreCajaValorQuery A secondary query class using the current class as primary query
     */
    public function useCierreCajaValorQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinCierreCajaValor($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'CierreCajaValor', 'CierreCajaValorQuery');
    }

    /**
     * Filter the query by a related CierreCheck object
     *
     * @param   CierreCheck|PropelObjectCollection $cierreCheck  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   CierreCajaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByCierreCheck($cierreCheck, $comparison = null)
    {
        if ($cierreCheck instanceof CierreCheck) {
            return $this
                ->addUsingAlias(CierreCajaPeer::ID, $cierreCheck->getCierreCajaId(), $comparison);
        } elseif ($cierreCheck instanceof PropelObjectCollection) {
            return $this
                ->useCierreCheckQuery()
                ->filterByPrimaryKeys($cierreCheck->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByCierreCheck() only accepts arguments of type CierreCheck or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the CierreCheck relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return CierreCajaQuery The current query, for fluid interface
     */
    public function joinCierreCheck($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('CierreCheck');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'CierreCheck');
        }

        return $this;
    }

    /**
     * Use the CierreCheck relation CierreCheck object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   CierreCheckQuery A secondary query class using the current class as primary query
     */
    public function useCierreCheckQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinCierreCheck($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'CierreCheck', 'CierreCheckQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   CierreCaja $cierreCaja Object to remove from the list of results
     *
     * @return CierreCajaQuery The current query, for fluid interface
     */
    public function prune($cierreCaja = null)
    {
        if ($cierreCaja) {
            $this->addUsingAlias(CierreCajaPeer::ID, $cierreCaja->getId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}
