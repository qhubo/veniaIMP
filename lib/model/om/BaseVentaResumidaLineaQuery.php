<?php


/**
 * Base class that represents a query for the 'venta_resumida_linea' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.6.7 on:
 *
 * 01/03/26 04:09:44
 *
 * @method VentaResumidaLineaQuery orderById($order = Criteria::ASC) Order by the id column
 * @method VentaResumidaLineaQuery orderByVentaResumidaId($order = Criteria::ASC) Order by the venta_resumida_id column
 * @method VentaResumidaLineaQuery orderByNumeroCuotas($order = Criteria::ASC) Order by the numero_cuotas column
 * @method VentaResumidaLineaQuery orderByTotalLinea($order = Criteria::ASC) Order by the total_linea column
 * @method VentaResumidaLineaQuery orderByComisionLinea($order = Criteria::ASC) Order by the comision_linea column
 * @method VentaResumidaLineaQuery orderByIvaLinea($order = Criteria::ASC) Order by the iva_linea column
 * @method VentaResumidaLineaQuery orderByRetencionLinea($order = Criteria::ASC) Order by the retencion_linea column
 * @method VentaResumidaLineaQuery orderByMedioPagoId($order = Criteria::ASC) Order by the medio_pago_id column
 * @method VentaResumidaLineaQuery orderByEmpresaId($order = Criteria::ASC) Order by the empresa_id column
 * @method VentaResumidaLineaQuery orderByBancoId($order = Criteria::ASC) Order by the banco_id column
 *
 * @method VentaResumidaLineaQuery groupById() Group by the id column
 * @method VentaResumidaLineaQuery groupByVentaResumidaId() Group by the venta_resumida_id column
 * @method VentaResumidaLineaQuery groupByNumeroCuotas() Group by the numero_cuotas column
 * @method VentaResumidaLineaQuery groupByTotalLinea() Group by the total_linea column
 * @method VentaResumidaLineaQuery groupByComisionLinea() Group by the comision_linea column
 * @method VentaResumidaLineaQuery groupByIvaLinea() Group by the iva_linea column
 * @method VentaResumidaLineaQuery groupByRetencionLinea() Group by the retencion_linea column
 * @method VentaResumidaLineaQuery groupByMedioPagoId() Group by the medio_pago_id column
 * @method VentaResumidaLineaQuery groupByEmpresaId() Group by the empresa_id column
 * @method VentaResumidaLineaQuery groupByBancoId() Group by the banco_id column
 *
 * @method VentaResumidaLineaQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method VentaResumidaLineaQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method VentaResumidaLineaQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method VentaResumidaLineaQuery leftJoinVentaResumida($relationAlias = null) Adds a LEFT JOIN clause to the query using the VentaResumida relation
 * @method VentaResumidaLineaQuery rightJoinVentaResumida($relationAlias = null) Adds a RIGHT JOIN clause to the query using the VentaResumida relation
 * @method VentaResumidaLineaQuery innerJoinVentaResumida($relationAlias = null) Adds a INNER JOIN clause to the query using the VentaResumida relation
 *
 * @method VentaResumidaLineaQuery leftJoinMedioPago($relationAlias = null) Adds a LEFT JOIN clause to the query using the MedioPago relation
 * @method VentaResumidaLineaQuery rightJoinMedioPago($relationAlias = null) Adds a RIGHT JOIN clause to the query using the MedioPago relation
 * @method VentaResumidaLineaQuery innerJoinMedioPago($relationAlias = null) Adds a INNER JOIN clause to the query using the MedioPago relation
 *
 * @method VentaResumidaLineaQuery leftJoinEmpresa($relationAlias = null) Adds a LEFT JOIN clause to the query using the Empresa relation
 * @method VentaResumidaLineaQuery rightJoinEmpresa($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Empresa relation
 * @method VentaResumidaLineaQuery innerJoinEmpresa($relationAlias = null) Adds a INNER JOIN clause to the query using the Empresa relation
 *
 * @method VentaResumidaLineaQuery leftJoinBanco($relationAlias = null) Adds a LEFT JOIN clause to the query using the Banco relation
 * @method VentaResumidaLineaQuery rightJoinBanco($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Banco relation
 * @method VentaResumidaLineaQuery innerJoinBanco($relationAlias = null) Adds a INNER JOIN clause to the query using the Banco relation
 *
 * @method VentaResumidaLineaQuery leftJoinMovimientoBanco($relationAlias = null) Adds a LEFT JOIN clause to the query using the MovimientoBanco relation
 * @method VentaResumidaLineaQuery rightJoinMovimientoBanco($relationAlias = null) Adds a RIGHT JOIN clause to the query using the MovimientoBanco relation
 * @method VentaResumidaLineaQuery innerJoinMovimientoBanco($relationAlias = null) Adds a INNER JOIN clause to the query using the MovimientoBanco relation
 *
 * @method VentaResumidaLinea findOne(PropelPDO $con = null) Return the first VentaResumidaLinea matching the query
 * @method VentaResumidaLinea findOneOrCreate(PropelPDO $con = null) Return the first VentaResumidaLinea matching the query, or a new VentaResumidaLinea object populated from the query conditions when no match is found
 *
 * @method VentaResumidaLinea findOneById(int $id) Return the first VentaResumidaLinea filtered by the id column
 * @method VentaResumidaLinea findOneByVentaResumidaId(int $venta_resumida_id) Return the first VentaResumidaLinea filtered by the venta_resumida_id column
 * @method VentaResumidaLinea findOneByNumeroCuotas(int $numero_cuotas) Return the first VentaResumidaLinea filtered by the numero_cuotas column
 * @method VentaResumidaLinea findOneByTotalLinea(double $total_linea) Return the first VentaResumidaLinea filtered by the total_linea column
 * @method VentaResumidaLinea findOneByComisionLinea(double $comision_linea) Return the first VentaResumidaLinea filtered by the comision_linea column
 * @method VentaResumidaLinea findOneByIvaLinea(double $iva_linea) Return the first VentaResumidaLinea filtered by the iva_linea column
 * @method VentaResumidaLinea findOneByRetencionLinea(double $retencion_linea) Return the first VentaResumidaLinea filtered by the retencion_linea column
 * @method VentaResumidaLinea findOneByMedioPagoId(int $medio_pago_id) Return the first VentaResumidaLinea filtered by the medio_pago_id column
 * @method VentaResumidaLinea findOneByEmpresaId(int $empresa_id) Return the first VentaResumidaLinea filtered by the empresa_id column
 * @method VentaResumidaLinea findOneByBancoId(int $banco_id) Return the first VentaResumidaLinea filtered by the banco_id column
 *
 * @method array findById(int $id) Return VentaResumidaLinea objects filtered by the id column
 * @method array findByVentaResumidaId(int $venta_resumida_id) Return VentaResumidaLinea objects filtered by the venta_resumida_id column
 * @method array findByNumeroCuotas(int $numero_cuotas) Return VentaResumidaLinea objects filtered by the numero_cuotas column
 * @method array findByTotalLinea(double $total_linea) Return VentaResumidaLinea objects filtered by the total_linea column
 * @method array findByComisionLinea(double $comision_linea) Return VentaResumidaLinea objects filtered by the comision_linea column
 * @method array findByIvaLinea(double $iva_linea) Return VentaResumidaLinea objects filtered by the iva_linea column
 * @method array findByRetencionLinea(double $retencion_linea) Return VentaResumidaLinea objects filtered by the retencion_linea column
 * @method array findByMedioPagoId(int $medio_pago_id) Return VentaResumidaLinea objects filtered by the medio_pago_id column
 * @method array findByEmpresaId(int $empresa_id) Return VentaResumidaLinea objects filtered by the empresa_id column
 * @method array findByBancoId(int $banco_id) Return VentaResumidaLinea objects filtered by the banco_id column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseVentaResumidaLineaQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseVentaResumidaLineaQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = 'propel', $modelName = 'VentaResumidaLinea', $modelAlias = null)
    {
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new VentaResumidaLineaQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param     VentaResumidaLineaQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return VentaResumidaLineaQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof VentaResumidaLineaQuery) {
            return $criteria;
        }
        $query = new VentaResumidaLineaQuery();
        if (null !== $modelAlias) {
            $query->setModelAlias($modelAlias);
        }
        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return   VentaResumidaLinea|VentaResumidaLinea[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = VentaResumidaLineaPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is alredy in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(VentaResumidaLineaPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return   VentaResumidaLinea A model object, or null if the key is not found
     * @throws   PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT `ID`, `VENTA_RESUMIDA_ID`, `NUMERO_CUOTAS`, `TOTAL_LINEA`, `COMISION_LINEA`, `IVA_LINEA`, `RETENCION_LINEA`, `MEDIO_PAGO_ID`, `EMPRESA_ID`, `BANCO_ID` FROM `venta_resumida_linea` WHERE `ID` = :p0';
        try {
            $stmt = $con->prepare($sql);
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new VentaResumidaLinea();
            $obj->hydrate($row);
            VentaResumidaLineaPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return VentaResumidaLinea|VentaResumidaLinea[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|VentaResumidaLinea[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return VentaResumidaLineaQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(VentaResumidaLineaPeer::ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return VentaResumidaLineaQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(VentaResumidaLineaPeer::ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the id column
     *
     * Example usage:
     * <code>
     * $query->filterById(1234); // WHERE id = 1234
     * $query->filterById(array(12, 34)); // WHERE id IN (12, 34)
     * $query->filterById(array('min' => 12)); // WHERE id > 12
     * </code>
     *
     * @param     mixed $id The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return VentaResumidaLineaQuery The current query, for fluid interface
     */
    public function filterById($id = null, $comparison = null)
    {
        if (is_array($id) && null === $comparison) {
            $comparison = Criteria::IN;
        }

        return $this->addUsingAlias(VentaResumidaLineaPeer::ID, $id, $comparison);
    }

    /**
     * Filter the query on the venta_resumida_id column
     *
     * Example usage:
     * <code>
     * $query->filterByVentaResumidaId(1234); // WHERE venta_resumida_id = 1234
     * $query->filterByVentaResumidaId(array(12, 34)); // WHERE venta_resumida_id IN (12, 34)
     * $query->filterByVentaResumidaId(array('min' => 12)); // WHERE venta_resumida_id > 12
     * </code>
     *
     * @see       filterByVentaResumida()
     *
     * @param     mixed $ventaResumidaId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return VentaResumidaLineaQuery The current query, for fluid interface
     */
    public function filterByVentaResumidaId($ventaResumidaId = null, $comparison = null)
    {
        if (is_array($ventaResumidaId)) {
            $useMinMax = false;
            if (isset($ventaResumidaId['min'])) {
                $this->addUsingAlias(VentaResumidaLineaPeer::VENTA_RESUMIDA_ID, $ventaResumidaId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($ventaResumidaId['max'])) {
                $this->addUsingAlias(VentaResumidaLineaPeer::VENTA_RESUMIDA_ID, $ventaResumidaId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(VentaResumidaLineaPeer::VENTA_RESUMIDA_ID, $ventaResumidaId, $comparison);
    }

    /**
     * Filter the query on the numero_cuotas column
     *
     * Example usage:
     * <code>
     * $query->filterByNumeroCuotas(1234); // WHERE numero_cuotas = 1234
     * $query->filterByNumeroCuotas(array(12, 34)); // WHERE numero_cuotas IN (12, 34)
     * $query->filterByNumeroCuotas(array('min' => 12)); // WHERE numero_cuotas > 12
     * </code>
     *
     * @param     mixed $numeroCuotas The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return VentaResumidaLineaQuery The current query, for fluid interface
     */
    public function filterByNumeroCuotas($numeroCuotas = null, $comparison = null)
    {
        if (is_array($numeroCuotas)) {
            $useMinMax = false;
            if (isset($numeroCuotas['min'])) {
                $this->addUsingAlias(VentaResumidaLineaPeer::NUMERO_CUOTAS, $numeroCuotas['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($numeroCuotas['max'])) {
                $this->addUsingAlias(VentaResumidaLineaPeer::NUMERO_CUOTAS, $numeroCuotas['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(VentaResumidaLineaPeer::NUMERO_CUOTAS, $numeroCuotas, $comparison);
    }

    /**
     * Filter the query on the total_linea column
     *
     * Example usage:
     * <code>
     * $query->filterByTotalLinea(1234); // WHERE total_linea = 1234
     * $query->filterByTotalLinea(array(12, 34)); // WHERE total_linea IN (12, 34)
     * $query->filterByTotalLinea(array('min' => 12)); // WHERE total_linea > 12
     * </code>
     *
     * @param     mixed $totalLinea The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return VentaResumidaLineaQuery The current query, for fluid interface
     */
    public function filterByTotalLinea($totalLinea = null, $comparison = null)
    {
        if (is_array($totalLinea)) {
            $useMinMax = false;
            if (isset($totalLinea['min'])) {
                $this->addUsingAlias(VentaResumidaLineaPeer::TOTAL_LINEA, $totalLinea['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($totalLinea['max'])) {
                $this->addUsingAlias(VentaResumidaLineaPeer::TOTAL_LINEA, $totalLinea['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(VentaResumidaLineaPeer::TOTAL_LINEA, $totalLinea, $comparison);
    }

    /**
     * Filter the query on the comision_linea column
     *
     * Example usage:
     * <code>
     * $query->filterByComisionLinea(1234); // WHERE comision_linea = 1234
     * $query->filterByComisionLinea(array(12, 34)); // WHERE comision_linea IN (12, 34)
     * $query->filterByComisionLinea(array('min' => 12)); // WHERE comision_linea > 12
     * </code>
     *
     * @param     mixed $comisionLinea The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return VentaResumidaLineaQuery The current query, for fluid interface
     */
    public function filterByComisionLinea($comisionLinea = null, $comparison = null)
    {
        if (is_array($comisionLinea)) {
            $useMinMax = false;
            if (isset($comisionLinea['min'])) {
                $this->addUsingAlias(VentaResumidaLineaPeer::COMISION_LINEA, $comisionLinea['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($comisionLinea['max'])) {
                $this->addUsingAlias(VentaResumidaLineaPeer::COMISION_LINEA, $comisionLinea['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(VentaResumidaLineaPeer::COMISION_LINEA, $comisionLinea, $comparison);
    }

    /**
     * Filter the query on the iva_linea column
     *
     * Example usage:
     * <code>
     * $query->filterByIvaLinea(1234); // WHERE iva_linea = 1234
     * $query->filterByIvaLinea(array(12, 34)); // WHERE iva_linea IN (12, 34)
     * $query->filterByIvaLinea(array('min' => 12)); // WHERE iva_linea > 12
     * </code>
     *
     * @param     mixed $ivaLinea The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return VentaResumidaLineaQuery The current query, for fluid interface
     */
    public function filterByIvaLinea($ivaLinea = null, $comparison = null)
    {
        if (is_array($ivaLinea)) {
            $useMinMax = false;
            if (isset($ivaLinea['min'])) {
                $this->addUsingAlias(VentaResumidaLineaPeer::IVA_LINEA, $ivaLinea['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($ivaLinea['max'])) {
                $this->addUsingAlias(VentaResumidaLineaPeer::IVA_LINEA, $ivaLinea['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(VentaResumidaLineaPeer::IVA_LINEA, $ivaLinea, $comparison);
    }

    /**
     * Filter the query on the retencion_linea column
     *
     * Example usage:
     * <code>
     * $query->filterByRetencionLinea(1234); // WHERE retencion_linea = 1234
     * $query->filterByRetencionLinea(array(12, 34)); // WHERE retencion_linea IN (12, 34)
     * $query->filterByRetencionLinea(array('min' => 12)); // WHERE retencion_linea > 12
     * </code>
     *
     * @param     mixed $retencionLinea The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return VentaResumidaLineaQuery The current query, for fluid interface
     */
    public function filterByRetencionLinea($retencionLinea = null, $comparison = null)
    {
        if (is_array($retencionLinea)) {
            $useMinMax = false;
            if (isset($retencionLinea['min'])) {
                $this->addUsingAlias(VentaResumidaLineaPeer::RETENCION_LINEA, $retencionLinea['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($retencionLinea['max'])) {
                $this->addUsingAlias(VentaResumidaLineaPeer::RETENCION_LINEA, $retencionLinea['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(VentaResumidaLineaPeer::RETENCION_LINEA, $retencionLinea, $comparison);
    }

    /**
     * Filter the query on the medio_pago_id column
     *
     * Example usage:
     * <code>
     * $query->filterByMedioPagoId(1234); // WHERE medio_pago_id = 1234
     * $query->filterByMedioPagoId(array(12, 34)); // WHERE medio_pago_id IN (12, 34)
     * $query->filterByMedioPagoId(array('min' => 12)); // WHERE medio_pago_id > 12
     * </code>
     *
     * @see       filterByMedioPago()
     *
     * @param     mixed $medioPagoId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return VentaResumidaLineaQuery The current query, for fluid interface
     */
    public function filterByMedioPagoId($medioPagoId = null, $comparison = null)
    {
        if (is_array($medioPagoId)) {
            $useMinMax = false;
            if (isset($medioPagoId['min'])) {
                $this->addUsingAlias(VentaResumidaLineaPeer::MEDIO_PAGO_ID, $medioPagoId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($medioPagoId['max'])) {
                $this->addUsingAlias(VentaResumidaLineaPeer::MEDIO_PAGO_ID, $medioPagoId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(VentaResumidaLineaPeer::MEDIO_PAGO_ID, $medioPagoId, $comparison);
    }

    /**
     * Filter the query on the empresa_id column
     *
     * Example usage:
     * <code>
     * $query->filterByEmpresaId(1234); // WHERE empresa_id = 1234
     * $query->filterByEmpresaId(array(12, 34)); // WHERE empresa_id IN (12, 34)
     * $query->filterByEmpresaId(array('min' => 12)); // WHERE empresa_id > 12
     * </code>
     *
     * @see       filterByEmpresa()
     *
     * @param     mixed $empresaId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return VentaResumidaLineaQuery The current query, for fluid interface
     */
    public function filterByEmpresaId($empresaId = null, $comparison = null)
    {
        if (is_array($empresaId)) {
            $useMinMax = false;
            if (isset($empresaId['min'])) {
                $this->addUsingAlias(VentaResumidaLineaPeer::EMPRESA_ID, $empresaId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($empresaId['max'])) {
                $this->addUsingAlias(VentaResumidaLineaPeer::EMPRESA_ID, $empresaId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(VentaResumidaLineaPeer::EMPRESA_ID, $empresaId, $comparison);
    }

    /**
     * Filter the query on the banco_id column
     *
     * Example usage:
     * <code>
     * $query->filterByBancoId(1234); // WHERE banco_id = 1234
     * $query->filterByBancoId(array(12, 34)); // WHERE banco_id IN (12, 34)
     * $query->filterByBancoId(array('min' => 12)); // WHERE banco_id > 12
     * </code>
     *
     * @see       filterByBanco()
     *
     * @param     mixed $bancoId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return VentaResumidaLineaQuery The current query, for fluid interface
     */
    public function filterByBancoId($bancoId = null, $comparison = null)
    {
        if (is_array($bancoId)) {
            $useMinMax = false;
            if (isset($bancoId['min'])) {
                $this->addUsingAlias(VentaResumidaLineaPeer::BANCO_ID, $bancoId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($bancoId['max'])) {
                $this->addUsingAlias(VentaResumidaLineaPeer::BANCO_ID, $bancoId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(VentaResumidaLineaPeer::BANCO_ID, $bancoId, $comparison);
    }

    /**
     * Filter the query by a related VentaResumida object
     *
     * @param   VentaResumida|PropelObjectCollection $ventaResumida The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   VentaResumidaLineaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByVentaResumida($ventaResumida, $comparison = null)
    {
        if ($ventaResumida instanceof VentaResumida) {
            return $this
                ->addUsingAlias(VentaResumidaLineaPeer::VENTA_RESUMIDA_ID, $ventaResumida->getId(), $comparison);
        } elseif ($ventaResumida instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(VentaResumidaLineaPeer::VENTA_RESUMIDA_ID, $ventaResumida->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByVentaResumida() only accepts arguments of type VentaResumida or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the VentaResumida relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return VentaResumidaLineaQuery The current query, for fluid interface
     */
    public function joinVentaResumida($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('VentaResumida');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'VentaResumida');
        }

        return $this;
    }

    /**
     * Use the VentaResumida relation VentaResumida object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   VentaResumidaQuery A secondary query class using the current class as primary query
     */
    public function useVentaResumidaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinVentaResumida($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'VentaResumida', 'VentaResumidaQuery');
    }

    /**
     * Filter the query by a related MedioPago object
     *
     * @param   MedioPago|PropelObjectCollection $medioPago The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   VentaResumidaLineaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByMedioPago($medioPago, $comparison = null)
    {
        if ($medioPago instanceof MedioPago) {
            return $this
                ->addUsingAlias(VentaResumidaLineaPeer::MEDIO_PAGO_ID, $medioPago->getId(), $comparison);
        } elseif ($medioPago instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(VentaResumidaLineaPeer::MEDIO_PAGO_ID, $medioPago->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByMedioPago() only accepts arguments of type MedioPago or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the MedioPago relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return VentaResumidaLineaQuery The current query, for fluid interface
     */
    public function joinMedioPago($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('MedioPago');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'MedioPago');
        }

        return $this;
    }

    /**
     * Use the MedioPago relation MedioPago object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   MedioPagoQuery A secondary query class using the current class as primary query
     */
    public function useMedioPagoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinMedioPago($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'MedioPago', 'MedioPagoQuery');
    }

    /**
     * Filter the query by a related Empresa object
     *
     * @param   Empresa|PropelObjectCollection $empresa The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   VentaResumidaLineaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByEmpresa($empresa, $comparison = null)
    {
        if ($empresa instanceof Empresa) {
            return $this
                ->addUsingAlias(VentaResumidaLineaPeer::EMPRESA_ID, $empresa->getId(), $comparison);
        } elseif ($empresa instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(VentaResumidaLineaPeer::EMPRESA_ID, $empresa->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByEmpresa() only accepts arguments of type Empresa or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Empresa relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return VentaResumidaLineaQuery The current query, for fluid interface
     */
    public function joinEmpresa($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Empresa');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Empresa');
        }

        return $this;
    }

    /**
     * Use the Empresa relation Empresa object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   EmpresaQuery A secondary query class using the current class as primary query
     */
    public function useEmpresaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinEmpresa($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Empresa', 'EmpresaQuery');
    }

    /**
     * Filter the query by a related Banco object
     *
     * @param   Banco|PropelObjectCollection $banco The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   VentaResumidaLineaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByBanco($banco, $comparison = null)
    {
        if ($banco instanceof Banco) {
            return $this
                ->addUsingAlias(VentaResumidaLineaPeer::BANCO_ID, $banco->getId(), $comparison);
        } elseif ($banco instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(VentaResumidaLineaPeer::BANCO_ID, $banco->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByBanco() only accepts arguments of type Banco or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Banco relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return VentaResumidaLineaQuery The current query, for fluid interface
     */
    public function joinBanco($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Banco');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Banco');
        }

        return $this;
    }

    /**
     * Use the Banco relation Banco object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   BancoQuery A secondary query class using the current class as primary query
     */
    public function useBancoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinBanco($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Banco', 'BancoQuery');
    }

    /**
     * Filter the query by a related MovimientoBanco object
     *
     * @param   MovimientoBanco|PropelObjectCollection $movimientoBanco  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   VentaResumidaLineaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByMovimientoBanco($movimientoBanco, $comparison = null)
    {
        if ($movimientoBanco instanceof MovimientoBanco) {
            return $this
                ->addUsingAlias(VentaResumidaLineaPeer::ID, $movimientoBanco->getVentaResumidaLineaId(), $comparison);
        } elseif ($movimientoBanco instanceof PropelObjectCollection) {
            return $this
                ->useMovimientoBancoQuery()
                ->filterByPrimaryKeys($movimientoBanco->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByMovimientoBanco() only accepts arguments of type MovimientoBanco or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the MovimientoBanco relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return VentaResumidaLineaQuery The current query, for fluid interface
     */
    public function joinMovimientoBanco($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('MovimientoBanco');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'MovimientoBanco');
        }

        return $this;
    }

    /**
     * Use the MovimientoBanco relation MovimientoBanco object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   MovimientoBancoQuery A secondary query class using the current class as primary query
     */
    public function useMovimientoBancoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinMovimientoBanco($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'MovimientoBanco', 'MovimientoBancoQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   VentaResumidaLinea $ventaResumidaLinea Object to remove from the list of results
     *
     * @return VentaResumidaLineaQuery The current query, for fluid interface
     */
    public function prune($ventaResumidaLinea = null)
    {
        if ($ventaResumidaLinea) {
            $this->addUsingAlias(VentaResumidaLineaPeer::ID, $ventaResumidaLinea->getId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}
