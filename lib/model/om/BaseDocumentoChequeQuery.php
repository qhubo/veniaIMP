<?php


/**
 * Base class that represents a query for the 'documento_cheque' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.6.7 on:
 *
 * 12/30/25 15:40:26
 *
 * @method DocumentoChequeQuery orderById($order = Criteria::ASC) Order by the id column
 * @method DocumentoChequeQuery orderByBancoId($order = Criteria::ASC) Order by the banco_id column
 * @method DocumentoChequeQuery orderByTitulo($order = Criteria::ASC) Order by the titulo column
 * @method DocumentoChequeQuery orderByTipoNegociable($order = Criteria::ASC) Order by the tipo_negociable column
 * @method DocumentoChequeQuery orderByFormato($order = Criteria::ASC) Order by the formato column
 * @method DocumentoChequeQuery orderByActivo($order = Criteria::ASC) Order by the activo column
 * @method DocumentoChequeQuery orderByMargenSuperior($order = Criteria::ASC) Order by the margen_superior column
 * @method DocumentoChequeQuery orderByMargenIzquierdo($order = Criteria::ASC) Order by the margen_izquierdo column
 * @method DocumentoChequeQuery orderByAncho($order = Criteria::ASC) Order by the ancho column
 * @method DocumentoChequeQuery orderByAlto($order = Criteria::ASC) Order by the alto column
 * @method DocumentoChequeQuery orderByEmpresaId($order = Criteria::ASC) Order by the empresa_id column
 * @method DocumentoChequeQuery orderByCorrelativo($order = Criteria::ASC) Order by the correlativo column
 *
 * @method DocumentoChequeQuery groupById() Group by the id column
 * @method DocumentoChequeQuery groupByBancoId() Group by the banco_id column
 * @method DocumentoChequeQuery groupByTitulo() Group by the titulo column
 * @method DocumentoChequeQuery groupByTipoNegociable() Group by the tipo_negociable column
 * @method DocumentoChequeQuery groupByFormato() Group by the formato column
 * @method DocumentoChequeQuery groupByActivo() Group by the activo column
 * @method DocumentoChequeQuery groupByMargenSuperior() Group by the margen_superior column
 * @method DocumentoChequeQuery groupByMargenIzquierdo() Group by the margen_izquierdo column
 * @method DocumentoChequeQuery groupByAncho() Group by the ancho column
 * @method DocumentoChequeQuery groupByAlto() Group by the alto column
 * @method DocumentoChequeQuery groupByEmpresaId() Group by the empresa_id column
 * @method DocumentoChequeQuery groupByCorrelativo() Group by the correlativo column
 *
 * @method DocumentoChequeQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method DocumentoChequeQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method DocumentoChequeQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method DocumentoChequeQuery leftJoinBanco($relationAlias = null) Adds a LEFT JOIN clause to the query using the Banco relation
 * @method DocumentoChequeQuery rightJoinBanco($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Banco relation
 * @method DocumentoChequeQuery innerJoinBanco($relationAlias = null) Adds a INNER JOIN clause to the query using the Banco relation
 *
 * @method DocumentoChequeQuery leftJoinEmpresa($relationAlias = null) Adds a LEFT JOIN clause to the query using the Empresa relation
 * @method DocumentoChequeQuery rightJoinEmpresa($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Empresa relation
 * @method DocumentoChequeQuery innerJoinEmpresa($relationAlias = null) Adds a INNER JOIN clause to the query using the Empresa relation
 *
 * @method DocumentoChequeQuery leftJoinCheque($relationAlias = null) Adds a LEFT JOIN clause to the query using the Cheque relation
 * @method DocumentoChequeQuery rightJoinCheque($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Cheque relation
 * @method DocumentoChequeQuery innerJoinCheque($relationAlias = null) Adds a INNER JOIN clause to the query using the Cheque relation
 *
 * @method DocumentoCheque findOne(PropelPDO $con = null) Return the first DocumentoCheque matching the query
 * @method DocumentoCheque findOneOrCreate(PropelPDO $con = null) Return the first DocumentoCheque matching the query, or a new DocumentoCheque object populated from the query conditions when no match is found
 *
 * @method DocumentoCheque findOneById(int $id) Return the first DocumentoCheque filtered by the id column
 * @method DocumentoCheque findOneByBancoId(int $banco_id) Return the first DocumentoCheque filtered by the banco_id column
 * @method DocumentoCheque findOneByTitulo(string $titulo) Return the first DocumentoCheque filtered by the titulo column
 * @method DocumentoCheque findOneByTipoNegociable(string $tipo_negociable) Return the first DocumentoCheque filtered by the tipo_negociable column
 * @method DocumentoCheque findOneByFormato(string $formato) Return the first DocumentoCheque filtered by the formato column
 * @method DocumentoCheque findOneByActivo(boolean $activo) Return the first DocumentoCheque filtered by the activo column
 * @method DocumentoCheque findOneByMargenSuperior(int $margen_superior) Return the first DocumentoCheque filtered by the margen_superior column
 * @method DocumentoCheque findOneByMargenIzquierdo(int $margen_izquierdo) Return the first DocumentoCheque filtered by the margen_izquierdo column
 * @method DocumentoCheque findOneByAncho(int $ancho) Return the first DocumentoCheque filtered by the ancho column
 * @method DocumentoCheque findOneByAlto(int $alto) Return the first DocumentoCheque filtered by the alto column
 * @method DocumentoCheque findOneByEmpresaId(int $empresa_id) Return the first DocumentoCheque filtered by the empresa_id column
 * @method DocumentoCheque findOneByCorrelativo(int $correlativo) Return the first DocumentoCheque filtered by the correlativo column
 *
 * @method array findById(int $id) Return DocumentoCheque objects filtered by the id column
 * @method array findByBancoId(int $banco_id) Return DocumentoCheque objects filtered by the banco_id column
 * @method array findByTitulo(string $titulo) Return DocumentoCheque objects filtered by the titulo column
 * @method array findByTipoNegociable(string $tipo_negociable) Return DocumentoCheque objects filtered by the tipo_negociable column
 * @method array findByFormato(string $formato) Return DocumentoCheque objects filtered by the formato column
 * @method array findByActivo(boolean $activo) Return DocumentoCheque objects filtered by the activo column
 * @method array findByMargenSuperior(int $margen_superior) Return DocumentoCheque objects filtered by the margen_superior column
 * @method array findByMargenIzquierdo(int $margen_izquierdo) Return DocumentoCheque objects filtered by the margen_izquierdo column
 * @method array findByAncho(int $ancho) Return DocumentoCheque objects filtered by the ancho column
 * @method array findByAlto(int $alto) Return DocumentoCheque objects filtered by the alto column
 * @method array findByEmpresaId(int $empresa_id) Return DocumentoCheque objects filtered by the empresa_id column
 * @method array findByCorrelativo(int $correlativo) Return DocumentoCheque objects filtered by the correlativo column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseDocumentoChequeQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseDocumentoChequeQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = 'propel', $modelName = 'DocumentoCheque', $modelAlias = null)
    {
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new DocumentoChequeQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param     DocumentoChequeQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return DocumentoChequeQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof DocumentoChequeQuery) {
            return $criteria;
        }
        $query = new DocumentoChequeQuery();
        if (null !== $modelAlias) {
            $query->setModelAlias($modelAlias);
        }
        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return   DocumentoCheque|DocumentoCheque[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = DocumentoChequePeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is alredy in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(DocumentoChequePeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return   DocumentoCheque A model object, or null if the key is not found
     * @throws   PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT `ID`, `BANCO_ID`, `TITULO`, `TIPO_NEGOCIABLE`, `FORMATO`, `ACTIVO`, `MARGEN_SUPERIOR`, `MARGEN_IZQUIERDO`, `ANCHO`, `ALTO`, `EMPRESA_ID`, `CORRELATIVO` FROM `documento_cheque` WHERE `ID` = :p0';
        try {
            $stmt = $con->prepare($sql);
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new DocumentoCheque();
            $obj->hydrate($row);
            DocumentoChequePeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return DocumentoCheque|DocumentoCheque[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|DocumentoCheque[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return DocumentoChequeQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(DocumentoChequePeer::ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return DocumentoChequeQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(DocumentoChequePeer::ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the id column
     *
     * Example usage:
     * <code>
     * $query->filterById(1234); // WHERE id = 1234
     * $query->filterById(array(12, 34)); // WHERE id IN (12, 34)
     * $query->filterById(array('min' => 12)); // WHERE id > 12
     * </code>
     *
     * @param     mixed $id The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return DocumentoChequeQuery The current query, for fluid interface
     */
    public function filterById($id = null, $comparison = null)
    {
        if (is_array($id) && null === $comparison) {
            $comparison = Criteria::IN;
        }

        return $this->addUsingAlias(DocumentoChequePeer::ID, $id, $comparison);
    }

    /**
     * Filter the query on the banco_id column
     *
     * Example usage:
     * <code>
     * $query->filterByBancoId(1234); // WHERE banco_id = 1234
     * $query->filterByBancoId(array(12, 34)); // WHERE banco_id IN (12, 34)
     * $query->filterByBancoId(array('min' => 12)); // WHERE banco_id > 12
     * </code>
     *
     * @see       filterByBanco()
     *
     * @param     mixed $bancoId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return DocumentoChequeQuery The current query, for fluid interface
     */
    public function filterByBancoId($bancoId = null, $comparison = null)
    {
        if (is_array($bancoId)) {
            $useMinMax = false;
            if (isset($bancoId['min'])) {
                $this->addUsingAlias(DocumentoChequePeer::BANCO_ID, $bancoId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($bancoId['max'])) {
                $this->addUsingAlias(DocumentoChequePeer::BANCO_ID, $bancoId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(DocumentoChequePeer::BANCO_ID, $bancoId, $comparison);
    }

    /**
     * Filter the query on the titulo column
     *
     * Example usage:
     * <code>
     * $query->filterByTitulo('fooValue');   // WHERE titulo = 'fooValue'
     * $query->filterByTitulo('%fooValue%'); // WHERE titulo LIKE '%fooValue%'
     * </code>
     *
     * @param     string $titulo The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return DocumentoChequeQuery The current query, for fluid interface
     */
    public function filterByTitulo($titulo = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($titulo)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $titulo)) {
                $titulo = str_replace('*', '%', $titulo);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(DocumentoChequePeer::TITULO, $titulo, $comparison);
    }

    /**
     * Filter the query on the tipo_negociable column
     *
     * Example usage:
     * <code>
     * $query->filterByTipoNegociable('fooValue');   // WHERE tipo_negociable = 'fooValue'
     * $query->filterByTipoNegociable('%fooValue%'); // WHERE tipo_negociable LIKE '%fooValue%'
     * </code>
     *
     * @param     string $tipoNegociable The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return DocumentoChequeQuery The current query, for fluid interface
     */
    public function filterByTipoNegociable($tipoNegociable = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($tipoNegociable)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $tipoNegociable)) {
                $tipoNegociable = str_replace('*', '%', $tipoNegociable);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(DocumentoChequePeer::TIPO_NEGOCIABLE, $tipoNegociable, $comparison);
    }

    /**
     * Filter the query on the formato column
     *
     * Example usage:
     * <code>
     * $query->filterByFormato('fooValue');   // WHERE formato = 'fooValue'
     * $query->filterByFormato('%fooValue%'); // WHERE formato LIKE '%fooValue%'
     * </code>
     *
     * @param     string $formato The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return DocumentoChequeQuery The current query, for fluid interface
     */
    public function filterByFormato($formato = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($formato)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $formato)) {
                $formato = str_replace('*', '%', $formato);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(DocumentoChequePeer::FORMATO, $formato, $comparison);
    }

    /**
     * Filter the query on the activo column
     *
     * Example usage:
     * <code>
     * $query->filterByActivo(true); // WHERE activo = true
     * $query->filterByActivo('yes'); // WHERE activo = true
     * </code>
     *
     * @param     boolean|string $activo The value to use as filter.
     *              Non-boolean arguments are converted using the following rules:
     *                * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *                * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     *              Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return DocumentoChequeQuery The current query, for fluid interface
     */
    public function filterByActivo($activo = null, $comparison = null)
    {
        if (is_string($activo)) {
            $activo = in_array(strtolower($activo), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
        }

        return $this->addUsingAlias(DocumentoChequePeer::ACTIVO, $activo, $comparison);
    }

    /**
     * Filter the query on the margen_superior column
     *
     * Example usage:
     * <code>
     * $query->filterByMargenSuperior(1234); // WHERE margen_superior = 1234
     * $query->filterByMargenSuperior(array(12, 34)); // WHERE margen_superior IN (12, 34)
     * $query->filterByMargenSuperior(array('min' => 12)); // WHERE margen_superior > 12
     * </code>
     *
     * @param     mixed $margenSuperior The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return DocumentoChequeQuery The current query, for fluid interface
     */
    public function filterByMargenSuperior($margenSuperior = null, $comparison = null)
    {
        if (is_array($margenSuperior)) {
            $useMinMax = false;
            if (isset($margenSuperior['min'])) {
                $this->addUsingAlias(DocumentoChequePeer::MARGEN_SUPERIOR, $margenSuperior['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($margenSuperior['max'])) {
                $this->addUsingAlias(DocumentoChequePeer::MARGEN_SUPERIOR, $margenSuperior['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(DocumentoChequePeer::MARGEN_SUPERIOR, $margenSuperior, $comparison);
    }

    /**
     * Filter the query on the margen_izquierdo column
     *
     * Example usage:
     * <code>
     * $query->filterByMargenIzquierdo(1234); // WHERE margen_izquierdo = 1234
     * $query->filterByMargenIzquierdo(array(12, 34)); // WHERE margen_izquierdo IN (12, 34)
     * $query->filterByMargenIzquierdo(array('min' => 12)); // WHERE margen_izquierdo > 12
     * </code>
     *
     * @param     mixed $margenIzquierdo The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return DocumentoChequeQuery The current query, for fluid interface
     */
    public function filterByMargenIzquierdo($margenIzquierdo = null, $comparison = null)
    {
        if (is_array($margenIzquierdo)) {
            $useMinMax = false;
            if (isset($margenIzquierdo['min'])) {
                $this->addUsingAlias(DocumentoChequePeer::MARGEN_IZQUIERDO, $margenIzquierdo['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($margenIzquierdo['max'])) {
                $this->addUsingAlias(DocumentoChequePeer::MARGEN_IZQUIERDO, $margenIzquierdo['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(DocumentoChequePeer::MARGEN_IZQUIERDO, $margenIzquierdo, $comparison);
    }

    /**
     * Filter the query on the ancho column
     *
     * Example usage:
     * <code>
     * $query->filterByAncho(1234); // WHERE ancho = 1234
     * $query->filterByAncho(array(12, 34)); // WHERE ancho IN (12, 34)
     * $query->filterByAncho(array('min' => 12)); // WHERE ancho > 12
     * </code>
     *
     * @param     mixed $ancho The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return DocumentoChequeQuery The current query, for fluid interface
     */
    public function filterByAncho($ancho = null, $comparison = null)
    {
        if (is_array($ancho)) {
            $useMinMax = false;
            if (isset($ancho['min'])) {
                $this->addUsingAlias(DocumentoChequePeer::ANCHO, $ancho['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($ancho['max'])) {
                $this->addUsingAlias(DocumentoChequePeer::ANCHO, $ancho['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(DocumentoChequePeer::ANCHO, $ancho, $comparison);
    }

    /**
     * Filter the query on the alto column
     *
     * Example usage:
     * <code>
     * $query->filterByAlto(1234); // WHERE alto = 1234
     * $query->filterByAlto(array(12, 34)); // WHERE alto IN (12, 34)
     * $query->filterByAlto(array('min' => 12)); // WHERE alto > 12
     * </code>
     *
     * @param     mixed $alto The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return DocumentoChequeQuery The current query, for fluid interface
     */
    public function filterByAlto($alto = null, $comparison = null)
    {
        if (is_array($alto)) {
            $useMinMax = false;
            if (isset($alto['min'])) {
                $this->addUsingAlias(DocumentoChequePeer::ALTO, $alto['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($alto['max'])) {
                $this->addUsingAlias(DocumentoChequePeer::ALTO, $alto['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(DocumentoChequePeer::ALTO, $alto, $comparison);
    }

    /**
     * Filter the query on the empresa_id column
     *
     * Example usage:
     * <code>
     * $query->filterByEmpresaId(1234); // WHERE empresa_id = 1234
     * $query->filterByEmpresaId(array(12, 34)); // WHERE empresa_id IN (12, 34)
     * $query->filterByEmpresaId(array('min' => 12)); // WHERE empresa_id > 12
     * </code>
     *
     * @see       filterByEmpresa()
     *
     * @param     mixed $empresaId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return DocumentoChequeQuery The current query, for fluid interface
     */
    public function filterByEmpresaId($empresaId = null, $comparison = null)
    {
        if (is_array($empresaId)) {
            $useMinMax = false;
            if (isset($empresaId['min'])) {
                $this->addUsingAlias(DocumentoChequePeer::EMPRESA_ID, $empresaId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($empresaId['max'])) {
                $this->addUsingAlias(DocumentoChequePeer::EMPRESA_ID, $empresaId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(DocumentoChequePeer::EMPRESA_ID, $empresaId, $comparison);
    }

    /**
     * Filter the query on the correlativo column
     *
     * Example usage:
     * <code>
     * $query->filterByCorrelativo(1234); // WHERE correlativo = 1234
     * $query->filterByCorrelativo(array(12, 34)); // WHERE correlativo IN (12, 34)
     * $query->filterByCorrelativo(array('min' => 12)); // WHERE correlativo > 12
     * </code>
     *
     * @param     mixed $correlativo The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return DocumentoChequeQuery The current query, for fluid interface
     */
    public function filterByCorrelativo($correlativo = null, $comparison = null)
    {
        if (is_array($correlativo)) {
            $useMinMax = false;
            if (isset($correlativo['min'])) {
                $this->addUsingAlias(DocumentoChequePeer::CORRELATIVO, $correlativo['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($correlativo['max'])) {
                $this->addUsingAlias(DocumentoChequePeer::CORRELATIVO, $correlativo['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(DocumentoChequePeer::CORRELATIVO, $correlativo, $comparison);
    }

    /**
     * Filter the query by a related Banco object
     *
     * @param   Banco|PropelObjectCollection $banco The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   DocumentoChequeQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByBanco($banco, $comparison = null)
    {
        if ($banco instanceof Banco) {
            return $this
                ->addUsingAlias(DocumentoChequePeer::BANCO_ID, $banco->getId(), $comparison);
        } elseif ($banco instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(DocumentoChequePeer::BANCO_ID, $banco->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByBanco() only accepts arguments of type Banco or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Banco relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return DocumentoChequeQuery The current query, for fluid interface
     */
    public function joinBanco($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Banco');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Banco');
        }

        return $this;
    }

    /**
     * Use the Banco relation Banco object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   BancoQuery A secondary query class using the current class as primary query
     */
    public function useBancoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinBanco($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Banco', 'BancoQuery');
    }

    /**
     * Filter the query by a related Empresa object
     *
     * @param   Empresa|PropelObjectCollection $empresa The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   DocumentoChequeQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByEmpresa($empresa, $comparison = null)
    {
        if ($empresa instanceof Empresa) {
            return $this
                ->addUsingAlias(DocumentoChequePeer::EMPRESA_ID, $empresa->getId(), $comparison);
        } elseif ($empresa instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(DocumentoChequePeer::EMPRESA_ID, $empresa->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByEmpresa() only accepts arguments of type Empresa or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Empresa relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return DocumentoChequeQuery The current query, for fluid interface
     */
    public function joinEmpresa($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Empresa');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Empresa');
        }

        return $this;
    }

    /**
     * Use the Empresa relation Empresa object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   EmpresaQuery A secondary query class using the current class as primary query
     */
    public function useEmpresaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinEmpresa($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Empresa', 'EmpresaQuery');
    }

    /**
     * Filter the query by a related Cheque object
     *
     * @param   Cheque|PropelObjectCollection $cheque  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   DocumentoChequeQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByCheque($cheque, $comparison = null)
    {
        if ($cheque instanceof Cheque) {
            return $this
                ->addUsingAlias(DocumentoChequePeer::ID, $cheque->getDocumentoChequeId(), $comparison);
        } elseif ($cheque instanceof PropelObjectCollection) {
            return $this
                ->useChequeQuery()
                ->filterByPrimaryKeys($cheque->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByCheque() only accepts arguments of type Cheque or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Cheque relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return DocumentoChequeQuery The current query, for fluid interface
     */
    public function joinCheque($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Cheque');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Cheque');
        }

        return $this;
    }

    /**
     * Use the Cheque relation Cheque object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ChequeQuery A secondary query class using the current class as primary query
     */
    public function useChequeQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinCheque($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Cheque', 'ChequeQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   DocumentoCheque $documentoCheque Object to remove from the list of results
     *
     * @return DocumentoChequeQuery The current query, for fluid interface
     */
    public function prune($documentoCheque = null)
    {
        if ($documentoCheque) {
            $this->addUsingAlias(DocumentoChequePeer::ID, $documentoCheque->getId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}
