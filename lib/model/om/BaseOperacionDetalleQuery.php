<?php


/**
 * Base class that represents a query for the 'operacion_detalle' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.6.7 on:
 *
 * 12/30/25 22:58:25
 *
 * @method OperacionDetalleQuery orderById($order = Criteria::ASC) Order by the id column
 * @method OperacionDetalleQuery orderByCodigo($order = Criteria::ASC) Order by the codigo column
 * @method OperacionDetalleQuery orderByEmpresaId($order = Criteria::ASC) Order by the empresa_id column
 * @method OperacionDetalleQuery orderByOperacionId($order = Criteria::ASC) Order by the operacion_id column
 * @method OperacionDetalleQuery orderByProductoId($order = Criteria::ASC) Order by the producto_id column
 * @method OperacionDetalleQuery orderByDetalle($order = Criteria::ASC) Order by the detalle column
 * @method OperacionDetalleQuery orderByCantidad($order = Criteria::ASC) Order by the cantidad column
 * @method OperacionDetalleQuery orderByValorTotal($order = Criteria::ASC) Order by the valor_total column
 * @method OperacionDetalleQuery orderByServicioId($order = Criteria::ASC) Order by the servicio_id column
 * @method OperacionDetalleQuery orderByValorUnitario($order = Criteria::ASC) Order by the valor_unitario column
 * @method OperacionDetalleQuery orderByDescuento($order = Criteria::ASC) Order by the descuento column
 * @method OperacionDetalleQuery orderByTotalIva($order = Criteria::ASC) Order by the total_iva column
 * @method OperacionDetalleQuery orderByCostoUnitario($order = Criteria::ASC) Order by the costo_unitario column
 * @method OperacionDetalleQuery orderByLineaNo($order = Criteria::ASC) Order by the linea_no column
 *
 * @method OperacionDetalleQuery groupById() Group by the id column
 * @method OperacionDetalleQuery groupByCodigo() Group by the codigo column
 * @method OperacionDetalleQuery groupByEmpresaId() Group by the empresa_id column
 * @method OperacionDetalleQuery groupByOperacionId() Group by the operacion_id column
 * @method OperacionDetalleQuery groupByProductoId() Group by the producto_id column
 * @method OperacionDetalleQuery groupByDetalle() Group by the detalle column
 * @method OperacionDetalleQuery groupByCantidad() Group by the cantidad column
 * @method OperacionDetalleQuery groupByValorTotal() Group by the valor_total column
 * @method OperacionDetalleQuery groupByServicioId() Group by the servicio_id column
 * @method OperacionDetalleQuery groupByValorUnitario() Group by the valor_unitario column
 * @method OperacionDetalleQuery groupByDescuento() Group by the descuento column
 * @method OperacionDetalleQuery groupByTotalIva() Group by the total_iva column
 * @method OperacionDetalleQuery groupByCostoUnitario() Group by the costo_unitario column
 * @method OperacionDetalleQuery groupByLineaNo() Group by the linea_no column
 *
 * @method OperacionDetalleQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method OperacionDetalleQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method OperacionDetalleQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method OperacionDetalleQuery leftJoinEmpresa($relationAlias = null) Adds a LEFT JOIN clause to the query using the Empresa relation
 * @method OperacionDetalleQuery rightJoinEmpresa($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Empresa relation
 * @method OperacionDetalleQuery innerJoinEmpresa($relationAlias = null) Adds a INNER JOIN clause to the query using the Empresa relation
 *
 * @method OperacionDetalleQuery leftJoinOperacion($relationAlias = null) Adds a LEFT JOIN clause to the query using the Operacion relation
 * @method OperacionDetalleQuery rightJoinOperacion($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Operacion relation
 * @method OperacionDetalleQuery innerJoinOperacion($relationAlias = null) Adds a INNER JOIN clause to the query using the Operacion relation
 *
 * @method OperacionDetalleQuery leftJoinProducto($relationAlias = null) Adds a LEFT JOIN clause to the query using the Producto relation
 * @method OperacionDetalleQuery rightJoinProducto($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Producto relation
 * @method OperacionDetalleQuery innerJoinProducto($relationAlias = null) Adds a INNER JOIN clause to the query using the Producto relation
 *
 * @method OperacionDetalleQuery leftJoinServicio($relationAlias = null) Adds a LEFT JOIN clause to the query using the Servicio relation
 * @method OperacionDetalleQuery rightJoinServicio($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Servicio relation
 * @method OperacionDetalleQuery innerJoinServicio($relationAlias = null) Adds a INNER JOIN clause to the query using the Servicio relation
 *
 * @method OperacionDetalle findOne(PropelPDO $con = null) Return the first OperacionDetalle matching the query
 * @method OperacionDetalle findOneOrCreate(PropelPDO $con = null) Return the first OperacionDetalle matching the query, or a new OperacionDetalle object populated from the query conditions when no match is found
 *
 * @method OperacionDetalle findOneById(int $id) Return the first OperacionDetalle filtered by the id column
 * @method OperacionDetalle findOneByCodigo(string $codigo) Return the first OperacionDetalle filtered by the codigo column
 * @method OperacionDetalle findOneByEmpresaId(int $empresa_id) Return the first OperacionDetalle filtered by the empresa_id column
 * @method OperacionDetalle findOneByOperacionId(int $operacion_id) Return the first OperacionDetalle filtered by the operacion_id column
 * @method OperacionDetalle findOneByProductoId(int $producto_id) Return the first OperacionDetalle filtered by the producto_id column
 * @method OperacionDetalle findOneByDetalle(string $detalle) Return the first OperacionDetalle filtered by the detalle column
 * @method OperacionDetalle findOneByCantidad(int $cantidad) Return the first OperacionDetalle filtered by the cantidad column
 * @method OperacionDetalle findOneByValorTotal(double $valor_total) Return the first OperacionDetalle filtered by the valor_total column
 * @method OperacionDetalle findOneByServicioId(int $servicio_id) Return the first OperacionDetalle filtered by the servicio_id column
 * @method OperacionDetalle findOneByValorUnitario(double $valor_unitario) Return the first OperacionDetalle filtered by the valor_unitario column
 * @method OperacionDetalle findOneByDescuento(double $descuento) Return the first OperacionDetalle filtered by the descuento column
 * @method OperacionDetalle findOneByTotalIva(double $total_iva) Return the first OperacionDetalle filtered by the total_iva column
 * @method OperacionDetalle findOneByCostoUnitario(double $costo_unitario) Return the first OperacionDetalle filtered by the costo_unitario column
 * @method OperacionDetalle findOneByLineaNo(string $linea_no) Return the first OperacionDetalle filtered by the linea_no column
 *
 * @method array findById(int $id) Return OperacionDetalle objects filtered by the id column
 * @method array findByCodigo(string $codigo) Return OperacionDetalle objects filtered by the codigo column
 * @method array findByEmpresaId(int $empresa_id) Return OperacionDetalle objects filtered by the empresa_id column
 * @method array findByOperacionId(int $operacion_id) Return OperacionDetalle objects filtered by the operacion_id column
 * @method array findByProductoId(int $producto_id) Return OperacionDetalle objects filtered by the producto_id column
 * @method array findByDetalle(string $detalle) Return OperacionDetalle objects filtered by the detalle column
 * @method array findByCantidad(int $cantidad) Return OperacionDetalle objects filtered by the cantidad column
 * @method array findByValorTotal(double $valor_total) Return OperacionDetalle objects filtered by the valor_total column
 * @method array findByServicioId(int $servicio_id) Return OperacionDetalle objects filtered by the servicio_id column
 * @method array findByValorUnitario(double $valor_unitario) Return OperacionDetalle objects filtered by the valor_unitario column
 * @method array findByDescuento(double $descuento) Return OperacionDetalle objects filtered by the descuento column
 * @method array findByTotalIva(double $total_iva) Return OperacionDetalle objects filtered by the total_iva column
 * @method array findByCostoUnitario(double $costo_unitario) Return OperacionDetalle objects filtered by the costo_unitario column
 * @method array findByLineaNo(string $linea_no) Return OperacionDetalle objects filtered by the linea_no column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseOperacionDetalleQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseOperacionDetalleQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = 'propel', $modelName = 'OperacionDetalle', $modelAlias = null)
    {
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new OperacionDetalleQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param     OperacionDetalleQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return OperacionDetalleQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof OperacionDetalleQuery) {
            return $criteria;
        }
        $query = new OperacionDetalleQuery();
        if (null !== $modelAlias) {
            $query->setModelAlias($modelAlias);
        }
        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return   OperacionDetalle|OperacionDetalle[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = OperacionDetallePeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is alredy in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(OperacionDetallePeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return   OperacionDetalle A model object, or null if the key is not found
     * @throws   PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT `ID`, `CODIGO`, `EMPRESA_ID`, `OPERACION_ID`, `PRODUCTO_ID`, `DETALLE`, `CANTIDAD`, `VALOR_TOTAL`, `SERVICIO_ID`, `VALOR_UNITARIO`, `DESCUENTO`, `TOTAL_IVA`, `COSTO_UNITARIO`, `LINEA_NO` FROM `operacion_detalle` WHERE `ID` = :p0';
        try {
            $stmt = $con->prepare($sql);
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new OperacionDetalle();
            $obj->hydrate($row);
            OperacionDetallePeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return OperacionDetalle|OperacionDetalle[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|OperacionDetalle[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return OperacionDetalleQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(OperacionDetallePeer::ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return OperacionDetalleQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(OperacionDetallePeer::ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the id column
     *
     * Example usage:
     * <code>
     * $query->filterById(1234); // WHERE id = 1234
     * $query->filterById(array(12, 34)); // WHERE id IN (12, 34)
     * $query->filterById(array('min' => 12)); // WHERE id > 12
     * </code>
     *
     * @param     mixed $id The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OperacionDetalleQuery The current query, for fluid interface
     */
    public function filterById($id = null, $comparison = null)
    {
        if (is_array($id) && null === $comparison) {
            $comparison = Criteria::IN;
        }

        return $this->addUsingAlias(OperacionDetallePeer::ID, $id, $comparison);
    }

    /**
     * Filter the query on the codigo column
     *
     * Example usage:
     * <code>
     * $query->filterByCodigo('fooValue');   // WHERE codigo = 'fooValue'
     * $query->filterByCodigo('%fooValue%'); // WHERE codigo LIKE '%fooValue%'
     * </code>
     *
     * @param     string $codigo The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OperacionDetalleQuery The current query, for fluid interface
     */
    public function filterByCodigo($codigo = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($codigo)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $codigo)) {
                $codigo = str_replace('*', '%', $codigo);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(OperacionDetallePeer::CODIGO, $codigo, $comparison);
    }

    /**
     * Filter the query on the empresa_id column
     *
     * Example usage:
     * <code>
     * $query->filterByEmpresaId(1234); // WHERE empresa_id = 1234
     * $query->filterByEmpresaId(array(12, 34)); // WHERE empresa_id IN (12, 34)
     * $query->filterByEmpresaId(array('min' => 12)); // WHERE empresa_id > 12
     * </code>
     *
     * @see       filterByEmpresa()
     *
     * @param     mixed $empresaId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OperacionDetalleQuery The current query, for fluid interface
     */
    public function filterByEmpresaId($empresaId = null, $comparison = null)
    {
        if (is_array($empresaId)) {
            $useMinMax = false;
            if (isset($empresaId['min'])) {
                $this->addUsingAlias(OperacionDetallePeer::EMPRESA_ID, $empresaId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($empresaId['max'])) {
                $this->addUsingAlias(OperacionDetallePeer::EMPRESA_ID, $empresaId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(OperacionDetallePeer::EMPRESA_ID, $empresaId, $comparison);
    }

    /**
     * Filter the query on the operacion_id column
     *
     * Example usage:
     * <code>
     * $query->filterByOperacionId(1234); // WHERE operacion_id = 1234
     * $query->filterByOperacionId(array(12, 34)); // WHERE operacion_id IN (12, 34)
     * $query->filterByOperacionId(array('min' => 12)); // WHERE operacion_id > 12
     * </code>
     *
     * @see       filterByOperacion()
     *
     * @param     mixed $operacionId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OperacionDetalleQuery The current query, for fluid interface
     */
    public function filterByOperacionId($operacionId = null, $comparison = null)
    {
        if (is_array($operacionId)) {
            $useMinMax = false;
            if (isset($operacionId['min'])) {
                $this->addUsingAlias(OperacionDetallePeer::OPERACION_ID, $operacionId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($operacionId['max'])) {
                $this->addUsingAlias(OperacionDetallePeer::OPERACION_ID, $operacionId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(OperacionDetallePeer::OPERACION_ID, $operacionId, $comparison);
    }

    /**
     * Filter the query on the producto_id column
     *
     * Example usage:
     * <code>
     * $query->filterByProductoId(1234); // WHERE producto_id = 1234
     * $query->filterByProductoId(array(12, 34)); // WHERE producto_id IN (12, 34)
     * $query->filterByProductoId(array('min' => 12)); // WHERE producto_id > 12
     * </code>
     *
     * @see       filterByProducto()
     *
     * @param     mixed $productoId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OperacionDetalleQuery The current query, for fluid interface
     */
    public function filterByProductoId($productoId = null, $comparison = null)
    {
        if (is_array($productoId)) {
            $useMinMax = false;
            if (isset($productoId['min'])) {
                $this->addUsingAlias(OperacionDetallePeer::PRODUCTO_ID, $productoId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($productoId['max'])) {
                $this->addUsingAlias(OperacionDetallePeer::PRODUCTO_ID, $productoId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(OperacionDetallePeer::PRODUCTO_ID, $productoId, $comparison);
    }

    /**
     * Filter the query on the detalle column
     *
     * Example usage:
     * <code>
     * $query->filterByDetalle('fooValue');   // WHERE detalle = 'fooValue'
     * $query->filterByDetalle('%fooValue%'); // WHERE detalle LIKE '%fooValue%'
     * </code>
     *
     * @param     string $detalle The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OperacionDetalleQuery The current query, for fluid interface
     */
    public function filterByDetalle($detalle = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($detalle)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $detalle)) {
                $detalle = str_replace('*', '%', $detalle);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(OperacionDetallePeer::DETALLE, $detalle, $comparison);
    }

    /**
     * Filter the query on the cantidad column
     *
     * Example usage:
     * <code>
     * $query->filterByCantidad(1234); // WHERE cantidad = 1234
     * $query->filterByCantidad(array(12, 34)); // WHERE cantidad IN (12, 34)
     * $query->filterByCantidad(array('min' => 12)); // WHERE cantidad > 12
     * </code>
     *
     * @param     mixed $cantidad The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OperacionDetalleQuery The current query, for fluid interface
     */
    public function filterByCantidad($cantidad = null, $comparison = null)
    {
        if (is_array($cantidad)) {
            $useMinMax = false;
            if (isset($cantidad['min'])) {
                $this->addUsingAlias(OperacionDetallePeer::CANTIDAD, $cantidad['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($cantidad['max'])) {
                $this->addUsingAlias(OperacionDetallePeer::CANTIDAD, $cantidad['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(OperacionDetallePeer::CANTIDAD, $cantidad, $comparison);
    }

    /**
     * Filter the query on the valor_total column
     *
     * Example usage:
     * <code>
     * $query->filterByValorTotal(1234); // WHERE valor_total = 1234
     * $query->filterByValorTotal(array(12, 34)); // WHERE valor_total IN (12, 34)
     * $query->filterByValorTotal(array('min' => 12)); // WHERE valor_total > 12
     * </code>
     *
     * @param     mixed $valorTotal The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OperacionDetalleQuery The current query, for fluid interface
     */
    public function filterByValorTotal($valorTotal = null, $comparison = null)
    {
        if (is_array($valorTotal)) {
            $useMinMax = false;
            if (isset($valorTotal['min'])) {
                $this->addUsingAlias(OperacionDetallePeer::VALOR_TOTAL, $valorTotal['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($valorTotal['max'])) {
                $this->addUsingAlias(OperacionDetallePeer::VALOR_TOTAL, $valorTotal['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(OperacionDetallePeer::VALOR_TOTAL, $valorTotal, $comparison);
    }

    /**
     * Filter the query on the servicio_id column
     *
     * Example usage:
     * <code>
     * $query->filterByServicioId(1234); // WHERE servicio_id = 1234
     * $query->filterByServicioId(array(12, 34)); // WHERE servicio_id IN (12, 34)
     * $query->filterByServicioId(array('min' => 12)); // WHERE servicio_id > 12
     * </code>
     *
     * @see       filterByServicio()
     *
     * @param     mixed $servicioId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OperacionDetalleQuery The current query, for fluid interface
     */
    public function filterByServicioId($servicioId = null, $comparison = null)
    {
        if (is_array($servicioId)) {
            $useMinMax = false;
            if (isset($servicioId['min'])) {
                $this->addUsingAlias(OperacionDetallePeer::SERVICIO_ID, $servicioId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($servicioId['max'])) {
                $this->addUsingAlias(OperacionDetallePeer::SERVICIO_ID, $servicioId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(OperacionDetallePeer::SERVICIO_ID, $servicioId, $comparison);
    }

    /**
     * Filter the query on the valor_unitario column
     *
     * Example usage:
     * <code>
     * $query->filterByValorUnitario(1234); // WHERE valor_unitario = 1234
     * $query->filterByValorUnitario(array(12, 34)); // WHERE valor_unitario IN (12, 34)
     * $query->filterByValorUnitario(array('min' => 12)); // WHERE valor_unitario > 12
     * </code>
     *
     * @param     mixed $valorUnitario The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OperacionDetalleQuery The current query, for fluid interface
     */
    public function filterByValorUnitario($valorUnitario = null, $comparison = null)
    {
        if (is_array($valorUnitario)) {
            $useMinMax = false;
            if (isset($valorUnitario['min'])) {
                $this->addUsingAlias(OperacionDetallePeer::VALOR_UNITARIO, $valorUnitario['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($valorUnitario['max'])) {
                $this->addUsingAlias(OperacionDetallePeer::VALOR_UNITARIO, $valorUnitario['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(OperacionDetallePeer::VALOR_UNITARIO, $valorUnitario, $comparison);
    }

    /**
     * Filter the query on the descuento column
     *
     * Example usage:
     * <code>
     * $query->filterByDescuento(1234); // WHERE descuento = 1234
     * $query->filterByDescuento(array(12, 34)); // WHERE descuento IN (12, 34)
     * $query->filterByDescuento(array('min' => 12)); // WHERE descuento > 12
     * </code>
     *
     * @param     mixed $descuento The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OperacionDetalleQuery The current query, for fluid interface
     */
    public function filterByDescuento($descuento = null, $comparison = null)
    {
        if (is_array($descuento)) {
            $useMinMax = false;
            if (isset($descuento['min'])) {
                $this->addUsingAlias(OperacionDetallePeer::DESCUENTO, $descuento['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($descuento['max'])) {
                $this->addUsingAlias(OperacionDetallePeer::DESCUENTO, $descuento['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(OperacionDetallePeer::DESCUENTO, $descuento, $comparison);
    }

    /**
     * Filter the query on the total_iva column
     *
     * Example usage:
     * <code>
     * $query->filterByTotalIva(1234); // WHERE total_iva = 1234
     * $query->filterByTotalIva(array(12, 34)); // WHERE total_iva IN (12, 34)
     * $query->filterByTotalIva(array('min' => 12)); // WHERE total_iva > 12
     * </code>
     *
     * @param     mixed $totalIva The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OperacionDetalleQuery The current query, for fluid interface
     */
    public function filterByTotalIva($totalIva = null, $comparison = null)
    {
        if (is_array($totalIva)) {
            $useMinMax = false;
            if (isset($totalIva['min'])) {
                $this->addUsingAlias(OperacionDetallePeer::TOTAL_IVA, $totalIva['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($totalIva['max'])) {
                $this->addUsingAlias(OperacionDetallePeer::TOTAL_IVA, $totalIva['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(OperacionDetallePeer::TOTAL_IVA, $totalIva, $comparison);
    }

    /**
     * Filter the query on the costo_unitario column
     *
     * Example usage:
     * <code>
     * $query->filterByCostoUnitario(1234); // WHERE costo_unitario = 1234
     * $query->filterByCostoUnitario(array(12, 34)); // WHERE costo_unitario IN (12, 34)
     * $query->filterByCostoUnitario(array('min' => 12)); // WHERE costo_unitario > 12
     * </code>
     *
     * @param     mixed $costoUnitario The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OperacionDetalleQuery The current query, for fluid interface
     */
    public function filterByCostoUnitario($costoUnitario = null, $comparison = null)
    {
        if (is_array($costoUnitario)) {
            $useMinMax = false;
            if (isset($costoUnitario['min'])) {
                $this->addUsingAlias(OperacionDetallePeer::COSTO_UNITARIO, $costoUnitario['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($costoUnitario['max'])) {
                $this->addUsingAlias(OperacionDetallePeer::COSTO_UNITARIO, $costoUnitario['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(OperacionDetallePeer::COSTO_UNITARIO, $costoUnitario, $comparison);
    }

    /**
     * Filter the query on the linea_no column
     *
     * Example usage:
     * <code>
     * $query->filterByLineaNo('fooValue');   // WHERE linea_no = 'fooValue'
     * $query->filterByLineaNo('%fooValue%'); // WHERE linea_no LIKE '%fooValue%'
     * </code>
     *
     * @param     string $lineaNo The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OperacionDetalleQuery The current query, for fluid interface
     */
    public function filterByLineaNo($lineaNo = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($lineaNo)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $lineaNo)) {
                $lineaNo = str_replace('*', '%', $lineaNo);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(OperacionDetallePeer::LINEA_NO, $lineaNo, $comparison);
    }

    /**
     * Filter the query by a related Empresa object
     *
     * @param   Empresa|PropelObjectCollection $empresa The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   OperacionDetalleQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByEmpresa($empresa, $comparison = null)
    {
        if ($empresa instanceof Empresa) {
            return $this
                ->addUsingAlias(OperacionDetallePeer::EMPRESA_ID, $empresa->getId(), $comparison);
        } elseif ($empresa instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(OperacionDetallePeer::EMPRESA_ID, $empresa->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByEmpresa() only accepts arguments of type Empresa or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Empresa relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return OperacionDetalleQuery The current query, for fluid interface
     */
    public function joinEmpresa($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Empresa');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Empresa');
        }

        return $this;
    }

    /**
     * Use the Empresa relation Empresa object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   EmpresaQuery A secondary query class using the current class as primary query
     */
    public function useEmpresaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinEmpresa($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Empresa', 'EmpresaQuery');
    }

    /**
     * Filter the query by a related Operacion object
     *
     * @param   Operacion|PropelObjectCollection $operacion The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   OperacionDetalleQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByOperacion($operacion, $comparison = null)
    {
        if ($operacion instanceof Operacion) {
            return $this
                ->addUsingAlias(OperacionDetallePeer::OPERACION_ID, $operacion->getId(), $comparison);
        } elseif ($operacion instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(OperacionDetallePeer::OPERACION_ID, $operacion->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByOperacion() only accepts arguments of type Operacion or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Operacion relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return OperacionDetalleQuery The current query, for fluid interface
     */
    public function joinOperacion($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Operacion');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Operacion');
        }

        return $this;
    }

    /**
     * Use the Operacion relation Operacion object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   OperacionQuery A secondary query class using the current class as primary query
     */
    public function useOperacionQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinOperacion($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Operacion', 'OperacionQuery');
    }

    /**
     * Filter the query by a related Producto object
     *
     * @param   Producto|PropelObjectCollection $producto The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   OperacionDetalleQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByProducto($producto, $comparison = null)
    {
        if ($producto instanceof Producto) {
            return $this
                ->addUsingAlias(OperacionDetallePeer::PRODUCTO_ID, $producto->getId(), $comparison);
        } elseif ($producto instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(OperacionDetallePeer::PRODUCTO_ID, $producto->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByProducto() only accepts arguments of type Producto or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Producto relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return OperacionDetalleQuery The current query, for fluid interface
     */
    public function joinProducto($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Producto');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Producto');
        }

        return $this;
    }

    /**
     * Use the Producto relation Producto object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ProductoQuery A secondary query class using the current class as primary query
     */
    public function useProductoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinProducto($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Producto', 'ProductoQuery');
    }

    /**
     * Filter the query by a related Servicio object
     *
     * @param   Servicio|PropelObjectCollection $servicio The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   OperacionDetalleQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByServicio($servicio, $comparison = null)
    {
        if ($servicio instanceof Servicio) {
            return $this
                ->addUsingAlias(OperacionDetallePeer::SERVICIO_ID, $servicio->getId(), $comparison);
        } elseif ($servicio instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(OperacionDetallePeer::SERVICIO_ID, $servicio->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByServicio() only accepts arguments of type Servicio or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Servicio relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return OperacionDetalleQuery The current query, for fluid interface
     */
    public function joinServicio($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Servicio');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Servicio');
        }

        return $this;
    }

    /**
     * Use the Servicio relation Servicio object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ServicioQuery A secondary query class using the current class as primary query
     */
    public function useServicioQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinServicio($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Servicio', 'ServicioQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   OperacionDetalle $operacionDetalle Object to remove from the list of results
     *
     * @return OperacionDetalleQuery The current query, for fluid interface
     */
    public function prune($operacionDetalle = null)
    {
        if ($operacionDetalle) {
            $this->addUsingAlias(OperacionDetallePeer::ID, $operacionDetalle->getId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}
