<?php


/**
 * Base class that represents a query for the 'gasto_detalle' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.6.7 on:
 *
 * 01/03/26 04:09:42
 *
 * @method GastoDetalleQuery orderById($order = Criteria::ASC) Order by the id column
 * @method GastoDetalleQuery orderByGastoId($order = Criteria::ASC) Order by the gasto_id column
 * @method GastoDetalleQuery orderByCuentaContable($order = Criteria::ASC) Order by the cuenta_contable column
 * @method GastoDetalleQuery orderByConcepto($order = Criteria::ASC) Order by the concepto column
 * @method GastoDetalleQuery orderByCantidad($order = Criteria::ASC) Order by the cantidad column
 * @method GastoDetalleQuery orderBySubTotal($order = Criteria::ASC) Order by the sub_total column
 * @method GastoDetalleQuery orderByIva($order = Criteria::ASC) Order by the iva column
 * @method GastoDetalleQuery orderByValorTotal($order = Criteria::ASC) Order by the valor_total column
 *
 * @method GastoDetalleQuery groupById() Group by the id column
 * @method GastoDetalleQuery groupByGastoId() Group by the gasto_id column
 * @method GastoDetalleQuery groupByCuentaContable() Group by the cuenta_contable column
 * @method GastoDetalleQuery groupByConcepto() Group by the concepto column
 * @method GastoDetalleQuery groupByCantidad() Group by the cantidad column
 * @method GastoDetalleQuery groupBySubTotal() Group by the sub_total column
 * @method GastoDetalleQuery groupByIva() Group by the iva column
 * @method GastoDetalleQuery groupByValorTotal() Group by the valor_total column
 *
 * @method GastoDetalleQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method GastoDetalleQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method GastoDetalleQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method GastoDetalleQuery leftJoinGasto($relationAlias = null) Adds a LEFT JOIN clause to the query using the Gasto relation
 * @method GastoDetalleQuery rightJoinGasto($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Gasto relation
 * @method GastoDetalleQuery innerJoinGasto($relationAlias = null) Adds a INNER JOIN clause to the query using the Gasto relation
 *
 * @method GastoDetalle findOne(PropelPDO $con = null) Return the first GastoDetalle matching the query
 * @method GastoDetalle findOneOrCreate(PropelPDO $con = null) Return the first GastoDetalle matching the query, or a new GastoDetalle object populated from the query conditions when no match is found
 *
 * @method GastoDetalle findOneById(int $id) Return the first GastoDetalle filtered by the id column
 * @method GastoDetalle findOneByGastoId(int $gasto_id) Return the first GastoDetalle filtered by the gasto_id column
 * @method GastoDetalle findOneByCuentaContable(string $cuenta_contable) Return the first GastoDetalle filtered by the cuenta_contable column
 * @method GastoDetalle findOneByConcepto(string $concepto) Return the first GastoDetalle filtered by the concepto column
 * @method GastoDetalle findOneByCantidad(double $cantidad) Return the first GastoDetalle filtered by the cantidad column
 * @method GastoDetalle findOneBySubTotal(double $sub_total) Return the first GastoDetalle filtered by the sub_total column
 * @method GastoDetalle findOneByIva(double $iva) Return the first GastoDetalle filtered by the iva column
 * @method GastoDetalle findOneByValorTotal(double $valor_total) Return the first GastoDetalle filtered by the valor_total column
 *
 * @method array findById(int $id) Return GastoDetalle objects filtered by the id column
 * @method array findByGastoId(int $gasto_id) Return GastoDetalle objects filtered by the gasto_id column
 * @method array findByCuentaContable(string $cuenta_contable) Return GastoDetalle objects filtered by the cuenta_contable column
 * @method array findByConcepto(string $concepto) Return GastoDetalle objects filtered by the concepto column
 * @method array findByCantidad(double $cantidad) Return GastoDetalle objects filtered by the cantidad column
 * @method array findBySubTotal(double $sub_total) Return GastoDetalle objects filtered by the sub_total column
 * @method array findByIva(double $iva) Return GastoDetalle objects filtered by the iva column
 * @method array findByValorTotal(double $valor_total) Return GastoDetalle objects filtered by the valor_total column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseGastoDetalleQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseGastoDetalleQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = 'propel', $modelName = 'GastoDetalle', $modelAlias = null)
    {
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new GastoDetalleQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param     GastoDetalleQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return GastoDetalleQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof GastoDetalleQuery) {
            return $criteria;
        }
        $query = new GastoDetalleQuery();
        if (null !== $modelAlias) {
            $query->setModelAlias($modelAlias);
        }
        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return   GastoDetalle|GastoDetalle[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = GastoDetallePeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is alredy in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(GastoDetallePeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return   GastoDetalle A model object, or null if the key is not found
     * @throws   PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT `ID`, `GASTO_ID`, `CUENTA_CONTABLE`, `CONCEPTO`, `CANTIDAD`, `SUB_TOTAL`, `IVA`, `VALOR_TOTAL` FROM `gasto_detalle` WHERE `ID` = :p0';
        try {
            $stmt = $con->prepare($sql);
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new GastoDetalle();
            $obj->hydrate($row);
            GastoDetallePeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return GastoDetalle|GastoDetalle[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|GastoDetalle[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return GastoDetalleQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(GastoDetallePeer::ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return GastoDetalleQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(GastoDetallePeer::ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the id column
     *
     * Example usage:
     * <code>
     * $query->filterById(1234); // WHERE id = 1234
     * $query->filterById(array(12, 34)); // WHERE id IN (12, 34)
     * $query->filterById(array('min' => 12)); // WHERE id > 12
     * </code>
     *
     * @param     mixed $id The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return GastoDetalleQuery The current query, for fluid interface
     */
    public function filterById($id = null, $comparison = null)
    {
        if (is_array($id) && null === $comparison) {
            $comparison = Criteria::IN;
        }

        return $this->addUsingAlias(GastoDetallePeer::ID, $id, $comparison);
    }

    /**
     * Filter the query on the gasto_id column
     *
     * Example usage:
     * <code>
     * $query->filterByGastoId(1234); // WHERE gasto_id = 1234
     * $query->filterByGastoId(array(12, 34)); // WHERE gasto_id IN (12, 34)
     * $query->filterByGastoId(array('min' => 12)); // WHERE gasto_id > 12
     * </code>
     *
     * @see       filterByGasto()
     *
     * @param     mixed $gastoId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return GastoDetalleQuery The current query, for fluid interface
     */
    public function filterByGastoId($gastoId = null, $comparison = null)
    {
        if (is_array($gastoId)) {
            $useMinMax = false;
            if (isset($gastoId['min'])) {
                $this->addUsingAlias(GastoDetallePeer::GASTO_ID, $gastoId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($gastoId['max'])) {
                $this->addUsingAlias(GastoDetallePeer::GASTO_ID, $gastoId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(GastoDetallePeer::GASTO_ID, $gastoId, $comparison);
    }

    /**
     * Filter the query on the cuenta_contable column
     *
     * Example usage:
     * <code>
     * $query->filterByCuentaContable('fooValue');   // WHERE cuenta_contable = 'fooValue'
     * $query->filterByCuentaContable('%fooValue%'); // WHERE cuenta_contable LIKE '%fooValue%'
     * </code>
     *
     * @param     string $cuentaContable The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return GastoDetalleQuery The current query, for fluid interface
     */
    public function filterByCuentaContable($cuentaContable = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($cuentaContable)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $cuentaContable)) {
                $cuentaContable = str_replace('*', '%', $cuentaContable);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(GastoDetallePeer::CUENTA_CONTABLE, $cuentaContable, $comparison);
    }

    /**
     * Filter the query on the concepto column
     *
     * Example usage:
     * <code>
     * $query->filterByConcepto('fooValue');   // WHERE concepto = 'fooValue'
     * $query->filterByConcepto('%fooValue%'); // WHERE concepto LIKE '%fooValue%'
     * </code>
     *
     * @param     string $concepto The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return GastoDetalleQuery The current query, for fluid interface
     */
    public function filterByConcepto($concepto = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($concepto)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $concepto)) {
                $concepto = str_replace('*', '%', $concepto);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(GastoDetallePeer::CONCEPTO, $concepto, $comparison);
    }

    /**
     * Filter the query on the cantidad column
     *
     * Example usage:
     * <code>
     * $query->filterByCantidad(1234); // WHERE cantidad = 1234
     * $query->filterByCantidad(array(12, 34)); // WHERE cantidad IN (12, 34)
     * $query->filterByCantidad(array('min' => 12)); // WHERE cantidad > 12
     * </code>
     *
     * @param     mixed $cantidad The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return GastoDetalleQuery The current query, for fluid interface
     */
    public function filterByCantidad($cantidad = null, $comparison = null)
    {
        if (is_array($cantidad)) {
            $useMinMax = false;
            if (isset($cantidad['min'])) {
                $this->addUsingAlias(GastoDetallePeer::CANTIDAD, $cantidad['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($cantidad['max'])) {
                $this->addUsingAlias(GastoDetallePeer::CANTIDAD, $cantidad['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(GastoDetallePeer::CANTIDAD, $cantidad, $comparison);
    }

    /**
     * Filter the query on the sub_total column
     *
     * Example usage:
     * <code>
     * $query->filterBySubTotal(1234); // WHERE sub_total = 1234
     * $query->filterBySubTotal(array(12, 34)); // WHERE sub_total IN (12, 34)
     * $query->filterBySubTotal(array('min' => 12)); // WHERE sub_total > 12
     * </code>
     *
     * @param     mixed $subTotal The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return GastoDetalleQuery The current query, for fluid interface
     */
    public function filterBySubTotal($subTotal = null, $comparison = null)
    {
        if (is_array($subTotal)) {
            $useMinMax = false;
            if (isset($subTotal['min'])) {
                $this->addUsingAlias(GastoDetallePeer::SUB_TOTAL, $subTotal['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($subTotal['max'])) {
                $this->addUsingAlias(GastoDetallePeer::SUB_TOTAL, $subTotal['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(GastoDetallePeer::SUB_TOTAL, $subTotal, $comparison);
    }

    /**
     * Filter the query on the iva column
     *
     * Example usage:
     * <code>
     * $query->filterByIva(1234); // WHERE iva = 1234
     * $query->filterByIva(array(12, 34)); // WHERE iva IN (12, 34)
     * $query->filterByIva(array('min' => 12)); // WHERE iva > 12
     * </code>
     *
     * @param     mixed $iva The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return GastoDetalleQuery The current query, for fluid interface
     */
    public function filterByIva($iva = null, $comparison = null)
    {
        if (is_array($iva)) {
            $useMinMax = false;
            if (isset($iva['min'])) {
                $this->addUsingAlias(GastoDetallePeer::IVA, $iva['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($iva['max'])) {
                $this->addUsingAlias(GastoDetallePeer::IVA, $iva['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(GastoDetallePeer::IVA, $iva, $comparison);
    }

    /**
     * Filter the query on the valor_total column
     *
     * Example usage:
     * <code>
     * $query->filterByValorTotal(1234); // WHERE valor_total = 1234
     * $query->filterByValorTotal(array(12, 34)); // WHERE valor_total IN (12, 34)
     * $query->filterByValorTotal(array('min' => 12)); // WHERE valor_total > 12
     * </code>
     *
     * @param     mixed $valorTotal The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return GastoDetalleQuery The current query, for fluid interface
     */
    public function filterByValorTotal($valorTotal = null, $comparison = null)
    {
        if (is_array($valorTotal)) {
            $useMinMax = false;
            if (isset($valorTotal['min'])) {
                $this->addUsingAlias(GastoDetallePeer::VALOR_TOTAL, $valorTotal['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($valorTotal['max'])) {
                $this->addUsingAlias(GastoDetallePeer::VALOR_TOTAL, $valorTotal['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(GastoDetallePeer::VALOR_TOTAL, $valorTotal, $comparison);
    }

    /**
     * Filter the query by a related Gasto object
     *
     * @param   Gasto|PropelObjectCollection $gasto The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   GastoDetalleQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByGasto($gasto, $comparison = null)
    {
        if ($gasto instanceof Gasto) {
            return $this
                ->addUsingAlias(GastoDetallePeer::GASTO_ID, $gasto->getId(), $comparison);
        } elseif ($gasto instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(GastoDetallePeer::GASTO_ID, $gasto->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByGasto() only accepts arguments of type Gasto or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Gasto relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return GastoDetalleQuery The current query, for fluid interface
     */
    public function joinGasto($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Gasto');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Gasto');
        }

        return $this;
    }

    /**
     * Use the Gasto relation Gasto object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   GastoQuery A secondary query class using the current class as primary query
     */
    public function useGastoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinGasto($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Gasto', 'GastoQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   GastoDetalle $gastoDetalle Object to remove from the list of results
     *
     * @return GastoDetalleQuery The current query, for fluid interface
     */
    public function prune($gastoDetalle = null)
    {
        if ($gastoDetalle) {
            $this->addUsingAlias(GastoDetallePeer::ID, $gastoDetalle->getId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}
