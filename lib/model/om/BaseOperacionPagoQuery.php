<?php


/**
 * Base class that represents a query for the 'operacion_pago' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.6.7 on:
 *
 * 12/26/25 01:36:27
 *
 * @method OperacionPagoQuery orderById($order = Criteria::ASC) Order by the id column
 * @method OperacionPagoQuery orderByEmpresaId($order = Criteria::ASC) Order by the empresa_id column
 * @method OperacionPagoQuery orderByOperacionId($order = Criteria::ASC) Order by the operacion_id column
 * @method OperacionPagoQuery orderByTipo($order = Criteria::ASC) Order by the tipo column
 * @method OperacionPagoQuery orderByValor($order = Criteria::ASC) Order by the valor column
 * @method OperacionPagoQuery orderByDocumento($order = Criteria::ASC) Order by the documento column
 * @method OperacionPagoQuery orderByFechaDocumento($order = Criteria::ASC) Order by the fecha_documento column
 * @method OperacionPagoQuery orderByBancoId($order = Criteria::ASC) Order by the banco_id column
 * @method OperacionPagoQuery orderByCuentaContable($order = Criteria::ASC) Order by the cuenta_contable column
 * @method OperacionPagoQuery orderByPartidaNo($order = Criteria::ASC) Order by the partida_no column
 * @method OperacionPagoQuery orderByUsuario($order = Criteria::ASC) Order by the usuario column
 * @method OperacionPagoQuery orderByFechaCreo($order = Criteria::ASC) Order by the fecha_creo column
 * @method OperacionPagoQuery orderByCxcCobrar($order = Criteria::ASC) Order by the cxc_cobrar column
 * @method OperacionPagoQuery orderByComision($order = Criteria::ASC) Order by the comision column
 * @method OperacionPagoQuery orderByVuelto($order = Criteria::ASC) Order by the vuelto column
 *
 * @method OperacionPagoQuery groupById() Group by the id column
 * @method OperacionPagoQuery groupByEmpresaId() Group by the empresa_id column
 * @method OperacionPagoQuery groupByOperacionId() Group by the operacion_id column
 * @method OperacionPagoQuery groupByTipo() Group by the tipo column
 * @method OperacionPagoQuery groupByValor() Group by the valor column
 * @method OperacionPagoQuery groupByDocumento() Group by the documento column
 * @method OperacionPagoQuery groupByFechaDocumento() Group by the fecha_documento column
 * @method OperacionPagoQuery groupByBancoId() Group by the banco_id column
 * @method OperacionPagoQuery groupByCuentaContable() Group by the cuenta_contable column
 * @method OperacionPagoQuery groupByPartidaNo() Group by the partida_no column
 * @method OperacionPagoQuery groupByUsuario() Group by the usuario column
 * @method OperacionPagoQuery groupByFechaCreo() Group by the fecha_creo column
 * @method OperacionPagoQuery groupByCxcCobrar() Group by the cxc_cobrar column
 * @method OperacionPagoQuery groupByComision() Group by the comision column
 * @method OperacionPagoQuery groupByVuelto() Group by the vuelto column
 *
 * @method OperacionPagoQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method OperacionPagoQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method OperacionPagoQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method OperacionPagoQuery leftJoinEmpresa($relationAlias = null) Adds a LEFT JOIN clause to the query using the Empresa relation
 * @method OperacionPagoQuery rightJoinEmpresa($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Empresa relation
 * @method OperacionPagoQuery innerJoinEmpresa($relationAlias = null) Adds a INNER JOIN clause to the query using the Empresa relation
 *
 * @method OperacionPagoQuery leftJoinOperacion($relationAlias = null) Adds a LEFT JOIN clause to the query using the Operacion relation
 * @method OperacionPagoQuery rightJoinOperacion($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Operacion relation
 * @method OperacionPagoQuery innerJoinOperacion($relationAlias = null) Adds a INNER JOIN clause to the query using the Operacion relation
 *
 * @method OperacionPagoQuery leftJoinBanco($relationAlias = null) Adds a LEFT JOIN clause to the query using the Banco relation
 * @method OperacionPagoQuery rightJoinBanco($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Banco relation
 * @method OperacionPagoQuery innerJoinBanco($relationAlias = null) Adds a INNER JOIN clause to the query using the Banco relation
 *
 * @method OperacionPagoQuery leftJoinCuentaBanco($relationAlias = null) Adds a LEFT JOIN clause to the query using the CuentaBanco relation
 * @method OperacionPagoQuery rightJoinCuentaBanco($relationAlias = null) Adds a RIGHT JOIN clause to the query using the CuentaBanco relation
 * @method OperacionPagoQuery innerJoinCuentaBanco($relationAlias = null) Adds a INNER JOIN clause to the query using the CuentaBanco relation
 *
 * @method OperacionPago findOne(PropelPDO $con = null) Return the first OperacionPago matching the query
 * @method OperacionPago findOneOrCreate(PropelPDO $con = null) Return the first OperacionPago matching the query, or a new OperacionPago object populated from the query conditions when no match is found
 *
 * @method OperacionPago findOneById(int $id) Return the first OperacionPago filtered by the id column
 * @method OperacionPago findOneByEmpresaId(int $empresa_id) Return the first OperacionPago filtered by the empresa_id column
 * @method OperacionPago findOneByOperacionId(int $operacion_id) Return the first OperacionPago filtered by the operacion_id column
 * @method OperacionPago findOneByTipo(string $tipo) Return the first OperacionPago filtered by the tipo column
 * @method OperacionPago findOneByValor(double $valor) Return the first OperacionPago filtered by the valor column
 * @method OperacionPago findOneByDocumento(string $documento) Return the first OperacionPago filtered by the documento column
 * @method OperacionPago findOneByFechaDocumento(string $fecha_documento) Return the first OperacionPago filtered by the fecha_documento column
 * @method OperacionPago findOneByBancoId(int $banco_id) Return the first OperacionPago filtered by the banco_id column
 * @method OperacionPago findOneByCuentaContable(string $cuenta_contable) Return the first OperacionPago filtered by the cuenta_contable column
 * @method OperacionPago findOneByPartidaNo(int $partida_no) Return the first OperacionPago filtered by the partida_no column
 * @method OperacionPago findOneByUsuario(string $usuario) Return the first OperacionPago filtered by the usuario column
 * @method OperacionPago findOneByFechaCreo(string $fecha_creo) Return the first OperacionPago filtered by the fecha_creo column
 * @method OperacionPago findOneByCxcCobrar(string $cxc_cobrar) Return the first OperacionPago filtered by the cxc_cobrar column
 * @method OperacionPago findOneByComision(double $comision) Return the first OperacionPago filtered by the comision column
 * @method OperacionPago findOneByVuelto(double $vuelto) Return the first OperacionPago filtered by the vuelto column
 *
 * @method array findById(int $id) Return OperacionPago objects filtered by the id column
 * @method array findByEmpresaId(int $empresa_id) Return OperacionPago objects filtered by the empresa_id column
 * @method array findByOperacionId(int $operacion_id) Return OperacionPago objects filtered by the operacion_id column
 * @method array findByTipo(string $tipo) Return OperacionPago objects filtered by the tipo column
 * @method array findByValor(double $valor) Return OperacionPago objects filtered by the valor column
 * @method array findByDocumento(string $documento) Return OperacionPago objects filtered by the documento column
 * @method array findByFechaDocumento(string $fecha_documento) Return OperacionPago objects filtered by the fecha_documento column
 * @method array findByBancoId(int $banco_id) Return OperacionPago objects filtered by the banco_id column
 * @method array findByCuentaContable(string $cuenta_contable) Return OperacionPago objects filtered by the cuenta_contable column
 * @method array findByPartidaNo(int $partida_no) Return OperacionPago objects filtered by the partida_no column
 * @method array findByUsuario(string $usuario) Return OperacionPago objects filtered by the usuario column
 * @method array findByFechaCreo(string $fecha_creo) Return OperacionPago objects filtered by the fecha_creo column
 * @method array findByCxcCobrar(string $cxc_cobrar) Return OperacionPago objects filtered by the cxc_cobrar column
 * @method array findByComision(double $comision) Return OperacionPago objects filtered by the comision column
 * @method array findByVuelto(double $vuelto) Return OperacionPago objects filtered by the vuelto column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseOperacionPagoQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseOperacionPagoQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = 'propel', $modelName = 'OperacionPago', $modelAlias = null)
    {
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new OperacionPagoQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param     OperacionPagoQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return OperacionPagoQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof OperacionPagoQuery) {
            return $criteria;
        }
        $query = new OperacionPagoQuery();
        if (null !== $modelAlias) {
            $query->setModelAlias($modelAlias);
        }
        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return   OperacionPago|OperacionPago[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = OperacionPagoPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is alredy in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(OperacionPagoPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return   OperacionPago A model object, or null if the key is not found
     * @throws   PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT `ID`, `EMPRESA_ID`, `OPERACION_ID`, `TIPO`, `VALOR`, `DOCUMENTO`, `FECHA_DOCUMENTO`, `BANCO_ID`, `CUENTA_CONTABLE`, `PARTIDA_NO`, `USUARIO`, `FECHA_CREO`, `CXC_COBRAR`, `COMISION`, `VUELTO` FROM `operacion_pago` WHERE `ID` = :p0';
        try {
            $stmt = $con->prepare($sql);
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new OperacionPago();
            $obj->hydrate($row);
            OperacionPagoPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return OperacionPago|OperacionPago[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|OperacionPago[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return OperacionPagoQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(OperacionPagoPeer::ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return OperacionPagoQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(OperacionPagoPeer::ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the id column
     *
     * Example usage:
     * <code>
     * $query->filterById(1234); // WHERE id = 1234
     * $query->filterById(array(12, 34)); // WHERE id IN (12, 34)
     * $query->filterById(array('min' => 12)); // WHERE id > 12
     * </code>
     *
     * @param     mixed $id The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OperacionPagoQuery The current query, for fluid interface
     */
    public function filterById($id = null, $comparison = null)
    {
        if (is_array($id) && null === $comparison) {
            $comparison = Criteria::IN;
        }

        return $this->addUsingAlias(OperacionPagoPeer::ID, $id, $comparison);
    }

    /**
     * Filter the query on the empresa_id column
     *
     * Example usage:
     * <code>
     * $query->filterByEmpresaId(1234); // WHERE empresa_id = 1234
     * $query->filterByEmpresaId(array(12, 34)); // WHERE empresa_id IN (12, 34)
     * $query->filterByEmpresaId(array('min' => 12)); // WHERE empresa_id > 12
     * </code>
     *
     * @see       filterByEmpresa()
     *
     * @param     mixed $empresaId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OperacionPagoQuery The current query, for fluid interface
     */
    public function filterByEmpresaId($empresaId = null, $comparison = null)
    {
        if (is_array($empresaId)) {
            $useMinMax = false;
            if (isset($empresaId['min'])) {
                $this->addUsingAlias(OperacionPagoPeer::EMPRESA_ID, $empresaId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($empresaId['max'])) {
                $this->addUsingAlias(OperacionPagoPeer::EMPRESA_ID, $empresaId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(OperacionPagoPeer::EMPRESA_ID, $empresaId, $comparison);
    }

    /**
     * Filter the query on the operacion_id column
     *
     * Example usage:
     * <code>
     * $query->filterByOperacionId(1234); // WHERE operacion_id = 1234
     * $query->filterByOperacionId(array(12, 34)); // WHERE operacion_id IN (12, 34)
     * $query->filterByOperacionId(array('min' => 12)); // WHERE operacion_id > 12
     * </code>
     *
     * @see       filterByOperacion()
     *
     * @param     mixed $operacionId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OperacionPagoQuery The current query, for fluid interface
     */
    public function filterByOperacionId($operacionId = null, $comparison = null)
    {
        if (is_array($operacionId)) {
            $useMinMax = false;
            if (isset($operacionId['min'])) {
                $this->addUsingAlias(OperacionPagoPeer::OPERACION_ID, $operacionId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($operacionId['max'])) {
                $this->addUsingAlias(OperacionPagoPeer::OPERACION_ID, $operacionId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(OperacionPagoPeer::OPERACION_ID, $operacionId, $comparison);
    }

    /**
     * Filter the query on the tipo column
     *
     * Example usage:
     * <code>
     * $query->filterByTipo('fooValue');   // WHERE tipo = 'fooValue'
     * $query->filterByTipo('%fooValue%'); // WHERE tipo LIKE '%fooValue%'
     * </code>
     *
     * @param     string $tipo The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OperacionPagoQuery The current query, for fluid interface
     */
    public function filterByTipo($tipo = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($tipo)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $tipo)) {
                $tipo = str_replace('*', '%', $tipo);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(OperacionPagoPeer::TIPO, $tipo, $comparison);
    }

    /**
     * Filter the query on the valor column
     *
     * Example usage:
     * <code>
     * $query->filterByValor(1234); // WHERE valor = 1234
     * $query->filterByValor(array(12, 34)); // WHERE valor IN (12, 34)
     * $query->filterByValor(array('min' => 12)); // WHERE valor > 12
     * </code>
     *
     * @param     mixed $valor The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OperacionPagoQuery The current query, for fluid interface
     */
    public function filterByValor($valor = null, $comparison = null)
    {
        if (is_array($valor)) {
            $useMinMax = false;
            if (isset($valor['min'])) {
                $this->addUsingAlias(OperacionPagoPeer::VALOR, $valor['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($valor['max'])) {
                $this->addUsingAlias(OperacionPagoPeer::VALOR, $valor['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(OperacionPagoPeer::VALOR, $valor, $comparison);
    }

    /**
     * Filter the query on the documento column
     *
     * Example usage:
     * <code>
     * $query->filterByDocumento('fooValue');   // WHERE documento = 'fooValue'
     * $query->filterByDocumento('%fooValue%'); // WHERE documento LIKE '%fooValue%'
     * </code>
     *
     * @param     string $documento The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OperacionPagoQuery The current query, for fluid interface
     */
    public function filterByDocumento($documento = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($documento)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $documento)) {
                $documento = str_replace('*', '%', $documento);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(OperacionPagoPeer::DOCUMENTO, $documento, $comparison);
    }

    /**
     * Filter the query on the fecha_documento column
     *
     * Example usage:
     * <code>
     * $query->filterByFechaDocumento('2011-03-14'); // WHERE fecha_documento = '2011-03-14'
     * $query->filterByFechaDocumento('now'); // WHERE fecha_documento = '2011-03-14'
     * $query->filterByFechaDocumento(array('max' => 'yesterday')); // WHERE fecha_documento > '2011-03-13'
     * </code>
     *
     * @param     mixed $fechaDocumento The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OperacionPagoQuery The current query, for fluid interface
     */
    public function filterByFechaDocumento($fechaDocumento = null, $comparison = null)
    {
        if (is_array($fechaDocumento)) {
            $useMinMax = false;
            if (isset($fechaDocumento['min'])) {
                $this->addUsingAlias(OperacionPagoPeer::FECHA_DOCUMENTO, $fechaDocumento['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($fechaDocumento['max'])) {
                $this->addUsingAlias(OperacionPagoPeer::FECHA_DOCUMENTO, $fechaDocumento['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(OperacionPagoPeer::FECHA_DOCUMENTO, $fechaDocumento, $comparison);
    }

    /**
     * Filter the query on the banco_id column
     *
     * Example usage:
     * <code>
     * $query->filterByBancoId(1234); // WHERE banco_id = 1234
     * $query->filterByBancoId(array(12, 34)); // WHERE banco_id IN (12, 34)
     * $query->filterByBancoId(array('min' => 12)); // WHERE banco_id > 12
     * </code>
     *
     * @see       filterByBanco()
     *
     * @param     mixed $bancoId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OperacionPagoQuery The current query, for fluid interface
     */
    public function filterByBancoId($bancoId = null, $comparison = null)
    {
        if (is_array($bancoId)) {
            $useMinMax = false;
            if (isset($bancoId['min'])) {
                $this->addUsingAlias(OperacionPagoPeer::BANCO_ID, $bancoId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($bancoId['max'])) {
                $this->addUsingAlias(OperacionPagoPeer::BANCO_ID, $bancoId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(OperacionPagoPeer::BANCO_ID, $bancoId, $comparison);
    }

    /**
     * Filter the query on the cuenta_contable column
     *
     * Example usage:
     * <code>
     * $query->filterByCuentaContable('fooValue');   // WHERE cuenta_contable = 'fooValue'
     * $query->filterByCuentaContable('%fooValue%'); // WHERE cuenta_contable LIKE '%fooValue%'
     * </code>
     *
     * @param     string $cuentaContable The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OperacionPagoQuery The current query, for fluid interface
     */
    public function filterByCuentaContable($cuentaContable = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($cuentaContable)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $cuentaContable)) {
                $cuentaContable = str_replace('*', '%', $cuentaContable);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(OperacionPagoPeer::CUENTA_CONTABLE, $cuentaContable, $comparison);
    }

    /**
     * Filter the query on the partida_no column
     *
     * Example usage:
     * <code>
     * $query->filterByPartidaNo(1234); // WHERE partida_no = 1234
     * $query->filterByPartidaNo(array(12, 34)); // WHERE partida_no IN (12, 34)
     * $query->filterByPartidaNo(array('min' => 12)); // WHERE partida_no > 12
     * </code>
     *
     * @param     mixed $partidaNo The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OperacionPagoQuery The current query, for fluid interface
     */
    public function filterByPartidaNo($partidaNo = null, $comparison = null)
    {
        if (is_array($partidaNo)) {
            $useMinMax = false;
            if (isset($partidaNo['min'])) {
                $this->addUsingAlias(OperacionPagoPeer::PARTIDA_NO, $partidaNo['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($partidaNo['max'])) {
                $this->addUsingAlias(OperacionPagoPeer::PARTIDA_NO, $partidaNo['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(OperacionPagoPeer::PARTIDA_NO, $partidaNo, $comparison);
    }

    /**
     * Filter the query on the usuario column
     *
     * Example usage:
     * <code>
     * $query->filterByUsuario('fooValue');   // WHERE usuario = 'fooValue'
     * $query->filterByUsuario('%fooValue%'); // WHERE usuario LIKE '%fooValue%'
     * </code>
     *
     * @param     string $usuario The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OperacionPagoQuery The current query, for fluid interface
     */
    public function filterByUsuario($usuario = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($usuario)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $usuario)) {
                $usuario = str_replace('*', '%', $usuario);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(OperacionPagoPeer::USUARIO, $usuario, $comparison);
    }

    /**
     * Filter the query on the fecha_creo column
     *
     * Example usage:
     * <code>
     * $query->filterByFechaCreo('2011-03-14'); // WHERE fecha_creo = '2011-03-14'
     * $query->filterByFechaCreo('now'); // WHERE fecha_creo = '2011-03-14'
     * $query->filterByFechaCreo(array('max' => 'yesterday')); // WHERE fecha_creo > '2011-03-13'
     * </code>
     *
     * @param     mixed $fechaCreo The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OperacionPagoQuery The current query, for fluid interface
     */
    public function filterByFechaCreo($fechaCreo = null, $comparison = null)
    {
        if (is_array($fechaCreo)) {
            $useMinMax = false;
            if (isset($fechaCreo['min'])) {
                $this->addUsingAlias(OperacionPagoPeer::FECHA_CREO, $fechaCreo['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($fechaCreo['max'])) {
                $this->addUsingAlias(OperacionPagoPeer::FECHA_CREO, $fechaCreo['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(OperacionPagoPeer::FECHA_CREO, $fechaCreo, $comparison);
    }

    /**
     * Filter the query on the cxc_cobrar column
     *
     * Example usage:
     * <code>
     * $query->filterByCxcCobrar('fooValue');   // WHERE cxc_cobrar = 'fooValue'
     * $query->filterByCxcCobrar('%fooValue%'); // WHERE cxc_cobrar LIKE '%fooValue%'
     * </code>
     *
     * @param     string $cxcCobrar The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OperacionPagoQuery The current query, for fluid interface
     */
    public function filterByCxcCobrar($cxcCobrar = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($cxcCobrar)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $cxcCobrar)) {
                $cxcCobrar = str_replace('*', '%', $cxcCobrar);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(OperacionPagoPeer::CXC_COBRAR, $cxcCobrar, $comparison);
    }

    /**
     * Filter the query on the comision column
     *
     * Example usage:
     * <code>
     * $query->filterByComision(1234); // WHERE comision = 1234
     * $query->filterByComision(array(12, 34)); // WHERE comision IN (12, 34)
     * $query->filterByComision(array('min' => 12)); // WHERE comision > 12
     * </code>
     *
     * @param     mixed $comision The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OperacionPagoQuery The current query, for fluid interface
     */
    public function filterByComision($comision = null, $comparison = null)
    {
        if (is_array($comision)) {
            $useMinMax = false;
            if (isset($comision['min'])) {
                $this->addUsingAlias(OperacionPagoPeer::COMISION, $comision['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($comision['max'])) {
                $this->addUsingAlias(OperacionPagoPeer::COMISION, $comision['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(OperacionPagoPeer::COMISION, $comision, $comparison);
    }

    /**
     * Filter the query on the vuelto column
     *
     * Example usage:
     * <code>
     * $query->filterByVuelto(1234); // WHERE vuelto = 1234
     * $query->filterByVuelto(array(12, 34)); // WHERE vuelto IN (12, 34)
     * $query->filterByVuelto(array('min' => 12)); // WHERE vuelto > 12
     * </code>
     *
     * @param     mixed $vuelto The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OperacionPagoQuery The current query, for fluid interface
     */
    public function filterByVuelto($vuelto = null, $comparison = null)
    {
        if (is_array($vuelto)) {
            $useMinMax = false;
            if (isset($vuelto['min'])) {
                $this->addUsingAlias(OperacionPagoPeer::VUELTO, $vuelto['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($vuelto['max'])) {
                $this->addUsingAlias(OperacionPagoPeer::VUELTO, $vuelto['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(OperacionPagoPeer::VUELTO, $vuelto, $comparison);
    }

    /**
     * Filter the query by a related Empresa object
     *
     * @param   Empresa|PropelObjectCollection $empresa The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   OperacionPagoQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByEmpresa($empresa, $comparison = null)
    {
        if ($empresa instanceof Empresa) {
            return $this
                ->addUsingAlias(OperacionPagoPeer::EMPRESA_ID, $empresa->getId(), $comparison);
        } elseif ($empresa instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(OperacionPagoPeer::EMPRESA_ID, $empresa->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByEmpresa() only accepts arguments of type Empresa or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Empresa relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return OperacionPagoQuery The current query, for fluid interface
     */
    public function joinEmpresa($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Empresa');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Empresa');
        }

        return $this;
    }

    /**
     * Use the Empresa relation Empresa object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   EmpresaQuery A secondary query class using the current class as primary query
     */
    public function useEmpresaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinEmpresa($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Empresa', 'EmpresaQuery');
    }

    /**
     * Filter the query by a related Operacion object
     *
     * @param   Operacion|PropelObjectCollection $operacion The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   OperacionPagoQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByOperacion($operacion, $comparison = null)
    {
        if ($operacion instanceof Operacion) {
            return $this
                ->addUsingAlias(OperacionPagoPeer::OPERACION_ID, $operacion->getId(), $comparison);
        } elseif ($operacion instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(OperacionPagoPeer::OPERACION_ID, $operacion->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByOperacion() only accepts arguments of type Operacion or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Operacion relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return OperacionPagoQuery The current query, for fluid interface
     */
    public function joinOperacion($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Operacion');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Operacion');
        }

        return $this;
    }

    /**
     * Use the Operacion relation Operacion object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   OperacionQuery A secondary query class using the current class as primary query
     */
    public function useOperacionQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinOperacion($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Operacion', 'OperacionQuery');
    }

    /**
     * Filter the query by a related Banco object
     *
     * @param   Banco|PropelObjectCollection $banco The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   OperacionPagoQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByBanco($banco, $comparison = null)
    {
        if ($banco instanceof Banco) {
            return $this
                ->addUsingAlias(OperacionPagoPeer::BANCO_ID, $banco->getId(), $comparison);
        } elseif ($banco instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(OperacionPagoPeer::BANCO_ID, $banco->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByBanco() only accepts arguments of type Banco or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Banco relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return OperacionPagoQuery The current query, for fluid interface
     */
    public function joinBanco($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Banco');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Banco');
        }

        return $this;
    }

    /**
     * Use the Banco relation Banco object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   BancoQuery A secondary query class using the current class as primary query
     */
    public function useBancoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinBanco($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Banco', 'BancoQuery');
    }

    /**
     * Filter the query by a related CuentaBanco object
     *
     * @param   CuentaBanco|PropelObjectCollection $cuentaBanco  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   OperacionPagoQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByCuentaBanco($cuentaBanco, $comparison = null)
    {
        if ($cuentaBanco instanceof CuentaBanco) {
            return $this
                ->addUsingAlias(OperacionPagoPeer::ID, $cuentaBanco->getOperacionPagoId(), $comparison);
        } elseif ($cuentaBanco instanceof PropelObjectCollection) {
            return $this
                ->useCuentaBancoQuery()
                ->filterByPrimaryKeys($cuentaBanco->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByCuentaBanco() only accepts arguments of type CuentaBanco or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the CuentaBanco relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return OperacionPagoQuery The current query, for fluid interface
     */
    public function joinCuentaBanco($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('CuentaBanco');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'CuentaBanco');
        }

        return $this;
    }

    /**
     * Use the CuentaBanco relation CuentaBanco object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   CuentaBancoQuery A secondary query class using the current class as primary query
     */
    public function useCuentaBancoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinCuentaBanco($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'CuentaBanco', 'CuentaBancoQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   OperacionPago $operacionPago Object to remove from the list of results
     *
     * @return OperacionPagoQuery The current query, for fluid interface
     */
    public function prune($operacionPago = null)
    {
        if ($operacionPago) {
            $this->addUsingAlias(OperacionPagoPeer::ID, $operacionPago->getId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}
