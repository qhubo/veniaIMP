<?php


/**
 * Base class that represents a query for the 'cierre_dia' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.6.7 on:
 *
 * 12/28/25 02:48:06
 *
 * @method CierreQuery orderById($order = Criteria::ASC) Order by the id column
 * @method CierreQuery orderByFechaCreo($order = Criteria::ASC) Order by the fecha_creo column
 * @method CierreQuery orderByUsuario($order = Criteria::ASC) Order by the usuario column
 * @method CierreQuery orderByValorTotal($order = Criteria::ASC) Order by the valor_total column
 * @method CierreQuery orderByNoCierre($order = Criteria::ASC) Order by the no_cierre column
 * @method CierreQuery orderByEmpresaId($order = Criteria::ASC) Order by the empresa_id column
 * @method CierreQuery orderByTiendaId($order = Criteria::ASC) Order by the tienda_id column
 *
 * @method CierreQuery groupById() Group by the id column
 * @method CierreQuery groupByFechaCreo() Group by the fecha_creo column
 * @method CierreQuery groupByUsuario() Group by the usuario column
 * @method CierreQuery groupByValorTotal() Group by the valor_total column
 * @method CierreQuery groupByNoCierre() Group by the no_cierre column
 * @method CierreQuery groupByEmpresaId() Group by the empresa_id column
 * @method CierreQuery groupByTiendaId() Group by the tienda_id column
 *
 * @method CierreQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method CierreQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method CierreQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method CierreQuery leftJoinEmpresa($relationAlias = null) Adds a LEFT JOIN clause to the query using the Empresa relation
 * @method CierreQuery rightJoinEmpresa($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Empresa relation
 * @method CierreQuery innerJoinEmpresa($relationAlias = null) Adds a INNER JOIN clause to the query using the Empresa relation
 *
 * @method CierreQuery leftJoinTienda($relationAlias = null) Adds a LEFT JOIN clause to the query using the Tienda relation
 * @method CierreQuery rightJoinTienda($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Tienda relation
 * @method CierreQuery innerJoinTienda($relationAlias = null) Adds a INNER JOIN clause to the query using the Tienda relation
 *
 * @method CierreQuery leftJoinCierreCaja($relationAlias = null) Adds a LEFT JOIN clause to the query using the CierreCaja relation
 * @method CierreQuery rightJoinCierreCaja($relationAlias = null) Adds a RIGHT JOIN clause to the query using the CierreCaja relation
 * @method CierreQuery innerJoinCierreCaja($relationAlias = null) Adds a INNER JOIN clause to the query using the CierreCaja relation
 *
 * @method CierreQuery leftJoinCierreCheck($relationAlias = null) Adds a LEFT JOIN clause to the query using the CierreCheck relation
 * @method CierreQuery rightJoinCierreCheck($relationAlias = null) Adds a RIGHT JOIN clause to the query using the CierreCheck relation
 * @method CierreQuery innerJoinCierreCheck($relationAlias = null) Adds a INNER JOIN clause to the query using the CierreCheck relation
 *
 * @method Cierre findOne(PropelPDO $con = null) Return the first Cierre matching the query
 * @method Cierre findOneOrCreate(PropelPDO $con = null) Return the first Cierre matching the query, or a new Cierre object populated from the query conditions when no match is found
 *
 * @method Cierre findOneById(int $id) Return the first Cierre filtered by the id column
 * @method Cierre findOneByFechaCreo(string $fecha_creo) Return the first Cierre filtered by the fecha_creo column
 * @method Cierre findOneByUsuario(string $usuario) Return the first Cierre filtered by the usuario column
 * @method Cierre findOneByValorTotal(double $valor_total) Return the first Cierre filtered by the valor_total column
 * @method Cierre findOneByNoCierre(int $no_cierre) Return the first Cierre filtered by the no_cierre column
 * @method Cierre findOneByEmpresaId(int $empresa_id) Return the first Cierre filtered by the empresa_id column
 * @method Cierre findOneByTiendaId(int $tienda_id) Return the first Cierre filtered by the tienda_id column
 *
 * @method array findById(int $id) Return Cierre objects filtered by the id column
 * @method array findByFechaCreo(string $fecha_creo) Return Cierre objects filtered by the fecha_creo column
 * @method array findByUsuario(string $usuario) Return Cierre objects filtered by the usuario column
 * @method array findByValorTotal(double $valor_total) Return Cierre objects filtered by the valor_total column
 * @method array findByNoCierre(int $no_cierre) Return Cierre objects filtered by the no_cierre column
 * @method array findByEmpresaId(int $empresa_id) Return Cierre objects filtered by the empresa_id column
 * @method array findByTiendaId(int $tienda_id) Return Cierre objects filtered by the tienda_id column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseCierreQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseCierreQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = 'propel', $modelName = 'Cierre', $modelAlias = null)
    {
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new CierreQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param     CierreQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return CierreQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof CierreQuery) {
            return $criteria;
        }
        $query = new CierreQuery();
        if (null !== $modelAlias) {
            $query->setModelAlias($modelAlias);
        }
        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return   Cierre|Cierre[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = CierrePeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is alredy in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(CierrePeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return   Cierre A model object, or null if the key is not found
     * @throws   PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT `ID`, `FECHA_CREO`, `USUARIO`, `VALOR_TOTAL`, `NO_CIERRE`, `EMPRESA_ID`, `TIENDA_ID` FROM `cierre_dia` WHERE `ID` = :p0';
        try {
            $stmt = $con->prepare($sql);
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new Cierre();
            $obj->hydrate($row);
            CierrePeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return Cierre|Cierre[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|Cierre[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return CierreQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(CierrePeer::ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return CierreQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(CierrePeer::ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the id column
     *
     * Example usage:
     * <code>
     * $query->filterById(1234); // WHERE id = 1234
     * $query->filterById(array(12, 34)); // WHERE id IN (12, 34)
     * $query->filterById(array('min' => 12)); // WHERE id > 12
     * </code>
     *
     * @param     mixed $id The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return CierreQuery The current query, for fluid interface
     */
    public function filterById($id = null, $comparison = null)
    {
        if (is_array($id) && null === $comparison) {
            $comparison = Criteria::IN;
        }

        return $this->addUsingAlias(CierrePeer::ID, $id, $comparison);
    }

    /**
     * Filter the query on the fecha_creo column
     *
     * Example usage:
     * <code>
     * $query->filterByFechaCreo('2011-03-14'); // WHERE fecha_creo = '2011-03-14'
     * $query->filterByFechaCreo('now'); // WHERE fecha_creo = '2011-03-14'
     * $query->filterByFechaCreo(array('max' => 'yesterday')); // WHERE fecha_creo > '2011-03-13'
     * </code>
     *
     * @param     mixed $fechaCreo The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return CierreQuery The current query, for fluid interface
     */
    public function filterByFechaCreo($fechaCreo = null, $comparison = null)
    {
        if (is_array($fechaCreo)) {
            $useMinMax = false;
            if (isset($fechaCreo['min'])) {
                $this->addUsingAlias(CierrePeer::FECHA_CREO, $fechaCreo['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($fechaCreo['max'])) {
                $this->addUsingAlias(CierrePeer::FECHA_CREO, $fechaCreo['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(CierrePeer::FECHA_CREO, $fechaCreo, $comparison);
    }

    /**
     * Filter the query on the usuario column
     *
     * Example usage:
     * <code>
     * $query->filterByUsuario('fooValue');   // WHERE usuario = 'fooValue'
     * $query->filterByUsuario('%fooValue%'); // WHERE usuario LIKE '%fooValue%'
     * </code>
     *
     * @param     string $usuario The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return CierreQuery The current query, for fluid interface
     */
    public function filterByUsuario($usuario = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($usuario)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $usuario)) {
                $usuario = str_replace('*', '%', $usuario);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(CierrePeer::USUARIO, $usuario, $comparison);
    }

    /**
     * Filter the query on the valor_total column
     *
     * Example usage:
     * <code>
     * $query->filterByValorTotal(1234); // WHERE valor_total = 1234
     * $query->filterByValorTotal(array(12, 34)); // WHERE valor_total IN (12, 34)
     * $query->filterByValorTotal(array('min' => 12)); // WHERE valor_total > 12
     * </code>
     *
     * @param     mixed $valorTotal The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return CierreQuery The current query, for fluid interface
     */
    public function filterByValorTotal($valorTotal = null, $comparison = null)
    {
        if (is_array($valorTotal)) {
            $useMinMax = false;
            if (isset($valorTotal['min'])) {
                $this->addUsingAlias(CierrePeer::VALOR_TOTAL, $valorTotal['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($valorTotal['max'])) {
                $this->addUsingAlias(CierrePeer::VALOR_TOTAL, $valorTotal['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(CierrePeer::VALOR_TOTAL, $valorTotal, $comparison);
    }

    /**
     * Filter the query on the no_cierre column
     *
     * Example usage:
     * <code>
     * $query->filterByNoCierre(1234); // WHERE no_cierre = 1234
     * $query->filterByNoCierre(array(12, 34)); // WHERE no_cierre IN (12, 34)
     * $query->filterByNoCierre(array('min' => 12)); // WHERE no_cierre > 12
     * </code>
     *
     * @param     mixed $noCierre The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return CierreQuery The current query, for fluid interface
     */
    public function filterByNoCierre($noCierre = null, $comparison = null)
    {
        if (is_array($noCierre)) {
            $useMinMax = false;
            if (isset($noCierre['min'])) {
                $this->addUsingAlias(CierrePeer::NO_CIERRE, $noCierre['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($noCierre['max'])) {
                $this->addUsingAlias(CierrePeer::NO_CIERRE, $noCierre['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(CierrePeer::NO_CIERRE, $noCierre, $comparison);
    }

    /**
     * Filter the query on the empresa_id column
     *
     * Example usage:
     * <code>
     * $query->filterByEmpresaId(1234); // WHERE empresa_id = 1234
     * $query->filterByEmpresaId(array(12, 34)); // WHERE empresa_id IN (12, 34)
     * $query->filterByEmpresaId(array('min' => 12)); // WHERE empresa_id > 12
     * </code>
     *
     * @see       filterByEmpresa()
     *
     * @param     mixed $empresaId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return CierreQuery The current query, for fluid interface
     */
    public function filterByEmpresaId($empresaId = null, $comparison = null)
    {
        if (is_array($empresaId)) {
            $useMinMax = false;
            if (isset($empresaId['min'])) {
                $this->addUsingAlias(CierrePeer::EMPRESA_ID, $empresaId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($empresaId['max'])) {
                $this->addUsingAlias(CierrePeer::EMPRESA_ID, $empresaId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(CierrePeer::EMPRESA_ID, $empresaId, $comparison);
    }

    /**
     * Filter the query on the tienda_id column
     *
     * Example usage:
     * <code>
     * $query->filterByTiendaId(1234); // WHERE tienda_id = 1234
     * $query->filterByTiendaId(array(12, 34)); // WHERE tienda_id IN (12, 34)
     * $query->filterByTiendaId(array('min' => 12)); // WHERE tienda_id > 12
     * </code>
     *
     * @see       filterByTienda()
     *
     * @param     mixed $tiendaId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return CierreQuery The current query, for fluid interface
     */
    public function filterByTiendaId($tiendaId = null, $comparison = null)
    {
        if (is_array($tiendaId)) {
            $useMinMax = false;
            if (isset($tiendaId['min'])) {
                $this->addUsingAlias(CierrePeer::TIENDA_ID, $tiendaId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($tiendaId['max'])) {
                $this->addUsingAlias(CierrePeer::TIENDA_ID, $tiendaId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(CierrePeer::TIENDA_ID, $tiendaId, $comparison);
    }

    /**
     * Filter the query by a related Empresa object
     *
     * @param   Empresa|PropelObjectCollection $empresa The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   CierreQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByEmpresa($empresa, $comparison = null)
    {
        if ($empresa instanceof Empresa) {
            return $this
                ->addUsingAlias(CierrePeer::EMPRESA_ID, $empresa->getId(), $comparison);
        } elseif ($empresa instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(CierrePeer::EMPRESA_ID, $empresa->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByEmpresa() only accepts arguments of type Empresa or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Empresa relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return CierreQuery The current query, for fluid interface
     */
    public function joinEmpresa($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Empresa');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Empresa');
        }

        return $this;
    }

    /**
     * Use the Empresa relation Empresa object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   EmpresaQuery A secondary query class using the current class as primary query
     */
    public function useEmpresaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinEmpresa($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Empresa', 'EmpresaQuery');
    }

    /**
     * Filter the query by a related Tienda object
     *
     * @param   Tienda|PropelObjectCollection $tienda The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   CierreQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByTienda($tienda, $comparison = null)
    {
        if ($tienda instanceof Tienda) {
            return $this
                ->addUsingAlias(CierrePeer::TIENDA_ID, $tienda->getId(), $comparison);
        } elseif ($tienda instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(CierrePeer::TIENDA_ID, $tienda->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByTienda() only accepts arguments of type Tienda or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Tienda relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return CierreQuery The current query, for fluid interface
     */
    public function joinTienda($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Tienda');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Tienda');
        }

        return $this;
    }

    /**
     * Use the Tienda relation Tienda object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   TiendaQuery A secondary query class using the current class as primary query
     */
    public function useTiendaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinTienda($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Tienda', 'TiendaQuery');
    }

    /**
     * Filter the query by a related CierreCaja object
     *
     * @param   CierreCaja|PropelObjectCollection $cierreCaja  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   CierreQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByCierreCaja($cierreCaja, $comparison = null)
    {
        if ($cierreCaja instanceof CierreCaja) {
            return $this
                ->addUsingAlias(CierrePeer::ID, $cierreCaja->getCierreDiaId(), $comparison);
        } elseif ($cierreCaja instanceof PropelObjectCollection) {
            return $this
                ->useCierreCajaQuery()
                ->filterByPrimaryKeys($cierreCaja->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByCierreCaja() only accepts arguments of type CierreCaja or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the CierreCaja relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return CierreQuery The current query, for fluid interface
     */
    public function joinCierreCaja($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('CierreCaja');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'CierreCaja');
        }

        return $this;
    }

    /**
     * Use the CierreCaja relation CierreCaja object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   CierreCajaQuery A secondary query class using the current class as primary query
     */
    public function useCierreCajaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinCierreCaja($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'CierreCaja', 'CierreCajaQuery');
    }

    /**
     * Filter the query by a related CierreCheck object
     *
     * @param   CierreCheck|PropelObjectCollection $cierreCheck  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   CierreQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByCierreCheck($cierreCheck, $comparison = null)
    {
        if ($cierreCheck instanceof CierreCheck) {
            return $this
                ->addUsingAlias(CierrePeer::ID, $cierreCheck->getCierreDiaId(), $comparison);
        } elseif ($cierreCheck instanceof PropelObjectCollection) {
            return $this
                ->useCierreCheckQuery()
                ->filterByPrimaryKeys($cierreCheck->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByCierreCheck() only accepts arguments of type CierreCheck or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the CierreCheck relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return CierreQuery The current query, for fluid interface
     */
    public function joinCierreCheck($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('CierreCheck');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'CierreCheck');
        }

        return $this;
    }

    /**
     * Use the CierreCheck relation CierreCheck object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   CierreCheckQuery A secondary query class using the current class as primary query
     */
    public function useCierreCheckQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinCierreCheck($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'CierreCheck', 'CierreCheckQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   Cierre $cierre Object to remove from the list of results
     *
     * @return CierreQuery The current query, for fluid interface
     */
    public function prune($cierre = null)
    {
        if ($cierre) {
            $this->addUsingAlias(CierrePeer::ID, $cierre->getId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}
