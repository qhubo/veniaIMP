<?php


/**
 * Base class that represents a query for the 'solicitud_deposito' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.6.7 on:
 *
 * 01/03/26 04:09:46
 *
 * @method SolicitudDepositoQuery orderById($order = Criteria::ASC) Order by the id column
 * @method SolicitudDepositoQuery orderByCodigo($order = Criteria::ASC) Order by the codigo column
 * @method SolicitudDepositoQuery orderByBancoId($order = Criteria::ASC) Order by the banco_id column
 * @method SolicitudDepositoQuery orderByTiendaId($order = Criteria::ASC) Order by the tienda_id column
 * @method SolicitudDepositoQuery orderByFechaIngreso($order = Criteria::ASC) Order by the fecha_ingreso column
 * @method SolicitudDepositoQuery orderByFechaDeposito($order = Criteria::ASC) Order by the fecha_deposito column
 * @method SolicitudDepositoQuery orderByTotal($order = Criteria::ASC) Order by the total column
 * @method SolicitudDepositoQuery orderByBoleta($order = Criteria::ASC) Order by the boleta column
 * @method SolicitudDepositoQuery orderByVendedor($order = Criteria::ASC) Order by the vendedor column
 * @method SolicitudDepositoQuery orderByTelefono($order = Criteria::ASC) Order by the telefono column
 * @method SolicitudDepositoQuery orderByCliente($order = Criteria::ASC) Order by the cliente column
 * @method SolicitudDepositoQuery orderByPieza($order = Criteria::ASC) Order by the pieza column
 * @method SolicitudDepositoQuery orderByStock($order = Criteria::ASC) Order by the stock column
 * @method SolicitudDepositoQuery orderByDocumentoConfirmacion($order = Criteria::ASC) Order by the documento_confirmacion column
 * @method SolicitudDepositoQuery orderByUsuarioConfirmo($order = Criteria::ASC) Order by the usuario_confirmo column
 * @method SolicitudDepositoQuery orderByFechaConfirmo($order = Criteria::ASC) Order by the fecha_confirmo column
 * @method SolicitudDepositoQuery orderByEstatus($order = Criteria::ASC) Order by the estatus column
 * @method SolicitudDepositoQuery orderByCreatedAt($order = Criteria::ASC) Order by the created_at column
 * @method SolicitudDepositoQuery orderByUpdatedAt($order = Criteria::ASC) Order by the updated_at column
 * @method SolicitudDepositoQuery orderByCreatedBy($order = Criteria::ASC) Order by the created_by column
 * @method SolicitudDepositoQuery orderByUpdatedBy($order = Criteria::ASC) Order by the updated_by column
 * @method SolicitudDepositoQuery orderByEmpresaId($order = Criteria::ASC) Order by the empresa_id column
 *
 * @method SolicitudDepositoQuery groupById() Group by the id column
 * @method SolicitudDepositoQuery groupByCodigo() Group by the codigo column
 * @method SolicitudDepositoQuery groupByBancoId() Group by the banco_id column
 * @method SolicitudDepositoQuery groupByTiendaId() Group by the tienda_id column
 * @method SolicitudDepositoQuery groupByFechaIngreso() Group by the fecha_ingreso column
 * @method SolicitudDepositoQuery groupByFechaDeposito() Group by the fecha_deposito column
 * @method SolicitudDepositoQuery groupByTotal() Group by the total column
 * @method SolicitudDepositoQuery groupByBoleta() Group by the boleta column
 * @method SolicitudDepositoQuery groupByVendedor() Group by the vendedor column
 * @method SolicitudDepositoQuery groupByTelefono() Group by the telefono column
 * @method SolicitudDepositoQuery groupByCliente() Group by the cliente column
 * @method SolicitudDepositoQuery groupByPieza() Group by the pieza column
 * @method SolicitudDepositoQuery groupByStock() Group by the stock column
 * @method SolicitudDepositoQuery groupByDocumentoConfirmacion() Group by the documento_confirmacion column
 * @method SolicitudDepositoQuery groupByUsuarioConfirmo() Group by the usuario_confirmo column
 * @method SolicitudDepositoQuery groupByFechaConfirmo() Group by the fecha_confirmo column
 * @method SolicitudDepositoQuery groupByEstatus() Group by the estatus column
 * @method SolicitudDepositoQuery groupByCreatedAt() Group by the created_at column
 * @method SolicitudDepositoQuery groupByUpdatedAt() Group by the updated_at column
 * @method SolicitudDepositoQuery groupByCreatedBy() Group by the created_by column
 * @method SolicitudDepositoQuery groupByUpdatedBy() Group by the updated_by column
 * @method SolicitudDepositoQuery groupByEmpresaId() Group by the empresa_id column
 *
 * @method SolicitudDepositoQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method SolicitudDepositoQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method SolicitudDepositoQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method SolicitudDepositoQuery leftJoinBanco($relationAlias = null) Adds a LEFT JOIN clause to the query using the Banco relation
 * @method SolicitudDepositoQuery rightJoinBanco($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Banco relation
 * @method SolicitudDepositoQuery innerJoinBanco($relationAlias = null) Adds a INNER JOIN clause to the query using the Banco relation
 *
 * @method SolicitudDepositoQuery leftJoinTienda($relationAlias = null) Adds a LEFT JOIN clause to the query using the Tienda relation
 * @method SolicitudDepositoQuery rightJoinTienda($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Tienda relation
 * @method SolicitudDepositoQuery innerJoinTienda($relationAlias = null) Adds a INNER JOIN clause to the query using the Tienda relation
 *
 * @method SolicitudDepositoQuery leftJoinEmpresa($relationAlias = null) Adds a LEFT JOIN clause to the query using the Empresa relation
 * @method SolicitudDepositoQuery rightJoinEmpresa($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Empresa relation
 * @method SolicitudDepositoQuery innerJoinEmpresa($relationAlias = null) Adds a INNER JOIN clause to the query using the Empresa relation
 *
 * @method SolicitudDeposito findOne(PropelPDO $con = null) Return the first SolicitudDeposito matching the query
 * @method SolicitudDeposito findOneOrCreate(PropelPDO $con = null) Return the first SolicitudDeposito matching the query, or a new SolicitudDeposito object populated from the query conditions when no match is found
 *
 * @method SolicitudDeposito findOneById(int $id) Return the first SolicitudDeposito filtered by the id column
 * @method SolicitudDeposito findOneByCodigo(string $codigo) Return the first SolicitudDeposito filtered by the codigo column
 * @method SolicitudDeposito findOneByBancoId(int $banco_id) Return the first SolicitudDeposito filtered by the banco_id column
 * @method SolicitudDeposito findOneByTiendaId(int $tienda_id) Return the first SolicitudDeposito filtered by the tienda_id column
 * @method SolicitudDeposito findOneByFechaIngreso(string $fecha_ingreso) Return the first SolicitudDeposito filtered by the fecha_ingreso column
 * @method SolicitudDeposito findOneByFechaDeposito(string $fecha_deposito) Return the first SolicitudDeposito filtered by the fecha_deposito column
 * @method SolicitudDeposito findOneByTotal(double $total) Return the first SolicitudDeposito filtered by the total column
 * @method SolicitudDeposito findOneByBoleta(string $boleta) Return the first SolicitudDeposito filtered by the boleta column
 * @method SolicitudDeposito findOneByVendedor(string $vendedor) Return the first SolicitudDeposito filtered by the vendedor column
 * @method SolicitudDeposito findOneByTelefono(string $telefono) Return the first SolicitudDeposito filtered by the telefono column
 * @method SolicitudDeposito findOneByCliente(string $cliente) Return the first SolicitudDeposito filtered by the cliente column
 * @method SolicitudDeposito findOneByPieza(string $pieza) Return the first SolicitudDeposito filtered by the pieza column
 * @method SolicitudDeposito findOneByStock(string $stock) Return the first SolicitudDeposito filtered by the stock column
 * @method SolicitudDeposito findOneByDocumentoConfirmacion(string $documento_confirmacion) Return the first SolicitudDeposito filtered by the documento_confirmacion column
 * @method SolicitudDeposito findOneByUsuarioConfirmo(string $usuario_confirmo) Return the first SolicitudDeposito filtered by the usuario_confirmo column
 * @method SolicitudDeposito findOneByFechaConfirmo(string $fecha_confirmo) Return the first SolicitudDeposito filtered by the fecha_confirmo column
 * @method SolicitudDeposito findOneByEstatus(string $estatus) Return the first SolicitudDeposito filtered by the estatus column
 * @method SolicitudDeposito findOneByCreatedAt(string $created_at) Return the first SolicitudDeposito filtered by the created_at column
 * @method SolicitudDeposito findOneByUpdatedAt(string $updated_at) Return the first SolicitudDeposito filtered by the updated_at column
 * @method SolicitudDeposito findOneByCreatedBy(string $created_by) Return the first SolicitudDeposito filtered by the created_by column
 * @method SolicitudDeposito findOneByUpdatedBy(string $updated_by) Return the first SolicitudDeposito filtered by the updated_by column
 * @method SolicitudDeposito findOneByEmpresaId(int $empresa_id) Return the first SolicitudDeposito filtered by the empresa_id column
 *
 * @method array findById(int $id) Return SolicitudDeposito objects filtered by the id column
 * @method array findByCodigo(string $codigo) Return SolicitudDeposito objects filtered by the codigo column
 * @method array findByBancoId(int $banco_id) Return SolicitudDeposito objects filtered by the banco_id column
 * @method array findByTiendaId(int $tienda_id) Return SolicitudDeposito objects filtered by the tienda_id column
 * @method array findByFechaIngreso(string $fecha_ingreso) Return SolicitudDeposito objects filtered by the fecha_ingreso column
 * @method array findByFechaDeposito(string $fecha_deposito) Return SolicitudDeposito objects filtered by the fecha_deposito column
 * @method array findByTotal(double $total) Return SolicitudDeposito objects filtered by the total column
 * @method array findByBoleta(string $boleta) Return SolicitudDeposito objects filtered by the boleta column
 * @method array findByVendedor(string $vendedor) Return SolicitudDeposito objects filtered by the vendedor column
 * @method array findByTelefono(string $telefono) Return SolicitudDeposito objects filtered by the telefono column
 * @method array findByCliente(string $cliente) Return SolicitudDeposito objects filtered by the cliente column
 * @method array findByPieza(string $pieza) Return SolicitudDeposito objects filtered by the pieza column
 * @method array findByStock(string $stock) Return SolicitudDeposito objects filtered by the stock column
 * @method array findByDocumentoConfirmacion(string $documento_confirmacion) Return SolicitudDeposito objects filtered by the documento_confirmacion column
 * @method array findByUsuarioConfirmo(string $usuario_confirmo) Return SolicitudDeposito objects filtered by the usuario_confirmo column
 * @method array findByFechaConfirmo(string $fecha_confirmo) Return SolicitudDeposito objects filtered by the fecha_confirmo column
 * @method array findByEstatus(string $estatus) Return SolicitudDeposito objects filtered by the estatus column
 * @method array findByCreatedAt(string $created_at) Return SolicitudDeposito objects filtered by the created_at column
 * @method array findByUpdatedAt(string $updated_at) Return SolicitudDeposito objects filtered by the updated_at column
 * @method array findByCreatedBy(string $created_by) Return SolicitudDeposito objects filtered by the created_by column
 * @method array findByUpdatedBy(string $updated_by) Return SolicitudDeposito objects filtered by the updated_by column
 * @method array findByEmpresaId(int $empresa_id) Return SolicitudDeposito objects filtered by the empresa_id column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseSolicitudDepositoQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseSolicitudDepositoQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = 'propel', $modelName = 'SolicitudDeposito', $modelAlias = null)
    {
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new SolicitudDepositoQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param     SolicitudDepositoQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return SolicitudDepositoQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof SolicitudDepositoQuery) {
            return $criteria;
        }
        $query = new SolicitudDepositoQuery();
        if (null !== $modelAlias) {
            $query->setModelAlias($modelAlias);
        }
        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return   SolicitudDeposito|SolicitudDeposito[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = SolicitudDepositoPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is alredy in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(SolicitudDepositoPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return   SolicitudDeposito A model object, or null if the key is not found
     * @throws   PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT `ID`, `CODIGO`, `BANCO_ID`, `TIENDA_ID`, `FECHA_INGRESO`, `FECHA_DEPOSITO`, `TOTAL`, `BOLETA`, `VENDEDOR`, `TELEFONO`, `CLIENTE`, `PIEZA`, `STOCK`, `DOCUMENTO_CONFIRMACION`, `USUARIO_CONFIRMO`, `FECHA_CONFIRMO`, `ESTATUS`, `CREATED_AT`, `UPDATED_AT`, `CREATED_BY`, `UPDATED_BY`, `EMPRESA_ID` FROM `solicitud_deposito` WHERE `ID` = :p0';
        try {
            $stmt = $con->prepare($sql);
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new SolicitudDeposito();
            $obj->hydrate($row);
            SolicitudDepositoPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return SolicitudDeposito|SolicitudDeposito[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|SolicitudDeposito[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return SolicitudDepositoQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(SolicitudDepositoPeer::ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return SolicitudDepositoQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(SolicitudDepositoPeer::ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the id column
     *
     * Example usage:
     * <code>
     * $query->filterById(1234); // WHERE id = 1234
     * $query->filterById(array(12, 34)); // WHERE id IN (12, 34)
     * $query->filterById(array('min' => 12)); // WHERE id > 12
     * </code>
     *
     * @param     mixed $id The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return SolicitudDepositoQuery The current query, for fluid interface
     */
    public function filterById($id = null, $comparison = null)
    {
        if (is_array($id) && null === $comparison) {
            $comparison = Criteria::IN;
        }

        return $this->addUsingAlias(SolicitudDepositoPeer::ID, $id, $comparison);
    }

    /**
     * Filter the query on the codigo column
     *
     * Example usage:
     * <code>
     * $query->filterByCodigo('fooValue');   // WHERE codigo = 'fooValue'
     * $query->filterByCodigo('%fooValue%'); // WHERE codigo LIKE '%fooValue%'
     * </code>
     *
     * @param     string $codigo The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return SolicitudDepositoQuery The current query, for fluid interface
     */
    public function filterByCodigo($codigo = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($codigo)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $codigo)) {
                $codigo = str_replace('*', '%', $codigo);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(SolicitudDepositoPeer::CODIGO, $codigo, $comparison);
    }

    /**
     * Filter the query on the banco_id column
     *
     * Example usage:
     * <code>
     * $query->filterByBancoId(1234); // WHERE banco_id = 1234
     * $query->filterByBancoId(array(12, 34)); // WHERE banco_id IN (12, 34)
     * $query->filterByBancoId(array('min' => 12)); // WHERE banco_id > 12
     * </code>
     *
     * @see       filterByBanco()
     *
     * @param     mixed $bancoId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return SolicitudDepositoQuery The current query, for fluid interface
     */
    public function filterByBancoId($bancoId = null, $comparison = null)
    {
        if (is_array($bancoId)) {
            $useMinMax = false;
            if (isset($bancoId['min'])) {
                $this->addUsingAlias(SolicitudDepositoPeer::BANCO_ID, $bancoId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($bancoId['max'])) {
                $this->addUsingAlias(SolicitudDepositoPeer::BANCO_ID, $bancoId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(SolicitudDepositoPeer::BANCO_ID, $bancoId, $comparison);
    }

    /**
     * Filter the query on the tienda_id column
     *
     * Example usage:
     * <code>
     * $query->filterByTiendaId(1234); // WHERE tienda_id = 1234
     * $query->filterByTiendaId(array(12, 34)); // WHERE tienda_id IN (12, 34)
     * $query->filterByTiendaId(array('min' => 12)); // WHERE tienda_id > 12
     * </code>
     *
     * @see       filterByTienda()
     *
     * @param     mixed $tiendaId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return SolicitudDepositoQuery The current query, for fluid interface
     */
    public function filterByTiendaId($tiendaId = null, $comparison = null)
    {
        if (is_array($tiendaId)) {
            $useMinMax = false;
            if (isset($tiendaId['min'])) {
                $this->addUsingAlias(SolicitudDepositoPeer::TIENDA_ID, $tiendaId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($tiendaId['max'])) {
                $this->addUsingAlias(SolicitudDepositoPeer::TIENDA_ID, $tiendaId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(SolicitudDepositoPeer::TIENDA_ID, $tiendaId, $comparison);
    }

    /**
     * Filter the query on the fecha_ingreso column
     *
     * Example usage:
     * <code>
     * $query->filterByFechaIngreso('2011-03-14'); // WHERE fecha_ingreso = '2011-03-14'
     * $query->filterByFechaIngreso('now'); // WHERE fecha_ingreso = '2011-03-14'
     * $query->filterByFechaIngreso(array('max' => 'yesterday')); // WHERE fecha_ingreso > '2011-03-13'
     * </code>
     *
     * @param     mixed $fechaIngreso The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return SolicitudDepositoQuery The current query, for fluid interface
     */
    public function filterByFechaIngreso($fechaIngreso = null, $comparison = null)
    {
        if (is_array($fechaIngreso)) {
            $useMinMax = false;
            if (isset($fechaIngreso['min'])) {
                $this->addUsingAlias(SolicitudDepositoPeer::FECHA_INGRESO, $fechaIngreso['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($fechaIngreso['max'])) {
                $this->addUsingAlias(SolicitudDepositoPeer::FECHA_INGRESO, $fechaIngreso['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(SolicitudDepositoPeer::FECHA_INGRESO, $fechaIngreso, $comparison);
    }

    /**
     * Filter the query on the fecha_deposito column
     *
     * Example usage:
     * <code>
     * $query->filterByFechaDeposito('2011-03-14'); // WHERE fecha_deposito = '2011-03-14'
     * $query->filterByFechaDeposito('now'); // WHERE fecha_deposito = '2011-03-14'
     * $query->filterByFechaDeposito(array('max' => 'yesterday')); // WHERE fecha_deposito > '2011-03-13'
     * </code>
     *
     * @param     mixed $fechaDeposito The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return SolicitudDepositoQuery The current query, for fluid interface
     */
    public function filterByFechaDeposito($fechaDeposito = null, $comparison = null)
    {
        if (is_array($fechaDeposito)) {
            $useMinMax = false;
            if (isset($fechaDeposito['min'])) {
                $this->addUsingAlias(SolicitudDepositoPeer::FECHA_DEPOSITO, $fechaDeposito['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($fechaDeposito['max'])) {
                $this->addUsingAlias(SolicitudDepositoPeer::FECHA_DEPOSITO, $fechaDeposito['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(SolicitudDepositoPeer::FECHA_DEPOSITO, $fechaDeposito, $comparison);
    }

    /**
     * Filter the query on the total column
     *
     * Example usage:
     * <code>
     * $query->filterByTotal(1234); // WHERE total = 1234
     * $query->filterByTotal(array(12, 34)); // WHERE total IN (12, 34)
     * $query->filterByTotal(array('min' => 12)); // WHERE total > 12
     * </code>
     *
     * @param     mixed $total The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return SolicitudDepositoQuery The current query, for fluid interface
     */
    public function filterByTotal($total = null, $comparison = null)
    {
        if (is_array($total)) {
            $useMinMax = false;
            if (isset($total['min'])) {
                $this->addUsingAlias(SolicitudDepositoPeer::TOTAL, $total['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($total['max'])) {
                $this->addUsingAlias(SolicitudDepositoPeer::TOTAL, $total['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(SolicitudDepositoPeer::TOTAL, $total, $comparison);
    }

    /**
     * Filter the query on the boleta column
     *
     * Example usage:
     * <code>
     * $query->filterByBoleta('fooValue');   // WHERE boleta = 'fooValue'
     * $query->filterByBoleta('%fooValue%'); // WHERE boleta LIKE '%fooValue%'
     * </code>
     *
     * @param     string $boleta The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return SolicitudDepositoQuery The current query, for fluid interface
     */
    public function filterByBoleta($boleta = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($boleta)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $boleta)) {
                $boleta = str_replace('*', '%', $boleta);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(SolicitudDepositoPeer::BOLETA, $boleta, $comparison);
    }

    /**
     * Filter the query on the vendedor column
     *
     * Example usage:
     * <code>
     * $query->filterByVendedor('fooValue');   // WHERE vendedor = 'fooValue'
     * $query->filterByVendedor('%fooValue%'); // WHERE vendedor LIKE '%fooValue%'
     * </code>
     *
     * @param     string $vendedor The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return SolicitudDepositoQuery The current query, for fluid interface
     */
    public function filterByVendedor($vendedor = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($vendedor)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $vendedor)) {
                $vendedor = str_replace('*', '%', $vendedor);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(SolicitudDepositoPeer::VENDEDOR, $vendedor, $comparison);
    }

    /**
     * Filter the query on the telefono column
     *
     * Example usage:
     * <code>
     * $query->filterByTelefono('fooValue');   // WHERE telefono = 'fooValue'
     * $query->filterByTelefono('%fooValue%'); // WHERE telefono LIKE '%fooValue%'
     * </code>
     *
     * @param     string $telefono The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return SolicitudDepositoQuery The current query, for fluid interface
     */
    public function filterByTelefono($telefono = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($telefono)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $telefono)) {
                $telefono = str_replace('*', '%', $telefono);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(SolicitudDepositoPeer::TELEFONO, $telefono, $comparison);
    }

    /**
     * Filter the query on the cliente column
     *
     * Example usage:
     * <code>
     * $query->filterByCliente('fooValue');   // WHERE cliente = 'fooValue'
     * $query->filterByCliente('%fooValue%'); // WHERE cliente LIKE '%fooValue%'
     * </code>
     *
     * @param     string $cliente The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return SolicitudDepositoQuery The current query, for fluid interface
     */
    public function filterByCliente($cliente = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($cliente)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $cliente)) {
                $cliente = str_replace('*', '%', $cliente);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(SolicitudDepositoPeer::CLIENTE, $cliente, $comparison);
    }

    /**
     * Filter the query on the pieza column
     *
     * Example usage:
     * <code>
     * $query->filterByPieza('fooValue');   // WHERE pieza = 'fooValue'
     * $query->filterByPieza('%fooValue%'); // WHERE pieza LIKE '%fooValue%'
     * </code>
     *
     * @param     string $pieza The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return SolicitudDepositoQuery The current query, for fluid interface
     */
    public function filterByPieza($pieza = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($pieza)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $pieza)) {
                $pieza = str_replace('*', '%', $pieza);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(SolicitudDepositoPeer::PIEZA, $pieza, $comparison);
    }

    /**
     * Filter the query on the stock column
     *
     * Example usage:
     * <code>
     * $query->filterByStock('fooValue');   // WHERE stock = 'fooValue'
     * $query->filterByStock('%fooValue%'); // WHERE stock LIKE '%fooValue%'
     * </code>
     *
     * @param     string $stock The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return SolicitudDepositoQuery The current query, for fluid interface
     */
    public function filterByStock($stock = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($stock)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $stock)) {
                $stock = str_replace('*', '%', $stock);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(SolicitudDepositoPeer::STOCK, $stock, $comparison);
    }

    /**
     * Filter the query on the documento_confirmacion column
     *
     * Example usage:
     * <code>
     * $query->filterByDocumentoConfirmacion('fooValue');   // WHERE documento_confirmacion = 'fooValue'
     * $query->filterByDocumentoConfirmacion('%fooValue%'); // WHERE documento_confirmacion LIKE '%fooValue%'
     * </code>
     *
     * @param     string $documentoConfirmacion The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return SolicitudDepositoQuery The current query, for fluid interface
     */
    public function filterByDocumentoConfirmacion($documentoConfirmacion = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($documentoConfirmacion)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $documentoConfirmacion)) {
                $documentoConfirmacion = str_replace('*', '%', $documentoConfirmacion);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(SolicitudDepositoPeer::DOCUMENTO_CONFIRMACION, $documentoConfirmacion, $comparison);
    }

    /**
     * Filter the query on the usuario_confirmo column
     *
     * Example usage:
     * <code>
     * $query->filterByUsuarioConfirmo('fooValue');   // WHERE usuario_confirmo = 'fooValue'
     * $query->filterByUsuarioConfirmo('%fooValue%'); // WHERE usuario_confirmo LIKE '%fooValue%'
     * </code>
     *
     * @param     string $usuarioConfirmo The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return SolicitudDepositoQuery The current query, for fluid interface
     */
    public function filterByUsuarioConfirmo($usuarioConfirmo = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($usuarioConfirmo)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $usuarioConfirmo)) {
                $usuarioConfirmo = str_replace('*', '%', $usuarioConfirmo);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(SolicitudDepositoPeer::USUARIO_CONFIRMO, $usuarioConfirmo, $comparison);
    }

    /**
     * Filter the query on the fecha_confirmo column
     *
     * Example usage:
     * <code>
     * $query->filterByFechaConfirmo('2011-03-14'); // WHERE fecha_confirmo = '2011-03-14'
     * $query->filterByFechaConfirmo('now'); // WHERE fecha_confirmo = '2011-03-14'
     * $query->filterByFechaConfirmo(array('max' => 'yesterday')); // WHERE fecha_confirmo > '2011-03-13'
     * </code>
     *
     * @param     mixed $fechaConfirmo The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return SolicitudDepositoQuery The current query, for fluid interface
     */
    public function filterByFechaConfirmo($fechaConfirmo = null, $comparison = null)
    {
        if (is_array($fechaConfirmo)) {
            $useMinMax = false;
            if (isset($fechaConfirmo['min'])) {
                $this->addUsingAlias(SolicitudDepositoPeer::FECHA_CONFIRMO, $fechaConfirmo['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($fechaConfirmo['max'])) {
                $this->addUsingAlias(SolicitudDepositoPeer::FECHA_CONFIRMO, $fechaConfirmo['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(SolicitudDepositoPeer::FECHA_CONFIRMO, $fechaConfirmo, $comparison);
    }

    /**
     * Filter the query on the estatus column
     *
     * Example usage:
     * <code>
     * $query->filterByEstatus('fooValue');   // WHERE estatus = 'fooValue'
     * $query->filterByEstatus('%fooValue%'); // WHERE estatus LIKE '%fooValue%'
     * </code>
     *
     * @param     string $estatus The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return SolicitudDepositoQuery The current query, for fluid interface
     */
    public function filterByEstatus($estatus = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($estatus)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $estatus)) {
                $estatus = str_replace('*', '%', $estatus);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(SolicitudDepositoPeer::ESTATUS, $estatus, $comparison);
    }

    /**
     * Filter the query on the created_at column
     *
     * Example usage:
     * <code>
     * $query->filterByCreatedAt('2011-03-14'); // WHERE created_at = '2011-03-14'
     * $query->filterByCreatedAt('now'); // WHERE created_at = '2011-03-14'
     * $query->filterByCreatedAt(array('max' => 'yesterday')); // WHERE created_at > '2011-03-13'
     * </code>
     *
     * @param     mixed $createdAt The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return SolicitudDepositoQuery The current query, for fluid interface
     */
    public function filterByCreatedAt($createdAt = null, $comparison = null)
    {
        if (is_array($createdAt)) {
            $useMinMax = false;
            if (isset($createdAt['min'])) {
                $this->addUsingAlias(SolicitudDepositoPeer::CREATED_AT, $createdAt['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($createdAt['max'])) {
                $this->addUsingAlias(SolicitudDepositoPeer::CREATED_AT, $createdAt['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(SolicitudDepositoPeer::CREATED_AT, $createdAt, $comparison);
    }

    /**
     * Filter the query on the updated_at column
     *
     * Example usage:
     * <code>
     * $query->filterByUpdatedAt('2011-03-14'); // WHERE updated_at = '2011-03-14'
     * $query->filterByUpdatedAt('now'); // WHERE updated_at = '2011-03-14'
     * $query->filterByUpdatedAt(array('max' => 'yesterday')); // WHERE updated_at > '2011-03-13'
     * </code>
     *
     * @param     mixed $updatedAt The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return SolicitudDepositoQuery The current query, for fluid interface
     */
    public function filterByUpdatedAt($updatedAt = null, $comparison = null)
    {
        if (is_array($updatedAt)) {
            $useMinMax = false;
            if (isset($updatedAt['min'])) {
                $this->addUsingAlias(SolicitudDepositoPeer::UPDATED_AT, $updatedAt['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($updatedAt['max'])) {
                $this->addUsingAlias(SolicitudDepositoPeer::UPDATED_AT, $updatedAt['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(SolicitudDepositoPeer::UPDATED_AT, $updatedAt, $comparison);
    }

    /**
     * Filter the query on the created_by column
     *
     * Example usage:
     * <code>
     * $query->filterByCreatedBy('fooValue');   // WHERE created_by = 'fooValue'
     * $query->filterByCreatedBy('%fooValue%'); // WHERE created_by LIKE '%fooValue%'
     * </code>
     *
     * @param     string $createdBy The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return SolicitudDepositoQuery The current query, for fluid interface
     */
    public function filterByCreatedBy($createdBy = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($createdBy)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $createdBy)) {
                $createdBy = str_replace('*', '%', $createdBy);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(SolicitudDepositoPeer::CREATED_BY, $createdBy, $comparison);
    }

    /**
     * Filter the query on the updated_by column
     *
     * Example usage:
     * <code>
     * $query->filterByUpdatedBy('fooValue');   // WHERE updated_by = 'fooValue'
     * $query->filterByUpdatedBy('%fooValue%'); // WHERE updated_by LIKE '%fooValue%'
     * </code>
     *
     * @param     string $updatedBy The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return SolicitudDepositoQuery The current query, for fluid interface
     */
    public function filterByUpdatedBy($updatedBy = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($updatedBy)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $updatedBy)) {
                $updatedBy = str_replace('*', '%', $updatedBy);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(SolicitudDepositoPeer::UPDATED_BY, $updatedBy, $comparison);
    }

    /**
     * Filter the query on the empresa_id column
     *
     * Example usage:
     * <code>
     * $query->filterByEmpresaId(1234); // WHERE empresa_id = 1234
     * $query->filterByEmpresaId(array(12, 34)); // WHERE empresa_id IN (12, 34)
     * $query->filterByEmpresaId(array('min' => 12)); // WHERE empresa_id > 12
     * </code>
     *
     * @see       filterByEmpresa()
     *
     * @param     mixed $empresaId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return SolicitudDepositoQuery The current query, for fluid interface
     */
    public function filterByEmpresaId($empresaId = null, $comparison = null)
    {
        if (is_array($empresaId)) {
            $useMinMax = false;
            if (isset($empresaId['min'])) {
                $this->addUsingAlias(SolicitudDepositoPeer::EMPRESA_ID, $empresaId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($empresaId['max'])) {
                $this->addUsingAlias(SolicitudDepositoPeer::EMPRESA_ID, $empresaId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(SolicitudDepositoPeer::EMPRESA_ID, $empresaId, $comparison);
    }

    /**
     * Filter the query by a related Banco object
     *
     * @param   Banco|PropelObjectCollection $banco The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   SolicitudDepositoQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByBanco($banco, $comparison = null)
    {
        if ($banco instanceof Banco) {
            return $this
                ->addUsingAlias(SolicitudDepositoPeer::BANCO_ID, $banco->getId(), $comparison);
        } elseif ($banco instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(SolicitudDepositoPeer::BANCO_ID, $banco->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByBanco() only accepts arguments of type Banco or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Banco relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return SolicitudDepositoQuery The current query, for fluid interface
     */
    public function joinBanco($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Banco');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Banco');
        }

        return $this;
    }

    /**
     * Use the Banco relation Banco object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   BancoQuery A secondary query class using the current class as primary query
     */
    public function useBancoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinBanco($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Banco', 'BancoQuery');
    }

    /**
     * Filter the query by a related Tienda object
     *
     * @param   Tienda|PropelObjectCollection $tienda The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   SolicitudDepositoQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByTienda($tienda, $comparison = null)
    {
        if ($tienda instanceof Tienda) {
            return $this
                ->addUsingAlias(SolicitudDepositoPeer::TIENDA_ID, $tienda->getId(), $comparison);
        } elseif ($tienda instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(SolicitudDepositoPeer::TIENDA_ID, $tienda->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByTienda() only accepts arguments of type Tienda or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Tienda relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return SolicitudDepositoQuery The current query, for fluid interface
     */
    public function joinTienda($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Tienda');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Tienda');
        }

        return $this;
    }

    /**
     * Use the Tienda relation Tienda object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   TiendaQuery A secondary query class using the current class as primary query
     */
    public function useTiendaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinTienda($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Tienda', 'TiendaQuery');
    }

    /**
     * Filter the query by a related Empresa object
     *
     * @param   Empresa|PropelObjectCollection $empresa The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   SolicitudDepositoQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByEmpresa($empresa, $comparison = null)
    {
        if ($empresa instanceof Empresa) {
            return $this
                ->addUsingAlias(SolicitudDepositoPeer::EMPRESA_ID, $empresa->getId(), $comparison);
        } elseif ($empresa instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(SolicitudDepositoPeer::EMPRESA_ID, $empresa->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByEmpresa() only accepts arguments of type Empresa or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Empresa relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return SolicitudDepositoQuery The current query, for fluid interface
     */
    public function joinEmpresa($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Empresa');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Empresa');
        }

        return $this;
    }

    /**
     * Use the Empresa relation Empresa object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   EmpresaQuery A secondary query class using the current class as primary query
     */
    public function useEmpresaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinEmpresa($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Empresa', 'EmpresaQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   SolicitudDeposito $solicitudDeposito Object to remove from the list of results
     *
     * @return SolicitudDepositoQuery The current query, for fluid interface
     */
    public function prune($solicitudDeposito = null)
    {
        if ($solicitudDeposito) {
            $this->addUsingAlias(SolicitudDepositoPeer::ID, $solicitudDeposito->getId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}
