<?php


/**
 * Base class that represents a query for the 'tipo_aparato' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.6.7 on:
 *
 * 12/26/25 01:36:29
 *
 * @method TipoAparatoQuery orderById($order = Criteria::ASC) Order by the id column
 * @method TipoAparatoQuery orderByEmpresaId($order = Criteria::ASC) Order by the empresa_id column
 * @method TipoAparatoQuery orderByCodigo($order = Criteria::ASC) Order by the codigo column
 * @method TipoAparatoQuery orderByDescripcion($order = Criteria::ASC) Order by the descripcion column
 * @method TipoAparatoQuery orderByActivo($order = Criteria::ASC) Order by the activo column
 * @method TipoAparatoQuery orderByMenuInvertido($order = Criteria::ASC) Order by the menu_invertido column
 * @method TipoAparatoQuery orderByMuestraMenu($order = Criteria::ASC) Order by the muestra_menu column
 * @method TipoAparatoQuery orderByLogo($order = Criteria::ASC) Order by the logo column
 * @method TipoAparatoQuery orderByCargoPesoLibraProducto($order = Criteria::ASC) Order by the cargo_peso_libra_producto column
 * @method TipoAparatoQuery orderByImagen($order = Criteria::ASC) Order by the imagen column
 * @method TipoAparatoQuery orderByOrdenMostrar($order = Criteria::ASC) Order by the orden_mostrar column
 * @method TipoAparatoQuery orderByMenuMuestraProducto($order = Criteria::ASC) Order by the menu_muestra_producto column
 * @method TipoAparatoQuery orderByTipoMenu($order = Criteria::ASC) Order by the tipo_menu column
 * @method TipoAparatoQuery orderByMenuLateral($order = Criteria::ASC) Order by the menu_lateral column
 * @method TipoAparatoQuery orderByReceta($order = Criteria::ASC) Order by the receta column
 * @method TipoAparatoQuery orderByStatus($order = Criteria::ASC) Order by the status column
 * @method TipoAparatoQuery orderByCuentaContable($order = Criteria::ASC) Order by the cuenta_contable column
 * @method TipoAparatoQuery orderByVenta($order = Criteria::ASC) Order by the venta column
 *
 * @method TipoAparatoQuery groupById() Group by the id column
 * @method TipoAparatoQuery groupByEmpresaId() Group by the empresa_id column
 * @method TipoAparatoQuery groupByCodigo() Group by the codigo column
 * @method TipoAparatoQuery groupByDescripcion() Group by the descripcion column
 * @method TipoAparatoQuery groupByActivo() Group by the activo column
 * @method TipoAparatoQuery groupByMenuInvertido() Group by the menu_invertido column
 * @method TipoAparatoQuery groupByMuestraMenu() Group by the muestra_menu column
 * @method TipoAparatoQuery groupByLogo() Group by the logo column
 * @method TipoAparatoQuery groupByCargoPesoLibraProducto() Group by the cargo_peso_libra_producto column
 * @method TipoAparatoQuery groupByImagen() Group by the imagen column
 * @method TipoAparatoQuery groupByOrdenMostrar() Group by the orden_mostrar column
 * @method TipoAparatoQuery groupByMenuMuestraProducto() Group by the menu_muestra_producto column
 * @method TipoAparatoQuery groupByTipoMenu() Group by the tipo_menu column
 * @method TipoAparatoQuery groupByMenuLateral() Group by the menu_lateral column
 * @method TipoAparatoQuery groupByReceta() Group by the receta column
 * @method TipoAparatoQuery groupByStatus() Group by the status column
 * @method TipoAparatoQuery groupByCuentaContable() Group by the cuenta_contable column
 * @method TipoAparatoQuery groupByVenta() Group by the venta column
 *
 * @method TipoAparatoQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method TipoAparatoQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method TipoAparatoQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method TipoAparatoQuery leftJoinEmpresa($relationAlias = null) Adds a LEFT JOIN clause to the query using the Empresa relation
 * @method TipoAparatoQuery rightJoinEmpresa($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Empresa relation
 * @method TipoAparatoQuery innerJoinEmpresa($relationAlias = null) Adds a INNER JOIN clause to the query using the Empresa relation
 *
 * @method TipoAparatoQuery leftJoinMarca($relationAlias = null) Adds a LEFT JOIN clause to the query using the Marca relation
 * @method TipoAparatoQuery rightJoinMarca($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Marca relation
 * @method TipoAparatoQuery innerJoinMarca($relationAlias = null) Adds a INNER JOIN clause to the query using the Marca relation
 *
 * @method TipoAparatoQuery leftJoinProducto($relationAlias = null) Adds a LEFT JOIN clause to the query using the Producto relation
 * @method TipoAparatoQuery rightJoinProducto($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Producto relation
 * @method TipoAparatoQuery innerJoinProducto($relationAlias = null) Adds a INNER JOIN clause to the query using the Producto relation
 *
 * @method TipoAparato findOne(PropelPDO $con = null) Return the first TipoAparato matching the query
 * @method TipoAparato findOneOrCreate(PropelPDO $con = null) Return the first TipoAparato matching the query, or a new TipoAparato object populated from the query conditions when no match is found
 *
 * @method TipoAparato findOneById(int $id) Return the first TipoAparato filtered by the id column
 * @method TipoAparato findOneByEmpresaId(int $empresa_id) Return the first TipoAparato filtered by the empresa_id column
 * @method TipoAparato findOneByCodigo(string $codigo) Return the first TipoAparato filtered by the codigo column
 * @method TipoAparato findOneByDescripcion(string $descripcion) Return the first TipoAparato filtered by the descripcion column
 * @method TipoAparato findOneByActivo(boolean $activo) Return the first TipoAparato filtered by the activo column
 * @method TipoAparato findOneByMenuInvertido(boolean $menu_invertido) Return the first TipoAparato filtered by the menu_invertido column
 * @method TipoAparato findOneByMuestraMenu(boolean $muestra_menu) Return the first TipoAparato filtered by the muestra_menu column
 * @method TipoAparato findOneByLogo(string $logo) Return the first TipoAparato filtered by the logo column
 * @method TipoAparato findOneByCargoPesoLibraProducto(double $cargo_peso_libra_producto) Return the first TipoAparato filtered by the cargo_peso_libra_producto column
 * @method TipoAparato findOneByImagen(string $imagen) Return the first TipoAparato filtered by the imagen column
 * @method TipoAparato findOneByOrdenMostrar(int $orden_mostrar) Return the first TipoAparato filtered by the orden_mostrar column
 * @method TipoAparato findOneByMenuMuestraProducto(boolean $menu_muestra_producto) Return the first TipoAparato filtered by the menu_muestra_producto column
 * @method TipoAparato findOneByTipoMenu(int $tipo_menu) Return the first TipoAparato filtered by the tipo_menu column
 * @method TipoAparato findOneByMenuLateral(boolean $menu_lateral) Return the first TipoAparato filtered by the menu_lateral column
 * @method TipoAparato findOneByReceta(boolean $receta) Return the first TipoAparato filtered by the receta column
 * @method TipoAparato findOneByStatus(int $status) Return the first TipoAparato filtered by the status column
 * @method TipoAparato findOneByCuentaContable(string $cuenta_contable) Return the first TipoAparato filtered by the cuenta_contable column
 * @method TipoAparato findOneByVenta(boolean $venta) Return the first TipoAparato filtered by the venta column
 *
 * @method array findById(int $id) Return TipoAparato objects filtered by the id column
 * @method array findByEmpresaId(int $empresa_id) Return TipoAparato objects filtered by the empresa_id column
 * @method array findByCodigo(string $codigo) Return TipoAparato objects filtered by the codigo column
 * @method array findByDescripcion(string $descripcion) Return TipoAparato objects filtered by the descripcion column
 * @method array findByActivo(boolean $activo) Return TipoAparato objects filtered by the activo column
 * @method array findByMenuInvertido(boolean $menu_invertido) Return TipoAparato objects filtered by the menu_invertido column
 * @method array findByMuestraMenu(boolean $muestra_menu) Return TipoAparato objects filtered by the muestra_menu column
 * @method array findByLogo(string $logo) Return TipoAparato objects filtered by the logo column
 * @method array findByCargoPesoLibraProducto(double $cargo_peso_libra_producto) Return TipoAparato objects filtered by the cargo_peso_libra_producto column
 * @method array findByImagen(string $imagen) Return TipoAparato objects filtered by the imagen column
 * @method array findByOrdenMostrar(int $orden_mostrar) Return TipoAparato objects filtered by the orden_mostrar column
 * @method array findByMenuMuestraProducto(boolean $menu_muestra_producto) Return TipoAparato objects filtered by the menu_muestra_producto column
 * @method array findByTipoMenu(int $tipo_menu) Return TipoAparato objects filtered by the tipo_menu column
 * @method array findByMenuLateral(boolean $menu_lateral) Return TipoAparato objects filtered by the menu_lateral column
 * @method array findByReceta(boolean $receta) Return TipoAparato objects filtered by the receta column
 * @method array findByStatus(int $status) Return TipoAparato objects filtered by the status column
 * @method array findByCuentaContable(string $cuenta_contable) Return TipoAparato objects filtered by the cuenta_contable column
 * @method array findByVenta(boolean $venta) Return TipoAparato objects filtered by the venta column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseTipoAparatoQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseTipoAparatoQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = 'propel', $modelName = 'TipoAparato', $modelAlias = null)
    {
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new TipoAparatoQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param     TipoAparatoQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return TipoAparatoQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof TipoAparatoQuery) {
            return $criteria;
        }
        $query = new TipoAparatoQuery();
        if (null !== $modelAlias) {
            $query->setModelAlias($modelAlias);
        }
        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return   TipoAparato|TipoAparato[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = TipoAparatoPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is alredy in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(TipoAparatoPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return   TipoAparato A model object, or null if the key is not found
     * @throws   PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT `ID`, `EMPRESA_ID`, `CODIGO`, `DESCRIPCION`, `ACTIVO`, `MENU_INVERTIDO`, `MUESTRA_MENU`, `LOGO`, `CARGO_PESO_LIBRA_PRODUCTO`, `IMAGEN`, `ORDEN_MOSTRAR`, `MENU_MUESTRA_PRODUCTO`, `TIPO_MENU`, `MENU_LATERAL`, `RECETA`, `STATUS`, `CUENTA_CONTABLE`, `VENTA` FROM `tipo_aparato` WHERE `ID` = :p0';
        try {
            $stmt = $con->prepare($sql);
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new TipoAparato();
            $obj->hydrate($row);
            TipoAparatoPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return TipoAparato|TipoAparato[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|TipoAparato[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return TipoAparatoQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(TipoAparatoPeer::ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return TipoAparatoQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(TipoAparatoPeer::ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the id column
     *
     * Example usage:
     * <code>
     * $query->filterById(1234); // WHERE id = 1234
     * $query->filterById(array(12, 34)); // WHERE id IN (12, 34)
     * $query->filterById(array('min' => 12)); // WHERE id > 12
     * </code>
     *
     * @param     mixed $id The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TipoAparatoQuery The current query, for fluid interface
     */
    public function filterById($id = null, $comparison = null)
    {
        if (is_array($id) && null === $comparison) {
            $comparison = Criteria::IN;
        }

        return $this->addUsingAlias(TipoAparatoPeer::ID, $id, $comparison);
    }

    /**
     * Filter the query on the empresa_id column
     *
     * Example usage:
     * <code>
     * $query->filterByEmpresaId(1234); // WHERE empresa_id = 1234
     * $query->filterByEmpresaId(array(12, 34)); // WHERE empresa_id IN (12, 34)
     * $query->filterByEmpresaId(array('min' => 12)); // WHERE empresa_id > 12
     * </code>
     *
     * @see       filterByEmpresa()
     *
     * @param     mixed $empresaId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TipoAparatoQuery The current query, for fluid interface
     */
    public function filterByEmpresaId($empresaId = null, $comparison = null)
    {
        if (is_array($empresaId)) {
            $useMinMax = false;
            if (isset($empresaId['min'])) {
                $this->addUsingAlias(TipoAparatoPeer::EMPRESA_ID, $empresaId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($empresaId['max'])) {
                $this->addUsingAlias(TipoAparatoPeer::EMPRESA_ID, $empresaId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(TipoAparatoPeer::EMPRESA_ID, $empresaId, $comparison);
    }

    /**
     * Filter the query on the codigo column
     *
     * Example usage:
     * <code>
     * $query->filterByCodigo('fooValue');   // WHERE codigo = 'fooValue'
     * $query->filterByCodigo('%fooValue%'); // WHERE codigo LIKE '%fooValue%'
     * </code>
     *
     * @param     string $codigo The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TipoAparatoQuery The current query, for fluid interface
     */
    public function filterByCodigo($codigo = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($codigo)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $codigo)) {
                $codigo = str_replace('*', '%', $codigo);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(TipoAparatoPeer::CODIGO, $codigo, $comparison);
    }

    /**
     * Filter the query on the descripcion column
     *
     * Example usage:
     * <code>
     * $query->filterByDescripcion('fooValue');   // WHERE descripcion = 'fooValue'
     * $query->filterByDescripcion('%fooValue%'); // WHERE descripcion LIKE '%fooValue%'
     * </code>
     *
     * @param     string $descripcion The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TipoAparatoQuery The current query, for fluid interface
     */
    public function filterByDescripcion($descripcion = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($descripcion)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $descripcion)) {
                $descripcion = str_replace('*', '%', $descripcion);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(TipoAparatoPeer::DESCRIPCION, $descripcion, $comparison);
    }

    /**
     * Filter the query on the activo column
     *
     * Example usage:
     * <code>
     * $query->filterByActivo(true); // WHERE activo = true
     * $query->filterByActivo('yes'); // WHERE activo = true
     * </code>
     *
     * @param     boolean|string $activo The value to use as filter.
     *              Non-boolean arguments are converted using the following rules:
     *                * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *                * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     *              Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TipoAparatoQuery The current query, for fluid interface
     */
    public function filterByActivo($activo = null, $comparison = null)
    {
        if (is_string($activo)) {
            $activo = in_array(strtolower($activo), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
        }

        return $this->addUsingAlias(TipoAparatoPeer::ACTIVO, $activo, $comparison);
    }

    /**
     * Filter the query on the menu_invertido column
     *
     * Example usage:
     * <code>
     * $query->filterByMenuInvertido(true); // WHERE menu_invertido = true
     * $query->filterByMenuInvertido('yes'); // WHERE menu_invertido = true
     * </code>
     *
     * @param     boolean|string $menuInvertido The value to use as filter.
     *              Non-boolean arguments are converted using the following rules:
     *                * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *                * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     *              Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TipoAparatoQuery The current query, for fluid interface
     */
    public function filterByMenuInvertido($menuInvertido = null, $comparison = null)
    {
        if (is_string($menuInvertido)) {
            $menu_invertido = in_array(strtolower($menuInvertido), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
        }

        return $this->addUsingAlias(TipoAparatoPeer::MENU_INVERTIDO, $menuInvertido, $comparison);
    }

    /**
     * Filter the query on the muestra_menu column
     *
     * Example usage:
     * <code>
     * $query->filterByMuestraMenu(true); // WHERE muestra_menu = true
     * $query->filterByMuestraMenu('yes'); // WHERE muestra_menu = true
     * </code>
     *
     * @param     boolean|string $muestraMenu The value to use as filter.
     *              Non-boolean arguments are converted using the following rules:
     *                * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *                * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     *              Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TipoAparatoQuery The current query, for fluid interface
     */
    public function filterByMuestraMenu($muestraMenu = null, $comparison = null)
    {
        if (is_string($muestraMenu)) {
            $muestra_menu = in_array(strtolower($muestraMenu), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
        }

        return $this->addUsingAlias(TipoAparatoPeer::MUESTRA_MENU, $muestraMenu, $comparison);
    }

    /**
     * Filter the query on the logo column
     *
     * Example usage:
     * <code>
     * $query->filterByLogo('fooValue');   // WHERE logo = 'fooValue'
     * $query->filterByLogo('%fooValue%'); // WHERE logo LIKE '%fooValue%'
     * </code>
     *
     * @param     string $logo The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TipoAparatoQuery The current query, for fluid interface
     */
    public function filterByLogo($logo = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($logo)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $logo)) {
                $logo = str_replace('*', '%', $logo);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(TipoAparatoPeer::LOGO, $logo, $comparison);
    }

    /**
     * Filter the query on the cargo_peso_libra_producto column
     *
     * Example usage:
     * <code>
     * $query->filterByCargoPesoLibraProducto(1234); // WHERE cargo_peso_libra_producto = 1234
     * $query->filterByCargoPesoLibraProducto(array(12, 34)); // WHERE cargo_peso_libra_producto IN (12, 34)
     * $query->filterByCargoPesoLibraProducto(array('min' => 12)); // WHERE cargo_peso_libra_producto > 12
     * </code>
     *
     * @param     mixed $cargoPesoLibraProducto The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TipoAparatoQuery The current query, for fluid interface
     */
    public function filterByCargoPesoLibraProducto($cargoPesoLibraProducto = null, $comparison = null)
    {
        if (is_array($cargoPesoLibraProducto)) {
            $useMinMax = false;
            if (isset($cargoPesoLibraProducto['min'])) {
                $this->addUsingAlias(TipoAparatoPeer::CARGO_PESO_LIBRA_PRODUCTO, $cargoPesoLibraProducto['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($cargoPesoLibraProducto['max'])) {
                $this->addUsingAlias(TipoAparatoPeer::CARGO_PESO_LIBRA_PRODUCTO, $cargoPesoLibraProducto['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(TipoAparatoPeer::CARGO_PESO_LIBRA_PRODUCTO, $cargoPesoLibraProducto, $comparison);
    }

    /**
     * Filter the query on the imagen column
     *
     * Example usage:
     * <code>
     * $query->filterByImagen('fooValue');   // WHERE imagen = 'fooValue'
     * $query->filterByImagen('%fooValue%'); // WHERE imagen LIKE '%fooValue%'
     * </code>
     *
     * @param     string $imagen The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TipoAparatoQuery The current query, for fluid interface
     */
    public function filterByImagen($imagen = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($imagen)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $imagen)) {
                $imagen = str_replace('*', '%', $imagen);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(TipoAparatoPeer::IMAGEN, $imagen, $comparison);
    }

    /**
     * Filter the query on the orden_mostrar column
     *
     * Example usage:
     * <code>
     * $query->filterByOrdenMostrar(1234); // WHERE orden_mostrar = 1234
     * $query->filterByOrdenMostrar(array(12, 34)); // WHERE orden_mostrar IN (12, 34)
     * $query->filterByOrdenMostrar(array('min' => 12)); // WHERE orden_mostrar > 12
     * </code>
     *
     * @param     mixed $ordenMostrar The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TipoAparatoQuery The current query, for fluid interface
     */
    public function filterByOrdenMostrar($ordenMostrar = null, $comparison = null)
    {
        if (is_array($ordenMostrar)) {
            $useMinMax = false;
            if (isset($ordenMostrar['min'])) {
                $this->addUsingAlias(TipoAparatoPeer::ORDEN_MOSTRAR, $ordenMostrar['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($ordenMostrar['max'])) {
                $this->addUsingAlias(TipoAparatoPeer::ORDEN_MOSTRAR, $ordenMostrar['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(TipoAparatoPeer::ORDEN_MOSTRAR, $ordenMostrar, $comparison);
    }

    /**
     * Filter the query on the menu_muestra_producto column
     *
     * Example usage:
     * <code>
     * $query->filterByMenuMuestraProducto(true); // WHERE menu_muestra_producto = true
     * $query->filterByMenuMuestraProducto('yes'); // WHERE menu_muestra_producto = true
     * </code>
     *
     * @param     boolean|string $menuMuestraProducto The value to use as filter.
     *              Non-boolean arguments are converted using the following rules:
     *                * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *                * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     *              Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TipoAparatoQuery The current query, for fluid interface
     */
    public function filterByMenuMuestraProducto($menuMuestraProducto = null, $comparison = null)
    {
        if (is_string($menuMuestraProducto)) {
            $menu_muestra_producto = in_array(strtolower($menuMuestraProducto), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
        }

        return $this->addUsingAlias(TipoAparatoPeer::MENU_MUESTRA_PRODUCTO, $menuMuestraProducto, $comparison);
    }

    /**
     * Filter the query on the tipo_menu column
     *
     * Example usage:
     * <code>
     * $query->filterByTipoMenu(1234); // WHERE tipo_menu = 1234
     * $query->filterByTipoMenu(array(12, 34)); // WHERE tipo_menu IN (12, 34)
     * $query->filterByTipoMenu(array('min' => 12)); // WHERE tipo_menu > 12
     * </code>
     *
     * @param     mixed $tipoMenu The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TipoAparatoQuery The current query, for fluid interface
     */
    public function filterByTipoMenu($tipoMenu = null, $comparison = null)
    {
        if (is_array($tipoMenu)) {
            $useMinMax = false;
            if (isset($tipoMenu['min'])) {
                $this->addUsingAlias(TipoAparatoPeer::TIPO_MENU, $tipoMenu['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($tipoMenu['max'])) {
                $this->addUsingAlias(TipoAparatoPeer::TIPO_MENU, $tipoMenu['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(TipoAparatoPeer::TIPO_MENU, $tipoMenu, $comparison);
    }

    /**
     * Filter the query on the menu_lateral column
     *
     * Example usage:
     * <code>
     * $query->filterByMenuLateral(true); // WHERE menu_lateral = true
     * $query->filterByMenuLateral('yes'); // WHERE menu_lateral = true
     * </code>
     *
     * @param     boolean|string $menuLateral The value to use as filter.
     *              Non-boolean arguments are converted using the following rules:
     *                * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *                * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     *              Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TipoAparatoQuery The current query, for fluid interface
     */
    public function filterByMenuLateral($menuLateral = null, $comparison = null)
    {
        if (is_string($menuLateral)) {
            $menu_lateral = in_array(strtolower($menuLateral), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
        }

        return $this->addUsingAlias(TipoAparatoPeer::MENU_LATERAL, $menuLateral, $comparison);
    }

    /**
     * Filter the query on the receta column
     *
     * Example usage:
     * <code>
     * $query->filterByReceta(true); // WHERE receta = true
     * $query->filterByReceta('yes'); // WHERE receta = true
     * </code>
     *
     * @param     boolean|string $receta The value to use as filter.
     *              Non-boolean arguments are converted using the following rules:
     *                * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *                * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     *              Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TipoAparatoQuery The current query, for fluid interface
     */
    public function filterByReceta($receta = null, $comparison = null)
    {
        if (is_string($receta)) {
            $receta = in_array(strtolower($receta), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
        }

        return $this->addUsingAlias(TipoAparatoPeer::RECETA, $receta, $comparison);
    }

    /**
     * Filter the query on the status column
     *
     * Example usage:
     * <code>
     * $query->filterByStatus(1234); // WHERE status = 1234
     * $query->filterByStatus(array(12, 34)); // WHERE status IN (12, 34)
     * $query->filterByStatus(array('min' => 12)); // WHERE status > 12
     * </code>
     *
     * @param     mixed $status The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TipoAparatoQuery The current query, for fluid interface
     */
    public function filterByStatus($status = null, $comparison = null)
    {
        if (is_array($status)) {
            $useMinMax = false;
            if (isset($status['min'])) {
                $this->addUsingAlias(TipoAparatoPeer::STATUS, $status['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($status['max'])) {
                $this->addUsingAlias(TipoAparatoPeer::STATUS, $status['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(TipoAparatoPeer::STATUS, $status, $comparison);
    }

    /**
     * Filter the query on the cuenta_contable column
     *
     * Example usage:
     * <code>
     * $query->filterByCuentaContable('fooValue');   // WHERE cuenta_contable = 'fooValue'
     * $query->filterByCuentaContable('%fooValue%'); // WHERE cuenta_contable LIKE '%fooValue%'
     * </code>
     *
     * @param     string $cuentaContable The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TipoAparatoQuery The current query, for fluid interface
     */
    public function filterByCuentaContable($cuentaContable = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($cuentaContable)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $cuentaContable)) {
                $cuentaContable = str_replace('*', '%', $cuentaContable);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(TipoAparatoPeer::CUENTA_CONTABLE, $cuentaContable, $comparison);
    }

    /**
     * Filter the query on the venta column
     *
     * Example usage:
     * <code>
     * $query->filterByVenta(true); // WHERE venta = true
     * $query->filterByVenta('yes'); // WHERE venta = true
     * </code>
     *
     * @param     boolean|string $venta The value to use as filter.
     *              Non-boolean arguments are converted using the following rules:
     *                * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *                * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     *              Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return TipoAparatoQuery The current query, for fluid interface
     */
    public function filterByVenta($venta = null, $comparison = null)
    {
        if (is_string($venta)) {
            $venta = in_array(strtolower($venta), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
        }

        return $this->addUsingAlias(TipoAparatoPeer::VENTA, $venta, $comparison);
    }

    /**
     * Filter the query by a related Empresa object
     *
     * @param   Empresa|PropelObjectCollection $empresa The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   TipoAparatoQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByEmpresa($empresa, $comparison = null)
    {
        if ($empresa instanceof Empresa) {
            return $this
                ->addUsingAlias(TipoAparatoPeer::EMPRESA_ID, $empresa->getId(), $comparison);
        } elseif ($empresa instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(TipoAparatoPeer::EMPRESA_ID, $empresa->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByEmpresa() only accepts arguments of type Empresa or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Empresa relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TipoAparatoQuery The current query, for fluid interface
     */
    public function joinEmpresa($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Empresa');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Empresa');
        }

        return $this;
    }

    /**
     * Use the Empresa relation Empresa object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   EmpresaQuery A secondary query class using the current class as primary query
     */
    public function useEmpresaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinEmpresa($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Empresa', 'EmpresaQuery');
    }

    /**
     * Filter the query by a related Marca object
     *
     * @param   Marca|PropelObjectCollection $marca  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   TipoAparatoQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByMarca($marca, $comparison = null)
    {
        if ($marca instanceof Marca) {
            return $this
                ->addUsingAlias(TipoAparatoPeer::ID, $marca->getTipoAparatoId(), $comparison);
        } elseif ($marca instanceof PropelObjectCollection) {
            return $this
                ->useMarcaQuery()
                ->filterByPrimaryKeys($marca->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByMarca() only accepts arguments of type Marca or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Marca relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TipoAparatoQuery The current query, for fluid interface
     */
    public function joinMarca($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Marca');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Marca');
        }

        return $this;
    }

    /**
     * Use the Marca relation Marca object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   MarcaQuery A secondary query class using the current class as primary query
     */
    public function useMarcaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinMarca($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Marca', 'MarcaQuery');
    }

    /**
     * Filter the query by a related Producto object
     *
     * @param   Producto|PropelObjectCollection $producto  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   TipoAparatoQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByProducto($producto, $comparison = null)
    {
        if ($producto instanceof Producto) {
            return $this
                ->addUsingAlias(TipoAparatoPeer::ID, $producto->getTipoAparatoId(), $comparison);
        } elseif ($producto instanceof PropelObjectCollection) {
            return $this
                ->useProductoQuery()
                ->filterByPrimaryKeys($producto->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByProducto() only accepts arguments of type Producto or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Producto relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return TipoAparatoQuery The current query, for fluid interface
     */
    public function joinProducto($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Producto');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Producto');
        }

        return $this;
    }

    /**
     * Use the Producto relation Producto object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ProductoQuery A secondary query class using the current class as primary query
     */
    public function useProductoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinProducto($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Producto', 'ProductoQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   TipoAparato $tipoAparato Object to remove from the list of results
     *
     * @return TipoAparatoQuery The current query, for fluid interface
     */
    public function prune($tipoAparato = null)
    {
        if ($tipoAparato) {
            $this->addUsingAlias(TipoAparatoPeer::ID, $tipoAparato->getId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}
