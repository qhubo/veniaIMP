<?php


/**
 * Base class that represents a query for the 'vendedor' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.6.7 on:
 *
 * 12/30/25 15:40:26
 *
 * @method VendedorQuery orderById($order = Criteria::ASC) Order by the id column
 * @method VendedorQuery orderByCodigo($order = Criteria::ASC) Order by the codigo column
 * @method VendedorQuery orderByNombre($order = Criteria::ASC) Order by the nombre column
 * @method VendedorQuery orderByActivo($order = Criteria::ASC) Order by the activo column
 * @method VendedorQuery orderByEmpresaId($order = Criteria::ASC) Order by the empresa_id column
 * @method VendedorQuery orderByPorcentajeComision($order = Criteria::ASC) Order by the porcentaje_comision column
 * @method VendedorQuery orderByTiendaComision($order = Criteria::ASC) Order by the tienda_comision column
 * @method VendedorQuery orderByEncargadoTienda($order = Criteria::ASC) Order by the encargado_tienda column
 * @method VendedorQuery orderByCodigoPlanilla($order = Criteria::ASC) Order by the codigo_planilla column
 * @method VendedorQuery orderByObservaciones($order = Criteria::ASC) Order by the observaciones column
 *
 * @method VendedorQuery groupById() Group by the id column
 * @method VendedorQuery groupByCodigo() Group by the codigo column
 * @method VendedorQuery groupByNombre() Group by the nombre column
 * @method VendedorQuery groupByActivo() Group by the activo column
 * @method VendedorQuery groupByEmpresaId() Group by the empresa_id column
 * @method VendedorQuery groupByPorcentajeComision() Group by the porcentaje_comision column
 * @method VendedorQuery groupByTiendaComision() Group by the tienda_comision column
 * @method VendedorQuery groupByEncargadoTienda() Group by the encargado_tienda column
 * @method VendedorQuery groupByCodigoPlanilla() Group by the codigo_planilla column
 * @method VendedorQuery groupByObservaciones() Group by the observaciones column
 *
 * @method VendedorQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method VendedorQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method VendedorQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method VendedorQuery leftJoinEmpresa($relationAlias = null) Adds a LEFT JOIN clause to the query using the Empresa relation
 * @method VendedorQuery rightJoinEmpresa($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Empresa relation
 * @method VendedorQuery innerJoinEmpresa($relationAlias = null) Adds a INNER JOIN clause to the query using the Empresa relation
 *
 * @method VendedorQuery leftJoinUsuario($relationAlias = null) Adds a LEFT JOIN clause to the query using the Usuario relation
 * @method VendedorQuery rightJoinUsuario($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Usuario relation
 * @method VendedorQuery innerJoinUsuario($relationAlias = null) Adds a INNER JOIN clause to the query using the Usuario relation
 *
 * @method VendedorQuery leftJoinOperacion($relationAlias = null) Adds a LEFT JOIN clause to the query using the Operacion relation
 * @method VendedorQuery rightJoinOperacion($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Operacion relation
 * @method VendedorQuery innerJoinOperacion($relationAlias = null) Adds a INNER JOIN clause to the query using the Operacion relation
 *
 * @method VendedorQuery leftJoinOrdenCotizacion($relationAlias = null) Adds a LEFT JOIN clause to the query using the OrdenCotizacion relation
 * @method VendedorQuery rightJoinOrdenCotizacion($relationAlias = null) Adds a RIGHT JOIN clause to the query using the OrdenCotizacion relation
 * @method VendedorQuery innerJoinOrdenCotizacion($relationAlias = null) Adds a INNER JOIN clause to the query using the OrdenCotizacion relation
 *
 * @method VendedorQuery leftJoinOrdenVendedor($relationAlias = null) Adds a LEFT JOIN clause to the query using the OrdenVendedor relation
 * @method VendedorQuery rightJoinOrdenVendedor($relationAlias = null) Adds a RIGHT JOIN clause to the query using the OrdenVendedor relation
 * @method VendedorQuery innerJoinOrdenVendedor($relationAlias = null) Adds a INNER JOIN clause to the query using the OrdenVendedor relation
 *
 * @method VendedorQuery leftJoinVendedorProducto($relationAlias = null) Adds a LEFT JOIN clause to the query using the VendedorProducto relation
 * @method VendedorQuery rightJoinVendedorProducto($relationAlias = null) Adds a RIGHT JOIN clause to the query using the VendedorProducto relation
 * @method VendedorQuery innerJoinVendedorProducto($relationAlias = null) Adds a INNER JOIN clause to the query using the VendedorProducto relation
 *
 * @method Vendedor findOne(PropelPDO $con = null) Return the first Vendedor matching the query
 * @method Vendedor findOneOrCreate(PropelPDO $con = null) Return the first Vendedor matching the query, or a new Vendedor object populated from the query conditions when no match is found
 *
 * @method Vendedor findOneById(int $id) Return the first Vendedor filtered by the id column
 * @method Vendedor findOneByCodigo(string $codigo) Return the first Vendedor filtered by the codigo column
 * @method Vendedor findOneByNombre(string $nombre) Return the first Vendedor filtered by the nombre column
 * @method Vendedor findOneByActivo(boolean $activo) Return the first Vendedor filtered by the activo column
 * @method Vendedor findOneByEmpresaId(int $empresa_id) Return the first Vendedor filtered by the empresa_id column
 * @method Vendedor findOneByPorcentajeComision(double $porcentaje_comision) Return the first Vendedor filtered by the porcentaje_comision column
 * @method Vendedor findOneByTiendaComision(string $tienda_comision) Return the first Vendedor filtered by the tienda_comision column
 * @method Vendedor findOneByEncargadoTienda(boolean $encargado_tienda) Return the first Vendedor filtered by the encargado_tienda column
 * @method Vendedor findOneByCodigoPlanilla(string $codigo_planilla) Return the first Vendedor filtered by the codigo_planilla column
 * @method Vendedor findOneByObservaciones(string $observaciones) Return the first Vendedor filtered by the observaciones column
 *
 * @method array findById(int $id) Return Vendedor objects filtered by the id column
 * @method array findByCodigo(string $codigo) Return Vendedor objects filtered by the codigo column
 * @method array findByNombre(string $nombre) Return Vendedor objects filtered by the nombre column
 * @method array findByActivo(boolean $activo) Return Vendedor objects filtered by the activo column
 * @method array findByEmpresaId(int $empresa_id) Return Vendedor objects filtered by the empresa_id column
 * @method array findByPorcentajeComision(double $porcentaje_comision) Return Vendedor objects filtered by the porcentaje_comision column
 * @method array findByTiendaComision(string $tienda_comision) Return Vendedor objects filtered by the tienda_comision column
 * @method array findByEncargadoTienda(boolean $encargado_tienda) Return Vendedor objects filtered by the encargado_tienda column
 * @method array findByCodigoPlanilla(string $codigo_planilla) Return Vendedor objects filtered by the codigo_planilla column
 * @method array findByObservaciones(string $observaciones) Return Vendedor objects filtered by the observaciones column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseVendedorQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseVendedorQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = 'propel', $modelName = 'Vendedor', $modelAlias = null)
    {
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new VendedorQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param     VendedorQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return VendedorQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof VendedorQuery) {
            return $criteria;
        }
        $query = new VendedorQuery();
        if (null !== $modelAlias) {
            $query->setModelAlias($modelAlias);
        }
        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return   Vendedor|Vendedor[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = VendedorPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is alredy in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(VendedorPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return   Vendedor A model object, or null if the key is not found
     * @throws   PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT `ID`, `CODIGO`, `NOMBRE`, `ACTIVO`, `EMPRESA_ID`, `PORCENTAJE_COMISION`, `TIENDA_COMISION`, `ENCARGADO_TIENDA`, `CODIGO_PLANILLA`, `OBSERVACIONES` FROM `vendedor` WHERE `ID` = :p0';
        try {
            $stmt = $con->prepare($sql);
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new Vendedor();
            $obj->hydrate($row);
            VendedorPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return Vendedor|Vendedor[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|Vendedor[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return VendedorQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(VendedorPeer::ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return VendedorQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(VendedorPeer::ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the id column
     *
     * Example usage:
     * <code>
     * $query->filterById(1234); // WHERE id = 1234
     * $query->filterById(array(12, 34)); // WHERE id IN (12, 34)
     * $query->filterById(array('min' => 12)); // WHERE id > 12
     * </code>
     *
     * @param     mixed $id The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return VendedorQuery The current query, for fluid interface
     */
    public function filterById($id = null, $comparison = null)
    {
        if (is_array($id) && null === $comparison) {
            $comparison = Criteria::IN;
        }

        return $this->addUsingAlias(VendedorPeer::ID, $id, $comparison);
    }

    /**
     * Filter the query on the codigo column
     *
     * Example usage:
     * <code>
     * $query->filterByCodigo('fooValue');   // WHERE codigo = 'fooValue'
     * $query->filterByCodigo('%fooValue%'); // WHERE codigo LIKE '%fooValue%'
     * </code>
     *
     * @param     string $codigo The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return VendedorQuery The current query, for fluid interface
     */
    public function filterByCodigo($codigo = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($codigo)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $codigo)) {
                $codigo = str_replace('*', '%', $codigo);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(VendedorPeer::CODIGO, $codigo, $comparison);
    }

    /**
     * Filter the query on the nombre column
     *
     * Example usage:
     * <code>
     * $query->filterByNombre('fooValue');   // WHERE nombre = 'fooValue'
     * $query->filterByNombre('%fooValue%'); // WHERE nombre LIKE '%fooValue%'
     * </code>
     *
     * @param     string $nombre The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return VendedorQuery The current query, for fluid interface
     */
    public function filterByNombre($nombre = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($nombre)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $nombre)) {
                $nombre = str_replace('*', '%', $nombre);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(VendedorPeer::NOMBRE, $nombre, $comparison);
    }

    /**
     * Filter the query on the activo column
     *
     * Example usage:
     * <code>
     * $query->filterByActivo(true); // WHERE activo = true
     * $query->filterByActivo('yes'); // WHERE activo = true
     * </code>
     *
     * @param     boolean|string $activo The value to use as filter.
     *              Non-boolean arguments are converted using the following rules:
     *                * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *                * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     *              Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return VendedorQuery The current query, for fluid interface
     */
    public function filterByActivo($activo = null, $comparison = null)
    {
        if (is_string($activo)) {
            $activo = in_array(strtolower($activo), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
        }

        return $this->addUsingAlias(VendedorPeer::ACTIVO, $activo, $comparison);
    }

    /**
     * Filter the query on the empresa_id column
     *
     * Example usage:
     * <code>
     * $query->filterByEmpresaId(1234); // WHERE empresa_id = 1234
     * $query->filterByEmpresaId(array(12, 34)); // WHERE empresa_id IN (12, 34)
     * $query->filterByEmpresaId(array('min' => 12)); // WHERE empresa_id > 12
     * </code>
     *
     * @see       filterByEmpresa()
     *
     * @param     mixed $empresaId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return VendedorQuery The current query, for fluid interface
     */
    public function filterByEmpresaId($empresaId = null, $comparison = null)
    {
        if (is_array($empresaId)) {
            $useMinMax = false;
            if (isset($empresaId['min'])) {
                $this->addUsingAlias(VendedorPeer::EMPRESA_ID, $empresaId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($empresaId['max'])) {
                $this->addUsingAlias(VendedorPeer::EMPRESA_ID, $empresaId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(VendedorPeer::EMPRESA_ID, $empresaId, $comparison);
    }

    /**
     * Filter the query on the porcentaje_comision column
     *
     * Example usage:
     * <code>
     * $query->filterByPorcentajeComision(1234); // WHERE porcentaje_comision = 1234
     * $query->filterByPorcentajeComision(array(12, 34)); // WHERE porcentaje_comision IN (12, 34)
     * $query->filterByPorcentajeComision(array('min' => 12)); // WHERE porcentaje_comision > 12
     * </code>
     *
     * @param     mixed $porcentajeComision The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return VendedorQuery The current query, for fluid interface
     */
    public function filterByPorcentajeComision($porcentajeComision = null, $comparison = null)
    {
        if (is_array($porcentajeComision)) {
            $useMinMax = false;
            if (isset($porcentajeComision['min'])) {
                $this->addUsingAlias(VendedorPeer::PORCENTAJE_COMISION, $porcentajeComision['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($porcentajeComision['max'])) {
                $this->addUsingAlias(VendedorPeer::PORCENTAJE_COMISION, $porcentajeComision['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(VendedorPeer::PORCENTAJE_COMISION, $porcentajeComision, $comparison);
    }

    /**
     * Filter the query on the tienda_comision column
     *
     * Example usage:
     * <code>
     * $query->filterByTiendaComision('fooValue');   // WHERE tienda_comision = 'fooValue'
     * $query->filterByTiendaComision('%fooValue%'); // WHERE tienda_comision LIKE '%fooValue%'
     * </code>
     *
     * @param     string $tiendaComision The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return VendedorQuery The current query, for fluid interface
     */
    public function filterByTiendaComision($tiendaComision = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($tiendaComision)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $tiendaComision)) {
                $tiendaComision = str_replace('*', '%', $tiendaComision);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(VendedorPeer::TIENDA_COMISION, $tiendaComision, $comparison);
    }

    /**
     * Filter the query on the encargado_tienda column
     *
     * Example usage:
     * <code>
     * $query->filterByEncargadoTienda(true); // WHERE encargado_tienda = true
     * $query->filterByEncargadoTienda('yes'); // WHERE encargado_tienda = true
     * </code>
     *
     * @param     boolean|string $encargadoTienda The value to use as filter.
     *              Non-boolean arguments are converted using the following rules:
     *                * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *                * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     *              Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return VendedorQuery The current query, for fluid interface
     */
    public function filterByEncargadoTienda($encargadoTienda = null, $comparison = null)
    {
        if (is_string($encargadoTienda)) {
            $encargado_tienda = in_array(strtolower($encargadoTienda), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
        }

        return $this->addUsingAlias(VendedorPeer::ENCARGADO_TIENDA, $encargadoTienda, $comparison);
    }

    /**
     * Filter the query on the codigo_planilla column
     *
     * Example usage:
     * <code>
     * $query->filterByCodigoPlanilla('fooValue');   // WHERE codigo_planilla = 'fooValue'
     * $query->filterByCodigoPlanilla('%fooValue%'); // WHERE codigo_planilla LIKE '%fooValue%'
     * </code>
     *
     * @param     string $codigoPlanilla The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return VendedorQuery The current query, for fluid interface
     */
    public function filterByCodigoPlanilla($codigoPlanilla = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($codigoPlanilla)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $codigoPlanilla)) {
                $codigoPlanilla = str_replace('*', '%', $codigoPlanilla);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(VendedorPeer::CODIGO_PLANILLA, $codigoPlanilla, $comparison);
    }

    /**
     * Filter the query on the observaciones column
     *
     * Example usage:
     * <code>
     * $query->filterByObservaciones('fooValue');   // WHERE observaciones = 'fooValue'
     * $query->filterByObservaciones('%fooValue%'); // WHERE observaciones LIKE '%fooValue%'
     * </code>
     *
     * @param     string $observaciones The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return VendedorQuery The current query, for fluid interface
     */
    public function filterByObservaciones($observaciones = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($observaciones)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $observaciones)) {
                $observaciones = str_replace('*', '%', $observaciones);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(VendedorPeer::OBSERVACIONES, $observaciones, $comparison);
    }

    /**
     * Filter the query by a related Empresa object
     *
     * @param   Empresa|PropelObjectCollection $empresa The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   VendedorQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByEmpresa($empresa, $comparison = null)
    {
        if ($empresa instanceof Empresa) {
            return $this
                ->addUsingAlias(VendedorPeer::EMPRESA_ID, $empresa->getId(), $comparison);
        } elseif ($empresa instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(VendedorPeer::EMPRESA_ID, $empresa->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByEmpresa() only accepts arguments of type Empresa or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Empresa relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return VendedorQuery The current query, for fluid interface
     */
    public function joinEmpresa($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Empresa');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Empresa');
        }

        return $this;
    }

    /**
     * Use the Empresa relation Empresa object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   EmpresaQuery A secondary query class using the current class as primary query
     */
    public function useEmpresaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinEmpresa($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Empresa', 'EmpresaQuery');
    }

    /**
     * Filter the query by a related Usuario object
     *
     * @param   Usuario|PropelObjectCollection $usuario  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   VendedorQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByUsuario($usuario, $comparison = null)
    {
        if ($usuario instanceof Usuario) {
            return $this
                ->addUsingAlias(VendedorPeer::ID, $usuario->getVendedorId(), $comparison);
        } elseif ($usuario instanceof PropelObjectCollection) {
            return $this
                ->useUsuarioQuery()
                ->filterByPrimaryKeys($usuario->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByUsuario() only accepts arguments of type Usuario or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Usuario relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return VendedorQuery The current query, for fluid interface
     */
    public function joinUsuario($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Usuario');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Usuario');
        }

        return $this;
    }

    /**
     * Use the Usuario relation Usuario object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   UsuarioQuery A secondary query class using the current class as primary query
     */
    public function useUsuarioQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinUsuario($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Usuario', 'UsuarioQuery');
    }

    /**
     * Filter the query by a related Operacion object
     *
     * @param   Operacion|PropelObjectCollection $operacion  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   VendedorQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByOperacion($operacion, $comparison = null)
    {
        if ($operacion instanceof Operacion) {
            return $this
                ->addUsingAlias(VendedorPeer::ID, $operacion->getVendedorId(), $comparison);
        } elseif ($operacion instanceof PropelObjectCollection) {
            return $this
                ->useOperacionQuery()
                ->filterByPrimaryKeys($operacion->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByOperacion() only accepts arguments of type Operacion or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Operacion relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return VendedorQuery The current query, for fluid interface
     */
    public function joinOperacion($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Operacion');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Operacion');
        }

        return $this;
    }

    /**
     * Use the Operacion relation Operacion object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   OperacionQuery A secondary query class using the current class as primary query
     */
    public function useOperacionQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinOperacion($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Operacion', 'OperacionQuery');
    }

    /**
     * Filter the query by a related OrdenCotizacion object
     *
     * @param   OrdenCotizacion|PropelObjectCollection $ordenCotizacion  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   VendedorQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByOrdenCotizacion($ordenCotizacion, $comparison = null)
    {
        if ($ordenCotizacion instanceof OrdenCotizacion) {
            return $this
                ->addUsingAlias(VendedorPeer::ID, $ordenCotizacion->getVendedorId(), $comparison);
        } elseif ($ordenCotizacion instanceof PropelObjectCollection) {
            return $this
                ->useOrdenCotizacionQuery()
                ->filterByPrimaryKeys($ordenCotizacion->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByOrdenCotizacion() only accepts arguments of type OrdenCotizacion or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the OrdenCotizacion relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return VendedorQuery The current query, for fluid interface
     */
    public function joinOrdenCotizacion($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('OrdenCotizacion');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'OrdenCotizacion');
        }

        return $this;
    }

    /**
     * Use the OrdenCotizacion relation OrdenCotizacion object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   OrdenCotizacionQuery A secondary query class using the current class as primary query
     */
    public function useOrdenCotizacionQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinOrdenCotizacion($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'OrdenCotizacion', 'OrdenCotizacionQuery');
    }

    /**
     * Filter the query by a related OrdenVendedor object
     *
     * @param   OrdenVendedor|PropelObjectCollection $ordenVendedor  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   VendedorQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByOrdenVendedor($ordenVendedor, $comparison = null)
    {
        if ($ordenVendedor instanceof OrdenVendedor) {
            return $this
                ->addUsingAlias(VendedorPeer::ID, $ordenVendedor->getVendedorId(), $comparison);
        } elseif ($ordenVendedor instanceof PropelObjectCollection) {
            return $this
                ->useOrdenVendedorQuery()
                ->filterByPrimaryKeys($ordenVendedor->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByOrdenVendedor() only accepts arguments of type OrdenVendedor or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the OrdenVendedor relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return VendedorQuery The current query, for fluid interface
     */
    public function joinOrdenVendedor($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('OrdenVendedor');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'OrdenVendedor');
        }

        return $this;
    }

    /**
     * Use the OrdenVendedor relation OrdenVendedor object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   OrdenVendedorQuery A secondary query class using the current class as primary query
     */
    public function useOrdenVendedorQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinOrdenVendedor($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'OrdenVendedor', 'OrdenVendedorQuery');
    }

    /**
     * Filter the query by a related VendedorProducto object
     *
     * @param   VendedorProducto|PropelObjectCollection $vendedorProducto  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   VendedorQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByVendedorProducto($vendedorProducto, $comparison = null)
    {
        if ($vendedorProducto instanceof VendedorProducto) {
            return $this
                ->addUsingAlias(VendedorPeer::ID, $vendedorProducto->getVendedorId(), $comparison);
        } elseif ($vendedorProducto instanceof PropelObjectCollection) {
            return $this
                ->useVendedorProductoQuery()
                ->filterByPrimaryKeys($vendedorProducto->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByVendedorProducto() only accepts arguments of type VendedorProducto or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the VendedorProducto relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return VendedorQuery The current query, for fluid interface
     */
    public function joinVendedorProducto($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('VendedorProducto');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'VendedorProducto');
        }

        return $this;
    }

    /**
     * Use the VendedorProducto relation VendedorProducto object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   VendedorProductoQuery A secondary query class using the current class as primary query
     */
    public function useVendedorProductoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinVendedorProducto($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'VendedorProducto', 'VendedorProductoQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   Vendedor $vendedor Object to remove from the list of results
     *
     * @return VendedorQuery The current query, for fluid interface
     */
    public function prune($vendedor = null)
    {
        if ($vendedor) {
            $this->addUsingAlias(VendedorPeer::ID, $vendedor->getId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}
