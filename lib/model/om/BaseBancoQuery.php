<?php


/**
 * Base class that represents a query for the 'banco' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.6.7 on:
 *
 * 12/28/25 02:48:02
 *
 * @method BancoQuery orderById($order = Criteria::ASC) Order by the id column
 * @method BancoQuery orderByEmpresaId($order = Criteria::ASC) Order by the empresa_id column
 * @method BancoQuery orderByCodigo($order = Criteria::ASC) Order by the codigo column
 * @method BancoQuery orderByNombre($order = Criteria::ASC) Order by the nombre column
 * @method BancoQuery orderByCuenta($order = Criteria::ASC) Order by the cuenta column
 * @method BancoQuery orderByTipoBanco($order = Criteria::ASC) Order by the tipo_banco column
 * @method BancoQuery orderByObservaciones($order = Criteria::ASC) Order by the observaciones column
 * @method BancoQuery orderByActivo($order = Criteria::ASC) Order by the activo column
 * @method BancoQuery orderByCuentaContable($order = Criteria::ASC) Order by the cuenta_contable column
 * @method BancoQuery orderByNombreBancoId($order = Criteria::ASC) Order by the nombre_banco_id column
 * @method BancoQuery orderByPagoCheque($order = Criteria::ASC) Order by the pago_cheque column
 * @method BancoQuery orderByDolares($order = Criteria::ASC) Order by the dolares column
 *
 * @method BancoQuery groupById() Group by the id column
 * @method BancoQuery groupByEmpresaId() Group by the empresa_id column
 * @method BancoQuery groupByCodigo() Group by the codigo column
 * @method BancoQuery groupByNombre() Group by the nombre column
 * @method BancoQuery groupByCuenta() Group by the cuenta column
 * @method BancoQuery groupByTipoBanco() Group by the tipo_banco column
 * @method BancoQuery groupByObservaciones() Group by the observaciones column
 * @method BancoQuery groupByActivo() Group by the activo column
 * @method BancoQuery groupByCuentaContable() Group by the cuenta_contable column
 * @method BancoQuery groupByNombreBancoId() Group by the nombre_banco_id column
 * @method BancoQuery groupByPagoCheque() Group by the pago_cheque column
 * @method BancoQuery groupByDolares() Group by the dolares column
 *
 * @method BancoQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method BancoQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method BancoQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method BancoQuery leftJoinEmpresa($relationAlias = null) Adds a LEFT JOIN clause to the query using the Empresa relation
 * @method BancoQuery rightJoinEmpresa($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Empresa relation
 * @method BancoQuery innerJoinEmpresa($relationAlias = null) Adds a INNER JOIN clause to the query using the Empresa relation
 *
 * @method BancoQuery leftJoinNombreBanco($relationAlias = null) Adds a LEFT JOIN clause to the query using the NombreBanco relation
 * @method BancoQuery rightJoinNombreBanco($relationAlias = null) Adds a RIGHT JOIN clause to the query using the NombreBanco relation
 * @method BancoQuery innerJoinNombreBanco($relationAlias = null) Adds a INNER JOIN clause to the query using the NombreBanco relation
 *
 * @method BancoQuery leftJoinBancoTemporal($relationAlias = null) Adds a LEFT JOIN clause to the query using the BancoTemporal relation
 * @method BancoQuery rightJoinBancoTemporal($relationAlias = null) Adds a RIGHT JOIN clause to the query using the BancoTemporal relation
 * @method BancoQuery innerJoinBancoTemporal($relationAlias = null) Adds a INNER JOIN clause to the query using the BancoTemporal relation
 *
 * @method BancoQuery leftJoinOperacionPago($relationAlias = null) Adds a LEFT JOIN clause to the query using the OperacionPago relation
 * @method BancoQuery rightJoinOperacionPago($relationAlias = null) Adds a RIGHT JOIN clause to the query using the OperacionPago relation
 * @method BancoQuery innerJoinOperacionPago($relationAlias = null) Adds a INNER JOIN clause to the query using the OperacionPago relation
 *
 * @method BancoQuery leftJoinOrdenProveedorPago($relationAlias = null) Adds a LEFT JOIN clause to the query using the OrdenProveedorPago relation
 * @method BancoQuery rightJoinOrdenProveedorPago($relationAlias = null) Adds a RIGHT JOIN clause to the query using the OrdenProveedorPago relation
 * @method BancoQuery innerJoinOrdenProveedorPago($relationAlias = null) Adds a INNER JOIN clause to the query using the OrdenProveedorPago relation
 *
 * @method BancoQuery leftJoinMedioPago($relationAlias = null) Adds a LEFT JOIN clause to the query using the MedioPago relation
 * @method BancoQuery rightJoinMedioPago($relationAlias = null) Adds a RIGHT JOIN clause to the query using the MedioPago relation
 * @method BancoQuery innerJoinMedioPago($relationAlias = null) Adds a INNER JOIN clause to the query using the MedioPago relation
 *
 * @method BancoQuery leftJoinMovimientoBancoRelatedByBancoId($relationAlias = null) Adds a LEFT JOIN clause to the query using the MovimientoBancoRelatedByBancoId relation
 * @method BancoQuery rightJoinMovimientoBancoRelatedByBancoId($relationAlias = null) Adds a RIGHT JOIN clause to the query using the MovimientoBancoRelatedByBancoId relation
 * @method BancoQuery innerJoinMovimientoBancoRelatedByBancoId($relationAlias = null) Adds a INNER JOIN clause to the query using the MovimientoBancoRelatedByBancoId relation
 *
 * @method BancoQuery leftJoinMovimientoBancoRelatedByBancoOrigen($relationAlias = null) Adds a LEFT JOIN clause to the query using the MovimientoBancoRelatedByBancoOrigen relation
 * @method BancoQuery rightJoinMovimientoBancoRelatedByBancoOrigen($relationAlias = null) Adds a RIGHT JOIN clause to the query using the MovimientoBancoRelatedByBancoOrigen relation
 * @method BancoQuery innerJoinMovimientoBancoRelatedByBancoOrigen($relationAlias = null) Adds a INNER JOIN clause to the query using the MovimientoBancoRelatedByBancoOrigen relation
 *
 * @method BancoQuery leftJoinCierreCajaValor($relationAlias = null) Adds a LEFT JOIN clause to the query using the CierreCajaValor relation
 * @method BancoQuery rightJoinCierreCajaValor($relationAlias = null) Adds a RIGHT JOIN clause to the query using the CierreCajaValor relation
 * @method BancoQuery innerJoinCierreCajaValor($relationAlias = null) Adds a INNER JOIN clause to the query using the CierreCajaValor relation
 *
 * @method BancoQuery leftJoinUsuarioCorteValor($relationAlias = null) Adds a LEFT JOIN clause to the query using the UsuarioCorteValor relation
 * @method BancoQuery rightJoinUsuarioCorteValor($relationAlias = null) Adds a RIGHT JOIN clause to the query using the UsuarioCorteValor relation
 * @method BancoQuery innerJoinUsuarioCorteValor($relationAlias = null) Adds a INNER JOIN clause to the query using the UsuarioCorteValor relation
 *
 * @method BancoQuery leftJoinGastoPago($relationAlias = null) Adds a LEFT JOIN clause to the query using the GastoPago relation
 * @method BancoQuery rightJoinGastoPago($relationAlias = null) Adds a RIGHT JOIN clause to the query using the GastoPago relation
 * @method BancoQuery innerJoinGastoPago($relationAlias = null) Adds a INNER JOIN clause to the query using the GastoPago relation
 *
 * @method BancoQuery leftJoinBancoSaldo($relationAlias = null) Adds a LEFT JOIN clause to the query using the BancoSaldo relation
 * @method BancoQuery rightJoinBancoSaldo($relationAlias = null) Adds a RIGHT JOIN clause to the query using the BancoSaldo relation
 * @method BancoQuery innerJoinBancoSaldo($relationAlias = null) Adds a INNER JOIN clause to the query using the BancoSaldo relation
 *
 * @method BancoQuery leftJoinDocumentoCheque($relationAlias = null) Adds a LEFT JOIN clause to the query using the DocumentoCheque relation
 * @method BancoQuery rightJoinDocumentoCheque($relationAlias = null) Adds a RIGHT JOIN clause to the query using the DocumentoCheque relation
 * @method BancoQuery innerJoinDocumentoCheque($relationAlias = null) Adds a INNER JOIN clause to the query using the DocumentoCheque relation
 *
 * @method BancoQuery leftJoinCheque($relationAlias = null) Adds a LEFT JOIN clause to the query using the Cheque relation
 * @method BancoQuery rightJoinCheque($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Cheque relation
 * @method BancoQuery innerJoinCheque($relationAlias = null) Adds a INNER JOIN clause to the query using the Cheque relation
 *
 * @method BancoQuery leftJoinCuentaBanco($relationAlias = null) Adds a LEFT JOIN clause to the query using the CuentaBanco relation
 * @method BancoQuery rightJoinCuentaBanco($relationAlias = null) Adds a RIGHT JOIN clause to the query using the CuentaBanco relation
 * @method BancoQuery innerJoinCuentaBanco($relationAlias = null) Adds a INNER JOIN clause to the query using the CuentaBanco relation
 *
 * @method BancoQuery leftJoinVentaResumidaLinea($relationAlias = null) Adds a LEFT JOIN clause to the query using the VentaResumidaLinea relation
 * @method BancoQuery rightJoinVentaResumidaLinea($relationAlias = null) Adds a RIGHT JOIN clause to the query using the VentaResumidaLinea relation
 * @method BancoQuery innerJoinVentaResumidaLinea($relationAlias = null) Adds a INNER JOIN clause to the query using the VentaResumidaLinea relation
 *
 * @method BancoQuery leftJoinSaldoBanco($relationAlias = null) Adds a LEFT JOIN clause to the query using the SaldoBanco relation
 * @method BancoQuery rightJoinSaldoBanco($relationAlias = null) Adds a RIGHT JOIN clause to the query using the SaldoBanco relation
 * @method BancoQuery innerJoinSaldoBanco($relationAlias = null) Adds a INNER JOIN clause to the query using the SaldoBanco relation
 *
 * @method BancoQuery leftJoinSolicitudDeposito($relationAlias = null) Adds a LEFT JOIN clause to the query using the SolicitudDeposito relation
 * @method BancoQuery rightJoinSolicitudDeposito($relationAlias = null) Adds a RIGHT JOIN clause to the query using the SolicitudDeposito relation
 * @method BancoQuery innerJoinSolicitudDeposito($relationAlias = null) Adds a INNER JOIN clause to the query using the SolicitudDeposito relation
 *
 * @method BancoQuery leftJoinPrestamoDetalle($relationAlias = null) Adds a LEFT JOIN clause to the query using the PrestamoDetalle relation
 * @method BancoQuery rightJoinPrestamoDetalle($relationAlias = null) Adds a RIGHT JOIN clause to the query using the PrestamoDetalle relation
 * @method BancoQuery innerJoinPrestamoDetalle($relationAlias = null) Adds a INNER JOIN clause to the query using the PrestamoDetalle relation
 *
 * @method Banco findOne(PropelPDO $con = null) Return the first Banco matching the query
 * @method Banco findOneOrCreate(PropelPDO $con = null) Return the first Banco matching the query, or a new Banco object populated from the query conditions when no match is found
 *
 * @method Banco findOneById(int $id) Return the first Banco filtered by the id column
 * @method Banco findOneByEmpresaId(int $empresa_id) Return the first Banco filtered by the empresa_id column
 * @method Banco findOneByCodigo(string $codigo) Return the first Banco filtered by the codigo column
 * @method Banco findOneByNombre(string $nombre) Return the first Banco filtered by the nombre column
 * @method Banco findOneByCuenta(string $cuenta) Return the first Banco filtered by the cuenta column
 * @method Banco findOneByTipoBanco(string $tipo_banco) Return the first Banco filtered by the tipo_banco column
 * @method Banco findOneByObservaciones(string $observaciones) Return the first Banco filtered by the observaciones column
 * @method Banco findOneByActivo(boolean $activo) Return the first Banco filtered by the activo column
 * @method Banco findOneByCuentaContable(string $cuenta_contable) Return the first Banco filtered by the cuenta_contable column
 * @method Banco findOneByNombreBancoId(int $nombre_banco_id) Return the first Banco filtered by the nombre_banco_id column
 * @method Banco findOneByPagoCheque(boolean $pago_cheque) Return the first Banco filtered by the pago_cheque column
 * @method Banco findOneByDolares(boolean $dolares) Return the first Banco filtered by the dolares column
 *
 * @method array findById(int $id) Return Banco objects filtered by the id column
 * @method array findByEmpresaId(int $empresa_id) Return Banco objects filtered by the empresa_id column
 * @method array findByCodigo(string $codigo) Return Banco objects filtered by the codigo column
 * @method array findByNombre(string $nombre) Return Banco objects filtered by the nombre column
 * @method array findByCuenta(string $cuenta) Return Banco objects filtered by the cuenta column
 * @method array findByTipoBanco(string $tipo_banco) Return Banco objects filtered by the tipo_banco column
 * @method array findByObservaciones(string $observaciones) Return Banco objects filtered by the observaciones column
 * @method array findByActivo(boolean $activo) Return Banco objects filtered by the activo column
 * @method array findByCuentaContable(string $cuenta_contable) Return Banco objects filtered by the cuenta_contable column
 * @method array findByNombreBancoId(int $nombre_banco_id) Return Banco objects filtered by the nombre_banco_id column
 * @method array findByPagoCheque(boolean $pago_cheque) Return Banco objects filtered by the pago_cheque column
 * @method array findByDolares(boolean $dolares) Return Banco objects filtered by the dolares column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseBancoQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseBancoQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = 'propel', $modelName = 'Banco', $modelAlias = null)
    {
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new BancoQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param     BancoQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return BancoQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof BancoQuery) {
            return $criteria;
        }
        $query = new BancoQuery();
        if (null !== $modelAlias) {
            $query->setModelAlias($modelAlias);
        }
        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return   Banco|Banco[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = BancoPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is alredy in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(BancoPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return   Banco A model object, or null if the key is not found
     * @throws   PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT `ID`, `EMPRESA_ID`, `CODIGO`, `NOMBRE`, `CUENTA`, `TIPO_BANCO`, `OBSERVACIONES`, `ACTIVO`, `CUENTA_CONTABLE`, `NOMBRE_BANCO_ID`, `PAGO_CHEQUE`, `DOLARES` FROM `banco` WHERE `ID` = :p0';
        try {
            $stmt = $con->prepare($sql);
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new Banco();
            $obj->hydrate($row);
            BancoPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return Banco|Banco[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|Banco[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return BancoQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(BancoPeer::ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return BancoQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(BancoPeer::ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the id column
     *
     * Example usage:
     * <code>
     * $query->filterById(1234); // WHERE id = 1234
     * $query->filterById(array(12, 34)); // WHERE id IN (12, 34)
     * $query->filterById(array('min' => 12)); // WHERE id > 12
     * </code>
     *
     * @param     mixed $id The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return BancoQuery The current query, for fluid interface
     */
    public function filterById($id = null, $comparison = null)
    {
        if (is_array($id) && null === $comparison) {
            $comparison = Criteria::IN;
        }

        return $this->addUsingAlias(BancoPeer::ID, $id, $comparison);
    }

    /**
     * Filter the query on the empresa_id column
     *
     * Example usage:
     * <code>
     * $query->filterByEmpresaId(1234); // WHERE empresa_id = 1234
     * $query->filterByEmpresaId(array(12, 34)); // WHERE empresa_id IN (12, 34)
     * $query->filterByEmpresaId(array('min' => 12)); // WHERE empresa_id > 12
     * </code>
     *
     * @see       filterByEmpresa()
     *
     * @param     mixed $empresaId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return BancoQuery The current query, for fluid interface
     */
    public function filterByEmpresaId($empresaId = null, $comparison = null)
    {
        if (is_array($empresaId)) {
            $useMinMax = false;
            if (isset($empresaId['min'])) {
                $this->addUsingAlias(BancoPeer::EMPRESA_ID, $empresaId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($empresaId['max'])) {
                $this->addUsingAlias(BancoPeer::EMPRESA_ID, $empresaId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(BancoPeer::EMPRESA_ID, $empresaId, $comparison);
    }

    /**
     * Filter the query on the codigo column
     *
     * Example usage:
     * <code>
     * $query->filterByCodigo('fooValue');   // WHERE codigo = 'fooValue'
     * $query->filterByCodigo('%fooValue%'); // WHERE codigo LIKE '%fooValue%'
     * </code>
     *
     * @param     string $codigo The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return BancoQuery The current query, for fluid interface
     */
    public function filterByCodigo($codigo = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($codigo)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $codigo)) {
                $codigo = str_replace('*', '%', $codigo);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(BancoPeer::CODIGO, $codigo, $comparison);
    }

    /**
     * Filter the query on the nombre column
     *
     * Example usage:
     * <code>
     * $query->filterByNombre('fooValue');   // WHERE nombre = 'fooValue'
     * $query->filterByNombre('%fooValue%'); // WHERE nombre LIKE '%fooValue%'
     * </code>
     *
     * @param     string $nombre The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return BancoQuery The current query, for fluid interface
     */
    public function filterByNombre($nombre = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($nombre)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $nombre)) {
                $nombre = str_replace('*', '%', $nombre);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(BancoPeer::NOMBRE, $nombre, $comparison);
    }

    /**
     * Filter the query on the cuenta column
     *
     * Example usage:
     * <code>
     * $query->filterByCuenta('fooValue');   // WHERE cuenta = 'fooValue'
     * $query->filterByCuenta('%fooValue%'); // WHERE cuenta LIKE '%fooValue%'
     * </code>
     *
     * @param     string $cuenta The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return BancoQuery The current query, for fluid interface
     */
    public function filterByCuenta($cuenta = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($cuenta)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $cuenta)) {
                $cuenta = str_replace('*', '%', $cuenta);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(BancoPeer::CUENTA, $cuenta, $comparison);
    }

    /**
     * Filter the query on the tipo_banco column
     *
     * Example usage:
     * <code>
     * $query->filterByTipoBanco('fooValue');   // WHERE tipo_banco = 'fooValue'
     * $query->filterByTipoBanco('%fooValue%'); // WHERE tipo_banco LIKE '%fooValue%'
     * </code>
     *
     * @param     string $tipoBanco The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return BancoQuery The current query, for fluid interface
     */
    public function filterByTipoBanco($tipoBanco = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($tipoBanco)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $tipoBanco)) {
                $tipoBanco = str_replace('*', '%', $tipoBanco);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(BancoPeer::TIPO_BANCO, $tipoBanco, $comparison);
    }

    /**
     * Filter the query on the observaciones column
     *
     * Example usage:
     * <code>
     * $query->filterByObservaciones('fooValue');   // WHERE observaciones = 'fooValue'
     * $query->filterByObservaciones('%fooValue%'); // WHERE observaciones LIKE '%fooValue%'
     * </code>
     *
     * @param     string $observaciones The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return BancoQuery The current query, for fluid interface
     */
    public function filterByObservaciones($observaciones = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($observaciones)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $observaciones)) {
                $observaciones = str_replace('*', '%', $observaciones);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(BancoPeer::OBSERVACIONES, $observaciones, $comparison);
    }

    /**
     * Filter the query on the activo column
     *
     * Example usage:
     * <code>
     * $query->filterByActivo(true); // WHERE activo = true
     * $query->filterByActivo('yes'); // WHERE activo = true
     * </code>
     *
     * @param     boolean|string $activo The value to use as filter.
     *              Non-boolean arguments are converted using the following rules:
     *                * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *                * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     *              Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return BancoQuery The current query, for fluid interface
     */
    public function filterByActivo($activo = null, $comparison = null)
    {
        if (is_string($activo)) {
            $activo = in_array(strtolower($activo), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
        }

        return $this->addUsingAlias(BancoPeer::ACTIVO, $activo, $comparison);
    }

    /**
     * Filter the query on the cuenta_contable column
     *
     * Example usage:
     * <code>
     * $query->filterByCuentaContable('fooValue');   // WHERE cuenta_contable = 'fooValue'
     * $query->filterByCuentaContable('%fooValue%'); // WHERE cuenta_contable LIKE '%fooValue%'
     * </code>
     *
     * @param     string $cuentaContable The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return BancoQuery The current query, for fluid interface
     */
    public function filterByCuentaContable($cuentaContable = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($cuentaContable)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $cuentaContable)) {
                $cuentaContable = str_replace('*', '%', $cuentaContable);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(BancoPeer::CUENTA_CONTABLE, $cuentaContable, $comparison);
    }

    /**
     * Filter the query on the nombre_banco_id column
     *
     * Example usage:
     * <code>
     * $query->filterByNombreBancoId(1234); // WHERE nombre_banco_id = 1234
     * $query->filterByNombreBancoId(array(12, 34)); // WHERE nombre_banco_id IN (12, 34)
     * $query->filterByNombreBancoId(array('min' => 12)); // WHERE nombre_banco_id > 12
     * </code>
     *
     * @see       filterByNombreBanco()
     *
     * @param     mixed $nombreBancoId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return BancoQuery The current query, for fluid interface
     */
    public function filterByNombreBancoId($nombreBancoId = null, $comparison = null)
    {
        if (is_array($nombreBancoId)) {
            $useMinMax = false;
            if (isset($nombreBancoId['min'])) {
                $this->addUsingAlias(BancoPeer::NOMBRE_BANCO_ID, $nombreBancoId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($nombreBancoId['max'])) {
                $this->addUsingAlias(BancoPeer::NOMBRE_BANCO_ID, $nombreBancoId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(BancoPeer::NOMBRE_BANCO_ID, $nombreBancoId, $comparison);
    }

    /**
     * Filter the query on the pago_cheque column
     *
     * Example usage:
     * <code>
     * $query->filterByPagoCheque(true); // WHERE pago_cheque = true
     * $query->filterByPagoCheque('yes'); // WHERE pago_cheque = true
     * </code>
     *
     * @param     boolean|string $pagoCheque The value to use as filter.
     *              Non-boolean arguments are converted using the following rules:
     *                * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *                * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     *              Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return BancoQuery The current query, for fluid interface
     */
    public function filterByPagoCheque($pagoCheque = null, $comparison = null)
    {
        if (is_string($pagoCheque)) {
            $pago_cheque = in_array(strtolower($pagoCheque), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
        }

        return $this->addUsingAlias(BancoPeer::PAGO_CHEQUE, $pagoCheque, $comparison);
    }

    /**
     * Filter the query on the dolares column
     *
     * Example usage:
     * <code>
     * $query->filterByDolares(true); // WHERE dolares = true
     * $query->filterByDolares('yes'); // WHERE dolares = true
     * </code>
     *
     * @param     boolean|string $dolares The value to use as filter.
     *              Non-boolean arguments are converted using the following rules:
     *                * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *                * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     *              Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return BancoQuery The current query, for fluid interface
     */
    public function filterByDolares($dolares = null, $comparison = null)
    {
        if (is_string($dolares)) {
            $dolares = in_array(strtolower($dolares), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
        }

        return $this->addUsingAlias(BancoPeer::DOLARES, $dolares, $comparison);
    }

    /**
     * Filter the query by a related Empresa object
     *
     * @param   Empresa|PropelObjectCollection $empresa The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   BancoQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByEmpresa($empresa, $comparison = null)
    {
        if ($empresa instanceof Empresa) {
            return $this
                ->addUsingAlias(BancoPeer::EMPRESA_ID, $empresa->getId(), $comparison);
        } elseif ($empresa instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(BancoPeer::EMPRESA_ID, $empresa->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByEmpresa() only accepts arguments of type Empresa or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Empresa relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return BancoQuery The current query, for fluid interface
     */
    public function joinEmpresa($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Empresa');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Empresa');
        }

        return $this;
    }

    /**
     * Use the Empresa relation Empresa object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   EmpresaQuery A secondary query class using the current class as primary query
     */
    public function useEmpresaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinEmpresa($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Empresa', 'EmpresaQuery');
    }

    /**
     * Filter the query by a related NombreBanco object
     *
     * @param   NombreBanco|PropelObjectCollection $nombreBanco The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   BancoQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByNombreBanco($nombreBanco, $comparison = null)
    {
        if ($nombreBanco instanceof NombreBanco) {
            return $this
                ->addUsingAlias(BancoPeer::NOMBRE_BANCO_ID, $nombreBanco->getId(), $comparison);
        } elseif ($nombreBanco instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(BancoPeer::NOMBRE_BANCO_ID, $nombreBanco->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByNombreBanco() only accepts arguments of type NombreBanco or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the NombreBanco relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return BancoQuery The current query, for fluid interface
     */
    public function joinNombreBanco($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('NombreBanco');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'NombreBanco');
        }

        return $this;
    }

    /**
     * Use the NombreBanco relation NombreBanco object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   NombreBancoQuery A secondary query class using the current class as primary query
     */
    public function useNombreBancoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinNombreBanco($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'NombreBanco', 'NombreBancoQuery');
    }

    /**
     * Filter the query by a related BancoTemporal object
     *
     * @param   BancoTemporal|PropelObjectCollection $bancoTemporal  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   BancoQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByBancoTemporal($bancoTemporal, $comparison = null)
    {
        if ($bancoTemporal instanceof BancoTemporal) {
            return $this
                ->addUsingAlias(BancoPeer::ID, $bancoTemporal->getBancoId(), $comparison);
        } elseif ($bancoTemporal instanceof PropelObjectCollection) {
            return $this
                ->useBancoTemporalQuery()
                ->filterByPrimaryKeys($bancoTemporal->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByBancoTemporal() only accepts arguments of type BancoTemporal or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the BancoTemporal relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return BancoQuery The current query, for fluid interface
     */
    public function joinBancoTemporal($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('BancoTemporal');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'BancoTemporal');
        }

        return $this;
    }

    /**
     * Use the BancoTemporal relation BancoTemporal object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   BancoTemporalQuery A secondary query class using the current class as primary query
     */
    public function useBancoTemporalQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinBancoTemporal($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'BancoTemporal', 'BancoTemporalQuery');
    }

    /**
     * Filter the query by a related OperacionPago object
     *
     * @param   OperacionPago|PropelObjectCollection $operacionPago  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   BancoQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByOperacionPago($operacionPago, $comparison = null)
    {
        if ($operacionPago instanceof OperacionPago) {
            return $this
                ->addUsingAlias(BancoPeer::ID, $operacionPago->getBancoId(), $comparison);
        } elseif ($operacionPago instanceof PropelObjectCollection) {
            return $this
                ->useOperacionPagoQuery()
                ->filterByPrimaryKeys($operacionPago->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByOperacionPago() only accepts arguments of type OperacionPago or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the OperacionPago relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return BancoQuery The current query, for fluid interface
     */
    public function joinOperacionPago($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('OperacionPago');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'OperacionPago');
        }

        return $this;
    }

    /**
     * Use the OperacionPago relation OperacionPago object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   OperacionPagoQuery A secondary query class using the current class as primary query
     */
    public function useOperacionPagoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinOperacionPago($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'OperacionPago', 'OperacionPagoQuery');
    }

    /**
     * Filter the query by a related OrdenProveedorPago object
     *
     * @param   OrdenProveedorPago|PropelObjectCollection $ordenProveedorPago  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   BancoQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByOrdenProveedorPago($ordenProveedorPago, $comparison = null)
    {
        if ($ordenProveedorPago instanceof OrdenProveedorPago) {
            return $this
                ->addUsingAlias(BancoPeer::ID, $ordenProveedorPago->getBancoId(), $comparison);
        } elseif ($ordenProveedorPago instanceof PropelObjectCollection) {
            return $this
                ->useOrdenProveedorPagoQuery()
                ->filterByPrimaryKeys($ordenProveedorPago->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByOrdenProveedorPago() only accepts arguments of type OrdenProveedorPago or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the OrdenProveedorPago relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return BancoQuery The current query, for fluid interface
     */
    public function joinOrdenProveedorPago($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('OrdenProveedorPago');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'OrdenProveedorPago');
        }

        return $this;
    }

    /**
     * Use the OrdenProveedorPago relation OrdenProveedorPago object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   OrdenProveedorPagoQuery A secondary query class using the current class as primary query
     */
    public function useOrdenProveedorPagoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinOrdenProveedorPago($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'OrdenProveedorPago', 'OrdenProveedorPagoQuery');
    }

    /**
     * Filter the query by a related MedioPago object
     *
     * @param   MedioPago|PropelObjectCollection $medioPago  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   BancoQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByMedioPago($medioPago, $comparison = null)
    {
        if ($medioPago instanceof MedioPago) {
            return $this
                ->addUsingAlias(BancoPeer::ID, $medioPago->getBancoId(), $comparison);
        } elseif ($medioPago instanceof PropelObjectCollection) {
            return $this
                ->useMedioPagoQuery()
                ->filterByPrimaryKeys($medioPago->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByMedioPago() only accepts arguments of type MedioPago or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the MedioPago relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return BancoQuery The current query, for fluid interface
     */
    public function joinMedioPago($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('MedioPago');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'MedioPago');
        }

        return $this;
    }

    /**
     * Use the MedioPago relation MedioPago object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   MedioPagoQuery A secondary query class using the current class as primary query
     */
    public function useMedioPagoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinMedioPago($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'MedioPago', 'MedioPagoQuery');
    }

    /**
     * Filter the query by a related MovimientoBanco object
     *
     * @param   MovimientoBanco|PropelObjectCollection $movimientoBanco  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   BancoQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByMovimientoBancoRelatedByBancoId($movimientoBanco, $comparison = null)
    {
        if ($movimientoBanco instanceof MovimientoBanco) {
            return $this
                ->addUsingAlias(BancoPeer::ID, $movimientoBanco->getBancoId(), $comparison);
        } elseif ($movimientoBanco instanceof PropelObjectCollection) {
            return $this
                ->useMovimientoBancoRelatedByBancoIdQuery()
                ->filterByPrimaryKeys($movimientoBanco->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByMovimientoBancoRelatedByBancoId() only accepts arguments of type MovimientoBanco or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the MovimientoBancoRelatedByBancoId relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return BancoQuery The current query, for fluid interface
     */
    public function joinMovimientoBancoRelatedByBancoId($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('MovimientoBancoRelatedByBancoId');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'MovimientoBancoRelatedByBancoId');
        }

        return $this;
    }

    /**
     * Use the MovimientoBancoRelatedByBancoId relation MovimientoBanco object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   MovimientoBancoQuery A secondary query class using the current class as primary query
     */
    public function useMovimientoBancoRelatedByBancoIdQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinMovimientoBancoRelatedByBancoId($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'MovimientoBancoRelatedByBancoId', 'MovimientoBancoQuery');
    }

    /**
     * Filter the query by a related MovimientoBanco object
     *
     * @param   MovimientoBanco|PropelObjectCollection $movimientoBanco  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   BancoQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByMovimientoBancoRelatedByBancoOrigen($movimientoBanco, $comparison = null)
    {
        if ($movimientoBanco instanceof MovimientoBanco) {
            return $this
                ->addUsingAlias(BancoPeer::ID, $movimientoBanco->getBancoOrigen(), $comparison);
        } elseif ($movimientoBanco instanceof PropelObjectCollection) {
            return $this
                ->useMovimientoBancoRelatedByBancoOrigenQuery()
                ->filterByPrimaryKeys($movimientoBanco->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByMovimientoBancoRelatedByBancoOrigen() only accepts arguments of type MovimientoBanco or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the MovimientoBancoRelatedByBancoOrigen relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return BancoQuery The current query, for fluid interface
     */
    public function joinMovimientoBancoRelatedByBancoOrigen($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('MovimientoBancoRelatedByBancoOrigen');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'MovimientoBancoRelatedByBancoOrigen');
        }

        return $this;
    }

    /**
     * Use the MovimientoBancoRelatedByBancoOrigen relation MovimientoBanco object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   MovimientoBancoQuery A secondary query class using the current class as primary query
     */
    public function useMovimientoBancoRelatedByBancoOrigenQuery($relationAlias = null, $joinType = Criteria::INNER_JOIN)
    {
        return $this
            ->joinMovimientoBancoRelatedByBancoOrigen($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'MovimientoBancoRelatedByBancoOrigen', 'MovimientoBancoQuery');
    }

    /**
     * Filter the query by a related CierreCajaValor object
     *
     * @param   CierreCajaValor|PropelObjectCollection $cierreCajaValor  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   BancoQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByCierreCajaValor($cierreCajaValor, $comparison = null)
    {
        if ($cierreCajaValor instanceof CierreCajaValor) {
            return $this
                ->addUsingAlias(BancoPeer::ID, $cierreCajaValor->getBancoId(), $comparison);
        } elseif ($cierreCajaValor instanceof PropelObjectCollection) {
            return $this
                ->useCierreCajaValorQuery()
                ->filterByPrimaryKeys($cierreCajaValor->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByCierreCajaValor() only accepts arguments of type CierreCajaValor or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the CierreCajaValor relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return BancoQuery The current query, for fluid interface
     */
    public function joinCierreCajaValor($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('CierreCajaValor');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'CierreCajaValor');
        }

        return $this;
    }

    /**
     * Use the CierreCajaValor relation CierreCajaValor object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   CierreCajaValorQuery A secondary query class using the current class as primary query
     */
    public function useCierreCajaValorQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinCierreCajaValor($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'CierreCajaValor', 'CierreCajaValorQuery');
    }

    /**
     * Filter the query by a related UsuarioCorteValor object
     *
     * @param   UsuarioCorteValor|PropelObjectCollection $usuarioCorteValor  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   BancoQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByUsuarioCorteValor($usuarioCorteValor, $comparison = null)
    {
        if ($usuarioCorteValor instanceof UsuarioCorteValor) {
            return $this
                ->addUsingAlias(BancoPeer::ID, $usuarioCorteValor->getBancoId(), $comparison);
        } elseif ($usuarioCorteValor instanceof PropelObjectCollection) {
            return $this
                ->useUsuarioCorteValorQuery()
                ->filterByPrimaryKeys($usuarioCorteValor->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByUsuarioCorteValor() only accepts arguments of type UsuarioCorteValor or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the UsuarioCorteValor relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return BancoQuery The current query, for fluid interface
     */
    public function joinUsuarioCorteValor($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('UsuarioCorteValor');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'UsuarioCorteValor');
        }

        return $this;
    }

    /**
     * Use the UsuarioCorteValor relation UsuarioCorteValor object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   UsuarioCorteValorQuery A secondary query class using the current class as primary query
     */
    public function useUsuarioCorteValorQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinUsuarioCorteValor($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'UsuarioCorteValor', 'UsuarioCorteValorQuery');
    }

    /**
     * Filter the query by a related GastoPago object
     *
     * @param   GastoPago|PropelObjectCollection $gastoPago  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   BancoQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByGastoPago($gastoPago, $comparison = null)
    {
        if ($gastoPago instanceof GastoPago) {
            return $this
                ->addUsingAlias(BancoPeer::ID, $gastoPago->getBancoId(), $comparison);
        } elseif ($gastoPago instanceof PropelObjectCollection) {
            return $this
                ->useGastoPagoQuery()
                ->filterByPrimaryKeys($gastoPago->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByGastoPago() only accepts arguments of type GastoPago or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the GastoPago relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return BancoQuery The current query, for fluid interface
     */
    public function joinGastoPago($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('GastoPago');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'GastoPago');
        }

        return $this;
    }

    /**
     * Use the GastoPago relation GastoPago object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   GastoPagoQuery A secondary query class using the current class as primary query
     */
    public function useGastoPagoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinGastoPago($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'GastoPago', 'GastoPagoQuery');
    }

    /**
     * Filter the query by a related BancoSaldo object
     *
     * @param   BancoSaldo|PropelObjectCollection $bancoSaldo  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   BancoQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByBancoSaldo($bancoSaldo, $comparison = null)
    {
        if ($bancoSaldo instanceof BancoSaldo) {
            return $this
                ->addUsingAlias(BancoPeer::ID, $bancoSaldo->getBancoId(), $comparison);
        } elseif ($bancoSaldo instanceof PropelObjectCollection) {
            return $this
                ->useBancoSaldoQuery()
                ->filterByPrimaryKeys($bancoSaldo->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByBancoSaldo() only accepts arguments of type BancoSaldo or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the BancoSaldo relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return BancoQuery The current query, for fluid interface
     */
    public function joinBancoSaldo($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('BancoSaldo');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'BancoSaldo');
        }

        return $this;
    }

    /**
     * Use the BancoSaldo relation BancoSaldo object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   BancoSaldoQuery A secondary query class using the current class as primary query
     */
    public function useBancoSaldoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinBancoSaldo($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'BancoSaldo', 'BancoSaldoQuery');
    }

    /**
     * Filter the query by a related DocumentoCheque object
     *
     * @param   DocumentoCheque|PropelObjectCollection $documentoCheque  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   BancoQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByDocumentoCheque($documentoCheque, $comparison = null)
    {
        if ($documentoCheque instanceof DocumentoCheque) {
            return $this
                ->addUsingAlias(BancoPeer::ID, $documentoCheque->getBancoId(), $comparison);
        } elseif ($documentoCheque instanceof PropelObjectCollection) {
            return $this
                ->useDocumentoChequeQuery()
                ->filterByPrimaryKeys($documentoCheque->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByDocumentoCheque() only accepts arguments of type DocumentoCheque or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the DocumentoCheque relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return BancoQuery The current query, for fluid interface
     */
    public function joinDocumentoCheque($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('DocumentoCheque');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'DocumentoCheque');
        }

        return $this;
    }

    /**
     * Use the DocumentoCheque relation DocumentoCheque object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   DocumentoChequeQuery A secondary query class using the current class as primary query
     */
    public function useDocumentoChequeQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinDocumentoCheque($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'DocumentoCheque', 'DocumentoChequeQuery');
    }

    /**
     * Filter the query by a related Cheque object
     *
     * @param   Cheque|PropelObjectCollection $cheque  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   BancoQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByCheque($cheque, $comparison = null)
    {
        if ($cheque instanceof Cheque) {
            return $this
                ->addUsingAlias(BancoPeer::ID, $cheque->getBancoId(), $comparison);
        } elseif ($cheque instanceof PropelObjectCollection) {
            return $this
                ->useChequeQuery()
                ->filterByPrimaryKeys($cheque->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByCheque() only accepts arguments of type Cheque or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Cheque relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return BancoQuery The current query, for fluid interface
     */
    public function joinCheque($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Cheque');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Cheque');
        }

        return $this;
    }

    /**
     * Use the Cheque relation Cheque object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ChequeQuery A secondary query class using the current class as primary query
     */
    public function useChequeQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinCheque($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Cheque', 'ChequeQuery');
    }

    /**
     * Filter the query by a related CuentaBanco object
     *
     * @param   CuentaBanco|PropelObjectCollection $cuentaBanco  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   BancoQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByCuentaBanco($cuentaBanco, $comparison = null)
    {
        if ($cuentaBanco instanceof CuentaBanco) {
            return $this
                ->addUsingAlias(BancoPeer::ID, $cuentaBanco->getBancoId(), $comparison);
        } elseif ($cuentaBanco instanceof PropelObjectCollection) {
            return $this
                ->useCuentaBancoQuery()
                ->filterByPrimaryKeys($cuentaBanco->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByCuentaBanco() only accepts arguments of type CuentaBanco or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the CuentaBanco relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return BancoQuery The current query, for fluid interface
     */
    public function joinCuentaBanco($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('CuentaBanco');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'CuentaBanco');
        }

        return $this;
    }

    /**
     * Use the CuentaBanco relation CuentaBanco object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   CuentaBancoQuery A secondary query class using the current class as primary query
     */
    public function useCuentaBancoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinCuentaBanco($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'CuentaBanco', 'CuentaBancoQuery');
    }

    /**
     * Filter the query by a related VentaResumidaLinea object
     *
     * @param   VentaResumidaLinea|PropelObjectCollection $ventaResumidaLinea  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   BancoQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByVentaResumidaLinea($ventaResumidaLinea, $comparison = null)
    {
        if ($ventaResumidaLinea instanceof VentaResumidaLinea) {
            return $this
                ->addUsingAlias(BancoPeer::ID, $ventaResumidaLinea->getBancoId(), $comparison);
        } elseif ($ventaResumidaLinea instanceof PropelObjectCollection) {
            return $this
                ->useVentaResumidaLineaQuery()
                ->filterByPrimaryKeys($ventaResumidaLinea->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByVentaResumidaLinea() only accepts arguments of type VentaResumidaLinea or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the VentaResumidaLinea relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return BancoQuery The current query, for fluid interface
     */
    public function joinVentaResumidaLinea($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('VentaResumidaLinea');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'VentaResumidaLinea');
        }

        return $this;
    }

    /**
     * Use the VentaResumidaLinea relation VentaResumidaLinea object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   VentaResumidaLineaQuery A secondary query class using the current class as primary query
     */
    public function useVentaResumidaLineaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinVentaResumidaLinea($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'VentaResumidaLinea', 'VentaResumidaLineaQuery');
    }

    /**
     * Filter the query by a related SaldoBanco object
     *
     * @param   SaldoBanco|PropelObjectCollection $saldoBanco  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   BancoQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterBySaldoBanco($saldoBanco, $comparison = null)
    {
        if ($saldoBanco instanceof SaldoBanco) {
            return $this
                ->addUsingAlias(BancoPeer::ID, $saldoBanco->getBancoId(), $comparison);
        } elseif ($saldoBanco instanceof PropelObjectCollection) {
            return $this
                ->useSaldoBancoQuery()
                ->filterByPrimaryKeys($saldoBanco->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterBySaldoBanco() only accepts arguments of type SaldoBanco or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the SaldoBanco relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return BancoQuery The current query, for fluid interface
     */
    public function joinSaldoBanco($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('SaldoBanco');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'SaldoBanco');
        }

        return $this;
    }

    /**
     * Use the SaldoBanco relation SaldoBanco object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   SaldoBancoQuery A secondary query class using the current class as primary query
     */
    public function useSaldoBancoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinSaldoBanco($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'SaldoBanco', 'SaldoBancoQuery');
    }

    /**
     * Filter the query by a related SolicitudDeposito object
     *
     * @param   SolicitudDeposito|PropelObjectCollection $solicitudDeposito  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   BancoQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterBySolicitudDeposito($solicitudDeposito, $comparison = null)
    {
        if ($solicitudDeposito instanceof SolicitudDeposito) {
            return $this
                ->addUsingAlias(BancoPeer::ID, $solicitudDeposito->getBancoId(), $comparison);
        } elseif ($solicitudDeposito instanceof PropelObjectCollection) {
            return $this
                ->useSolicitudDepositoQuery()
                ->filterByPrimaryKeys($solicitudDeposito->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterBySolicitudDeposito() only accepts arguments of type SolicitudDeposito or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the SolicitudDeposito relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return BancoQuery The current query, for fluid interface
     */
    public function joinSolicitudDeposito($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('SolicitudDeposito');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'SolicitudDeposito');
        }

        return $this;
    }

    /**
     * Use the SolicitudDeposito relation SolicitudDeposito object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   SolicitudDepositoQuery A secondary query class using the current class as primary query
     */
    public function useSolicitudDepositoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinSolicitudDeposito($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'SolicitudDeposito', 'SolicitudDepositoQuery');
    }

    /**
     * Filter the query by a related PrestamoDetalle object
     *
     * @param   PrestamoDetalle|PropelObjectCollection $prestamoDetalle  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   BancoQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByPrestamoDetalle($prestamoDetalle, $comparison = null)
    {
        if ($prestamoDetalle instanceof PrestamoDetalle) {
            return $this
                ->addUsingAlias(BancoPeer::ID, $prestamoDetalle->getBancoId(), $comparison);
        } elseif ($prestamoDetalle instanceof PropelObjectCollection) {
            return $this
                ->usePrestamoDetalleQuery()
                ->filterByPrimaryKeys($prestamoDetalle->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByPrestamoDetalle() only accepts arguments of type PrestamoDetalle or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the PrestamoDetalle relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return BancoQuery The current query, for fluid interface
     */
    public function joinPrestamoDetalle($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('PrestamoDetalle');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'PrestamoDetalle');
        }

        return $this;
    }

    /**
     * Use the PrestamoDetalle relation PrestamoDetalle object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   PrestamoDetalleQuery A secondary query class using the current class as primary query
     */
    public function usePrestamoDetalleQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinPrestamoDetalle($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'PrestamoDetalle', 'PrestamoDetalleQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   Banco $banco Object to remove from the list of results
     *
     * @return BancoQuery The current query, for fluid interface
     */
    public function prune($banco = null)
    {
        if ($banco) {
            $this->addUsingAlias(BancoPeer::ID, $banco->getId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}
