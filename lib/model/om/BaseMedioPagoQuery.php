<?php


/**
 * Base class that represents a query for the 'medio_pago' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.6.7 on:
 *
 * 12/30/25 15:40:22
 *
 * @method MedioPagoQuery orderById($order = Criteria::ASC) Order by the id column
 * @method MedioPagoQuery orderByEmpresaId($order = Criteria::ASC) Order by the empresa_id column
 * @method MedioPagoQuery orderByCodigo($order = Criteria::ASC) Order by the codigo column
 * @method MedioPagoQuery orderByNombre($order = Criteria::ASC) Order by the nombre column
 * @method MedioPagoQuery orderByActivo($order = Criteria::ASC) Order by the activo column
 * @method MedioPagoQuery orderByCuentaContable($order = Criteria::ASC) Order by the cuenta_contable column
 * @method MedioPagoQuery orderByOrden($order = Criteria::ASC) Order by the orden column
 * @method MedioPagoQuery orderByPos($order = Criteria::ASC) Order by the pos column
 * @method MedioPagoQuery orderByAplicaMovBanco($order = Criteria::ASC) Order by the aplica_mov_banco column
 * @method MedioPagoQuery orderByCreatedBy($order = Criteria::ASC) Order by the created_by column
 * @method MedioPagoQuery orderByUpdatedBy($order = Criteria::ASC) Order by the updated_by column
 * @method MedioPagoQuery orderByCreatedAt($order = Criteria::ASC) Order by the created_at column
 * @method MedioPagoQuery orderByUpdatedAt($order = Criteria::ASC) Order by the updated_at column
 * @method MedioPagoQuery orderByAplicaCuotas($order = Criteria::ASC) Order by the aplica_cuotas column
 * @method MedioPagoQuery orderByNumeroCuotas($order = Criteria::ASC) Order by the numero_cuotas column
 * @method MedioPagoQuery orderByComision($order = Criteria::ASC) Order by the comision column
 * @method MedioPagoQuery orderByRetieneIsr($order = Criteria::ASC) Order by the retiene_isr column
 * @method MedioPagoQuery orderByPideBanco($order = Criteria::ASC) Order by the pide_banco column
 * @method MedioPagoQuery orderByBancoId($order = Criteria::ASC) Order by the banco_id column
 * @method MedioPagoQuery orderByPagoProveedor($order = Criteria::ASC) Order by the pago_proveedor column
 *
 * @method MedioPagoQuery groupById() Group by the id column
 * @method MedioPagoQuery groupByEmpresaId() Group by the empresa_id column
 * @method MedioPagoQuery groupByCodigo() Group by the codigo column
 * @method MedioPagoQuery groupByNombre() Group by the nombre column
 * @method MedioPagoQuery groupByActivo() Group by the activo column
 * @method MedioPagoQuery groupByCuentaContable() Group by the cuenta_contable column
 * @method MedioPagoQuery groupByOrden() Group by the orden column
 * @method MedioPagoQuery groupByPos() Group by the pos column
 * @method MedioPagoQuery groupByAplicaMovBanco() Group by the aplica_mov_banco column
 * @method MedioPagoQuery groupByCreatedBy() Group by the created_by column
 * @method MedioPagoQuery groupByUpdatedBy() Group by the updated_by column
 * @method MedioPagoQuery groupByCreatedAt() Group by the created_at column
 * @method MedioPagoQuery groupByUpdatedAt() Group by the updated_at column
 * @method MedioPagoQuery groupByAplicaCuotas() Group by the aplica_cuotas column
 * @method MedioPagoQuery groupByNumeroCuotas() Group by the numero_cuotas column
 * @method MedioPagoQuery groupByComision() Group by the comision column
 * @method MedioPagoQuery groupByRetieneIsr() Group by the retiene_isr column
 * @method MedioPagoQuery groupByPideBanco() Group by the pide_banco column
 * @method MedioPagoQuery groupByBancoId() Group by the banco_id column
 * @method MedioPagoQuery groupByPagoProveedor() Group by the pago_proveedor column
 *
 * @method MedioPagoQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method MedioPagoQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method MedioPagoQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method MedioPagoQuery leftJoinEmpresa($relationAlias = null) Adds a LEFT JOIN clause to the query using the Empresa relation
 * @method MedioPagoQuery rightJoinEmpresa($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Empresa relation
 * @method MedioPagoQuery innerJoinEmpresa($relationAlias = null) Adds a INNER JOIN clause to the query using the Empresa relation
 *
 * @method MedioPagoQuery leftJoinBanco($relationAlias = null) Adds a LEFT JOIN clause to the query using the Banco relation
 * @method MedioPagoQuery rightJoinBanco($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Banco relation
 * @method MedioPagoQuery innerJoinBanco($relationAlias = null) Adds a INNER JOIN clause to the query using the Banco relation
 *
 * @method MedioPagoQuery leftJoinMovimientoBanco($relationAlias = null) Adds a LEFT JOIN clause to the query using the MovimientoBanco relation
 * @method MedioPagoQuery rightJoinMovimientoBanco($relationAlias = null) Adds a RIGHT JOIN clause to the query using the MovimientoBanco relation
 * @method MedioPagoQuery innerJoinMovimientoBanco($relationAlias = null) Adds a INNER JOIN clause to the query using the MovimientoBanco relation
 *
 * @method MedioPagoQuery leftJoinPartida($relationAlias = null) Adds a LEFT JOIN clause to the query using the Partida relation
 * @method MedioPagoQuery rightJoinPartida($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Partida relation
 * @method MedioPagoQuery innerJoinPartida($relationAlias = null) Adds a INNER JOIN clause to the query using the Partida relation
 *
 * @method MedioPagoQuery leftJoinNotaCredito($relationAlias = null) Adds a LEFT JOIN clause to the query using the NotaCredito relation
 * @method MedioPagoQuery rightJoinNotaCredito($relationAlias = null) Adds a RIGHT JOIN clause to the query using the NotaCredito relation
 * @method MedioPagoQuery innerJoinNotaCredito($relationAlias = null) Adds a INNER JOIN clause to the query using the NotaCredito relation
 *
 * @method MedioPagoQuery leftJoinVentaResumida($relationAlias = null) Adds a LEFT JOIN clause to the query using the VentaResumida relation
 * @method MedioPagoQuery rightJoinVentaResumida($relationAlias = null) Adds a RIGHT JOIN clause to the query using the VentaResumida relation
 * @method MedioPagoQuery innerJoinVentaResumida($relationAlias = null) Adds a INNER JOIN clause to the query using the VentaResumida relation
 *
 * @method MedioPagoQuery leftJoinVentaResumidaLinea($relationAlias = null) Adds a LEFT JOIN clause to the query using the VentaResumidaLinea relation
 * @method MedioPagoQuery rightJoinVentaResumidaLinea($relationAlias = null) Adds a RIGHT JOIN clause to the query using the VentaResumidaLinea relation
 * @method MedioPagoQuery innerJoinVentaResumidaLinea($relationAlias = null) Adds a INNER JOIN clause to the query using the VentaResumidaLinea relation
 *
 * @method MedioPagoQuery leftJoinEgresosRecibido($relationAlias = null) Adds a LEFT JOIN clause to the query using the EgresosRecibido relation
 * @method MedioPagoQuery rightJoinEgresosRecibido($relationAlias = null) Adds a RIGHT JOIN clause to the query using the EgresosRecibido relation
 * @method MedioPagoQuery innerJoinEgresosRecibido($relationAlias = null) Adds a INNER JOIN clause to the query using the EgresosRecibido relation
 *
 * @method MedioPago findOne(PropelPDO $con = null) Return the first MedioPago matching the query
 * @method MedioPago findOneOrCreate(PropelPDO $con = null) Return the first MedioPago matching the query, or a new MedioPago object populated from the query conditions when no match is found
 *
 * @method MedioPago findOneById(int $id) Return the first MedioPago filtered by the id column
 * @method MedioPago findOneByEmpresaId(int $empresa_id) Return the first MedioPago filtered by the empresa_id column
 * @method MedioPago findOneByCodigo(string $codigo) Return the first MedioPago filtered by the codigo column
 * @method MedioPago findOneByNombre(string $nombre) Return the first MedioPago filtered by the nombre column
 * @method MedioPago findOneByActivo(boolean $activo) Return the first MedioPago filtered by the activo column
 * @method MedioPago findOneByCuentaContable(string $cuenta_contable) Return the first MedioPago filtered by the cuenta_contable column
 * @method MedioPago findOneByOrden(int $orden) Return the first MedioPago filtered by the orden column
 * @method MedioPago findOneByPos(boolean $pos) Return the first MedioPago filtered by the pos column
 * @method MedioPago findOneByAplicaMovBanco(boolean $aplica_mov_banco) Return the first MedioPago filtered by the aplica_mov_banco column
 * @method MedioPago findOneByCreatedBy(string $created_by) Return the first MedioPago filtered by the created_by column
 * @method MedioPago findOneByUpdatedBy(string $updated_by) Return the first MedioPago filtered by the updated_by column
 * @method MedioPago findOneByCreatedAt(string $created_at) Return the first MedioPago filtered by the created_at column
 * @method MedioPago findOneByUpdatedAt(string $updated_at) Return the first MedioPago filtered by the updated_at column
 * @method MedioPago findOneByAplicaCuotas(boolean $aplica_cuotas) Return the first MedioPago filtered by the aplica_cuotas column
 * @method MedioPago findOneByNumeroCuotas(int $numero_cuotas) Return the first MedioPago filtered by the numero_cuotas column
 * @method MedioPago findOneByComision(double $comision) Return the first MedioPago filtered by the comision column
 * @method MedioPago findOneByRetieneIsr(boolean $retiene_isr) Return the first MedioPago filtered by the retiene_isr column
 * @method MedioPago findOneByPideBanco(boolean $pide_banco) Return the first MedioPago filtered by the pide_banco column
 * @method MedioPago findOneByBancoId(int $banco_id) Return the first MedioPago filtered by the banco_id column
 * @method MedioPago findOneByPagoProveedor(boolean $pago_proveedor) Return the first MedioPago filtered by the pago_proveedor column
 *
 * @method array findById(int $id) Return MedioPago objects filtered by the id column
 * @method array findByEmpresaId(int $empresa_id) Return MedioPago objects filtered by the empresa_id column
 * @method array findByCodigo(string $codigo) Return MedioPago objects filtered by the codigo column
 * @method array findByNombre(string $nombre) Return MedioPago objects filtered by the nombre column
 * @method array findByActivo(boolean $activo) Return MedioPago objects filtered by the activo column
 * @method array findByCuentaContable(string $cuenta_contable) Return MedioPago objects filtered by the cuenta_contable column
 * @method array findByOrden(int $orden) Return MedioPago objects filtered by the orden column
 * @method array findByPos(boolean $pos) Return MedioPago objects filtered by the pos column
 * @method array findByAplicaMovBanco(boolean $aplica_mov_banco) Return MedioPago objects filtered by the aplica_mov_banco column
 * @method array findByCreatedBy(string $created_by) Return MedioPago objects filtered by the created_by column
 * @method array findByUpdatedBy(string $updated_by) Return MedioPago objects filtered by the updated_by column
 * @method array findByCreatedAt(string $created_at) Return MedioPago objects filtered by the created_at column
 * @method array findByUpdatedAt(string $updated_at) Return MedioPago objects filtered by the updated_at column
 * @method array findByAplicaCuotas(boolean $aplica_cuotas) Return MedioPago objects filtered by the aplica_cuotas column
 * @method array findByNumeroCuotas(int $numero_cuotas) Return MedioPago objects filtered by the numero_cuotas column
 * @method array findByComision(double $comision) Return MedioPago objects filtered by the comision column
 * @method array findByRetieneIsr(boolean $retiene_isr) Return MedioPago objects filtered by the retiene_isr column
 * @method array findByPideBanco(boolean $pide_banco) Return MedioPago objects filtered by the pide_banco column
 * @method array findByBancoId(int $banco_id) Return MedioPago objects filtered by the banco_id column
 * @method array findByPagoProveedor(boolean $pago_proveedor) Return MedioPago objects filtered by the pago_proveedor column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseMedioPagoQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseMedioPagoQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = 'propel', $modelName = 'MedioPago', $modelAlias = null)
    {
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new MedioPagoQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param     MedioPagoQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return MedioPagoQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof MedioPagoQuery) {
            return $criteria;
        }
        $query = new MedioPagoQuery();
        if (null !== $modelAlias) {
            $query->setModelAlias($modelAlias);
        }
        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return   MedioPago|MedioPago[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = MedioPagoPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is alredy in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(MedioPagoPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return   MedioPago A model object, or null if the key is not found
     * @throws   PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT `ID`, `EMPRESA_ID`, `CODIGO`, `NOMBRE`, `ACTIVO`, `CUENTA_CONTABLE`, `ORDEN`, `POS`, `APLICA_MOV_BANCO`, `CREATED_BY`, `UPDATED_BY`, `CREATED_AT`, `UPDATED_AT`, `APLICA_CUOTAS`, `NUMERO_CUOTAS`, `COMISION`, `RETIENE_ISR`, `PIDE_BANCO`, `BANCO_ID`, `PAGO_PROVEEDOR` FROM `medio_pago` WHERE `ID` = :p0';
        try {
            $stmt = $con->prepare($sql);
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new MedioPago();
            $obj->hydrate($row);
            MedioPagoPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return MedioPago|MedioPago[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|MedioPago[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return MedioPagoQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(MedioPagoPeer::ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return MedioPagoQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(MedioPagoPeer::ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the id column
     *
     * Example usage:
     * <code>
     * $query->filterById(1234); // WHERE id = 1234
     * $query->filterById(array(12, 34)); // WHERE id IN (12, 34)
     * $query->filterById(array('min' => 12)); // WHERE id > 12
     * </code>
     *
     * @param     mixed $id The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return MedioPagoQuery The current query, for fluid interface
     */
    public function filterById($id = null, $comparison = null)
    {
        if (is_array($id) && null === $comparison) {
            $comparison = Criteria::IN;
        }

        return $this->addUsingAlias(MedioPagoPeer::ID, $id, $comparison);
    }

    /**
     * Filter the query on the empresa_id column
     *
     * Example usage:
     * <code>
     * $query->filterByEmpresaId(1234); // WHERE empresa_id = 1234
     * $query->filterByEmpresaId(array(12, 34)); // WHERE empresa_id IN (12, 34)
     * $query->filterByEmpresaId(array('min' => 12)); // WHERE empresa_id > 12
     * </code>
     *
     * @see       filterByEmpresa()
     *
     * @param     mixed $empresaId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return MedioPagoQuery The current query, for fluid interface
     */
    public function filterByEmpresaId($empresaId = null, $comparison = null)
    {
        if (is_array($empresaId)) {
            $useMinMax = false;
            if (isset($empresaId['min'])) {
                $this->addUsingAlias(MedioPagoPeer::EMPRESA_ID, $empresaId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($empresaId['max'])) {
                $this->addUsingAlias(MedioPagoPeer::EMPRESA_ID, $empresaId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(MedioPagoPeer::EMPRESA_ID, $empresaId, $comparison);
    }

    /**
     * Filter the query on the codigo column
     *
     * Example usage:
     * <code>
     * $query->filterByCodigo('fooValue');   // WHERE codigo = 'fooValue'
     * $query->filterByCodigo('%fooValue%'); // WHERE codigo LIKE '%fooValue%'
     * </code>
     *
     * @param     string $codigo The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return MedioPagoQuery The current query, for fluid interface
     */
    public function filterByCodigo($codigo = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($codigo)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $codigo)) {
                $codigo = str_replace('*', '%', $codigo);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(MedioPagoPeer::CODIGO, $codigo, $comparison);
    }

    /**
     * Filter the query on the nombre column
     *
     * Example usage:
     * <code>
     * $query->filterByNombre('fooValue');   // WHERE nombre = 'fooValue'
     * $query->filterByNombre('%fooValue%'); // WHERE nombre LIKE '%fooValue%'
     * </code>
     *
     * @param     string $nombre The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return MedioPagoQuery The current query, for fluid interface
     */
    public function filterByNombre($nombre = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($nombre)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $nombre)) {
                $nombre = str_replace('*', '%', $nombre);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(MedioPagoPeer::NOMBRE, $nombre, $comparison);
    }

    /**
     * Filter the query on the activo column
     *
     * Example usage:
     * <code>
     * $query->filterByActivo(true); // WHERE activo = true
     * $query->filterByActivo('yes'); // WHERE activo = true
     * </code>
     *
     * @param     boolean|string $activo The value to use as filter.
     *              Non-boolean arguments are converted using the following rules:
     *                * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *                * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     *              Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return MedioPagoQuery The current query, for fluid interface
     */
    public function filterByActivo($activo = null, $comparison = null)
    {
        if (is_string($activo)) {
            $activo = in_array(strtolower($activo), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
        }

        return $this->addUsingAlias(MedioPagoPeer::ACTIVO, $activo, $comparison);
    }

    /**
     * Filter the query on the cuenta_contable column
     *
     * Example usage:
     * <code>
     * $query->filterByCuentaContable('fooValue');   // WHERE cuenta_contable = 'fooValue'
     * $query->filterByCuentaContable('%fooValue%'); // WHERE cuenta_contable LIKE '%fooValue%'
     * </code>
     *
     * @param     string $cuentaContable The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return MedioPagoQuery The current query, for fluid interface
     */
    public function filterByCuentaContable($cuentaContable = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($cuentaContable)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $cuentaContable)) {
                $cuentaContable = str_replace('*', '%', $cuentaContable);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(MedioPagoPeer::CUENTA_CONTABLE, $cuentaContable, $comparison);
    }

    /**
     * Filter the query on the orden column
     *
     * Example usage:
     * <code>
     * $query->filterByOrden(1234); // WHERE orden = 1234
     * $query->filterByOrden(array(12, 34)); // WHERE orden IN (12, 34)
     * $query->filterByOrden(array('min' => 12)); // WHERE orden > 12
     * </code>
     *
     * @param     mixed $orden The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return MedioPagoQuery The current query, for fluid interface
     */
    public function filterByOrden($orden = null, $comparison = null)
    {
        if (is_array($orden)) {
            $useMinMax = false;
            if (isset($orden['min'])) {
                $this->addUsingAlias(MedioPagoPeer::ORDEN, $orden['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($orden['max'])) {
                $this->addUsingAlias(MedioPagoPeer::ORDEN, $orden['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(MedioPagoPeer::ORDEN, $orden, $comparison);
    }

    /**
     * Filter the query on the pos column
     *
     * Example usage:
     * <code>
     * $query->filterByPos(true); // WHERE pos = true
     * $query->filterByPos('yes'); // WHERE pos = true
     * </code>
     *
     * @param     boolean|string $pos The value to use as filter.
     *              Non-boolean arguments are converted using the following rules:
     *                * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *                * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     *              Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return MedioPagoQuery The current query, for fluid interface
     */
    public function filterByPos($pos = null, $comparison = null)
    {
        if (is_string($pos)) {
            $pos = in_array(strtolower($pos), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
        }

        return $this->addUsingAlias(MedioPagoPeer::POS, $pos, $comparison);
    }

    /**
     * Filter the query on the aplica_mov_banco column
     *
     * Example usage:
     * <code>
     * $query->filterByAplicaMovBanco(true); // WHERE aplica_mov_banco = true
     * $query->filterByAplicaMovBanco('yes'); // WHERE aplica_mov_banco = true
     * </code>
     *
     * @param     boolean|string $aplicaMovBanco The value to use as filter.
     *              Non-boolean arguments are converted using the following rules:
     *                * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *                * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     *              Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return MedioPagoQuery The current query, for fluid interface
     */
    public function filterByAplicaMovBanco($aplicaMovBanco = null, $comparison = null)
    {
        if (is_string($aplicaMovBanco)) {
            $aplica_mov_banco = in_array(strtolower($aplicaMovBanco), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
        }

        return $this->addUsingAlias(MedioPagoPeer::APLICA_MOV_BANCO, $aplicaMovBanco, $comparison);
    }

    /**
     * Filter the query on the created_by column
     *
     * Example usage:
     * <code>
     * $query->filterByCreatedBy('fooValue');   // WHERE created_by = 'fooValue'
     * $query->filterByCreatedBy('%fooValue%'); // WHERE created_by LIKE '%fooValue%'
     * </code>
     *
     * @param     string $createdBy The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return MedioPagoQuery The current query, for fluid interface
     */
    public function filterByCreatedBy($createdBy = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($createdBy)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $createdBy)) {
                $createdBy = str_replace('*', '%', $createdBy);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(MedioPagoPeer::CREATED_BY, $createdBy, $comparison);
    }

    /**
     * Filter the query on the updated_by column
     *
     * Example usage:
     * <code>
     * $query->filterByUpdatedBy('fooValue');   // WHERE updated_by = 'fooValue'
     * $query->filterByUpdatedBy('%fooValue%'); // WHERE updated_by LIKE '%fooValue%'
     * </code>
     *
     * @param     string $updatedBy The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return MedioPagoQuery The current query, for fluid interface
     */
    public function filterByUpdatedBy($updatedBy = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($updatedBy)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $updatedBy)) {
                $updatedBy = str_replace('*', '%', $updatedBy);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(MedioPagoPeer::UPDATED_BY, $updatedBy, $comparison);
    }

    /**
     * Filter the query on the created_at column
     *
     * Example usage:
     * <code>
     * $query->filterByCreatedAt('2011-03-14'); // WHERE created_at = '2011-03-14'
     * $query->filterByCreatedAt('now'); // WHERE created_at = '2011-03-14'
     * $query->filterByCreatedAt(array('max' => 'yesterday')); // WHERE created_at > '2011-03-13'
     * </code>
     *
     * @param     mixed $createdAt The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return MedioPagoQuery The current query, for fluid interface
     */
    public function filterByCreatedAt($createdAt = null, $comparison = null)
    {
        if (is_array($createdAt)) {
            $useMinMax = false;
            if (isset($createdAt['min'])) {
                $this->addUsingAlias(MedioPagoPeer::CREATED_AT, $createdAt['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($createdAt['max'])) {
                $this->addUsingAlias(MedioPagoPeer::CREATED_AT, $createdAt['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(MedioPagoPeer::CREATED_AT, $createdAt, $comparison);
    }

    /**
     * Filter the query on the updated_at column
     *
     * Example usage:
     * <code>
     * $query->filterByUpdatedAt('2011-03-14'); // WHERE updated_at = '2011-03-14'
     * $query->filterByUpdatedAt('now'); // WHERE updated_at = '2011-03-14'
     * $query->filterByUpdatedAt(array('max' => 'yesterday')); // WHERE updated_at > '2011-03-13'
     * </code>
     *
     * @param     mixed $updatedAt The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return MedioPagoQuery The current query, for fluid interface
     */
    public function filterByUpdatedAt($updatedAt = null, $comparison = null)
    {
        if (is_array($updatedAt)) {
            $useMinMax = false;
            if (isset($updatedAt['min'])) {
                $this->addUsingAlias(MedioPagoPeer::UPDATED_AT, $updatedAt['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($updatedAt['max'])) {
                $this->addUsingAlias(MedioPagoPeer::UPDATED_AT, $updatedAt['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(MedioPagoPeer::UPDATED_AT, $updatedAt, $comparison);
    }

    /**
     * Filter the query on the aplica_cuotas column
     *
     * Example usage:
     * <code>
     * $query->filterByAplicaCuotas(true); // WHERE aplica_cuotas = true
     * $query->filterByAplicaCuotas('yes'); // WHERE aplica_cuotas = true
     * </code>
     *
     * @param     boolean|string $aplicaCuotas The value to use as filter.
     *              Non-boolean arguments are converted using the following rules:
     *                * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *                * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     *              Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return MedioPagoQuery The current query, for fluid interface
     */
    public function filterByAplicaCuotas($aplicaCuotas = null, $comparison = null)
    {
        if (is_string($aplicaCuotas)) {
            $aplica_cuotas = in_array(strtolower($aplicaCuotas), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
        }

        return $this->addUsingAlias(MedioPagoPeer::APLICA_CUOTAS, $aplicaCuotas, $comparison);
    }

    /**
     * Filter the query on the numero_cuotas column
     *
     * Example usage:
     * <code>
     * $query->filterByNumeroCuotas(1234); // WHERE numero_cuotas = 1234
     * $query->filterByNumeroCuotas(array(12, 34)); // WHERE numero_cuotas IN (12, 34)
     * $query->filterByNumeroCuotas(array('min' => 12)); // WHERE numero_cuotas > 12
     * </code>
     *
     * @param     mixed $numeroCuotas The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return MedioPagoQuery The current query, for fluid interface
     */
    public function filterByNumeroCuotas($numeroCuotas = null, $comparison = null)
    {
        if (is_array($numeroCuotas)) {
            $useMinMax = false;
            if (isset($numeroCuotas['min'])) {
                $this->addUsingAlias(MedioPagoPeer::NUMERO_CUOTAS, $numeroCuotas['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($numeroCuotas['max'])) {
                $this->addUsingAlias(MedioPagoPeer::NUMERO_CUOTAS, $numeroCuotas['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(MedioPagoPeer::NUMERO_CUOTAS, $numeroCuotas, $comparison);
    }

    /**
     * Filter the query on the comision column
     *
     * Example usage:
     * <code>
     * $query->filterByComision(1234); // WHERE comision = 1234
     * $query->filterByComision(array(12, 34)); // WHERE comision IN (12, 34)
     * $query->filterByComision(array('min' => 12)); // WHERE comision > 12
     * </code>
     *
     * @param     mixed $comision The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return MedioPagoQuery The current query, for fluid interface
     */
    public function filterByComision($comision = null, $comparison = null)
    {
        if (is_array($comision)) {
            $useMinMax = false;
            if (isset($comision['min'])) {
                $this->addUsingAlias(MedioPagoPeer::COMISION, $comision['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($comision['max'])) {
                $this->addUsingAlias(MedioPagoPeer::COMISION, $comision['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(MedioPagoPeer::COMISION, $comision, $comparison);
    }

    /**
     * Filter the query on the retiene_isr column
     *
     * Example usage:
     * <code>
     * $query->filterByRetieneIsr(true); // WHERE retiene_isr = true
     * $query->filterByRetieneIsr('yes'); // WHERE retiene_isr = true
     * </code>
     *
     * @param     boolean|string $retieneIsr The value to use as filter.
     *              Non-boolean arguments are converted using the following rules:
     *                * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *                * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     *              Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return MedioPagoQuery The current query, for fluid interface
     */
    public function filterByRetieneIsr($retieneIsr = null, $comparison = null)
    {
        if (is_string($retieneIsr)) {
            $retiene_isr = in_array(strtolower($retieneIsr), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
        }

        return $this->addUsingAlias(MedioPagoPeer::RETIENE_ISR, $retieneIsr, $comparison);
    }

    /**
     * Filter the query on the pide_banco column
     *
     * Example usage:
     * <code>
     * $query->filterByPideBanco(true); // WHERE pide_banco = true
     * $query->filterByPideBanco('yes'); // WHERE pide_banco = true
     * </code>
     *
     * @param     boolean|string $pideBanco The value to use as filter.
     *              Non-boolean arguments are converted using the following rules:
     *                * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *                * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     *              Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return MedioPagoQuery The current query, for fluid interface
     */
    public function filterByPideBanco($pideBanco = null, $comparison = null)
    {
        if (is_string($pideBanco)) {
            $pide_banco = in_array(strtolower($pideBanco), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
        }

        return $this->addUsingAlias(MedioPagoPeer::PIDE_BANCO, $pideBanco, $comparison);
    }

    /**
     * Filter the query on the banco_id column
     *
     * Example usage:
     * <code>
     * $query->filterByBancoId(1234); // WHERE banco_id = 1234
     * $query->filterByBancoId(array(12, 34)); // WHERE banco_id IN (12, 34)
     * $query->filterByBancoId(array('min' => 12)); // WHERE banco_id > 12
     * </code>
     *
     * @see       filterByBanco()
     *
     * @param     mixed $bancoId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return MedioPagoQuery The current query, for fluid interface
     */
    public function filterByBancoId($bancoId = null, $comparison = null)
    {
        if (is_array($bancoId)) {
            $useMinMax = false;
            if (isset($bancoId['min'])) {
                $this->addUsingAlias(MedioPagoPeer::BANCO_ID, $bancoId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($bancoId['max'])) {
                $this->addUsingAlias(MedioPagoPeer::BANCO_ID, $bancoId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(MedioPagoPeer::BANCO_ID, $bancoId, $comparison);
    }

    /**
     * Filter the query on the pago_proveedor column
     *
     * Example usage:
     * <code>
     * $query->filterByPagoProveedor(true); // WHERE pago_proveedor = true
     * $query->filterByPagoProveedor('yes'); // WHERE pago_proveedor = true
     * </code>
     *
     * @param     boolean|string $pagoProveedor The value to use as filter.
     *              Non-boolean arguments are converted using the following rules:
     *                * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *                * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     *              Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return MedioPagoQuery The current query, for fluid interface
     */
    public function filterByPagoProveedor($pagoProveedor = null, $comparison = null)
    {
        if (is_string($pagoProveedor)) {
            $pago_proveedor = in_array(strtolower($pagoProveedor), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
        }

        return $this->addUsingAlias(MedioPagoPeer::PAGO_PROVEEDOR, $pagoProveedor, $comparison);
    }

    /**
     * Filter the query by a related Empresa object
     *
     * @param   Empresa|PropelObjectCollection $empresa The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   MedioPagoQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByEmpresa($empresa, $comparison = null)
    {
        if ($empresa instanceof Empresa) {
            return $this
                ->addUsingAlias(MedioPagoPeer::EMPRESA_ID, $empresa->getId(), $comparison);
        } elseif ($empresa instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(MedioPagoPeer::EMPRESA_ID, $empresa->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByEmpresa() only accepts arguments of type Empresa or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Empresa relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return MedioPagoQuery The current query, for fluid interface
     */
    public function joinEmpresa($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Empresa');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Empresa');
        }

        return $this;
    }

    /**
     * Use the Empresa relation Empresa object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   EmpresaQuery A secondary query class using the current class as primary query
     */
    public function useEmpresaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinEmpresa($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Empresa', 'EmpresaQuery');
    }

    /**
     * Filter the query by a related Banco object
     *
     * @param   Banco|PropelObjectCollection $banco The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   MedioPagoQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByBanco($banco, $comparison = null)
    {
        if ($banco instanceof Banco) {
            return $this
                ->addUsingAlias(MedioPagoPeer::BANCO_ID, $banco->getId(), $comparison);
        } elseif ($banco instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(MedioPagoPeer::BANCO_ID, $banco->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByBanco() only accepts arguments of type Banco or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Banco relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return MedioPagoQuery The current query, for fluid interface
     */
    public function joinBanco($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Banco');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Banco');
        }

        return $this;
    }

    /**
     * Use the Banco relation Banco object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   BancoQuery A secondary query class using the current class as primary query
     */
    public function useBancoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinBanco($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Banco', 'BancoQuery');
    }

    /**
     * Filter the query by a related MovimientoBanco object
     *
     * @param   MovimientoBanco|PropelObjectCollection $movimientoBanco  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   MedioPagoQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByMovimientoBanco($movimientoBanco, $comparison = null)
    {
        if ($movimientoBanco instanceof MovimientoBanco) {
            return $this
                ->addUsingAlias(MedioPagoPeer::ID, $movimientoBanco->getMedioPagoId(), $comparison);
        } elseif ($movimientoBanco instanceof PropelObjectCollection) {
            return $this
                ->useMovimientoBancoQuery()
                ->filterByPrimaryKeys($movimientoBanco->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByMovimientoBanco() only accepts arguments of type MovimientoBanco or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the MovimientoBanco relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return MedioPagoQuery The current query, for fluid interface
     */
    public function joinMovimientoBanco($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('MovimientoBanco');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'MovimientoBanco');
        }

        return $this;
    }

    /**
     * Use the MovimientoBanco relation MovimientoBanco object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   MovimientoBancoQuery A secondary query class using the current class as primary query
     */
    public function useMovimientoBancoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinMovimientoBanco($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'MovimientoBanco', 'MovimientoBancoQuery');
    }

    /**
     * Filter the query by a related Partida object
     *
     * @param   Partida|PropelObjectCollection $partida  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   MedioPagoQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByPartida($partida, $comparison = null)
    {
        if ($partida instanceof Partida) {
            return $this
                ->addUsingAlias(MedioPagoPeer::ID, $partida->getMedioPagoId(), $comparison);
        } elseif ($partida instanceof PropelObjectCollection) {
            return $this
                ->usePartidaQuery()
                ->filterByPrimaryKeys($partida->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByPartida() only accepts arguments of type Partida or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Partida relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return MedioPagoQuery The current query, for fluid interface
     */
    public function joinPartida($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Partida');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Partida');
        }

        return $this;
    }

    /**
     * Use the Partida relation Partida object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   PartidaQuery A secondary query class using the current class as primary query
     */
    public function usePartidaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinPartida($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Partida', 'PartidaQuery');
    }

    /**
     * Filter the query by a related NotaCredito object
     *
     * @param   NotaCredito|PropelObjectCollection $notaCredito  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   MedioPagoQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByNotaCredito($notaCredito, $comparison = null)
    {
        if ($notaCredito instanceof NotaCredito) {
            return $this
                ->addUsingAlias(MedioPagoPeer::ID, $notaCredito->getMedioPagoId(), $comparison);
        } elseif ($notaCredito instanceof PropelObjectCollection) {
            return $this
                ->useNotaCreditoQuery()
                ->filterByPrimaryKeys($notaCredito->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByNotaCredito() only accepts arguments of type NotaCredito or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the NotaCredito relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return MedioPagoQuery The current query, for fluid interface
     */
    public function joinNotaCredito($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('NotaCredito');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'NotaCredito');
        }

        return $this;
    }

    /**
     * Use the NotaCredito relation NotaCredito object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   NotaCreditoQuery A secondary query class using the current class as primary query
     */
    public function useNotaCreditoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinNotaCredito($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'NotaCredito', 'NotaCreditoQuery');
    }

    /**
     * Filter the query by a related VentaResumida object
     *
     * @param   VentaResumida|PropelObjectCollection $ventaResumida  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   MedioPagoQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByVentaResumida($ventaResumida, $comparison = null)
    {
        if ($ventaResumida instanceof VentaResumida) {
            return $this
                ->addUsingAlias(MedioPagoPeer::ID, $ventaResumida->getMedioPagoId(), $comparison);
        } elseif ($ventaResumida instanceof PropelObjectCollection) {
            return $this
                ->useVentaResumidaQuery()
                ->filterByPrimaryKeys($ventaResumida->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByVentaResumida() only accepts arguments of type VentaResumida or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the VentaResumida relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return MedioPagoQuery The current query, for fluid interface
     */
    public function joinVentaResumida($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('VentaResumida');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'VentaResumida');
        }

        return $this;
    }

    /**
     * Use the VentaResumida relation VentaResumida object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   VentaResumidaQuery A secondary query class using the current class as primary query
     */
    public function useVentaResumidaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinVentaResumida($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'VentaResumida', 'VentaResumidaQuery');
    }

    /**
     * Filter the query by a related VentaResumidaLinea object
     *
     * @param   VentaResumidaLinea|PropelObjectCollection $ventaResumidaLinea  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   MedioPagoQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByVentaResumidaLinea($ventaResumidaLinea, $comparison = null)
    {
        if ($ventaResumidaLinea instanceof VentaResumidaLinea) {
            return $this
                ->addUsingAlias(MedioPagoPeer::ID, $ventaResumidaLinea->getMedioPagoId(), $comparison);
        } elseif ($ventaResumidaLinea instanceof PropelObjectCollection) {
            return $this
                ->useVentaResumidaLineaQuery()
                ->filterByPrimaryKeys($ventaResumidaLinea->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByVentaResumidaLinea() only accepts arguments of type VentaResumidaLinea or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the VentaResumidaLinea relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return MedioPagoQuery The current query, for fluid interface
     */
    public function joinVentaResumidaLinea($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('VentaResumidaLinea');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'VentaResumidaLinea');
        }

        return $this;
    }

    /**
     * Use the VentaResumidaLinea relation VentaResumidaLinea object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   VentaResumidaLineaQuery A secondary query class using the current class as primary query
     */
    public function useVentaResumidaLineaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinVentaResumidaLinea($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'VentaResumidaLinea', 'VentaResumidaLineaQuery');
    }

    /**
     * Filter the query by a related EgresosRecibido object
     *
     * @param   EgresosRecibido|PropelObjectCollection $egresosRecibido  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   MedioPagoQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByEgresosRecibido($egresosRecibido, $comparison = null)
    {
        if ($egresosRecibido instanceof EgresosRecibido) {
            return $this
                ->addUsingAlias(MedioPagoPeer::ID, $egresosRecibido->getMedioPagoId(), $comparison);
        } elseif ($egresosRecibido instanceof PropelObjectCollection) {
            return $this
                ->useEgresosRecibidoQuery()
                ->filterByPrimaryKeys($egresosRecibido->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByEgresosRecibido() only accepts arguments of type EgresosRecibido or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the EgresosRecibido relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return MedioPagoQuery The current query, for fluid interface
     */
    public function joinEgresosRecibido($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('EgresosRecibido');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'EgresosRecibido');
        }

        return $this;
    }

    /**
     * Use the EgresosRecibido relation EgresosRecibido object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   EgresosRecibidoQuery A secondary query class using the current class as primary query
     */
    public function useEgresosRecibidoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinEgresosRecibido($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'EgresosRecibido', 'EgresosRecibidoQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   MedioPago $medioPago Object to remove from the list of results
     *
     * @return MedioPagoQuery The current query, for fluid interface
     */
    public function prune($medioPago = null)
    {
        if ($medioPago) {
            $this->addUsingAlias(MedioPagoPeer::ID, $medioPago->getId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}
