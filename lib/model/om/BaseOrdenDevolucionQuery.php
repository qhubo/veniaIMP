<?php


/**
 * Base class that represents a query for the 'orden_devolucion' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.6.7 on:
 *
 * 12/28/25 02:48:09
 *
 * @method OrdenDevolucionQuery orderById($order = Criteria::ASC) Order by the id column
 * @method OrdenDevolucionQuery orderByFecha($order = Criteria::ASC) Order by the fecha column
 * @method OrdenDevolucionQuery orderByNombre($order = Criteria::ASC) Order by the nombre column
 * @method OrdenDevolucionQuery orderByReferenciaFactura($order = Criteria::ASC) Order by the referencia_factura column
 * @method OrdenDevolucionQuery orderByConcepto($order = Criteria::ASC) Order by the concepto column
 * @method OrdenDevolucionQuery orderByArchivo($order = Criteria::ASC) Order by the archivo column
 * @method OrdenDevolucionQuery orderByValor($order = Criteria::ASC) Order by the valor column
 * @method OrdenDevolucionQuery orderByRetencion($order = Criteria::ASC) Order by the retencion column
 * @method OrdenDevolucionQuery orderByPartidaNo($order = Criteria::ASC) Order by the partida_no column
 * @method OrdenDevolucionQuery orderByEstatus($order = Criteria::ASC) Order by the estatus column
 * @method OrdenDevolucionQuery orderByChequeNo($order = Criteria::ASC) Order by the cheque_no column
 * @method OrdenDevolucionQuery orderByUsuarioCreo($order = Criteria::ASC) Order by the usuario_creo column
 * @method OrdenDevolucionQuery orderByCreatedAt($order = Criteria::ASC) Order by the created_at column
 * @method OrdenDevolucionQuery orderByUsuarioConfirmo($order = Criteria::ASC) Order by the usuario_confirmo column
 * @method OrdenDevolucionQuery orderByUpdatedAt($order = Criteria::ASC) Order by the updated_at column
 * @method OrdenDevolucionQuery orderByMotivoAutorizo($order = Criteria::ASC) Order by the motivo_autorizo column
 * @method OrdenDevolucionQuery orderByValorOtros($order = Criteria::ASC) Order by the valor_otros column
 * @method OrdenDevolucionQuery orderByEmpresaId($order = Criteria::ASC) Order by the empresa_id column
 * @method OrdenDevolucionQuery orderByToken($order = Criteria::ASC) Order by the token column
 * @method OrdenDevolucionQuery orderByCodigo($order = Criteria::ASC) Order by the codigo column
 * @method OrdenDevolucionQuery orderByFechaConfirmo($order = Criteria::ASC) Order by the fecha_confirmo column
 * @method OrdenDevolucionQuery orderByPorcentajeRetenie($order = Criteria::ASC) Order by the porcentaje_retenie column
 * @method OrdenDevolucionQuery orderByTipo($order = Criteria::ASC) Order by the tipo column
 * @method OrdenDevolucionQuery orderByProveedorId($order = Criteria::ASC) Order by the proveedor_id column
 * @method OrdenDevolucionQuery orderByPagoMedio($order = Criteria::ASC) Order by the pago_medio column
 * @method OrdenDevolucionQuery orderByVendedor($order = Criteria::ASC) Order by the vendedor column
 * @method OrdenDevolucionQuery orderByReferenciaNota($order = Criteria::ASC) Order by the referencia_nota column
 * @method OrdenDevolucionQuery orderByNoHollander($order = Criteria::ASC) Order by the no_hollander column
 * @method OrdenDevolucionQuery orderByNoStock($order = Criteria::ASC) Order by the no_stock column
 * @method OrdenDevolucionQuery orderByDescripcion($order = Criteria::ASC) Order by the descripcion column
 * @method OrdenDevolucionQuery orderBySolicitudDevolucionId($order = Criteria::ASC) Order by the solicitud_devolucion_id column
 * @method OrdenDevolucionQuery orderByArchivo2($order = Criteria::ASC) Order by the archivo_2 column
 * @method OrdenDevolucionQuery orderByTiendaId($order = Criteria::ASC) Order by the tienda_id column
 * @method OrdenDevolucionQuery orderByFechaFactura($order = Criteria::ASC) Order by the fecha_factura column
 * @method OrdenDevolucionQuery orderByMotivo($order = Criteria::ASC) Order by the motivo column
 * @method OrdenDevolucionQuery orderByDetalleMotivo($order = Criteria::ASC) Order by the detalle_motivo column
 * @method OrdenDevolucionQuery orderByDetalleRepuesto($order = Criteria::ASC) Order by the detalle_repuesto column
 * @method OrdenDevolucionQuery orderByProductoId($order = Criteria::ASC) Order by the producto_id column
 * @method OrdenDevolucionQuery orderByCantidad($order = Criteria::ASC) Order by the cantidad column
 *
 * @method OrdenDevolucionQuery groupById() Group by the id column
 * @method OrdenDevolucionQuery groupByFecha() Group by the fecha column
 * @method OrdenDevolucionQuery groupByNombre() Group by the nombre column
 * @method OrdenDevolucionQuery groupByReferenciaFactura() Group by the referencia_factura column
 * @method OrdenDevolucionQuery groupByConcepto() Group by the concepto column
 * @method OrdenDevolucionQuery groupByArchivo() Group by the archivo column
 * @method OrdenDevolucionQuery groupByValor() Group by the valor column
 * @method OrdenDevolucionQuery groupByRetencion() Group by the retencion column
 * @method OrdenDevolucionQuery groupByPartidaNo() Group by the partida_no column
 * @method OrdenDevolucionQuery groupByEstatus() Group by the estatus column
 * @method OrdenDevolucionQuery groupByChequeNo() Group by the cheque_no column
 * @method OrdenDevolucionQuery groupByUsuarioCreo() Group by the usuario_creo column
 * @method OrdenDevolucionQuery groupByCreatedAt() Group by the created_at column
 * @method OrdenDevolucionQuery groupByUsuarioConfirmo() Group by the usuario_confirmo column
 * @method OrdenDevolucionQuery groupByUpdatedAt() Group by the updated_at column
 * @method OrdenDevolucionQuery groupByMotivoAutorizo() Group by the motivo_autorizo column
 * @method OrdenDevolucionQuery groupByValorOtros() Group by the valor_otros column
 * @method OrdenDevolucionQuery groupByEmpresaId() Group by the empresa_id column
 * @method OrdenDevolucionQuery groupByToken() Group by the token column
 * @method OrdenDevolucionQuery groupByCodigo() Group by the codigo column
 * @method OrdenDevolucionQuery groupByFechaConfirmo() Group by the fecha_confirmo column
 * @method OrdenDevolucionQuery groupByPorcentajeRetenie() Group by the porcentaje_retenie column
 * @method OrdenDevolucionQuery groupByTipo() Group by the tipo column
 * @method OrdenDevolucionQuery groupByProveedorId() Group by the proveedor_id column
 * @method OrdenDevolucionQuery groupByPagoMedio() Group by the pago_medio column
 * @method OrdenDevolucionQuery groupByVendedor() Group by the vendedor column
 * @method OrdenDevolucionQuery groupByReferenciaNota() Group by the referencia_nota column
 * @method OrdenDevolucionQuery groupByNoHollander() Group by the no_hollander column
 * @method OrdenDevolucionQuery groupByNoStock() Group by the no_stock column
 * @method OrdenDevolucionQuery groupByDescripcion() Group by the descripcion column
 * @method OrdenDevolucionQuery groupBySolicitudDevolucionId() Group by the solicitud_devolucion_id column
 * @method OrdenDevolucionQuery groupByArchivo2() Group by the archivo_2 column
 * @method OrdenDevolucionQuery groupByTiendaId() Group by the tienda_id column
 * @method OrdenDevolucionQuery groupByFechaFactura() Group by the fecha_factura column
 * @method OrdenDevolucionQuery groupByMotivo() Group by the motivo column
 * @method OrdenDevolucionQuery groupByDetalleMotivo() Group by the detalle_motivo column
 * @method OrdenDevolucionQuery groupByDetalleRepuesto() Group by the detalle_repuesto column
 * @method OrdenDevolucionQuery groupByProductoId() Group by the producto_id column
 * @method OrdenDevolucionQuery groupByCantidad() Group by the cantidad column
 *
 * @method OrdenDevolucionQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method OrdenDevolucionQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method OrdenDevolucionQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method OrdenDevolucionQuery leftJoinEmpresa($relationAlias = null) Adds a LEFT JOIN clause to the query using the Empresa relation
 * @method OrdenDevolucionQuery rightJoinEmpresa($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Empresa relation
 * @method OrdenDevolucionQuery innerJoinEmpresa($relationAlias = null) Adds a INNER JOIN clause to the query using the Empresa relation
 *
 * @method OrdenDevolucionQuery leftJoinProveedor($relationAlias = null) Adds a LEFT JOIN clause to the query using the Proveedor relation
 * @method OrdenDevolucionQuery rightJoinProveedor($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Proveedor relation
 * @method OrdenDevolucionQuery innerJoinProveedor($relationAlias = null) Adds a INNER JOIN clause to the query using the Proveedor relation
 *
 * @method OrdenDevolucionQuery leftJoinSolicitudDevolucion($relationAlias = null) Adds a LEFT JOIN clause to the query using the SolicitudDevolucion relation
 * @method OrdenDevolucionQuery rightJoinSolicitudDevolucion($relationAlias = null) Adds a RIGHT JOIN clause to the query using the SolicitudDevolucion relation
 * @method OrdenDevolucionQuery innerJoinSolicitudDevolucion($relationAlias = null) Adds a INNER JOIN clause to the query using the SolicitudDevolucion relation
 *
 * @method OrdenDevolucionQuery leftJoinTienda($relationAlias = null) Adds a LEFT JOIN clause to the query using the Tienda relation
 * @method OrdenDevolucionQuery rightJoinTienda($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Tienda relation
 * @method OrdenDevolucionQuery innerJoinTienda($relationAlias = null) Adds a INNER JOIN clause to the query using the Tienda relation
 *
 * @method OrdenDevolucionQuery leftJoinProducto($relationAlias = null) Adds a LEFT JOIN clause to the query using the Producto relation
 * @method OrdenDevolucionQuery rightJoinProducto($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Producto relation
 * @method OrdenDevolucionQuery innerJoinProducto($relationAlias = null) Adds a INNER JOIN clause to the query using the Producto relation
 *
 * @method OrdenDevolucionQuery leftJoinCheque($relationAlias = null) Adds a LEFT JOIN clause to the query using the Cheque relation
 * @method OrdenDevolucionQuery rightJoinCheque($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Cheque relation
 * @method OrdenDevolucionQuery innerJoinCheque($relationAlias = null) Adds a INNER JOIN clause to the query using the Cheque relation
 *
 * @method OrdenDevolucion findOne(PropelPDO $con = null) Return the first OrdenDevolucion matching the query
 * @method OrdenDevolucion findOneOrCreate(PropelPDO $con = null) Return the first OrdenDevolucion matching the query, or a new OrdenDevolucion object populated from the query conditions when no match is found
 *
 * @method OrdenDevolucion findOneById(int $id) Return the first OrdenDevolucion filtered by the id column
 * @method OrdenDevolucion findOneByFecha(string $fecha) Return the first OrdenDevolucion filtered by the fecha column
 * @method OrdenDevolucion findOneByNombre(string $nombre) Return the first OrdenDevolucion filtered by the nombre column
 * @method OrdenDevolucion findOneByReferenciaFactura(string $referencia_factura) Return the first OrdenDevolucion filtered by the referencia_factura column
 * @method OrdenDevolucion findOneByConcepto(string $concepto) Return the first OrdenDevolucion filtered by the concepto column
 * @method OrdenDevolucion findOneByArchivo(string $archivo) Return the first OrdenDevolucion filtered by the archivo column
 * @method OrdenDevolucion findOneByValor(double $valor) Return the first OrdenDevolucion filtered by the valor column
 * @method OrdenDevolucion findOneByRetencion(boolean $retencion) Return the first OrdenDevolucion filtered by the retencion column
 * @method OrdenDevolucion findOneByPartidaNo(int $partida_no) Return the first OrdenDevolucion filtered by the partida_no column
 * @method OrdenDevolucion findOneByEstatus(string $estatus) Return the first OrdenDevolucion filtered by the estatus column
 * @method OrdenDevolucion findOneByChequeNo(int $cheque_no) Return the first OrdenDevolucion filtered by the cheque_no column
 * @method OrdenDevolucion findOneByUsuarioCreo(string $usuario_creo) Return the first OrdenDevolucion filtered by the usuario_creo column
 * @method OrdenDevolucion findOneByCreatedAt(string $created_at) Return the first OrdenDevolucion filtered by the created_at column
 * @method OrdenDevolucion findOneByUsuarioConfirmo(string $usuario_confirmo) Return the first OrdenDevolucion filtered by the usuario_confirmo column
 * @method OrdenDevolucion findOneByUpdatedAt(string $updated_at) Return the first OrdenDevolucion filtered by the updated_at column
 * @method OrdenDevolucion findOneByMotivoAutorizo(string $motivo_autorizo) Return the first OrdenDevolucion filtered by the motivo_autorizo column
 * @method OrdenDevolucion findOneByValorOtros(double $valor_otros) Return the first OrdenDevolucion filtered by the valor_otros column
 * @method OrdenDevolucion findOneByEmpresaId(int $empresa_id) Return the first OrdenDevolucion filtered by the empresa_id column
 * @method OrdenDevolucion findOneByToken(string $token) Return the first OrdenDevolucion filtered by the token column
 * @method OrdenDevolucion findOneByCodigo(string $codigo) Return the first OrdenDevolucion filtered by the codigo column
 * @method OrdenDevolucion findOneByFechaConfirmo(string $fecha_confirmo) Return the first OrdenDevolucion filtered by the fecha_confirmo column
 * @method OrdenDevolucion findOneByPorcentajeRetenie(int $porcentaje_retenie) Return the first OrdenDevolucion filtered by the porcentaje_retenie column
 * @method OrdenDevolucion findOneByTipo(string $tipo) Return the first OrdenDevolucion filtered by the tipo column
 * @method OrdenDevolucion findOneByProveedorId(int $proveedor_id) Return the first OrdenDevolucion filtered by the proveedor_id column
 * @method OrdenDevolucion findOneByPagoMedio(string $pago_medio) Return the first OrdenDevolucion filtered by the pago_medio column
 * @method OrdenDevolucion findOneByVendedor(string $vendedor) Return the first OrdenDevolucion filtered by the vendedor column
 * @method OrdenDevolucion findOneByReferenciaNota(string $referencia_nota) Return the first OrdenDevolucion filtered by the referencia_nota column
 * @method OrdenDevolucion findOneByNoHollander(string $no_hollander) Return the first OrdenDevolucion filtered by the no_hollander column
 * @method OrdenDevolucion findOneByNoStock(string $no_stock) Return the first OrdenDevolucion filtered by the no_stock column
 * @method OrdenDevolucion findOneByDescripcion(string $descripcion) Return the first OrdenDevolucion filtered by the descripcion column
 * @method OrdenDevolucion findOneBySolicitudDevolucionId(int $solicitud_devolucion_id) Return the first OrdenDevolucion filtered by the solicitud_devolucion_id column
 * @method OrdenDevolucion findOneByArchivo2(string $archivo_2) Return the first OrdenDevolucion filtered by the archivo_2 column
 * @method OrdenDevolucion findOneByTiendaId(int $tienda_id) Return the first OrdenDevolucion filtered by the tienda_id column
 * @method OrdenDevolucion findOneByFechaFactura(string $fecha_factura) Return the first OrdenDevolucion filtered by the fecha_factura column
 * @method OrdenDevolucion findOneByMotivo(string $motivo) Return the first OrdenDevolucion filtered by the motivo column
 * @method OrdenDevolucion findOneByDetalleMotivo(string $detalle_motivo) Return the first OrdenDevolucion filtered by the detalle_motivo column
 * @method OrdenDevolucion findOneByDetalleRepuesto(string $detalle_repuesto) Return the first OrdenDevolucion filtered by the detalle_repuesto column
 * @method OrdenDevolucion findOneByProductoId(int $producto_id) Return the first OrdenDevolucion filtered by the producto_id column
 * @method OrdenDevolucion findOneByCantidad(int $cantidad) Return the first OrdenDevolucion filtered by the cantidad column
 *
 * @method array findById(int $id) Return OrdenDevolucion objects filtered by the id column
 * @method array findByFecha(string $fecha) Return OrdenDevolucion objects filtered by the fecha column
 * @method array findByNombre(string $nombre) Return OrdenDevolucion objects filtered by the nombre column
 * @method array findByReferenciaFactura(string $referencia_factura) Return OrdenDevolucion objects filtered by the referencia_factura column
 * @method array findByConcepto(string $concepto) Return OrdenDevolucion objects filtered by the concepto column
 * @method array findByArchivo(string $archivo) Return OrdenDevolucion objects filtered by the archivo column
 * @method array findByValor(double $valor) Return OrdenDevolucion objects filtered by the valor column
 * @method array findByRetencion(boolean $retencion) Return OrdenDevolucion objects filtered by the retencion column
 * @method array findByPartidaNo(int $partida_no) Return OrdenDevolucion objects filtered by the partida_no column
 * @method array findByEstatus(string $estatus) Return OrdenDevolucion objects filtered by the estatus column
 * @method array findByChequeNo(int $cheque_no) Return OrdenDevolucion objects filtered by the cheque_no column
 * @method array findByUsuarioCreo(string $usuario_creo) Return OrdenDevolucion objects filtered by the usuario_creo column
 * @method array findByCreatedAt(string $created_at) Return OrdenDevolucion objects filtered by the created_at column
 * @method array findByUsuarioConfirmo(string $usuario_confirmo) Return OrdenDevolucion objects filtered by the usuario_confirmo column
 * @method array findByUpdatedAt(string $updated_at) Return OrdenDevolucion objects filtered by the updated_at column
 * @method array findByMotivoAutorizo(string $motivo_autorizo) Return OrdenDevolucion objects filtered by the motivo_autorizo column
 * @method array findByValorOtros(double $valor_otros) Return OrdenDevolucion objects filtered by the valor_otros column
 * @method array findByEmpresaId(int $empresa_id) Return OrdenDevolucion objects filtered by the empresa_id column
 * @method array findByToken(string $token) Return OrdenDevolucion objects filtered by the token column
 * @method array findByCodigo(string $codigo) Return OrdenDevolucion objects filtered by the codigo column
 * @method array findByFechaConfirmo(string $fecha_confirmo) Return OrdenDevolucion objects filtered by the fecha_confirmo column
 * @method array findByPorcentajeRetenie(int $porcentaje_retenie) Return OrdenDevolucion objects filtered by the porcentaje_retenie column
 * @method array findByTipo(string $tipo) Return OrdenDevolucion objects filtered by the tipo column
 * @method array findByProveedorId(int $proveedor_id) Return OrdenDevolucion objects filtered by the proveedor_id column
 * @method array findByPagoMedio(string $pago_medio) Return OrdenDevolucion objects filtered by the pago_medio column
 * @method array findByVendedor(string $vendedor) Return OrdenDevolucion objects filtered by the vendedor column
 * @method array findByReferenciaNota(string $referencia_nota) Return OrdenDevolucion objects filtered by the referencia_nota column
 * @method array findByNoHollander(string $no_hollander) Return OrdenDevolucion objects filtered by the no_hollander column
 * @method array findByNoStock(string $no_stock) Return OrdenDevolucion objects filtered by the no_stock column
 * @method array findByDescripcion(string $descripcion) Return OrdenDevolucion objects filtered by the descripcion column
 * @method array findBySolicitudDevolucionId(int $solicitud_devolucion_id) Return OrdenDevolucion objects filtered by the solicitud_devolucion_id column
 * @method array findByArchivo2(string $archivo_2) Return OrdenDevolucion objects filtered by the archivo_2 column
 * @method array findByTiendaId(int $tienda_id) Return OrdenDevolucion objects filtered by the tienda_id column
 * @method array findByFechaFactura(string $fecha_factura) Return OrdenDevolucion objects filtered by the fecha_factura column
 * @method array findByMotivo(string $motivo) Return OrdenDevolucion objects filtered by the motivo column
 * @method array findByDetalleMotivo(string $detalle_motivo) Return OrdenDevolucion objects filtered by the detalle_motivo column
 * @method array findByDetalleRepuesto(string $detalle_repuesto) Return OrdenDevolucion objects filtered by the detalle_repuesto column
 * @method array findByProductoId(int $producto_id) Return OrdenDevolucion objects filtered by the producto_id column
 * @method array findByCantidad(int $cantidad) Return OrdenDevolucion objects filtered by the cantidad column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseOrdenDevolucionQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseOrdenDevolucionQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = 'propel', $modelName = 'OrdenDevolucion', $modelAlias = null)
    {
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new OrdenDevolucionQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param     OrdenDevolucionQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return OrdenDevolucionQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof OrdenDevolucionQuery) {
            return $criteria;
        }
        $query = new OrdenDevolucionQuery();
        if (null !== $modelAlias) {
            $query->setModelAlias($modelAlias);
        }
        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return   OrdenDevolucion|OrdenDevolucion[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = OrdenDevolucionPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is alredy in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(OrdenDevolucionPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return   OrdenDevolucion A model object, or null if the key is not found
     * @throws   PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT `ID`, `FECHA`, `NOMBRE`, `REFERENCIA_FACTURA`, `CONCEPTO`, `ARCHIVO`, `VALOR`, `RETENCION`, `PARTIDA_NO`, `ESTATUS`, `CHEQUE_NO`, `USUARIO_CREO`, `CREATED_AT`, `USUARIO_CONFIRMO`, `UPDATED_AT`, `MOTIVO_AUTORIZO`, `VALOR_OTROS`, `EMPRESA_ID`, `TOKEN`, `CODIGO`, `FECHA_CONFIRMO`, `PORCENTAJE_RETENIE`, `TIPO`, `PROVEEDOR_ID`, `PAGO_MEDIO`, `VENDEDOR`, `REFERENCIA_NOTA`, `NO_HOLLANDER`, `NO_STOCK`, `DESCRIPCION`, `SOLICITUD_DEVOLUCION_ID`, `ARCHIVO_2`, `TIENDA_ID`, `FECHA_FACTURA`, `MOTIVO`, `DETALLE_MOTIVO`, `DETALLE_REPUESTO`, `PRODUCTO_ID`, `CANTIDAD` FROM `orden_devolucion` WHERE `ID` = :p0';
        try {
            $stmt = $con->prepare($sql);
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new OrdenDevolucion();
            $obj->hydrate($row);
            OrdenDevolucionPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return OrdenDevolucion|OrdenDevolucion[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|OrdenDevolucion[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(OrdenDevolucionPeer::ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(OrdenDevolucionPeer::ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the id column
     *
     * Example usage:
     * <code>
     * $query->filterById(1234); // WHERE id = 1234
     * $query->filterById(array(12, 34)); // WHERE id IN (12, 34)
     * $query->filterById(array('min' => 12)); // WHERE id > 12
     * </code>
     *
     * @param     mixed $id The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function filterById($id = null, $comparison = null)
    {
        if (is_array($id) && null === $comparison) {
            $comparison = Criteria::IN;
        }

        return $this->addUsingAlias(OrdenDevolucionPeer::ID, $id, $comparison);
    }

    /**
     * Filter the query on the fecha column
     *
     * Example usage:
     * <code>
     * $query->filterByFecha('2011-03-14'); // WHERE fecha = '2011-03-14'
     * $query->filterByFecha('now'); // WHERE fecha = '2011-03-14'
     * $query->filterByFecha(array('max' => 'yesterday')); // WHERE fecha > '2011-03-13'
     * </code>
     *
     * @param     mixed $fecha The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function filterByFecha($fecha = null, $comparison = null)
    {
        if (is_array($fecha)) {
            $useMinMax = false;
            if (isset($fecha['min'])) {
                $this->addUsingAlias(OrdenDevolucionPeer::FECHA, $fecha['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($fecha['max'])) {
                $this->addUsingAlias(OrdenDevolucionPeer::FECHA, $fecha['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(OrdenDevolucionPeer::FECHA, $fecha, $comparison);
    }

    /**
     * Filter the query on the nombre column
     *
     * Example usage:
     * <code>
     * $query->filterByNombre('fooValue');   // WHERE nombre = 'fooValue'
     * $query->filterByNombre('%fooValue%'); // WHERE nombre LIKE '%fooValue%'
     * </code>
     *
     * @param     string $nombre The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function filterByNombre($nombre = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($nombre)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $nombre)) {
                $nombre = str_replace('*', '%', $nombre);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(OrdenDevolucionPeer::NOMBRE, $nombre, $comparison);
    }

    /**
     * Filter the query on the referencia_factura column
     *
     * Example usage:
     * <code>
     * $query->filterByReferenciaFactura('fooValue');   // WHERE referencia_factura = 'fooValue'
     * $query->filterByReferenciaFactura('%fooValue%'); // WHERE referencia_factura LIKE '%fooValue%'
     * </code>
     *
     * @param     string $referenciaFactura The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function filterByReferenciaFactura($referenciaFactura = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($referenciaFactura)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $referenciaFactura)) {
                $referenciaFactura = str_replace('*', '%', $referenciaFactura);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(OrdenDevolucionPeer::REFERENCIA_FACTURA, $referenciaFactura, $comparison);
    }

    /**
     * Filter the query on the concepto column
     *
     * Example usage:
     * <code>
     * $query->filterByConcepto('fooValue');   // WHERE concepto = 'fooValue'
     * $query->filterByConcepto('%fooValue%'); // WHERE concepto LIKE '%fooValue%'
     * </code>
     *
     * @param     string $concepto The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function filterByConcepto($concepto = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($concepto)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $concepto)) {
                $concepto = str_replace('*', '%', $concepto);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(OrdenDevolucionPeer::CONCEPTO, $concepto, $comparison);
    }

    /**
     * Filter the query on the archivo column
     *
     * Example usage:
     * <code>
     * $query->filterByArchivo('fooValue');   // WHERE archivo = 'fooValue'
     * $query->filterByArchivo('%fooValue%'); // WHERE archivo LIKE '%fooValue%'
     * </code>
     *
     * @param     string $archivo The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function filterByArchivo($archivo = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($archivo)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $archivo)) {
                $archivo = str_replace('*', '%', $archivo);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(OrdenDevolucionPeer::ARCHIVO, $archivo, $comparison);
    }

    /**
     * Filter the query on the valor column
     *
     * Example usage:
     * <code>
     * $query->filterByValor(1234); // WHERE valor = 1234
     * $query->filterByValor(array(12, 34)); // WHERE valor IN (12, 34)
     * $query->filterByValor(array('min' => 12)); // WHERE valor > 12
     * </code>
     *
     * @param     mixed $valor The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function filterByValor($valor = null, $comparison = null)
    {
        if (is_array($valor)) {
            $useMinMax = false;
            if (isset($valor['min'])) {
                $this->addUsingAlias(OrdenDevolucionPeer::VALOR, $valor['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($valor['max'])) {
                $this->addUsingAlias(OrdenDevolucionPeer::VALOR, $valor['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(OrdenDevolucionPeer::VALOR, $valor, $comparison);
    }

    /**
     * Filter the query on the retencion column
     *
     * Example usage:
     * <code>
     * $query->filterByRetencion(true); // WHERE retencion = true
     * $query->filterByRetencion('yes'); // WHERE retencion = true
     * </code>
     *
     * @param     boolean|string $retencion The value to use as filter.
     *              Non-boolean arguments are converted using the following rules:
     *                * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *                * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     *              Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function filterByRetencion($retencion = null, $comparison = null)
    {
        if (is_string($retencion)) {
            $retencion = in_array(strtolower($retencion), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
        }

        return $this->addUsingAlias(OrdenDevolucionPeer::RETENCION, $retencion, $comparison);
    }

    /**
     * Filter the query on the partida_no column
     *
     * Example usage:
     * <code>
     * $query->filterByPartidaNo(1234); // WHERE partida_no = 1234
     * $query->filterByPartidaNo(array(12, 34)); // WHERE partida_no IN (12, 34)
     * $query->filterByPartidaNo(array('min' => 12)); // WHERE partida_no > 12
     * </code>
     *
     * @param     mixed $partidaNo The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function filterByPartidaNo($partidaNo = null, $comparison = null)
    {
        if (is_array($partidaNo)) {
            $useMinMax = false;
            if (isset($partidaNo['min'])) {
                $this->addUsingAlias(OrdenDevolucionPeer::PARTIDA_NO, $partidaNo['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($partidaNo['max'])) {
                $this->addUsingAlias(OrdenDevolucionPeer::PARTIDA_NO, $partidaNo['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(OrdenDevolucionPeer::PARTIDA_NO, $partidaNo, $comparison);
    }

    /**
     * Filter the query on the estatus column
     *
     * Example usage:
     * <code>
     * $query->filterByEstatus('fooValue');   // WHERE estatus = 'fooValue'
     * $query->filterByEstatus('%fooValue%'); // WHERE estatus LIKE '%fooValue%'
     * </code>
     *
     * @param     string $estatus The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function filterByEstatus($estatus = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($estatus)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $estatus)) {
                $estatus = str_replace('*', '%', $estatus);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(OrdenDevolucionPeer::ESTATUS, $estatus, $comparison);
    }

    /**
     * Filter the query on the cheque_no column
     *
     * Example usage:
     * <code>
     * $query->filterByChequeNo(1234); // WHERE cheque_no = 1234
     * $query->filterByChequeNo(array(12, 34)); // WHERE cheque_no IN (12, 34)
     * $query->filterByChequeNo(array('min' => 12)); // WHERE cheque_no > 12
     * </code>
     *
     * @param     mixed $chequeNo The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function filterByChequeNo($chequeNo = null, $comparison = null)
    {
        if (is_array($chequeNo)) {
            $useMinMax = false;
            if (isset($chequeNo['min'])) {
                $this->addUsingAlias(OrdenDevolucionPeer::CHEQUE_NO, $chequeNo['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($chequeNo['max'])) {
                $this->addUsingAlias(OrdenDevolucionPeer::CHEQUE_NO, $chequeNo['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(OrdenDevolucionPeer::CHEQUE_NO, $chequeNo, $comparison);
    }

    /**
     * Filter the query on the usuario_creo column
     *
     * Example usage:
     * <code>
     * $query->filterByUsuarioCreo('fooValue');   // WHERE usuario_creo = 'fooValue'
     * $query->filterByUsuarioCreo('%fooValue%'); // WHERE usuario_creo LIKE '%fooValue%'
     * </code>
     *
     * @param     string $usuarioCreo The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function filterByUsuarioCreo($usuarioCreo = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($usuarioCreo)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $usuarioCreo)) {
                $usuarioCreo = str_replace('*', '%', $usuarioCreo);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(OrdenDevolucionPeer::USUARIO_CREO, $usuarioCreo, $comparison);
    }

    /**
     * Filter the query on the created_at column
     *
     * Example usage:
     * <code>
     * $query->filterByCreatedAt('2011-03-14'); // WHERE created_at = '2011-03-14'
     * $query->filterByCreatedAt('now'); // WHERE created_at = '2011-03-14'
     * $query->filterByCreatedAt(array('max' => 'yesterday')); // WHERE created_at > '2011-03-13'
     * </code>
     *
     * @param     mixed $createdAt The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function filterByCreatedAt($createdAt = null, $comparison = null)
    {
        if (is_array($createdAt)) {
            $useMinMax = false;
            if (isset($createdAt['min'])) {
                $this->addUsingAlias(OrdenDevolucionPeer::CREATED_AT, $createdAt['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($createdAt['max'])) {
                $this->addUsingAlias(OrdenDevolucionPeer::CREATED_AT, $createdAt['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(OrdenDevolucionPeer::CREATED_AT, $createdAt, $comparison);
    }

    /**
     * Filter the query on the usuario_confirmo column
     *
     * Example usage:
     * <code>
     * $query->filterByUsuarioConfirmo('fooValue');   // WHERE usuario_confirmo = 'fooValue'
     * $query->filterByUsuarioConfirmo('%fooValue%'); // WHERE usuario_confirmo LIKE '%fooValue%'
     * </code>
     *
     * @param     string $usuarioConfirmo The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function filterByUsuarioConfirmo($usuarioConfirmo = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($usuarioConfirmo)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $usuarioConfirmo)) {
                $usuarioConfirmo = str_replace('*', '%', $usuarioConfirmo);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(OrdenDevolucionPeer::USUARIO_CONFIRMO, $usuarioConfirmo, $comparison);
    }

    /**
     * Filter the query on the updated_at column
     *
     * Example usage:
     * <code>
     * $query->filterByUpdatedAt('2011-03-14'); // WHERE updated_at = '2011-03-14'
     * $query->filterByUpdatedAt('now'); // WHERE updated_at = '2011-03-14'
     * $query->filterByUpdatedAt(array('max' => 'yesterday')); // WHERE updated_at > '2011-03-13'
     * </code>
     *
     * @param     mixed $updatedAt The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function filterByUpdatedAt($updatedAt = null, $comparison = null)
    {
        if (is_array($updatedAt)) {
            $useMinMax = false;
            if (isset($updatedAt['min'])) {
                $this->addUsingAlias(OrdenDevolucionPeer::UPDATED_AT, $updatedAt['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($updatedAt['max'])) {
                $this->addUsingAlias(OrdenDevolucionPeer::UPDATED_AT, $updatedAt['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(OrdenDevolucionPeer::UPDATED_AT, $updatedAt, $comparison);
    }

    /**
     * Filter the query on the motivo_autorizo column
     *
     * Example usage:
     * <code>
     * $query->filterByMotivoAutorizo('fooValue');   // WHERE motivo_autorizo = 'fooValue'
     * $query->filterByMotivoAutorizo('%fooValue%'); // WHERE motivo_autorizo LIKE '%fooValue%'
     * </code>
     *
     * @param     string $motivoAutorizo The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function filterByMotivoAutorizo($motivoAutorizo = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($motivoAutorizo)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $motivoAutorizo)) {
                $motivoAutorizo = str_replace('*', '%', $motivoAutorizo);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(OrdenDevolucionPeer::MOTIVO_AUTORIZO, $motivoAutorizo, $comparison);
    }

    /**
     * Filter the query on the valor_otros column
     *
     * Example usage:
     * <code>
     * $query->filterByValorOtros(1234); // WHERE valor_otros = 1234
     * $query->filterByValorOtros(array(12, 34)); // WHERE valor_otros IN (12, 34)
     * $query->filterByValorOtros(array('min' => 12)); // WHERE valor_otros > 12
     * </code>
     *
     * @param     mixed $valorOtros The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function filterByValorOtros($valorOtros = null, $comparison = null)
    {
        if (is_array($valorOtros)) {
            $useMinMax = false;
            if (isset($valorOtros['min'])) {
                $this->addUsingAlias(OrdenDevolucionPeer::VALOR_OTROS, $valorOtros['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($valorOtros['max'])) {
                $this->addUsingAlias(OrdenDevolucionPeer::VALOR_OTROS, $valorOtros['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(OrdenDevolucionPeer::VALOR_OTROS, $valorOtros, $comparison);
    }

    /**
     * Filter the query on the empresa_id column
     *
     * Example usage:
     * <code>
     * $query->filterByEmpresaId(1234); // WHERE empresa_id = 1234
     * $query->filterByEmpresaId(array(12, 34)); // WHERE empresa_id IN (12, 34)
     * $query->filterByEmpresaId(array('min' => 12)); // WHERE empresa_id > 12
     * </code>
     *
     * @see       filterByEmpresa()
     *
     * @param     mixed $empresaId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function filterByEmpresaId($empresaId = null, $comparison = null)
    {
        if (is_array($empresaId)) {
            $useMinMax = false;
            if (isset($empresaId['min'])) {
                $this->addUsingAlias(OrdenDevolucionPeer::EMPRESA_ID, $empresaId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($empresaId['max'])) {
                $this->addUsingAlias(OrdenDevolucionPeer::EMPRESA_ID, $empresaId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(OrdenDevolucionPeer::EMPRESA_ID, $empresaId, $comparison);
    }

    /**
     * Filter the query on the token column
     *
     * Example usage:
     * <code>
     * $query->filterByToken('fooValue');   // WHERE token = 'fooValue'
     * $query->filterByToken('%fooValue%'); // WHERE token LIKE '%fooValue%'
     * </code>
     *
     * @param     string $token The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function filterByToken($token = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($token)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $token)) {
                $token = str_replace('*', '%', $token);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(OrdenDevolucionPeer::TOKEN, $token, $comparison);
    }

    /**
     * Filter the query on the codigo column
     *
     * Example usage:
     * <code>
     * $query->filterByCodigo('fooValue');   // WHERE codigo = 'fooValue'
     * $query->filterByCodigo('%fooValue%'); // WHERE codigo LIKE '%fooValue%'
     * </code>
     *
     * @param     string $codigo The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function filterByCodigo($codigo = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($codigo)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $codigo)) {
                $codigo = str_replace('*', '%', $codigo);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(OrdenDevolucionPeer::CODIGO, $codigo, $comparison);
    }

    /**
     * Filter the query on the fecha_confirmo column
     *
     * Example usage:
     * <code>
     * $query->filterByFechaConfirmo('2011-03-14'); // WHERE fecha_confirmo = '2011-03-14'
     * $query->filterByFechaConfirmo('now'); // WHERE fecha_confirmo = '2011-03-14'
     * $query->filterByFechaConfirmo(array('max' => 'yesterday')); // WHERE fecha_confirmo > '2011-03-13'
     * </code>
     *
     * @param     mixed $fechaConfirmo The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function filterByFechaConfirmo($fechaConfirmo = null, $comparison = null)
    {
        if (is_array($fechaConfirmo)) {
            $useMinMax = false;
            if (isset($fechaConfirmo['min'])) {
                $this->addUsingAlias(OrdenDevolucionPeer::FECHA_CONFIRMO, $fechaConfirmo['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($fechaConfirmo['max'])) {
                $this->addUsingAlias(OrdenDevolucionPeer::FECHA_CONFIRMO, $fechaConfirmo['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(OrdenDevolucionPeer::FECHA_CONFIRMO, $fechaConfirmo, $comparison);
    }

    /**
     * Filter the query on the porcentaje_retenie column
     *
     * Example usage:
     * <code>
     * $query->filterByPorcentajeRetenie(1234); // WHERE porcentaje_retenie = 1234
     * $query->filterByPorcentajeRetenie(array(12, 34)); // WHERE porcentaje_retenie IN (12, 34)
     * $query->filterByPorcentajeRetenie(array('min' => 12)); // WHERE porcentaje_retenie > 12
     * </code>
     *
     * @param     mixed $porcentajeRetenie The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function filterByPorcentajeRetenie($porcentajeRetenie = null, $comparison = null)
    {
        if (is_array($porcentajeRetenie)) {
            $useMinMax = false;
            if (isset($porcentajeRetenie['min'])) {
                $this->addUsingAlias(OrdenDevolucionPeer::PORCENTAJE_RETENIE, $porcentajeRetenie['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($porcentajeRetenie['max'])) {
                $this->addUsingAlias(OrdenDevolucionPeer::PORCENTAJE_RETENIE, $porcentajeRetenie['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(OrdenDevolucionPeer::PORCENTAJE_RETENIE, $porcentajeRetenie, $comparison);
    }

    /**
     * Filter the query on the tipo column
     *
     * Example usage:
     * <code>
     * $query->filterByTipo('fooValue');   // WHERE tipo = 'fooValue'
     * $query->filterByTipo('%fooValue%'); // WHERE tipo LIKE '%fooValue%'
     * </code>
     *
     * @param     string $tipo The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function filterByTipo($tipo = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($tipo)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $tipo)) {
                $tipo = str_replace('*', '%', $tipo);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(OrdenDevolucionPeer::TIPO, $tipo, $comparison);
    }

    /**
     * Filter the query on the proveedor_id column
     *
     * Example usage:
     * <code>
     * $query->filterByProveedorId(1234); // WHERE proveedor_id = 1234
     * $query->filterByProveedorId(array(12, 34)); // WHERE proveedor_id IN (12, 34)
     * $query->filterByProveedorId(array('min' => 12)); // WHERE proveedor_id > 12
     * </code>
     *
     * @see       filterByProveedor()
     *
     * @param     mixed $proveedorId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function filterByProveedorId($proveedorId = null, $comparison = null)
    {
        if (is_array($proveedorId)) {
            $useMinMax = false;
            if (isset($proveedorId['min'])) {
                $this->addUsingAlias(OrdenDevolucionPeer::PROVEEDOR_ID, $proveedorId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($proveedorId['max'])) {
                $this->addUsingAlias(OrdenDevolucionPeer::PROVEEDOR_ID, $proveedorId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(OrdenDevolucionPeer::PROVEEDOR_ID, $proveedorId, $comparison);
    }

    /**
     * Filter the query on the pago_medio column
     *
     * Example usage:
     * <code>
     * $query->filterByPagoMedio('fooValue');   // WHERE pago_medio = 'fooValue'
     * $query->filterByPagoMedio('%fooValue%'); // WHERE pago_medio LIKE '%fooValue%'
     * </code>
     *
     * @param     string $pagoMedio The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function filterByPagoMedio($pagoMedio = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($pagoMedio)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $pagoMedio)) {
                $pagoMedio = str_replace('*', '%', $pagoMedio);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(OrdenDevolucionPeer::PAGO_MEDIO, $pagoMedio, $comparison);
    }

    /**
     * Filter the query on the vendedor column
     *
     * Example usage:
     * <code>
     * $query->filterByVendedor('fooValue');   // WHERE vendedor = 'fooValue'
     * $query->filterByVendedor('%fooValue%'); // WHERE vendedor LIKE '%fooValue%'
     * </code>
     *
     * @param     string $vendedor The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function filterByVendedor($vendedor = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($vendedor)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $vendedor)) {
                $vendedor = str_replace('*', '%', $vendedor);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(OrdenDevolucionPeer::VENDEDOR, $vendedor, $comparison);
    }

    /**
     * Filter the query on the referencia_nota column
     *
     * Example usage:
     * <code>
     * $query->filterByReferenciaNota('fooValue');   // WHERE referencia_nota = 'fooValue'
     * $query->filterByReferenciaNota('%fooValue%'); // WHERE referencia_nota LIKE '%fooValue%'
     * </code>
     *
     * @param     string $referenciaNota The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function filterByReferenciaNota($referenciaNota = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($referenciaNota)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $referenciaNota)) {
                $referenciaNota = str_replace('*', '%', $referenciaNota);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(OrdenDevolucionPeer::REFERENCIA_NOTA, $referenciaNota, $comparison);
    }

    /**
     * Filter the query on the no_hollander column
     *
     * Example usage:
     * <code>
     * $query->filterByNoHollander('fooValue');   // WHERE no_hollander = 'fooValue'
     * $query->filterByNoHollander('%fooValue%'); // WHERE no_hollander LIKE '%fooValue%'
     * </code>
     *
     * @param     string $noHollander The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function filterByNoHollander($noHollander = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($noHollander)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $noHollander)) {
                $noHollander = str_replace('*', '%', $noHollander);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(OrdenDevolucionPeer::NO_HOLLANDER, $noHollander, $comparison);
    }

    /**
     * Filter the query on the no_stock column
     *
     * Example usage:
     * <code>
     * $query->filterByNoStock('fooValue');   // WHERE no_stock = 'fooValue'
     * $query->filterByNoStock('%fooValue%'); // WHERE no_stock LIKE '%fooValue%'
     * </code>
     *
     * @param     string $noStock The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function filterByNoStock($noStock = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($noStock)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $noStock)) {
                $noStock = str_replace('*', '%', $noStock);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(OrdenDevolucionPeer::NO_STOCK, $noStock, $comparison);
    }

    /**
     * Filter the query on the descripcion column
     *
     * Example usage:
     * <code>
     * $query->filterByDescripcion('fooValue');   // WHERE descripcion = 'fooValue'
     * $query->filterByDescripcion('%fooValue%'); // WHERE descripcion LIKE '%fooValue%'
     * </code>
     *
     * @param     string $descripcion The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function filterByDescripcion($descripcion = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($descripcion)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $descripcion)) {
                $descripcion = str_replace('*', '%', $descripcion);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(OrdenDevolucionPeer::DESCRIPCION, $descripcion, $comparison);
    }

    /**
     * Filter the query on the solicitud_devolucion_id column
     *
     * Example usage:
     * <code>
     * $query->filterBySolicitudDevolucionId(1234); // WHERE solicitud_devolucion_id = 1234
     * $query->filterBySolicitudDevolucionId(array(12, 34)); // WHERE solicitud_devolucion_id IN (12, 34)
     * $query->filterBySolicitudDevolucionId(array('min' => 12)); // WHERE solicitud_devolucion_id > 12
     * </code>
     *
     * @see       filterBySolicitudDevolucion()
     *
     * @param     mixed $solicitudDevolucionId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function filterBySolicitudDevolucionId($solicitudDevolucionId = null, $comparison = null)
    {
        if (is_array($solicitudDevolucionId)) {
            $useMinMax = false;
            if (isset($solicitudDevolucionId['min'])) {
                $this->addUsingAlias(OrdenDevolucionPeer::SOLICITUD_DEVOLUCION_ID, $solicitudDevolucionId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($solicitudDevolucionId['max'])) {
                $this->addUsingAlias(OrdenDevolucionPeer::SOLICITUD_DEVOLUCION_ID, $solicitudDevolucionId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(OrdenDevolucionPeer::SOLICITUD_DEVOLUCION_ID, $solicitudDevolucionId, $comparison);
    }

    /**
     * Filter the query on the archivo_2 column
     *
     * Example usage:
     * <code>
     * $query->filterByArchivo2('fooValue');   // WHERE archivo_2 = 'fooValue'
     * $query->filterByArchivo2('%fooValue%'); // WHERE archivo_2 LIKE '%fooValue%'
     * </code>
     *
     * @param     string $archivo2 The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function filterByArchivo2($archivo2 = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($archivo2)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $archivo2)) {
                $archivo2 = str_replace('*', '%', $archivo2);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(OrdenDevolucionPeer::ARCHIVO_2, $archivo2, $comparison);
    }

    /**
     * Filter the query on the tienda_id column
     *
     * Example usage:
     * <code>
     * $query->filterByTiendaId(1234); // WHERE tienda_id = 1234
     * $query->filterByTiendaId(array(12, 34)); // WHERE tienda_id IN (12, 34)
     * $query->filterByTiendaId(array('min' => 12)); // WHERE tienda_id > 12
     * </code>
     *
     * @see       filterByTienda()
     *
     * @param     mixed $tiendaId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function filterByTiendaId($tiendaId = null, $comparison = null)
    {
        if (is_array($tiendaId)) {
            $useMinMax = false;
            if (isset($tiendaId['min'])) {
                $this->addUsingAlias(OrdenDevolucionPeer::TIENDA_ID, $tiendaId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($tiendaId['max'])) {
                $this->addUsingAlias(OrdenDevolucionPeer::TIENDA_ID, $tiendaId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(OrdenDevolucionPeer::TIENDA_ID, $tiendaId, $comparison);
    }

    /**
     * Filter the query on the fecha_factura column
     *
     * Example usage:
     * <code>
     * $query->filterByFechaFactura('2011-03-14'); // WHERE fecha_factura = '2011-03-14'
     * $query->filterByFechaFactura('now'); // WHERE fecha_factura = '2011-03-14'
     * $query->filterByFechaFactura(array('max' => 'yesterday')); // WHERE fecha_factura > '2011-03-13'
     * </code>
     *
     * @param     mixed $fechaFactura The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function filterByFechaFactura($fechaFactura = null, $comparison = null)
    {
        if (is_array($fechaFactura)) {
            $useMinMax = false;
            if (isset($fechaFactura['min'])) {
                $this->addUsingAlias(OrdenDevolucionPeer::FECHA_FACTURA, $fechaFactura['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($fechaFactura['max'])) {
                $this->addUsingAlias(OrdenDevolucionPeer::FECHA_FACTURA, $fechaFactura['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(OrdenDevolucionPeer::FECHA_FACTURA, $fechaFactura, $comparison);
    }

    /**
     * Filter the query on the motivo column
     *
     * Example usage:
     * <code>
     * $query->filterByMotivo('fooValue');   // WHERE motivo = 'fooValue'
     * $query->filterByMotivo('%fooValue%'); // WHERE motivo LIKE '%fooValue%'
     * </code>
     *
     * @param     string $motivo The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function filterByMotivo($motivo = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($motivo)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $motivo)) {
                $motivo = str_replace('*', '%', $motivo);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(OrdenDevolucionPeer::MOTIVO, $motivo, $comparison);
    }

    /**
     * Filter the query on the detalle_motivo column
     *
     * Example usage:
     * <code>
     * $query->filterByDetalleMotivo('fooValue');   // WHERE detalle_motivo = 'fooValue'
     * $query->filterByDetalleMotivo('%fooValue%'); // WHERE detalle_motivo LIKE '%fooValue%'
     * </code>
     *
     * @param     string $detalleMotivo The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function filterByDetalleMotivo($detalleMotivo = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($detalleMotivo)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $detalleMotivo)) {
                $detalleMotivo = str_replace('*', '%', $detalleMotivo);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(OrdenDevolucionPeer::DETALLE_MOTIVO, $detalleMotivo, $comparison);
    }

    /**
     * Filter the query on the detalle_repuesto column
     *
     * Example usage:
     * <code>
     * $query->filterByDetalleRepuesto('fooValue');   // WHERE detalle_repuesto = 'fooValue'
     * $query->filterByDetalleRepuesto('%fooValue%'); // WHERE detalle_repuesto LIKE '%fooValue%'
     * </code>
     *
     * @param     string $detalleRepuesto The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function filterByDetalleRepuesto($detalleRepuesto = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($detalleRepuesto)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $detalleRepuesto)) {
                $detalleRepuesto = str_replace('*', '%', $detalleRepuesto);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(OrdenDevolucionPeer::DETALLE_REPUESTO, $detalleRepuesto, $comparison);
    }

    /**
     * Filter the query on the producto_id column
     *
     * Example usage:
     * <code>
     * $query->filterByProductoId(1234); // WHERE producto_id = 1234
     * $query->filterByProductoId(array(12, 34)); // WHERE producto_id IN (12, 34)
     * $query->filterByProductoId(array('min' => 12)); // WHERE producto_id > 12
     * </code>
     *
     * @see       filterByProducto()
     *
     * @param     mixed $productoId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function filterByProductoId($productoId = null, $comparison = null)
    {
        if (is_array($productoId)) {
            $useMinMax = false;
            if (isset($productoId['min'])) {
                $this->addUsingAlias(OrdenDevolucionPeer::PRODUCTO_ID, $productoId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($productoId['max'])) {
                $this->addUsingAlias(OrdenDevolucionPeer::PRODUCTO_ID, $productoId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(OrdenDevolucionPeer::PRODUCTO_ID, $productoId, $comparison);
    }

    /**
     * Filter the query on the cantidad column
     *
     * Example usage:
     * <code>
     * $query->filterByCantidad(1234); // WHERE cantidad = 1234
     * $query->filterByCantidad(array(12, 34)); // WHERE cantidad IN (12, 34)
     * $query->filterByCantidad(array('min' => 12)); // WHERE cantidad > 12
     * </code>
     *
     * @param     mixed $cantidad The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function filterByCantidad($cantidad = null, $comparison = null)
    {
        if (is_array($cantidad)) {
            $useMinMax = false;
            if (isset($cantidad['min'])) {
                $this->addUsingAlias(OrdenDevolucionPeer::CANTIDAD, $cantidad['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($cantidad['max'])) {
                $this->addUsingAlias(OrdenDevolucionPeer::CANTIDAD, $cantidad['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(OrdenDevolucionPeer::CANTIDAD, $cantidad, $comparison);
    }

    /**
     * Filter the query by a related Empresa object
     *
     * @param   Empresa|PropelObjectCollection $empresa The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   OrdenDevolucionQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByEmpresa($empresa, $comparison = null)
    {
        if ($empresa instanceof Empresa) {
            return $this
                ->addUsingAlias(OrdenDevolucionPeer::EMPRESA_ID, $empresa->getId(), $comparison);
        } elseif ($empresa instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(OrdenDevolucionPeer::EMPRESA_ID, $empresa->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByEmpresa() only accepts arguments of type Empresa or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Empresa relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function joinEmpresa($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Empresa');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Empresa');
        }

        return $this;
    }

    /**
     * Use the Empresa relation Empresa object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   EmpresaQuery A secondary query class using the current class as primary query
     */
    public function useEmpresaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinEmpresa($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Empresa', 'EmpresaQuery');
    }

    /**
     * Filter the query by a related Proveedor object
     *
     * @param   Proveedor|PropelObjectCollection $proveedor The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   OrdenDevolucionQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByProveedor($proveedor, $comparison = null)
    {
        if ($proveedor instanceof Proveedor) {
            return $this
                ->addUsingAlias(OrdenDevolucionPeer::PROVEEDOR_ID, $proveedor->getId(), $comparison);
        } elseif ($proveedor instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(OrdenDevolucionPeer::PROVEEDOR_ID, $proveedor->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByProveedor() only accepts arguments of type Proveedor or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Proveedor relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function joinProveedor($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Proveedor');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Proveedor');
        }

        return $this;
    }

    /**
     * Use the Proveedor relation Proveedor object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ProveedorQuery A secondary query class using the current class as primary query
     */
    public function useProveedorQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinProveedor($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Proveedor', 'ProveedorQuery');
    }

    /**
     * Filter the query by a related SolicitudDevolucion object
     *
     * @param   SolicitudDevolucion|PropelObjectCollection $solicitudDevolucion The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   OrdenDevolucionQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterBySolicitudDevolucion($solicitudDevolucion, $comparison = null)
    {
        if ($solicitudDevolucion instanceof SolicitudDevolucion) {
            return $this
                ->addUsingAlias(OrdenDevolucionPeer::SOLICITUD_DEVOLUCION_ID, $solicitudDevolucion->getId(), $comparison);
        } elseif ($solicitudDevolucion instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(OrdenDevolucionPeer::SOLICITUD_DEVOLUCION_ID, $solicitudDevolucion->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterBySolicitudDevolucion() only accepts arguments of type SolicitudDevolucion or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the SolicitudDevolucion relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function joinSolicitudDevolucion($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('SolicitudDevolucion');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'SolicitudDevolucion');
        }

        return $this;
    }

    /**
     * Use the SolicitudDevolucion relation SolicitudDevolucion object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   SolicitudDevolucionQuery A secondary query class using the current class as primary query
     */
    public function useSolicitudDevolucionQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinSolicitudDevolucion($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'SolicitudDevolucion', 'SolicitudDevolucionQuery');
    }

    /**
     * Filter the query by a related Tienda object
     *
     * @param   Tienda|PropelObjectCollection $tienda The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   OrdenDevolucionQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByTienda($tienda, $comparison = null)
    {
        if ($tienda instanceof Tienda) {
            return $this
                ->addUsingAlias(OrdenDevolucionPeer::TIENDA_ID, $tienda->getId(), $comparison);
        } elseif ($tienda instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(OrdenDevolucionPeer::TIENDA_ID, $tienda->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByTienda() only accepts arguments of type Tienda or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Tienda relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function joinTienda($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Tienda');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Tienda');
        }

        return $this;
    }

    /**
     * Use the Tienda relation Tienda object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   TiendaQuery A secondary query class using the current class as primary query
     */
    public function useTiendaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinTienda($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Tienda', 'TiendaQuery');
    }

    /**
     * Filter the query by a related Producto object
     *
     * @param   Producto|PropelObjectCollection $producto The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   OrdenDevolucionQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByProducto($producto, $comparison = null)
    {
        if ($producto instanceof Producto) {
            return $this
                ->addUsingAlias(OrdenDevolucionPeer::PRODUCTO_ID, $producto->getId(), $comparison);
        } elseif ($producto instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(OrdenDevolucionPeer::PRODUCTO_ID, $producto->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByProducto() only accepts arguments of type Producto or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Producto relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function joinProducto($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Producto');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Producto');
        }

        return $this;
    }

    /**
     * Use the Producto relation Producto object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ProductoQuery A secondary query class using the current class as primary query
     */
    public function useProductoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinProducto($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Producto', 'ProductoQuery');
    }

    /**
     * Filter the query by a related Cheque object
     *
     * @param   Cheque|PropelObjectCollection $cheque  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   OrdenDevolucionQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByCheque($cheque, $comparison = null)
    {
        if ($cheque instanceof Cheque) {
            return $this
                ->addUsingAlias(OrdenDevolucionPeer::ID, $cheque->getOrdenDevolucionId(), $comparison);
        } elseif ($cheque instanceof PropelObjectCollection) {
            return $this
                ->useChequeQuery()
                ->filterByPrimaryKeys($cheque->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByCheque() only accepts arguments of type Cheque or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Cheque relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function joinCheque($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Cheque');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Cheque');
        }

        return $this;
    }

    /**
     * Use the Cheque relation Cheque object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ChequeQuery A secondary query class using the current class as primary query
     */
    public function useChequeQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinCheque($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Cheque', 'ChequeQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   OrdenDevolucion $ordenDevolucion Object to remove from the list of results
     *
     * @return OrdenDevolucionQuery The current query, for fluid interface
     */
    public function prune($ordenDevolucion = null)
    {
        if ($ordenDevolucion) {
            $this->addUsingAlias(OrdenDevolucionPeer::ID, $ordenDevolucion->getId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}
