<?php


/**
 * Base class that represents a query for the 'facturas' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.6.7 on:
 *
 * 01/03/26 04:09:45
 *
 * @method FacturasQuery orderById($order = Criteria::ASC) Order by the id column
 * @method FacturasQuery orderByTienda($order = Criteria::ASC) Order by the tienda column
 * @method FacturasQuery orderByReferencia($order = Criteria::ASC) Order by the referencia column
 * @method FacturasQuery orderByFecha($order = Criteria::ASC) Order by the fecha column
 * @method FacturasQuery orderByTipoDocumento($order = Criteria::ASC) Order by the tipo_documento column
 * @method FacturasQuery orderByFirma($order = Criteria::ASC) Order by the firma column
 * @method FacturasQuery orderByMotivo($order = Criteria::ASC) Order by the motivo column
 *
 * @method FacturasQuery groupById() Group by the id column
 * @method FacturasQuery groupByTienda() Group by the tienda column
 * @method FacturasQuery groupByReferencia() Group by the referencia column
 * @method FacturasQuery groupByFecha() Group by the fecha column
 * @method FacturasQuery groupByTipoDocumento() Group by the tipo_documento column
 * @method FacturasQuery groupByFirma() Group by the firma column
 * @method FacturasQuery groupByMotivo() Group by the motivo column
 *
 * @method FacturasQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method FacturasQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method FacturasQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method Facturas findOne(PropelPDO $con = null) Return the first Facturas matching the query
 * @method Facturas findOneOrCreate(PropelPDO $con = null) Return the first Facturas matching the query, or a new Facturas object populated from the query conditions when no match is found
 *
 * @method Facturas findOneById(int $id) Return the first Facturas filtered by the id column
 * @method Facturas findOneByTienda(string $tienda) Return the first Facturas filtered by the tienda column
 * @method Facturas findOneByReferencia(string $referencia) Return the first Facturas filtered by the referencia column
 * @method Facturas findOneByFecha(string $fecha) Return the first Facturas filtered by the fecha column
 * @method Facturas findOneByTipoDocumento(string $tipo_documento) Return the first Facturas filtered by the tipo_documento column
 * @method Facturas findOneByFirma(string $firma) Return the first Facturas filtered by the firma column
 * @method Facturas findOneByMotivo(string $motivo) Return the first Facturas filtered by the motivo column
 *
 * @method array findById(int $id) Return Facturas objects filtered by the id column
 * @method array findByTienda(string $tienda) Return Facturas objects filtered by the tienda column
 * @method array findByReferencia(string $referencia) Return Facturas objects filtered by the referencia column
 * @method array findByFecha(string $fecha) Return Facturas objects filtered by the fecha column
 * @method array findByTipoDocumento(string $tipo_documento) Return Facturas objects filtered by the tipo_documento column
 * @method array findByFirma(string $firma) Return Facturas objects filtered by the firma column
 * @method array findByMotivo(string $motivo) Return Facturas objects filtered by the motivo column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseFacturasQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseFacturasQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = 'propel', $modelName = 'Facturas', $modelAlias = null)
    {
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new FacturasQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param     FacturasQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return FacturasQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof FacturasQuery) {
            return $criteria;
        }
        $query = new FacturasQuery();
        if (null !== $modelAlias) {
            $query->setModelAlias($modelAlias);
        }
        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return   Facturas|Facturas[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = FacturasPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is alredy in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(FacturasPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return   Facturas A model object, or null if the key is not found
     * @throws   PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT `ID`, `TIENDA`, `REFERENCIA`, `FECHA`, `TIPO_DOCUMENTO`, `FIRMA`, `MOTIVO` FROM `facturas` WHERE `ID` = :p0';
        try {
            $stmt = $con->prepare($sql);
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new Facturas();
            $obj->hydrate($row);
            FacturasPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return Facturas|Facturas[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|Facturas[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return FacturasQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(FacturasPeer::ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return FacturasQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(FacturasPeer::ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the id column
     *
     * Example usage:
     * <code>
     * $query->filterById(1234); // WHERE id = 1234
     * $query->filterById(array(12, 34)); // WHERE id IN (12, 34)
     * $query->filterById(array('min' => 12)); // WHERE id > 12
     * </code>
     *
     * @param     mixed $id The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FacturasQuery The current query, for fluid interface
     */
    public function filterById($id = null, $comparison = null)
    {
        if (is_array($id) && null === $comparison) {
            $comparison = Criteria::IN;
        }

        return $this->addUsingAlias(FacturasPeer::ID, $id, $comparison);
    }

    /**
     * Filter the query on the tienda column
     *
     * Example usage:
     * <code>
     * $query->filterByTienda('fooValue');   // WHERE tienda = 'fooValue'
     * $query->filterByTienda('%fooValue%'); // WHERE tienda LIKE '%fooValue%'
     * </code>
     *
     * @param     string $tienda The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FacturasQuery The current query, for fluid interface
     */
    public function filterByTienda($tienda = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($tienda)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $tienda)) {
                $tienda = str_replace('*', '%', $tienda);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(FacturasPeer::TIENDA, $tienda, $comparison);
    }

    /**
     * Filter the query on the referencia column
     *
     * Example usage:
     * <code>
     * $query->filterByReferencia('fooValue');   // WHERE referencia = 'fooValue'
     * $query->filterByReferencia('%fooValue%'); // WHERE referencia LIKE '%fooValue%'
     * </code>
     *
     * @param     string $referencia The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FacturasQuery The current query, for fluid interface
     */
    public function filterByReferencia($referencia = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($referencia)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $referencia)) {
                $referencia = str_replace('*', '%', $referencia);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(FacturasPeer::REFERENCIA, $referencia, $comparison);
    }

    /**
     * Filter the query on the fecha column
     *
     * Example usage:
     * <code>
     * $query->filterByFecha('2011-03-14'); // WHERE fecha = '2011-03-14'
     * $query->filterByFecha('now'); // WHERE fecha = '2011-03-14'
     * $query->filterByFecha(array('max' => 'yesterday')); // WHERE fecha > '2011-03-13'
     * </code>
     *
     * @param     mixed $fecha The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FacturasQuery The current query, for fluid interface
     */
    public function filterByFecha($fecha = null, $comparison = null)
    {
        if (is_array($fecha)) {
            $useMinMax = false;
            if (isset($fecha['min'])) {
                $this->addUsingAlias(FacturasPeer::FECHA, $fecha['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($fecha['max'])) {
                $this->addUsingAlias(FacturasPeer::FECHA, $fecha['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(FacturasPeer::FECHA, $fecha, $comparison);
    }

    /**
     * Filter the query on the tipo_documento column
     *
     * Example usage:
     * <code>
     * $query->filterByTipoDocumento('fooValue');   // WHERE tipo_documento = 'fooValue'
     * $query->filterByTipoDocumento('%fooValue%'); // WHERE tipo_documento LIKE '%fooValue%'
     * </code>
     *
     * @param     string $tipoDocumento The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FacturasQuery The current query, for fluid interface
     */
    public function filterByTipoDocumento($tipoDocumento = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($tipoDocumento)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $tipoDocumento)) {
                $tipoDocumento = str_replace('*', '%', $tipoDocumento);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(FacturasPeer::TIPO_DOCUMENTO, $tipoDocumento, $comparison);
    }

    /**
     * Filter the query on the firma column
     *
     * Example usage:
     * <code>
     * $query->filterByFirma('fooValue');   // WHERE firma = 'fooValue'
     * $query->filterByFirma('%fooValue%'); // WHERE firma LIKE '%fooValue%'
     * </code>
     *
     * @param     string $firma The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FacturasQuery The current query, for fluid interface
     */
    public function filterByFirma($firma = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($firma)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $firma)) {
                $firma = str_replace('*', '%', $firma);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(FacturasPeer::FIRMA, $firma, $comparison);
    }

    /**
     * Filter the query on the motivo column
     *
     * Example usage:
     * <code>
     * $query->filterByMotivo('fooValue');   // WHERE motivo = 'fooValue'
     * $query->filterByMotivo('%fooValue%'); // WHERE motivo LIKE '%fooValue%'
     * </code>
     *
     * @param     string $motivo The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return FacturasQuery The current query, for fluid interface
     */
    public function filterByMotivo($motivo = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($motivo)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $motivo)) {
                $motivo = str_replace('*', '%', $motivo);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(FacturasPeer::MOTIVO, $motivo, $comparison);
    }

    /**
     * Exclude object from result
     *
     * @param   Facturas $facturas Object to remove from the list of results
     *
     * @return FacturasQuery The current query, for fluid interface
     */
    public function prune($facturas = null)
    {
        if ($facturas) {
            $this->addUsingAlias(FacturasPeer::ID, $facturas->getId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}
