<?php


/**
 * Base class that represents a query for the 'empresa' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.6.7 on:
 *
 * 12/30/25 15:40:21
 *
 * @method EmpresaQuery orderById($order = Criteria::ASC) Order by the id column
 * @method EmpresaQuery orderByCodigo($order = Criteria::ASC) Order by the codigo column
 * @method EmpresaQuery orderByNombre($order = Criteria::ASC) Order by the nombre column
 * @method EmpresaQuery orderByNomenclatura($order = Criteria::ASC) Order by the nomenclatura column
 * @method EmpresaQuery orderByTelefono($order = Criteria::ASC) Order by the telefono column
 * @method EmpresaQuery orderByDepartamentoId($order = Criteria::ASC) Order by the departamento_id column
 * @method EmpresaQuery orderByMunicipioId($order = Criteria::ASC) Order by the municipio_id column
 * @method EmpresaQuery orderByDireccion($order = Criteria::ASC) Order by the direccion column
 * @method EmpresaQuery orderByMapaGeo($order = Criteria::ASC) Order by the mapa_geo column
 * @method EmpresaQuery orderByLogo($order = Criteria::ASC) Order by the logo column
 * @method EmpresaQuery orderByContactoNombre($order = Criteria::ASC) Order by the contacto_nombre column
 * @method EmpresaQuery orderByContactoTelefono($order = Criteria::ASC) Order by the contacto_telefono column
 * @method EmpresaQuery orderByContactoMovil($order = Criteria::ASC) Order by the contacto_movil column
 * @method EmpresaQuery orderByObservaciones($order = Criteria::ASC) Order by the observaciones column
 * @method EmpresaQuery orderByFacturaElectronica($order = Criteria::ASC) Order by the factura_electronica column
 * @method EmpresaQuery orderByContactoCorreo($order = Criteria::ASC) Order by the contacto_correo column
 * @method EmpresaQuery orderByFeelNombre($order = Criteria::ASC) Order by the feel_nombre column
 * @method EmpresaQuery orderByFeelUsuario($order = Criteria::ASC) Order by the feel_usuario column
 * @method EmpresaQuery orderByFeelToken($order = Criteria::ASC) Order by the feel_token column
 * @method EmpresaQuery orderByFeelLlave($order = Criteria::ASC) Order by the feel_llave column
 * @method EmpresaQuery orderByFeelEscenarioFrase($order = Criteria::ASC) Order by the feel_escenario_frase column
 * @method EmpresaQuery orderByDiasCredito($order = Criteria::ASC) Order by the dias_credito column
 * @method EmpresaQuery orderByRetieneIsr($order = Criteria::ASC) Order by the retiene_isr column
 * @method EmpresaQuery orderByMonedaQ($order = Criteria::ASC) Order by the moneda_q column
 *
 * @method EmpresaQuery groupById() Group by the id column
 * @method EmpresaQuery groupByCodigo() Group by the codigo column
 * @method EmpresaQuery groupByNombre() Group by the nombre column
 * @method EmpresaQuery groupByNomenclatura() Group by the nomenclatura column
 * @method EmpresaQuery groupByTelefono() Group by the telefono column
 * @method EmpresaQuery groupByDepartamentoId() Group by the departamento_id column
 * @method EmpresaQuery groupByMunicipioId() Group by the municipio_id column
 * @method EmpresaQuery groupByDireccion() Group by the direccion column
 * @method EmpresaQuery groupByMapaGeo() Group by the mapa_geo column
 * @method EmpresaQuery groupByLogo() Group by the logo column
 * @method EmpresaQuery groupByContactoNombre() Group by the contacto_nombre column
 * @method EmpresaQuery groupByContactoTelefono() Group by the contacto_telefono column
 * @method EmpresaQuery groupByContactoMovil() Group by the contacto_movil column
 * @method EmpresaQuery groupByObservaciones() Group by the observaciones column
 * @method EmpresaQuery groupByFacturaElectronica() Group by the factura_electronica column
 * @method EmpresaQuery groupByContactoCorreo() Group by the contacto_correo column
 * @method EmpresaQuery groupByFeelNombre() Group by the feel_nombre column
 * @method EmpresaQuery groupByFeelUsuario() Group by the feel_usuario column
 * @method EmpresaQuery groupByFeelToken() Group by the feel_token column
 * @method EmpresaQuery groupByFeelLlave() Group by the feel_llave column
 * @method EmpresaQuery groupByFeelEscenarioFrase() Group by the feel_escenario_frase column
 * @method EmpresaQuery groupByDiasCredito() Group by the dias_credito column
 * @method EmpresaQuery groupByRetieneIsr() Group by the retiene_isr column
 * @method EmpresaQuery groupByMonedaQ() Group by the moneda_q column
 *
 * @method EmpresaQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method EmpresaQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method EmpresaQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method EmpresaQuery leftJoinDepartamento($relationAlias = null) Adds a LEFT JOIN clause to the query using the Departamento relation
 * @method EmpresaQuery rightJoinDepartamento($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Departamento relation
 * @method EmpresaQuery innerJoinDepartamento($relationAlias = null) Adds a INNER JOIN clause to the query using the Departamento relation
 *
 * @method EmpresaQuery leftJoinMunicipio($relationAlias = null) Adds a LEFT JOIN clause to the query using the Municipio relation
 * @method EmpresaQuery rightJoinMunicipio($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Municipio relation
 * @method EmpresaQuery innerJoinMunicipio($relationAlias = null) Adds a INNER JOIN clause to the query using the Municipio relation
 *
 * @method EmpresaQuery leftJoinUsuario($relationAlias = null) Adds a LEFT JOIN clause to the query using the Usuario relation
 * @method EmpresaQuery rightJoinUsuario($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Usuario relation
 * @method EmpresaQuery innerJoinUsuario($relationAlias = null) Adds a INNER JOIN clause to the query using the Usuario relation
 *
 * @method EmpresaQuery leftJoinUsuarioEmpresa($relationAlias = null) Adds a LEFT JOIN clause to the query using the UsuarioEmpresa relation
 * @method EmpresaQuery rightJoinUsuarioEmpresa($relationAlias = null) Adds a RIGHT JOIN clause to the query using the UsuarioEmpresa relation
 * @method EmpresaQuery innerJoinUsuarioEmpresa($relationAlias = null) Adds a INNER JOIN clause to the query using the UsuarioEmpresa relation
 *
 * @method EmpresaQuery leftJoinBanco($relationAlias = null) Adds a LEFT JOIN clause to the query using the Banco relation
 * @method EmpresaQuery rightJoinBanco($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Banco relation
 * @method EmpresaQuery innerJoinBanco($relationAlias = null) Adds a INNER JOIN clause to the query using the Banco relation
 *
 * @method EmpresaQuery leftJoinBancoTemporal($relationAlias = null) Adds a LEFT JOIN clause to the query using the BancoTemporal relation
 * @method EmpresaQuery rightJoinBancoTemporal($relationAlias = null) Adds a RIGHT JOIN clause to the query using the BancoTemporal relation
 * @method EmpresaQuery innerJoinBancoTemporal($relationAlias = null) Adds a INNER JOIN clause to the query using the BancoTemporal relation
 *
 * @method EmpresaQuery leftJoinOperacion($relationAlias = null) Adds a LEFT JOIN clause to the query using the Operacion relation
 * @method EmpresaQuery rightJoinOperacion($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Operacion relation
 * @method EmpresaQuery innerJoinOperacion($relationAlias = null) Adds a INNER JOIN clause to the query using the Operacion relation
 *
 * @method EmpresaQuery leftJoinOperacionDetalle($relationAlias = null) Adds a LEFT JOIN clause to the query using the OperacionDetalle relation
 * @method EmpresaQuery rightJoinOperacionDetalle($relationAlias = null) Adds a RIGHT JOIN clause to the query using the OperacionDetalle relation
 * @method EmpresaQuery innerJoinOperacionDetalle($relationAlias = null) Adds a INNER JOIN clause to the query using the OperacionDetalle relation
 *
 * @method EmpresaQuery leftJoinOperacionPago($relationAlias = null) Adds a LEFT JOIN clause to the query using the OperacionPago relation
 * @method EmpresaQuery rightJoinOperacionPago($relationAlias = null) Adds a RIGHT JOIN clause to the query using the OperacionPago relation
 * @method EmpresaQuery innerJoinOperacionPago($relationAlias = null) Adds a INNER JOIN clause to the query using the OperacionPago relation
 *
 * @method EmpresaQuery leftJoinProveedor($relationAlias = null) Adds a LEFT JOIN clause to the query using the Proveedor relation
 * @method EmpresaQuery rightJoinProveedor($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Proveedor relation
 * @method EmpresaQuery innerJoinProveedor($relationAlias = null) Adds a INNER JOIN clause to the query using the Proveedor relation
 *
 * @method EmpresaQuery leftJoinTipoServicio($relationAlias = null) Adds a LEFT JOIN clause to the query using the TipoServicio relation
 * @method EmpresaQuery rightJoinTipoServicio($relationAlias = null) Adds a RIGHT JOIN clause to the query using the TipoServicio relation
 * @method EmpresaQuery innerJoinTipoServicio($relationAlias = null) Adds a INNER JOIN clause to the query using the TipoServicio relation
 *
 * @method EmpresaQuery leftJoinOrdenProveedor($relationAlias = null) Adds a LEFT JOIN clause to the query using the OrdenProveedor relation
 * @method EmpresaQuery rightJoinOrdenProveedor($relationAlias = null) Adds a RIGHT JOIN clause to the query using the OrdenProveedor relation
 * @method EmpresaQuery innerJoinOrdenProveedor($relationAlias = null) Adds a INNER JOIN clause to the query using the OrdenProveedor relation
 *
 * @method EmpresaQuery leftJoinCuentaProveedor($relationAlias = null) Adds a LEFT JOIN clause to the query using the CuentaProveedor relation
 * @method EmpresaQuery rightJoinCuentaProveedor($relationAlias = null) Adds a RIGHT JOIN clause to the query using the CuentaProveedor relation
 * @method EmpresaQuery innerJoinCuentaProveedor($relationAlias = null) Adds a INNER JOIN clause to the query using the CuentaProveedor relation
 *
 * @method EmpresaQuery leftJoinContrasenaCrea($relationAlias = null) Adds a LEFT JOIN clause to the query using the ContrasenaCrea relation
 * @method EmpresaQuery rightJoinContrasenaCrea($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ContrasenaCrea relation
 * @method EmpresaQuery innerJoinContrasenaCrea($relationAlias = null) Adds a INNER JOIN clause to the query using the ContrasenaCrea relation
 *
 * @method EmpresaQuery leftJoinOrdenProveedorPago($relationAlias = null) Adds a LEFT JOIN clause to the query using the OrdenProveedorPago relation
 * @method EmpresaQuery rightJoinOrdenProveedorPago($relationAlias = null) Adds a RIGHT JOIN clause to the query using the OrdenProveedorPago relation
 * @method EmpresaQuery innerJoinOrdenProveedorPago($relationAlias = null) Adds a INNER JOIN clause to the query using the OrdenProveedorPago relation
 *
 * @method EmpresaQuery leftJoinBitacoraArchivo($relationAlias = null) Adds a LEFT JOIN clause to the query using the BitacoraArchivo relation
 * @method EmpresaQuery rightJoinBitacoraArchivo($relationAlias = null) Adds a RIGHT JOIN clause to the query using the BitacoraArchivo relation
 * @method EmpresaQuery innerJoinBitacoraArchivo($relationAlias = null) Adds a INNER JOIN clause to the query using the BitacoraArchivo relation
 *
 * @method EmpresaQuery leftJoinBitacoraElimino($relationAlias = null) Adds a LEFT JOIN clause to the query using the BitacoraElimino relation
 * @method EmpresaQuery rightJoinBitacoraElimino($relationAlias = null) Adds a RIGHT JOIN clause to the query using the BitacoraElimino relation
 * @method EmpresaQuery innerJoinBitacoraElimino($relationAlias = null) Adds a INNER JOIN clause to the query using the BitacoraElimino relation
 *
 * @method EmpresaQuery leftJoinMedioPago($relationAlias = null) Adds a LEFT JOIN clause to the query using the MedioPago relation
 * @method EmpresaQuery rightJoinMedioPago($relationAlias = null) Adds a RIGHT JOIN clause to the query using the MedioPago relation
 * @method EmpresaQuery innerJoinMedioPago($relationAlias = null) Adds a INNER JOIN clause to the query using the MedioPago relation
 *
 * @method EmpresaQuery leftJoinTienda($relationAlias = null) Adds a LEFT JOIN clause to the query using the Tienda relation
 * @method EmpresaQuery rightJoinTienda($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Tienda relation
 * @method EmpresaQuery innerJoinTienda($relationAlias = null) Adds a INNER JOIN clause to the query using the Tienda relation
 *
 * @method EmpresaQuery leftJoinMovimientoBanco($relationAlias = null) Adds a LEFT JOIN clause to the query using the MovimientoBanco relation
 * @method EmpresaQuery rightJoinMovimientoBanco($relationAlias = null) Adds a RIGHT JOIN clause to the query using the MovimientoBanco relation
 * @method EmpresaQuery innerJoinMovimientoBanco($relationAlias = null) Adds a INNER JOIN clause to the query using the MovimientoBanco relation
 *
 * @method EmpresaQuery leftJoinPartida($relationAlias = null) Adds a LEFT JOIN clause to the query using the Partida relation
 * @method EmpresaQuery rightJoinPartida($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Partida relation
 * @method EmpresaQuery innerJoinPartida($relationAlias = null) Adds a INNER JOIN clause to the query using the Partida relation
 *
 * @method EmpresaQuery leftJoinPartidaDetalle($relationAlias = null) Adds a LEFT JOIN clause to the query using the PartidaDetalle relation
 * @method EmpresaQuery rightJoinPartidaDetalle($relationAlias = null) Adds a RIGHT JOIN clause to the query using the PartidaDetalle relation
 * @method EmpresaQuery innerJoinPartidaDetalle($relationAlias = null) Adds a INNER JOIN clause to the query using the PartidaDetalle relation
 *
 * @method EmpresaQuery leftJoinPartidaAgrupa($relationAlias = null) Adds a LEFT JOIN clause to the query using the PartidaAgrupa relation
 * @method EmpresaQuery rightJoinPartidaAgrupa($relationAlias = null) Adds a RIGHT JOIN clause to the query using the PartidaAgrupa relation
 * @method EmpresaQuery innerJoinPartidaAgrupa($relationAlias = null) Adds a INNER JOIN clause to the query using the PartidaAgrupa relation
 *
 * @method EmpresaQuery leftJoinPartidaAgrupaLinea($relationAlias = null) Adds a LEFT JOIN clause to the query using the PartidaAgrupaLinea relation
 * @method EmpresaQuery rightJoinPartidaAgrupaLinea($relationAlias = null) Adds a RIGHT JOIN clause to the query using the PartidaAgrupaLinea relation
 * @method EmpresaQuery innerJoinPartidaAgrupaLinea($relationAlias = null) Adds a INNER JOIN clause to the query using the PartidaAgrupaLinea relation
 *
 * @method EmpresaQuery leftJoinCuentaErpContable($relationAlias = null) Adds a LEFT JOIN clause to the query using the CuentaErpContable relation
 * @method EmpresaQuery rightJoinCuentaErpContable($relationAlias = null) Adds a RIGHT JOIN clause to the query using the CuentaErpContable relation
 * @method EmpresaQuery innerJoinCuentaErpContable($relationAlias = null) Adds a INNER JOIN clause to the query using the CuentaErpContable relation
 *
 * @method EmpresaQuery leftJoinTipoAparato($relationAlias = null) Adds a LEFT JOIN clause to the query using the TipoAparato relation
 * @method EmpresaQuery rightJoinTipoAparato($relationAlias = null) Adds a RIGHT JOIN clause to the query using the TipoAparato relation
 * @method EmpresaQuery innerJoinTipoAparato($relationAlias = null) Adds a INNER JOIN clause to the query using the TipoAparato relation
 *
 * @method EmpresaQuery leftJoinMarcaProducto($relationAlias = null) Adds a LEFT JOIN clause to the query using the MarcaProducto relation
 * @method EmpresaQuery rightJoinMarcaProducto($relationAlias = null) Adds a RIGHT JOIN clause to the query using the MarcaProducto relation
 * @method EmpresaQuery innerJoinMarcaProducto($relationAlias = null) Adds a INNER JOIN clause to the query using the MarcaProducto relation
 *
 * @method EmpresaQuery leftJoinMarca($relationAlias = null) Adds a LEFT JOIN clause to the query using the Marca relation
 * @method EmpresaQuery rightJoinMarca($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Marca relation
 * @method EmpresaQuery innerJoinMarca($relationAlias = null) Adds a INNER JOIN clause to the query using the Marca relation
 *
 * @method EmpresaQuery leftJoinModelo($relationAlias = null) Adds a LEFT JOIN clause to the query using the Modelo relation
 * @method EmpresaQuery rightJoinModelo($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Modelo relation
 * @method EmpresaQuery innerJoinModelo($relationAlias = null) Adds a INNER JOIN clause to the query using the Modelo relation
 *
 * @method EmpresaQuery leftJoinProducto($relationAlias = null) Adds a LEFT JOIN clause to the query using the Producto relation
 * @method EmpresaQuery rightJoinProducto($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Producto relation
 * @method EmpresaQuery innerJoinProducto($relationAlias = null) Adds a INNER JOIN clause to the query using the Producto relation
 *
 * @method EmpresaQuery leftJoinRecetaProducto($relationAlias = null) Adds a LEFT JOIN clause to the query using the RecetaProducto relation
 * @method EmpresaQuery rightJoinRecetaProducto($relationAlias = null) Adds a RIGHT JOIN clause to the query using the RecetaProducto relation
 * @method EmpresaQuery innerJoinRecetaProducto($relationAlias = null) Adds a INNER JOIN clause to the query using the RecetaProducto relation
 *
 * @method EmpresaQuery leftJoinRecetaProductoDetalle($relationAlias = null) Adds a LEFT JOIN clause to the query using the RecetaProductoDetalle relation
 * @method EmpresaQuery rightJoinRecetaProductoDetalle($relationAlias = null) Adds a RIGHT JOIN clause to the query using the RecetaProductoDetalle relation
 * @method EmpresaQuery innerJoinRecetaProductoDetalle($relationAlias = null) Adds a INNER JOIN clause to the query using the RecetaProductoDetalle relation
 *
 * @method EmpresaQuery leftJoinComboProducto($relationAlias = null) Adds a LEFT JOIN clause to the query using the ComboProducto relation
 * @method EmpresaQuery rightJoinComboProducto($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ComboProducto relation
 * @method EmpresaQuery innerJoinComboProducto($relationAlias = null) Adds a INNER JOIN clause to the query using the ComboProducto relation
 *
 * @method EmpresaQuery leftJoinComboProductoDetalle($relationAlias = null) Adds a LEFT JOIN clause to the query using the ComboProductoDetalle relation
 * @method EmpresaQuery rightJoinComboProductoDetalle($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ComboProductoDetalle relation
 * @method EmpresaQuery innerJoinComboProductoDetalle($relationAlias = null) Adds a INNER JOIN clause to the query using the ComboProductoDetalle relation
 *
 * @method EmpresaQuery leftJoinListaComboDetalle($relationAlias = null) Adds a LEFT JOIN clause to the query using the ListaComboDetalle relation
 * @method EmpresaQuery rightJoinListaComboDetalle($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ListaComboDetalle relation
 * @method EmpresaQuery innerJoinListaComboDetalle($relationAlias = null) Adds a INNER JOIN clause to the query using the ListaComboDetalle relation
 *
 * @method EmpresaQuery leftJoinListaRecetaDetalle($relationAlias = null) Adds a LEFT JOIN clause to the query using the ListaRecetaDetalle relation
 * @method EmpresaQuery rightJoinListaRecetaDetalle($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ListaRecetaDetalle relation
 * @method EmpresaQuery innerJoinListaRecetaDetalle($relationAlias = null) Adds a INNER JOIN clause to the query using the ListaRecetaDetalle relation
 *
 * @method EmpresaQuery leftJoinIngresoProductoPro($relationAlias = null) Adds a LEFT JOIN clause to the query using the IngresoProductoPro relation
 * @method EmpresaQuery rightJoinIngresoProductoPro($relationAlias = null) Adds a RIGHT JOIN clause to the query using the IngresoProductoPro relation
 * @method EmpresaQuery innerJoinIngresoProductoPro($relationAlias = null) Adds a INNER JOIN clause to the query using the IngresoProductoPro relation
 *
 * @method EmpresaQuery leftJoinIngresoProductoDetallePro($relationAlias = null) Adds a LEFT JOIN clause to the query using the IngresoProductoDetallePro relation
 * @method EmpresaQuery rightJoinIngresoProductoDetallePro($relationAlias = null) Adds a RIGHT JOIN clause to the query using the IngresoProductoDetallePro relation
 * @method EmpresaQuery innerJoinIngresoProductoDetallePro($relationAlias = null) Adds a INNER JOIN clause to the query using the IngresoProductoDetallePro relation
 *
 * @method EmpresaQuery leftJoinSalidaProducto($relationAlias = null) Adds a LEFT JOIN clause to the query using the SalidaProducto relation
 * @method EmpresaQuery rightJoinSalidaProducto($relationAlias = null) Adds a RIGHT JOIN clause to the query using the SalidaProducto relation
 * @method EmpresaQuery innerJoinSalidaProducto($relationAlias = null) Adds a INNER JOIN clause to the query using the SalidaProducto relation
 *
 * @method EmpresaQuery leftJoinSalidaProductoDetalle($relationAlias = null) Adds a LEFT JOIN clause to the query using the SalidaProductoDetalle relation
 * @method EmpresaQuery rightJoinSalidaProductoDetalle($relationAlias = null) Adds a RIGHT JOIN clause to the query using the SalidaProductoDetalle relation
 * @method EmpresaQuery innerJoinSalidaProductoDetalle($relationAlias = null) Adds a INNER JOIN clause to the query using the SalidaProductoDetalle relation
 *
 * @method EmpresaQuery leftJoinMotivoMovimientoProducto($relationAlias = null) Adds a LEFT JOIN clause to the query using the MotivoMovimientoProducto relation
 * @method EmpresaQuery rightJoinMotivoMovimientoProducto($relationAlias = null) Adds a RIGHT JOIN clause to the query using the MotivoMovimientoProducto relation
 * @method EmpresaQuery innerJoinMotivoMovimientoProducto($relationAlias = null) Adds a INNER JOIN clause to the query using the MotivoMovimientoProducto relation
 *
 * @method EmpresaQuery leftJoinCliente($relationAlias = null) Adds a LEFT JOIN clause to the query using the Cliente relation
 * @method EmpresaQuery rightJoinCliente($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Cliente relation
 * @method EmpresaQuery innerJoinCliente($relationAlias = null) Adds a INNER JOIN clause to the query using the Cliente relation
 *
 * @method EmpresaQuery leftJoinCierreCaja($relationAlias = null) Adds a LEFT JOIN clause to the query using the CierreCaja relation
 * @method EmpresaQuery rightJoinCierreCaja($relationAlias = null) Adds a RIGHT JOIN clause to the query using the CierreCaja relation
 * @method EmpresaQuery innerJoinCierreCaja($relationAlias = null) Adds a INNER JOIN clause to the query using the CierreCaja relation
 *
 * @method EmpresaQuery leftJoinCierre($relationAlias = null) Adds a LEFT JOIN clause to the query using the Cierre relation
 * @method EmpresaQuery rightJoinCierre($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Cierre relation
 * @method EmpresaQuery innerJoinCierre($relationAlias = null) Adds a INNER JOIN clause to the query using the Cierre relation
 *
 * @method EmpresaQuery leftJoinServicio($relationAlias = null) Adds a LEFT JOIN clause to the query using the Servicio relation
 * @method EmpresaQuery rightJoinServicio($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Servicio relation
 * @method EmpresaQuery innerJoinServicio($relationAlias = null) Adds a INNER JOIN clause to the query using the Servicio relation
 *
 * @method EmpresaQuery leftJoinCampoUsuario($relationAlias = null) Adds a LEFT JOIN clause to the query using the CampoUsuario relation
 * @method EmpresaQuery rightJoinCampoUsuario($relationAlias = null) Adds a RIGHT JOIN clause to the query using the CampoUsuario relation
 * @method EmpresaQuery innerJoinCampoUsuario($relationAlias = null) Adds a INNER JOIN clause to the query using the CampoUsuario relation
 *
 * @method EmpresaQuery leftJoinValorUsuario($relationAlias = null) Adds a LEFT JOIN clause to the query using the ValorUsuario relation
 * @method EmpresaQuery rightJoinValorUsuario($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ValorUsuario relation
 * @method EmpresaQuery innerJoinValorUsuario($relationAlias = null) Adds a INNER JOIN clause to the query using the ValorUsuario relation
 *
 * @method EmpresaQuery leftJoinProyecto($relationAlias = null) Adds a LEFT JOIN clause to the query using the Proyecto relation
 * @method EmpresaQuery rightJoinProyecto($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Proyecto relation
 * @method EmpresaQuery innerJoinProyecto($relationAlias = null) Adds a INNER JOIN clause to the query using the Proyecto relation
 *
 * @method EmpresaQuery leftJoinGasto($relationAlias = null) Adds a LEFT JOIN clause to the query using the Gasto relation
 * @method EmpresaQuery rightJoinGasto($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Gasto relation
 * @method EmpresaQuery innerJoinGasto($relationAlias = null) Adds a INNER JOIN clause to the query using the Gasto relation
 *
 * @method EmpresaQuery leftJoinGastoPago($relationAlias = null) Adds a LEFT JOIN clause to the query using the GastoPago relation
 * @method EmpresaQuery rightJoinGastoPago($relationAlias = null) Adds a RIGHT JOIN clause to the query using the GastoPago relation
 * @method EmpresaQuery innerJoinGastoPago($relationAlias = null) Adds a INNER JOIN clause to the query using the GastoPago relation
 *
 * @method EmpresaQuery leftJoinParametroCuenta($relationAlias = null) Adds a LEFT JOIN clause to the query using the ParametroCuenta relation
 * @method EmpresaQuery rightJoinParametroCuenta($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ParametroCuenta relation
 * @method EmpresaQuery innerJoinParametroCuenta($relationAlias = null) Adds a INNER JOIN clause to the query using the ParametroCuenta relation
 *
 * @method EmpresaQuery leftJoinProductoExistencia($relationAlias = null) Adds a LEFT JOIN clause to the query using the ProductoExistencia relation
 * @method EmpresaQuery rightJoinProductoExistencia($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ProductoExistencia relation
 * @method EmpresaQuery innerJoinProductoExistencia($relationAlias = null) Adds a INNER JOIN clause to the query using the ProductoExistencia relation
 *
 * @method EmpresaQuery leftJoinProductoUbicacion($relationAlias = null) Adds a LEFT JOIN clause to the query using the ProductoUbicacion relation
 * @method EmpresaQuery rightJoinProductoUbicacion($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ProductoUbicacion relation
 * @method EmpresaQuery innerJoinProductoUbicacion($relationAlias = null) Adds a INNER JOIN clause to the query using the ProductoUbicacion relation
 *
 * @method EmpresaQuery leftJoinProductoPrecio($relationAlias = null) Adds a LEFT JOIN clause to the query using the ProductoPrecio relation
 * @method EmpresaQuery rightJoinProductoPrecio($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ProductoPrecio relation
 * @method EmpresaQuery innerJoinProductoPrecio($relationAlias = null) Adds a INNER JOIN clause to the query using the ProductoPrecio relation
 *
 * @method EmpresaQuery leftJoinListaPrecio($relationAlias = null) Adds a LEFT JOIN clause to the query using the ListaPrecio relation
 * @method EmpresaQuery rightJoinListaPrecio($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ListaPrecio relation
 * @method EmpresaQuery innerJoinListaPrecio($relationAlias = null) Adds a INNER JOIN clause to the query using the ListaPrecio relation
 *
 * @method EmpresaQuery leftJoinIngresoProducto($relationAlias = null) Adds a LEFT JOIN clause to the query using the IngresoProducto relation
 * @method EmpresaQuery rightJoinIngresoProducto($relationAlias = null) Adds a RIGHT JOIN clause to the query using the IngresoProducto relation
 * @method EmpresaQuery innerJoinIngresoProducto($relationAlias = null) Adds a INNER JOIN clause to the query using the IngresoProducto relation
 *
 * @method EmpresaQuery leftJoinIngresoProductoDetalle($relationAlias = null) Adds a LEFT JOIN clause to the query using the IngresoProductoDetalle relation
 * @method EmpresaQuery rightJoinIngresoProductoDetalle($relationAlias = null) Adds a RIGHT JOIN clause to the query using the IngresoProductoDetalle relation
 * @method EmpresaQuery innerJoinIngresoProductoDetalle($relationAlias = null) Adds a INNER JOIN clause to the query using the IngresoProductoDetalle relation
 *
 * @method EmpresaQuery leftJoinProductoMovimiento($relationAlias = null) Adds a LEFT JOIN clause to the query using the ProductoMovimiento relation
 * @method EmpresaQuery rightJoinProductoMovimiento($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ProductoMovimiento relation
 * @method EmpresaQuery innerJoinProductoMovimiento($relationAlias = null) Adds a INNER JOIN clause to the query using the ProductoMovimiento relation
 *
 * @method EmpresaQuery leftJoinPrecioProducto($relationAlias = null) Adds a LEFT JOIN clause to the query using the PrecioProducto relation
 * @method EmpresaQuery rightJoinPrecioProducto($relationAlias = null) Adds a RIGHT JOIN clause to the query using the PrecioProducto relation
 * @method EmpresaQuery innerJoinPrecioProducto($relationAlias = null) Adds a INNER JOIN clause to the query using the PrecioProducto relation
 *
 * @method EmpresaQuery leftJoinPrecioProductoDetalle($relationAlias = null) Adds a LEFT JOIN clause to the query using the PrecioProductoDetalle relation
 * @method EmpresaQuery rightJoinPrecioProductoDetalle($relationAlias = null) Adds a RIGHT JOIN clause to the query using the PrecioProductoDetalle relation
 * @method EmpresaQuery innerJoinPrecioProductoDetalle($relationAlias = null) Adds a INNER JOIN clause to the query using the PrecioProductoDetalle relation
 *
 * @method EmpresaQuery leftJoinOrdenCotizacion($relationAlias = null) Adds a LEFT JOIN clause to the query using the OrdenCotizacion relation
 * @method EmpresaQuery rightJoinOrdenCotizacion($relationAlias = null) Adds a RIGHT JOIN clause to the query using the OrdenCotizacion relation
 * @method EmpresaQuery innerJoinOrdenCotizacion($relationAlias = null) Adds a INNER JOIN clause to the query using the OrdenCotizacion relation
 *
 * @method EmpresaQuery leftJoinMoneda($relationAlias = null) Adds a LEFT JOIN clause to the query using the Moneda relation
 * @method EmpresaQuery rightJoinMoneda($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Moneda relation
 * @method EmpresaQuery innerJoinMoneda($relationAlias = null) Adds a INNER JOIN clause to the query using the Moneda relation
 *
 * @method EmpresaQuery leftJoinBancoSaldo($relationAlias = null) Adds a LEFT JOIN clause to the query using the BancoSaldo relation
 * @method EmpresaQuery rightJoinBancoSaldo($relationAlias = null) Adds a RIGHT JOIN clause to the query using the BancoSaldo relation
 * @method EmpresaQuery innerJoinBancoSaldo($relationAlias = null) Adds a INNER JOIN clause to the query using the BancoSaldo relation
 *
 * @method EmpresaQuery leftJoinNotaCredito($relationAlias = null) Adds a LEFT JOIN clause to the query using the NotaCredito relation
 * @method EmpresaQuery rightJoinNotaCredito($relationAlias = null) Adds a RIGHT JOIN clause to the query using the NotaCredito relation
 * @method EmpresaQuery innerJoinNotaCredito($relationAlias = null) Adds a INNER JOIN clause to the query using the NotaCredito relation
 *
 * @method EmpresaQuery leftJoinDefinicionCuenta($relationAlias = null) Adds a LEFT JOIN clause to the query using the DefinicionCuenta relation
 * @method EmpresaQuery rightJoinDefinicionCuenta($relationAlias = null) Adds a RIGHT JOIN clause to the query using the DefinicionCuenta relation
 * @method EmpresaQuery innerJoinDefinicionCuenta($relationAlias = null) Adds a INNER JOIN clause to the query using the DefinicionCuenta relation
 *
 * @method EmpresaQuery leftJoinDocumentoCheque($relationAlias = null) Adds a LEFT JOIN clause to the query using the DocumentoCheque relation
 * @method EmpresaQuery rightJoinDocumentoCheque($relationAlias = null) Adds a RIGHT JOIN clause to the query using the DocumentoCheque relation
 * @method EmpresaQuery innerJoinDocumentoCheque($relationAlias = null) Adds a INNER JOIN clause to the query using the DocumentoCheque relation
 *
 * @method EmpresaQuery leftJoinCuentaBanco($relationAlias = null) Adds a LEFT JOIN clause to the query using the CuentaBanco relation
 * @method EmpresaQuery rightJoinCuentaBanco($relationAlias = null) Adds a RIGHT JOIN clause to the query using the CuentaBanco relation
 * @method EmpresaQuery innerJoinCuentaBanco($relationAlias = null) Adds a INNER JOIN clause to the query using the CuentaBanco relation
 *
 * @method EmpresaQuery leftJoinVentaResumida($relationAlias = null) Adds a LEFT JOIN clause to the query using the VentaResumida relation
 * @method EmpresaQuery rightJoinVentaResumida($relationAlias = null) Adds a RIGHT JOIN clause to the query using the VentaResumida relation
 * @method EmpresaQuery innerJoinVentaResumida($relationAlias = null) Adds a INNER JOIN clause to the query using the VentaResumida relation
 *
 * @method EmpresaQuery leftJoinVentaResumidaLinea($relationAlias = null) Adds a LEFT JOIN clause to the query using the VentaResumidaLinea relation
 * @method EmpresaQuery rightJoinVentaResumidaLinea($relationAlias = null) Adds a RIGHT JOIN clause to the query using the VentaResumidaLinea relation
 * @method EmpresaQuery innerJoinVentaResumidaLinea($relationAlias = null) Adds a INNER JOIN clause to the query using the VentaResumidaLinea relation
 *
 * @method EmpresaQuery leftJoinOrdenDevolucion($relationAlias = null) Adds a LEFT JOIN clause to the query using the OrdenDevolucion relation
 * @method EmpresaQuery rightJoinOrdenDevolucion($relationAlias = null) Adds a RIGHT JOIN clause to the query using the OrdenDevolucion relation
 * @method EmpresaQuery innerJoinOrdenDevolucion($relationAlias = null) Adds a INNER JOIN clause to the query using the OrdenDevolucion relation
 *
 * @method EmpresaQuery leftJoinEgresosRecibido($relationAlias = null) Adds a LEFT JOIN clause to the query using the EgresosRecibido relation
 * @method EmpresaQuery rightJoinEgresosRecibido($relationAlias = null) Adds a RIGHT JOIN clause to the query using the EgresosRecibido relation
 * @method EmpresaQuery innerJoinEgresosRecibido($relationAlias = null) Adds a INNER JOIN clause to the query using the EgresosRecibido relation
 *
 * @method EmpresaQuery leftJoinGastoCaja($relationAlias = null) Adds a LEFT JOIN clause to the query using the GastoCaja relation
 * @method EmpresaQuery rightJoinGastoCaja($relationAlias = null) Adds a RIGHT JOIN clause to the query using the GastoCaja relation
 * @method EmpresaQuery innerJoinGastoCaja($relationAlias = null) Adds a INNER JOIN clause to the query using the GastoCaja relation
 *
 * @method EmpresaQuery leftJoinVendedor($relationAlias = null) Adds a LEFT JOIN clause to the query using the Vendedor relation
 * @method EmpresaQuery rightJoinVendedor($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Vendedor relation
 * @method EmpresaQuery innerJoinVendedor($relationAlias = null) Adds a INNER JOIN clause to the query using the Vendedor relation
 *
 * @method EmpresaQuery leftJoinOrdenVendedor($relationAlias = null) Adds a LEFT JOIN clause to the query using the OrdenVendedor relation
 * @method EmpresaQuery rightJoinOrdenVendedor($relationAlias = null) Adds a RIGHT JOIN clause to the query using the OrdenVendedor relation
 * @method EmpresaQuery innerJoinOrdenVendedor($relationAlias = null) Adds a INNER JOIN clause to the query using the OrdenVendedor relation
 *
 * @method EmpresaQuery leftJoinSolicitudDevolucion($relationAlias = null) Adds a LEFT JOIN clause to the query using the SolicitudDevolucion relation
 * @method EmpresaQuery rightJoinSolicitudDevolucion($relationAlias = null) Adds a RIGHT JOIN clause to the query using the SolicitudDevolucion relation
 * @method EmpresaQuery innerJoinSolicitudDevolucion($relationAlias = null) Adds a INNER JOIN clause to the query using the SolicitudDevolucion relation
 *
 * @method EmpresaQuery leftJoinSolicitudDevMotivo($relationAlias = null) Adds a LEFT JOIN clause to the query using the SolicitudDevMotivo relation
 * @method EmpresaQuery rightJoinSolicitudDevMotivo($relationAlias = null) Adds a RIGHT JOIN clause to the query using the SolicitudDevMotivo relation
 * @method EmpresaQuery innerJoinSolicitudDevMotivo($relationAlias = null) Adds a INNER JOIN clause to the query using the SolicitudDevMotivo relation
 *
 * @method EmpresaQuery leftJoinSolicitudCheque($relationAlias = null) Adds a LEFT JOIN clause to the query using the SolicitudCheque relation
 * @method EmpresaQuery rightJoinSolicitudCheque($relationAlias = null) Adds a RIGHT JOIN clause to the query using the SolicitudCheque relation
 * @method EmpresaQuery innerJoinSolicitudCheque($relationAlias = null) Adds a INNER JOIN clause to the query using the SolicitudCheque relation
 *
 * @method EmpresaQuery leftJoinLibroAgrupado($relationAlias = null) Adds a LEFT JOIN clause to the query using the LibroAgrupado relation
 * @method EmpresaQuery rightJoinLibroAgrupado($relationAlias = null) Adds a RIGHT JOIN clause to the query using the LibroAgrupado relation
 * @method EmpresaQuery innerJoinLibroAgrupado($relationAlias = null) Adds a INNER JOIN clause to the query using the LibroAgrupado relation
 *
 * @method EmpresaQuery leftJoinLibroAgrupadoDetalle($relationAlias = null) Adds a LEFT JOIN clause to the query using the LibroAgrupadoDetalle relation
 * @method EmpresaQuery rightJoinLibroAgrupadoDetalle($relationAlias = null) Adds a RIGHT JOIN clause to the query using the LibroAgrupadoDetalle relation
 * @method EmpresaQuery innerJoinLibroAgrupadoDetalle($relationAlias = null) Adds a INNER JOIN clause to the query using the LibroAgrupadoDetalle relation
 *
 * @method EmpresaQuery leftJoinListaActivos($relationAlias = null) Adds a LEFT JOIN clause to the query using the ListaActivos relation
 * @method EmpresaQuery rightJoinListaActivos($relationAlias = null) Adds a RIGHT JOIN clause to the query using the ListaActivos relation
 * @method EmpresaQuery innerJoinListaActivos($relationAlias = null) Adds a INNER JOIN clause to the query using the ListaActivos relation
 *
 * @method EmpresaQuery leftJoinSolicitudDeposito($relationAlias = null) Adds a LEFT JOIN clause to the query using the SolicitudDeposito relation
 * @method EmpresaQuery rightJoinSolicitudDeposito($relationAlias = null) Adds a RIGHT JOIN clause to the query using the SolicitudDeposito relation
 * @method EmpresaQuery innerJoinSolicitudDeposito($relationAlias = null) Adds a INNER JOIN clause to the query using the SolicitudDeposito relation
 *
 * @method EmpresaQuery leftJoinPrestamo($relationAlias = null) Adds a LEFT JOIN clause to the query using the Prestamo relation
 * @method EmpresaQuery rightJoinPrestamo($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Prestamo relation
 * @method EmpresaQuery innerJoinPrestamo($relationAlias = null) Adds a INNER JOIN clause to the query using the Prestamo relation
 *
 * @method EmpresaQuery leftJoinVentasVendedor($relationAlias = null) Adds a LEFT JOIN clause to the query using the VentasVendedor relation
 * @method EmpresaQuery rightJoinVentasVendedor($relationAlias = null) Adds a RIGHT JOIN clause to the query using the VentasVendedor relation
 * @method EmpresaQuery innerJoinVentasVendedor($relationAlias = null) Adds a INNER JOIN clause to the query using the VentasVendedor relation
 *
 * @method EmpresaQuery leftJoinAgenda($relationAlias = null) Adds a LEFT JOIN clause to the query using the Agenda relation
 * @method EmpresaQuery rightJoinAgenda($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Agenda relation
 * @method EmpresaQuery innerJoinAgenda($relationAlias = null) Adds a INNER JOIN clause to the query using the Agenda relation
 *
 * @method EmpresaQuery leftJoinRecetario($relationAlias = null) Adds a LEFT JOIN clause to the query using the Recetario relation
 * @method EmpresaQuery rightJoinRecetario($relationAlias = null) Adds a RIGHT JOIN clause to the query using the Recetario relation
 * @method EmpresaQuery innerJoinRecetario($relationAlias = null) Adds a INNER JOIN clause to the query using the Recetario relation
 *
 * @method EmpresaQuery leftJoinUsuarioReport($relationAlias = null) Adds a LEFT JOIN clause to the query using the UsuarioReport relation
 * @method EmpresaQuery rightJoinUsuarioReport($relationAlias = null) Adds a RIGHT JOIN clause to the query using the UsuarioReport relation
 * @method EmpresaQuery innerJoinUsuarioReport($relationAlias = null) Adds a INNER JOIN clause to the query using the UsuarioReport relation
 *
 * @method EmpresaQuery leftJoinInventarioVence($relationAlias = null) Adds a LEFT JOIN clause to the query using the InventarioVence relation
 * @method EmpresaQuery rightJoinInventarioVence($relationAlias = null) Adds a RIGHT JOIN clause to the query using the InventarioVence relation
 * @method EmpresaQuery innerJoinInventarioVence($relationAlias = null) Adds a INNER JOIN clause to the query using the InventarioVence relation
 *
 * @method EmpresaQuery leftJoinTrasladoProducto($relationAlias = null) Adds a LEFT JOIN clause to the query using the TrasladoProducto relation
 * @method EmpresaQuery rightJoinTrasladoProducto($relationAlias = null) Adds a RIGHT JOIN clause to the query using the TrasladoProducto relation
 * @method EmpresaQuery innerJoinTrasladoProducto($relationAlias = null) Adds a INNER JOIN clause to the query using the TrasladoProducto relation
 *
 * @method EmpresaQuery leftJoinTrasladoUbicacion($relationAlias = null) Adds a LEFT JOIN clause to the query using the TrasladoUbicacion relation
 * @method EmpresaQuery rightJoinTrasladoUbicacion($relationAlias = null) Adds a RIGHT JOIN clause to the query using the TrasladoUbicacion relation
 * @method EmpresaQuery innerJoinTrasladoUbicacion($relationAlias = null) Adds a INNER JOIN clause to the query using the TrasladoUbicacion relation
 *
 * @method Empresa findOne(PropelPDO $con = null) Return the first Empresa matching the query
 * @method Empresa findOneOrCreate(PropelPDO $con = null) Return the first Empresa matching the query, or a new Empresa object populated from the query conditions when no match is found
 *
 * @method Empresa findOneById(int $id) Return the first Empresa filtered by the id column
 * @method Empresa findOneByCodigo(string $codigo) Return the first Empresa filtered by the codigo column
 * @method Empresa findOneByNombre(string $nombre) Return the first Empresa filtered by the nombre column
 * @method Empresa findOneByNomenclatura(string $nomenclatura) Return the first Empresa filtered by the nomenclatura column
 * @method Empresa findOneByTelefono(string $telefono) Return the first Empresa filtered by the telefono column
 * @method Empresa findOneByDepartamentoId(int $departamento_id) Return the first Empresa filtered by the departamento_id column
 * @method Empresa findOneByMunicipioId(int $municipio_id) Return the first Empresa filtered by the municipio_id column
 * @method Empresa findOneByDireccion(string $direccion) Return the first Empresa filtered by the direccion column
 * @method Empresa findOneByMapaGeo(string $mapa_geo) Return the first Empresa filtered by the mapa_geo column
 * @method Empresa findOneByLogo(string $logo) Return the first Empresa filtered by the logo column
 * @method Empresa findOneByContactoNombre(string $contacto_nombre) Return the first Empresa filtered by the contacto_nombre column
 * @method Empresa findOneByContactoTelefono(string $contacto_telefono) Return the first Empresa filtered by the contacto_telefono column
 * @method Empresa findOneByContactoMovil(string $contacto_movil) Return the first Empresa filtered by the contacto_movil column
 * @method Empresa findOneByObservaciones(string $observaciones) Return the first Empresa filtered by the observaciones column
 * @method Empresa findOneByFacturaElectronica(boolean $factura_electronica) Return the first Empresa filtered by the factura_electronica column
 * @method Empresa findOneByContactoCorreo(string $contacto_correo) Return the first Empresa filtered by the contacto_correo column
 * @method Empresa findOneByFeelNombre(string $feel_nombre) Return the first Empresa filtered by the feel_nombre column
 * @method Empresa findOneByFeelUsuario(string $feel_usuario) Return the first Empresa filtered by the feel_usuario column
 * @method Empresa findOneByFeelToken(string $feel_token) Return the first Empresa filtered by the feel_token column
 * @method Empresa findOneByFeelLlave(string $feel_llave) Return the first Empresa filtered by the feel_llave column
 * @method Empresa findOneByFeelEscenarioFrase(string $feel_escenario_frase) Return the first Empresa filtered by the feel_escenario_frase column
 * @method Empresa findOneByDiasCredito(int $dias_credito) Return the first Empresa filtered by the dias_credito column
 * @method Empresa findOneByRetieneIsr(boolean $retiene_isr) Return the first Empresa filtered by the retiene_isr column
 * @method Empresa findOneByMonedaQ(string $moneda_q) Return the first Empresa filtered by the moneda_q column
 *
 * @method array findById(int $id) Return Empresa objects filtered by the id column
 * @method array findByCodigo(string $codigo) Return Empresa objects filtered by the codigo column
 * @method array findByNombre(string $nombre) Return Empresa objects filtered by the nombre column
 * @method array findByNomenclatura(string $nomenclatura) Return Empresa objects filtered by the nomenclatura column
 * @method array findByTelefono(string $telefono) Return Empresa objects filtered by the telefono column
 * @method array findByDepartamentoId(int $departamento_id) Return Empresa objects filtered by the departamento_id column
 * @method array findByMunicipioId(int $municipio_id) Return Empresa objects filtered by the municipio_id column
 * @method array findByDireccion(string $direccion) Return Empresa objects filtered by the direccion column
 * @method array findByMapaGeo(string $mapa_geo) Return Empresa objects filtered by the mapa_geo column
 * @method array findByLogo(string $logo) Return Empresa objects filtered by the logo column
 * @method array findByContactoNombre(string $contacto_nombre) Return Empresa objects filtered by the contacto_nombre column
 * @method array findByContactoTelefono(string $contacto_telefono) Return Empresa objects filtered by the contacto_telefono column
 * @method array findByContactoMovil(string $contacto_movil) Return Empresa objects filtered by the contacto_movil column
 * @method array findByObservaciones(string $observaciones) Return Empresa objects filtered by the observaciones column
 * @method array findByFacturaElectronica(boolean $factura_electronica) Return Empresa objects filtered by the factura_electronica column
 * @method array findByContactoCorreo(string $contacto_correo) Return Empresa objects filtered by the contacto_correo column
 * @method array findByFeelNombre(string $feel_nombre) Return Empresa objects filtered by the feel_nombre column
 * @method array findByFeelUsuario(string $feel_usuario) Return Empresa objects filtered by the feel_usuario column
 * @method array findByFeelToken(string $feel_token) Return Empresa objects filtered by the feel_token column
 * @method array findByFeelLlave(string $feel_llave) Return Empresa objects filtered by the feel_llave column
 * @method array findByFeelEscenarioFrase(string $feel_escenario_frase) Return Empresa objects filtered by the feel_escenario_frase column
 * @method array findByDiasCredito(int $dias_credito) Return Empresa objects filtered by the dias_credito column
 * @method array findByRetieneIsr(boolean $retiene_isr) Return Empresa objects filtered by the retiene_isr column
 * @method array findByMonedaQ(string $moneda_q) Return Empresa objects filtered by the moneda_q column
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseEmpresaQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseEmpresaQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = 'propel', $modelName = 'Empresa', $modelAlias = null)
    {
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new EmpresaQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param     EmpresaQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return EmpresaQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof EmpresaQuery) {
            return $criteria;
        }
        $query = new EmpresaQuery();
        if (null !== $modelAlias) {
            $query->setModelAlias($modelAlias);
        }
        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return   Empresa|Empresa[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = EmpresaPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is alredy in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(EmpresaPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return   Empresa A model object, or null if the key is not found
     * @throws   PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT `ID`, `CODIGO`, `NOMBRE`, `NOMENCLATURA`, `TELEFONO`, `DEPARTAMENTO_ID`, `MUNICIPIO_ID`, `DIRECCION`, `MAPA_GEO`, `LOGO`, `CONTACTO_NOMBRE`, `CONTACTO_TELEFONO`, `CONTACTO_MOVIL`, `OBSERVACIONES`, `FACTURA_ELECTRONICA`, `CONTACTO_CORREO`, `FEEL_NOMBRE`, `FEEL_USUARIO`, `FEEL_TOKEN`, `FEEL_LLAVE`, `FEEL_ESCENARIO_FRASE`, `DIAS_CREDITO`, `RETIENE_ISR`, `MONEDA_Q` FROM `empresa` WHERE `ID` = :p0';
        try {
            $stmt = $con->prepare($sql);
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new Empresa();
            $obj->hydrate($row);
            EmpresaPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return Empresa|Empresa[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|Empresa[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(EmpresaPeer::ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(EmpresaPeer::ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the id column
     *
     * Example usage:
     * <code>
     * $query->filterById(1234); // WHERE id = 1234
     * $query->filterById(array(12, 34)); // WHERE id IN (12, 34)
     * $query->filterById(array('min' => 12)); // WHERE id > 12
     * </code>
     *
     * @param     mixed $id The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function filterById($id = null, $comparison = null)
    {
        if (is_array($id) && null === $comparison) {
            $comparison = Criteria::IN;
        }

        return $this->addUsingAlias(EmpresaPeer::ID, $id, $comparison);
    }

    /**
     * Filter the query on the codigo column
     *
     * Example usage:
     * <code>
     * $query->filterByCodigo('fooValue');   // WHERE codigo = 'fooValue'
     * $query->filterByCodigo('%fooValue%'); // WHERE codigo LIKE '%fooValue%'
     * </code>
     *
     * @param     string $codigo The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function filterByCodigo($codigo = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($codigo)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $codigo)) {
                $codigo = str_replace('*', '%', $codigo);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(EmpresaPeer::CODIGO, $codigo, $comparison);
    }

    /**
     * Filter the query on the nombre column
     *
     * Example usage:
     * <code>
     * $query->filterByNombre('fooValue');   // WHERE nombre = 'fooValue'
     * $query->filterByNombre('%fooValue%'); // WHERE nombre LIKE '%fooValue%'
     * </code>
     *
     * @param     string $nombre The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function filterByNombre($nombre = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($nombre)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $nombre)) {
                $nombre = str_replace('*', '%', $nombre);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(EmpresaPeer::NOMBRE, $nombre, $comparison);
    }

    /**
     * Filter the query on the nomenclatura column
     *
     * Example usage:
     * <code>
     * $query->filterByNomenclatura('fooValue');   // WHERE nomenclatura = 'fooValue'
     * $query->filterByNomenclatura('%fooValue%'); // WHERE nomenclatura LIKE '%fooValue%'
     * </code>
     *
     * @param     string $nomenclatura The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function filterByNomenclatura($nomenclatura = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($nomenclatura)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $nomenclatura)) {
                $nomenclatura = str_replace('*', '%', $nomenclatura);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(EmpresaPeer::NOMENCLATURA, $nomenclatura, $comparison);
    }

    /**
     * Filter the query on the telefono column
     *
     * Example usage:
     * <code>
     * $query->filterByTelefono('fooValue');   // WHERE telefono = 'fooValue'
     * $query->filterByTelefono('%fooValue%'); // WHERE telefono LIKE '%fooValue%'
     * </code>
     *
     * @param     string $telefono The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function filterByTelefono($telefono = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($telefono)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $telefono)) {
                $telefono = str_replace('*', '%', $telefono);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(EmpresaPeer::TELEFONO, $telefono, $comparison);
    }

    /**
     * Filter the query on the departamento_id column
     *
     * Example usage:
     * <code>
     * $query->filterByDepartamentoId(1234); // WHERE departamento_id = 1234
     * $query->filterByDepartamentoId(array(12, 34)); // WHERE departamento_id IN (12, 34)
     * $query->filterByDepartamentoId(array('min' => 12)); // WHERE departamento_id > 12
     * </code>
     *
     * @see       filterByDepartamento()
     *
     * @param     mixed $departamentoId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function filterByDepartamentoId($departamentoId = null, $comparison = null)
    {
        if (is_array($departamentoId)) {
            $useMinMax = false;
            if (isset($departamentoId['min'])) {
                $this->addUsingAlias(EmpresaPeer::DEPARTAMENTO_ID, $departamentoId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($departamentoId['max'])) {
                $this->addUsingAlias(EmpresaPeer::DEPARTAMENTO_ID, $departamentoId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(EmpresaPeer::DEPARTAMENTO_ID, $departamentoId, $comparison);
    }

    /**
     * Filter the query on the municipio_id column
     *
     * Example usage:
     * <code>
     * $query->filterByMunicipioId(1234); // WHERE municipio_id = 1234
     * $query->filterByMunicipioId(array(12, 34)); // WHERE municipio_id IN (12, 34)
     * $query->filterByMunicipioId(array('min' => 12)); // WHERE municipio_id > 12
     * </code>
     *
     * @see       filterByMunicipio()
     *
     * @param     mixed $municipioId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function filterByMunicipioId($municipioId = null, $comparison = null)
    {
        if (is_array($municipioId)) {
            $useMinMax = false;
            if (isset($municipioId['min'])) {
                $this->addUsingAlias(EmpresaPeer::MUNICIPIO_ID, $municipioId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($municipioId['max'])) {
                $this->addUsingAlias(EmpresaPeer::MUNICIPIO_ID, $municipioId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(EmpresaPeer::MUNICIPIO_ID, $municipioId, $comparison);
    }

    /**
     * Filter the query on the direccion column
     *
     * Example usage:
     * <code>
     * $query->filterByDireccion('fooValue');   // WHERE direccion = 'fooValue'
     * $query->filterByDireccion('%fooValue%'); // WHERE direccion LIKE '%fooValue%'
     * </code>
     *
     * @param     string $direccion The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function filterByDireccion($direccion = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($direccion)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $direccion)) {
                $direccion = str_replace('*', '%', $direccion);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(EmpresaPeer::DIRECCION, $direccion, $comparison);
    }

    /**
     * Filter the query on the mapa_geo column
     *
     * Example usage:
     * <code>
     * $query->filterByMapaGeo('fooValue');   // WHERE mapa_geo = 'fooValue'
     * $query->filterByMapaGeo('%fooValue%'); // WHERE mapa_geo LIKE '%fooValue%'
     * </code>
     *
     * @param     string $mapaGeo The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function filterByMapaGeo($mapaGeo = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($mapaGeo)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $mapaGeo)) {
                $mapaGeo = str_replace('*', '%', $mapaGeo);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(EmpresaPeer::MAPA_GEO, $mapaGeo, $comparison);
    }

    /**
     * Filter the query on the logo column
     *
     * Example usage:
     * <code>
     * $query->filterByLogo('fooValue');   // WHERE logo = 'fooValue'
     * $query->filterByLogo('%fooValue%'); // WHERE logo LIKE '%fooValue%'
     * </code>
     *
     * @param     string $logo The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function filterByLogo($logo = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($logo)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $logo)) {
                $logo = str_replace('*', '%', $logo);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(EmpresaPeer::LOGO, $logo, $comparison);
    }

    /**
     * Filter the query on the contacto_nombre column
     *
     * Example usage:
     * <code>
     * $query->filterByContactoNombre('fooValue');   // WHERE contacto_nombre = 'fooValue'
     * $query->filterByContactoNombre('%fooValue%'); // WHERE contacto_nombre LIKE '%fooValue%'
     * </code>
     *
     * @param     string $contactoNombre The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function filterByContactoNombre($contactoNombre = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($contactoNombre)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $contactoNombre)) {
                $contactoNombre = str_replace('*', '%', $contactoNombre);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(EmpresaPeer::CONTACTO_NOMBRE, $contactoNombre, $comparison);
    }

    /**
     * Filter the query on the contacto_telefono column
     *
     * Example usage:
     * <code>
     * $query->filterByContactoTelefono('fooValue');   // WHERE contacto_telefono = 'fooValue'
     * $query->filterByContactoTelefono('%fooValue%'); // WHERE contacto_telefono LIKE '%fooValue%'
     * </code>
     *
     * @param     string $contactoTelefono The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function filterByContactoTelefono($contactoTelefono = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($contactoTelefono)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $contactoTelefono)) {
                $contactoTelefono = str_replace('*', '%', $contactoTelefono);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(EmpresaPeer::CONTACTO_TELEFONO, $contactoTelefono, $comparison);
    }

    /**
     * Filter the query on the contacto_movil column
     *
     * Example usage:
     * <code>
     * $query->filterByContactoMovil('fooValue');   // WHERE contacto_movil = 'fooValue'
     * $query->filterByContactoMovil('%fooValue%'); // WHERE contacto_movil LIKE '%fooValue%'
     * </code>
     *
     * @param     string $contactoMovil The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function filterByContactoMovil($contactoMovil = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($contactoMovil)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $contactoMovil)) {
                $contactoMovil = str_replace('*', '%', $contactoMovil);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(EmpresaPeer::CONTACTO_MOVIL, $contactoMovil, $comparison);
    }

    /**
     * Filter the query on the observaciones column
     *
     * Example usage:
     * <code>
     * $query->filterByObservaciones('fooValue');   // WHERE observaciones = 'fooValue'
     * $query->filterByObservaciones('%fooValue%'); // WHERE observaciones LIKE '%fooValue%'
     * </code>
     *
     * @param     string $observaciones The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function filterByObservaciones($observaciones = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($observaciones)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $observaciones)) {
                $observaciones = str_replace('*', '%', $observaciones);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(EmpresaPeer::OBSERVACIONES, $observaciones, $comparison);
    }

    /**
     * Filter the query on the factura_electronica column
     *
     * Example usage:
     * <code>
     * $query->filterByFacturaElectronica(true); // WHERE factura_electronica = true
     * $query->filterByFacturaElectronica('yes'); // WHERE factura_electronica = true
     * </code>
     *
     * @param     boolean|string $facturaElectronica The value to use as filter.
     *              Non-boolean arguments are converted using the following rules:
     *                * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *                * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     *              Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function filterByFacturaElectronica($facturaElectronica = null, $comparison = null)
    {
        if (is_string($facturaElectronica)) {
            $factura_electronica = in_array(strtolower($facturaElectronica), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
        }

        return $this->addUsingAlias(EmpresaPeer::FACTURA_ELECTRONICA, $facturaElectronica, $comparison);
    }

    /**
     * Filter the query on the contacto_correo column
     *
     * Example usage:
     * <code>
     * $query->filterByContactoCorreo('fooValue');   // WHERE contacto_correo = 'fooValue'
     * $query->filterByContactoCorreo('%fooValue%'); // WHERE contacto_correo LIKE '%fooValue%'
     * </code>
     *
     * @param     string $contactoCorreo The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function filterByContactoCorreo($contactoCorreo = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($contactoCorreo)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $contactoCorreo)) {
                $contactoCorreo = str_replace('*', '%', $contactoCorreo);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(EmpresaPeer::CONTACTO_CORREO, $contactoCorreo, $comparison);
    }

    /**
     * Filter the query on the feel_nombre column
     *
     * Example usage:
     * <code>
     * $query->filterByFeelNombre('fooValue');   // WHERE feel_nombre = 'fooValue'
     * $query->filterByFeelNombre('%fooValue%'); // WHERE feel_nombre LIKE '%fooValue%'
     * </code>
     *
     * @param     string $feelNombre The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function filterByFeelNombre($feelNombre = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($feelNombre)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $feelNombre)) {
                $feelNombre = str_replace('*', '%', $feelNombre);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(EmpresaPeer::FEEL_NOMBRE, $feelNombre, $comparison);
    }

    /**
     * Filter the query on the feel_usuario column
     *
     * Example usage:
     * <code>
     * $query->filterByFeelUsuario('fooValue');   // WHERE feel_usuario = 'fooValue'
     * $query->filterByFeelUsuario('%fooValue%'); // WHERE feel_usuario LIKE '%fooValue%'
     * </code>
     *
     * @param     string $feelUsuario The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function filterByFeelUsuario($feelUsuario = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($feelUsuario)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $feelUsuario)) {
                $feelUsuario = str_replace('*', '%', $feelUsuario);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(EmpresaPeer::FEEL_USUARIO, $feelUsuario, $comparison);
    }

    /**
     * Filter the query on the feel_token column
     *
     * Example usage:
     * <code>
     * $query->filterByFeelToken('fooValue');   // WHERE feel_token = 'fooValue'
     * $query->filterByFeelToken('%fooValue%'); // WHERE feel_token LIKE '%fooValue%'
     * </code>
     *
     * @param     string $feelToken The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function filterByFeelToken($feelToken = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($feelToken)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $feelToken)) {
                $feelToken = str_replace('*', '%', $feelToken);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(EmpresaPeer::FEEL_TOKEN, $feelToken, $comparison);
    }

    /**
     * Filter the query on the feel_llave column
     *
     * Example usage:
     * <code>
     * $query->filterByFeelLlave('fooValue');   // WHERE feel_llave = 'fooValue'
     * $query->filterByFeelLlave('%fooValue%'); // WHERE feel_llave LIKE '%fooValue%'
     * </code>
     *
     * @param     string $feelLlave The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function filterByFeelLlave($feelLlave = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($feelLlave)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $feelLlave)) {
                $feelLlave = str_replace('*', '%', $feelLlave);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(EmpresaPeer::FEEL_LLAVE, $feelLlave, $comparison);
    }

    /**
     * Filter the query on the feel_escenario_frase column
     *
     * Example usage:
     * <code>
     * $query->filterByFeelEscenarioFrase('fooValue');   // WHERE feel_escenario_frase = 'fooValue'
     * $query->filterByFeelEscenarioFrase('%fooValue%'); // WHERE feel_escenario_frase LIKE '%fooValue%'
     * </code>
     *
     * @param     string $feelEscenarioFrase The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function filterByFeelEscenarioFrase($feelEscenarioFrase = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($feelEscenarioFrase)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $feelEscenarioFrase)) {
                $feelEscenarioFrase = str_replace('*', '%', $feelEscenarioFrase);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(EmpresaPeer::FEEL_ESCENARIO_FRASE, $feelEscenarioFrase, $comparison);
    }

    /**
     * Filter the query on the dias_credito column
     *
     * Example usage:
     * <code>
     * $query->filterByDiasCredito(1234); // WHERE dias_credito = 1234
     * $query->filterByDiasCredito(array(12, 34)); // WHERE dias_credito IN (12, 34)
     * $query->filterByDiasCredito(array('min' => 12)); // WHERE dias_credito > 12
     * </code>
     *
     * @param     mixed $diasCredito The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function filterByDiasCredito($diasCredito = null, $comparison = null)
    {
        if (is_array($diasCredito)) {
            $useMinMax = false;
            if (isset($diasCredito['min'])) {
                $this->addUsingAlias(EmpresaPeer::DIAS_CREDITO, $diasCredito['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($diasCredito['max'])) {
                $this->addUsingAlias(EmpresaPeer::DIAS_CREDITO, $diasCredito['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(EmpresaPeer::DIAS_CREDITO, $diasCredito, $comparison);
    }

    /**
     * Filter the query on the retiene_isr column
     *
     * Example usage:
     * <code>
     * $query->filterByRetieneIsr(true); // WHERE retiene_isr = true
     * $query->filterByRetieneIsr('yes'); // WHERE retiene_isr = true
     * </code>
     *
     * @param     boolean|string $retieneIsr The value to use as filter.
     *              Non-boolean arguments are converted using the following rules:
     *                * 1, '1', 'true',  'on',  and 'yes' are converted to boolean true
     *                * 0, '0', 'false', 'off', and 'no'  are converted to boolean false
     *              Check on string values is case insensitive (so 'FaLsE' is seen as 'false').
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function filterByRetieneIsr($retieneIsr = null, $comparison = null)
    {
        if (is_string($retieneIsr)) {
            $retiene_isr = in_array(strtolower($retieneIsr), array('false', 'off', '-', 'no', 'n', '0', '')) ? false : true;
        }

        return $this->addUsingAlias(EmpresaPeer::RETIENE_ISR, $retieneIsr, $comparison);
    }

    /**
     * Filter the query on the moneda_q column
     *
     * Example usage:
     * <code>
     * $query->filterByMonedaQ('fooValue');   // WHERE moneda_q = 'fooValue'
     * $query->filterByMonedaQ('%fooValue%'); // WHERE moneda_q LIKE '%fooValue%'
     * </code>
     *
     * @param     string $monedaQ The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function filterByMonedaQ($monedaQ = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($monedaQ)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $monedaQ)) {
                $monedaQ = str_replace('*', '%', $monedaQ);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(EmpresaPeer::MONEDA_Q, $monedaQ, $comparison);
    }

    /**
     * Filter the query by a related Departamento object
     *
     * @param   Departamento|PropelObjectCollection $departamento The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByDepartamento($departamento, $comparison = null)
    {
        if ($departamento instanceof Departamento) {
            return $this
                ->addUsingAlias(EmpresaPeer::DEPARTAMENTO_ID, $departamento->getId(), $comparison);
        } elseif ($departamento instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(EmpresaPeer::DEPARTAMENTO_ID, $departamento->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByDepartamento() only accepts arguments of type Departamento or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Departamento relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinDepartamento($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Departamento');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Departamento');
        }

        return $this;
    }

    /**
     * Use the Departamento relation Departamento object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   DepartamentoQuery A secondary query class using the current class as primary query
     */
    public function useDepartamentoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinDepartamento($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Departamento', 'DepartamentoQuery');
    }

    /**
     * Filter the query by a related Municipio object
     *
     * @param   Municipio|PropelObjectCollection $municipio The related object(s) to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByMunicipio($municipio, $comparison = null)
    {
        if ($municipio instanceof Municipio) {
            return $this
                ->addUsingAlias(EmpresaPeer::MUNICIPIO_ID, $municipio->getId(), $comparison);
        } elseif ($municipio instanceof PropelObjectCollection) {
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }

            return $this
                ->addUsingAlias(EmpresaPeer::MUNICIPIO_ID, $municipio->toKeyValue('PrimaryKey', 'Id'), $comparison);
        } else {
            throw new PropelException('filterByMunicipio() only accepts arguments of type Municipio or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Municipio relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinMunicipio($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Municipio');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Municipio');
        }

        return $this;
    }

    /**
     * Use the Municipio relation Municipio object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   MunicipioQuery A secondary query class using the current class as primary query
     */
    public function useMunicipioQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinMunicipio($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Municipio', 'MunicipioQuery');
    }

    /**
     * Filter the query by a related Usuario object
     *
     * @param   Usuario|PropelObjectCollection $usuario  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByUsuario($usuario, $comparison = null)
    {
        if ($usuario instanceof Usuario) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $usuario->getEmpresaId(), $comparison);
        } elseif ($usuario instanceof PropelObjectCollection) {
            return $this
                ->useUsuarioQuery()
                ->filterByPrimaryKeys($usuario->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByUsuario() only accepts arguments of type Usuario or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Usuario relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinUsuario($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Usuario');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Usuario');
        }

        return $this;
    }

    /**
     * Use the Usuario relation Usuario object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   UsuarioQuery A secondary query class using the current class as primary query
     */
    public function useUsuarioQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinUsuario($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Usuario', 'UsuarioQuery');
    }

    /**
     * Filter the query by a related UsuarioEmpresa object
     *
     * @param   UsuarioEmpresa|PropelObjectCollection $usuarioEmpresa  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByUsuarioEmpresa($usuarioEmpresa, $comparison = null)
    {
        if ($usuarioEmpresa instanceof UsuarioEmpresa) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $usuarioEmpresa->getEmpresaId(), $comparison);
        } elseif ($usuarioEmpresa instanceof PropelObjectCollection) {
            return $this
                ->useUsuarioEmpresaQuery()
                ->filterByPrimaryKeys($usuarioEmpresa->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByUsuarioEmpresa() only accepts arguments of type UsuarioEmpresa or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the UsuarioEmpresa relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinUsuarioEmpresa($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('UsuarioEmpresa');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'UsuarioEmpresa');
        }

        return $this;
    }

    /**
     * Use the UsuarioEmpresa relation UsuarioEmpresa object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   UsuarioEmpresaQuery A secondary query class using the current class as primary query
     */
    public function useUsuarioEmpresaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinUsuarioEmpresa($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'UsuarioEmpresa', 'UsuarioEmpresaQuery');
    }

    /**
     * Filter the query by a related Banco object
     *
     * @param   Banco|PropelObjectCollection $banco  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByBanco($banco, $comparison = null)
    {
        if ($banco instanceof Banco) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $banco->getEmpresaId(), $comparison);
        } elseif ($banco instanceof PropelObjectCollection) {
            return $this
                ->useBancoQuery()
                ->filterByPrimaryKeys($banco->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByBanco() only accepts arguments of type Banco or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Banco relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinBanco($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Banco');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Banco');
        }

        return $this;
    }

    /**
     * Use the Banco relation Banco object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   BancoQuery A secondary query class using the current class as primary query
     */
    public function useBancoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinBanco($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Banco', 'BancoQuery');
    }

    /**
     * Filter the query by a related BancoTemporal object
     *
     * @param   BancoTemporal|PropelObjectCollection $bancoTemporal  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByBancoTemporal($bancoTemporal, $comparison = null)
    {
        if ($bancoTemporal instanceof BancoTemporal) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $bancoTemporal->getEmpresaId(), $comparison);
        } elseif ($bancoTemporal instanceof PropelObjectCollection) {
            return $this
                ->useBancoTemporalQuery()
                ->filterByPrimaryKeys($bancoTemporal->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByBancoTemporal() only accepts arguments of type BancoTemporal or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the BancoTemporal relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinBancoTemporal($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('BancoTemporal');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'BancoTemporal');
        }

        return $this;
    }

    /**
     * Use the BancoTemporal relation BancoTemporal object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   BancoTemporalQuery A secondary query class using the current class as primary query
     */
    public function useBancoTemporalQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinBancoTemporal($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'BancoTemporal', 'BancoTemporalQuery');
    }

    /**
     * Filter the query by a related Operacion object
     *
     * @param   Operacion|PropelObjectCollection $operacion  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByOperacion($operacion, $comparison = null)
    {
        if ($operacion instanceof Operacion) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $operacion->getEmpresaId(), $comparison);
        } elseif ($operacion instanceof PropelObjectCollection) {
            return $this
                ->useOperacionQuery()
                ->filterByPrimaryKeys($operacion->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByOperacion() only accepts arguments of type Operacion or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Operacion relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinOperacion($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Operacion');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Operacion');
        }

        return $this;
    }

    /**
     * Use the Operacion relation Operacion object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   OperacionQuery A secondary query class using the current class as primary query
     */
    public function useOperacionQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinOperacion($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Operacion', 'OperacionQuery');
    }

    /**
     * Filter the query by a related OperacionDetalle object
     *
     * @param   OperacionDetalle|PropelObjectCollection $operacionDetalle  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByOperacionDetalle($operacionDetalle, $comparison = null)
    {
        if ($operacionDetalle instanceof OperacionDetalle) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $operacionDetalle->getEmpresaId(), $comparison);
        } elseif ($operacionDetalle instanceof PropelObjectCollection) {
            return $this
                ->useOperacionDetalleQuery()
                ->filterByPrimaryKeys($operacionDetalle->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByOperacionDetalle() only accepts arguments of type OperacionDetalle or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the OperacionDetalle relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinOperacionDetalle($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('OperacionDetalle');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'OperacionDetalle');
        }

        return $this;
    }

    /**
     * Use the OperacionDetalle relation OperacionDetalle object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   OperacionDetalleQuery A secondary query class using the current class as primary query
     */
    public function useOperacionDetalleQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinOperacionDetalle($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'OperacionDetalle', 'OperacionDetalleQuery');
    }

    /**
     * Filter the query by a related OperacionPago object
     *
     * @param   OperacionPago|PropelObjectCollection $operacionPago  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByOperacionPago($operacionPago, $comparison = null)
    {
        if ($operacionPago instanceof OperacionPago) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $operacionPago->getEmpresaId(), $comparison);
        } elseif ($operacionPago instanceof PropelObjectCollection) {
            return $this
                ->useOperacionPagoQuery()
                ->filterByPrimaryKeys($operacionPago->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByOperacionPago() only accepts arguments of type OperacionPago or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the OperacionPago relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinOperacionPago($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('OperacionPago');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'OperacionPago');
        }

        return $this;
    }

    /**
     * Use the OperacionPago relation OperacionPago object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   OperacionPagoQuery A secondary query class using the current class as primary query
     */
    public function useOperacionPagoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinOperacionPago($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'OperacionPago', 'OperacionPagoQuery');
    }

    /**
     * Filter the query by a related Proveedor object
     *
     * @param   Proveedor|PropelObjectCollection $proveedor  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByProveedor($proveedor, $comparison = null)
    {
        if ($proveedor instanceof Proveedor) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $proveedor->getEmpresaId(), $comparison);
        } elseif ($proveedor instanceof PropelObjectCollection) {
            return $this
                ->useProveedorQuery()
                ->filterByPrimaryKeys($proveedor->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByProveedor() only accepts arguments of type Proveedor or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Proveedor relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinProveedor($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Proveedor');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Proveedor');
        }

        return $this;
    }

    /**
     * Use the Proveedor relation Proveedor object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ProveedorQuery A secondary query class using the current class as primary query
     */
    public function useProveedorQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinProveedor($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Proveedor', 'ProveedorQuery');
    }

    /**
     * Filter the query by a related TipoServicio object
     *
     * @param   TipoServicio|PropelObjectCollection $tipoServicio  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByTipoServicio($tipoServicio, $comparison = null)
    {
        if ($tipoServicio instanceof TipoServicio) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $tipoServicio->getEmpresaId(), $comparison);
        } elseif ($tipoServicio instanceof PropelObjectCollection) {
            return $this
                ->useTipoServicioQuery()
                ->filterByPrimaryKeys($tipoServicio->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByTipoServicio() only accepts arguments of type TipoServicio or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the TipoServicio relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinTipoServicio($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('TipoServicio');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'TipoServicio');
        }

        return $this;
    }

    /**
     * Use the TipoServicio relation TipoServicio object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   TipoServicioQuery A secondary query class using the current class as primary query
     */
    public function useTipoServicioQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinTipoServicio($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'TipoServicio', 'TipoServicioQuery');
    }

    /**
     * Filter the query by a related OrdenProveedor object
     *
     * @param   OrdenProveedor|PropelObjectCollection $ordenProveedor  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByOrdenProveedor($ordenProveedor, $comparison = null)
    {
        if ($ordenProveedor instanceof OrdenProveedor) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $ordenProveedor->getEmpresaId(), $comparison);
        } elseif ($ordenProveedor instanceof PropelObjectCollection) {
            return $this
                ->useOrdenProveedorQuery()
                ->filterByPrimaryKeys($ordenProveedor->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByOrdenProveedor() only accepts arguments of type OrdenProveedor or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the OrdenProveedor relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinOrdenProveedor($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('OrdenProveedor');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'OrdenProveedor');
        }

        return $this;
    }

    /**
     * Use the OrdenProveedor relation OrdenProveedor object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   OrdenProveedorQuery A secondary query class using the current class as primary query
     */
    public function useOrdenProveedorQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinOrdenProveedor($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'OrdenProveedor', 'OrdenProveedorQuery');
    }

    /**
     * Filter the query by a related CuentaProveedor object
     *
     * @param   CuentaProveedor|PropelObjectCollection $cuentaProveedor  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByCuentaProveedor($cuentaProveedor, $comparison = null)
    {
        if ($cuentaProveedor instanceof CuentaProveedor) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $cuentaProveedor->getEmpresaId(), $comparison);
        } elseif ($cuentaProveedor instanceof PropelObjectCollection) {
            return $this
                ->useCuentaProveedorQuery()
                ->filterByPrimaryKeys($cuentaProveedor->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByCuentaProveedor() only accepts arguments of type CuentaProveedor or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the CuentaProveedor relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinCuentaProveedor($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('CuentaProveedor');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'CuentaProveedor');
        }

        return $this;
    }

    /**
     * Use the CuentaProveedor relation CuentaProveedor object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   CuentaProveedorQuery A secondary query class using the current class as primary query
     */
    public function useCuentaProveedorQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinCuentaProveedor($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'CuentaProveedor', 'CuentaProveedorQuery');
    }

    /**
     * Filter the query by a related ContrasenaCrea object
     *
     * @param   ContrasenaCrea|PropelObjectCollection $contrasenaCrea  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByContrasenaCrea($contrasenaCrea, $comparison = null)
    {
        if ($contrasenaCrea instanceof ContrasenaCrea) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $contrasenaCrea->getEmpresaId(), $comparison);
        } elseif ($contrasenaCrea instanceof PropelObjectCollection) {
            return $this
                ->useContrasenaCreaQuery()
                ->filterByPrimaryKeys($contrasenaCrea->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByContrasenaCrea() only accepts arguments of type ContrasenaCrea or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ContrasenaCrea relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinContrasenaCrea($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ContrasenaCrea');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ContrasenaCrea');
        }

        return $this;
    }

    /**
     * Use the ContrasenaCrea relation ContrasenaCrea object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ContrasenaCreaQuery A secondary query class using the current class as primary query
     */
    public function useContrasenaCreaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinContrasenaCrea($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ContrasenaCrea', 'ContrasenaCreaQuery');
    }

    /**
     * Filter the query by a related OrdenProveedorPago object
     *
     * @param   OrdenProveedorPago|PropelObjectCollection $ordenProveedorPago  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByOrdenProveedorPago($ordenProveedorPago, $comparison = null)
    {
        if ($ordenProveedorPago instanceof OrdenProveedorPago) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $ordenProveedorPago->getEmpresaId(), $comparison);
        } elseif ($ordenProveedorPago instanceof PropelObjectCollection) {
            return $this
                ->useOrdenProveedorPagoQuery()
                ->filterByPrimaryKeys($ordenProveedorPago->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByOrdenProveedorPago() only accepts arguments of type OrdenProveedorPago or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the OrdenProveedorPago relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinOrdenProveedorPago($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('OrdenProveedorPago');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'OrdenProveedorPago');
        }

        return $this;
    }

    /**
     * Use the OrdenProveedorPago relation OrdenProveedorPago object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   OrdenProveedorPagoQuery A secondary query class using the current class as primary query
     */
    public function useOrdenProveedorPagoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinOrdenProveedorPago($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'OrdenProveedorPago', 'OrdenProveedorPagoQuery');
    }

    /**
     * Filter the query by a related BitacoraArchivo object
     *
     * @param   BitacoraArchivo|PropelObjectCollection $bitacoraArchivo  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByBitacoraArchivo($bitacoraArchivo, $comparison = null)
    {
        if ($bitacoraArchivo instanceof BitacoraArchivo) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $bitacoraArchivo->getEmpresaId(), $comparison);
        } elseif ($bitacoraArchivo instanceof PropelObjectCollection) {
            return $this
                ->useBitacoraArchivoQuery()
                ->filterByPrimaryKeys($bitacoraArchivo->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByBitacoraArchivo() only accepts arguments of type BitacoraArchivo or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the BitacoraArchivo relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinBitacoraArchivo($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('BitacoraArchivo');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'BitacoraArchivo');
        }

        return $this;
    }

    /**
     * Use the BitacoraArchivo relation BitacoraArchivo object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   BitacoraArchivoQuery A secondary query class using the current class as primary query
     */
    public function useBitacoraArchivoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinBitacoraArchivo($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'BitacoraArchivo', 'BitacoraArchivoQuery');
    }

    /**
     * Filter the query by a related BitacoraElimino object
     *
     * @param   BitacoraElimino|PropelObjectCollection $bitacoraElimino  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByBitacoraElimino($bitacoraElimino, $comparison = null)
    {
        if ($bitacoraElimino instanceof BitacoraElimino) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $bitacoraElimino->getEmpresaId(), $comparison);
        } elseif ($bitacoraElimino instanceof PropelObjectCollection) {
            return $this
                ->useBitacoraEliminoQuery()
                ->filterByPrimaryKeys($bitacoraElimino->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByBitacoraElimino() only accepts arguments of type BitacoraElimino or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the BitacoraElimino relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinBitacoraElimino($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('BitacoraElimino');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'BitacoraElimino');
        }

        return $this;
    }

    /**
     * Use the BitacoraElimino relation BitacoraElimino object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   BitacoraEliminoQuery A secondary query class using the current class as primary query
     */
    public function useBitacoraEliminoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinBitacoraElimino($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'BitacoraElimino', 'BitacoraEliminoQuery');
    }

    /**
     * Filter the query by a related MedioPago object
     *
     * @param   MedioPago|PropelObjectCollection $medioPago  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByMedioPago($medioPago, $comparison = null)
    {
        if ($medioPago instanceof MedioPago) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $medioPago->getEmpresaId(), $comparison);
        } elseif ($medioPago instanceof PropelObjectCollection) {
            return $this
                ->useMedioPagoQuery()
                ->filterByPrimaryKeys($medioPago->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByMedioPago() only accepts arguments of type MedioPago or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the MedioPago relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinMedioPago($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('MedioPago');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'MedioPago');
        }

        return $this;
    }

    /**
     * Use the MedioPago relation MedioPago object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   MedioPagoQuery A secondary query class using the current class as primary query
     */
    public function useMedioPagoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinMedioPago($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'MedioPago', 'MedioPagoQuery');
    }

    /**
     * Filter the query by a related Tienda object
     *
     * @param   Tienda|PropelObjectCollection $tienda  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByTienda($tienda, $comparison = null)
    {
        if ($tienda instanceof Tienda) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $tienda->getEmpresaId(), $comparison);
        } elseif ($tienda instanceof PropelObjectCollection) {
            return $this
                ->useTiendaQuery()
                ->filterByPrimaryKeys($tienda->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByTienda() only accepts arguments of type Tienda or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Tienda relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinTienda($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Tienda');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Tienda');
        }

        return $this;
    }

    /**
     * Use the Tienda relation Tienda object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   TiendaQuery A secondary query class using the current class as primary query
     */
    public function useTiendaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinTienda($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Tienda', 'TiendaQuery');
    }

    /**
     * Filter the query by a related MovimientoBanco object
     *
     * @param   MovimientoBanco|PropelObjectCollection $movimientoBanco  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByMovimientoBanco($movimientoBanco, $comparison = null)
    {
        if ($movimientoBanco instanceof MovimientoBanco) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $movimientoBanco->getEmpresaId(), $comparison);
        } elseif ($movimientoBanco instanceof PropelObjectCollection) {
            return $this
                ->useMovimientoBancoQuery()
                ->filterByPrimaryKeys($movimientoBanco->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByMovimientoBanco() only accepts arguments of type MovimientoBanco or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the MovimientoBanco relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinMovimientoBanco($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('MovimientoBanco');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'MovimientoBanco');
        }

        return $this;
    }

    /**
     * Use the MovimientoBanco relation MovimientoBanco object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   MovimientoBancoQuery A secondary query class using the current class as primary query
     */
    public function useMovimientoBancoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinMovimientoBanco($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'MovimientoBanco', 'MovimientoBancoQuery');
    }

    /**
     * Filter the query by a related Partida object
     *
     * @param   Partida|PropelObjectCollection $partida  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByPartida($partida, $comparison = null)
    {
        if ($partida instanceof Partida) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $partida->getEmpresaId(), $comparison);
        } elseif ($partida instanceof PropelObjectCollection) {
            return $this
                ->usePartidaQuery()
                ->filterByPrimaryKeys($partida->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByPartida() only accepts arguments of type Partida or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Partida relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinPartida($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Partida');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Partida');
        }

        return $this;
    }

    /**
     * Use the Partida relation Partida object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   PartidaQuery A secondary query class using the current class as primary query
     */
    public function usePartidaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinPartida($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Partida', 'PartidaQuery');
    }

    /**
     * Filter the query by a related PartidaDetalle object
     *
     * @param   PartidaDetalle|PropelObjectCollection $partidaDetalle  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByPartidaDetalle($partidaDetalle, $comparison = null)
    {
        if ($partidaDetalle instanceof PartidaDetalle) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $partidaDetalle->getEmpresaId(), $comparison);
        } elseif ($partidaDetalle instanceof PropelObjectCollection) {
            return $this
                ->usePartidaDetalleQuery()
                ->filterByPrimaryKeys($partidaDetalle->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByPartidaDetalle() only accepts arguments of type PartidaDetalle or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the PartidaDetalle relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinPartidaDetalle($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('PartidaDetalle');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'PartidaDetalle');
        }

        return $this;
    }

    /**
     * Use the PartidaDetalle relation PartidaDetalle object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   PartidaDetalleQuery A secondary query class using the current class as primary query
     */
    public function usePartidaDetalleQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinPartidaDetalle($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'PartidaDetalle', 'PartidaDetalleQuery');
    }

    /**
     * Filter the query by a related PartidaAgrupa object
     *
     * @param   PartidaAgrupa|PropelObjectCollection $partidaAgrupa  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByPartidaAgrupa($partidaAgrupa, $comparison = null)
    {
        if ($partidaAgrupa instanceof PartidaAgrupa) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $partidaAgrupa->getEmpresaId(), $comparison);
        } elseif ($partidaAgrupa instanceof PropelObjectCollection) {
            return $this
                ->usePartidaAgrupaQuery()
                ->filterByPrimaryKeys($partidaAgrupa->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByPartidaAgrupa() only accepts arguments of type PartidaAgrupa or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the PartidaAgrupa relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinPartidaAgrupa($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('PartidaAgrupa');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'PartidaAgrupa');
        }

        return $this;
    }

    /**
     * Use the PartidaAgrupa relation PartidaAgrupa object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   PartidaAgrupaQuery A secondary query class using the current class as primary query
     */
    public function usePartidaAgrupaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinPartidaAgrupa($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'PartidaAgrupa', 'PartidaAgrupaQuery');
    }

    /**
     * Filter the query by a related PartidaAgrupaLinea object
     *
     * @param   PartidaAgrupaLinea|PropelObjectCollection $partidaAgrupaLinea  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByPartidaAgrupaLinea($partidaAgrupaLinea, $comparison = null)
    {
        if ($partidaAgrupaLinea instanceof PartidaAgrupaLinea) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $partidaAgrupaLinea->getEmpresaId(), $comparison);
        } elseif ($partidaAgrupaLinea instanceof PropelObjectCollection) {
            return $this
                ->usePartidaAgrupaLineaQuery()
                ->filterByPrimaryKeys($partidaAgrupaLinea->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByPartidaAgrupaLinea() only accepts arguments of type PartidaAgrupaLinea or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the PartidaAgrupaLinea relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinPartidaAgrupaLinea($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('PartidaAgrupaLinea');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'PartidaAgrupaLinea');
        }

        return $this;
    }

    /**
     * Use the PartidaAgrupaLinea relation PartidaAgrupaLinea object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   PartidaAgrupaLineaQuery A secondary query class using the current class as primary query
     */
    public function usePartidaAgrupaLineaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinPartidaAgrupaLinea($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'PartidaAgrupaLinea', 'PartidaAgrupaLineaQuery');
    }

    /**
     * Filter the query by a related CuentaErpContable object
     *
     * @param   CuentaErpContable|PropelObjectCollection $cuentaErpContable  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByCuentaErpContable($cuentaErpContable, $comparison = null)
    {
        if ($cuentaErpContable instanceof CuentaErpContable) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $cuentaErpContable->getEmpresaId(), $comparison);
        } elseif ($cuentaErpContable instanceof PropelObjectCollection) {
            return $this
                ->useCuentaErpContableQuery()
                ->filterByPrimaryKeys($cuentaErpContable->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByCuentaErpContable() only accepts arguments of type CuentaErpContable or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the CuentaErpContable relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinCuentaErpContable($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('CuentaErpContable');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'CuentaErpContable');
        }

        return $this;
    }

    /**
     * Use the CuentaErpContable relation CuentaErpContable object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   CuentaErpContableQuery A secondary query class using the current class as primary query
     */
    public function useCuentaErpContableQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinCuentaErpContable($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'CuentaErpContable', 'CuentaErpContableQuery');
    }

    /**
     * Filter the query by a related TipoAparato object
     *
     * @param   TipoAparato|PropelObjectCollection $tipoAparato  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByTipoAparato($tipoAparato, $comparison = null)
    {
        if ($tipoAparato instanceof TipoAparato) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $tipoAparato->getEmpresaId(), $comparison);
        } elseif ($tipoAparato instanceof PropelObjectCollection) {
            return $this
                ->useTipoAparatoQuery()
                ->filterByPrimaryKeys($tipoAparato->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByTipoAparato() only accepts arguments of type TipoAparato or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the TipoAparato relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinTipoAparato($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('TipoAparato');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'TipoAparato');
        }

        return $this;
    }

    /**
     * Use the TipoAparato relation TipoAparato object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   TipoAparatoQuery A secondary query class using the current class as primary query
     */
    public function useTipoAparatoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinTipoAparato($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'TipoAparato', 'TipoAparatoQuery');
    }

    /**
     * Filter the query by a related MarcaProducto object
     *
     * @param   MarcaProducto|PropelObjectCollection $marcaProducto  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByMarcaProducto($marcaProducto, $comparison = null)
    {
        if ($marcaProducto instanceof MarcaProducto) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $marcaProducto->getEmpresaId(), $comparison);
        } elseif ($marcaProducto instanceof PropelObjectCollection) {
            return $this
                ->useMarcaProductoQuery()
                ->filterByPrimaryKeys($marcaProducto->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByMarcaProducto() only accepts arguments of type MarcaProducto or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the MarcaProducto relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinMarcaProducto($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('MarcaProducto');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'MarcaProducto');
        }

        return $this;
    }

    /**
     * Use the MarcaProducto relation MarcaProducto object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   MarcaProductoQuery A secondary query class using the current class as primary query
     */
    public function useMarcaProductoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinMarcaProducto($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'MarcaProducto', 'MarcaProductoQuery');
    }

    /**
     * Filter the query by a related Marca object
     *
     * @param   Marca|PropelObjectCollection $marca  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByMarca($marca, $comparison = null)
    {
        if ($marca instanceof Marca) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $marca->getEmpresaId(), $comparison);
        } elseif ($marca instanceof PropelObjectCollection) {
            return $this
                ->useMarcaQuery()
                ->filterByPrimaryKeys($marca->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByMarca() only accepts arguments of type Marca or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Marca relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinMarca($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Marca');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Marca');
        }

        return $this;
    }

    /**
     * Use the Marca relation Marca object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   MarcaQuery A secondary query class using the current class as primary query
     */
    public function useMarcaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinMarca($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Marca', 'MarcaQuery');
    }

    /**
     * Filter the query by a related Modelo object
     *
     * @param   Modelo|PropelObjectCollection $modelo  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByModelo($modelo, $comparison = null)
    {
        if ($modelo instanceof Modelo) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $modelo->getEmpresaId(), $comparison);
        } elseif ($modelo instanceof PropelObjectCollection) {
            return $this
                ->useModeloQuery()
                ->filterByPrimaryKeys($modelo->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByModelo() only accepts arguments of type Modelo or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Modelo relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinModelo($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Modelo');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Modelo');
        }

        return $this;
    }

    /**
     * Use the Modelo relation Modelo object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ModeloQuery A secondary query class using the current class as primary query
     */
    public function useModeloQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinModelo($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Modelo', 'ModeloQuery');
    }

    /**
     * Filter the query by a related Producto object
     *
     * @param   Producto|PropelObjectCollection $producto  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByProducto($producto, $comparison = null)
    {
        if ($producto instanceof Producto) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $producto->getEmpresaId(), $comparison);
        } elseif ($producto instanceof PropelObjectCollection) {
            return $this
                ->useProductoQuery()
                ->filterByPrimaryKeys($producto->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByProducto() only accepts arguments of type Producto or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Producto relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinProducto($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Producto');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Producto');
        }

        return $this;
    }

    /**
     * Use the Producto relation Producto object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ProductoQuery A secondary query class using the current class as primary query
     */
    public function useProductoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinProducto($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Producto', 'ProductoQuery');
    }

    /**
     * Filter the query by a related RecetaProducto object
     *
     * @param   RecetaProducto|PropelObjectCollection $recetaProducto  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByRecetaProducto($recetaProducto, $comparison = null)
    {
        if ($recetaProducto instanceof RecetaProducto) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $recetaProducto->getEmpresaId(), $comparison);
        } elseif ($recetaProducto instanceof PropelObjectCollection) {
            return $this
                ->useRecetaProductoQuery()
                ->filterByPrimaryKeys($recetaProducto->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByRecetaProducto() only accepts arguments of type RecetaProducto or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the RecetaProducto relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinRecetaProducto($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('RecetaProducto');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'RecetaProducto');
        }

        return $this;
    }

    /**
     * Use the RecetaProducto relation RecetaProducto object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   RecetaProductoQuery A secondary query class using the current class as primary query
     */
    public function useRecetaProductoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinRecetaProducto($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'RecetaProducto', 'RecetaProductoQuery');
    }

    /**
     * Filter the query by a related RecetaProductoDetalle object
     *
     * @param   RecetaProductoDetalle|PropelObjectCollection $recetaProductoDetalle  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByRecetaProductoDetalle($recetaProductoDetalle, $comparison = null)
    {
        if ($recetaProductoDetalle instanceof RecetaProductoDetalle) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $recetaProductoDetalle->getEmpresaId(), $comparison);
        } elseif ($recetaProductoDetalle instanceof PropelObjectCollection) {
            return $this
                ->useRecetaProductoDetalleQuery()
                ->filterByPrimaryKeys($recetaProductoDetalle->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByRecetaProductoDetalle() only accepts arguments of type RecetaProductoDetalle or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the RecetaProductoDetalle relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinRecetaProductoDetalle($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('RecetaProductoDetalle');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'RecetaProductoDetalle');
        }

        return $this;
    }

    /**
     * Use the RecetaProductoDetalle relation RecetaProductoDetalle object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   RecetaProductoDetalleQuery A secondary query class using the current class as primary query
     */
    public function useRecetaProductoDetalleQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinRecetaProductoDetalle($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'RecetaProductoDetalle', 'RecetaProductoDetalleQuery');
    }

    /**
     * Filter the query by a related ComboProducto object
     *
     * @param   ComboProducto|PropelObjectCollection $comboProducto  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByComboProducto($comboProducto, $comparison = null)
    {
        if ($comboProducto instanceof ComboProducto) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $comboProducto->getEmpresaId(), $comparison);
        } elseif ($comboProducto instanceof PropelObjectCollection) {
            return $this
                ->useComboProductoQuery()
                ->filterByPrimaryKeys($comboProducto->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByComboProducto() only accepts arguments of type ComboProducto or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ComboProducto relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinComboProducto($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ComboProducto');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ComboProducto');
        }

        return $this;
    }

    /**
     * Use the ComboProducto relation ComboProducto object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ComboProductoQuery A secondary query class using the current class as primary query
     */
    public function useComboProductoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinComboProducto($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ComboProducto', 'ComboProductoQuery');
    }

    /**
     * Filter the query by a related ComboProductoDetalle object
     *
     * @param   ComboProductoDetalle|PropelObjectCollection $comboProductoDetalle  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByComboProductoDetalle($comboProductoDetalle, $comparison = null)
    {
        if ($comboProductoDetalle instanceof ComboProductoDetalle) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $comboProductoDetalle->getEmpresaId(), $comparison);
        } elseif ($comboProductoDetalle instanceof PropelObjectCollection) {
            return $this
                ->useComboProductoDetalleQuery()
                ->filterByPrimaryKeys($comboProductoDetalle->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByComboProductoDetalle() only accepts arguments of type ComboProductoDetalle or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ComboProductoDetalle relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinComboProductoDetalle($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ComboProductoDetalle');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ComboProductoDetalle');
        }

        return $this;
    }

    /**
     * Use the ComboProductoDetalle relation ComboProductoDetalle object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ComboProductoDetalleQuery A secondary query class using the current class as primary query
     */
    public function useComboProductoDetalleQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinComboProductoDetalle($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ComboProductoDetalle', 'ComboProductoDetalleQuery');
    }

    /**
     * Filter the query by a related ListaComboDetalle object
     *
     * @param   ListaComboDetalle|PropelObjectCollection $listaComboDetalle  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByListaComboDetalle($listaComboDetalle, $comparison = null)
    {
        if ($listaComboDetalle instanceof ListaComboDetalle) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $listaComboDetalle->getEmpresaId(), $comparison);
        } elseif ($listaComboDetalle instanceof PropelObjectCollection) {
            return $this
                ->useListaComboDetalleQuery()
                ->filterByPrimaryKeys($listaComboDetalle->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByListaComboDetalle() only accepts arguments of type ListaComboDetalle or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ListaComboDetalle relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinListaComboDetalle($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ListaComboDetalle');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ListaComboDetalle');
        }

        return $this;
    }

    /**
     * Use the ListaComboDetalle relation ListaComboDetalle object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ListaComboDetalleQuery A secondary query class using the current class as primary query
     */
    public function useListaComboDetalleQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinListaComboDetalle($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ListaComboDetalle', 'ListaComboDetalleQuery');
    }

    /**
     * Filter the query by a related ListaRecetaDetalle object
     *
     * @param   ListaRecetaDetalle|PropelObjectCollection $listaRecetaDetalle  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByListaRecetaDetalle($listaRecetaDetalle, $comparison = null)
    {
        if ($listaRecetaDetalle instanceof ListaRecetaDetalle) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $listaRecetaDetalle->getEmpresaId(), $comparison);
        } elseif ($listaRecetaDetalle instanceof PropelObjectCollection) {
            return $this
                ->useListaRecetaDetalleQuery()
                ->filterByPrimaryKeys($listaRecetaDetalle->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByListaRecetaDetalle() only accepts arguments of type ListaRecetaDetalle or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ListaRecetaDetalle relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinListaRecetaDetalle($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ListaRecetaDetalle');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ListaRecetaDetalle');
        }

        return $this;
    }

    /**
     * Use the ListaRecetaDetalle relation ListaRecetaDetalle object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ListaRecetaDetalleQuery A secondary query class using the current class as primary query
     */
    public function useListaRecetaDetalleQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinListaRecetaDetalle($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ListaRecetaDetalle', 'ListaRecetaDetalleQuery');
    }

    /**
     * Filter the query by a related IngresoProductoPro object
     *
     * @param   IngresoProductoPro|PropelObjectCollection $ingresoProductoPro  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByIngresoProductoPro($ingresoProductoPro, $comparison = null)
    {
        if ($ingresoProductoPro instanceof IngresoProductoPro) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $ingresoProductoPro->getEmpresaId(), $comparison);
        } elseif ($ingresoProductoPro instanceof PropelObjectCollection) {
            return $this
                ->useIngresoProductoProQuery()
                ->filterByPrimaryKeys($ingresoProductoPro->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByIngresoProductoPro() only accepts arguments of type IngresoProductoPro or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the IngresoProductoPro relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinIngresoProductoPro($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('IngresoProductoPro');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'IngresoProductoPro');
        }

        return $this;
    }

    /**
     * Use the IngresoProductoPro relation IngresoProductoPro object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   IngresoProductoProQuery A secondary query class using the current class as primary query
     */
    public function useIngresoProductoProQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinIngresoProductoPro($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'IngresoProductoPro', 'IngresoProductoProQuery');
    }

    /**
     * Filter the query by a related IngresoProductoDetallePro object
     *
     * @param   IngresoProductoDetallePro|PropelObjectCollection $ingresoProductoDetallePro  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByIngresoProductoDetallePro($ingresoProductoDetallePro, $comparison = null)
    {
        if ($ingresoProductoDetallePro instanceof IngresoProductoDetallePro) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $ingresoProductoDetallePro->getEmpresaId(), $comparison);
        } elseif ($ingresoProductoDetallePro instanceof PropelObjectCollection) {
            return $this
                ->useIngresoProductoDetalleProQuery()
                ->filterByPrimaryKeys($ingresoProductoDetallePro->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByIngresoProductoDetallePro() only accepts arguments of type IngresoProductoDetallePro or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the IngresoProductoDetallePro relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinIngresoProductoDetallePro($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('IngresoProductoDetallePro');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'IngresoProductoDetallePro');
        }

        return $this;
    }

    /**
     * Use the IngresoProductoDetallePro relation IngresoProductoDetallePro object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   IngresoProductoDetalleProQuery A secondary query class using the current class as primary query
     */
    public function useIngresoProductoDetalleProQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinIngresoProductoDetallePro($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'IngresoProductoDetallePro', 'IngresoProductoDetalleProQuery');
    }

    /**
     * Filter the query by a related SalidaProducto object
     *
     * @param   SalidaProducto|PropelObjectCollection $salidaProducto  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterBySalidaProducto($salidaProducto, $comparison = null)
    {
        if ($salidaProducto instanceof SalidaProducto) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $salidaProducto->getEmpresaId(), $comparison);
        } elseif ($salidaProducto instanceof PropelObjectCollection) {
            return $this
                ->useSalidaProductoQuery()
                ->filterByPrimaryKeys($salidaProducto->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterBySalidaProducto() only accepts arguments of type SalidaProducto or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the SalidaProducto relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinSalidaProducto($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('SalidaProducto');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'SalidaProducto');
        }

        return $this;
    }

    /**
     * Use the SalidaProducto relation SalidaProducto object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   SalidaProductoQuery A secondary query class using the current class as primary query
     */
    public function useSalidaProductoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinSalidaProducto($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'SalidaProducto', 'SalidaProductoQuery');
    }

    /**
     * Filter the query by a related SalidaProductoDetalle object
     *
     * @param   SalidaProductoDetalle|PropelObjectCollection $salidaProductoDetalle  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterBySalidaProductoDetalle($salidaProductoDetalle, $comparison = null)
    {
        if ($salidaProductoDetalle instanceof SalidaProductoDetalle) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $salidaProductoDetalle->getEmpresaId(), $comparison);
        } elseif ($salidaProductoDetalle instanceof PropelObjectCollection) {
            return $this
                ->useSalidaProductoDetalleQuery()
                ->filterByPrimaryKeys($salidaProductoDetalle->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterBySalidaProductoDetalle() only accepts arguments of type SalidaProductoDetalle or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the SalidaProductoDetalle relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinSalidaProductoDetalle($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('SalidaProductoDetalle');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'SalidaProductoDetalle');
        }

        return $this;
    }

    /**
     * Use the SalidaProductoDetalle relation SalidaProductoDetalle object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   SalidaProductoDetalleQuery A secondary query class using the current class as primary query
     */
    public function useSalidaProductoDetalleQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinSalidaProductoDetalle($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'SalidaProductoDetalle', 'SalidaProductoDetalleQuery');
    }

    /**
     * Filter the query by a related MotivoMovimientoProducto object
     *
     * @param   MotivoMovimientoProducto|PropelObjectCollection $motivoMovimientoProducto  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByMotivoMovimientoProducto($motivoMovimientoProducto, $comparison = null)
    {
        if ($motivoMovimientoProducto instanceof MotivoMovimientoProducto) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $motivoMovimientoProducto->getEmpresaId(), $comparison);
        } elseif ($motivoMovimientoProducto instanceof PropelObjectCollection) {
            return $this
                ->useMotivoMovimientoProductoQuery()
                ->filterByPrimaryKeys($motivoMovimientoProducto->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByMotivoMovimientoProducto() only accepts arguments of type MotivoMovimientoProducto or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the MotivoMovimientoProducto relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinMotivoMovimientoProducto($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('MotivoMovimientoProducto');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'MotivoMovimientoProducto');
        }

        return $this;
    }

    /**
     * Use the MotivoMovimientoProducto relation MotivoMovimientoProducto object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   MotivoMovimientoProductoQuery A secondary query class using the current class as primary query
     */
    public function useMotivoMovimientoProductoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinMotivoMovimientoProducto($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'MotivoMovimientoProducto', 'MotivoMovimientoProductoQuery');
    }

    /**
     * Filter the query by a related Cliente object
     *
     * @param   Cliente|PropelObjectCollection $cliente  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByCliente($cliente, $comparison = null)
    {
        if ($cliente instanceof Cliente) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $cliente->getEmpresaId(), $comparison);
        } elseif ($cliente instanceof PropelObjectCollection) {
            return $this
                ->useClienteQuery()
                ->filterByPrimaryKeys($cliente->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByCliente() only accepts arguments of type Cliente or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Cliente relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinCliente($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Cliente');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Cliente');
        }

        return $this;
    }

    /**
     * Use the Cliente relation Cliente object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ClienteQuery A secondary query class using the current class as primary query
     */
    public function useClienteQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinCliente($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Cliente', 'ClienteQuery');
    }

    /**
     * Filter the query by a related CierreCaja object
     *
     * @param   CierreCaja|PropelObjectCollection $cierreCaja  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByCierreCaja($cierreCaja, $comparison = null)
    {
        if ($cierreCaja instanceof CierreCaja) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $cierreCaja->getEmpresaId(), $comparison);
        } elseif ($cierreCaja instanceof PropelObjectCollection) {
            return $this
                ->useCierreCajaQuery()
                ->filterByPrimaryKeys($cierreCaja->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByCierreCaja() only accepts arguments of type CierreCaja or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the CierreCaja relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinCierreCaja($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('CierreCaja');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'CierreCaja');
        }

        return $this;
    }

    /**
     * Use the CierreCaja relation CierreCaja object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   CierreCajaQuery A secondary query class using the current class as primary query
     */
    public function useCierreCajaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinCierreCaja($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'CierreCaja', 'CierreCajaQuery');
    }

    /**
     * Filter the query by a related Cierre object
     *
     * @param   Cierre|PropelObjectCollection $cierre  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByCierre($cierre, $comparison = null)
    {
        if ($cierre instanceof Cierre) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $cierre->getEmpresaId(), $comparison);
        } elseif ($cierre instanceof PropelObjectCollection) {
            return $this
                ->useCierreQuery()
                ->filterByPrimaryKeys($cierre->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByCierre() only accepts arguments of type Cierre or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Cierre relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinCierre($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Cierre');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Cierre');
        }

        return $this;
    }

    /**
     * Use the Cierre relation Cierre object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   CierreQuery A secondary query class using the current class as primary query
     */
    public function useCierreQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinCierre($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Cierre', 'CierreQuery');
    }

    /**
     * Filter the query by a related Servicio object
     *
     * @param   Servicio|PropelObjectCollection $servicio  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByServicio($servicio, $comparison = null)
    {
        if ($servicio instanceof Servicio) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $servicio->getEmpresaId(), $comparison);
        } elseif ($servicio instanceof PropelObjectCollection) {
            return $this
                ->useServicioQuery()
                ->filterByPrimaryKeys($servicio->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByServicio() only accepts arguments of type Servicio or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Servicio relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinServicio($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Servicio');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Servicio');
        }

        return $this;
    }

    /**
     * Use the Servicio relation Servicio object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ServicioQuery A secondary query class using the current class as primary query
     */
    public function useServicioQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinServicio($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Servicio', 'ServicioQuery');
    }

    /**
     * Filter the query by a related CampoUsuario object
     *
     * @param   CampoUsuario|PropelObjectCollection $campoUsuario  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByCampoUsuario($campoUsuario, $comparison = null)
    {
        if ($campoUsuario instanceof CampoUsuario) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $campoUsuario->getEmpresaId(), $comparison);
        } elseif ($campoUsuario instanceof PropelObjectCollection) {
            return $this
                ->useCampoUsuarioQuery()
                ->filterByPrimaryKeys($campoUsuario->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByCampoUsuario() only accepts arguments of type CampoUsuario or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the CampoUsuario relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinCampoUsuario($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('CampoUsuario');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'CampoUsuario');
        }

        return $this;
    }

    /**
     * Use the CampoUsuario relation CampoUsuario object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   CampoUsuarioQuery A secondary query class using the current class as primary query
     */
    public function useCampoUsuarioQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinCampoUsuario($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'CampoUsuario', 'CampoUsuarioQuery');
    }

    /**
     * Filter the query by a related ValorUsuario object
     *
     * @param   ValorUsuario|PropelObjectCollection $valorUsuario  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByValorUsuario($valorUsuario, $comparison = null)
    {
        if ($valorUsuario instanceof ValorUsuario) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $valorUsuario->getEmpresaId(), $comparison);
        } elseif ($valorUsuario instanceof PropelObjectCollection) {
            return $this
                ->useValorUsuarioQuery()
                ->filterByPrimaryKeys($valorUsuario->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByValorUsuario() only accepts arguments of type ValorUsuario or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ValorUsuario relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinValorUsuario($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ValorUsuario');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ValorUsuario');
        }

        return $this;
    }

    /**
     * Use the ValorUsuario relation ValorUsuario object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ValorUsuarioQuery A secondary query class using the current class as primary query
     */
    public function useValorUsuarioQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinValorUsuario($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ValorUsuario', 'ValorUsuarioQuery');
    }

    /**
     * Filter the query by a related Proyecto object
     *
     * @param   Proyecto|PropelObjectCollection $proyecto  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByProyecto($proyecto, $comparison = null)
    {
        if ($proyecto instanceof Proyecto) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $proyecto->getEmpresaId(), $comparison);
        } elseif ($proyecto instanceof PropelObjectCollection) {
            return $this
                ->useProyectoQuery()
                ->filterByPrimaryKeys($proyecto->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByProyecto() only accepts arguments of type Proyecto or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Proyecto relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinProyecto($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Proyecto');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Proyecto');
        }

        return $this;
    }

    /**
     * Use the Proyecto relation Proyecto object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ProyectoQuery A secondary query class using the current class as primary query
     */
    public function useProyectoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinProyecto($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Proyecto', 'ProyectoQuery');
    }

    /**
     * Filter the query by a related Gasto object
     *
     * @param   Gasto|PropelObjectCollection $gasto  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByGasto($gasto, $comparison = null)
    {
        if ($gasto instanceof Gasto) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $gasto->getEmpresaId(), $comparison);
        } elseif ($gasto instanceof PropelObjectCollection) {
            return $this
                ->useGastoQuery()
                ->filterByPrimaryKeys($gasto->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByGasto() only accepts arguments of type Gasto or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Gasto relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinGasto($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Gasto');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Gasto');
        }

        return $this;
    }

    /**
     * Use the Gasto relation Gasto object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   GastoQuery A secondary query class using the current class as primary query
     */
    public function useGastoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinGasto($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Gasto', 'GastoQuery');
    }

    /**
     * Filter the query by a related GastoPago object
     *
     * @param   GastoPago|PropelObjectCollection $gastoPago  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByGastoPago($gastoPago, $comparison = null)
    {
        if ($gastoPago instanceof GastoPago) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $gastoPago->getEmpresaId(), $comparison);
        } elseif ($gastoPago instanceof PropelObjectCollection) {
            return $this
                ->useGastoPagoQuery()
                ->filterByPrimaryKeys($gastoPago->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByGastoPago() only accepts arguments of type GastoPago or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the GastoPago relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinGastoPago($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('GastoPago');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'GastoPago');
        }

        return $this;
    }

    /**
     * Use the GastoPago relation GastoPago object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   GastoPagoQuery A secondary query class using the current class as primary query
     */
    public function useGastoPagoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinGastoPago($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'GastoPago', 'GastoPagoQuery');
    }

    /**
     * Filter the query by a related ParametroCuenta object
     *
     * @param   ParametroCuenta|PropelObjectCollection $parametroCuenta  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByParametroCuenta($parametroCuenta, $comparison = null)
    {
        if ($parametroCuenta instanceof ParametroCuenta) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $parametroCuenta->getEmpresaId(), $comparison);
        } elseif ($parametroCuenta instanceof PropelObjectCollection) {
            return $this
                ->useParametroCuentaQuery()
                ->filterByPrimaryKeys($parametroCuenta->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByParametroCuenta() only accepts arguments of type ParametroCuenta or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ParametroCuenta relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinParametroCuenta($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ParametroCuenta');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ParametroCuenta');
        }

        return $this;
    }

    /**
     * Use the ParametroCuenta relation ParametroCuenta object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ParametroCuentaQuery A secondary query class using the current class as primary query
     */
    public function useParametroCuentaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinParametroCuenta($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ParametroCuenta', 'ParametroCuentaQuery');
    }

    /**
     * Filter the query by a related ProductoExistencia object
     *
     * @param   ProductoExistencia|PropelObjectCollection $productoExistencia  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByProductoExistencia($productoExistencia, $comparison = null)
    {
        if ($productoExistencia instanceof ProductoExistencia) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $productoExistencia->getEmpresaId(), $comparison);
        } elseif ($productoExistencia instanceof PropelObjectCollection) {
            return $this
                ->useProductoExistenciaQuery()
                ->filterByPrimaryKeys($productoExistencia->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByProductoExistencia() only accepts arguments of type ProductoExistencia or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ProductoExistencia relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinProductoExistencia($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ProductoExistencia');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ProductoExistencia');
        }

        return $this;
    }

    /**
     * Use the ProductoExistencia relation ProductoExistencia object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ProductoExistenciaQuery A secondary query class using the current class as primary query
     */
    public function useProductoExistenciaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinProductoExistencia($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ProductoExistencia', 'ProductoExistenciaQuery');
    }

    /**
     * Filter the query by a related ProductoUbicacion object
     *
     * @param   ProductoUbicacion|PropelObjectCollection $productoUbicacion  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByProductoUbicacion($productoUbicacion, $comparison = null)
    {
        if ($productoUbicacion instanceof ProductoUbicacion) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $productoUbicacion->getEmpresaId(), $comparison);
        } elseif ($productoUbicacion instanceof PropelObjectCollection) {
            return $this
                ->useProductoUbicacionQuery()
                ->filterByPrimaryKeys($productoUbicacion->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByProductoUbicacion() only accepts arguments of type ProductoUbicacion or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ProductoUbicacion relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinProductoUbicacion($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ProductoUbicacion');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ProductoUbicacion');
        }

        return $this;
    }

    /**
     * Use the ProductoUbicacion relation ProductoUbicacion object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ProductoUbicacionQuery A secondary query class using the current class as primary query
     */
    public function useProductoUbicacionQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinProductoUbicacion($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ProductoUbicacion', 'ProductoUbicacionQuery');
    }

    /**
     * Filter the query by a related ProductoPrecio object
     *
     * @param   ProductoPrecio|PropelObjectCollection $productoPrecio  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByProductoPrecio($productoPrecio, $comparison = null)
    {
        if ($productoPrecio instanceof ProductoPrecio) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $productoPrecio->getEmpresaId(), $comparison);
        } elseif ($productoPrecio instanceof PropelObjectCollection) {
            return $this
                ->useProductoPrecioQuery()
                ->filterByPrimaryKeys($productoPrecio->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByProductoPrecio() only accepts arguments of type ProductoPrecio or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ProductoPrecio relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinProductoPrecio($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ProductoPrecio');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ProductoPrecio');
        }

        return $this;
    }

    /**
     * Use the ProductoPrecio relation ProductoPrecio object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ProductoPrecioQuery A secondary query class using the current class as primary query
     */
    public function useProductoPrecioQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinProductoPrecio($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ProductoPrecio', 'ProductoPrecioQuery');
    }

    /**
     * Filter the query by a related ListaPrecio object
     *
     * @param   ListaPrecio|PropelObjectCollection $listaPrecio  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByListaPrecio($listaPrecio, $comparison = null)
    {
        if ($listaPrecio instanceof ListaPrecio) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $listaPrecio->getEmpresaId(), $comparison);
        } elseif ($listaPrecio instanceof PropelObjectCollection) {
            return $this
                ->useListaPrecioQuery()
                ->filterByPrimaryKeys($listaPrecio->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByListaPrecio() only accepts arguments of type ListaPrecio or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ListaPrecio relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinListaPrecio($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ListaPrecio');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ListaPrecio');
        }

        return $this;
    }

    /**
     * Use the ListaPrecio relation ListaPrecio object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ListaPrecioQuery A secondary query class using the current class as primary query
     */
    public function useListaPrecioQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinListaPrecio($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ListaPrecio', 'ListaPrecioQuery');
    }

    /**
     * Filter the query by a related IngresoProducto object
     *
     * @param   IngresoProducto|PropelObjectCollection $ingresoProducto  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByIngresoProducto($ingresoProducto, $comparison = null)
    {
        if ($ingresoProducto instanceof IngresoProducto) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $ingresoProducto->getEmpresaId(), $comparison);
        } elseif ($ingresoProducto instanceof PropelObjectCollection) {
            return $this
                ->useIngresoProductoQuery()
                ->filterByPrimaryKeys($ingresoProducto->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByIngresoProducto() only accepts arguments of type IngresoProducto or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the IngresoProducto relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinIngresoProducto($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('IngresoProducto');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'IngresoProducto');
        }

        return $this;
    }

    /**
     * Use the IngresoProducto relation IngresoProducto object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   IngresoProductoQuery A secondary query class using the current class as primary query
     */
    public function useIngresoProductoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinIngresoProducto($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'IngresoProducto', 'IngresoProductoQuery');
    }

    /**
     * Filter the query by a related IngresoProductoDetalle object
     *
     * @param   IngresoProductoDetalle|PropelObjectCollection $ingresoProductoDetalle  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByIngresoProductoDetalle($ingresoProductoDetalle, $comparison = null)
    {
        if ($ingresoProductoDetalle instanceof IngresoProductoDetalle) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $ingresoProductoDetalle->getEmpresaId(), $comparison);
        } elseif ($ingresoProductoDetalle instanceof PropelObjectCollection) {
            return $this
                ->useIngresoProductoDetalleQuery()
                ->filterByPrimaryKeys($ingresoProductoDetalle->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByIngresoProductoDetalle() only accepts arguments of type IngresoProductoDetalle or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the IngresoProductoDetalle relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinIngresoProductoDetalle($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('IngresoProductoDetalle');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'IngresoProductoDetalle');
        }

        return $this;
    }

    /**
     * Use the IngresoProductoDetalle relation IngresoProductoDetalle object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   IngresoProductoDetalleQuery A secondary query class using the current class as primary query
     */
    public function useIngresoProductoDetalleQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinIngresoProductoDetalle($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'IngresoProductoDetalle', 'IngresoProductoDetalleQuery');
    }

    /**
     * Filter the query by a related ProductoMovimiento object
     *
     * @param   ProductoMovimiento|PropelObjectCollection $productoMovimiento  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByProductoMovimiento($productoMovimiento, $comparison = null)
    {
        if ($productoMovimiento instanceof ProductoMovimiento) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $productoMovimiento->getEmpresaId(), $comparison);
        } elseif ($productoMovimiento instanceof PropelObjectCollection) {
            return $this
                ->useProductoMovimientoQuery()
                ->filterByPrimaryKeys($productoMovimiento->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByProductoMovimiento() only accepts arguments of type ProductoMovimiento or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ProductoMovimiento relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinProductoMovimiento($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ProductoMovimiento');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ProductoMovimiento');
        }

        return $this;
    }

    /**
     * Use the ProductoMovimiento relation ProductoMovimiento object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ProductoMovimientoQuery A secondary query class using the current class as primary query
     */
    public function useProductoMovimientoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinProductoMovimiento($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ProductoMovimiento', 'ProductoMovimientoQuery');
    }

    /**
     * Filter the query by a related PrecioProducto object
     *
     * @param   PrecioProducto|PropelObjectCollection $precioProducto  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByPrecioProducto($precioProducto, $comparison = null)
    {
        if ($precioProducto instanceof PrecioProducto) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $precioProducto->getEmpresaId(), $comparison);
        } elseif ($precioProducto instanceof PropelObjectCollection) {
            return $this
                ->usePrecioProductoQuery()
                ->filterByPrimaryKeys($precioProducto->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByPrecioProducto() only accepts arguments of type PrecioProducto or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the PrecioProducto relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinPrecioProducto($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('PrecioProducto');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'PrecioProducto');
        }

        return $this;
    }

    /**
     * Use the PrecioProducto relation PrecioProducto object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   PrecioProductoQuery A secondary query class using the current class as primary query
     */
    public function usePrecioProductoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinPrecioProducto($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'PrecioProducto', 'PrecioProductoQuery');
    }

    /**
     * Filter the query by a related PrecioProductoDetalle object
     *
     * @param   PrecioProductoDetalle|PropelObjectCollection $precioProductoDetalle  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByPrecioProductoDetalle($precioProductoDetalle, $comparison = null)
    {
        if ($precioProductoDetalle instanceof PrecioProductoDetalle) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $precioProductoDetalle->getEmpresaId(), $comparison);
        } elseif ($precioProductoDetalle instanceof PropelObjectCollection) {
            return $this
                ->usePrecioProductoDetalleQuery()
                ->filterByPrimaryKeys($precioProductoDetalle->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByPrecioProductoDetalle() only accepts arguments of type PrecioProductoDetalle or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the PrecioProductoDetalle relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinPrecioProductoDetalle($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('PrecioProductoDetalle');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'PrecioProductoDetalle');
        }

        return $this;
    }

    /**
     * Use the PrecioProductoDetalle relation PrecioProductoDetalle object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   PrecioProductoDetalleQuery A secondary query class using the current class as primary query
     */
    public function usePrecioProductoDetalleQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinPrecioProductoDetalle($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'PrecioProductoDetalle', 'PrecioProductoDetalleQuery');
    }

    /**
     * Filter the query by a related OrdenCotizacion object
     *
     * @param   OrdenCotizacion|PropelObjectCollection $ordenCotizacion  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByOrdenCotizacion($ordenCotizacion, $comparison = null)
    {
        if ($ordenCotizacion instanceof OrdenCotizacion) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $ordenCotizacion->getEmpresaId(), $comparison);
        } elseif ($ordenCotizacion instanceof PropelObjectCollection) {
            return $this
                ->useOrdenCotizacionQuery()
                ->filterByPrimaryKeys($ordenCotizacion->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByOrdenCotizacion() only accepts arguments of type OrdenCotizacion or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the OrdenCotizacion relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinOrdenCotizacion($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('OrdenCotizacion');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'OrdenCotizacion');
        }

        return $this;
    }

    /**
     * Use the OrdenCotizacion relation OrdenCotizacion object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   OrdenCotizacionQuery A secondary query class using the current class as primary query
     */
    public function useOrdenCotizacionQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinOrdenCotizacion($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'OrdenCotizacion', 'OrdenCotizacionQuery');
    }

    /**
     * Filter the query by a related Moneda object
     *
     * @param   Moneda|PropelObjectCollection $moneda  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByMoneda($moneda, $comparison = null)
    {
        if ($moneda instanceof Moneda) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $moneda->getEmpresaId(), $comparison);
        } elseif ($moneda instanceof PropelObjectCollection) {
            return $this
                ->useMonedaQuery()
                ->filterByPrimaryKeys($moneda->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByMoneda() only accepts arguments of type Moneda or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Moneda relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinMoneda($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Moneda');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Moneda');
        }

        return $this;
    }

    /**
     * Use the Moneda relation Moneda object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   MonedaQuery A secondary query class using the current class as primary query
     */
    public function useMonedaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinMoneda($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Moneda', 'MonedaQuery');
    }

    /**
     * Filter the query by a related BancoSaldo object
     *
     * @param   BancoSaldo|PropelObjectCollection $bancoSaldo  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByBancoSaldo($bancoSaldo, $comparison = null)
    {
        if ($bancoSaldo instanceof BancoSaldo) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $bancoSaldo->getEmpresaId(), $comparison);
        } elseif ($bancoSaldo instanceof PropelObjectCollection) {
            return $this
                ->useBancoSaldoQuery()
                ->filterByPrimaryKeys($bancoSaldo->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByBancoSaldo() only accepts arguments of type BancoSaldo or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the BancoSaldo relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinBancoSaldo($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('BancoSaldo');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'BancoSaldo');
        }

        return $this;
    }

    /**
     * Use the BancoSaldo relation BancoSaldo object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   BancoSaldoQuery A secondary query class using the current class as primary query
     */
    public function useBancoSaldoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinBancoSaldo($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'BancoSaldo', 'BancoSaldoQuery');
    }

    /**
     * Filter the query by a related NotaCredito object
     *
     * @param   NotaCredito|PropelObjectCollection $notaCredito  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByNotaCredito($notaCredito, $comparison = null)
    {
        if ($notaCredito instanceof NotaCredito) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $notaCredito->getEmpresaId(), $comparison);
        } elseif ($notaCredito instanceof PropelObjectCollection) {
            return $this
                ->useNotaCreditoQuery()
                ->filterByPrimaryKeys($notaCredito->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByNotaCredito() only accepts arguments of type NotaCredito or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the NotaCredito relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinNotaCredito($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('NotaCredito');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'NotaCredito');
        }

        return $this;
    }

    /**
     * Use the NotaCredito relation NotaCredito object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   NotaCreditoQuery A secondary query class using the current class as primary query
     */
    public function useNotaCreditoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinNotaCredito($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'NotaCredito', 'NotaCreditoQuery');
    }

    /**
     * Filter the query by a related DefinicionCuenta object
     *
     * @param   DefinicionCuenta|PropelObjectCollection $definicionCuenta  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByDefinicionCuenta($definicionCuenta, $comparison = null)
    {
        if ($definicionCuenta instanceof DefinicionCuenta) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $definicionCuenta->getEmpresaId(), $comparison);
        } elseif ($definicionCuenta instanceof PropelObjectCollection) {
            return $this
                ->useDefinicionCuentaQuery()
                ->filterByPrimaryKeys($definicionCuenta->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByDefinicionCuenta() only accepts arguments of type DefinicionCuenta or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the DefinicionCuenta relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinDefinicionCuenta($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('DefinicionCuenta');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'DefinicionCuenta');
        }

        return $this;
    }

    /**
     * Use the DefinicionCuenta relation DefinicionCuenta object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   DefinicionCuentaQuery A secondary query class using the current class as primary query
     */
    public function useDefinicionCuentaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinDefinicionCuenta($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'DefinicionCuenta', 'DefinicionCuentaQuery');
    }

    /**
     * Filter the query by a related DocumentoCheque object
     *
     * @param   DocumentoCheque|PropelObjectCollection $documentoCheque  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByDocumentoCheque($documentoCheque, $comparison = null)
    {
        if ($documentoCheque instanceof DocumentoCheque) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $documentoCheque->getEmpresaId(), $comparison);
        } elseif ($documentoCheque instanceof PropelObjectCollection) {
            return $this
                ->useDocumentoChequeQuery()
                ->filterByPrimaryKeys($documentoCheque->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByDocumentoCheque() only accepts arguments of type DocumentoCheque or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the DocumentoCheque relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinDocumentoCheque($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('DocumentoCheque');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'DocumentoCheque');
        }

        return $this;
    }

    /**
     * Use the DocumentoCheque relation DocumentoCheque object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   DocumentoChequeQuery A secondary query class using the current class as primary query
     */
    public function useDocumentoChequeQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinDocumentoCheque($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'DocumentoCheque', 'DocumentoChequeQuery');
    }

    /**
     * Filter the query by a related CuentaBanco object
     *
     * @param   CuentaBanco|PropelObjectCollection $cuentaBanco  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByCuentaBanco($cuentaBanco, $comparison = null)
    {
        if ($cuentaBanco instanceof CuentaBanco) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $cuentaBanco->getEmpresaId(), $comparison);
        } elseif ($cuentaBanco instanceof PropelObjectCollection) {
            return $this
                ->useCuentaBancoQuery()
                ->filterByPrimaryKeys($cuentaBanco->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByCuentaBanco() only accepts arguments of type CuentaBanco or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the CuentaBanco relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinCuentaBanco($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('CuentaBanco');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'CuentaBanco');
        }

        return $this;
    }

    /**
     * Use the CuentaBanco relation CuentaBanco object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   CuentaBancoQuery A secondary query class using the current class as primary query
     */
    public function useCuentaBancoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinCuentaBanco($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'CuentaBanco', 'CuentaBancoQuery');
    }

    /**
     * Filter the query by a related VentaResumida object
     *
     * @param   VentaResumida|PropelObjectCollection $ventaResumida  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByVentaResumida($ventaResumida, $comparison = null)
    {
        if ($ventaResumida instanceof VentaResumida) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $ventaResumida->getEmpresaId(), $comparison);
        } elseif ($ventaResumida instanceof PropelObjectCollection) {
            return $this
                ->useVentaResumidaQuery()
                ->filterByPrimaryKeys($ventaResumida->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByVentaResumida() only accepts arguments of type VentaResumida or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the VentaResumida relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinVentaResumida($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('VentaResumida');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'VentaResumida');
        }

        return $this;
    }

    /**
     * Use the VentaResumida relation VentaResumida object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   VentaResumidaQuery A secondary query class using the current class as primary query
     */
    public function useVentaResumidaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinVentaResumida($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'VentaResumida', 'VentaResumidaQuery');
    }

    /**
     * Filter the query by a related VentaResumidaLinea object
     *
     * @param   VentaResumidaLinea|PropelObjectCollection $ventaResumidaLinea  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByVentaResumidaLinea($ventaResumidaLinea, $comparison = null)
    {
        if ($ventaResumidaLinea instanceof VentaResumidaLinea) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $ventaResumidaLinea->getEmpresaId(), $comparison);
        } elseif ($ventaResumidaLinea instanceof PropelObjectCollection) {
            return $this
                ->useVentaResumidaLineaQuery()
                ->filterByPrimaryKeys($ventaResumidaLinea->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByVentaResumidaLinea() only accepts arguments of type VentaResumidaLinea or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the VentaResumidaLinea relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinVentaResumidaLinea($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('VentaResumidaLinea');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'VentaResumidaLinea');
        }

        return $this;
    }

    /**
     * Use the VentaResumidaLinea relation VentaResumidaLinea object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   VentaResumidaLineaQuery A secondary query class using the current class as primary query
     */
    public function useVentaResumidaLineaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinVentaResumidaLinea($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'VentaResumidaLinea', 'VentaResumidaLineaQuery');
    }

    /**
     * Filter the query by a related OrdenDevolucion object
     *
     * @param   OrdenDevolucion|PropelObjectCollection $ordenDevolucion  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByOrdenDevolucion($ordenDevolucion, $comparison = null)
    {
        if ($ordenDevolucion instanceof OrdenDevolucion) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $ordenDevolucion->getEmpresaId(), $comparison);
        } elseif ($ordenDevolucion instanceof PropelObjectCollection) {
            return $this
                ->useOrdenDevolucionQuery()
                ->filterByPrimaryKeys($ordenDevolucion->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByOrdenDevolucion() only accepts arguments of type OrdenDevolucion or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the OrdenDevolucion relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinOrdenDevolucion($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('OrdenDevolucion');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'OrdenDevolucion');
        }

        return $this;
    }

    /**
     * Use the OrdenDevolucion relation OrdenDevolucion object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   OrdenDevolucionQuery A secondary query class using the current class as primary query
     */
    public function useOrdenDevolucionQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinOrdenDevolucion($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'OrdenDevolucion', 'OrdenDevolucionQuery');
    }

    /**
     * Filter the query by a related EgresosRecibido object
     *
     * @param   EgresosRecibido|PropelObjectCollection $egresosRecibido  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByEgresosRecibido($egresosRecibido, $comparison = null)
    {
        if ($egresosRecibido instanceof EgresosRecibido) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $egresosRecibido->getEmpresaId(), $comparison);
        } elseif ($egresosRecibido instanceof PropelObjectCollection) {
            return $this
                ->useEgresosRecibidoQuery()
                ->filterByPrimaryKeys($egresosRecibido->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByEgresosRecibido() only accepts arguments of type EgresosRecibido or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the EgresosRecibido relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinEgresosRecibido($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('EgresosRecibido');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'EgresosRecibido');
        }

        return $this;
    }

    /**
     * Use the EgresosRecibido relation EgresosRecibido object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   EgresosRecibidoQuery A secondary query class using the current class as primary query
     */
    public function useEgresosRecibidoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinEgresosRecibido($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'EgresosRecibido', 'EgresosRecibidoQuery');
    }

    /**
     * Filter the query by a related GastoCaja object
     *
     * @param   GastoCaja|PropelObjectCollection $gastoCaja  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByGastoCaja($gastoCaja, $comparison = null)
    {
        if ($gastoCaja instanceof GastoCaja) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $gastoCaja->getEmpresaId(), $comparison);
        } elseif ($gastoCaja instanceof PropelObjectCollection) {
            return $this
                ->useGastoCajaQuery()
                ->filterByPrimaryKeys($gastoCaja->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByGastoCaja() only accepts arguments of type GastoCaja or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the GastoCaja relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinGastoCaja($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('GastoCaja');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'GastoCaja');
        }

        return $this;
    }

    /**
     * Use the GastoCaja relation GastoCaja object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   GastoCajaQuery A secondary query class using the current class as primary query
     */
    public function useGastoCajaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinGastoCaja($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'GastoCaja', 'GastoCajaQuery');
    }

    /**
     * Filter the query by a related Vendedor object
     *
     * @param   Vendedor|PropelObjectCollection $vendedor  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByVendedor($vendedor, $comparison = null)
    {
        if ($vendedor instanceof Vendedor) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $vendedor->getEmpresaId(), $comparison);
        } elseif ($vendedor instanceof PropelObjectCollection) {
            return $this
                ->useVendedorQuery()
                ->filterByPrimaryKeys($vendedor->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByVendedor() only accepts arguments of type Vendedor or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Vendedor relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinVendedor($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Vendedor');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Vendedor');
        }

        return $this;
    }

    /**
     * Use the Vendedor relation Vendedor object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   VendedorQuery A secondary query class using the current class as primary query
     */
    public function useVendedorQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinVendedor($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Vendedor', 'VendedorQuery');
    }

    /**
     * Filter the query by a related OrdenVendedor object
     *
     * @param   OrdenVendedor|PropelObjectCollection $ordenVendedor  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByOrdenVendedor($ordenVendedor, $comparison = null)
    {
        if ($ordenVendedor instanceof OrdenVendedor) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $ordenVendedor->getEmpresaId(), $comparison);
        } elseif ($ordenVendedor instanceof PropelObjectCollection) {
            return $this
                ->useOrdenVendedorQuery()
                ->filterByPrimaryKeys($ordenVendedor->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByOrdenVendedor() only accepts arguments of type OrdenVendedor or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the OrdenVendedor relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinOrdenVendedor($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('OrdenVendedor');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'OrdenVendedor');
        }

        return $this;
    }

    /**
     * Use the OrdenVendedor relation OrdenVendedor object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   OrdenVendedorQuery A secondary query class using the current class as primary query
     */
    public function useOrdenVendedorQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinOrdenVendedor($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'OrdenVendedor', 'OrdenVendedorQuery');
    }

    /**
     * Filter the query by a related SolicitudDevolucion object
     *
     * @param   SolicitudDevolucion|PropelObjectCollection $solicitudDevolucion  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterBySolicitudDevolucion($solicitudDevolucion, $comparison = null)
    {
        if ($solicitudDevolucion instanceof SolicitudDevolucion) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $solicitudDevolucion->getEmpresaId(), $comparison);
        } elseif ($solicitudDevolucion instanceof PropelObjectCollection) {
            return $this
                ->useSolicitudDevolucionQuery()
                ->filterByPrimaryKeys($solicitudDevolucion->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterBySolicitudDevolucion() only accepts arguments of type SolicitudDevolucion or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the SolicitudDevolucion relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinSolicitudDevolucion($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('SolicitudDevolucion');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'SolicitudDevolucion');
        }

        return $this;
    }

    /**
     * Use the SolicitudDevolucion relation SolicitudDevolucion object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   SolicitudDevolucionQuery A secondary query class using the current class as primary query
     */
    public function useSolicitudDevolucionQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinSolicitudDevolucion($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'SolicitudDevolucion', 'SolicitudDevolucionQuery');
    }

    /**
     * Filter the query by a related SolicitudDevMotivo object
     *
     * @param   SolicitudDevMotivo|PropelObjectCollection $solicitudDevMotivo  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterBySolicitudDevMotivo($solicitudDevMotivo, $comparison = null)
    {
        if ($solicitudDevMotivo instanceof SolicitudDevMotivo) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $solicitudDevMotivo->getEmpresaId(), $comparison);
        } elseif ($solicitudDevMotivo instanceof PropelObjectCollection) {
            return $this
                ->useSolicitudDevMotivoQuery()
                ->filterByPrimaryKeys($solicitudDevMotivo->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterBySolicitudDevMotivo() only accepts arguments of type SolicitudDevMotivo or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the SolicitudDevMotivo relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinSolicitudDevMotivo($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('SolicitudDevMotivo');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'SolicitudDevMotivo');
        }

        return $this;
    }

    /**
     * Use the SolicitudDevMotivo relation SolicitudDevMotivo object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   SolicitudDevMotivoQuery A secondary query class using the current class as primary query
     */
    public function useSolicitudDevMotivoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinSolicitudDevMotivo($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'SolicitudDevMotivo', 'SolicitudDevMotivoQuery');
    }

    /**
     * Filter the query by a related SolicitudCheque object
     *
     * @param   SolicitudCheque|PropelObjectCollection $solicitudCheque  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterBySolicitudCheque($solicitudCheque, $comparison = null)
    {
        if ($solicitudCheque instanceof SolicitudCheque) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $solicitudCheque->getEmpresaId(), $comparison);
        } elseif ($solicitudCheque instanceof PropelObjectCollection) {
            return $this
                ->useSolicitudChequeQuery()
                ->filterByPrimaryKeys($solicitudCheque->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterBySolicitudCheque() only accepts arguments of type SolicitudCheque or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the SolicitudCheque relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinSolicitudCheque($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('SolicitudCheque');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'SolicitudCheque');
        }

        return $this;
    }

    /**
     * Use the SolicitudCheque relation SolicitudCheque object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   SolicitudChequeQuery A secondary query class using the current class as primary query
     */
    public function useSolicitudChequeQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinSolicitudCheque($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'SolicitudCheque', 'SolicitudChequeQuery');
    }

    /**
     * Filter the query by a related LibroAgrupado object
     *
     * @param   LibroAgrupado|PropelObjectCollection $libroAgrupado  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByLibroAgrupado($libroAgrupado, $comparison = null)
    {
        if ($libroAgrupado instanceof LibroAgrupado) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $libroAgrupado->getEmpresaId(), $comparison);
        } elseif ($libroAgrupado instanceof PropelObjectCollection) {
            return $this
                ->useLibroAgrupadoQuery()
                ->filterByPrimaryKeys($libroAgrupado->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByLibroAgrupado() only accepts arguments of type LibroAgrupado or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the LibroAgrupado relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinLibroAgrupado($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('LibroAgrupado');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'LibroAgrupado');
        }

        return $this;
    }

    /**
     * Use the LibroAgrupado relation LibroAgrupado object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   LibroAgrupadoQuery A secondary query class using the current class as primary query
     */
    public function useLibroAgrupadoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinLibroAgrupado($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'LibroAgrupado', 'LibroAgrupadoQuery');
    }

    /**
     * Filter the query by a related LibroAgrupadoDetalle object
     *
     * @param   LibroAgrupadoDetalle|PropelObjectCollection $libroAgrupadoDetalle  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByLibroAgrupadoDetalle($libroAgrupadoDetalle, $comparison = null)
    {
        if ($libroAgrupadoDetalle instanceof LibroAgrupadoDetalle) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $libroAgrupadoDetalle->getEmpresaId(), $comparison);
        } elseif ($libroAgrupadoDetalle instanceof PropelObjectCollection) {
            return $this
                ->useLibroAgrupadoDetalleQuery()
                ->filterByPrimaryKeys($libroAgrupadoDetalle->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByLibroAgrupadoDetalle() only accepts arguments of type LibroAgrupadoDetalle or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the LibroAgrupadoDetalle relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinLibroAgrupadoDetalle($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('LibroAgrupadoDetalle');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'LibroAgrupadoDetalle');
        }

        return $this;
    }

    /**
     * Use the LibroAgrupadoDetalle relation LibroAgrupadoDetalle object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   LibroAgrupadoDetalleQuery A secondary query class using the current class as primary query
     */
    public function useLibroAgrupadoDetalleQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinLibroAgrupadoDetalle($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'LibroAgrupadoDetalle', 'LibroAgrupadoDetalleQuery');
    }

    /**
     * Filter the query by a related ListaActivos object
     *
     * @param   ListaActivos|PropelObjectCollection $listaActivos  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByListaActivos($listaActivos, $comparison = null)
    {
        if ($listaActivos instanceof ListaActivos) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $listaActivos->getEmpresaId(), $comparison);
        } elseif ($listaActivos instanceof PropelObjectCollection) {
            return $this
                ->useListaActivosQuery()
                ->filterByPrimaryKeys($listaActivos->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByListaActivos() only accepts arguments of type ListaActivos or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the ListaActivos relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinListaActivos($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('ListaActivos');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'ListaActivos');
        }

        return $this;
    }

    /**
     * Use the ListaActivos relation ListaActivos object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   ListaActivosQuery A secondary query class using the current class as primary query
     */
    public function useListaActivosQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinListaActivos($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'ListaActivos', 'ListaActivosQuery');
    }

    /**
     * Filter the query by a related SolicitudDeposito object
     *
     * @param   SolicitudDeposito|PropelObjectCollection $solicitudDeposito  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterBySolicitudDeposito($solicitudDeposito, $comparison = null)
    {
        if ($solicitudDeposito instanceof SolicitudDeposito) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $solicitudDeposito->getEmpresaId(), $comparison);
        } elseif ($solicitudDeposito instanceof PropelObjectCollection) {
            return $this
                ->useSolicitudDepositoQuery()
                ->filterByPrimaryKeys($solicitudDeposito->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterBySolicitudDeposito() only accepts arguments of type SolicitudDeposito or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the SolicitudDeposito relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinSolicitudDeposito($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('SolicitudDeposito');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'SolicitudDeposito');
        }

        return $this;
    }

    /**
     * Use the SolicitudDeposito relation SolicitudDeposito object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   SolicitudDepositoQuery A secondary query class using the current class as primary query
     */
    public function useSolicitudDepositoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinSolicitudDeposito($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'SolicitudDeposito', 'SolicitudDepositoQuery');
    }

    /**
     * Filter the query by a related Prestamo object
     *
     * @param   Prestamo|PropelObjectCollection $prestamo  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByPrestamo($prestamo, $comparison = null)
    {
        if ($prestamo instanceof Prestamo) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $prestamo->getEmpresaId(), $comparison);
        } elseif ($prestamo instanceof PropelObjectCollection) {
            return $this
                ->usePrestamoQuery()
                ->filterByPrimaryKeys($prestamo->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByPrestamo() only accepts arguments of type Prestamo or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Prestamo relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinPrestamo($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Prestamo');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Prestamo');
        }

        return $this;
    }

    /**
     * Use the Prestamo relation Prestamo object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   PrestamoQuery A secondary query class using the current class as primary query
     */
    public function usePrestamoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinPrestamo($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Prestamo', 'PrestamoQuery');
    }

    /**
     * Filter the query by a related VentasVendedor object
     *
     * @param   VentasVendedor|PropelObjectCollection $ventasVendedor  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByVentasVendedor($ventasVendedor, $comparison = null)
    {
        if ($ventasVendedor instanceof VentasVendedor) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $ventasVendedor->getEmpresaId(), $comparison);
        } elseif ($ventasVendedor instanceof PropelObjectCollection) {
            return $this
                ->useVentasVendedorQuery()
                ->filterByPrimaryKeys($ventasVendedor->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByVentasVendedor() only accepts arguments of type VentasVendedor or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the VentasVendedor relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinVentasVendedor($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('VentasVendedor');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'VentasVendedor');
        }

        return $this;
    }

    /**
     * Use the VentasVendedor relation VentasVendedor object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   VentasVendedorQuery A secondary query class using the current class as primary query
     */
    public function useVentasVendedorQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinVentasVendedor($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'VentasVendedor', 'VentasVendedorQuery');
    }

    /**
     * Filter the query by a related Agenda object
     *
     * @param   Agenda|PropelObjectCollection $agenda  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByAgenda($agenda, $comparison = null)
    {
        if ($agenda instanceof Agenda) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $agenda->getEmpresaId(), $comparison);
        } elseif ($agenda instanceof PropelObjectCollection) {
            return $this
                ->useAgendaQuery()
                ->filterByPrimaryKeys($agenda->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByAgenda() only accepts arguments of type Agenda or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Agenda relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinAgenda($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Agenda');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Agenda');
        }

        return $this;
    }

    /**
     * Use the Agenda relation Agenda object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   AgendaQuery A secondary query class using the current class as primary query
     */
    public function useAgendaQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinAgenda($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Agenda', 'AgendaQuery');
    }

    /**
     * Filter the query by a related Recetario object
     *
     * @param   Recetario|PropelObjectCollection $recetario  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByRecetario($recetario, $comparison = null)
    {
        if ($recetario instanceof Recetario) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $recetario->getEmpresaId(), $comparison);
        } elseif ($recetario instanceof PropelObjectCollection) {
            return $this
                ->useRecetarioQuery()
                ->filterByPrimaryKeys($recetario->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByRecetario() only accepts arguments of type Recetario or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the Recetario relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinRecetario($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('Recetario');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'Recetario');
        }

        return $this;
    }

    /**
     * Use the Recetario relation Recetario object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   RecetarioQuery A secondary query class using the current class as primary query
     */
    public function useRecetarioQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinRecetario($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'Recetario', 'RecetarioQuery');
    }

    /**
     * Filter the query by a related UsuarioReport object
     *
     * @param   UsuarioReport|PropelObjectCollection $usuarioReport  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByUsuarioReport($usuarioReport, $comparison = null)
    {
        if ($usuarioReport instanceof UsuarioReport) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $usuarioReport->getEmpresaId(), $comparison);
        } elseif ($usuarioReport instanceof PropelObjectCollection) {
            return $this
                ->useUsuarioReportQuery()
                ->filterByPrimaryKeys($usuarioReport->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByUsuarioReport() only accepts arguments of type UsuarioReport or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the UsuarioReport relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinUsuarioReport($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('UsuarioReport');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'UsuarioReport');
        }

        return $this;
    }

    /**
     * Use the UsuarioReport relation UsuarioReport object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   UsuarioReportQuery A secondary query class using the current class as primary query
     */
    public function useUsuarioReportQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinUsuarioReport($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'UsuarioReport', 'UsuarioReportQuery');
    }

    /**
     * Filter the query by a related InventarioVence object
     *
     * @param   InventarioVence|PropelObjectCollection $inventarioVence  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByInventarioVence($inventarioVence, $comparison = null)
    {
        if ($inventarioVence instanceof InventarioVence) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $inventarioVence->getEmpresaId(), $comparison);
        } elseif ($inventarioVence instanceof PropelObjectCollection) {
            return $this
                ->useInventarioVenceQuery()
                ->filterByPrimaryKeys($inventarioVence->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByInventarioVence() only accepts arguments of type InventarioVence or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the InventarioVence relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinInventarioVence($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('InventarioVence');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'InventarioVence');
        }

        return $this;
    }

    /**
     * Use the InventarioVence relation InventarioVence object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   InventarioVenceQuery A secondary query class using the current class as primary query
     */
    public function useInventarioVenceQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinInventarioVence($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'InventarioVence', 'InventarioVenceQuery');
    }

    /**
     * Filter the query by a related TrasladoProducto object
     *
     * @param   TrasladoProducto|PropelObjectCollection $trasladoProducto  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByTrasladoProducto($trasladoProducto, $comparison = null)
    {
        if ($trasladoProducto instanceof TrasladoProducto) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $trasladoProducto->getEmpresaId(), $comparison);
        } elseif ($trasladoProducto instanceof PropelObjectCollection) {
            return $this
                ->useTrasladoProductoQuery()
                ->filterByPrimaryKeys($trasladoProducto->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByTrasladoProducto() only accepts arguments of type TrasladoProducto or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the TrasladoProducto relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinTrasladoProducto($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('TrasladoProducto');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'TrasladoProducto');
        }

        return $this;
    }

    /**
     * Use the TrasladoProducto relation TrasladoProducto object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   TrasladoProductoQuery A secondary query class using the current class as primary query
     */
    public function useTrasladoProductoQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinTrasladoProducto($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'TrasladoProducto', 'TrasladoProductoQuery');
    }

    /**
     * Filter the query by a related TrasladoUbicacion object
     *
     * @param   TrasladoUbicacion|PropelObjectCollection $trasladoUbicacion  the related object to use as filter
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return   EmpresaQuery The current query, for fluid interface
     * @throws   PropelException - if the provided filter is invalid.
     */
    public function filterByTrasladoUbicacion($trasladoUbicacion, $comparison = null)
    {
        if ($trasladoUbicacion instanceof TrasladoUbicacion) {
            return $this
                ->addUsingAlias(EmpresaPeer::ID, $trasladoUbicacion->getEmpresaId(), $comparison);
        } elseif ($trasladoUbicacion instanceof PropelObjectCollection) {
            return $this
                ->useTrasladoUbicacionQuery()
                ->filterByPrimaryKeys($trasladoUbicacion->getPrimaryKeys())
                ->endUse();
        } else {
            throw new PropelException('filterByTrasladoUbicacion() only accepts arguments of type TrasladoUbicacion or PropelCollection');
        }
    }

    /**
     * Adds a JOIN clause to the query using the TrasladoUbicacion relation
     *
     * @param     string $relationAlias optional alias for the relation
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function joinTrasladoUbicacion($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        $tableMap = $this->getTableMap();
        $relationMap = $tableMap->getRelation('TrasladoUbicacion');

        // create a ModelJoin object for this join
        $join = new ModelJoin();
        $join->setJoinType($joinType);
        $join->setRelationMap($relationMap, $this->useAliasInSQL ? $this->getModelAlias() : null, $relationAlias);
        if ($previousJoin = $this->getPreviousJoin()) {
            $join->setPreviousJoin($previousJoin);
        }

        // add the ModelJoin to the current object
        if ($relationAlias) {
            $this->addAlias($relationAlias, $relationMap->getRightTable()->getName());
            $this->addJoinObject($join, $relationAlias);
        } else {
            $this->addJoinObject($join, 'TrasladoUbicacion');
        }

        return $this;
    }

    /**
     * Use the TrasladoUbicacion relation TrasladoUbicacion object
     *
     * @see       useQuery()
     *
     * @param     string $relationAlias optional alias for the relation,
     *                                   to be used as main alias in the secondary query
     * @param     string $joinType Accepted values are null, 'left join', 'right join', 'inner join'
     *
     * @return   TrasladoUbicacionQuery A secondary query class using the current class as primary query
     */
    public function useTrasladoUbicacionQuery($relationAlias = null, $joinType = Criteria::LEFT_JOIN)
    {
        return $this
            ->joinTrasladoUbicacion($relationAlias, $joinType)
            ->useQuery($relationAlias ? $relationAlias : 'TrasladoUbicacion', 'TrasladoUbicacionQuery');
    }

    /**
     * Exclude object from result
     *
     * @param   Empresa $empresa Object to remove from the list of results
     *
     * @return EmpresaQuery The current query, for fluid interface
     */
    public function prune($empresa = null)
    {
        if ($empresa) {
            $this->addUsingAlias(EmpresaPeer::ID, $empresa->getId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}
