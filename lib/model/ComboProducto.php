<?php



/**
 * Skeleton subclass for representing a row from the 'combo_producto' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.6.7 on:
 *
 * 01/11/22 01:11:39
 *
 * You should add additional methods to this class to meet the
 * application requirements.  This class will only be generated as
 * long as it does not already exist in the output directory.
 *
 * @package    propel.generator.lib.model
 */
class ComboProducto extends BaseComboProducto
{
    

         public function save(PropelPDO $con = null) {
        $empresaId = UsuarioQuery::getEmpresaSeleccionada('ComboProducto');
       
        if ($this->isNew()) {
             if ($empresaId) {
            $this->setEmpresaId($empresaId);
        }
                      if (trim($this->getCodigoSku()) == ""){
            $tipo = $empresaId.'CP';
            $pre = $empresaId.'CP';
            $numero = 1;
            $busca = CorrelativoCodigoQuery::create()
                    ->filterByTipo($tipo)
                    ->findOne();
            if (!$busca) {
                $busca = New CorrelativoCodigo();
                $busca->setTipo($tipo);
                $busca->setPrefijo($pre);
                $busca->setNumeroAsginar($numero);
                $busca->save();
            }
            $numero = $busca->getNumeroAsginar();
            $numero = $busca->getNumeroAsginar();
            $prefijo=$pre.$numero;
            if (strlen($numero)==1){
                $prefijo=$pre.'0'.$numero;
            }
            if ($numero >99){
                $prefijo=$pre.$numero;
            }   
            $this->setCodigoSku($prefijo);
            $busca->setNumeroAsginar($numero + 1);
            $busca->save();
                      }
        }
//         $triger = sfContext::getInstance()->getUser()->getAttribute('activa',null, 'triger');
//         if ($triger <> 3) {
//          $this->setStatus(0);
//         }
        parent::save($con);
    }
    
    
    
}
